---
phase: quick
plan: 260625-h1g
type: execute
wave: 1
depends_on: []
files_modified:
  - src/etl/fukusho_label.py
  - tests/test_fukusho_label.py
autonomous: true
requirements: [LABEL-RACE-DATE-FAIL-LOUD]
must_haves:
  truths:
    - "compute_fukusho_labels は race_date が伝播しない（race_df 空 / race_date 列なし / merge 結果 NULL）場合に RuntimeError を raise し、診断情報（race_df 行数・race_date 列有無・non-NULL 数・normalized.n_race 側状態）を logger.error で出力する"
    - "正常ケース（race_date が race_df に存在し全行 non-NULL で伝播）は挙動不变・追加ログ出力なし・出力 checksum 同一"
    - "run_label_etl は staging-swap 後の label.fukusho_label で race_date IS NULL が 0 件であることを post-condition で検証し、>0 なら RuntimeError で fail-loud（compute_fukusho_labels を抜けた場合の二重防波堤）"
    - "race_date 伝播失敗ケース（race_df 空 / race_date 列なし）で RuntimeError が raise される回帰テストが存在する"
    - "既存テスト test_compute_fukusho_labels_propagates_race_date / test_race_meta_select_columns_includes_race_date は GREEN を維持"
  artifacts:
    - path: "src/etl/fukusho_label.py"
      provides: "compute_fukusho_labels の race_date fail-loud + 診断ログ / run_label_etl の post-condition 二重防波堤"
      contains: "race_date"
    - path: "tests/test_fukusho_label.py"
      provides: "race_date 伝播失敗ケースの回帰テスト（RuntimeError 検査）"
      contains: "RuntimeError"
  key_links:
    - from: "src/etl/fukusho_label.py::compute_fukusho_labels"
      to: "logger.error / RuntimeError"
      via: "merge 後 race_date NULL 検出 → 診断ログ → raise"
      pattern: "race_date.*isna|raise RuntimeError.*race_date"
    - from: "src/etl/fukusho_label.py::run_label_etl"
      to: "label.fukusho_label SELECT count(*) WHERE race_date IS NULL"
      via: "staging-swap 後の post-condition・compute_fukusho_labels を抜けた場合の最終検知"
      pattern: "race_date IS NULL"
---

<objective>
label.fukusho_label.race_date 全行 NULL 再発（2026-06-23/06-24 の2回・現在は backfill で復元済）の「原因特定可能化」fix を実装する。

過去の原因は回復した DB から確定不能（ユーザー合意：推測で直すのでなく、再発時に原因を記録して特定する仕組みを作る）。構造的欠陥は src/etl/fukusho_label.py:646-647 の fallback `if "race_date" not in merged.columns: merged["race_date"] = pd.NA` が「race_date が伝播しないと黙って全行 NULL」にする点にある。本 plan は再発した瞬間に止まり、なぜ race_date が抜けたか（race_df 空 / race_date 列なし / キー不整合）がログから分かる仕組みを作る。根本原因は再発時の診断ログで特定する。

Purpose: Phase 2/5 の label.fukusho_label.race_date 全行 NULL の silent corruption を構造的に防止し、再発時に原因診断を可能にする。聖域（raw 不変・再現性・通常ケース挙動不变）を一切侵さず、異常時のみ発火する。
Output: src/etl/fukusho_label.py（fail-loud + 診断ログ + 二重防波堤）・tests/test_fukusho_label.py（回帰テスト）
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/debug/fukusho-recovery-070.md
@src/etl/fukusho_label.py
@tests/test_fukusho_label.py
</context>

<scope_boundary>
【聖域・絶対厳守】
- raw 不変（D-06）: raw テーブルに一切書込まない。本 plan は src/etl/fukusho_label.py の読み取り/変換/label スキーマ書込のみ。
- 再現性（§19.1）: staging-swap / idempotent ロジック（_idempotent_load_label）の構造を壊さない。2回連続実行で同一 checksum。
- 通常ケース（race_date 正常伝播・全行 non-NULL）の挙動を変えない。fail-loud・診断ログは異常時のみ発火。正常ケースの出力列・行数・checksum は一切変わらない（検証で同一 checksum を assert）。
- 変更対象は src/etl/fukusho_label.py と tests/test_fukusho_label.py のみ。
- **既存の未コミット変更（scripts/run_backtest.py, src/db/prediction_load.py）には一切触らない・巻き込まない。** git add / commit 時にこれらのファイルを staging に入れないこと（`git add src/etl/fukusho_label.py tests/test_fukusho_label.py` のみ明示的に指定）。
- コミットメッセージ・コメント・docstring は全て日本語（CLAUDE.md 最優先）。
</scope_boundary>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: compute_fukusho_labels の race_date fail-loud + 診断ログ化</name>
  <files>src/etl/fukusho_label.py, tests/test_fukusho_label.py</files>
  <behavior>
    - 異常ケース1 (race_df 空): race_df が0行の場合、compute_fukusho_labels は RuntimeError を raise すること（race_date 列が merged に伝播しないことを検知）。
    - 異常ケース2 (race_date 列なし): race_df に race_date 列が無い場合、compute_fukusho_labels は RuntimeError を raise すること。
    - 異常ケース3 (merge 結果 race_date に NULL あり): race_df に race_date 列はあるが merge 結果（race_key 型不整合等）で NULL が1行でも発生した場合、RuntimeError を raise すること。
    - 正常ケース: race_df に race_date が存在し全行 non-NULL で伝播する場合、RuntimeError を raise せず・logger.error も出力せず・出力 checksum が従来と同一であること。
    - 診断ログ: RuntimeError 発火時に logger.error で race_df の状態（行数・race_date 列有無・race_date non-NULL 数）と merged 側の状態（行数・race_date 列有無・race_date NULL 数）を出力すること。
  </behavior>
  <action>
    src/etl/fukusho_label.py の compute_fukusho_labels を修正する。変更箇所は行631-647 の race_df merge と race_date fallback 周辺。

    実装内容（行645 の `merged = merged.merge(race_merge, on=_RACE_KEY, how="left")` の直後・既存の `if "race_date" not in merged.columns: merged["race_date"] = pd.NA` を置換）:

    (1) merge 後 race_date の状態を検査する fail-loud ブロックを追加。race_df が空（len(race_df)==0）・race_df に race_date 列が無い・merge 結果の merged に race_date 列が無い・merge 結果の merged["race_date"] に NULL が1行でもある、のいずれかの場合に logger.error で診断情報を出力して RuntimeError を raise する。

    (2) 診断情報の内容: race_df 側（行数 len(race_df)・race_date 列の有無 `("race_date" in race_df.columns)`・race_df["race_date"] の non-NULL 数（race_date 列がある場合））・merged 側（行数 len(merged)・race_date 列の有無・merged["race_date"] の NULL 数・non-NULL 数（race_date 列がある場合））。これらを1つの logger.error 呼出で dict/f-string 形式で詳細に出力する（再発時の原因特定を可能にするため・情報は多すぎる程度で十分）。

    (3) RuntimeError のメッセージは「race_date 伝播失敗・診断ログを確認」を明示する日本語メッセージにする。

    (4) **通常ケース（race_df に race_date 列があり merge 結果全行 non-NULL）は挙動不变**: 既存の `if "race_date" not in merged.columns: merged["race_date"] = pd.NA` fallback 行は削除し、fail-loud ブロックに置換する。正常ケースでは logger.error も RuntimeError も発火せず・以降の処理（_canonicalize_markers 以降）は一切変更しない。これにより出力 checksum は不変。

    同時に tests/test_fukusho_label.py に以下の回帰テストを追加する（test_compute_fukusho_labels_propagates_race_date の直後・regression セクション内）:

    (a) test_compute_fukusho_labels_raises_on_empty_race_df: _build_label_input_df(8) で race_df を空 DataFrame（columns のみ・行0）に変更し compute_fukusho_labels を呼出・RuntimeError が raise されることを pytest.raises で検証。race_df に race_date 列は含めない（空 DataFrame 相当）。

    (b) test_compute_fukusho_labels_raises_on_missing_race_date_column: _build_label_input_df(8) の race_df は1行あるが race_date 列を持たない（デフォルトの _build_label_input_df は race_date 列を含まない）状態で compute_fukusho_labels を呼出・RuntimeError が raise されることを検証。※注意: このテストは race_df を copy せずそのまま渡す（race_date 列を付与しない）。既存 test_compute_fukusho_labels_propagates_race_date は race_df に race_date を付与してから呼ぶため GREEN を維持。

    (c) test_compute_fukusho_labels_normal_case_no_diagnostic_log: 既存 test_compute_fukusho_labels_propagates_race_date と同じ入力（race_df に race_date 付与）で compute_fukusho_labels を呼出・RuntimeError が raise されないこと・出力の race_date が全行 non-NULL であることを検証（正常ケースの挙動不变保証）。logger.error が呼ばれないことは RuntimeError が raise されないことで間接保証する（caplog は使わず・正常パスは追加ログを出さない設計のため）。

    ※「1行でも NULL で raise」設計は既存テスト test_compute_fukusho_labels_propagates_race_date（race_df に valid race_date を付与）と整合する・同テストは race_date 全行 non-NULL のため fail-loud に引っかからず GREEN を維持。
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v3 && KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/test_fukusho_label.py -x -q</automated>
  </verify>
  <done>
    - compute_fukusho_labels が race_date 伝播失敗（race_df 空 / race_date 列なし / NULL 含む）で RuntimeError を raise し診断ログを出力する。
    - 正常ケース（race_date 全行 non-NULL）は RuntimeError 未発生・挙動不变。
    - tests/test_fukusho_label.py の新規3テスト（empty_race_df / missing_race_date_column / normal_case_no_diagnostic_log）が GREEN。
    - 既存テスト test_compute_fukusho_labels_propagates_race_date / test_race_meta_select_columns_includes_race_date が GREEN を維持。
    - test_fukusho_label.py 全体が GREEN（KEIBA_SKIP_DB_TESTS=1 で DB 不要テストのみ実行）。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: run_label_etl の post-condition 二重防波堤（staging-swap 後 race_date NULL 検知）</name>
  <files>src/etl/fukusho_label.py, tests/test_fukusho_label.py</files>
  <behavior>
    - 正常ケース: run_label_etl は label.fukusho_label の race_date IS NULL が 0 件の場合、従来通り rows_inserted/checksum/raw_touched を返す（post-condition は pass・何もしない）。
    - 異常ケース: 万が一 staging-swap 後の label.fukusho_label で race_date IS NULL > 0 となった場合、RuntimeError を raise して fail-loud する（compute_fukusho_labels を抜けた場合の最終検知）。
    - 診断情報: RuntimeError 発火時に総行数・NULL 行数を logger.error で出力する。
  </behavior>
  <action>
    src/etl/fukusho_label.py の run_label_etl を修正する。変更箇所は行1145-1161 の write_pool ブロック内・_idempotent_load_label 呼出の後・checksum 計算の前（または checksum 計算の直後・conn.commit の前）。

    実装内容:

    (1) _idempotent_load_label の戻り値（rows_inserted）取得後、同じ write_cursor（wcur）で `SELECT count(*) FROM label.fukusho_label WHERE race_date IS NULL` を実行し null_count を取得する。

    (2) null_count > 0 の場合、logger.error で「label.fukusho_label post-condition violation: race_date NULL {null_count} / {rows_inserted} rows（compute_fukusho_labels を抜けたが最終表で NULL 残存・二重防波堤）」を出力し RuntimeError を raise する。トランザクションは commit されず（RuntimeError で write_pool.connection() context を抜けることで rollback される・既存の conn.commit() に到達しない）。

    (3) null_count == 0 の場合は何もせず（既存の checksum 計算・conn.commit() へ進む）・挙動不变。正常ケースの出力（rows_inserted/checksum/raw_touched）は一切変わらない。

    (4) この post-condition は _idempotent_load_label の staging-swap（DROP+RENAME）完了後の実表に対する検査のため・compute_fukusho_labels の fail-loud を抜けた場合（race_df の状態は正常だが INSERT/staging-swap のどこかで race_date が欠損した等の異常）の最終防波堤として機能する。

    ※select は write_cursor（etl ロール）で実行する。label.fukusho_label には etl ロールが全権を持つ（HIGH #6）+ reader GRANT 済み（HIGH #3）のため SELECT 可能。

    テストは tests/test_fukusho_label.py に追加しない（run_label_etl は DB 接続を要し unit test では検証困難・live-DB 検証は Task 3 の verify で実施）。ただし post-condition の SQL 文が存在することを AST/grep ベースの構造検査テストを1つ追加してもよい（任意・src 文字列検査で `race_date IS NULL` と `raise RuntimeError` の存在を確認）。

    推奨: tests/test_fukusho_label.py に test_run_label_etl_has_race_date_post_condition を追加し・inspect.getsource(run_label_etl) で得たソース文字列に "race_date IS NULL" と "RuntimeError" の両方が含まれることを assert する（実DB不要の構造保証テスト・select_se_state WR-10 guard テストと同パターン）。
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v3 && KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/test_fukusho_label.py -x -q</automated>
  </verify>
  <done>
    - run_label_etl に staging-swap 後の label.fukusho_label race_date IS NULL post-condition 検査が追加されている。
    - null_count > 0 で RuntimeError + logger.error で fail-loud する。
    - null_count == 0 は挙動不变（rows_inserted/checksum/raw_touched は変更なし）。
    - test_run_label_etl_has_race_date_post_condition（構造検査テスト）が GREEN。
    - test_fukusho_label.py 全体が GREEN。
  </done>
</task>

<task type="auto">
  <name>Task 3: live-DB label ETL 再実行で再現性・race_date 全行 non-NULL・idempotent を検証</name>
  <files></files>
  <action>
    この task はコード変更なし・live-DB 検証のみ（メモリ [run-authorized-ops-directly]: 許可済み操作は自分で実行）。

    (1) live-DB で label ETL を実行: `cd /Users/hart/develop/keiba-ai-v3 && uv run python scripts/run_label_etl.py` を実行する。同 script は run_label_etl を2回連続実行し idempotent verify（同一 checksum）を行う設計。

    (2) 出力の確認:
      - 2回連続実行の checksum が一致すること（idempotent・§19.1 再現性）。
      - raw_touched が False であること（D-06 raw 不変）。
      - rows_inserted が 554267（既知の全 JRA ラベル行数）前後であること。
      - race_date の post-condition が発火せず（Task 2 の RuntimeError 未発生）・正常完了すること。

    (3) 追加の DB 直接検証（psql または psycopg 経由・readonly）:
      `SELECT count(*) FROM label.fukusho_label WHERE race_date IS NULL` が 0 であること。
      `SELECT count(*), count(race_date) FROM label.fukusho_label` で総行数と non-NULL 数が一致すること。

    (4) もし再実行で race_date に NULL が発生した場合（Task 1/2 の fail-loud が発火した場合）は・RuntimeError の診断ログから原因（race_df 空 / race_date 列なし / キー不整合）を特定し・ユーザーに報告する（本 plan の目的は「再発時に原因を記録して特定する仕組み」のため・発火したら成功）。

    (5) raw 不変性の最終確認: scripts/run_label_etl.py が raw_touched=False を返すことを assert する（同 script は raw fingerprint 比較を内蔵している場合それを確認・無い場合は `SELECT md5(string_agg(...)) FROM public.n_harai` 等で前後比較・ただし本 task は ETL 実行前後で raw に書込まない設計のため差分は発生しないはず）。
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v3 && uv run python scripts/run_label_etl.py 2>&1 | tail -30</automated>
  </verify>
  <done>
    - live-DB で label ETL が2回連続実行で同一 checksum（idempotent PASS）。
    - raw_touched=False（D-06 raw 不変）。
    - label.fukusho_label の race_date IS NULL が 0 件（全行 non-NULL・再現性）。
    - post-condition（Task 2）が発火せず正常完了（正常ケース挙動不变）。
    - もし fail-loud が発火した場合は診断ログから原因が特定できていること。
  </done>
</task>

</tasks>

<verification>
1. KEIBA_SKIP_DB_TESTS=1 で tests/test_fukusho_label.py 全テスト GREEN（DB 不要 unit test のみ）。
2. uv run pytest tests/test_fukusho_label.py（KEIBA_SKIP_DB_TESTS unset・DB 必須テスト含む）で label 関連テスト全体 GREEN。
3. uv run python scripts/run_label_etl.py が2回連続同一 checksum で idempotent PASS・raw_touched=False・race_date 全行 non-NULL。
4. 既存の未コミット変更（scripts/run_backtest.py, src/db/prediction_load.py）が本 plan の commit に巻き込まれていないこと（git diff --name-only HEAD~1 で src/etl/fukusho_label.py tests/test_fukusho_label.py のみであることを確認）。
5. ruff check src/etl/fukusho_label.py tests/test_fukusho_label.py が GREEN。
</verification>

<success_criteria>
- compute_fukusho_labels が race_date 伝播失敗で RuntimeError + 診断ログ（race_df/merged 状態）で fail-loud する。
- run_label_etl が staging-swap 後 race_date NULL で RuntimeError で fail-loud する（二重防波堤）。
- 正常ケースは挙動不变・追加ログなし・checksum 同一・raw 不変。
- 回帰テスト（空 race_df / race_date 列欠損 / 正常ケース / post-condition 構造検査）が GREEN。
- 既存テスト（propagates_race_date / select_columns_includes_race_date）が GREEN を維持。
- live-DB 再実行で idempotent PASS・race_date 全行 non-NULL。
- コミットが src/etl/fukusho_label.py と tests/test_fukusho_label.py のみ（未コミット変更を巻き込まない）。
</success_criteria>

<output>
Create `.planning/quick/260625-h1g-label-race-date-null-fail-loud-diagnosti/260625-h1g-SUMMARY.md` when done
</output>
