---
phase: 05-ev-backtest
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/ev/odds_snapshot.py
  - src/ev/refund_accounting.py
  - tests/ev/test_odds_snapshot.py
  - tests/ev/test_refund_accounting.py
autonomous: true
requirements: [BACK-03, BACK-04]
user_setup: []

must_haves:
  truths:
    - "BACK-04: odds_snapshot_policy は 30min_before/10min_before 固定・merge_asof(direction='backward', by=['race_key','umaban']) で未来リーク構造的不可"
    - "HIGH-1 馬単位オッズ: select_odds_snapshot は merge_asof の by= に race_key だけでなく umaban を含め・各馬に固有の odds を割り当てる（JODDS PK は race_key 7カラム+Umaban の8カラム・RESEARCH §1.1）"
    - "BACK-04 canonical rule: 特殊値 ----/****/0000/0999 と snapshot 0件は全て no_bet sentinel（silent fallback 禁止・§11.3）。CONTEXT D-02 を正とし・RESEARCH 行89の「0999=99.9倍以上」は廃棄：0999 は odds 欠損 sentinel（数百倍以上の代替表現）と解釈し no_bet 扱いとする（HIGH-3 三方向矛盾の正準決定）"
    - "cross-plan contract: select_odds_snapshot 戻り値は snake_case の fuku_odds_lower/fuku_odds_upper 列（JODDS raw FukuOddsLow/FukuOddsHigh を rename）・Plan 02 ev_rank.py と Plan 05 run_backtest.py が JOIN するだけで column 再名不要"
    - "BACK-03: 返還会計決定表（6シナリオ）: 取消/除外/不成立/レース中止=effective_stake=0(返還)・競走中止=effective_stake=100(loss・§10.6 除外禁止)"
    - "BACK-03: label.fukusho_label フラグが一次ソース・public.n_harai（FuseirituFlag2/PayFukusyoPay）は cross-check"
    - "BACK-03: PayFukusyoPay は表示オッズでなく実際払戻金（§11.6）・同着 slot 2-5 を lookup"
    - "FukusuoFlag 正例: '7'(発売あり) は選択対象・'0'/'1'/'3'(発売なし/取消) は no_bet（MEDIUM・EveryDB2 マニュアル値に基づく正常発売 positive test）"
  artifacts:
    - path: "src/ev/odds_snapshot.py"
      provides: "select_odds_snapshot(jodds_df, race_times, policy) + JODDS/HARAI 取得クエリ"
      contains: "def select_odds_snapshot"
    - path: "src/ev/refund_accounting.py"
      provides: "determine_stake_payout(row) + _lookup_payfukusyo_pay(row)"
      contains: "def determine_stake_payout"
  key_links:
    - from: "src/ev/odds_snapshot.py::select_odds_snapshot"
      to: "pandas.merge_asof(direction='backward', by=['race_key','umaban'])"
      via: "race_key + umaban 単位 backward 最近接・各馬に固有 odds を割当・未来 snapshot 構造的不可（D-02 + HIGH-1）"
      pattern: "by=\\['race_key', 'umaban'\\]"
    - from: "src/ev/refund_accounting.py::determine_stake_payout"
      to: "label.fukusho_label フラグ + public.n_harai PayFukusyoPay"
      via: "label フラグ一次・HARAI PayFukusyoPay で payout 確定"
      pattern: "is_dead_loss"
---

<objective>
JODDS 固定時点オッズ選択（D-01/D-02・BACK-04）と返還/中止 honest 会計決定表（D-05・BACK-03）を実装し、対応 RED stub（Plan 01 Task 2）を GREEN 化する。
Purpose: BACK-03/BACK-04 の構造的ブロック（後知恵オッズ選択拒否・返還 honest 会計）を確立・§11.2/§11.3/§10.6/§11.6 を履行。
Output: src/ev/odds_snapshot.py + src/ev/refund_accounting.py。
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ev-backtest/05-CONTEXT.md
@.planning/phases/05-ev-backtest/05-RESEARCH.md
@.planning/phases/05-ev-backtest/05-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: JODDS 固定時点オッズ選択（odds_snapshot.py）— RED→GREEN</name>
  <files>src/ev/odds_snapshot.py, tests/ev/test_odds_snapshot.py</files>
  <read_first>
    - src/model/baseline.py::fetch_market_data（行462-521・readonly_cur 明示 + PK 6カラム CAST(int) JOIN + where_clauses/params parameterized query + pd.DataFrame(rows, columns=cols)）
    - src/utils/pit_join.py（merge_asof 既存利用箇所・sort 前提 guard）
    - .planning/phases/05-ev-backtest/05-RESEARCH.md §1.1-§1.4（JODDS 実DBスキーマ・HappyoTime mmddHHMM / HassoTime hhmm・DataKubun は head テーブルのみ・Pitfall 1 日跨ぎ・Pitfall 2 DataKubun JOIN 必須・特殊値 no_bet）
    - .planning/phases/05-ev-backtest/05-PATTERNS.md「src/ev/odds_snapshot.py」節 + 共有パターン3（merge_asof backward）/ 5（parameterized SQL）
    - tests/ev/test_odds_snapshot.py（Plan 01 RED stub・5 テスト）
    - docs/everydb2/47-JODDS_TANPUKU.md / 46-JODDS_TANPUKUWAKU_HEAD.md / 03-RACE.md
    - CLAUDE.md §13（merge_asof direction='backward'）+ §11.2/§11.3
  </read_first>
  <behavior>
    - test_odds_snapshot_backward: 発走10:00・policy='30min_before'・HappyoTime 09:25/09:31/09:35 → 09:25 を選択（≤09:30 の最大・cutoff_datetime 計算）
    - test_odds_snapshot_no_bet_empty: snapshot 0件 → no_bet sentinel
    - test_odds_snapshot_special_values: FukuOddsLow が ----/****/0000/**（sp=登録なし含む） → no_bet（silent fallback 禁止）
    - test_odds_snapshot_0999_is_no_bet: FukuOddsLow='0999' → no_bet sentinel・fuku_odds_lower/fuku_odds_upper は NaN（HIGH-3 canonical rule: CONTEXT D-02 を正とし 0999 は odds 欠損 sentinel 扱い・RESEARCH 行89 の「99.9倍以上」は廃棄）
    - test_odds_snapshot_future_leak: cutoff より未来の HappyoTime が選択されない（merge_asof backward 原則）
    - test_odds_snapshot_day_boundary: 深夜発走（HassoTime='0030'）の日跨ぎで race_start_datetime 基準で正しい snapshot を選択（HHMM 整数比較でなく Timedelta 計算）
    - test_odds_snapshot_datakubun_filter: DataKubun='1'(中間) のみ使用・'3'最終/'4'確定 は除外（D-01・head テーブル JOIN で取得）
    - test_odds_snapshot_returns_snake_case: select_odds_snapshot 戻り値 DataFrame の columns に 'fuku_odds_lower'/'fuku_odds_upper'（snake_case）が含まれ・'FukuOddsLow'/'FukuOddsHigh'（raw）は含まれない（cross-plan contract・Plan 02 ev_rank.py と一致）
    - test_odds_snapshot_multi_horse: 同一 race_key で umaban={1,2,3} の3頭・各馬の cutoff 前に異なる HappyoTime の snapshot（例 umaban1: 09:25 @ 3.0/4.0, umaban2: 09:25 @ 5.0/6.0, umaban3: 09:25 @ 2.0/2.5）を与える → 戻り値が 3行（umaban 1/2/3 各1行）で・各馬の odds が他馬の odds で上書きされず保持される（HIGH-1 + MEDIUM: JODDS PK は race_key 7カラム + Umaban の8カラム・by=['race_key','umaban'] の as-of join 必須）
    - test_odds_snapshot_fukusyoflag_normal_sale: FukusuoFlag='7'(発売あり・EveryDB2 マニュアル正例) は odds を返す（MEDIUM: 正常発売 positive test）・FukusuoFlag='0'/'1'/'3' は no_bet
    - **test_odds_snapshot_multi_race（MEDIUM-C cycle-2）: 複数レース（例 R1: race_key=r1・umaban {1,2,3}・R2: race_key=r2・umaban {1,2}）で各馬の happyo_datetime がレース間で重複（例 R1-umaban1 と R2-umaban1 の両者が 09:25 に snapshot）する合成データを与える・select_odds_snapshot を呼出し merge_asof が sort 違反でエラーを出さず正しく各馬に per-horse snapshot を返すことを assert（by=['race_key','umaban'] と sort_values(['race_key','umaban','happyo_datetime']) が race_key 優先 sort で happyo_datetime の大域ソートが崩れても merge_asof が by= グループ内で正しく動作すること・pandas merge_asof by= セマンティクス検証）。戻り値が 5行（R1×3 + R2×2）で各馬の odds が正しく取得されることを assert**
  </behavior>
  <action>
    src/ev/odds_snapshot.py:
    - fetch_jodds(readonly_cur, *, year=None) -> pd.DataFrame: public.n_jodds_tanpuku j JOIN public.n_jodds_tanpukuwaku_head h ON PK 7カラム（Year,MonthDay,JyoCD,Kaiji,Nichiji,RaceNum,HappyoTime）・WHERE h.datakubun='1'（中間・D-01・Pitfall 2 head JOIN 必須）。parameterized query（%s placeholder・共有パターン5）。戻り値 DataFrame は race_key 7カラム + Umaban 列 + HappyoTime + happyo_datetime（Year+MonthDay+HappyoTime 下4桁から構築・race_start_datetime と比較可能な Timestamp）+ FukuOddsLow/FukuOddsHigh（raw）+ FukusyoFlag/DataKubun（head 由来）。**JODDS PK は race_key 7カラム + Umaban の8カラム（RESEARCH §1.1 実証）のため必ず Umaban 列を保持し・per-horse snapshot であることを担保する。**
    - select_odds_snapshot(jodds_df, race_times, policy) -> df: policy は '30min_before'/'10min_before'。minutes = 30 or 10。race_times['cutoff_datetime'] = race_times['race_start_datetime'] - pd.Timedelta(minutes=minutes)。
      - **HIGH-1 fix: merge_asof の by= を race_key 単独でなく ['race_key','umaban']（馬番グループ化）にする。** race_times 側も umaban を持つ（予測対象馬単位の cutoff 行）必要があるため・race_times が馬単位（race_key + umaban）であることを前提とする（Plan 05 は予測馬単位で race_times を構築して渡す）。
      - jodds_sorted = jodds_df.sort_values(['race_key','umaban','happyo_datetime'])
      - cutoff_sorted = race_times.sort_values(['race_key','umaban','cutoff_datetime'])
      - result = pd.merge_asof(cutoff_sorted, jodds_sorted, left_on='cutoff_datetime', right_on='happyo_datetime', by=['race_key','umaban'], direction='backward')（HIGH-1・未来リーク構造的不可 + per-horse odds 保証）
      - HHMM 整数比較は使わない（Pitfall 1 日跨ぎ・race_start_datetime + Timedelta で計算）
    - 特殊値処理（HIGH-3 canonical rule・CONTEXT D-02 正）: FukuOddsLow が '----'(発売前取消) / '****'(発売後取消) / '0000'(無投票) / '0999'(odds 欠損 sentinel・CONTEXT D-02 により no_bet と正準決定・RESEARCH 行89 の「99.9倍以上」記述は本モジュールで廃棄) / ' '(sp=登録なし) → odds_missing_reason='no_bet' sentinel・FukuOddsLow/FukuOddsHigh を NaN 化。snapshot 0件（merge_asof 結果 NaN）→ odds_missing_reason='no_bet_empty'。silent fallback 禁止（§11.3）。
    - FukusyoFlag が '0'(発売なし)/'1'(発売前取消)/'3'(発売後取消) も no_bet。FukusyoFlag='7'(発売あり・EveryDB2 マニュアル正例) は odds を返す（MEDIUM 正常発売 positive）。
    - odds_snapshot_at（選択した HappyoTime の Timestamp）/ odds_source_type='jodds_tanpuku' を保持。
    - カラム名正規化（cross-plan contract・Plan 02 ev_rank.py と一致必須）: JODDS 実DB raw 列 FukuOddsLow/FukuOddsHigh を select_odds_snapshot の戻り値 DataFrame で snake_case の fuku_odds_lower/fuku_odds_upper に rename して返す（Plan 05 Task 1 は JOIN するだけで column rename 不要）。merge_asof の left/right 結果列も snake_case で統一。

    tests/ev/test_odds_snapshot.py: Plan 01 RED stub を GREEN 実装で満たす。test_odds_snapshot_multi_horse は conftest.py::make_jodds_mock(race_key, snapshots, umabans=[1,2,3]) を使用し・同一 race_key で3頭の per-horse snapshot を生成して select_odds_snapshot に渡す・戻り値が 3行で各 umaban の odds が保持されることを assert（HIGH-1・行数不変のアサーション）。
  </action>
  <verify>
    <automated>uv run pytest tests/ev/test_odds_snapshot.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/ev/odds_snapshot.py に "def select_odds_snapshot" と "direction='backward'" と "def fetch_jodds" が含まれる
    - select_odds_snapshot の merge_asof 呼出しで by= に umaban が含まれる（HIGH-1: grep "by=['race_key', 'umaban']" または "by=[\"race_key\", \"umaban\"]" のいずれかが含まれる・race_key 単独は不可）
    - fetch_jodds の戻り値に Umaban 列が含まれる（HIGH-1: fetch_jodds の SELECT 列リストに umaban が含まれる・grep "umaban" が fetch_jodds の query 文に現れる）
    - select_odds_snapshot 戻り値 DataFrame の odds 列が snake_case の fuku_odds_lower/fuku_odds_upper（grep "fuku_odds_lower" と "fuku_odds_upper" 含む・JODDS raw FukuOddsLow/FukuOddsHigh を rename）
    - fetch_jodds が n_jodds_tanpukuwaku_head と JOIN して datakubun='1' で filter（Pitfall 2・grep "n_jodds_tanpukuwaku_head" と "datakubun" を含む）
    - select_odds_snapshot が race_start_datetime + pd.Timedelta で cutoff 計算（HHMM 整数比較なし・Pitfall 1）
    - 特殊値 ----/****/0000/0999 が全て no_bet（HIGH-3 canonical: test_odds_snapshot_special_values + test_odds_snapshot_0999_is_no_bet で検証）
    - test_odds_snapshot_multi_horse が3頭の異なる odds を入力し3行で返す（HIGH-1: 行数==入力馬数・各 umaban の odds 保存）
    - test_odds_snapshot_fukusyoflag_normal_sale が FukusuoFlag='7' で odds を返す（MEDIUM 正常発売 positive test）
    - 未来 snapshot が選択されない（test_odds_snapshot_future_leak・merge_asof backward）
    - **test_odds_snapshot_multi_race が GREEN（MEDIUM-C cycle-2: 複数レース + 重複 happyo_datetime で merge_asof が sort 違反なく各馬に正しい per-horse snapshot を返す・行数==入力候補馬数）**
    - uv run pytest tests/ev/test_odds_snapshot.py が全件 GREEN
  </acceptance_criteria>
  <done>BACK-04 odds 時点選択実装・D-01/D-02 準拠・HIGH-1 馬単位 odds 保証（by=['race_key','umaban']）・HIGH-3 0999=no_bet canonical rule 明文化・未来リーク構造的不可・特殊値 no_bet・test_odds_snapshot.py 全 GREEN（multi-horse + 0999 + 正常発売 case 含む）</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 返還/中止 honest 会計決定表（refund_accounting.py）— RED→GREEN</name>
  <files>src/ev/refund_accounting.py, tests/ev/test_refund_accounting.py</files>
  <read_first>
    - src/model/data.py::load_labels（行308-330・label.fukusho_label の既存フラグ SELECT: is_scratch_cancel/is_race_excluded/is_dead_loss/is_race_cancelled/is_fukusho_sale_available/fukusho_payout_places/fukusho_hit_validated）
    - src/model/baseline.py::_payout_places_from_row（行96-104・行ベース優先列→fallback パターン）
    - .planning/phases/05-ev-backtest/05-RESEARCH.md §2.1-§2.5（実DB label フラグ観測値 + HARAI PayFukusyoPay + 6シナリオ決定表 + _lookup_payfukusyo_pay slot lookup + Pitfall 4 is_dead_loss 除外禁止 + Pitfall 5 effective_stake=0）
    - .planning/phases/05-ev-backtest/05-PATTERNS.md「src/ev/refund_accounting.py」節（label フラグ読み + 行ベース決定 analog）
    - tests/ev/test_refund_accounting.py（Plan 01 RED stub・9 シナリオ @pytest.mark.parametrize）
    - docs/keiba_ai_requirements_v1.3.md §11.6 / §10.6
    - docs/everydb2/05-HARAI.md（FuseirituFlag2/HenkanFlag2/TokubaraiFlag2/PayFukusyoUmaban1..5/PayFukusyoPay1..5）
  </read_first>
  <behavior>
    - test_refund_normal_hit: fukusho_hit_validated=1 AND フラグ全 False → payout=PayFukusyoPay(該当 slot) / profit=payout-100 / effective_stake=100
    - test_refund_normal_miss: fukusho_hit_validated=0 AND フラグ全 False → payout=0 / profit=-100 / effective_stake=100
    - test_refund_scratch_cancel: is_scratch_cancel=True → refund=100 / effective_stake=0
    - test_refund_race_excluded: is_race_excluded=True → refund=100 / effective_stake=0
    - test_refund_dead_loss: is_dead_loss=True → profit=-100 / effective_stake=100（§10.6 除外禁止・Pitfall 4）
    - test_refund_fuseiritu: fuseirituflag2='1' → refund=100 / effective_stake=0
    - test_refund_race_cancelled: is_race_cancelled=True → refund=100 / effective_stake=0
    - test_refund_no_sale: is_fukusho_sale_available=False → stake=0（選択対象外・到達しないはずだが安全側に倒す）
    - test_refund_deadheat: PayFukusyoUmaban slot 2-5 に umaban → 該当 slot の PayFukusyoPay
    - test_refund_tokubarai: tokubaraiflag2='1'（特払）→ PayFukusyoPay を payout に計上（公式に従う）
    - **test_refund_tokubarai_harai_fixture（MEDIUM-D cycle-2）: 実DB観測値では tokubaraiflag2='1' が 0件のため合成必須。HARAI 実スキーマ形状の特払 fixture（row: race_key + FuseirituFlag2='0' + TokubaraiFlag2='1' + HenkanFlag2='0' + PayFukusyoUmaban1..5='00'(的中馬番なし・特払) + PayFukusyoPay1..5=70(特払金額) + label fukusho_hit_validated=0）を構築し・determine_stake_payout(row) が stake=100 / refund=0 / payout=70（特払 PayFukusyoPay） / profit=-30 / effective_stake=100 を返すことを assert（公式に従う・§2.4 特払 semantics）。fukusho_hit_validated=0 でも PayFukusyoPay>0 の場合は payout>0 となる契約（特払は的中フラグ非依存・HARAI PayFukusyoPay 一次）**
  </behavior>
  <action>
    src/ev/refund_accounting.py:
    - determine_stake_payout(row, *, stake_per_bet=100) -> dict（keys: stake, refund, payout, profit, effective_stake）: RESEARCH §2.2/§2.3 決定表を実装
      - if not is_fukusho_sale_available → stake=0（選択対象外）
      - elif is_scratch_cancel or is_race_excluded or is_race_cancelled or fuseirituflag2=='1' → stake=100, refund=100, payout=0, profit=0, effective_stake=0
      - elif is_dead_loss → stake=100, refund=0, payout=0, profit=-100, effective_stake=100（§10.6 除外禁止・Pitfall 4）
      - elif tokubaraiflag2=='1'（特払）→ payout=_lookup_payfukusyo_pay(row) / profit=payout-100 / effective_stake=100
      - else（通常）→ payout=_lookup_payfukusyo_pay(row) / profit=payout-100 / effective_stake=100
    - _lookup_payfukusyo_pay(row) -> int: row.umaban を PayFukusyoUmaban1..5 slot と照合・該当 slot の PayFukusyoPay1..5 を返す。該当なしは 0（不的中）。同着で slot 2-5 使用可。
    - label.fukusho_label フラグが一次ソース・HARAI（FuseirituFlag2/TokubaraiFlag2/PayFukusyoPay）は cross-check/補助。HenkanUma ビットマスクは直接見ない（label is_scratch_cancel/is_race_excluded が同等情報・A6）。

    tests/ev/test_refund_accounting.py: Plan 01 RED stub を GREEN 実装で満たす（9シナリオ + tokubarai）。合成 row は conftest.py の make_label_mock / make_harai_mock を使用。
  </action>
  <verify>
    <automated>uv run pytest tests/ev/test_refund_accounting.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/ev/refund_accounting.py に "def determine_stake_payout" と "def _lookup_payfukusyo_pay" が含まれる
    - is_dead_loss のケースが effective_stake=100, profit=-100（§10.6 除外禁止・test_refund_dead_loss で検証）
    - 取消/除外/不成立/レース中止のケースが effective_stake=0, refund=100（test_refund_scratch_cancel/excluded/fuseiritu/race_cancelled で検証）
    - _lookup_payfukusyo_pay が PayFukusyoUmaban1..5 slot → PayFukusyoPay1..5 の lookup（同着 slot 2-5 含む）
    - **test_refund_tokubarai_harai_fixture が GREEN（MEDIUM-D cycle-2: HARAI 実スキーマ形状の特払 fixture で PayFukusyoPay=70 を payout に計上・fukusho_hit_validated=0 でも PayFukusyoPay>0 で payout>0 の契約）**
    - uv run pytest tests/ev/test_refund_accounting.py が全件 GREEN
  </acceptance_criteria>
  <done>BACK-03 返還会計決定表実装・6シナリオ + 特払 対抗的テスト全 GREEN・§10.6/§11.6 準拠・**MEDIUM-C/D cycle-2 multi-race sort + 特払 HARAI fixture 検証 GREEN**</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JODDS raw → snapshot 選択 | merge_asof(direction='backward') で cutoff 以降の未来 snapshot が構造的に選択されない |
| label フラグ + HARAI → 会計 dict | label 一次・HARAI cross-check・HenkanUma 直接解析はしない（A6・取りこぼし回避） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Information Disclosure | JODDS 時点選択で未来 snapshot が混入（後知恵リーク） | mitigate | merge_asof(direction='backward') 固定 + sort_values 前提・test_odds_snapshot_future_leak で検証（D-02・§11.2） |
| T-05-06b | Information Disclosure | merge_asof の by= が race_key 単独で同一レースの別馬 odds で上書きされ各馬 odds が失われる（HIGH-1） | mitigate | by=['race_key','umaban'] で per-horse as-of join・test_odds_snapshot_multi_horse で3頭の行数と odds 保存を assert |
| T-05-07 | Tampering | 確定オッズ（DataKubun='4'）が混入（D-01 違反） | mitigate | head テーブル JOIN + datakubun='1' filter・test_odds_snapshot_datakubun_filter で検証（Pitfall 2） |
| T-05-07b | Tampering | 0999 を 99.9倍の有効 odds と誤解し高オッズ馬の EV を不当に膨らませる（HIGH-3） | mitigate | CONTEXT D-02 を正とし 0999=no_bet sentinel に正準化・test_odds_snapshot_0999_is_no_bet で assert・RESEARCH 行89 の矛盾記述は本モジュールで廃棄 |
| T-05-08 | Tampering | is_dead_loss 馬を除外して回収率過大評価（§10.6 違反） | mitigate | effective_stake=100, profit=-100 で計上・test_refund_dead_loss で検証（Pitfall 4） |
| T-05-09 | Tampering | 返還馬を effective_stake=100 で計上して回収率歪み | mitigate | 取消/除外/不成立/レース中止は effective_stake=0・test_refund_* で検証（Pitfall 5・§11.6） |
| T-05-10 | Information Disclosure | silent fallback で都合の良い別時点に差し替え（§11.3 違反） | mitigate | 特殊値/0件は no_bet sentinel・test_odds_snapshot_special_values/no_bet_empty で検証 |
| T-05-10b | Tampering | FukusuoFlag の正常発売値 '7' を誤って no_bet に分類し候補を不当に減らす（MEDIUM） | mitigate | EveryDB2 マニュアル値 '7'=発売あり を正例とし・test_odds_snapshot_fukusyoflag_normal_sale で odds 返却を assert・'0'/'1'/'3' のみ no_bet |
| T-05-22 | Information Disclosure | merge_asof の sort が race_key 優先で happyo_datetime の大域ソートが崩れ・複数レースで重複 happyo_datetime の場合に sort 違反でエラーまたは誤 snapshot 選択となる（MEDIUM-C cycle-2） | mitigate | test_odds_snapshot_multi_race で複数レース + 重複 happyo_datetime の合成データを与え merge_asof(by=['race_key','umaban']) が正常動作と per-horse 正しい snapshot を assert |
| T-05-23 | Tampering | 特払（TokubaraiFlag2='1'）の HARAI slot semantics（的中馬番なし PayFukusyoUmaban='00' + PayFukusyoPay=特払金額）が通常 hit slot と異なり _lookup_payfukusyo_pay が 0 を返してしまい payout 過少計上（MEDIUM-D cycle-2） | mitigate | test_refund_tokubarai_harai_fixture で実DB形状の特払 HARAI fixture（PayFukusyoUmaban='00' + PayFukusyoPay=70）を与え payout=70 を assert・§2.4 特払 semantics で的中フラグ非依存・HARAI PayFukusyoPay 一次契約 |
| T-05-SC | Tampering | JODDS/HARAI クエリ（新規パッケージなし・既存 psycopg3） | accept | parameterized query（%s placeholder）で SQL injection 対策済み・既存 baseline.py パターン踏襲・新規パッケージ無し |
| T-05-SC2 | Tampering | cross-plan odds カラム名不整合（fuku_odds_lower vs FukuOddsLow）で Plan 05 JOIN が KeyError | mitigate | select_odds_snapshot 戻り値を snake_case fuku_odds_lower/fuku_odds_upper に統一・Plan 02 ev_rank.py 期待名と一致・test_odds_snapshot_returns_snake_case で検証 |
</threat_model>

<verification>
- uv run pytest tests/ev/test_odds_snapshot.py tests/ev/test_refund_accounting.py -x -q → 全 GREEN
- uv run pytest tests/ev/ -q → 全 GREEN（Plan 02 完了後）
</verification>

<success_criteria>
- BACK-04 odds 時点固定・未来リーク構造的不可・特殊値/0件 no_bet sentinel
- HIGH-1 馬単位オッズ保証: merge_asof by=['race_key','umaban'] で各馬に固有 odds を割当（test_odds_snapshot_multi_horse 3頭行数・odds 保存 GREEN）
- HIGH-3 0999 canonical rule: 0999=no_bet sentinel（CONTEXT D-02 正・RESEARCH 行89 廃棄・test_odds_snapshot_0999_is_no_bet GREEN）
- BACK-03 返還会計決定表（6シナリオ + 特払）対抗的テスト全 GREEN・label 一次/HARAI cross-check
- §10.6（競走中止除外禁止）+ §11.6（effective_stake 分母控除）履行
- MEDIUM: FukusyoFlag='7' 正常発売 positive test + 行/集計 profit 不変量（Plan 02 連携）
</success_criteria>

<output>
Create `.planning/phases/05-ev-backtest/05-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces（本 plan の分）

- `src/ev/odds_snapshot.py::fetch_jodds(readonly_cur, *, year=None)` / `select_odds_snapshot(jodds_df, race_times, policy)` — 戻り値 odds 列は snake_case `fuku_odds_lower`/`fuku_odds_upper`（JODDS raw `FukuOddsLow`/`FukuOddsHigh` を rename）。`select_odds_snapshot` は `merge_asof(by=['race_key','umaban'], direction='backward')` で馬単位 odds を保証（HIGH-1）。特殊値 `----`/`****`/`0000`/`0999`/` `(sp) は全て no_bet sentinel（HIGH-3 canonical）。
- `src/ev/refund_accounting.py::determine_stake_payout(row, *, stake_per_bet=100)` / `_lookup_payfukusyo_pay(row)`
