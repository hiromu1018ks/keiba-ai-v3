---
phase: 05-ev-backtest
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/ev/__init__.py
  - src/ev/ev_rank.py
  - src/ev/purchase_simulator.py
  - src/ev/metrics.py
  - src/ev/bl3_betting.py
  - tests/ev/test_ev_rank.py
  - tests/ev/test_purchase_simulator.py
  - tests/ev/test_metrics.py
  - tests/ev/test_bl3_betting.py
autonomous: true
requirements: [EV-01, EV-02, BACK-02]
user_setup: []

must_haves:
  truths:
    - "EV-01: EV_lower = p × odds_lower / EV_upper = p × odds_upper を pandas Series 演算で算出（純粋関数）"
    - "EV-02: 推奨ランク S/A/B/C/D を EV/確率/odds_lower のみで階層判定（未定義の予測信頼度不使用）"
    - "BACK-02: 仮想購入ルール fukusho_ev_v1（EV_lower≥1.05, p≥0.15, odds_lower≥1.5, top-2/race, 100円, 複勝のみ）を groupby head で実装"
    - "§11.6: 回収率 = sum(payout)/sum(effective_stake)・effective_stake=0 は分母から控除・max drawdown は race_date 昇順の累積"
    - "D-04: BL-3 は確定複勝オッズ昇順 top-2（EV でなく人気順）で選ぶ（p=1/odds で EV 自己参照回避）"
  artifacts:
    - path: "src/ev/ev_rank.py"
      provides: "compute_ev_and_rank(df) / _rank(row)"
      contains: "def compute_ev_and_rank"
    - path: "src/ev/purchase_simulator.py"
      provides: "select_bets(df) — fukusho_ev_v1 ルール"
      contains: "def select_bets"
    - path: "src/ev/metrics.py"
      provides: "compute_backtest_metrics(df) — §11.6 回収率/P/L/max DD"
      contains: "def compute_backtest_metrics"
    - path: "src/ev/bl3_betting.py"
      provides: "select_bl3_bets(market_df) — 確定オッズ昇順 top-2"
      contains: "def select_bl3_bets"
  key_links:
    - from: "src/ev/purchase_simulator.py::select_bets"
      to: "src/ev/ev_rank.py::compute_ev_and_rank"
      via: "select_bets は事前に compute_ev_and_rank で EV_lower/rank が付与された df を受け取る"
      pattern: "EV_lower"
    - from: "src/ev/bl3_betting.py"
      to: "src/model/baseline.py::fetch_market_data / BL3_COMPARISON_CAVEAT"
      via: "fetch_market_data で確定オッズを取得・caveat を再利用"
      pattern: "BL3_COMPARISON_CAVEAT"
---

<objective>
EV 計算・推奨ランク・仮想購入ルール・回収率指標・BL-3 betting ROI の5つの純粋関数群を実装し、対応 RED stub（Plan 01 Task 2）を GREEN 化する。
Purpose: EV-01/EV-02/BACK-02 + §11.6 + D-04 を純粋関数（DB不要・合成データ検証可能）で実装。
Output: src/ev/__init__.py + ev_rank.py + purchase_simulator.py + metrics.py + bl3_betting.py。
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ev-backtest/05-CONTEXT.md
@.planning/phases/05-ev-backtest/05-RESEARCH.md
@.planning/phases/05-ev-backtest/05-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EV 計算 + 推奨ランク（ev_rank.py）— RED→GREEN</name>
  <files>src/ev/__init__.py, src/ev/ev_rank.py, tests/ev/test_ev_rank.py</files>
  <read_first>
    - src/model/baseline.py::compute_bl1（行110-128・pd.Series 初期化 + name 付与 + df.copy() 純粋関数パターン）
    - .planning/phases/05-ev-backtest/05-RESEARCH.md §3.1-§3.3（§11.1 EV 直線積・§11.5 ランク S/A/B/C/D 閾値・odds_lower no_bet→D）
    - .planning/phases/05-ev-backtest/05-PATTERNS.md「src/ev/ev_rank.py」節（compute_bl1 analog）
    - tests/ev/test_ev_rank.py（Plan 01 Task 2 で作成した RED stub・4 テスト）
    - docs/keiba_ai_requirements_v1.3.md §11.1/§11.5（閾値 S≥1.20/0.25/1.5・A≥1.10/0.20/1.5・B≥1.05/0.15・C≥1.00・D）
  </read_first>
  <behavior>
    - test_ev_calculation: p=0.3, odds_lower=5.0, odds_upper=6.0 → EV_lower=1.5, EV_upper=1.8
    - test_rank_S: EV_lower=1.25, p=0.30, odds_lower=2.0 → 'S'（3条件 AND）
    - test_rank_D_low_ev: EV_lower=0.8 → 'D'
    - test_rank_B_no_odds_threshold: EV_lower=1.06, p=0.16, odds_lower=1.2 → 'B'（odds 閾値なし・EV+p 2条件）
    - test_rank_no_bet_is_D: odds_lower=NaN（no_bet）→ 'D'
  </behavior>
  <action>
    src/ev/__init__.py（空モジュールマーカー）を新設。
    src/ev/ev_rank.py:
    - compute_ev_and_rank(df) -> df: df.copy() → df['EV_lower'] = df['p_fukusho_hit'] * df['fuku_odds_lower'] / df['EV_upper'] = df['p_fukusho_hit'] * df['fuku_odds_upper']（pandas Series 演算）→ df['recommend_rank'] = df.apply(_rank, axis=1)
    - _rank(row): 階層判定（S→A→B→C→D 上から順に最初に満たしたもの）。閾値: S(ev>=1.20 and p>=0.25 and ol>=1.5) / A(ev>=1.10 and p>=0.20 and ol>=1.5) / B(ev>=1.05 and p>=0.15) / C(ev>=1.00) / D(otherwise)。odds_lower が NaN（no_bet）は D。
    tests/ev/test_ev_rank.py: Plan 01 RED stub を GREEN 実装で満たす（test_rank_no_bet_is_D を追加可）。
  </action>
  <verify>
    <automated>uv run pytest tests/ev/test_ev_rank.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/ev/ev_rank.py に "def compute_ev_and_rank" と "def _rank" が含まれる
    - EV_lower/EV_upper の計算が p_fukusho_hit * fuku_odds_lower/high（§11.1 直線積）
    - _rank の閾値が §11.5 完全準拠（1.20/0.25/1.5・1.10/0.20/1.5・1.05/0.15・1.00）
    - uv run pytest tests/ev/test_ev_rank.py が全件 GREEN
  </acceptance_criteria>
  <done>EV-01/EV-02 純粋関数実装・§11.1/§11.5 準拠・test_ev_rank.py 全 GREEN</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 仮想購入ルール fukusho_ev_v1（purchase_simulator.py）+ 回収率指標（metrics.py）— RED→GREEN</name>
  <files>src/ev/purchase_simulator.py, src/ev/metrics.py, tests/ev/test_purchase_simulator.py, tests/ev/test_metrics.py</files>
  <read_first>
    - src/model/baseline.py::_race_normalize_inverse（行134-161・race_key groupby + sort・head パターン）
    - src/model/baseline.py::compute_bl1（純粋 pandas 関数パターン）
    - .planning/phases/05-ev-backtest/05-RESEARCH.md §4.1-§4.5（§11.4 fukusho_ev_v1 ルール・top-2 タイブレーク mergesort・保持項目・selected vs effective_bet）+ §8.1-§8.3（§11.6 回収率/P/L/max DD・effective_stake 分母控除・時系列順累積）
    - .planning/phases/05-ev-backtest/05-PATTERNS.md「purchase_simulator / metrics」節 + 共有パターン7（行整列 mergesort）
    - tests/ev/test_purchase_simulator.py / tests/ev/test_metrics.py（Plan 01 RED stub）
    - docs/keiba_ai_requirements_v1.3.md §11.4 / §11.6
  </read_first>
  <behavior>
    purchase_simulator:
    - test_purchase_filter_conditions: EV_lower<1.05 / p<0.15 / odds_lower<1.5 の各条件で除外
    - test_purchase_top2: レース内3候補 → 上位2頭選択
    - test_purchase_tiebreak: 同 EV_lower で umaban 昇順（mergesort 安定）
    - test_purchase_no_eligible: 条件満たさず → 0選択
    - test_purchase_no_sale: is_fukusho_sale_available=False → 除外
    - test_purchase_no_bet_excluded: fuku_odds_lower=NaN（no_bet）→ 除外
    metrics:
    - test_metrics_recovery_rate: payout=150, effective_stake=100 → 1.5
    - test_metrics_refund_excluded: 返還馬 effective_stake=0 → 分母から控除
    - test_metrics_max_drawdown: 累積 profit [100,200,50,150] → max DD=150（200→50）
    - test_metrics_counts: selected_count / effective_bet_count / refund_count / hit_count
    - test_metrics_zero_division: 全件 effective_stake=0 → recovery_rate=0.0
    - test_metrics_profit_invariant: 合成 selected_with_accounting（通常的中/不的中/返還/競走中止/特払を混在）で sum(row.profit) == sum(payout_amount) + sum(refund_amount) - sum(stake) が成立することを assert（MEDIUM-02：行ベース profit と集計 profit の不変量・refund 行や dead-loss 行で会計が分岐しないことを担保）
  </behavior>
  <action>
    src/ev/purchase_simulator.py:
    - select_bets(df, *, strategy='fukusho_ev_v1', max_bets_per_race=2, stake_per_bet=100) -> df
    - 事前 filter: is_fukusho_sale_available AND is_model_eligible AND fuku_odds_lower.notna()（no_bet 除外）
    - 購入条件 filter: EV_lower>=1.05 AND p_fukusho_hit>=0.15 AND fuku_odds_lower>=1.5
    - レース内 top-2: sort_values(['race_key','EV_lower','umaban'], ascending=[True,False,True], kind='mergesort') → groupby('race_key', group_keys=False).head(max_bets_per_race)
    - selected_flag=True / stake=stake_per_bet を付与

    src/ev/metrics.py:
    - compute_backtest_metrics(df) -> dict
    - recovery_rate = sum(payout_amount) / sum(effective_stake)（分母0なら 0.0・ゼロ除算回避）
    - profit_loss = sum(payout_amount) + sum(refund_amount) - sum(stake)
    - max_drawdown: df.sort_values(['race_date','race_key','umaban']) → profit.cumsum() → cummax - cumsum の max（行整列・共有パターン7）
    - selected_count = len(df) / effective_bet_count = (effective_stake>0).sum() / refund_count = refund_flag.sum() / hit_count = fukusho_hit_validated.sum()
    - 注記: profit_loss は行ごとの profit（refund_accounting が出力）の和と恒等的に等しい（test_metrics_profit_invariant で assert）。実装は集計式を用いるが、行 profit が payout+refund-stake の構造であることを refund_accounting と metrics で相互に検証する。

    tests/ev/test_purchase_simulator.py / test_metrics.py: Plan 01 RED stub を GREEN 実装で満たす（test_metrics_profit_invariant を追加）。
  </action>
  <verify>
    <automated>uv run pytest tests/ev/test_purchase_simulator.py tests/ev/test_metrics.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/ev/purchase_simulator.py に "def select_bets" と "kind='mergesort'" が含まれる（決定論的タイブレーク）
    - src/ev/purchase_simulator.py の filter 条件が §11.4 完全準拠（1.05/0.15/1.5）
    - src/ev/metrics.py に "def compute_backtest_metrics" と "effective_stake" の分母控除ロジックが含まれる
    - max_drawdown の計算が race_date 昇順の累積 cummax - cumsum
    - test_metrics_profit_invariant で sum(row.profit) == sum(payout_amount) + sum(refund_amount) - sum(stake) が混在シナリオで成立（MEDIUM-02）
    - uv run pytest tests/ev/test_purchase_simulator.py tests/ev/test_metrics.py が全件 GREEN
  </acceptance_criteria>
  <done>BACK-02 仮想購入ルール + §11.6 回収率指標 実装・テスト全 GREEN・profit 行/集計不変量検証（MEDIUM-02）</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: BL-3 投資ROI比較（bl3_betting.py）— RED→GREEN</name>
  <files>src/ev/bl3_betting.py, tests/ev/test_bl3_betting.py</files>
  <read_first>
    - src/model/baseline.py::fetch_market_data（行462-521・n_odds_tanpuku JOIN・確定オッズ取得）+ compute_bl3（行184-201）+ BL3_COMPARISON_CAVEAT（行63-66・§14.2 注記）
    - .planning/phases/05-ev-backtest/05-RESEARCH.md §9.1-§9.4（D-04 BL-3 は人気順・EV 自己参照回避・odds_snapshot_policy='confirmed' sentinel）
    - .planning/phases/05-ev-backtest/05-PATTERNS.md「src/ev/bl3_betting.py」節
    - tests/ev/test_bl3_betting.py（Plan 01 RED stub）
    - docs/keiba_ai_requirements_v1.3.md §14.2（BL-3 市場参照・同一情報条件ではない注記）
  </read_first>
  <behavior>
    - test_bl3_select_top2_low_odds: 確定複勝オッズ昇順 top-2（低い=人気高い）
    - test_bl3_no_ev: BL-3 は EV 計算しない（p=1/odds で EV=1.0 自己参照・比較無意味）
    - test_bl3_caveat: 戻り値に baseline.BL3_COMPARISON_CAVEAT 由来の注記メタが付与される
    - test_bl3_odds_policy_confirmed: odds_snapshot_policy='confirmed' sentinel で JODDS 時点非依存を明示
    - test_bl3_model_type: model_type='bl3' で区別
  </behavior>
  <action>
    src/ev/bl3_betting.py:
    - select_bl3_bets(market_df, *, max_bets_per_race=2, stake_per_bet=100) -> df
    - 事前 filter: is_fukusho_sale_available AND fukuoddslow.notna() AND fukuoddslow>0
    - 確定複勝オッズ昇順 top-2: sort_values(['race_key','fukuoddslow','umaban'], ascending=[True,True,True], kind='mergesort') → groupby('race_key', group_keys=False).head(max_bets_per_race)
    - selected_flag=True / stake=stake_per_bet / model_type='bl3' / odds_snapshot_policy='confirmed'（sentinel）を付与
    - BL-3 は EV 計算しない（p=1/odds で EV=1.0 自己参照回避 per D-04）
    - baseline.BL3_COMPARISON_CAVEAT を import して注記として付与（§14.2 同一情報条件ではない）
    - fetch_market_data は変更せず再利用（確定オッズ源）

    tests/ev/test_bl3_betting.py: Plan 01 RED stub を GREEN 実装で満たす。
  </action>
  <verify>
    <automated>uv run pytest tests/ev/test_bl3_betting.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/ev/bl3_betting.py に "def select_bl3_bets" と "BL3_COMPARISON_CAVEAT" import と "'confirmed'" sentinel が含まれる
    - BL-3 選択が fukuoddslow 昇順（EV でない）・p=1/odds 自己参照回避
    - uv run pytest tests/ev/test_bl3_betting.py が全件 GREEN
  </acceptance_criteria>
  <done>D-04 BL-3 投資ROI比較実装・確定オッズ昇順 top-2・§14.2 caveat 付与・test_bl3_betting.py 全 GREEN</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EV/rank 入力 df → 出力 | 純粋関数・入力破壊禁止（df.copy()）・odds_lower no_bet は rank='D' で選択除外 |
| BL-3 確定オッズ vs 主モデル odds-free feature | 情報条件の違い（§14.2）を BL3_COMPARISON_CAVEAT で明示しないと誤比較になる |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | recommend_rank 閾値のすり替え（§11.5 以外の値） | mitigate | test_rank_S/test_rank_B_no_odds_threshold が §11.5 閾値を assert（1.20/0.25/1.5 等） |
| T-05-04 | Information Disclosure | BL-3 を主モデルと「同条件比較」と誤表示 | mitigate | BL3_COMPARISON_CAVEAT を戻り値に付与・test_bl3_caveat で検証（§14.2） |
| T-05-05 | Tampering | max drawdown の時系列が崩れると過小評価 | mitigate | race_date 昇順ソート + cummax-cumsum（共有パターン7）・test_metrics_max_drawdown で検証 |
| T-05-05b | Tampering | 行 profit（refund_accounting）と集計 profit（metrics）が refund/dead-loss/tokubarai 行で分岐して ROI が歪む（MEDIUM-02） | mitigate | test_metrics_profit_invariant で sum(row.profit) == sum(payout) + sum(refund) - sum(stake) を混在シナリオで assert |
</threat_model>

<verification>
- uv run pytest tests/ev/test_ev_rank.py tests/ev/test_purchase_simulator.py tests/ev/test_metrics.py tests/ev/test_bl3_betting.py -x -q → 全 GREEN
- uv run pytest tests/ev/ -q（odds_snapshot/refund_accounting は Plan 03 で GREEN 化・本 plan では RED のまま許容）
</verification>

<success_criteria>
- EV-01/EV-02/BACK-02 + §11.6 + D-04 BL-3 が純粋関数で実装・対応 RED stub 全 GREEN
- §11.5 ランク閾値完全準拠・no_bet は rank='D'
- BL-3 は確定オッズ昇順（EV 自己参照回避）・§14.2 caveat 付与
</success_criteria>

<output>
Create `.planning/phases/05-ev-backtest/05-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces（本 plan の分）

- `src/ev/__init__.py`
- `src/ev/ev_rank.py::compute_ev_and_rank(df)` / `_rank(row)`
- `src/ev/purchase_simulator.py::select_bets(df, *, strategy='fukusho_ev_v1', max_bets_per_race=2, stake_per_bet=100)`
- `src/ev/metrics.py::compute_backtest_metrics(df) -> dict`
- `src/ev/bl3_betting.py::select_bl3_bets(market_df, *, max_bets_per_race=2, stake_per_bet=100)`
