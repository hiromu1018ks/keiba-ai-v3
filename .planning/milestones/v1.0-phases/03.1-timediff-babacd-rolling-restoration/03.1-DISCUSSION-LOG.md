# Phase 03.1: Timediff/Babacd Rolling Restoration - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-19
**Phase:** 03.1-Timediff/Babacd Rolling Restoration
**Areas discussed:** babacd方針, timediff変換とsentinel, fail-loudの強度, datakubun取り込み要否

---

## 実DB検証による前提修正（議論の前提）

live-DB 検証で roadmap SC#1/SC#2 の前提が崩れたことを議論前に共有:
- `timediff` は `public.n_uma_race` に実在（varchar・`+000`〜`9999`・sentinel 8,554件≈1.0%）
- `babacd` は**実カラム非存在**（馬場系は `n_race` の `sibababacd`/`dirtbabacd`/`trackcd` のみ）
- `datakubun` は raw に実在・normalized 非存在・`normalized.n_uma_race` は既に 554,267 行に dedupe 済み

---

## babacd方針

| Option | Description | Selected |
|--------|-------------|----------|
| A: 完全復元(24) | normalized.n_race に馬場3カラム追加 + history で trackcd 分岐派生。babacd 3feature も復元して計24 | ✓ |
| B: defer(21) | timediff 3feature のみ復元。babacd は別フェーズで正しく設計してから | |
| C: 簡易派生 | 例: sibababacd のみ(ダート無視)等。中間だが半端になりやすく作り直しリスク | |

**User's choice:** 初回回答「もう少しわかりやすく教えてくれ」→平易な具体例で再説明後に **A: 完全復元(24)**
**Notes:** ユーザーが Phase 3.1 を「Phase 4 学習前に6 feature を戻したい」と挿入した意図を重視。馬場適性信号を Phase 4 モデルに入れたい。babacd は本来「設計が必要な派生特徴量」だが、急挿入フェーズに詰める規律リスクも説明した上で A を選択。

---

## timediff変換とsentinel

| Option | Description | Selected |
|--------|-------------|----------|
| 標準: parse+sentinel除外 | 符号付き→real変換・sentinel '9999'除外・既存rollingの __MISSING__/count と一貫。parse書式はresearcher確定 | ✓ |
| Claude裁量に委ねる | parse/sentinel/過去走扱いを計画者に完全委ねる(既存パターン踏襲) | |
| 再設計(勝差として正規化) | 絶対値化や勝馬差正規化で情報量狙う。既存rolling(生値)との一貫性崩す | |

**User's choice:** 標準: parse+sentinel除外
**Notes:** 実DB検証で parse 仕様を確定済み（`[+-]NNN`=NNN/10秒・1着馬負値・2着以下正值・sign 分布 +800,330/-71,290/先頭0が1,028）。先頭`0`の1,028行 edge case は researcher 検証。

---

## fail-loudの強度

| Option | Description | Selected |
|--------|-------------|----------|
| 任せる | advisory 4件は todo 処方箝通り・WR-02 は「空データ検知で止める」採用。判断不要 | ✓ |
| 即エラー停止を指定 | WR-02 を即 raise(最厳格)。0行結果捕捉きれない場合あり | |
| hardening自体を後回し | advisory を別機会に。ただし operator 指示と不整合 | |

**User's choice:** 初回回答「全然意味がわからない」→advisory 4件を平易に料理の例等で再説明後に **任せる(推奨)**
**Notes:** WR-02 の fail-loud 実装スタイル（即raise vs チョークポイント検出）は純粋なコード内部の話と判断し、推奨(チョークポイント検出・空データ検知で止める)を採用して Claude/計画者に委ねた。

---

## datakubun取り込み要否

| Option | Description | Selected |
|--------|-------------|----------|
| 取り込まない | datakubun は normalized 非取り込み。dedupe済みのため不要。SC#1 厳格維持 | ✓ |
| 取り込む(将来用途) | 障害戦/取消行 history フィルタ等の将来用。ただし SC#1 広げるスコープ増 | |
| researcher検証に委ねる | dedupe 機構の詳細を researcher が検証して決定 | |

**User's choice:** 取り込まない(推奨)
**Notes:** `normalized.n_uma_race` が既に 554,267 行に dedupe 済み（version選別=ETL側完結）のため、timediff・babacd 派生のいずれにも不要と確認。スコープ厳格を維持。

---

## Claude's Discretion

- timediff parse 書式の最終確定（実DB edge case 含む）
- babacd 派生の edge case（trackcd 欠損/障害競走・派生不能時は `__MISSING__`）
- `normalized.n_race` 拡張の物理設計（raw-pass-through varchar・staging-swap idempotent DDL）
- テスト更新（3者 parity 6→8系統化・"no timediff/babacd" テスト反転・fixture 拡張）
- §12.4 reproducibility metadata（feature_count 18→24・新 snapshot_id・byte-repro/parity 再検証）
- live snapshot rebuild の実行（許可済み live-DB 操作は Claude 直接実行）
- advisory 4件の hardening 実装（todo 処方箝通り・WR-02 はチョークポイント検出）

## Deferred Ideas

- babacd 序数リマップ（CODE.md 2010 順序確定後・Phase 4 評価後の改善）
- datakubun による history フィルタ（障害戦/取消行除外・必要性が出れば別途）
- Phase 4 での rolling 24 feature のモデル寄与評価
- Phase 8 での babacd 派生 PIT 正しさの対抗的監査
