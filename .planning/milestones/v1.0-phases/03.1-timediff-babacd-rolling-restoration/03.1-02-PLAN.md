---
phase: 03.1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/run_feature_build.py
  - tests/features/test_snapshot_repro.py
autonomous: true
requirements: [FEAT-01]
user_setup: []

must_haves:
  truths:
    - "run_feature_build.py で persist_category_maps が write_manifest より先に実行される"
    - "persist_category_maps の直後に category_map_path.exists() の assert が入る"
    - "manifest の category_map_artifact が repo-root 相対パス（snapshots/category_map_*.json）で CWD 非依存"
    - "persist 失敗時に manifest が書かれない（例外で manifest 書出前に停止）"
  artifacts:
    - path: "scripts/run_feature_build.py"
      provides: "persist→manifest 順序化 + exists assert + repo-root 相対パス正規化（CR-01新）"
      contains: "persist_category_maps"
    - path: "tests/features/test_snapshot_repro.py"
      provides: "CR-01新 persist→manifest 順序の回帰テスト + stale rolling_timediff_mean_5 参照整理"
      contains: "test_snapshot_repro"
  key_links:
    - from: "scripts/run_feature_build.py::persist_category_maps"
      to: "scripts/run_feature_build.py::write_manifest"
      via: "persist 成功 → exists assert → manifest 書出（SHA256 含む）の順序・partial-failure 状態を防止"
      pattern: "persist_category_maps"
---

<objective>
`scripts/run_feature_build.py` の manifest→persist 順序依存（CR-01新・advisory hardening）を解消し、persist→manifest 順序化 + exists assert + repo-root 相対パス正規化を行う。SC#3（byte-reproducibility）の defense-in-depth 強化。per D-04（advisory 4件は todo 処方箝通り）。

Purpose: persist 失敗時に SHA256 一致の「完成済」manifest だけが残り category map artifact が欠損する再現性破壊状態（partial-failure）を防止する。同時に test_snapshot_repro.py の stale `rolling_timediff_mean_5` 参照（L25）を整理し、Phase 3.1 復元後の新 snapshot で test が GREEN を維持するよう準備する。
Output: scripts/run_feature_build.py の順序化・assert 追加・パス正規化。tests/features/test_snapshot_repro.py の stale 参照整理 + CR-01新 回帰テスト追加。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-timediff-babacd-rolling-restoration/03.1-CONTEXT.md
@.planning/phases/03.1-timediff-babacd-rolling-restoration/03.1-RESEARCH.md
@.planning/phases/03.1-timediff-babacd-rolling-restoration/03.1-PATTERNS.md
@.planning/todos/pending/phase3-advisory-hardening.md
@scripts/run_feature_build.py
@tests/features/test_snapshot_repro.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: persist→manifest 順序化 + exists assert + repo-root 相対パス正規化（CR-01新）</name>
  <files>scripts/run_feature_build.py</files>
  <read_first>
    - scripts/run_feature_build.py（現状の L201-228・`write_manifest` 呼出 L206-223・`persist_category_maps` 呼出 L227-228・`category_map_artifact` 変数 L204・`_SNAPSHOTS_DIR` 定義・`parquet_path`/`manifest_path`/`category_map_path` 構築箇所）
    - .planning/todos/pending/phase3-advisory-hardening.md（CR-01新 修正方針・テスト条件）
    - 03.1-PATTERNS.md（scripts/run_feature_build.py 拡張ポイント A・analog: L201-228 既存 write_manifest/persist_category_maps 箇所）
    - 03.1-RESEARCH.md（Pitfall 5 partial-failure 状態回避・persist→exists assert→manifest 順序）
  </read_first>
  <behavior>
    - persist_category_maps が write_manifest より先に呼ばれる
    - persist の直後に `assert category_map_path.exists()` が入る
    - manifest の category_map_artifact 引数が repo-root 相対（`snapshots/category_map_<id>.json`）で CWD に依存しない
    - persist が例外を投げた場合、manifest 書出処理に到達しない（例外伝播）
  </behavior>
  <action>
    scripts/run_feature_build.py L201-228 の順序を以下に変更する（per D-04・CR-01新 処方箝）: (1) `category_map_path = _SNAPSHOTS_DIR / f"category_map_{args.snapshot_id}.json"` 構築（既存 L227 を前へ移動）→ (2) `persist_category_maps(frozen_maps, category_map_path)` 呼出（既存 L228 を前へ移動）→ (3) `assert category_map_path.exists(), "persist_category_maps が category map artifact を書けなかった (CR-01新・再現性破壊防止)"` を新規追加 → (4) 既存の `write_manifest(manifest_path, ..., category_map_artifact=category_map_artifact, ...)` 呼出（L206-223）。`category_map_artifact`(L204・`f"snapshots/category_map_{args.snapshot_id}.json"`) が既に repo-root 相対であることを確認（既に満たしている場合はコメントで明記・CWD 依存性除去の副次要件は確認のみ）。persist 失敗時は例外で manifest 書出前に停止する構造（Pitfall 5）。`write_manifest`/`persist_category_maps` の関数シグネチャは変更しない（呼出順序のみ）。
  </action>
  <verify>
    <automated>uv run python -c "import ast; ast.parse(open('scripts/run_feature_build.py').read())" && uv run pytest tests/features/test_snapshot_repro.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `persist_category_maps` の呼出行が `write_manifest` の呼出行より**前**（行番号が小さい）に出現する
    - `persist_category_maps` 呼出と `write_manifest` 呼出の間に `assert category_map_path.exists()` が存在する
    - `category_map_artifact` 変数の値が `snapshots/category_map_` で始まる repo-root 相対文字列（CWD 依存の `./` や絶対パスでない）
    - `uv run pytest tests/features/test_snapshot_repro.py -q` が GREEN（既存 byte-repro test が順序変更で破壊されない）
    - 構文エラー無し（`ast.parse` exit 0）
  </acceptance_criteria>
  <done>persist→exists assert→manifest の順序が確立し、partial-failure 状態（manifest だけ残り category map 欠損）が防止される。repo-root 相対パスで CWD 依存性が除去される。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test_snapshot_repro.py の stale rolling_timediff_mean_5 参照整理 + CR-01新 回帰テスト追加</name>
  <files>tests/features/test_snapshot_repro.py</files>
  <read_first>
    - tests/features/test_snapshot_repro.py（`_build_synthetic_matrix` L20-26・L25 の `"rolling_timediff_mean_5": [-2.0]` stale 参照・byte-repro test 本体 L32-110）
    - 03.1-PATTERNS.md（tests/features/test_snapshot_repro.py 拡張ポイント A・整理案）
    - 03.1-RESEARCH.md（Wave 0 Gaps・L25 stale 参照整理・SC#3 byte-repro）
    - scripts/run_feature_build.py（Task 1 で変更後の persist→manifest 順序・テストが参照する構造）
  </read_first>
  <behavior>
    - Test 1: L25 の stale `rolling_timediff_mean_5` 参照が整理され（コメント追記 or 別 rolling feature へ置換）、byte-repro test が SHA256 一致検査のみで列名に依存しないことが維持される
    - Test 2: persist_category_maps が失敗（例外）する状態で manifest が書かれないことを assert する新規テスト（CR-01新 回帰）が追加される
  </behavior>
  <action>
    (a) `_build_synthetic_matrix`(L20-26) の L25 `"rolling_timediff_mean_5": [-2.0]` について、Phase 3.1 で当該 feature が registry に復元されるため stale ではなくなる旨をコメントで明記する（値の調整は任意・byte-repro test L32-44 は SHA256 一致検査のみで列名に依存しないため GREEN 維持・PATTERNS.md 整理案通り）。または他の rolling feature（例: `rolling_kakuteijyuni_mean_5`）に置換して時系列の意図を明示する。（b）CR-01新 回帰テストを新規追加: persist_category_maps が例外を投げる状態（mock で IO エラーを発生 または 存在しないディレクトリへの書込）で `write_manifest` が呼ばれないこと、または manifest_path が存在しないことを assert。関数名は `test_persist_before_manifest_order` 等とする（PATTERNS.md 拡張ポイント通り・`-k "persist_order or manifest"` で発見可能）。per D-04（advisory hardening・CR-01新 テスト条件: persist 失敗時 manifest が書かれない）。
  </action>
  <verify>
    <automated>uv run pytest tests/features/test_snapshot_repro.py -q && uv run pytest tests/features/test_snapshot_repro.py -k "persist_order or manifest" -q</automated>
  </verify>
  <acceptance_criteria>
    - `_build_synthetic_matrix` の stale 参照に Phase 3.1 コメントが付与される（または別 feature に置換）
    - `test_persist_before_manifest_order`（または同等名）テストが存在し、persist 失敗時に manifest が書かれないことを assert
    - `uv run pytest tests/features/test_snapshot_repro.py -q` が GREEN（全 test）
    - `uv run pytest tests/features/test_snapshot_repro.py -k "persist_order or manifest" -q` が GREEN（CR-01新 テスト単体）
  </acceptance_criteria>
  <done>stale 参照が整理され byte-repro test が GREEN を維持、CR-01新 の順序依存回帰テストが追加されて persist 失敗時の manifest 未書込が機械保証される。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| feature build → snapshot artifact files | run_feature_build.py が Parquet + manifest + category_map JSON を atomic に生成・partial-failure で不整合状態が残らない |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-04 | Tampering | partial-failure 状態（manifest だけ残り category map 欠損）で再現性破壊 | mitigate | persist→exists assert→manifest 順序化・persist 失敗時は例外で manifest 書出前に停止（Pitfall 5）・回帰テスト test_persist_before_manifest_order で機械保証 |
| T-03.1-05 | Tampering | CWD 依存の category_map_artifact パスで run 毎に manifest 値が変わる | mitigate | category_map_artifact を repo-root 相対（`snapshots/category_map_*.json`）で正規化・Task 1 で確認 |
| T-03.1-SC | Tampering | npm/pip/cargo installs | accept | 本 Plan は新規パッケージインストールなし |
</threat_model>

<verification>
- `uv run pytest tests/features/test_snapshot_repro.py -q` が GREEN
- persist→manifest 順序の行番号 assert（`persist_category_maps` 行 < `write_manifest` 行）
- `assert category_map_path.exists()` が両呼出の間に存在
</verification>

<success_criteria>
- CR-01新（persist→manifest 順序化 + exists assert + repo-root 相対パス）が実装された
- partial-failure 状態の回帰テストが追加され GREEN
- stale rolling_timediff_mean_5 参照が整理され byte-repro test が GREEN を維持
- 既存 test_snapshot_repro.py の byte-repro 検査が破壊されていない
</success_criteria>

<output>
Create `.planning/phases/03.1-timediff-babacd-rolling-restoration/03.1-02-SUMMARY.md` when done
</output>
