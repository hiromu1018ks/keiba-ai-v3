---
phase: 03.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/etl/normalize.py
autonomous: true
requirements: [FEAT-01]
user_setup: []

must_haves:
  truths:
    - "normalized.n_uma_race に timediff カラムが存在し、raw(public.n_uma_race.timediff) と同一の varchar(4) 値が格納される"
    - "normalized.n_race に sibababacd/dirtbabacd/trackcd が存在し、raw(public.n_race) と同一 varchar 値が格納される"
    - "staging-swap idempotent ETL が 2回実行で同一 rowcount（n_uma_race=554,267・n_race=39,593）を維持する"
    - "raw(public.n_*) は一切変更されない（raw_touched=False 不変）"
  artifacts:
    - path: "src/etl/normalize.py"
      provides: "n_uma_race に timediff(varchar4)・n_race に sibababacd(varchar1)/dirtbabacd(varchar1)/trackcd(varchar2) を SELECT/DDL/INSERT/cast 定義に追加"
      contains: "timediff"
  key_links:
    - from: "src/etl/normalize.py::_UMA_RACE_SELECT_COLUMNS"
      to: "src/etl/normalize.py::_NORMALIZED_COLUMNS[n_uma_race]"
      via: "_idempotent_load が cols を通じて staging INSERT に渡す・3者(SELECT/DDL/INSERT)のカラム順序が一致"
      pattern: "timediff"
---

<objective>
Phase 2 normalized ETL を拡張し、`timediff`(勝馬差・n_uma_race 側)と `sibababacd`/`dirtbabacd`/`trackcd`(芝ダ馬場・n_race 側) の4カラムを raw から normalized へ raw varchar pass-through で取り込む（per D-01/D-03・raw 不変性・全 varchar 明示キャスト・staging-swap idempotent）。

Purpose: 後続 Plan 02/03 の feature builder が history SELECT でこれらカラムを取得し、timediff(real parse)・babacd(trackcd 分岐派生) を構築するための DB source を整備する。SC#1（normalized への source カラム取り込み）の直接根拠。
Output: src/etl/normalize.py の n_uma_race/n_race の SELECT/DDL/INSERT 定義拡張。新規パッケージなし・`_idempotent_load`/`run_normalized_etl` は不変（再利用）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-timediff-babacd-rolling-restoration/03.1-CONTEXT.md
@.planning/phases/03.1-timediff-babacd-rolling-restoration/03.1-RESEARCH.md
@.planning/phases/03.1-timediff-babacd-rolling-restoration/03.1-PATTERNS.md
@src/etl/normalize.py
@docs/everydb2/04-UMA_RACE.md
@docs/everydb2/03-RACE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: n_uma_race に timediff(varchar4) を SELECT/DDL/INSERT 定義に追加</name>
  <files>src/etl/normalize.py</files>
  <read_first>
    - src/etl/normalize.py（現状の `_UMA_RACE_SELECT_COLUMNS` L187-231・`_UMA_RACE_COLUMNS` DDL L88-134・`_NORMALIZED_COLUMNS["n_uma_race"]` L472-516・`_transform_uma_race_df` L316-350 の real_cols L345 を確認）
    - .planning/phases/03.1-timediff-babacd-rolling-restoration/03.1-PATTERNS.md（拡張ポイント A・analog は harontimel3/harontimel4 の raw real pass-through・ただし timediff は varchar 保持・Anti-Pattern に従い real_cols に追加しない）
    - .planning/phases/03.1-timediff-babacd-rolling-restoration/03.1-RESEARCH.md（Pitfall 1 timediff sentinel・Pattern 2 raw varchar pass-through・timediff parse 仕様）
    - docs/everydb2/04-UMA_RACE.md field 66 TimeDIFN（timediff 実カラム仕様・varchar(4)・符号付き文字列）
  </read_first>
  <behavior>
    - n_uma_race の SELECT/DDL/INSERT の3定義が全て timediff を含み、カラム順序が3者で一致する
    - timediff は varchar(4) として DDL 定義される（raw と同一型）
    - `_transform_uma_race_df` の real_cols/int_cols には timediff を追加しない（varchar pass-through・Anti-Pattern）
  </behavior>
  <action>
    `_UMA_RACE_SELECT_COLUMNS`(L187-231) に `"timediff"` を1項目追加（analog: 既存 `"time"`/`"harontimel3"`/`"harontimel4"` の varchar/real pass-through 群・L219-221 付近）。`_UMA_RACE_COLUMNS`(DDL・L88-134) に `"timediff varchar(4)"` を同順序で追加（analog: `"dmkubun varchar(1)"` L133・varchar DDL idiom）。`_NORMALIZED_COLUMNS["n_uma_race"]`(L472-516) にも同位置に `"timediff"` を追加（SELECT/DDL/INSERT の3者がこの list/dict で整合・L601 `_idempotent_load(wcur, "n_uma_race", rows, cols)` へ渡る）。`_transform_uma_race_df`(L316-350) の real_cols(L345: `["futan","time","harontimel3","harontimel4","odds"]`) には timediff を**追加しない**（Pitfall 1/Pattern 2・varchar のまま保持し cast は後続 Plan 03 の builder 側で実施・sentinel 0000/9999 を builder で処理）。per D-03（timediff は標準 parse + sentinel 除外）・D-05（datakubun は取り込まない）。
  </action>
  <verify>
    <automated>uv run python -c "from src.etl.normalize import _UMA_RACE_SELECT_COLUMNS, _UMA_RACE_COLUMNS, _NORMALIZED_COLUMNS; assert 'timediff' in _UMA_RACE_SELECT_COLUMNS; assert any('timediff varchar(4)'==c for c in _UMA_RACE_COLUMNS); assert 'timediff' in _NORMALIZED_COLUMNS['n_uma_race']; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `_UMA_RACE_SELECT_COLUMNS` に `"timediff"` が含まれる
    - `_UMA_RACE_COLUMNS`(DDL) に `"timediff varchar(4)"` が含まれる
    - `_NORMALIZED_COLUMNS["n_uma_race"]` に `"timediff"` が含まれる
    - 3者の timediff の index 位置が一致する（SELECT と INSERT list と DDL の対応が崩れない）
    - `_transform_uma_race_df` の real_cols/int_cols に timediff が含まれない（grep で `"futan", "time", "harontimel3"` 行に timediff が無い）
    - `uv run python -c "import ast; ast.parse(open('src/etl/normalize.py').read())"` が exit 0（構文エラー無し）
  </acceptance_criteria>
  <done>n_uma_race の SELECT/DDL/INSERT の3定義が timediff(varchar4) で整合し、`_transform_uma_race_df` の cast 対象には含まれない（消費側 builder で parse）。</done>
</task>

<task type="auto">
  <name>Task 2: n_race に baba3(sibababacd/dirtbabacd/trackcd・全varchar) を SELECT/DDL/INSERT 定義に追加</name>
  <files>src/etl/normalize.py</files>
  <read_first>
    - src/etl/normalize.py（`_RACE_SELECT_COLUMNS` L162-175・`_RACE_COLUMNS` DDL L59-86・`_NORMALIZED_COLUMNS["n_race"]` L448-471・`_transform_race_df` が存在する場合はそちらも）
    - 03.1-PATTERNS.md 拡張ポイント B（n_race 側 baba3 追加・analog: syubetucd/gradecd の varchar SELECT/DDL）
    - 03.1-RESEARCH.md（babacd 派生設計・実データ値域・CODE.md §2010 は不完全）
    - docs/everydb2/03-RACE.md field 36/66/67（trackcd/sibababacd/dirtbabacd の所在・定義）
  </read_first>
  <behavior>
    - n_race の SELECT/DDL/INSERT の3定義が sibababacd/dirtbabacd/trackcd を含み、3者でカラム順序が一致する
    - 3カラムとも varchar として DDL 定義される（sibababacd varchar(1)・dirtbabacd varchar(1)・trackcd varchar(2)）
    - cast 対象（real/int）には含まれない（全 varchar pass-through）
  </behavior>
  <action>
    `_RACE_SELECT_COLUMNS`(L162-175) に `"sibababacd"`, `"dirtbabacd"`, `"trackcd"` を3項目追加（analog: `"syubetucd"`/`"gradecd"` L173-174 の varchar SELECT）。`_RACE_COLUMNS`(DDL・L59-86) に `"sibababacd varchar(1)"`, `"dirtbabacd varchar(1)"`, `"trackcd varchar(2)"` を追加（analog: `"syubetucd varchar(2)"`/`"gradecd varchar(1)"` L73-74）。`_NORMALIZED_COLUMNS["n_race"]`(L448-471) にも3カラムを同順序で追加。`_idempotent_load`(L356-441) と `run_normalized_etl`(L624-663) は**変更不要**（rows/cols が拡張された list/dict を通じて自動的に流通・advisory lock + staging-swap + rowcount verify で raw 不変性と idempotency を保証・L595/L614）。cast 系（`_transform_race_df` があれば real_cols/int_cols）には追加しない（全 varchar pass-through）。per D-01（babacd は完全復元・baba3 は n_race 拡張）。
  </action>
  <verify>
    <automated>uv run python -c "from src.etl.normalize import _RACE_SELECT_COLUMNS, _RACE_COLUMNS, _NORMALIZED_COLUMNS; assert all(c in _RACE_SELECT_COLUMNS for c in ('sibababacd','dirtbabacd','trackcd')); assert 'sibababacd varchar(1)' in _RACE_COLUMNS and 'dirtbabacd varchar(1)' in _RACE_COLUMNS and 'trackcd varchar(2)' in _RACE_COLUMNS; assert all(c in _NORMALIZED_COLUMNS['n_race'] for c in ('sibababacd','dirtbabacd','trackcd')); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `_RACE_SELECT_COLUMNS` に sibababacd/dirtbabacd/trackcd が全て含まれる
    - `_RACE_COLUMNS`(DDL) に `sibababacd varchar(1)`/`dirtbabacd varchar(1)`/`trackcd varchar(2)` が全て含まれる
    - `_NORMALIZED_COLUMNS["n_race"]` に3カラムが全て含まれる
    - 3者の3カラムの index 位置が一致する
    - 構文エラー無し（`ast.parse` exit 0）
  </acceptance_criteria>
  <done>n_race の SELECT/DDL/INSERT の3定義が baba3(varchar) で整合し、ETL 再実行で normalized.n_race に3カラムが取り込まれる準備が整う（実行は後続 Plan 04 の live snapshot rebuild で）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw(public) → normalized | ETL が raw から SELECT のみ・staging-swap で normalized に書込・timediff/baba3 も同一 boundary |
| normalized → feature builder | varchar pass-through・cast/parse は消費側（後続 Plan 03）・sentinel 処理も消費側 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-01 | Tampering | raw 不変性（_idempotent_load が raw に書込まない） | mitigate | staging-swap idempotent ETL（CREATE staging → TRUNCATE → INSERT → DROP/RENAME swap・advisory lock・rowcount verify）が raw に一切書込まない構造を再利用・timediff/baba3 追加で変更なし |
| T-03.1-02 | Tampering | timediff sentinel 0000/9999 が real cast で 0.0/999.9 として混入 | mitigate | `_transform_uma_race_df` の real_cols に timediff を追加しない（varchar 保持）・sentinel 判定は後続 Plan 03 builder 側で `isin({"0000","9999"})` で実施（Pitfall 1） |
| T-03.1-03 | Information Disclosure | timediff/baba3 の raw 値が不整合（NULL/欠損）で silent に誤値 | accept | raw は varchar で取り込み・欠損はそのまま NULL/空文字・消費側（builder）で `__MISSING__` sentinel 化（D-13）・ETL 層では関知しない |
| T-03.1-SC | Tampering | npm/pip/cargo installs | accept | 本 Plan は新規パッケージインストールなし・Package Legitimacy Gate 対象外 |
</threat_model>

<verification>
- `uv run python -c "import ast; ast.parse(open('src/etl/normalize.py').read())"` が exit 0
- Task 1/2 の `<automated>` コマンドが両方 OK を出力
- `uv run pytest tests/etl/ -q`（ETL 既存テスト・timediff/baba3 追加で破壊されていないか・staging-swap idempotent の既存テストが GREEN を維持）
</verification>

<success_criteria>
- n_uma_race に timediff(varchar4)・n_race に baba3(varchar) が SELECT/DDL/INSERT の3定義で整合して追加された
- `_transform_uma_race_df` の cast 対象に timediff が混入していない（varchar pass-through 維持）
- 既存 ETL テストが GREEN を維持（staging-swap idempotent 構造の不変性）
- `_idempotent_load`/`run_normalized_etl` は変更不要（再利用確認）
</success_criteria>

<output>
Create `.planning/phases/03.1-timediff-babacd-rolling-restoration/03.1-01-SUMMARY.md` when done
</output>
