# Phase 03.1: Timediff/Babacd Rolling Restoration - Pattern Map

**Mapped:** 2026-06-19
**Files analyzed:** 9（実コード編集対象）+ 3（生成物・live snapshot 等）
**Analogs found:** 9 / 9（全ファイルに codebase 内 exact/role-match analog あり）

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|-------------------|------|-----------|----------------|---------------|
| `src/etl/normalize.py` | etl / data-load | raw→normalized transform (staging-swap idempotent) | 同ファイル内 既存 `_UMA_RACE_*` / `_RACE_*` 構造（timediff/baba3 は同一 idiom の拡張） | exact（自己拡張） |
| `src/features/rolling.py` | feature / rolling | per-observation latest-K aggregation | 同ファイル内 既存 `jyocd` rolling 3軸前例（`_ROLLING_SYSTEMS`/`_SYSTEM_SOURCE`・D-02 序数扱い） | exact（自己拡張） |
| `src/features/availability.py` | feature / registry | 3者 parity（constant 再定義） | 同ファイル内 `_ROLLING_SYSTEMS_FOR_RESERVED` / `HISTORY_ALLOWED_POST_RACE_COLUMNS` | exact（自己拡張） |
| `src/features/builder.py` | feature / orchestration | request-response（DB SELECT→pandas 派生→rolling） | 同ファイル内 `_construct_derived_columns`（`race_nkey`/`as_of_datetime`/`days_since_prev` 派生前例） | exact（自己拡張） |
| `src/config/feature_availability.yaml` | config / registry | declarative registry (YAML) | 同ファイル内 既存 `rolling_jyocd_*` / `rolling_kakuteijyuni_*` 3軸エントリ | exact（自己拡張） |
| `src/config/label_spec.yaml` | config / sentinel-spec | declarative sentinel list | 同ファイル内 `timediff_sentinels: ["9999","9999.0"]` / `harontimel3_sentinels` | exact（自己拡張） |
| `scripts/run_feature_build.py` | script / orchestration | file I/O（persist→manifest 順序） | 同ファイル内 L201-228 既存 `write_manifest`/`persist_category_maps` 箇所 | exact（自己拡張） |
| `tests/features/test_builder.py` | test / unit | registry parity assertion | 同ファイル内 `test_registry_rolling_systems_match_rolling_impl` / `test_no_timediff_babacd_in_registry_or_rolling` / `test_no_registered_feature_column_all_nan_end_to_end` | exact（自己拡張） |
| `tests/features/test_allowlist.py` | test / unit | feature presence assertion | 同ファイル内 `test_history_allowed_rolling_present` | exact（自己拡張） |
| `tests/features/test_snapshot_repro.py` | test / unit | byte-reproducibility assertion | 同ファイル内 `_build_synthetic_matrix`（L25 `rolling_timediff_mean_5` 参照） | exact（自己拡張） |

備考: 本フェーズは既存 Phase 3 framework の拡張のみで新規ファイルゼロ。よって全 analog が「同一ファイル内の既存 idiom」となり、下記 Pattern Assignments は拡張ポイント単位で excerpt を示す。

---

## Pattern Assignments

### `src/etl/normalize.py` (etl / staging-swap idempotent load)

**Analog:** 同ファイル内 `_UMA_RACE_SELECT_COLUMNS` / `_UMA_RACE_COLUMNS` / `_NORMALIZED_COLUMNS["n_uma_race"]` / `_transform_uma_race_df` / `_RACE_SELECT_COLUMNS` / `_RACE_COLUMNS` / `_NORMALIZED_COLUMNS["n_race"]`

**拡張ポイント A — n_uma_race 側に timediff 追加（raw varchar pass-through・Phase 1 idiom）**

`_UMA_RACE_SELECT_COLUMNS`（L187-231）に `"timediff"` を1項目追加する。analog は既存 `harontimel3` / `harontimel4` 等（raw からの varchar/real pass-through・L220-221）。

```python
# 既存（normalize.py L219-221・analog・raw からの数値系 real カラム SELECT）:
    "time",
    "harontimel3",
    "harontimel4",
```

`_UMA_RACE_COLUMNS`（DDL・L88-134）に `"timediff varchar(4)"` を追加。analog は `dmkubun varchar(1)` 等の varchar 定義（L133）。

```python
# 既存（normalize.py L133-134・analog・varchar DDL）:
    "dochakukubun varchar(1)",
    "kyakusitukubun varchar(1)",
    "dmkubun varchar(1)",
```

`_NORMALIZED_COLUMNS["n_uma_race"]`（L472-516）にも同位置に `"timediff"` を追加（SELECT/DDL/INSERT の3者はこの dict と list で整合・staging-swap の `columns` 引数に渡る・L601 `inserted = _idempotent_load(wcur, "n_uma_race", rows, cols)`）。

**Anti-Pattern（Pitfall 2 of RESEARCH.md）:** `_transform_uma_race_df`（L316-350）の `real_cols`（L345: `["futan", "time", "harontimel3", "harontimel4", "odds"]`）には timediff を**追加しない**。varchar のまま保持し、消費側（builder）で parse + sentinel 処理（Pitfall 1 of RESEARCH.md 参照）。

**拡張ポイント B — n_race 側に baba3 カラム追加（raw varchar pass-through）**

`_RACE_SELECT_COLUMNS`（L162-175）に `"sibababacd"`, `"dirtbabacd"`, `"trackcd"` を追加。analog は `syubetucd` / `gradecd` 等（L173-174・varchar SELECT）。

`_RACE_COLUMNS`（DDL・L59-86）に `"sibababacd varchar(1)"`, `"dirtbabacd varchar(1)"`, `"trackcd varchar(2)"` を追加。analog は `syubetucd varchar(2)` / `gradecd varchar(1)`（L73-74）。

`_NORMALIZED_COLUMNS["n_race"]`（L448-471）にも3カラム追加。

**Core: staging-swap idempotent load（不変・再利用）**

`_idempotent_load`（L356-441）は変更不要。advisory lock → CREATE staging → TRUNCATE → INSERT → rowcount verify → DROP/RENAME swap → GRANT。`run_normalized_etl`（L624-663）も entry point のまま（timediff/baba3 は既存の `rows = _df_to_tuples(transformed, cols, table=...)` で自動的に流通・L595/L614）。

**Error handling（CR-04 既存）**: 空入力拒否（L398-402）・`executemany` rowcount verify（L425-430）は baba3/timediff 追加で影響なし。

---

### `src/features/rolling.py` (feature / per-observation latest-K rolling)

**Analog:** 同ファイル内 既存 `jyocd` 系統（D-02 序数扱いの直接前例）＋ `kakuteijyuni`/`harontimel3` 等の数値系 6系統

**拡張ポイント A — `_ROLLING_SYSTEMS` 6→8系統**

L63-70 に `"timediff"`, `"babacd"` を追加。

```python
# 既存（rolling.py L63-70・analog）:
_ROLLING_SYSTEMS: tuple[str, ...] = (
    "kakuteijyuni",
    "harontimel3",
    "jyuni3c_jyuni4c",
    "kyori",
    "jyocd",
    "days_since_prev",
)
```

**拡張ポイント B — `_SYSTEM_SOURCE` に source 追加**

L74-81 に `"timediff": ("timediff",)`, `"babacd": ("babacd",)` を追加。analog は `jyocd`（L79: `"jyocd": ("jyocd",)`・単一 source 列の最も単純な系統）。`babacd` は builder で trackcd 分岐派生された history 列名を指す点のみ差異。

```python
# 既存（rolling.py L74-81・analog・jyocd が babacd の最も近い前例・序数コード数値扱い・D-02）:
_SYSTEM_SOURCE: dict[str, tuple[str, ...]] = {
    "kakuteijyuni": ("kakuteijyuni",),
    "harontimel3": ("harontimel3",),
    "jyuni3c_jyuni4c": ("jyuni3c", "jyuni4c"),
    "kyori": ("kyori",),
    "jyocd": ("jyocd",),
    "days_since_prev": ("days_since_prev",),
}
```

`build_rolling_features`（L84-280）のループ（L218-279）は `_ROLLING_SYSTEMS` を iterate するため、系統追加だけで mean/latest/sd/count の 4軸が自動生成される。`jyuni3c_jyuni4c` 合成分岐（L224-229）以外の系統は `pd.to_numeric(recent[system], errors="coerce")`（L232）で処理されるため、timediff（real parse 済み）も babacd（序数コード varchar→to_numeric で int 化）も同一 path で処理可能。

**拡張ポイント C — WR-03 vector化（count 計算のみ・SHA256 維持）**

L236-240 の `groupby().apply(lambda)` を vector化する。

```python
# 変更前（rolling.py L236-240・WR-03 対象）:
        count_per_obs = (
            recent_sys.groupby("obs_id")["_sys_value"]
            .apply(lambda s: int(s.notna().sum()))
            .to_dict()
        )

# 変更後（推奨・RESEARCH.md「WR-03 vector化」節に基づく）:
        count_per_obs = (
            recent_sys["_sys_value"].notna()
            .groupby(recent_sys["obs_id"]).sum().astype(int).to_dict()
        )
```

mean（L242-244）/ latest（L246-248）/ sd（L250-252）は既に vector化されているため**維持**。これらを触ると SHA256 drift（Pitfall 4 of RESEARCH.md）。

---

### `src/features/availability.py` (feature / registry constant parity)

**Analog:** 同ファイル内 `_ROLLING_SYSTEMS_FOR_RESERVED`（L130-137）/ `HISTORY_ALLOWED_POST_RACE_COLUMNS`（L90-99）

**拡張ポイント A — `_ROLLING_SYSTEMS_FOR_RESERVED` 6→8系統**

L130-137 に `"timediff"`, `"babacd"` を追加。rolling.py の `_ROLLING_SYSTEMS` と**順序含め完全一致**が必要（`test_registry_rolling_systems_match_rolling_impl` が `tuple(...) == _ROLLING_SYSTEMS` で検査・builder test L204-206）。

```python
# 既存（availability.py L130-137・analog）:
_ROLLING_SYSTEMS_FOR_RESERVED: tuple[str, ...] = (
    "kakuteijyuni",
    "harontimel3",
    "jyuni3c_jyuni4c",
    "kyori",
    "jyocd",
    "days_since_prev",
)
```

**拡張ポイント B — `HISTORY_ALLOWED_POST_RACE_COLUMNS`（確認のみ・既存）**

L90-99 を確認。`timediff`（L92）と `babacd`（L97）は**既に含まれている**（Phase 3 当初の宣言通り・CR-01 03-05 で rolling 系統だけ削除し taxonomy は保持）。変更不要。`_RESERVED_NON_FEATURE_COLUMNS`（L143-159）の `{f"rolling_{sys}_count_5" for sys in _ROLLING_SYSTEMS_FOR_RESERVED}` 内包表記は、`_ROLLING_SYSTEMS_FOR_RESERVED` 拡張で自動的に `rolling_timediff_count_5` / `rolling_babacd_count_5` が reserved に追加される（手動編集不要）。

**Anti-Pattern:** `TARGET_OBS_BANNED_COLUMNS`（L79-88）に `sibababacd` / `dirtbabacd`（L84-85）が含まれるのは正しい（target race 当日の馬場＝禁止）。**babacd は history_allowed_post_race のみ**（HIGH #4 taxonomy）。babacd を TARGET_OBS_BANNED に混入させないこと。

---

### `src/features/builder.py` (feature / orchestration + derived columns)

**Analog:** 同ファイル内 `_construct_derived_columns`（L163-205・`race_nkey`/`as_of_datetime`/`days_since_prev` 派生前例）＋ `_HISTORY_DB_SELECT_COLUMNS` / `_DERIVED_HISTORY_COLUMN_NAMES`

**拡張ポイント A — history SELECT に timediff_raw / baba3 追加（BUG A fix 踏襲）**

L74-89 `_HISTORY_DB_SELECT_COLUMNS` に以下を追加:
- `"ur.timediff AS timediff_raw"`（raw カラム名は `timediff`・派生名 `timediff` と衝突回避のため alias `timediff_raw`・Pitfall 3 of RESEARCH.md）
- `"nr.sibababacd AS sibababacd"`, `"nr.dirtbabacd AS dirtbabacd"`, `"nr.trackcd AS trackcd"`（baba3・history 側）

```python
# 既存（builder.py L74-89・analog・history SELECT 構造）:
_HISTORY_DB_SELECT_COLUMNS: tuple[str, ...] = (
    "ur.kettonum AS kettonum",
    "ur.year AS year",
    "ur.jyocd AS jyocd",
    "ur.kaiji AS kaiji",
    "ur.nichiji AS nichiji",
    "ur.racenum AS racenum",
    "nr.race_date AS race_date",
    "nr.race_start_datetime AS race_start_datetime",
    "ur.kakuteijyuni AS kakuteijyuni",
    "ur.harontimel3 AS harontimel3",
    "ur.jyuni3c AS jyuni3c",
    "ur.jyuni4c AS jyuni4c",
    "ur.jyuni1c AS jyuni1c",
    "nr.kyori AS kyori",
)
```

`_DERIVED_HISTORY_COLUMN_NAMES`（L120-122）には `timediff` / `babacd` は**既に含まれている**（L121）。これにより `_HISTORY_SELECT_COLUMNS`（L123-125）は derived 名を除外するため、`timediff_raw` と baba3 のみが SELECT 後の frame に残り、`_construct_derived_columns` で派生される。L213-215 の `assert TARGET_OBS_BANNED_COLUMNS.isdisjoint(_HISTORY_SELECT_COLUMNS)` が、sibababacd/dirtbabacd（TARGET_OBS_BANNED・L84-85）を機械的に弾くため**注意**: babacd の派生は `sibababacd`/`dirtbabacd`/`trackcd` を SELECT した上で `trackcd` 分岐で行う。`babacd` 自体は `_DERIVED_HISTORY_COLUMN_NAMES` にあるため SELECT しない（HIGH #4 通り）。

**拡張ポイント B — `_construct_derived_columns` に timediff parse / babacd 派生ロジック追加**

L163-205 内（`with_days_since_prev` 等と同列）に timediff/babacd 派生を追加。RESEARCH.md「Pattern 1」節のコード例（L211-246）を踏襲。

timediff parse analog は normalize.py の `_safe_parse_hassotime`（L246-271・`series.map(_parse)` idiom・Warning #5 sentinel fallback）と同思想だが、builder 側は vector 化（`pd.to_numeric` + `str.slice`）。sentinel 集合は `label_spec.timediff_sentinels`（現在 `["9999","9999.0"]`・label_spec.yaml L130）に `"0000"`/`"0000.0"` を追加するか、builder 側で定数 `frozenset({"0000","9999"})` を持つ（Open Q1・推奨: builder 側定数）。

babacd trackcd 分岐 analog は存在しない（新規パターン）が、`make_race_nkey`（L136-160・`astype(str).str.zfill()` の vector 化文字列処理）が最も近い pandas vector 化前例。RESEARCH.md「babacd trackcd 分岐派生」節（L339-368）のコード例を実装。

```python
# Analog（builder.py L192-204・days_since_prev 派生・同じ _construct_derived_columns 内）:
    if (
        with_days_since_prev
        and "kettonum" in result.columns
        and "race_date" in result.columns
        and "days_since_prev" not in result.columns
    ):
        rd = pd.to_datetime(result["race_date"])
        ordered = result.sort_values(["kettonum", "race_date"]).copy()
        ordered["_rd"] = rd.loc[ordered.index]
        ordered["days_since_prev"] = (
            ordered.groupby("kettonum")["_rd"].diff().dt.total_seconds() / 86400.0
        )
        result["days_since_prev"] = ordered["days_since_prev"].reindex(result.index)
    return result
```

timediff/babacd 派生は同じ `if "X_raw" in result.columns and "X" not in result.columns:` guard（L181・L189・L192 と同一 idiom）で挿入。

**拡張ポイント C — WR-01' fail-loud（L353-354 silent no-filter fallback）**

L347-354 の `else: pit_filtered_style = expanded_style` を fail-loud に変更。

```python
# 変更前（builder.py L348-354・WR-01' 対象）:
        if "as_of_datetime" in expanded_style.columns:
            pit_filtered_style = expanded_style[
                expanded_style["as_of_datetime"]
                < expanded_style["feature_cutoff_datetime"]
            ].copy()
        else:
            pit_filtered_style = expanded_style

# 変更後（推奨・advisory hardening todo WR-01'）:
        if "as_of_datetime" not in expanded_style.columns:
            raise ValueError(
                "history に as_of_datetime 列が無い・PIT filter を適用できない "
                "(WR-01' fail-loud・将来 refactor での silent leak 防止)"
            )
        pit_filtered_style = expanded_style[
            expanded_style["as_of_datetime"]
            < expanded_style["feature_cutoff_datetime"]
        ].copy()
```

**拡張ポイント D — WR-02 fail-loud（チョークポイント検出・build_feature_matrix で検知）**

`_fetch_feature_sources`（L455-457）と `_fetch_history`（L485-487）の `except Exception → 空 DF 返却` は**維持**（advisory hardening todo D-04「チョークポイント検出」採用）。代わりに `build_feature_matrix` 内の呼出点（L297 `feature_matrix = _fetch_feature_sources(read_pool)` と L322 `history = _fetch_history(read_pool)`）の直後で「空結果/全NaN」を fail-loud 検知するチェックを追加。

```python
# Analog（builder.py L378-381・既存の canonical key assert・同じ build_feature_matrix 内 fail-loud idiom）:
    if "race_nkey" in feature_matrix.columns:
        n_groups = feature_matrix.groupby(["race_nkey", "kettonum"]).ngroups
        assert n_groups == len(feature_matrix), (
            f"feature matrix の (race_nkey, kettonum) が一意でない: ..."
        )

# 拡張例（L297 直後・WR-02 チョークポイント）:
    if len(feature_matrix) == 0:
        raise RuntimeError(
            "feature source fetch が空結果を返した・DB例外/0行結果の silent empty を検知 (WR-02)"
        )
```

history 側（L322 直後）も同様（ただし history=0 は新馬のみ observation で正当なので、feature_matrix 側の全NaN 検知と組み合わせる）。

---

### `src/config/feature_availability.yaml` (config / registry)

**Analog:** 同ファイル内 `rolling_jyocd_*` / `rolling_kakuteijyuni_*` の3軸エントリ（L174-195 / L272-293）

**拡張ポイント A — rolling_timediff_* 復元（L197-198 placeholder を置換）**

L197-198 のコメントプレースホルダを削除し、`rolling_jyocd_*`（L272-293）と同一 schema の3エントリを追加。`feature_group` は `rolling_timediff`、`source_role` は `history_allowed_post_race`。

```yaml
# Analog（feature_availability.yaml L272-293・rolling_jyocd 3軸・完全同一 schema）:
  # --- rolling_jyocd (過去走出走競馬場) ---
  - feature_name: rolling_jyocd_mean_5
    feature_group: rolling_jyocd
    available_from_timing: entry_confirmed
    source_role: history_allowed_post_race
    source_table: normalized.n_uma_race (history)
    cutoff_rule: "race_date - 1 day (strict < cutoff, JST midnight)"
    leakage_risk_level: low
  - feature_name: rolling_jyocd_latest_5
    feature_group: rolling_jyocd
    available_from_timing: entry_confirmed
    source_role: history_allowed_post_race
    source_table: normalized.n_uma_race (history)
    cutoff_rule: "race_date - 1 day (strict < cutoff, JST midnight)"
    leakage_risk_level: low
  - feature_name: rolling_jyocd_sd_5
    feature_group: rolling_jyocd
    available_from_timing: entry_confirmed
    source_role: history_allowed_post_race
    source_table: normalized.n_uma_race (history)
    cutoff_rule: "race_date - 1 day (strict < cutoff, JST midnight)"
    leakage_risk_level: low
```

`rolling_timediff_{mean,latest,sd}_5` / `rolling_babacd_{mean,latest,sd}_5` 計6エントリを `rolling_jyocd_*` と完全同一 schema で記述する。`source_table: normalized.n_uma_race (history)` は babacd も同一（派生元は n_race だが rolling source は history frame 上の派生列）。

**拡張ポイント B — rolling_babacd_* 復元（L269-270 placeholder を置換）**

同上。`feature_group: rolling_babacd`。

---

### `src/config/label_spec.yaml` (config / sentinel-spec)

**Analog:** 同ファイル内 `timediff_sentinels`（L130）/ `harontimel3_sentinels`（L128）

**拡張ポイント A — timediff_sentinels に "0000" 追加**

L130 `timediff_sentinels: ["9999", "9999.0"]` に `"0000"`, `"0000.0"` を追加。

```yaml
# 既存（label_spec.yaml L128-130・analog・sentinel 集合の宣言的定義）:
  harontimel3_sentinels: ["999", "999.0", "999.00"]
  # タイム差異常 sentinel（同上）
  timediff_sentinels: ["9999", "9999.0"]

# 変更後（推奨・実データ検証で 0000 は kakuteijyuni='00' と完全一致・376件）:
  timediff_sentinels: ["9999", "9999.0", "0000", "0000.0"]
```

注: この sentinel 集合は label 層（`se_marker_canonicalization`）用。rolling 層の timediff parse は builder 側で別途定数を持つ（Open Q1 推奨）。両者で `"0000"`/`"9999"` を含む点は整合。

---

### `scripts/run_feature_build.py` (script / file I/O orchestration)

**Analog:** 同ファイル内 L201-228 の `write_manifest` → `persist_category_maps` 順序

**拡張ポイント A — CR-01新: persist→manifest 順序化**

L201-228 を以下の順序に変更:
1. persist_category_maps（L227-228 を前に移動）
2. `assert category_map_path.exists()`（新規追加）
3. write_manifest（L206-223）

```python
# 既存（run_feature_build.py L201-228・CR-01新 対象箇所・manifest→persist の順序）:
        parquet_path = _SNAPSHOTS_DIR / f"feature_matrix_{args.snapshot_id}.parquet"
        manifest_path = _SNAPSHOTS_DIR / f"feature_matrix_{args.snapshot_id}.manifest.yaml"
        category_map_artifact = f"snapshots/category_map_{args.snapshot_id}.json"

        write_manifest(
            manifest_path,
            ...
            category_map_artifact=category_map_artifact,
            ...
        )
        logger.info("manifest written: %s", manifest_path)

        # --- Step 6: frozen category map artifact 永続化 ...
        category_map_path = _SNAPSHOTS_DIR / f"category_map_{args.snapshot_id}.json"
        persist_category_maps(frozen_maps, category_map_path)
```

変更後は persist を先に実行し `category_map_path` の存在を assert してから manifest を書く。`category_map_artifact`（L204）は既に repo-root 相対（`snapshots/category_map_*.json`）なので CWD 依存性は無し（advisory todo CR-01新 の副次要件は既に満たしている・要確認のみ）。

**Error handling:** persist 失敗時は例外で manifest 書出前に停止（advisory hardening todo Pitfall 5・partial-failure 状態回避）。

---

### `tests/features/test_builder.py` (test / unit)

**Analog:** 同ファイル内 `test_registry_rolling_systems_match_rolling_impl`（L180-206）/ `test_no_timediff_babacd_in_registry_or_rolling`（L209-240）/ `test_no_registered_feature_column_all_nan_end_to_end`（L103-177）

**拡張ポイント A — `test_registry_rolling_systems_match_rolling_impl`（6→8 自動追従・変更不要の可能性大）**

L180-206 の test は `_ROLLING_SYSTEMS` と registry と `_ROLLING_SYSTEMS_FOR_RESERVED` の一致を検査するため、3者全てに timediff/babacd を追加すれば本 test は自動的に GREEN になる。**変更不要**。

**拡張ポイント B — `test_no_timediff_babacd_in_registry_or_rolling`（反転・存在 assert 化）**

L209-240 を「含まれない」から「含まれる」に反転。

```python
# 既存（test_builder.py L220-240・反転対象・「not in」assert）:
    assert "timediff" not in _ROLLING_SYSTEMS, (
        "rolling.py に timediff が残存（CR-01 違反）"
    )
    assert "babacd" not in _ROLLING_SYSTEMS, (...)
    ...
    assert leaked == [], (
        f"registry に rolling_timediff_* / rolling_babacd_* が残存: {leaked} (CR-01 違反)"
    )

# 変更後（存在 assert 化・関数名も test_timediff_babacd_present_in_registry_or_rolling 等に rename）:
    assert "timediff" in _ROLLING_SYSTEMS, "rolling.py に timediff が無い（Phase 3.1 復元違反）"
    assert "babacd" in _ROLLING_SYSTEMS, "rolling.py に babacd が無い（Phase 3.1 復元違反）"
    ...
    required = {"rolling_timediff_mean_5", "rolling_babacd_mean_5",
                "rolling_timediff_latest_5", "rolling_babacd_latest_5",
                "rolling_timediff_sd_5", "rolling_babacd_sd_5"}
    missing = required - set(feats)
    assert missing == set(), f"registry に rolling_timediff_*/rolling_babacd_* が未復元: {missing}"
```

**拡張ポイント C — `test_no_registered_feature_column_all_nan_end_to_end`（fixture 拡張）**

L139-153 の `history_rows` に timediff と baba3（sibababacd/dirtbabacd/trackcd）のデータを追加。analog は既存 `kakuteijyuni`/`harontimel3`/`jyocd` 等（L148-151）。test の assert 部（L165-177）は rolling_mean_features 全てが 100% NaN で無いことを検査するため、`rolling_timediff_mean_5`/`rolling_babacd_mean_5` が registry に再登録されると自動的に検査対象に加わる。

```python
# 既存（test_builder.py L142-152・analog・合成 history fixture）:
        for day_offset, val in [(-2, 1), (-3, 2), (-4, 3), (-5, 4)]:
            hr = obs_rd + pd.Timedelta(days=day_offset)
            history_rows.append({
                "kettonum": kn,
                "race_date": hr,
                "as_of_datetime": hr,
                "race_start_datetime": hr + pd.Timedelta(hours=12),
                "kakuteijyuni": val, "harontimel3": 36.0 + val,
                "jyuni3c": val, "jyuni4c": val, "jyuni1c": 0,
                "kyori": 1600, "jyocd": "05",
                "days_since_prev": float(abs(day_offset)),
            })

# 追加項目（timediff_raw + baba3・派生を経由して rolling source になる）:
                "timediff_raw": f"+{val:03d}",   # 例: "+001" → 0.1秒
                "sibababacd": "1",               # 芝・良馬場
                "dirtbabacd": "0",
                "trackcd": "10",                 # 第1桁 1=芝
```

ただし timediff/babacd は `_construct_derived_columns` で派生されるため、合成 history が `build_rolling_features` 直接呼出（L155）の場合は派生ロジックを test 内で適用するか、`build_feature_matrix` 経由に切り替える必要がある（planner 判断・analog は L155 `builder.build_rolling_features(observations, history)`）。

**拡張ポイント D — WR-01' / WR-02 用新規 unit test 追加**

- WR-01': `as_of_datetime` 列を持たない合成 history で `build_feature_matrix` が `ValueError` で fail することを assert。
- WR-02: `_fetch_feature_sources` が空 DF を返す状態（mock で DB 例外を発生）で `build_feature_matrix` が `RuntimeError` で fail することを assert。

---

### `tests/features/test_allowlist.py` (test / unit)

**Analog:** 同ファイル内 `test_history_allowed_rolling_present`（L88-107）

**拡張ポイント A — timediff/babacd 存在 assert に切替**

L101-107 の「`rolling_timediff_mean_5` / `rolling_babacd_mean_5` が無いこと」assert を「存在すること」assert に反転。

```python
# 既存（test_allowlist.py L101-107・反転対象）:
    # CR-01 (03-05): 削除済みの timediff/babacd rolling が registry に無いことも防御的に確認
    for removed in (
        "rolling_timediff_mean_5", "rolling_babacd_mean_5",
    ):
        assert removed not in names, (
            f"{removed} が registry に残存（CR-01 03-05 で削除済み・Phase 3.1 で再登録予定）"
        )

# 変更後（Phase 3.1 で存在 assert に切替）:
    # Phase 3.1: rolling_timediff_* / rolling_babacd_* は registry に存在する（復元済み）
    for restored in (
        "rolling_timediff_mean_5", "rolling_babacd_mean_5",
        "rolling_timediff_latest_5", "rolling_babacd_latest_5",
        "rolling_timediff_sd_5", "rolling_babacd_sd_5",
    ):
        assert restored in names, (
            f"{restored} が registry に無い（Phase 3.1 復元違反）"
        )
```

---

### `tests/features/test_snapshot_repro.py` (test / unit)

**Analog:** 同ファイル内 `_build_synthetic_matrix`（L20-26）

**拡張ポイント A — L25 stale `rolling_timediff_mean_5` 参照整理**

L25 の `"rolling_timediff_mean_5": [-2.0]` は、Phase 3 03-05 で当該 feature が registry から削除された後も test 内に残存していた stale 参照。Phase 3.1 で feature が復元されるため stale ではなくなるが、byte-repro test（L32-44）は列の値ではなく SHA256 一致を検査するだけなので、値の調整は任意。

```python
# 既存（test_snapshot_repro.py L20-26・stale 参照・変更は任意）:
def _build_synthetic_matrix() -> pd.DataFrame:
    return pd.DataFrame({
        "race_nkey": ["2023A0610-R1"],
        "kettonum": [1001],
        "race_date": [pd.Timestamp("2023-06-04")],
        "rolling_timediff_mean_5": [-2.0],
    })
```

整理案: コメントで「Phase 3.1 で rolling_timediff_mean_5 は復元されたため stale ではなくなった」旨を追記、または他の rolling feature（例: `rolling_kakuteijyuni_mean_5`）に置換して時系列の意図を明示。test 本体（L32-110）は SHA256 一致検査のみで列名に依存しないため、任意の調整で GREEN を維持。

---

## Shared Patterns

### 3者 parity（registry ↔ rolling.py ↔ availability.py）
**Source:** `src/features/rolling.py` L63-81 / `src/features/availability.py` L130-159 / `src/config/feature_availability.yaml` L164-316
**Apply to:** rolling.py と availability.py と registry の3箇所全てに同系統を追加（順序含め一致）。機械保証は `tests/features/test_builder.py::test_registry_rolling_systems_match_rolling_impl`（L180-206）。

### raw varchar pass-through + 消費側 cast（Phase 1 idiom）
**Source:** `src/etl/normalize.py` L316-350（`_transform_uma_race_df`）/ `src/features/builder.py` L490-503（`_cast_static_columns`）
**Apply to:** timediff/baba3 の normalized への取り込み。DDL は varchar(4)/varchar(1)/varchar(2) で保持し、cast は builder 側。ETL 側の `_transform_uma_race_df` の `real_cols`/`int_cols` には追加しない。

### 派生 history カラム（DB 非実在・pandas 派生）
**Source:** `src/features/builder.py` L120-205（`_DERIVED_HISTORY_COLUMN_NAMES` / `_construct_derived_columns`・`race_nkey`/`as_of_datetime`/`days_since_prev` 前例）
**Apply to:** timediff（varchar→real parse・sentinel NaN 化）と babacd（trackcd 第1桁分岐派生）。両者とも `_DERIVED_HISTORY_COLUMN_NAMES` に既に含まれているため、history SELECT で alias（`timediff_raw`）を使い、`_construct_derived_columns` で派生。

### silent fallback 禁止（D-13 / `__MISSING__` sentinel）
**Source:** `src/features/rolling.py` L174（`MISSING` 初期化）/ `src/features/builder.py` L364-367（`estimated_running_style` の `__MISSING__` fillna）
**Apply to:** timediff sentinel（`0000`/`9999`）→ NaN 化、babacd 派生不能時（trackcd 欠損/曖昧/dirtbabacd='0'）→ `__MISSING__` sentinel。silent NaN fill は禁止。

### staging-swap idempotent load（raw 不変性）
**Source:** `src/etl/normalize.py` L356-441（`_idempotent_load`）/ L624-663（`run_normalized_etl`）
**Apply to:** timediff/baba3 追加後の normalized ETL 再実行。`_idempotent_load` 自体は変更不要・advisory lock + staging-swap + rowcount verify + GRANT で raw 不変性と idempotency を保証。

### cutoff semantics 不変量（PIT correctness）
**Source:** `src/features/availability.py` L45-56（`CUTOFF_SEMANTICS`）/ `src/features/rolling.py` L49（`assert` で strict_less_than 検査）/ L199-201（pre-filter）
**Apply to:** timediff/babacd も同一 pipeline。`history.as_of_datetime < observation.feature_cutoff_datetime`（strict `<`）で未来情報リークを防止。babacd 派生に使う過去レースの n_race 馬場が cutoff 以前であることは、history 行が cutoff 以前である構造で保証。

---

## No Analog Found

| File | Role | Data Flow | Reason |
|------|------|-----------|--------|
| *(none)* | — | — | 本フェーズは既存 Phase 3 framework の拡張のみで、新規ファイルゼロ。全ファイルに codebase 内 exact analog（同一ファイル内の既存 idiom）が存在する。babacd trackcd 分岐派生のみ新規パターンだが、`make_race_nkey`（vector 化文字列処理）と `_construct_derived_columns`（派生 history カラム）の2つの analog を組み合わせて実装可能（RESEARCH.md「Pattern 1」/「babacd trackcd 分岐派生」節に具体例あり）。 |

---

## Metadata

**Analog search scope:**
- `src/etl/normalize.py`（staging-swap idempotent ETL・517行）
- `src/features/builder.py`（feature orchestration + derived columns・504行）
- `src/features/rolling.py`（per-observation latest-K rolling・281行）
- `src/features/availability.py`（3者 parity registry constants・274行）
- `src/config/feature_availability.yaml`（declarative registry・327行）
- `src/config/label_spec.yaml`（sentinel-spec・257行）
- `scripts/run_feature_build.py`（snapshot orchestration・258行）
- `tests/features/test_builder.py`（unit test・266行）
- `tests/features/test_allowlist.py`（unit test・126行）
- `tests/features/test_snapshot_repro.py`（unit test・110行）
- `.planning/todos/pending/phase3-advisory-hardening.md`（advisory 4件 処方箝・48行）

**Files scanned:** 11（CONTEXT.md/RESEARCH.md の canonical_refs セクションに列挙されたコード契約・テスト契約・advisory 処方箝の全て）
**Pattern extraction date:** 2026-06-19
**Key insight:** 本フェーズは「既存 framework の拡張ポイント追加」が主作業で、新規パターンは babacd trackcd 分岐派生ロジックのみ。timediff parse は `_safe_parse_hassotime`（normalize.py）と `make_race_nkey`（builder.py）の pandas vector 化 idiom を組み合わせる。staging-swap idempotent load / per-observation latest-K rolling / 3者 parity / §12.4 metadata snapshot は全て既存のまま再利用。
