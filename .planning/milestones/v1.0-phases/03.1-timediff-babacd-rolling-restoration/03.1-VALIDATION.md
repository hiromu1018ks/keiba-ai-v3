---
phase: 03.1
slug: timediff-babacd-rolling-restoration
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-19
---

# Phase 03.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Per-Task Verification Map の Task IDs は planner がタスク分解後に設定する。Test Infrastructure / Sampling Rate / Manual-Only は本フェーズの実態で確定済み。

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 9.1.0（uv 管理） |
| **Config file** | `pyproject.toml` (`[tool.pytest.ini_options]`) |
| **Quick run command** | `uv run pytest tests/features/ -q` |
| **Full suite command** | `uv run pytest -q` |
| **Estimated runtime** | ~20-40 秒（quick）／ ~60-120 秒（full・live-DB 参照 test 含む） |

---

## Sampling Rate

- **After every task commit:** Run `uv run pytest tests/features/ -q`
- **After every plan wave:** Run `uv run pytest -q`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 120 秒

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD (planner 設定) | — | — | FEAT-01 | — | リーク防止（strict `< cutoff`・PIT 派生）・silent fallback 禁止 | unit | `uv run pytest tests/features/test_builder.py::test_registry_rolling_systems_match_rolling_impl -q` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*
*主要 test 関数（planner が各 task に割当）: `test_registry_rolling_systems_match_rolling_impl`（3者 parity 6→8）・`test_no_registered_feature_column_all_nan_end_to_end`（silent-NaN regression guard・fixture 拡張）・`test_no_timediff_babacd_in_registry_or_rolling`（反転/削除）・`test_history_allowed_rolling_present`（存在 assert に切替）・`test_snapshot_repro.py`（stale `rolling_timediff_mean_5` 参照整理）*

---

## Wave 0 Requirements

- [ ] `tests/features/test_builder.py` — 3者 parity test を6→8系統化・silent-NaN guard の fixture 拡張
- [ ] `tests/features/test_allowlist.py` — `test_history_allowed_rolling_present` を timediff/babacd 存在 assert に切替
- [ ] `tests/features/test_snapshot_repro.py` — stale `rolling_timediff_mean_5` 参照整理

*既存の pytest 9.1.0 インフラで cover されるため、新規 framework install は不要。*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| live snapshot rebuild（554,267行×列拡張）+ byte-reproducibility + registry↔Parquet parity | FEAT-01 (SC#3/SC#4) | live-DB（PostgreSQL readonly）への snapshot rebuild は許可済み human-verify checkpoint・SHA256 byte-repro の目視確認が必要 | `scripts/run_feature_build.py` で live snapshot rebuild → 新 snapshot の SHA256・feature_count=24・6 feature 列 non-null を確認・`test_snapshot_repro` GREEN |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
