---
phase: 04-model-prediction
plan: 06
type: execute
wave: 5
depends_on: [04-05]
files_modified:
  - .planning/ROADMAP.md
  - .planning/phases/04-model-prediction/04-VALIDATION.md
autonomous: true
requirements: [MODL-01, MODL-02, MODL-03]
tags: [phase-04, verification, sc3-leak-green, sc4-reproduce-green, roadmap-update, hybrid-gate]

must_haves:
  truths:
    - "SC#3 leak diagnostic（test_no_target_encoding_leak）が**対抗的構造診断**（合成データ・低基数 RARE_X + 高基数 _code train-only/test-unseen + 意図的リーク制御 DEMONSTRABLY fail）として GREEN になり・target encoding 非混入を構造実証する（review HIGH#3: live-data 証明と称さず対抗的構造診断と正確に呼ぶ・test_no_target_encoding_leak は合成データ）"
    - "SC#4 reproduce smoke（test_reproduce_bit_identical + run_train_predict --check-reproduce）が両モデルで GREEN になり固定 seed bit-identical 再現性を実証する（per SC#4 構造的ブロック・Phase 1 D-01/Phase 2 D-02 hybrid gate・review HIGH#7 固定 thread/as_of_datetime）"
    - "uv run python scripts/run_train_predict.py --model-type both --check-reproduce が両モデルで完了し reports/04-eval.md + reports/04-eval.json と prediction.fukusho_prediction（両モデル・model_version スコープ）と models/{version}/ artifact（base+calibrator 分離）を生成する"
    - "最終検証が `KEIBA_SKIP_DB_TESTS` を unset で実行され・Phase 4 critical テスト（test_idempotent_checksum_match / test_model_version_scoped_swap_preserves_other_models / test_market_data_source 等 requires_db マーク）の skipped count == 0 を確認する（review HIGH#10: green-by-skip 防止）"
    - "SC#2 比較表が生成されるだけでなく・主モデル（LightGBM/CatBoost）が baselines（BL-1..5）を**具体的な評価指標（Brier/LogLoss/Calibration 成分）で上回るか否か**を明示する・上回らない場合は『SC#2 比較表生成済み・AI 付加価値未証明』と Phase 4 完了を過大請求せず注記する（review HIGH#8: 比較表存在 ≠ model adds value）"
    - "全 test スイート（tests/ 全体）が GREEN で Phase 4 完了を宣言できる状態になる"
    - "ROADMAP.md の Phase 4 エントリが Plans リスト（01..06）と成果物で更新され・SC#1-#4 の達成が明記される・SC#2 は『比較表生成 + 主モデルが baselines を上回るか否か』の2要素を分けて記載（review HIGH#8）"
    - "04-VALIDATION.md に各テストのコマンド・終了コード・予測 checksum・artifact hash・行数の証拠が格納される（review MEDIUM: 手動更新でなく実行証拠）"
  prohibitions:
    - "KEIBA_SKIP_DB_TESTS=1 で critical テストが skip されたまま最終ゲート GREEN を宣言することは許容しない（review HIGH#10）"
    - "SC#3 leak diagnostic を live-data 証明と称することは許容しない（review HIGH#3: 対抗的構造診断と正確に呼ぶ）"
    - "SC#2 比較表が生成されただけで『AI モデルが付加価値を持つ』と宣言することは許容しない（review HIGH#8: 具体的指標で baselines 上回るか検証・未証明なら注記）"
    - "テスト実行時間 <120s を満たすために critical テストを縮小/skip することは許容しない（review MEDIUM: 実行時間は参考指標・critical テストは縮小不可）"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4 Plans リスト（01..06）+ 成果物 + SC#1-#4 達成記載"
      contains: "04-06-PLAN.md"
    - path: ".planning/phases/04-model-prediction/04-VALIDATION.md"
      provides: "nyquist_compliant: true・wave_0_complete: true・全 test status GREEN"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "scripts/run_train_predict.py --check-reproduce"
      to: "SC#4 構造的ブロック"
      via: "両モデル bit-identical 実証・失敗時 sys.exit(1)"
      pattern: "check-reproduce"
    - from: "tests/model/test_trainer.py::test_no_target_encoding_leak"
      to: "SC#3 構造的ブロック"
      via: "target encoding 非混入の対抗的実証（GREEN = 構造保証）"
      pattern: "test_no_target_encoding_leak"
---

<objective>
Phase 4 の最終検証と完了宣言: (a) scripts/run_train_predict.py --model-type both --check-reproduce を実データで実行し両モデル（LightGBM/CatBoost）の学習→キャリブレーション→予測→artifact 保存→prediction.fukusho_prediction 書込→reports/04-eval.md + .json 生成を完結させる、(b) **review HIGH#3 対応: SC#3 leak diagnostic（test_no_target_encoding_leak）を対抗的構造診断（合成データ・live-data 証明と称さない）として GREEN になることを構造的ブロックで確認**、**review HIGH#7 対応: SC#4 reproduce smoke（test_reproduce_bit_identical + --check-reproduce）が固定 thread/as_of_datetime で両モデル bit-identical で GREEN になることを構造的ブロックで確認**（hybrid gate Phase 1 D-01/Phase 2 D-02・失敗時 Phase 完了不可）、**review HIGH#10 対応: KEIBA_SKIP_DB_TESTS を unset で最終検証を実行し critical テストの skipped count == 0 を確認**（green-by-skip 防止）、(c) 全テストスイート（tests/ 全体）が GREEN であることを確認、(d) **review HIGH#8 対応: SC#2 を『比較表生成済み』と『主モデルが baselines を具体的指標で上回るか否か』の2要素に分けて記載**・上回らない場合は『AI 付加価値未証明』と正直に注記し Phase 4 完了を過大請求しない、(e) **review MEDIUM 対応: 04-VALIDATION.md に各テストのコマンド・終了コード・予測 checksum・artifact hash・行数の実行証拠を格納**し nyquist_compliant/wave_0_complete を true に更新、(f) ROADMAP.md の Phase 4 エントリを Plans リスト（01..06）と成果物で更新し SC#1-#4 達成を明記（SC#2 は2要素分離）。

Purpose: Phase 4 の成果物（両モデル予測 + BL 比較表 + リーク防止実証）が本番データで動作することを実証し、Phase 5（EV/backtest）・Phase 6（評価ゲート）・Phase 7（Streamlit）・Phase 8（adversarial audit）への引き渡しを確立する。SC#3/SC#4 は構造的ブロックであり、この PLAN が GREEN でなければ Phase 4 完了を宣言できない。review で指摘された「KEIBA_SKIP_DB_TESTS で green-by-skip」「SC#3 を live-data 証明と過大請求」「SC#2 を比較表存在だけで達成扱い」を実行証拠と正直な注記で防止する。
Output: 実行済み pipeline（reports/04-eval.md + .json + prediction テーブル両モデル + models/ artifact）・GREEN 全テスト（skip なし）・更新済み ROADMAP/VALIDATION（実行証拠付き）
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-model-prediction/04-CONTEXT.md
@.planning/phases/04-model-prediction/04-RESEARCH.md
@.planning/phases/04-model-prediction/04-VALIDATION.md

@scripts/run_train_predict.py
@src/model/calibrator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 実データで run_train_predict.py --model-type both --check-reproduce を実行し SC#3/SC#4 構造的ブロック GREEN を確認</name>
  <files>.planning/phases/04-model-prediction/04-VALIDATION.md</files>
  <read_first>
    - scripts/run_train_predict.py（PLAN 05: --check-reproduce / --model-type both の実行方法）
    - .planning/phases/04-model-prediction/04-VALIDATION.md（Per-Task Verification Map・13 test の status 更新対象）
    - .planning/phases/04-model-prediction/04-RESEARCH.md D-05（SC#3 leak diagnostic）・D-06（SC#4 reproduce smoke）・SC#3/SC#4 構造的ブロックの定義
    - .planning/STATE.md（Phase 4 status 更新対象・PLAN 01 で postreview-v2 ドリフト修正済み）
  </read_first>
  <action>
    1. 【前提確認】Wave 1-4 で全 PLAN が完了していること: 依存ライブラリ pin・prediction DDL/GRANT・tests/model/ RED→GREEN・data/trainer/orchestrator/calibrator/baseline/predict/evaluator/prediction_load/artifact・run_train_predict.py。`uv run pytest tests/model/ -x -q` が全 GREEN（20+ test）であることを確認。
    2. 【SC#3 leak diagnostic GREEN 確認 — review HIGH#3: 対抗的構造診断と正確に呼ぶ】`uv run pytest tests/model/test_trainer.py::test_no_target_encoding_leak -x -v` を実行。**対抗的構造診断**として: (a) 低基数希少カテゴリ RARE_X で LightGBM + CatBoost の両方の予測が mean（0.21 程度）に縮み `< 0.5` を満たすこと (b) 高基数 _code 列で train-only/test-unseen ID の予測が global mean に縮むこと (c) 意図的 target encoding 制御注入で予測が 0.9 を超える（false-pass でない）こと。GREEN = target encoding 非混入の**構造実証**（live-data 証明と称さない・review HIGH#3）。失敗時は Phase 4 完了不可（構造的ブロック）・原因調査は /gsd-debug で別途。
    3. 【SC#4 reproduce smoke GREEN 確認（unit）— review HIGH#7: 固定 thread/as_of_datetime】`uv run pytest tests/model/test_orchestrator.py::test_reproduce_bit_identical -x -v` を実行。両モデルで seed=42 + num_threads=1/thread_count=1 + as_of_datetime=FIXED の2回 train_and_predict が np.array_equal で bit-identical。GREEN = 固定 seed 再現性の構造実証。
    4. 【実データ pipeline 実行】`uv run python scripts/run_train_predict.py --model-type both --check-reproduce` を実行（D-01 snapshot_id デフォルト = 20260620-1a-postreview-v2・--version-n 1）。完了条件:
       - 両モデル（lightgbm + catboost）の学習が完了（LightGBM .txt / CatBoost .cbm 保存・calibrator.joblib 別途保存）
       - calibrate_model で isotonic（calib sample 24,884 > 1000）適用・calib_method が予測 provenance に記録
       - predict_p_fukusho で test slice（2024-H2）の p_fukusho_hit 算出・provenance 列 NOT NULL・行整列保証（review HIGH#2）
       - artifact 保存: models/20260620-1a-postreview-v2-lgb-v1/{lgb_model.txt, calibrator.joblib, metadata.json} と models/20260620-1a-postreview-v2-cb-v1/{cb_model.cbm, calibrator.joblib, metadata.json}（review HIGH#5 base+calibrator 分離）
       - load_predictions を各 model_type+model_version 単位で呼び prediction.fukusho_prediction に両モデル予測を永続化（model_version スコープ swap・review HIGH#1・他 model 破壊しない・2回実行で checksum 一致をログ確認）
       - evaluate_all_models で reports/04-eval.md + reports/04-eval.json 生成（LightGBM/CatBoost + BL-1..5 比較表・§15.2 sum(p) 診断結果・BL-3 §14.2 注記・BL-4/5 キャリブレーション注記・D-04 Calibration 重視選定基準素材）
       - --check-reproduce で両モデル bit-identical 実証（SC#4 構造的ブロック・固定 as_of_datetime・review HIGH#7）・失敗で sys.exit(1)
    5. 【出力検証】reports/04-eval.md と reports/04-eval.json が存在し比較表が揃う・`psql -c "SELECT model_type, count(*) FROM prediction.fukusho_prediction GROUP BY model_type"` で lightgbm/catboost 両方の行が存在・`ls models/20260620-1a-postreview-v2-lgb-v1/ models/20260620-1a-postreview-v2-cb-v1/` で base ネイティブ + calibrator.joblib + metadata.json が揃う。
    6. 【§15.2 sum(p) 受入基準確認 — review MEDIUM: 診断的扱い】reports/04-eval.md の sum(p) 診断結果を確認: 8頭以上は [2.7,3.3]・5-7頭は [1.8,2.2]。違反 race_key があれば割合と共に記録（Phase 6 ゲートで最終判定・本 PLAN では warning・hybrid gate D-01/D-02 準拠・複勝確率は独立二値のため厳密合計制約でなく診断的・review MEDIUM）。
    7. 【SC#2 主モデル vs baselines 上回り確認 — review HIGH#8】reports/04-eval.md と .json を読み・主モデル（LightGBM/CatBoost）が baselines（BL-1..5）を Brier/LogLoss/Calibration 成分（calibration_max_dev・sum(p) 適合度）で上回るか否かを確認。上回る場合は SC#2 達成を『比較表生成 + AI 付加価値実証（具体的指標）』と記載。**上回らない場合は SC#2 を『比較表生成済み・AI 付加価値未証明（baselines を Brier/LogLoss/Calibration で未上回る）』と正直に注記**し Phase 4 完了を過大請求しない（review HIGH#8・有効な否定的結果として Phase 5/6 に引き渡す）。
    8. 【全テストスイート GREEN 確認 — review HIGH#10: KEIBA_SKIP_DB_TESTS unset で実行】**`unset KEIBA_SKIP_DB_TESTS && uv run pytest` を実行**（tests/ 全体・24 既存 + tests/model/ 新設）。全 GREEN であることを確認・回帰なし。**critical テスト（requires_db マーク・test_idempotent_checksum_match / test_model_version_scoped_swap_preserves_other_models / test_market_data_source 等）の skipped count == 0 を別途確認**（review HIGH#10: green-by-skip 防止）・`uv run pytest -rs` で skipped 理由を表示し critical テストの skip がないことを確認。実行時間は参考指標（<120s 目安・review MEDIUM: critical テストを縮小/skip して満たすことは禁止）。
    9. 【04-VALIDATION.md 更新 — review MEDIUM: 実行証拠格納】Per-Task Verification Map の全 test の Status 列を GREEN（✅）に更新・Task ID 列に PLAN/Task 番号を埋込（PLAN 後割当の解消）。**各 test エントリに実行コマンド・終了コード・（該当すれば）予測 checksum・artifact SHA256・行数の証拠を追記**（review MEDIUM: 手動更新でなく実行証拠・`uv run pytest -v` の出力 / `psql` の count 結果 / `sha256sum models/.../lgb_model.txt` 等を貼付）。frontmatter の `nyquist_compliant: false` → `true`・`wave_0_complete: false` → `true`・`status: draft` → `approved`。**新規フィールド `final_gate_run_with_skip_unset: true` を追加**（review HIGH#10 の証拠）。Validation Sign-Off の6チェック項目に全てチェック。
  </action>
  <verify>
    <automated>unset KEIBA_SKIP_DB_TESTS && uv run pytest tests/model/ -q -rs 2>&1 | tail -10 && test -f reports/04-eval.md && test -f reports/04-eval.json && psql -c "SELECT model_type, count(*) FROM prediction.fukusho_prediction GROUP BY model_type" && uv run python scripts/run_train_predict.py --model-type both --check-reproduce 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/model/test_trainer.py::test_no_target_encoding_leak` が GREEN（SC#3 構造的ブロック・対抗的構造診断・target encoding 非混入実証・review HIGH#3）
    - `uv run pytest tests/model/test_orchestrator.py::test_reproduce_bit_identical` が GREEN（SC#4 構造的ブロック・bit-identical 実証・review HIGH#7）
    - `uv run python scripts/run_train_predict.py --model-type both --check-reproduce` が exit 0（両モデル pipeline 完了・SC#4 構造的ブロック GREEN・固定 as_of_datetime）
    - reports/04-eval.md と reports/04-eval.json が存在し LightGBM/CatBoost/BL-1..5 の比較表 + sum(p) 診断結果を含む
    - **SC#2 の記載が主モデル vs baselines の具体的指標比較を含み**・主モデルが baselines を上回る場合は『AI 付加価値実証』・上回らない場合は『AI 付加価値未証明』と注記される（review HIGH#8）
    - `psql -c "SELECT model_type, count(*) FROM prediction.fukusho_prediction GROUP BY model_type"` が lightgbm と catboost の両方の行数を返す
    - models/20260620-1a-postreview-v2-lgb-v1/ と models/20260620-1a-postreview-v2-cb-v1/ に lgb_model.txt/cb_model.cbm + calibrator.joblib + metadata.json が存在（review HIGH#5）
    - **`unset KEIBA_SKIP_DB_TESTS && uv run pytest` が全 GREEN**（review HIGH#10: skip unset で実行）
    - `uv run pytest -rs` で critical テスト（requires_db マーク）の skipped count == 0（review HIGH#10: green-by-skip 防止）
    - 04-VALIDATION.md の frontmatter が `nyquist_compliant: true` / `wave_0_complete: true` / `status: approved` / `final_gate_run_with_skip_unset: true`（review HIGH#10）
    - 04-VALIDATION.md の Per-Task Map が全 test ✅ GREEN・Task ID 埋込済み・**各 test に実行証拠（コマンド/終了コード/checksum/hash/行数）が格納される**（review MEDIUM）
  </acceptance_criteria>
  <done>実データ pipeline が両モデルで完了・SC#3（対抗的構造診断・review HIGH#3）・SC#4（固定 thread/as_of_datetime bit-identical・review HIGH#7）構造的ブロックが GREEN・reports/04-eval.md + .json + prediction テーブル + artifact が生成・全テスト GREEN（KEIBA_SKIP_DB_TESTS unset・critical skip 0・review HIGH#10）・SC#2 が具体的指標で主モデル vs baselines 比較を含む（review HIGH#8）・VALIDATION 実行証拠更新（review MEDIUM）（Phase 4 完了条件充足）</done>
</task>

<task type="auto">
  <name>Task 2: ROADMAP.md Phase 4 エントリを Plans リスト + 成果物 + SC#1-#4 達成で更新</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md（Phase 4 エントリ現状: Goal/Depends on/Requirements/Success Criteria#1-#4/Plans: TBD）
    - .planning/phases/04-model-prediction/04-01..06-PLAN.md（各 PLAN の objective / must_haves / Artifacts this phase produces）
    - .planning/phases/04-model-prediction/04-CONTEXT.md（D-01..D-08 locked decisions・Phase 4 境界）
    - .planning/STATE.md（Phase 4 status 更新対象）
  </read_first>
  <action>
    1. 【Plans 行更新】ROADMAP.md の Phase 4 エントリの `**Plans**: TBD` を `**Plans**: 6 plans` に更新（Edit scoped replacement・全ファイル Write 不可）。
    2. 【Plan リスト追加】Phase 4 エントリの Plans セクションに以下のリストを追加（未チェック → 全 PLAN 完了後は checked にする・本 PLAN 完了時点で checked）:
       ```
       Plans:
       - [x] 04-01-PLAN.md — 基盤（lightgbm/catboost pin + prediction DDL/GRANT + RED stubs + v3 ドリフト修正）
       - [x] 04-02-PLAN.md — data.py + calibrator.py + artifact.py（SC#1 stamped Parquet・3way split・prefit wrapper）
       - [x] 04-03-PLAN.md — trainer.py + baseline.py（SC#3 leak diagnostic・LightGBM native + CatBoost has_time・BL-1..5）
       - [x] 04-04-PLAN.md — predict.py + prediction_load.py + evaluator.py（provenance・staging-swap・比較表）
       - [x] 04-05-PLAN.md — run_train_predict.py + SC#4 reproduce smoke（両モデル統合・bit-identical）
       - [x] 04-06-PLAN.md — SC#3/SC#4 構造的ブロック GREEN + ROADMAP 更新（Phase 4 完了宣言）
       ```
    3. 【SC#1-#4 達成記載 — review HIGH#8: SC#2 は2要素分離】Success Criteria#1-#4 の直後に各 SC の達成根拠（PLAN 番号 + test 名 + 生成物）を1行ずつ追記:
       - SC#1（stamped Parquet のみ学習）→ "Achieved: PLAN 01-02・test_load_from_parquet_only/test_raw_ids_excluded/test_no_banned_features GREEN・models/{version}/ artifact 生成"
       - SC#2（BL-1..5 比較表）→ **review HIGH#8 対応: 2要素分離** (a) "比較表生成済み: PLAN 03-05・test_baseline.py 6 test GREEN・reports/04-eval.md + .json 生成" (b) "AI 付加価値: [実測値に基づき『主モデルが baselines を Brier/LogLoss/Calibration で上回る』または『未証明（上回らず）』のいずれかを正直に記載]（Task 1 step 7 の確認結果に基づく）"
       - SC#3（リークセーフ カテゴリ/欠損）→ "Achieved: PLAN 03・**対抗的構造診断** test_no_target_encoding_leak GREEN（低基数 RARE_X + 高基数 _code train-only/test-unseen + 意図的リーク制御 DEMONSTRABLY fail・target encoding 非混入の構造実証・review HIGH#3: live-data 証明と称さず対抗的構造診断と正確に呼ぶ）・test_lightgbm_nonneg_codes/test_catboost_has_time/test_catboost_predict_preserves_row_order GREEN・CatBoost _code 列 cat_features 宣言（review HIGH#6）"
       - SC#4（prefit isotonic + reproduce smoke）→ "Achieved: PLAN 02/05・test_strict_later_disjoint GREEN・test_reproduce_bit_identical GREEN（固定 thread count + 固定 as_of_datetime・review HIGH#7）・run_train_predict --check-reproduce 両モデル bit-identical"
    4. 【D-01..D-08 + review 反応記載】Phase 4 エントリ末尾に locked decisions と review 対応の集約を追記: "Locked decisions D-01..D-08 全て実装 + reviews HIGH#1..#12 + actionable MEDIUM/LOW 対応（DDL 11カラム PK + CHECK・FEATURE_COLUMNS allowlist・CatBoost _code cat_features・行整列 align_predictions・model_version D-10 形式・base+calibrator artifact 分離・SC#3 対抗的構造診断強化・SC#4 固定 thread/as_of_datetime・prediction model_version スコープ swap・KEIBA_SKIP_DB_TESTS unset 最終ゲート・SC#2 2要素分離）"
    5. 【STATE.md status 更新】`.planning/STATE.md` の `status: planning` → `status: phase-4-complete`（又は後続フェーズ移行を示す値）・current_phase_name / progress.completed_phases（4 → 5・percent 更新）・last_activity_desc に "Phase 4 complete, transitioned to Phase 5" を追記。progress.completed_plans に 6 追加（17 → 23）。Edit scoped replacement のみ。
    6. 【Phase 5 引き渡し確認】Phase 4 成果物（prediction.fukusho_prediction 両モデル・reports/04-eval.md・models/ artifact・D-04 事前登録選定基準）が Phase 5（EV/backtest）の入力として明記されていることを確認・Phase 5 エントリの Depends on が Phase 4 を指すことを確認。
  </action>
  <verify>
    <automated>grep -c '04-0[1-6]-PLAN.md' .planning/ROADMAP.md | awk '{ if ($1 >= 6) { exit 0 } else { exit 1 } }' && grep -q 'Achieved: PLAN' .planning/ROADMAP.md && grep -q 'status: phase-4-complete\|Phase 4 complete' .planning/STATE.md && echo "ROADMAP_UPDATE_OK"</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md の Phase 4 エントリが `**Plans**: 6 plans` を表示
    - ROADMAP.md に 04-01..06-PLAN.md の6行が checked ([x]) でリスト
    - ROADMAP.md に SC#1-#4 各々の "Achieved: PLAN ..." 行が存在・**SC#2 は2要素（比較表生成 + AI 付加価値 実証/未証明）に分けて記載**（review HIGH#8）
    - SC#3 の記載が『対抗的構造診断』と正確に呼ぶ（review HIGH#3・live-data 証明と称さない）
    - SC#4 の記載が固定 thread/as_of_datetime を明記（review HIGH#7）
    - ROADMAP.md に D-01..D-08 + reviews HIGH 対応の集約が存在
    - STATE.md の status が phase-4-complete（又は等価）・progress.completed_plans が 23（17+6）・last_activity_desc が Phase 4 complete を示す
    - Phase 5 エントリの Depends on が Phase 4 を指す
    - 全更新が Edit scoped replacement で実行（Write 全文上書きではない・Phase 5-8 エントリが破壊されていない）
  </acceptance_criteria>
  <done>ROADMAP.md と STATE.md が Phase 4 完了を反映し SC#1-#4 達成と D-01..D-08 反応が明記・Phase 5 への引き渡しが確立（Phase 4 完了宣言）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 実データ pipeline 実行 → prediction テーブル + artifact | 本番データでの副作用境界・部分失敗で破損 |
| SC#3/SC#4 GREEN 宣言 → Phase 4 完了 | 構造的ブロックの検証力を決定する境界（偽陽性 GREEN を防ぐ） |
| ROADMAP/STATE 更新 → 後続フェーズ | 文書と実態の整合を保つ境界（ドリフト再発防止） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-32 | Repudiation | SC#3/SC#4 が silent skip で GREEN 扱い | mitigate | 構造的ブロック宣言（hybrid gate Phase 1 D-01/Phase 2 D-02）・--check-reproduce 失敗で sys.exit(1)・各 test が docstring で SC#/D-XX を明記・VALIDATION Per-Task Map で追跡・**review HIGH#10: KEIBA_SKIP_DB_TESTS unset で最終検証を実行し critical テスト skipped count == 0 を記録** |
| T-04-32b | Repudiation | SC#3 leak diagnostic を live-data 証明と過大請求し監査で発覚 | mitigate | SC#3 を『対抗的構造診断（合成データ）』と正確に呼ぶ（review HIGH#3）・ROADMAP/VALIDATION の記載も同様・Phase 8 adversarial audit が live-data での target encoding 非混入を別途検証 |
| T-04-32c | Repudiation | SC#2 比較表が生成されただけで『AI 付加価値あり』と宣言 | mitigate | SC#2 を『比較表生成』と『主モデル vs baselines 具体的指標比較』の2要素に分け（review HIGH#8）・上回らない場合は『AI 付加価値未証明』と正直に注記・Phase 6 ゲートで最終判定 |
| T-04-33 | Tampering | 実データ pipeline の部分失敗で破損した予測が Phase 5 に渡る | mitigate | load_predictions staging-swap atomic・model_version スコープで他 model 破壊しない（review HIGH#1）・2回実行 checksum 一致・run_train_predict の try/except/finally・reports/04-eval.md の sum(p) 診断で異常検出 |
| T-04-34 | Repudiation | ROADMAP/STATE が実態と乖離（ドリフト再発） | mitigate | Edit scoped replacement のみ（Write 全文上書き禁止）・SC 達成根拠（PLAN 番号 + test 名）を明記・D-01 ドリフトの再発防止パターン確立・SC#2 は2要素分離で過大請求防止（review HIGH#8） |
| T-04-35 | Information Disclosure | 実行ログへの生 DSN 出力 | mitigate | run_train_predict の masked DSN ログ（settings.dsn_masked）・本 PLAN でも生 DSN を出力しない |
| T-04-36 | Tampering | D-04 事前登録選定基準を結果を見てすり替え | mitigate | reports/04-eval.md の comment 列で「Calibration 重視」を結果前に固定（PLAN 04 evaluator）・Phase 6 ゲートで最終判定時にすり替え検知 |
</threat_model>

<verification>
- Wave 5 完了 = Phase 4 完了: 
  - `uv run python scripts/run_train_predict.py --model-type both --check-reproduce` が exit 0
  - `uv run pytest` が全 GREEN（tests/ 全体）
  - reports/04-eval.md 存在・prediction.fukusho_prediction 両モデル行・models/ artifact 揃う
  - ROADMAP.md/STATE.md が Phase 4 完了を反映
  - 04-VALIDATION.md が nyquist_compliant: true
</verification>

<success_criteria>
- SC#3 leak diagnostic が**対抗的構造診断**として GREEN（review HIGH#3・target encoding 非混入の構造実証・live-data 証明と称さない）
- SC#4 reproduce smoke が GREEN（両モデル bit-identical・固定 thread/as_of_datetime・review HIGH#7・§19.1 構造的ブロック）
- run_train_predict.py --model-type both --check-reproduce が実データで完了
- reports/04-eval.md + .json + prediction.fukusho_prediction（両モデル・model_version スコープ）+ models/ artifact（base+calibrator 分離）が生成
- **SC#2 が比較表生成 + 主モデル vs baselines 具体的指標比較の2要素で記載**・上回らない場合は未証明注記（review HIGH#8）
- **KEIBA_SKIP_DB_TESTS unset で全テストスイート GREEN**（回帰なし・critical skip 0・review HIGH#10）
- ROADMAP/STATE が Phase 4 完了 + SC#1-#4 達成（SC#2 2要素分離・SC#3 対抗的構造診断と正確に呼ぶ・SC#4 固定 thread/as_of_datetime）+ D-01..D-08 + reviews HIGH 反応で更新
- 04-VALIDATION.md が approved/nyquist_compliant: true/final_gate_run_with_skip_unset: true・各 test に実行証拠格納（review MEDIUM）
</success_criteria>

<output>
Create `.planning/phases/04-model-prediction/04-06-SUMMARY.md` when done

## Artifacts this phase produces
- 実行生成物（再確認）: reports/04-eval.md + reports/04-eval.json（LightGBM/CatBoost + BL-1..5 比較表・§15.2 sum(p) 診断・D-04 事前登録選定基準素材・BL-3/BL-4/BL-5 注記）, models/20260620-1a-postreview-v2-lgb-v1/{lgb_model.txt, calibrator.joblib, metadata.json}（review HIGH#5 base+calibrator 分離）, models/20260620-1a-postreview-v2-cb-v1/{cb_model.cbm, calibrator.joblib, metadata.json}, prediction.fukusho_prediction テーブル（model_type='lightgbm' + 'catboost' の両方の行・model_version スコープ swap で互いに破壊しない・review HIGH#1）
- .planning/phases/04-model-prediction/04-VALIDATION.md: frontmatter nyquist_compliant: true / wave_0_complete: true / status: approved / **final_gate_run_with_skip_unset: true**（review HIGH#10）・Per-Task Verification Map 全 test ✅ GREEN・Task ID 埋込済み・**各 test に実行証拠（コマンド/終了コード/checksum/hash/行数）格納**（review MEDIUM）・Validation Sign-Off 6 項目チェック済み
- .planning/ROADMAP.md: Phase 4 エントリ `**Plans**: 6 plans`・6 PLAN リスト checked・SC#1-#4 各 "Achieved: PLAN ..." 根拠（**SC#2 は2要素分離・review HIGH#8**・SC#3 は対抗的構造診断と正確に呼ぶ・review HIGH#3・SC#4 は固定 thread/as_of_datetime・review HIGH#7）・D-01..D-08 + reviews HIGH 反応集約
- .planning/STATE.md: status 更新（phase-4-complete 系）・progress.completed_plans=23・last_activity_desc "Phase 4 complete"
</output>
