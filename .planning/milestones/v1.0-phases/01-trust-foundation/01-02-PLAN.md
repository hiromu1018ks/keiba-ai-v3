---
phase: 01-trust-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/etl/__init__.py
  - src/etl/quality_gate.py
  - scripts/run_quality_report.py
  - tests/test_quality_gate.py
  - reports/.gitkeep
autonomous: true
requirements: [DATA-01]
must_haves:
  truths:
    - "実 DB（everydb2.public.n_*）に対しハイブリッド品質ゲートを実行し verdict: pass/fail を含む JSON と人間用 Markdown を reports/ に出力できる"
    - "構造的欠陥（2015-01-01以降のJRAデータなし／主要5系統テーブル欠損／PK重複／自然キー重複）を検出した場合は verdict: fail となる"
    - "量的異常（NULL率・**文字化け(mojibake)**・code=0 割合・**コード値異常(allowed-code-set 違反)**・キャスト失敗率）は INFO レベルで報告され verdict に影響しない（DATA-01 完全実装・REVIEWS HIGH #7）"
    - "全ての品質クエリが jyocd BETWEEN '01' AND '10' で JRA に絞られ NAR 混入が排除される"
    - "**DB 接続テストは fail-by-default であり KEIBA_SKIP_DB_TESTS=1 設定時のみ skip される**（REVIEWS HIGH #8・`.env` 未設定時 skip は廃止・plan 01-01 Task 2 と同一 policy）"
  artifacts:
    - path: "src/etl/quality_gate.py"
      provides: "Hybrid Quality Gate（D-01）の run_quality_gate(cur) -> dict。mojibake + code-value-anomaly チェック含む（REVIEWS HIGH #7）"
      contains: "CheckResult"
    - path: "scripts/run_quality_report.py"
      provides: "reports/quality_report.md と .json を出力するエントリポイント"
      contains: "run_quality_gate"
    - path: "tests/test_quality_gate.py"
      provides: "blocking checks / pass-when-clean / jra-only filter / mojibake detection / code-anomaly detection の unit+integration test"
      contains: "test_blocking_checks"
    - path: "reports/quality_report.json"
      provides: "機械判定用レポート・verdict フィールド含む"
      contains: "verdict"
  key_links:
    - from: "scripts/run_quality_report.py"
      to: "src/etl/quality_gate.py"
      via: "readonly_cur 経由で run_quality_gate(cur) を呼出"
      pattern: "run_quality_gate\\("
    - from: "src/etl/quality_gate.py"
      to: "everydb2.public.n_race / n_uma_race / n_harai / n_hyosu / n_odds_tanpuku"
      via: "psycopg3 SELECT（全クエリで jyocd BETWEEN '01' AND '10'）"
      pattern: "jyocd BETWEEN '01' AND '10'"
---

<objective>
DATA-01 を実装する。`everydb2.public.n_*` テーブル群に対しハイブリッド品質ゲート（D-01）を実行し、構造的欠陥（BLOCK）と量的異常（INFO）を分離して pass/fail verdict を含む Markdown + JSON レポートを `reports/` に出力する。01-RESEARCH.md §Pattern 1 / Example 1 / Pitfall 1/2/4 をほぼ verbatim で採用する。

**REVIEWS Cycle-1 修正（本 plan の主要改訂）:**
- **HIGH #7:** Success Criterion #1 が要求する **mojibake（文字化け）flags** と **code-value anomalies**（各コードカラムが許容コードセット内か）のチェックを、`src/etl/quality_gate.py` の INFO チェック群に**具体的 SQL/Python ロジックとして追加**する。NULL/cast 率だけでは DATA-01 未達成。
  - **Mojibake 検出:** `bamei`（馬名）/ `kisyu`（騎手名）/ `hondai`（レース名）等の主要 varchar カラムに対し、`U+FFFD`（REPLACEMENT CHARACTER）の出現件数、および `convert_from(string, 'UTF8')` で失敗するバイト列の件数を INFO で報告。PostgreSQL の `position(chr(65533) in column)` / `column::bytea` と `lpad` を組み合わせた byte-level scan、または Python 側で `value.encode('utf-8', errors='surrogatepass').decode('utf-8', errors='replace')` 後の `'�'` 出現をカウント。
  - **Code-value anomaly 検出:** `jyokencd5`（`class_normalization.yaml` の6値: 701/703/005/010/016/999）、`gradecd`（A/B/C/D/L/E/F/G/H/空）、`jyocd`（01-10）、`syubetucd`（code_tables.yaml の定義値）の各カラムについて、`code_tables.yaml` と `class_normalization.yaml` で定義された allowed-code-set に**含まれない値の件数と割合**を INFO で報告。`gradecd='Z'` 等の不正値が検出される。
- **HIGH #8:** DB 接続テストの skip policy を **fail-by-default unless `KEIBA_SKIP_DB_TESTS=1`** に統一（plan 01-01 Task 2 と同一）。`.env` 未設定時 skip 可という記述は廃止。
- **MEDIUM (concat-key collision):** PK / 自然キーの一意性チェックで `year||jyocd||kaiji...` のような素の文字列結合を廃止し、`concat_ws('|', year, jyocd, kaiji, nichiji, racenum)` または `COUNT(DISTINCT (year, jyocd, kaiji, nichiji, racenum))` を使う（理論的な衝突を回避）。

Purpose: 下流（normalized ETL / クラス正規化 / label / feature / model）が信頼できる raw の上で動くための最初の検証ゲート（Build-Order DAG step 1）。§6.4 の9項目（テーブル存在/件数/日付範囲/2015以降/NULL/PK・自然キー重複/**文字化け/コード値異常**）をカバーする。Streamlit 表示は Phase 7（D-04）。
Output: src/etl/quality_gate.py / scripts/run_quality_report.py / tests/test_quality_gate.py / reports/quality_report.{md,json}
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-trust-foundation/01-CONTEXT.md
@.planning/phases/01-trust-foundation/01-RESEARCH.md
@.planning/phases/01-trust-foundation/01-PATTERNS.md
@.planning/phases/01-trust-foundation/01-VALIDATION.md
@.planning/phases/01-trust-foundation/01-REVIEWS.md
@.planning/phases/01-trust-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hybrid Quality Gate（D-01）の実装 — **mojibake + code-value-anomaly チェック含む（REVIEWS HIGH #7）** + tests/test_quality_gate.py（unit + integration・fail-by-default policy・HIGH #8）</name>
  <files>src/etl/__init__.py, src/etl/quality_gate.py, tests/test_quality_gate.py</files>
  <read_first>
    - 01-RESEARCH.md（§Pattern 1: Hybrid Quality Gate / §Example 1 / §Pitfall 1/2/4 / §Validation Architecture / §Security Domain）
    - 01-REVIEWS.md（§Plan 01-02: HIGH #7 mojibake + code-value anomalies 未実装 / HIGH #8 DB-test skip policy conflict / MEDIUM concat-key collision）
    - 01-PATTERNS.md（`src/etl/quality_gate.py` / `tests/test_quality_gate.py` の推奨イディオム）
    - 01-CONTEXT.md（D-01/D-02/D-03 品質ゲート、D-04 reports 出力）
    - src/config/class_normalization.yaml（plan 01-01 で作成・allowed codes の正：jyokencd5_map / gradecd_map）
    - src/config/code_tables.yaml（plan 01-01 で作成・allowed jyocd / syubetucd）
    - docs/keiba_ai_requirements_v1.3.md §6.4（品質チェック9項目・文字化けとコード値異常を明示）
    - src/etl/__init__.py が存在しない場合は作成（src/__init__.py は plan 01 で作成済み）
  </read_first>
  <behavior>
    - BLOCK チェック（severity="block"）: 5系統主要テーブル存在（n_race/n_uma_race/n_harai/n_hyosu/n_odds_tanpuku）、JRA 2015-01-01以降データ存在（成功基準#1）、n_race PK 一意（**`COUNT(DISTINCT (year, jyocd, kaiji, nichiji, racenum))` = 全件**・MEDIUM concat collision 回避）、n_uma_race 自然キー一意（**`COUNT(DISTINCT (year, jyocd, kaiji, nichiji, racenum, umaban, kettonum))` = 全件**・同様）
    - INFO チェック（severity="info"）: 各テーブルの総件数・JRA件数・min/max(year||monthday)・主要 varchar カラムの NULL率・code=0 or 空白の割合・数値化可能なカラム（kyori/futan/hassotime）のキャスト成功率
    - **REVIEWS HIGH #7 で追加する INFO チェック（必須）:**
      - **mojibake 検出:** `bamei`, `kisyu`, `hondai`, `torikishi` 等の主要日本語 varchar カラムに対し、`U+FFFD`（REPLACEMENT CHARACTER）を含む行数、および `convert_from(string::bytea, 'UTF8')` でエラーになる（不正バイト列を含む）行数を INFO で報告。PostgreSQL: `SELECT count(*) FROM n_race WHERE position(U&'\\+00FFFD' in hondai) > 0 OR bamei::bytea <> convert_from(bamei::bytea, 'UTF8')::bytea`（後者は try/catch を伴う関数定義が必要な場合は PL/pgSQL `BEGIN ... EXCEPTION WHEN others THEN ...` パターンで検出）。Python サブ経路も可: `psycopg3` で SELECT した `str` に対し `value.count('�')` を集計。
      - **code-value anomaly 検出:** 各コードカラム（`jyokencd5`, `gradecd`, `jyocd`, `syubetucd`）について、`class_normalization.yaml` と `code_tables.yaml` で定義された allowed-code-set に**含まれない値**の件数と全体に占める割合を INFO で報告。例: `SELECT count(*) FROM n_race WHERE jyocd BETWEEN '01' AND '10' AND jyokencd5 NOT IN ('701','703','005','010','016','999')`、`gradecd NOT IN ('A','B','C','D','L','E','F','G','H','')`、`jyocd NOT IN ('01',...,'10')`、`syubetucd NOT IN ('00','01',...,'19' /* code_tables.yaml の値 */)`. allowed-code-set は起動時に `src/config/*.yaml` を読み込んで組み立てる（plan 01-01 の YAML を import）。
    - 全ての SELECT で `WHERE jyocd BETWEEN '01' AND '10'`（Pitfall 2: NAR jyocd>=30 除外）。PK/natural-key/code-value-anomaly チェックも同様。
    - run_quality_gate(cur) の戻り値: `{"verdict": "pass"|"fail", "checks": [{"name","passed","severity","detail"}, ...]}`
    - verdict は severity="block" なチェックが全て passed の場合のみ "pass"、それ以外は "fail"
    - 入力 cur は psycopg3 cursor（plan 01 の readonly_cur fixture）
  </behavior>
  <action>
`src/etl/__init__.py` を作成（空 or docstring）。`src/etl/quality_gate.py` を作成し 01-RESEARCH.md §Example 1 の構造を採用:

1. `@dataclass class CheckResult: name: str; passed: bool; severity: str; detail: dict`（severity は `"block" | "info"`、D-01）。`from dataclasses import dataclass, asdict`。`import yaml` を追加し `src/config/class_normalization.yaml` と `src/config/code_tables.yaml` から allowed-code-set を構築するヘルパー `_load_allowed_codes() -> dict[str, set[str]]` も実装（jyokencd5 -> {701,703,005,010,016,999}, gradecd -> {A,B,C,D,L,E,F,G,H,''}, jyocd -> {01..10}, syubetucd -> code_tables.yaml の値）。
2. BLOCK チェック群（D-01 構造的欠陥 = pytest/CI fail）。01-RESEARCH.md §Pattern 1 の BLOCKING_CHECKS を展開:
   - `_check_table_exists(cur, "n_race")`, `"n_uma_race"`, `"n_harai"`, `"n_hyosu"`, `"n_odds_tanpuku"`（information_schema.tables で schema='public' を確認、D-02 主要5系統）
   - `_check_jra_since_2015(cur)`: `SELECT count(*) FROM n_race WHERE (year||monthday) >= '20150101' AND jyocd BETWEEN '01' AND '10'` で件数 > 0（成功基準#1）
   - `_check_n_race_pk_unique(cur)`: **`SELECT count(*) - count(DISTINCT (year, jyocd, kaiji, nichiji, racenum)) FROM n_race WHERE jyocd BETWEEN '01' AND '10'`** が 0（**MEDIUM: `year||jyocd||...` の文字列結合ではなく row-tuple 形式で理論衝突を回避**）
   - `_check_n_uma_race_natural_key_unique(cur)`: **`SELECT count(*) - count(DISTINCT (year, jyocd, kaiji, nichiji, racenum, umaban, kettonum)) FROM n_uma_race WHERE jyocd BETWEEN '01' AND '10'`** が 0（同様）
3. INFO チェック群（D-01 量的異常 = 参考レポート）:
   - 既存: 各テーブル件数（全体 / JRA限定）、min/max(year||monthday)、主要カラム（n_race の kyori/futan/hassotonen/jyokencd5/gradecd、n_uma_race の kakuteijyuni/umaban/kettonum）の NULL率、code=0 or 空白の割合、`CAST(kyori AS integer)` の成功率（Pitfall 1）
   - **REVIEWS HIGH #7 で追加（必須）:**
     - `_check_mojibake(cur)`: 主要 varchar カラム（`bamei`, `kisyu`, `hondai`, `torikishi`, `umaki` 等）のそれぞれについて `U+FFFD` を含む行数を INFO で報告。SQL 例: `SELECT count(*) AS cnt FROM n_race WHERE jyocd BETWEEN '01' AND '10' AND position(U&'\\+00FFFD' in coalesce(hondai,'')) > 0`。各カラム毎に実行し detail dict に格納。`n_uma_race` 側の `bamei`/`kisyu` も検査。
     - `_check_code_value_anomalies(cur, allowed_codes)`: `jyokencd5 NOT IN (...)`, `gradecd NOT IN (...)`, `jyocd NOT IN ('01'..'10')`, `syubetucd NOT IN (...)` の各検査を `n_race` に対し実行。件数と割合（%）を detail dict に格納。allowed_codes は `_load_allowed_codes()` で組み立てた dict。SQL は動的に組み立て（psycopg3 のパラメータ埋め込みは `%s` だが `IN (...)` のリスト展開は `ANY(%s::text[])` を使用して SQL injection を回避）。
4. `def run_quality_gate(cur) -> dict`: 上記 checks を実行し `CheckResult` のリストを構築、`verdict = "pass" if all(r.passed for r in results if r.severity == "block") else "fail"`、`return {"verdict": verdict, "checks": [asdict(r) for r in results]}`。

**Pitfall 対策（必須・01-RESEARCH.md Pitfall 1/2/4）:**
- Pitfall 1（全 varchar）: 数値カラム（kyori/futan/hassotime）は明示的 `CAST(... AS integer/numeric)` を行い、失敗率を INFO で報告
- Pitfall 2（NAR 混入）: 全クエリで `WHERE jyocd BETWEEN '01' AND '10'`（JRA東西10場限定）。品質件数も JRA と全体を分けて報告
- Pitfall 4（s_*/n_* 取り違え）: 対象テーブルは `n_*`（確定）のみ、`s_*`（速報）は対象外

`tests/test_quality_gate.py` を作成（unit + integration・**REVIEWS HIGH #8 fail-by-default policy 適用**）:
- `test_blocking_checks_fail_when_no_2015_data`: psycopg3 cursor を mock または実 DB の小さな temp schema で、2015以降データが無い状況を模擬し verdict=="fail"・該当 check の severity=="block" を assert
- `test_n_race_pk_duplicate_fails`: PK 重複を模擬データで発生させ verdict=="fail" を assert
- **`test_mojibake_detection`（HIGH #7・必須）:** `_check_mojibake` をモック cursor で呼出し、`U+FFFD` を含むサンプル文字列を渡したときに件数 > 0 が報告されることを assert。直接 `position('�' in ...)` のロジック相当を Python 側でも再現して cross-check
- **`test_code_value_anomaly_detection`（HIGH #7・必須）:** `_check_code_value_anomalies` をモック cursor で呼出し、allowed-code-set 外の値（例: `jyokencd5='ZZZ'`, `gradecd='Z'`, `jyocd='99'`）を含むデータで件数 > 0 が報告されることを assert。`allowed_codes = _load_allowed_codes()` が `class_normalization.yaml` と `code_tables.yaml` から正しく6+9+10+N 個の値を読込むことも assert
- `test_pass_when_clean` (`@pytest.mark.requires_db`): 実 everydb2 DB に接続し verdict=="pass" を確認（integration）。**`.env` 未設定時は skip ではなく fail する（plan 01-01 Task 2 の conftest policy に従う・HIGH #8）**。`KEIBA_SKIP_DB_TESTS=1` 設定時のみ skip。
- `test_jra_only_filter` (`@pytest.mark.requires_db`): 実 DB で取得した件数が `SELECT count(*) FROM n_race` 全体より `WHERE jyocd BETWEEN '01' AND '10'` で絞った件数が少ないことを確認（NAR 混入の検知）
  </action>
  <verify>
    <automated>uv run pytest tests/test_quality_gate.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `src/etl/quality_gate.py` が `@dataclass class CheckResult` と `def run_quality_gate(cur)` を含む
    - `src/etl/quality_gate.py` が `verdict = "pass"` のロジックで `r.severity == "block"` を参照する
    - `src/etl/quality_gate.py` が全ての SQL SELECT で `jyocd BETWEEN '01' AND '10'` を含む（Pitfall 2、`grep -c "jyocd BETWEEN '01' AND '10'" src/etl/quality_gate.py` が 3 以上）
    - `src/etl/quality_gate.py` が `n_race` / `n_uma_race` / `n_harai` / `n_hyosu` / `n_odds_tanpuku` の5テーブル名を含む（D-02 主要5系統）
    - `src/etl/quality_gate.py` が `CAST(kyori AS integer)` または同等の明示キャストを含む（Pitfall 1）
    - **HIGH #7 直接検証:** `src/etl/quality_gate.py` が mojibake 検出（`U+FFFD` / `\\+00FFFD` / `�` / `REPLACEMENT CHARACTER` のいずれかのトークン）を含む（`grep -cE 'FFFD|REPLACEMENT|\\\\ufffd' src/etl/quality_gate.py` が 1 以上）
    - **HIGH #7 直接検証:** `src/etl/quality_gate.py` が code-value-anomaly 検出（`NOT IN` と `_load_allowed_codes` / `allowed_codes` のいずれか）を含む（`grep -c 'NOT IN' src/etl/quality_gate.py` が 1 以上 かつ `grep -c '_load_allowed_codes\\|allowed_codes' src/etl/quality_gate.py` が 1 以上）
    - **MEDIUM concat-key 直接検証:** `src/etl/quality_gate.py` が `count(DISTINCT (` の row-tuple 形式 または `concat_ws` を含み、素の `year||jyocd||kaiji` 文字列結合を含まない（`grep -c 'year||jyocd' src/etl/quality_gate.py` が 0、`grep -cE 'count\\(DISTINCT \\(|concat_ws' src/etl/quality_gate.py` が 1 以上）
    - `tests/test_quality_gate.py` が `test_blocking_checks_fail_when_no_2015_data` / `test_n_race_pk_duplicate_fails` / `test_mojibake_detection` / `test_code_value_anomaly_detection` / `test_pass_when_clean` / `test_jra_only_filter` の6テストを含む（`test_mojibake_detection` と `test_code_value_anomaly_detection` が HIGH #7 で必須追加）
    - `tests/test_quality_gate.py` が `KEIBA_SKIP_DB_TESTS` を含まずに conftest の autouse policy に依存する（HIGH #8・plan 01-01 Task 2 と完全一致）
    - `uv run pytest tests/test_quality_gate.py -v` が green（実 DB 接続テストは `KEIBA_SKIP_DB_TESTS=1` 設定時のみ skip・HIGH #8）
  </acceptance_criteria>
  <done>Hybrid Quality Gate が構造的欠陥を BLOCK / 量的異常を INFO（**mojibake + code-value-anomaly 含む・HIGH #7**）で分離し verdict: pass|fail を返す・unit + integration テスト green（DB 接続テストは fail-by-default・HIGH #8）</done>
</task>

<task type="auto">
  <name>Task 2: reports/ 出力スクリプト（Markdown + JSON、verdict 含む）</name>
  <files>scripts/run_quality_report.py, reports/.gitkeep</files>
  <read_first>
    - src/etl/quality_gate.py（Task 1 で作成、関数シグネチャ確認）
    - 01-RESEARCH.md（§Pattern 1 / §Architecture Patterns）
    - 01-PATTERNS.md（`scripts/run_quality_report.py` 推奨イディオム）
    - 01-CONTEXT.md D-04（reports/ の Markdown + JSON）
    - 01-REVIEWS.md（§Plan 01-02: HIGH #8 DB-test skip policy / T-02-02 information disclosure）
    - src/db/connection.py（plan 01 で作成、make_pool/readonly_cursor）
    - src/config/settings.py（plan 01 で作成、Settings）
  </read_first>
  <action>
`scripts/run_quality_report.py` を作成。`src.config.settings.Settings` から接続情報を読み、`src.db.connection.make_pool` で readonly pool を作成、`readonly_cursor` 経由で `src.etl.quality_gate.run_quality_gate(cur)` を呼び出す。戻り値の dict を2形式で出力:

1. `reports/quality_report.json`: 機械判定用。`json.dump(result, indent=2, ensure_ascii=False, default=str)` で書込。最上位に `"verdict": "pass"|"fail"` を含む。CI は `jq -r .verdict` で pass/fail を判定し fail なら exit 1（D-01 ブロッキング要件）。**T-02-02 対応（情報漏洩防止）:** dump 前に allowlist filter を通し `name/passed/severity/detail` のみ許可・DSN/password 等の認証情報は絶対に含めない。
2. `reports/quality_report.md`: 人間用 Markdown。verdict を冒頭に表示（PASS/FAIL バッジ相当の見出し）、各 check をテーブル形式で（name / severity / passed / detail 要約）列出力。NULL率分布・件数・日付範囲・**mojibake 件数・code-anomaly 件数**等の補助情報も INFO 欄にまとめる（HIGH #7 で追加した項目も表示）。

`argparse` で `--output-dir`（デフォルト `reports/`）、`--fail-on-block`（デフォルト true、verdict=="fail" のとき exit 1）をサポート。reports/ ディレクトリが無い場合は `os.makedirs` で作成。

**DB 接続失敗時の挙動（REVIEWS HIGH #8）:** Settings の validation error（`.env` 未設定等）が起きた場合は、skip + WARNING ではなく exit code 2 で fail する。`KEIBA_SKIP_DB_TESTS=1` が設定されている場合はレポート生成自体を skip し exit 0 で終了（CI で明示的に DB 無し環境を許可する場合のみ）。

`reports/.gitkeep` を作成し、reports/ ディレクトリが空でも git tracking されるようにする（01-RESEARCH.md Recommended Project Structure で reports/ は gitignore 対象外・成果物）。
  </action>
  <verify>
    <automated>uv run python scripts/run_quality_report.py --output-dir reports/ &amp;&amp; test -f reports/quality_report.json &amp;&amp; test -f reports/quality_report.md &amp;&amp; uv run python -c "import json; d=json.load(open('reports/quality_report.json')); assert d['verdict'] in ('pass','fail'); assert 'checks' in d; assert isinstance(d['checks'], list); assert all(set(c.keys()) &lt;= {'name','passed','severity','detail'} for c in d['checks']), 'password leak guard'; print('report OK, verdict=', d['verdict'])"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/run_quality_report.py` が `argparse` と `from src.etl.quality_gate import run_quality_gate` と `from src.db.connection import make_pool` を含む
    - `scripts/run_quality_report.py` が `reports/quality_report.json` と `reports/quality_report.md` の両方を書き出す（`grep -c 'quality_report.json' scripts/run_quality_report.py` が 1 以上、`grep -c 'quality_report.md' scripts/run_quality_report.py` が 1 以上）
    - `scripts/run_quality_report.py` が `--fail-on-block` オプションをサポートし、verdict=="fail" のとき exit 1 するロジックを含む
    - `scripts/run_quality_report.py` が allowlist filter（`name/passed/severity/detail` のみ許可）を含み password/DSN を含まないことを検証（T-02-02）
    - `scripts/run_quality_report.py` が `.env` 未設定時に skip せず exit 2 で fail する policy を含む（HIGH #8・`KEIBA_SKIP_DB_TESTS=1` 設定時のみ skip）
    - `reports/.gitkeep` が存在する
    - スクリプト実行後 `reports/quality_report.json` が存在し `jq -r .verdict` が `pass` または `fail` を返す（実 DB 接続環境）
    - `reports/quality_report.md` が verdict と各 check の一覧を含む
  </acceptance_criteria>
  <done>reports/ に Markdown（人間用・mojibake/code-anomaly 項目含む）+ JSON（機械判定用、verdict 含む、password leak guard 付き）を出力・CI 連携可能・DB 未設定時は fail（HIGH #8）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| readonly_cur → everydb2.public.n_* | 品質ゲート SELECT の境界。読取専用ロールで UPDATE/DELETE 不可（plan 01 の REVOKE・HIGH #4 で public と raw_everydb2 の両方を保護） |
| reports/ → CI / 人間 | 出力レポートの境界。JSON に機密情報（DB パスワード等）を含めない（anti-pattern #20、ASVS V8・T-02-02 allowlist filter） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | silent fallback で品質チェック結果を隠蔽 | mitigate | verdict 計算を `all(r.passed for r in results if r.severity == "block")` で機械的に決定。CheckResult は dataclass でフィールド明示。未知テーブル/不明カラムは例外で fail に傾く（D-01、ASVS V7） |
| T-02-02 | Information Disclosure | reports/quality_report.json に DB 認証情報混入 | mitigate | レポート出力対象を CheckResult の name/passed/severity/detail に限定（allowlist filter）。DSN や password は一切含めない。`scripts/run_quality_report.py` で dump 前に allowlist filter を通す。acceptance_criteria で `set(c.keys()) <= {'name','passed','severity','detail'}` を検証（ASVS V8、anti-pattern #20） |
| T-02-03 | Repudiation | 品質ゲート未実行で下流 ETL が動く・偽の verdict=pass | mitigate | Build-Order DAG で品質ゲート → ETL の順序を運用で担保。Phase 8 adversarial audit で要再検証。Phase 1 では CI の `--fail-on-block` exit 1 で防止。**REVIEWS HIGH #8:** DB-test skip policy を fail-by-default に統一し `.env` 未設定で偽の pass が出ないようにする |
| T-02-04 | Tampering | mojibake や code-value anomaly が未検出のまま下流へ（REVIEWS HIGH #7） | mitigate | `_check_mojibake` と `_check_code_value_anomalies` を INFO チェックとして必ず実行。`test_mojibake_detection` / `test_code_value_anomaly_detection` が検出ロジックを回帰検証。allowlist は YAML から読み込み静的に決定（D-07 Git 管理） |
</threat_model>

<verification>
- `uv run pytest tests/test_quality_gate.py -v` が green（`KEIBA_SKIP_DB_TESTS=1` 設定時のみ DB 接続テスト skip・HIGH #8）
- `uv run python scripts/run_quality_report.py` が reports/quality_report.{md,json} を生成
- `jq -r .verdict reports/quality_report.json` が pass または fail を返す
- 実 DB 環境で verdict=="pass"（実測で重複0・2015以降存在）
- **HIGH #7:** reports/quality_report.md の INFO 欄に mojibake 件数と code-anomaly 件数が表示される
</verification>

<success_criteria>
- DATA-01 品質ゲートが §6.4 9項目（テーブル存在/件数/日付範囲/2015以降/NULL/PK・自然キー重複/**文字化け/コード値異常**）を報告（HIGH #7 で mojibake + code-anomaly を具体実装）
- 構造的欠陥（BLOCK）と量的異常（INFO）が分離され、BLOCK のみ verdict に影響
- reports/ に Markdown + JSON が出力され JSON に verdict フィールドが含まれる（password leak guard 付き）
- NAR 混入が jyocd BETWEEN '01' AND '10' で除外される（Pitfall 2）
- 全 varchar カラムの明示キャスト事故が防止される（Pitfall 1）
- **HIGH #8:** DB-test skip policy が fail-by-default unless `KEIBA_SKIP_DB_TESTS=1` に統一（plan 01-01/01-03 と完全一致）
- **MEDIUM:** PK/natural-key 一意性チェックが row-tuple `count(DISTINCT (...))` 形式で文字列衝突を回避
</success_criteria>

<output>
Create `.planning/phases/01-trust-foundation/01-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

### Functions / Classes
- `src.etl.quality_gate.CheckResult` (dataclass): `name: str`, `passed: bool`, `severity: str`, `detail: dict`
- `src.etl.quality_gate.run_quality_gate(cur) -> dict`: `{"verdict": "pass"|"fail", "checks": [...]}`
- 内部 helper:
  - `_check_table_exists(cur, table)`, `_check_jra_since_2015(cur)`, `_check_n_race_pk_unique(cur)`, `_check_n_uma_race_natural_key_unique(cur)` (BLOCK)
  - `_check_mojibake(cur)` (**HIGH #7 で追加**)
  - `_check_code_value_anomalies(cur, allowed_codes)` (**HIGH #7 で追加**)
  - `_load_allowed_codes() -> dict[str, set[str]]` (**HIGH #7 で追加**・class_normalization.yaml と code_tables.yaml から許容コードセットを構築）

### CLI flags
- `scripts/run_quality_report.py`: `--output-dir` (default `reports/`), `--fail-on-block` (default true, exit 1 on verdict=fail)

### Output files (generated)
- `reports/quality_report.json` — machine-readable, top-level `verdict` field, allowlist-filtered (no password/DSN)
- `reports/quality_report.md` — human-readable, mojibake/code-anomaly counts in INFO section (HIGH #7)

### New file paths
- `src/etl/__init__.py`, `src/etl/quality_gate.py`
- `scripts/run_quality_report.py`
- `tests/test_quality_gate.py`
- `reports/.gitkeep`

### Constants / SQL patterns referenced
- JRA filter: `jyocd BETWEEN '01' AND '10'` (Pitfall 2)
- Date filter: `(year||monthday) >= '20150101'` (成功基準#1)
- PK columns: `year, jyocd, kaiji, nichiji, racenum` (n_race) — **row-tuple `count(DISTINCT (...))` 形式**（MEDIUM concat-key collision 回避）
- Natural key columns: `year, jyocd, kaiji, nichiji, racenum, umaban, kettonum` (n_uma_race) — 同様
- Target tables (D-02): `n_race, n_uma_race, n_harai, n_hyosu, n_odds_tanpuku`
- Mojibake token: `U+FFFD` / `U&'\\+00FFFD'` / `�` (HIGH #7)
- Allowed-code sets (HIGH #7): from `src/config/class_normalization.yaml` (`jyokencd5_map` 6 keys, `gradecd_map` A/B/C/D/L/E/F/G/H/''), from `src/config/code_tables.yaml` (`jyocd` 01-10, `syubetucd`)
