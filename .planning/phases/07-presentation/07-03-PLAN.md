---
phase: 07-presentation
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - src/ui/app.py
  - src/ui/prediction_tab.py
  - src/ui/backtest_tab.py
  - src/ui/calibration_tab.py
  - tests/ui/test_streamlit_api_usage.py
  - tests/ui/test_ui_render_contract.py
autonomous: false
requirements: [UI-01, OUT-01, OUT-02]
user_setup: []

must_haves:
  truths:
    # UI-SPEC Layout Contract / Component Inventory（承認済み契約）からのリフト
    - "streamlit run src/ui/app.py が TypeError/StreamlitAPIException なく起動し・ページタイトル「Keiba AI v3 — 複勝予測分析」を表示する（UI-SPEC Copywriting・st.title 1回のみ）"
    - "3タブ st.tabs(['予測一覧','Backtest','Segment Calibration']) が表示される（D-01・multipage でなく単一ページ内タブ・UI-SPEC Layout）"
    - "予測一覧タブのレース一覧 st.dataframe が selection_mode='single-row' + on_select='rerun' を使用し（Pitfall 1 解決・UI-SPEC の selection='single-row' は古い API・RESEARCH で正引数確定）・行選択で下部に各馬詳細が展開される"
    - "各馬詳細の st.dataframe が p_fukusho_hit/EV_lower/EV_upper/fuku_odds_lower/fuku_odds_upper/recommend_rank を st.column_config.NumberColumn(format='%.3f') で表示する（UI-SPEC Typography・小数点以下3桁固定）"
    - "選択レース内の各馬行に p_fukusho_hit・EV_lower・EV_upper・fukusho_odds_lower・fukusho_odds_upper・recommend_rank の6数値が表示される（SC#1 user-observable truth・UI-01・OUT-01 予測CSV 列と1:1・Open Question #1/#2 解決経路 JODDS snapshot + compute_ev_and_rank で算出された値が表示される・どれか1つでも欠けると SC#1 達成不可）"
    - "選択レースの予測行に再現性スタンプ5項目（odds_snapshot_policy/odds_snapshot_at/model_version/feature_snapshot_id/backtest_strategy_version）が inline 表示される（§19.1 聖域・UI-SPEC L231）"
    - "st.sidebar に日付範囲（st.date_input range）・競馬場（st.multiselect・format_func=競馬場名）・推奨ランク（st.multiselect S/A/B/C/D・default=S,A,B）の主要フィルタが配置される（D-02 LOCKED）"
    - "Backtest タブが st.warning で honest 注記「注意: この backtest の odds 正確性は JODDS オッズ取得完了後に再検証する subject です。現状の回収率は暫定値であり、確定後に入れ替わる可能性があります。」を表示する（UI-SPEC Copywriting・Pitfall 6・Phase 5 manual-only 整合）"
    - "Backtest タブの st.caption で「推奨ランクは参考情報であり、購入判断を強制するものではありません（§19.3・実馬券購入はスコープ外）。」を表示する（UI-SPEC Copywriting・honest）"
    - "予測一覧タブと Backtest タブにそれぞれ st.download_button があり・予測=「予測CSVをダウンロード」・backtest=「Backtest CSVをダウンロード」のラベル（UI-SPEC Copywriting・D-04）"
    - "Segment Calibration タブが st.selectbox（6軸: year/month/jyocd/entry_count/ninki/odds_band・D-12）で軸切替を提供し・st.plotly_chart(use_container_width=True) で curve 重ね描き Figure を描画する（D-05 LOCKED）"
    - "Segment Calibration タブが scalar 指標表（ECE/MCE/max_dev_guarded・D-11）を st.dataframe で併記する（UI-SPEC Component Inventory）"
    - "Segment JSON が未生成の場合 st.info で「segment データが未生成です。scripts/run_evaluation.py を実行して reports/06-segments/ を生成してください。」を表示する（UI-SPEC Copywriting・empty state）"
    - "レース一覧のデフォルトソートが race_date DESC（最新日優先）・馬別詳細が p_fukusho_hit DESC（確率高い順・UI-SPEC Interaction Contract）"
    - "DB 接続失敗時に st.error で「PostgreSQL（keiba_readonly ロール）に接続できません。src/config/ の DSN と PostgreSQL 起動状態を確認してください。」を表示し生 DSN を含めない（UI-SPEC Copywriting・ASVS V8）"
    # read-only 保証（Phase 8 TEST-01 前提）
    - "src/ui/ 配下（app.py/prediction_tab.py/backtest_tab.py/calibration_tab.py 含む）に書き込み/DDL SQL が含まれず write_cursor/make_pool(role='etl') を使用しない（Plan 01 の test_readonly_guarantee.py が継続 green）"
    - "派生データ（フィルタ結果・選択レースの馬 DataFrame 等）を st.session_state に保持しない（CLAUDE.md 指示・@st.cache_data のみ・Pitfall 4）"
    - "§16.1 除外項目（ワイド候補/ワイド期待値/荒れ指数/コメント生成）が UI に一切表示されない（Phase 2 以降・UI-SPEC 設計原則4）"
  artifacts:
    - path: "src/ui/app.py"
      provides: "Streamlit エントリポイント・st.title/st.sidebar/st.tabs(3)・pool ライフサイクル"
      contains: "st.tabs"
    - path: "src/ui/prediction_tab.py"
      provides: "予測一覧タブ・マスター・ディテール・selection_mode 正引数・再現性スタンプ inline・CSV download"
      contains: "selection_mode"
    - path: "src/ui/backtest_tab.py"
      provides: "Backtest タブ・honest 注記・メトリクス表示・CSV download"
      contains: "st.warning"
    - path: "src/ui/calibration_tab.py"
      provides: "Segment Calibration タブ・6軸 selectbox・Plotly 動的描画・scalar 表"
      contains: "st.plotly_chart"
    - path: "tests/ui/test_ui_render_contract.py"
      provides: "UI 描画契約の AST 検証（st.title 1回・st.tabs 3・st.sidebar フィルタ・download_button ラベル・honest 注記文字列・§16.1 除外項目不存在）"
      contains: "selection_mode"
  key_links:
    - from: "src/ui/app.py"
      to: "src/ui/loaders.py"
      via: "load_predictions/load_backtests/load_segment_json を import・@st.cache_data でキャッシュ"
      pattern: "from src\\.ui\\.loaders import"
    - from: "src/ui/prediction_tab.py"
      to: "src/ui/loaders.py::build_prediction_csv_bytes"
      via: "st.download_button の data= に CSV bytes を渡す（D-04 DRY）"
      pattern: "build_prediction_csv_bytes"
    - from: "src/ui/calibration_tab.py"
      to: "reports/06-segments/<axis>.json"
      via: "load_segment_json(axis) で読込・build_calibration_figure で Plotly Figure 構築・st.plotly_chart で描画"
      pattern: "load_segment_json"
  prohibitions:
    - "st.dataframe に selection='single-row'（古い API・UI-SPEC 誤記）を渡さない・正引数 selection_mode='single-row' + on_select='rerun' を使用する（Pitfall 1・RESEARCH A1）"
    - "派生データ（フィルタ結果・馬 DataFrame 等）を st.session_state に保持しない（CLAUDE.md・@st.cache_data のみ・Pitfall 4）"
    - "@st.cache_data に ConnectionPool を hash_funcs={ConnectionPool: id} なしで渡さない（UnhashableParamError・Pitfall 2）・loaders.py 側で既に hash_funcs 付きで定義済みの関数を再利用"
    - "UI 配下に書き込み/DDL SQL・write_cursor・make_pool(role='etl') を含めない（read-only 保証・D-03）"
    - "§16.1 除外項目（ワイド候補/ワイド期待値/荒れ指数/コメント生成）を UI に表示しない（Phase 2 以降・UI-SPEC 設計原則4）"
    - "backtest の回収率/EV に「確定」「的中」等の確定ニュアンスの語を使用しない（UI-SPEC Copywriting・常に「暫定」「参考」修飾・実馬券購入しない個人分析ツール）"
    - "st.error に生 DSN・パスワードを含めない（dsn_masked のみ・ASVS V8）"
    - "サードパーティ Streamlit コンポーネントを使用しない（UI-SPEC Registry Safety・Streamlit 公式 + Plotly 公式のみ）"
    - "st.markdown で unsafe_allow_html=True を使用しない（デフォルト・UI-SPEC V13 API & Web Service）"
    - "Plotly 静的 HTML リンクを併設しない（D-05 で「HTML リンクのみ」は却下済み・UI-SPEC Performance Contract）"
---

<objective>
Phase 7 Presentation の**Streamlit UI 本体**を実装する。Plan 01（基盤・列定数・マッピング・テーマ・Wave 0 テスト）と Plan 02（loaders・CLI・CSV bytes 生成）の成果物を消費し、承認済み UI Design Contract（07-UI-SPEC.md・Sign-Off 6/6）どおりに read-only Streamlit アプリを構築する。

3タブ構成（予測一覧・Backtest・Segment Calibration）で D-01..D-05 LOCKED を実装に落とし、RESEARCH/PATTERNS で確定した修正（Streamlit 行選択の正引数 `selection_mode='single-row'` + `on_select='rerun'`・UI-SPEC の `selection='single-row'` は古い API・Pitfall 1）を反映する。再現性スタンプ5項目の inline 表示（§19.1 聖域）・honest 注記（backtest odds 再検証 subject・Phase 5 manual-only 整合）・§16.1 除外項目（ワイド/荒れ指数/コメント生成）の排除を保証する。

本 plan は UI 描画契約の AST/文字列検証テスト（`test_ui_render_contract.py`・`test_streamlit_api_usage.py`）を中心に検証する。Streamlit 描画の実 E2E 起動検証は最終 checkpoint:human-verify（本 plan に1つ組込）で実施する。

Purpose: UI-01（Streamlit 画面で予測/EV/推奨ランク/スタンプ表示）と OUT-01/OUT-02 の UI download 経路を完成させ・Phase 7 SC#1/SC#2/SC#3 を達成する。
Output: `src/ui/{app.py,prediction_tab.py,backtest_tab.py,calibration_tab.py}`・`tests/ui/test_ui_render_contract.py`（`test_streamlit_api_usage.py` 拡張）
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-presentation/07-CONTEXT.md
@.planning/phases/07-presentation/07-RESEARCH.md
@.planning/phases/07-presentation/07-PATTERNS.md
@.planning/phases/07-presentation/07-UI-SPEC.md
@.planning/phases/07-presentation/07-01-PLAN.md
@.planning/phases/07-presentation/07-02-PLAN.md

# ソースコード契約（analog・実装の正）
@src/db/connection.py
@src/config/settings.py
@scripts/run_backtest.py

# Plan 01/02 成果物（SUMMARY 参照）
@src/ui/csv_columns.py
@src/ui/jyocd_map.py
@src/ui/loaders.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/ui/app.py + prediction_tab.py + backtest_tab.py + calibration_tab.py（UI-SPEC Layout/Component Inventory 実装・Pitfall 1 正引数・W1 分割非採用の根拠）</name>
  <files>src/ui/app.py, src/ui/prediction_tab.py, src/ui/backtest_tab.py, src/ui/calibration_tab.py</files>
  <read_first>
    - src/ui/loaders.py（Plan 02 成果・load_predictions/load_backtests/load_segment_json/build_*_csv_bytes/make_readonly_pool の import 元・UI と CLI DRY 共有）
    - src/ui/csv_columns.py（Plan 01 成果・PREDICTION_CSV_COLUMNS/BACKTEST_CSV_COLUMNS）
    - src/ui/jyocd_map.py（Plan 01 成果・load_jyocd_map・st.multiselect format_func 用）
    - src/config/settings.py（Settings().dsn・dsn_masked・ASVS V8・pool ライフサイクル）
    - src/db/connection.py（make_pool L21-63・readonly_cursor L66-76・shared pattern 2）
    - scripts/run_backtest.py（analog・entry pattern・Settings + masked DSN + try-finally pool close L1011-1054・import 慣例）
    - reports/06-segments/year.json（入力・segment JSON 実スキーマ・axis_name + segments[] curve/scalar/segment_value）
    - 07-UI-SPEC.md（§Layout Contract L40-85・§Component Inventory L222-238・§Interaction Contract L263-273・§Copywriting Contract L178-199・§Color L146-173・§Performance & Caching Contract L277-282）
    - 07-RESEARCH.md（§Pattern 1 行選択 L229-260・Pitfall 1 L374-377・§Code Examples L440-496 Plotly）
    - 07-PATTERNS.md（§src/ui/app.py L215-267・§prediction_tab.py L270-330・§backtest_tab.py L332-349・§calibration_tab.py L352-392）
  </read_first>
  <behavior>
    - Test 1 (test_ui_render_contract.py): src/ui/app.py が `st.title(` をちょうど1回含み（ページタイトル・UI-SPEC Typography L137）その文字列が「Keiba AI v3 — 複勝予測分析」を含む
    - Test 2 (test_ui_render_contract.py): src/ui/app.py が `st.tabs(` を1回以上含み・引数リストに「予測一覧」「Backtest」「Segment Calibration」の3ラベルを含む
    - Test 3 (test_ui_render_contract.py): src/ui/app.py が `st.sidebar` または `with st.sidebar:` を含み・`st.date_input`/`st.multiselect` を含む（D-02 フィルタ）
    - Test 4 (test_streamlit_api_usage.py): src/ui/ 配下の st.dataframe 呼出（AST 検出）で `selection=` キーワード引数が存在せず `selection_mode=` キーワード引数が存在する（Pitfall 1・prediction_tab.py のレース一覧）
    - Test 5 (test_ui_render_contract.py): src/ui/prediction_tab.py が `st.column_config.NumberColumn` を含み `format="%.3f"` または `format='%.3f'` を含む（UI-SPEC Typography・小数点以下3桁）
    - Test 6 (test_ui_render_contract.py): src/ui/prediction_tab.py が `st.download_button` を含み ラベル文字列「予測CSVをダウンロード」を含む
    - Test 7 (test_ui_render_contract.py): src/ui/backtest_tab.py が `st.warning(` を含み・その文字列が「JODDS オッズ取得完了後に再検証する subject」または「暫定値」を含む（honest 注記・UI-SPEC Copywriting・Pitfall 6）
    - Test 8 (test_ui_render_contract.py): src/ui/backtest_tab.py が `st.download_button` を含み ラベル「Backtest CSVをダウンロード」を含む
    - Test 9 (test_ui_render_contract.py): src/ui/calibration_tab.py が `st.selectbox` と `st.plotly_chart` を含み・`use_container_width=True` を含む（D-05）
    - Test 10 (test_ui_render_contract.py): src/ui/calibration_tab.py が6軸（year/month/jyocd/entry_count/ninki/odds_band）の選択肢を含む（D-12）
    - Test 11 (test_ui_render_contract.py): src/ui/ 配下の全 .py ファイルを走査し `unsafe_allow_html=True` が含まれない（V13 API・UI-SPEC Registry Safety）
    - Test 12 (test_ui_render_contract.py): src/ui/ 配下の全 .py ファイルに「ワイド」「荒れ指数」「コメント生成」の語が含まれない（§16.1 除外項目・UI-SPEC 設計原則4・ただし "ワイド" 単語が変数名等で偶発的に出現する可能性を考慮し・検査は st.markdown/st.write/st.caption の文字列リテラル内に限定する・AST または正規表現で検証）
    - Test 13 (test_readonly_guarantee.py): Plan 01 の test_readonly_guarantee.py が Plan 03 で src/ui 配下に4ファイル追加されても引き続き green（書き込み/DDL SQL・write_cursor/make_pool(role='etl') 不存在）
  </behavior>
  <action>
    **W1（Task 1 の app+prediction_tab / backtest+calibration への2分割検討）判断: 分割しない（現状4ファイル1 task 維持）。** 根拠: (1) 07-03 は既に Task 1（UI本体）→ Task 2（テスト）→ Task 3（checkpoint:human-verify）の3 task 構成で GSD の3 task/plan 上限に達しており・Task 1 を2分割すると4 task になり上限超過。(2) app.py が3つの `render_*` 関数（render_prediction_tab/render_backtest_tab/render_calibration_tab）を import し・pool と sidebar フィルタ引数を渡す密接な interface 依存があるため・分割して別 task にすると interface 不整合（引数の型・順序・戻り値）の silent 回帰リスクが高まる。(3) prediction_tab.py は OUT-01/SC#1 の中心で backtest_tab.py/calibration_tab.py は OUT-02/D-05 で要件 ID が分散しているが・UI-SPEC Layout Contract（L40-85）が3タブを一貫したレイアウトとして規定しており・一貫性担保には同時実装が有利。(4) 各ファイルの行数は PATTERNS 予想（app.py ~50行・prediction_tab.py ~80行・backtest_tab.py ~40行・calibration_tab.py ~60行）で・合計 ~230行・context budget の範囲内。以上から分割コストが利益を上回ると判断し現状維持する。

    1. **src/ui/app.py を作成**（UI-SPEC Layout Contract L40-85・PATTERNS L215-267 を正に実装）:
       - imports: `from __future__ import annotations`・`import streamlit as st`・`from src.config.settings import Settings`・`from src.ui.loaders import load_predictions, load_backtests, make_readonly_pool`・`from src.ui.jyocd_map import load_jyocd_map`・`from src.ui.prediction_tab import render_prediction_tab`・`from src.ui.backtest_tab import render_backtest_tab`・`from src.ui.calibration_tab import render_calibration_tab`
       - `st.set_page_config(page_title="Keiba AI v3 — 複勝予測分析", layout="wide")`（ページ先頭・Streamlit 命令・1回のみ）
       - `st.title("Keiba AI v3 — 複勝予測分析")`（UI-SPEC Copywriting・1回のみ）
       - `settings = Settings()`・`jyocd_map = load_jyocd_map()`（Plan 01 helper・code_tables.yaml から読込）
       - サイドバー（D-02 LOCKED）: `with st.sidebar:` 内で `st.date_input("日付範囲", [])`（range・空リストで初期値なし・ユーザ選択）・`st.multiselect("競馬場", options=list(jyocd_map.keys()), format_func=lambda c: jyocd_map.get(c, c), default=[])`・`st.multiselect("推奨ランク", ["S","A","B","C","D"], default=["S","A","B"])`（UI-SPEC Layout・default=S,A,B）・サイドバー下部に `st.caption("データソース: PostgreSQL keiba_readonly ロール（read-only・stamped）")`（UI-SPEC Copywriting・データソース注記）
       - `pool = make_readonly_pool(settings)`（Plan 02 loaders・try-finally で close）
       - `tab_pred, tab_bt, tab_cal = st.tabs(["予測一覧", "Backtest", "Segment Calibration"])`（UI-SPEC Layout・3タブ固定・multipage でない）
       - `with tab_pred: render_prediction_tab(pool, date_range, selected_jyocd, selected_ranks)`・`with tab_bt: render_backtest_tab(pool)`・`with tab_cal: render_calibration_tab()`
       - try-finally で pool close（Shared Pattern 3・run_backtest.py L1011-1054 踏襲）。DB 接続失敗時は `st.error("PostgreSQL（keiba_readonly ロール）に接続できません。src/config/ の DSN と PostgreSQL 起動状態を確認してください。")`（UI-SPEC Copywriting・dsn_masked のみ・生 DSN 絶対禁止）。
    2. **src/ui/prediction_tab.py を作成**（UI-SPEC Layout・PATTERNS L270-330・RESEARCH Pattern 1 を正に実装・Pitfall 1 正引数）:
       - `def render_prediction_tab(pool, date_range, selected_jyocd, selected_ranks):` 関数
       - `pred_df = load_predictions(pool, date_from=..., date_to=..., jyocd_list=selected_jyocd)`（Plan 02 loaders・@st.cache_data hash_funcs 付き・is_primary=true 絞り済み）
       - `selected_ranks` でフィルタ（recommend_rank 列が selected_ranks に含まれる行のみ・空リスト時は全件）。
       - レース一覧をrace_id 単位で groupby して `race_date`/`競馬場`/`レース番号`/`頭数`/`件数` の DataFrame を構築（派生データは @st.cache_data を通す・st.session_state に保持しない・Pitfall 4）。
       - `event = st.dataframe(race_list_df, selection_mode="single-row", on_select="rerun", hide_index=False)`（**Pitfall 1 正引数**・UI-SPEC の `selection="single-row"` は古い API・RESEARCH A1 で確定）。
       - `selection = event.selection; rows = selection.get("rows", [])`（single-row でも list で返る・Pitfall 1）
       - `if rows:` で `selected_idx = rows[0]; selected_race = race_list_df.iloc[selected_idx]` → 選択レースの馬詳細 DataFrame をロード・`st.dataframe(horse_df, column_config={"p_fukusho_hit": st.column_config.NumberColumn(format="%.3f"), "EV_lower": ..., "EV_upper": ...})`（UI-SPEC Typography・%.3f）
       - 再現性スタンプ5項目を `st.columns(5)` + `st.caption` で inline 表示（§19.1 聖域・odds_snapshot_policy/odds_snapshot_at/model_version/feature_snapshot_id/backtest_strategy_version・選択レースの代表値を表示）
       - `st.download_button(label="予測CSVをダウンロード", data=build_prediction_csv_bytes(horse_df), file_name=f"predictions_{race_date}.csv", mime="text/csv")`（UI-SPEC Copywriting・D-04・Plan 02 build_prediction_csv_bytes）
       - empty state: `st.info("レースを 1 行選択すると各馬の詳細が表示されます。")` または `st.info("表示対象のレースがありません。フィルタ条件（日付範囲・競馬場・推奨ランク）を確認してください。")`（UI-SPEC Copywriting）
    3. **src/ui/backtest_tab.py を作成**（UI-SPEC Layout・PATTERNS L332-349 を正に実装）:
       - `def render_backtest_tab(pool):` 関数
       - `bt_df = load_backtests(pool)`（Plan 02 loaders・全 backtest 行）
       - backtest 一覧を backtest_id 単位で groupby してメトリクス表（backtest_id/backtest_strategy_version/train_period/odds_snapshot_policy/recovery_rate/selected_count）を構築・`st.dataframe` で表示。
       - `st.metric("recovery_rate", value=...)`・`st.metric("selected_count", value=...)` 等（UI-SPEC Component Inventory・任意・honest 注記と併記）
       - `st.warning("注意: この backtest の odds 正確性は JODDS オッズ取得完了後に再検証する subject です。現状の回収率は暫定値であり、確定後に入れ替わる可能性があります。")`（UI-SPEC Copywriting・Pitfall 6・必須）
       - `st.caption("推奨ランクは参考情報であり、購入判断を強制するものではありません（§19.3・実馬券購入はスコープ外）。")`（UI-SPEC Copywriting）
       - `st.download_button(label="Backtest CSVをダウンロード", data=build_backtest_csv_bytes(bt_df), file_name=f"backtest.csv", mime="text/csv")`（UI-SPEC Copywriting・D-04・Plan 02 build_backtest_csv_bytes）
    4. **src/ui/calibration_tab.py を作成**（UI-SPEC Layout・PATTERNS L352-392・RESEARCH Code Examples L440-496 を正に実装）:
       - `def render_calibration_tab():` 関数（DB pool 不要・reports/06-segments/*.json のみ）
       - `SEGMENT_AXES = ("year", "month", "jyocd", "entry_count", "ninki", "odds_band")`（D-12 の6軸）
       - `axis = st.selectbox("segment 軸", SEGMENT_AXES)`（UI-SPEC Layout）
       - `seg_data = load_segment_json(axis)`（Plan 02 loaders・@st.cache_data）
       - `if not seg_data:` で `st.info("segment データが未生成です。scripts/run_evaluation.py を実行して reports/06-segments/ を生成してください。")`（UI-SPEC Copywriting・empty state）
       - `else:` で `fig = build_calibration_figure(seg_data)` → `st.plotly_chart(fig, use_container_width=True)`（D-05・UI-SPEC Performance Contract・`fig.show()` でない）
       - `build_calibration_figure(seg_data: dict) -> go.Figure` helper 関数（PATTERNS L357-379・RESEARCH L458-485）: curve 重ね描き + 完全予測線（dash・gray）・xaxis_range=[0,1]/yaxis_range=[0,1]・hovertemplate 日本語
       - scalar 指標表: `scalar_rows = [{"segment_value": s["segment_value"], **s["scalar"]} for s in seg_data["segments"]]` → `st.dataframe(scalar_rows)`（D-11・ECE/MCE/max_dev_guarded/n_samples）
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v3 && KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/ui/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/ui/ -x -q` が exit 0（Plan 01+02+03 全テスト green・回帰なし）
    - `grep -c 'st.title' src/ui/app.py` が 1（ページタイトル1回）
    - `grep -c 'st.tabs' src/ui/app.py` が 1 以上
    - `grep '予測一覧' src/ui/app.py` と `grep 'Backtest' src/ui/app.py` と `grep 'Segment Calibration' src/ui/app.py` が全て一致（3タブラベル）
    - `grep 'selection_mode' src/ui/prediction_tab.py` が一致（Pitfall 1 正引数）
    - `grep 'on_select' src/ui/prediction_tab.py` が一致（Pitfall 1 正引数）
    - `! grep 'selection=' src/ui/prediction_tab.py`（古い API 引数不存在・selection_mode の部分一致に注意・`selection=` として `=` まで含めて検証）
    - `grep 'st.column_config.NumberColumn' src/ui/prediction_tab.py` が一致
    - `grep '%.3f' src/ui/prediction_tab.py` が一致
    - `grep '予測CSVをダウンロード' src/ui/prediction_tab.py` が一致
    - `grep 'JODDS オッズ取得完了後に再検証する subject' src/ui/backtest_tab.py` が一致（honest 注記）
    - `grep 'Backtest CSVをダウンロード' src/ui/backtest_tab.py` が一致
    - `grep 'st.plotly_chart' src/ui/calibration_tab.py` が一致
    - `grep 'use_container_width=True' src/ui/calibration_tab.py` が一致
    - `grep -c 'unsafe_allow_html=True' src/ui/app.py src/ui/prediction_tab.py src/ui/backtest_tab.py src/ui/calibration_tab.py` が全て 0（V13）
    - `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/ui/test_readonly_guarantee.py -x -q` が exit 0（Plan 03 で src/ui 配下に4ファイル追加されても read-only 保証 green）
  </acceptance_criteria>
  <done>app.py/prediction_tab.py/backtest_tab.py/calibration_tab.py が UI-SPEC Layout/Component Inventory どおり実装され・Pitfall 1 正引数（selection_mode/on_select）・honest 注記・再現性スタンプ inline・§16.1 除外項目排除・read-only 保証の全テスト green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test_ui_render_contract.py と test_streamlit_api_usage.py 拡張（UI 描画契約の機械検証・Phase 8 TEST-01 前提）</name>
  <files>tests/ui/test_ui_render_contract.py, tests/ui/test_streamlit_api_usage.py</files>
  <read_first>
    - src/ui/app.py（Task 1 成果・検証対象）
    - src/ui/prediction_tab.py（Task 1 成果・検証対象・selection_mode 正引数）
    - src/ui/backtest_tab.py（Task 1 成果・検証対象・honest 注記）
    - src/ui/calibration_tab.py（Task 1 成果・検証対象・6軸 selectbox・Plotly）
    - tests/ui/test_streamlit_api_usage.py（Plan 01 成果・Task 2 で拡張・selection_mode/on_select 検証を本格化）
    - 07-UI-SPEC.md（§Copywriting Contract・§Component Inventory・§Interaction Contract・§Layout Contract）
    - 07-RESEARCH.md（§Pitfall 1・A1 selection_mode 正引数確定）
  </read_first>
  <behavior>
    - Test 1 (test_ui_render_contract.py): app.py の `st.title(` 呼出が1回（AST で st.title の Attribute Call をカウント）で文字列が「Keiba AI v3 — 複勝予測分析」を含む
    - Test 2 (test_ui_render_contract.py): app.py の `st.tabs(` 呼出の第一引数（list）の要素に「予測一覧」「Backtest」「Segment Calibration」の3ラベルが全て含まれる
    - Test 3 (test_ui_render_contract.py): app.py に `st.sidebar`（Attribute access または with 文）が存在し・`st.date_input` と `st.multiselect` が存在する（D-02）
    - Test 4 (test_streamlit_api_usage.py): prediction_tab.py の `st.dataframe(` 呼出（AST）で `selection=` キーワード引数（古い API）が存在せず・`selection_mode=` キーワード引数が存在する（Pitfall 1）
    - Test 5 (test_ui_render_contract.py): prediction_tab.py のソースに `st.column_config.NumberColumn` と `format="%.3f"` または `format='%.3f'` が含まれる
    - Test 5b (test_ui_render_contract.py): prediction_tab.py の `st.dataframe`（馬詳細描画）の `column_config` dict の key に SC#1 の6数値列（`p_fukusho_hit`/`EV_lower`/`EV_upper`/`fuku_odds_lower`/`fuku_odds_upper`/`recommend_rank`）が全て含まれることを AST または文字列検証で確認（BLOCKER-4・SC#1 user-observable truth・どれか1つでも欠けると SC#1 達成不可・Open Question #1/#2 解決経路で算出された値が実際に表示経路に乗っていることの機械保証）
    - Test 6 (test_ui_render_contract.py): prediction_tab.py の `st.download_button` 呼出の label 文字列に「予測CSVをダウンロード」が含まれる
    - Test 7 (test_ui_render_contract.py): backtest_tab.py の `st.warning(` 呼出の文字列に「JODDS オッズ取得完了後に再検証する subject」または「暫定値」が含まれる（honest 注記）
    - Test 8 (test_ui_render_contract.py): backtest_tab.py の `st.download_button` label に「Backtest CSVをダウンロード」が含まれる
    - Test 9 (test_ui_render_contract.py): calibration_tab.py に `st.plotly_chart(` と `use_container_width=True` が含まれる
    - Test 10 (test_ui_render_contract.py): calibration_tab.py の `st.selectbox` の options に6軸（year/month/jyocd/entry_count/ninki/odds_band）が全て含まれる
    - Test 11 (test_ui_render_contract.py): src/ui/ 配下の全 .py ファイルに `unsafe_allow_html=True` が含まれない（AST または grep・V13）
    - Test 12 (test_ui_render_contract.py): src/ui/ 配下の st.markdown/st.write/st.caption/st.title/st.header/st.subheader の文字列リテラル（AST で Constant node 収集）に「ワイド」「荒れ指数」「コメント生成」の語が含まれない（§16.1 除外項目・UI-SPEC 設計原則4）
    - Test 13 (test_ui_render_contract.py・W2): `build_calibration_figure` を合成 `seg_data`（3 segment × 各 curve 4点・完全予測線 dash gray 1本）で呼出し・戻り値の `go.Figure` の `len(fig.data)` が期待 trace 数（3 segment curve + 1 完全予測線 = 4 trace）と一致することを assert（W2・Plotly trace 数の回帰検出・空 Figure や trace 欠落を fail-loud）
  </behavior>
  <action>
    1. **tests/ui/test_ui_render_contract.py を作成**（behavior Test 1-3・5-12 を実装・AST + 文字列検証）:
       - `_read(py_path) -> str` helper・`_ast_calls(source, attr_name)` helper で `st.XXX` Call ノードを収集
       - `test_app_title_once_with_ja`: app.py の `st.title` Call が1回・定数文字列が「Keiba AI v3 — 複勝予測分析」を含む
       - `test_app_tabs_three_labels`: app.py の `st.tabs` Call の第一引数（list of Constant）に3ラベルが全て含まれる
       - `test_app_sidebar_filters`: app.py に `st.sidebar`（with 文または Attribute access）・`st.date_input`・`st.multiselect` が存在
       - `test_prediction_tab_number_column_format`: prediction_tab.py のソースに `st.column_config.NumberColumn` と `%.3f` が含まれる
       - `test_prediction_tab_column_config_has_six_sc1_columns`: prediction_tab.py の `st.dataframe` Call ノード（AST）の `column_config` keyword 引数の dict keys（定数文字列のみ収集・変数は resolve 不可なので対象外だが・定数文字列の key が最低限含まれることを検証）に SC#1 の6数値列（`p_fukusho_hit`/`EV_lower`/`EV_upper`/`fuku_odds_lower`/`fuku_odds_upper`/`recommend_rank`）が全て含まれることを assert（BLOCKER-4・`REQUIRED = {"p_fukusho_hit","EV_lower","EV_upper","fuku_odds_lower","fuku_odds_upper","recommend_rank"}; assert REQUIRED <= set(column_config_keys)`）。変数で渡している場合は文字列 fallback 検証（prediction_tab.py のソースに6列名が全て文字列として出現することを grep 的に確認・AST と併用）
       - `test_prediction_tab_download_label`: prediction_tab.py の `st.download_button` Call の label 文字列に「予測CSVをダウンロード」が含まれる
       - `test_backtest_tab_honest_warning`: backtest_tab.py の `st.warning` Call の文字列に「JODDS オッズ取得完了後に再検証する subject」または「暫定値」が含まれる（Pitfall 6・UI-SPEC Copywriting）
       - `test_backtest_tab_download_label`: backtest_tab.py の `st.download_button` label に「Backtest CSVをダウンロード」
       - `test_calibration_tab_plotly`: calibration_tab.py に `st.plotly_chart` と `use_container_width=True`
       - `test_calibration_tab_six_axes`: calibration_tab.py の `st.selectbox` options に6軸が全て含まれる（D-12）
       - `test_no_unsafe_allow_html`: src/ui/ 配下の全 .py のソースに `unsafe_allow_html=True` が含まれない
       - `test_no_excluded_section16_1_terms`: src/ui/ 配下の .py の AST を走査し st.markdown/st.write/st.caption/st.title/st.header/st.subheader の Call の第一引数（定数文字列のみ・変数は対象外）を収集し「ワイド」「荒れ指数」「コメント生成」の語が含まれない（§16.1 除外項目）
       - `test_build_calibration_figure_trace_count`（W2）: 合成 `seg_data = {"axis_name":"year","segments":[{"segment_value":"2023","curve":{"count":[10,10,10,10],"frac_pos":[0.1,0.3,0.6,0.9],"mean_pred":[0.12,0.28,0.62,0.88]},"scalar":{...}}]}` を3 segment 構成で `build_calibration_figure`（calibration_tab.py から import）に渡し・`fig = build_calibration_figure(seg_data)` の `len(fig.data)` が `len(seg_data["segments"]) + 1`（各 segment curve + 完全予測線 dash gray 1本 = 3+1=4 trace）と一致することを assert。`build_calibration_figure` が calibration_tab.py 内の関数で import 可能である前提・import 不可の場合は `pytest.importorskip("src.ui.calibration_tab")` で Plan 03 Task 1 完了まで skip（W2・trace 欠落/空 Figure の回帰検出）
    2. **tests/ui/test_streamlit_api_usage.py を拡張**（Plan 01 雛形・behavior Test 4 を本格化）:
       - Plan 01 で作成した `test_no_legacy_selection_arg` を強化: src/ui/ 配下の `st.dataframe` Call（AST）を走査し・`selection=` キーワード引数が存在する場合は fail（古い API・Pitfall 1）。
       - `test_dataframe_uses_selection_mode`: `st.dataframe` Call が存在する場合（prediction_tab.py のレース一覧）は `selection_mode=` キーワード引数を持つことを検証。
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v3 && KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/ui/test_ui_render_contract.py tests/ui/test_streamlit_api_usage.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/ui/test_ui_render_contract.py -x -q` が exit 0
    - `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/ui/test_streamlit_api_usage.py -x -q` が exit 0
    - `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/ui/ -q` が exit 0（Plan 01+02+03 全テスト green）
    - `grep -c 'def test_app_title_once_with_ja\|def test_app_tabs_three_labels\|def test_backtest_tab_honest_warning\|def test_no_unsafe_allow_html\|def test_no_excluded_section16_1_terms\|def test_prediction_tab_column_config_has_six_sc1_columns\|def test_build_calibration_figure_trace_count' tests/ui/test_ui_render_contract.py` が7以上（BLOCKER-4 の SC#1 6列検証 + W2 trace 数検証含む）
    - `grep -c 'def test_no_legacy_selection_arg\|def test_dataframe_uses_selection_mode' tests/ui/test_streamlit_api_usage.py` が2
  </acceptance_criteria>
  <done>UI 描画契約（UI-SPEC Layout/Component Inventory/Copywriting/Color・Pitfall 1 正引数・§16.1 除外項目・V13 unsafe_allow_html）が AST/文字列検証で機械保証され・Phase 8 TEST-01 前提が確立</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: checkpoint:human-verify — Streamlit UI 実ブラウザ・live-DB 挙動確認（UI-01・OUT-01/OUT-02・D-05）</name>
  <action>Streamlit UI 本体（app.py + 3タブ）と OUT-01/OUT-02 CLI/UI 出力経路の実ブラウザ・live PostgreSQL 挙動を人間が確認する。自動テストは green だが・Streamlit 描画の実体感・live DB からのデータ表示・CSV ダウンロードの実動作・Plotly 動的描画は手動確認が必要。下記 how-to-verify に従い全項目を実施し approved または課題を返す。</action>
  <verify>
    人間がブラウザ（http://localhost:8501）で UI-01（予測一覧・スタンプ inline・ダウンロード）・OUT-01/OUT-02 CSV・D-05 Plotly・honest 注記・§16.1 除外項目を確認し approved を返す。
    <automated>cd /Users/hart/develop/keiba-ai-v3 && KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/ui/ -q && uv run python -c "import src.ui.app, src.ui.prediction_tab, src.ui.backtest_tab, src.ui.calibration_tab, src.ui.loaders, src.ui.csv_columns, src.ui.jyocd_map; print('import smoke OK')" && uv run python scripts/run_export_predictions_csv.py --help >/dev/null && uv run python scripts/run_export_backtest_csv.py --help >/dev/null && echo 'pre-checkpoint automated gate OK'</automated>
  </verify>
  <done>Streamlit UI が UI-SPEC Layout/Component Inventory どおり実動作し・OUT-01/OUT-02 の UI/CLI 両経路で CSV が生成され・Phase 7 SC#1/2/3 が人間の目で確認される</done>
  <what-built>
    Phase 7 Presentation の Streamlit UI 本体（app.py + 3タブ）と OUT-01/OUT-02 の CLI/UI 出力経路が完成しました。自動テスト（Plan 01/02/03 の全 unit/contract test）は green ですが・Streamlit 描画の実ブラウザ挙動・live PostgreSQL からのデータ表示・CSV ダウンロードの実動作・Plotly 動的描画の体感は手動確認が必要です。
  </what-built>
  <how-to-verify>
    1. **事前確認**: `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/ui/ -q` を実行し全テスト green を確認。
    2. **Streamlit 起動**: `uv run streamlit run src/ui/app.py` を実行（ポート8501で起動）。ブラウザで http://localhost:8501 を開く。
    3. **ページタイトル**: ページ先頭に「Keiba AI v3 — 複勝予測分析」が表示される（st.title 1回）。
    4. **3タブ**: 「予測一覧」「Backtest」「Segment Calibration」の3タブが表示される。
    5. **サイドバー（D-02）**: 左サイドバーに「日付範囲」「競馬場」（コード→名称変換で札幌/函館...小倉）「推奨ランク」（default=S,A,B）のフィルタがある。
    6. **予測一覧タブ（UI-01）**:
       - レース一覧（race_date DESC）が表示される。日付範囲・競馬場・推奨ランクのフィルタを変更して再描画される（@st.cache_data・<1s）。
       - レース一覧の行を1つ選択（single-row）→ 下部に各馬の `p_fukusho_hit`/`EV_lower`/`EV_upper`/`fuku_odds_lower/upper`/`recommend_rank` が小数点以下3桁（%.3f）で表示される。
       - 推奨ランク S の行が accent 色（#FF4B4B）で強調される（色弱配慮でラベル併記）。
       - 再現性スタンプ5項目（odds_snapshot_policy/odds_snapshot_at/model_version/feature_snapshot_id/backtest_strategy_version）が inline 表示される（§19.1 聖域）。
       - 「予測CSVをダウンロード」ボタンをクリック → UTF-8 BOM 付き CSV（Excel で開いて日本語馬名が化けない）がダウンロードされる。ヘッダが20列。
    7. **Backtest タブ（OUT-02 表示元）**:
       - backtest 一覧が表示される。
       - 黄色い `st.warning` で「注意: この backtest の odds 正確性は JODDS オッズ取得完了後に再検証する subject です。現状の回収率は暫定値...」が表示される（honest 注記・Pitfall 6）。
       - 「推奨ランクは参考情報であり...」の `st.caption` が表示される。
       - 「Backtest CSVをダウンロード」ボタンで CSV がダウンロードされる。ヘッダが16列。
    8. **Segment Calibration タブ（D-05）**:
       - 「segment 軸」selectbox で6軸（year/month/jyocd/entry_count/ninki/odds_band）が切替可能。
       - Plotly の calibration curve がインタラクティブに描画される（curve 重ね描き + 完全予測線）。
       - scalar 指標表（ECE/MCE/max_dev_guarded/n_samples）が表示される。
       - 軸切替で即時再描画される（@st.cache_data）。
    9. **§16.1 除外項目**: UI のどこにも「ワイド」「荒れ指数」「コメント生成」が表示されない（Phase 2 以降）。
    10. **CLI 出力（OUT-01/OUT-02）**: 別ターミナルで `uv run python scripts/run_export_predictions_csv.py --output /tmp/pred.csv --date-from 2024-01-01 --date-to 2024-12-31` を実行 → `/tmp/pred.csv` が生成される（22,213行・主モデル=LightGBM・is_primary=true 絞り）。`uv run python scripts/run_export_backtest_csv.py --output /tmp/bt.csv` で backtest CSV が生成される。
    11. **read-only 保証**: Streamlit 実行中に PostgreSQL 側で書き込みが発生しないことを確認（オプション・pg_stat_activity 等で確認可能）。
  </how-to-verify>
  <resume-signal>Type "approved"（全項目確認OK）または課題を記述（修正後再確認）</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→Streamlit server | ローカル HTTP（localhost:8501）・単一ユーザー・外部 API/fetch なし（UI-SPEC Registry Safety） |
| Streamlit server→PostgreSQL | UI が keiba_readonly ロール（GRANT 済み・SELECT のみ）で schema 修飾 SELECT・書き込み/DDL 経路不存在 |
| UI→CSV bytes | loaders.py の build_*_csv_bytes が列 assert で fail-loud・CSV ヘッダが PREDICTION_CSV_COLUMNS/BACKTEST_CSV_COLUMNS と1:1 |
| UI→reports/*.json | load_segment_json が reports/06-segments/*.json を読込むだけ・再集計しない（stamped 成果物消費・§19.1） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12 | Tampering（API 誤用・Silent Failure） | prediction_tab.py・st.dataframe selection API | mitigate | Pitfall 1 正引数 `selection_mode='single-row'` + `on_select='rerun'` を使用（RESEARCH A1 で確定・UI-SPEC の `selection=` は古い API）・test_streamlit_api_usage.py::test_no_legacy_selection_arg と test_dataframe_uses_selection_mode で AST 検証 |
| T-07-13 | Tampering（データ完全性・再現性スタンプ聖域） | prediction_tab.py・再現性スタンプ inline 表示 | mitigate | odds_snapshot_policy/odds_snapshot_at/model_version/feature_snapshot_id/backtest_strategy_version を選択レースの各予測行に inline 表示（§19.1・UI-SPEC L231）・odds_snapshot_at は JODDS snapshot（`select_odds_snapshot` 戻り値・L213/L326）から取得（Open Question #1 解決経路・backtest JOIN でなく・backtest テーブルに odds 値カラム不存在のため構造的不可）・model_version/feature_snapshot_id は prediction SELECT から・backtest_strategy_version は python 側で固定値='fukusho_ev_v1'（schema.py L148 CHECK 制約）・odds_snapshot_policy は UI/CLI 引数で事前固定（§11.2・事後選択禁止）・欠落時は Phase 8 が TEST-01 で対抗的監査 |
| T-07-14 | Tampering（hindsight odds・過大表示） | backtest_tab.py・回収率表示 | mitigate | `st.warning` で「odds 正確性は JODDS 取得完了後に再検証する subject・現状は暫定値」と注記（Pitfall 6・UI-SPEC Copywriting・Phase 5 manual-only 整合）・test_backtest_tab_honest_warning で文字列検証・確定ニュアンスの語を使用しない |
| T-07-15 | Tampering（スコープ逸脱・§16.1 除外項目） | 全 UI ファイル | mitigate | ワイド/荒れ指数/コメント生成を UI 文字列リテラルに含めない（Phase 2 以降）・test_no_excluded_section16_1_terms で AST 検証・UI-SPEC 設計原則4 |
| T-07-16 | Repudiation（Information Disclosure via DSN） | app.py・DB 接続失敗時 | mitigate | `st.error` に生 DSN を含めない（dsn_masked のみ・Shared Pattern 8・ASVS V8）・UI-SPEC Copywriting の Error state 文字列のみ使用 |
| T-07-17 | Tampering（派生データ陳腐化・CLAUDE.md 違反） | 全 UI ファイル・st.session_state | mitigate | 派生データを st.session_state に保持しない・@st.cache_data で再計算（CLAUDE.md 指示・Pitfall 4）・コードレビューと test_readonly_guarantee.py の範囲で検出（st.session_state への派生データ代入がないことを grep/AST 検証は本 plan では省略・Phase 8 で強化） |
| T-07-18 | Tampering（XSS・unsafe HTML） | 全 UI ファイル・st.markdown | mitigate | `unsafe_allow_html=True` を使用しない（デフォルト・UI-SPEC V13）・test_no_unsafe_allow_html で検証 |
| T-07-19 | Elevation of Privilege（サードパーティコンポーネント） | pyproject.toml・streamlit 依存 | mitigate | Streamlit 公式 + Plotly 公式のみ・サードパーティ Streamlit コンポーネントを使用しない（UI-SPEC Registry Safety）・pyproject.toml dependencies が streamlit==1.58.0 + plotly>=6.8.0 のみで証明 |
</threat_model>

<verification>
Plan 03 の検証（UI-01・OUT-01/OUT-02 UI 経路・Phase 7 SC#1/2/3）:

1. **UI 描画契約**: `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/ui/test_ui_render_contract.py -x -q` が green（UI-SPEC Layout/Component/Copywriting/Color 契約・§16.1 除外・unsafe_allow_html なし）
2. **Streamlit API 正引数**: `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/ui/test_streamlit_api_usage.py -x -q` が green（Pitfall 1・selection_mode/on_select）
3. **read-only 保証継続**: `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/ui/test_readonly_guarantee.py tests/ui/test_loaders_readonly.py -x -q` が green（Plan 03 で4ファイル追加後も書き込み/DDL/write_cursor/role='etl' 不存在）
4. **統合ゲート**: `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/ui/ -q` が exit 0（Plan 01+02+03 全テスト green・回帰なし）
5. **実起動**: `uv run streamlit run src/ui/app.py` が TypeError/StreamlitAPIException なく起動（ポート8501）
6. **CLI 経路**: `uv run python scripts/run_export_predictions_csv.py --help` と `scripts/run_export_backtest_csv.py --help` が exit 0
7. **checkpoint:human-verify**: Streamlit ブラウザでの UI-01（予測一覧・スタンプ・ダウンロード）・OUT-01/OUT-02 CSV・D-05 Plotly が承認される
</verification>

<success_criteria>
- streamlit run src/ui/app.py が起動し UI-SPEC Layout どおりの3タブ＋サイドバーが表示される
- 予測一覧タブで selection_mode='single-row' + on_select='rerun' でマスター・ディテール展開・%.3f 数値・推奨ランク色分け・再現性スタンプ5項目 inline が動作（Pitfall 1 正引数解決）
- 予測CSVダウンロード（OUT-01・20列・UTF-8 BOM）が UI/CLI 両経路で動作
- Backtest タブで honest 注記（JODDS 再検証 subject・暫定値）と CSV ダウンロード（OUT-02・16列）が動作
- Segment Calibration タブで6軸切替の Plotly 動的描画 + scalar 表が動作（D-05）
- §16.1 除外項目（ワイド/荒れ指数/コメント生成）が UI に一切表示されない
- read-only 保証（keiba_readonly ロール・書き込み/DDL 経路不存在）が維持される
- UI 描画契約と Streamlit API 正引数のテストが green
- Phase 7 SC#1（UI-01・予測/EV/推奨ランク/スタンプ inline 表示）・SC#2（OUT-01 予測CSV）・SC#3（OUT-02 backtest CSV）が達成される
</success_criteria>

<output>
Create `.planning/phases/07-presentation/07-03-SUMMARY.md` when done
</output>
