---
phase: 07-presentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - src/ui/__init__.py
  - src/ui/csv_columns.py
  - src/ui/jyocd_map.py
  - .streamlit/config.toml
  - tests/ui/__init__.py
  - tests/ui/test_csv_columns.py
  - tests/ui/test_readonly_guarantee.py
  - tests/ui/test_segment_schema.py
  - tests/ui/test_streamlit_api_usage.py
autonomous: true
requirements: [UI-01, OUT-01, OUT-02]
user_setup: []

must_haves:
  truths:
    # UI-SPEC Component Inventory / CSV Export Contract からのリフト
    - "PREDICTION_CSV_COLUMNS は20列（race_id/race_date/race_start_datetime/競馬場/レース番号/horse_id/horse_name/枠番/馬番/p_fukusho_hit/fukusho_odds_lower/fukusho_odds_upper/EV_lower/EV_upper/recommend_rank/odds_snapshot_policy/odds_snapshot_at/model_version/feature_snapshot_id/prediction_created_at）かつ §16.2 原典と1:1"
    - "BACKTEST_CSV_COLUMNS は16列（backtest_id/backtest_strategy_version/train_period/validation_period/odds_snapshot_policy/race_id/horse_id/selected_flag/stake/refund_flag/payout_amount/profit/fukusho_hit_validated/recommend_rank/EV_lower/EV_upper）であり「14列」表記は誤記として17列目以降不存在（要件§16.2 原典優先・RESEARCH Pitfall 3）"
    - "予測 CSV 用スタンプは4項目+created_at（odds_snapshot_policy/odds_snapshot_at/model_version/feature_snapshot_id/prediction_created_at）が PREDICTION_CSV_COLUMNS に過不足なく含まれ・UI 行表示用に5項目（上記4項目+backtest_strategy_version）を使い分ける（§19.1 聖域・REVIEW MEDIUM-1 解決・backtest_strategy_version は予測テーブルに非存在のため CSV 定数からは除外し UI 側で付与）"
    - "streamlit==1.58.0 が uv add 済み・uv run python -c 'import streamlit' が成功する"
    - "hatch wheel packages に src/ui が追加され uv run python -c 'from src.ui.csv_columns import PREDICTION_CSV_COLUMNS' が成功する"
    - "jyocd→競馬場名マッピングが src/config/code_tables.yaml から PyYAML 読込で取得でき UI 側に dict ハードコードを持たない（DRY）"
    - ".streamlit/config.toml が [theme] base='light' primaryColor='#FF4B4B' を持つ（UI-SPEC Color Contract）"
    # read-only 保証（Phase 8 TEST-01 前提）
    - "tests/ui/test_readonly_guarantee.py が src/ui/ 配下に書き込み/DDL SQL キーワード（insert into/update /delete from/truncate /create table/drop table/alter table）が含まれないことを検証する"
  artifacts:
    - path: "src/ui/__init__.py"
      provides: "src/ui パッケージ化"
      min_lines: 1
    - path: "src/ui/csv_columns.py"
      provides: "PREDICTION_CSV_COLUMNS (20列) / BACKTEST_CSV_COLUMNS (16列) の DRY 単一定数"
      contains: "PREDICTION_CSV_COLUMNS"
    - path: "src/ui/jyocd_map.py"
      provides: "code_tables.yaml からの jyocd→競馬場名マッピング読込 helper"
      contains: "def load_jyocd_map"
    - path: ".streamlit/config.toml"
      provides: "Streamlit light theme + primaryColor=#FF4B4B（UI-SPEC Color Contract）"
      contains: "primaryColor"
    - path: "tests/ui/test_csv_columns.py"
      provides: "OUT-01/OUT-02 列定数 presence assert（§16.2 1:1・LOW-05 再利用）"
      contains: "PREDICTION_CSV_COLUMNS"
    - path: "tests/ui/test_readonly_guarantee.py"
      provides: "read-only 保証の構造的検証（AST/grep・Phase 8 TEST-01 前提）"
      contains: "test_ui_has_no_write_ddl_sql"
    - path: "tests/ui/test_segment_schema.py"
      provides: "reports/06-segments/*.json 6軸スキーマ契約検証"
      contains: "test_segment_schema_contract"
    - path: "tests/ui/test_streamlit_api_usage.py"
      provides: "selection_mode/on_select 正引数使用の事前検証土俵（Pitfall 1）"
      contains: "selection_mode"
  key_links:
    - from: "src/ui/csv_columns.py"
      to: "要件§16.2 pin"
      via: "PREDICTION_CSV_COLUMNS/BACKTEST_CSV_COLUMNS が原典行1092-1133と1:1・test_csv_columns.py で機械検証"
      pattern: "PREDICTION_CSV_COLUMNS"
    - from: "src/ui/jyocd_map.py"
      to: "src/config/code_tables.yaml"
      via: "PyYAML safe_load・UI 側にマッピングをハードコードしない"
      pattern: "code_tables\\.yaml"
  prohibitions:
    # UI-SPEC §16.1 除外項目・read-only 保証
    - "src/ui/ 配下に書き込み/DDL SQL（INSERT/UPDATE/DELETE/TRUNCATE/CREATE TABLE/DROP TABLE/ALTER TABLE）を含めない（read-only 保証・D-03・Phase 8 TEST-01 前提）"
    - "src/ui/ 配下で make_pool(role='etl') / write_cursor を使用しない（keiba_readonly ロールのみ）"
    - "UI 側に jyocd→競馬場名の dict をハードコードしない（code_tables.yaml が単一ソース・DRY）"
    - "PREDICTION_CSV_COLUMNS を20列以外・BACKTEST_CSV_COLUMNS を16列以外で定義しない（§16.2 原典優先・14列表記は誤記）"
    - "再現性スタンプ5項目（odds_snapshot_policy/odds_snapshot_at/model_version/feature_snapshot_id/backtest_strategy_version）をいずれかの CSV 列定数から欠落させない（§19.1 聖域）"
    - "サードパーティ Streamlit コンポーネント（streamlit-components 等）を依存に追加しない（UI-SPEC Registry Safety・Streamlit 公式 + Plotly 公式のみ）"
---

<objective>
Phase 7 Presentation の**基盤**を構築する。後続 Plan 02（loaders + CLI）・Plan 03（Streamlit UI 本体）が消費する DRY 単一定数（`PREDICTION_CSV_COLUMNS`/`BACKTEST_CSV_COLUMNS`）・jyocd→競馬場名マッピング helper・Streamlit 依存追加・wheel packages 登録・`.streamlit/config.toml`（UI-SPEC Color Contract）・Wave 0 テスト集群（列定数 presence assert・read-only 保証・segment JSON スキーマ契約・Streamlit API 正引数）を作成する。

本 plan は D-04 LOCKED（UI と CLI で同一列定数を DRY 共有）と UI-SPEC 承認済み契約（Color/CSV Export Contract/Registry Safety）をコード上の契約として固定化することが目的。実データ SELECT・Streamlit 描画ロジック本体は後続 Plan 02/03 で実装する。

Open Questions 解決結果（planner が src/ev/ev_rank.py・src/ev/odds_snapshot.py・src/db/schema.py・src/db/backtest_load.py を読んで確定・07-02-PLAN.md revision で正経路を再確定）:
1. `odds_snapshot_at` の予測 CSV 取得元 — `prediction.fukusho_prediction` DDL（src/db/schema.py L59-94）に同名列なし。**正経路: `src/ev/odds_snapshot.py::select_odds_snapshot` 戻り値の `odds_snapshot_at` 列（選択 snapshot の `happyo_datetime` Timestamp・L213/L326）から取得**（`fetch_jodds` + `select_odds_snapshot(policy)`・`merge_asof(direction='backward')` で未来リーク構造的不可・D-02）。※当初「backtest テーブル JOIN」と構想したが backtest テーブル（`BACKTEST_COLUMNS` L77-116）に odds 値カラム `fuku_odds_lower`/`fuku_odds_upper` が不存在のため構造的に不可能と判明（07-02 revision で否定）。`prediction_created_at` は `as_of_datetime` を代用（予測生成時点タイムスタンプ・schema.py L65）。
2. EV/odds/rank 算出経路 — **正経路: `compute_ev_and_rank`（src/ev/ev_rank.py L80・純粋関数）を UI/CLI 側で都度適用**（prediction SELECT → JODDS snapshot 取得 → `(race_key, umaban)` 結合 → compute_ev_and_rank で EV_lower/EV_upper/recommend_rank 算出）。odds は EV 判定にのみ使用し特徴量に混入しない（D-07 odds 非依存確率 p_fukusho_hit + EV 分離・Core Value 直結・§19.1 再現性）。※当初「backtest JOIN を一次経路」と構想したが backtest テーブルに `fuku_odds_lower`/`fuku_odds_upper` が不存在（`BACKTEST_COLUMNS` L77-116）のため構造的に不可能と判明（07-02 revision で否定・正経路は JODDS snapshot + compute_ev_and_rank 再計算）。
3. ディレクトリ判断 — `src/ui/`（実コード慣例・import 単純）を採用。`pyproject.toml` `[tool.hatch.build.targets.wheel] packages` に `"src/ui"` を追加。

**W4 errata（CONTEXT D-04 の「14列」→「16列」）:** CONTEXT.md L15/L39 に「pin 済み14列」と「§16.2 pin 済み20/14列」の記述があるが・これは errata（誤記）。根拠: (1) 要件§16.2 原典（docs/keiba_ai_requirements_v1.3.md 行1118-1134）を行数えると backtest CSV は16列（backtest_id/backtest_strategy_version/train_period/validation_period/odds_snapshot_policy/race_id/horse_id/selected_flag/stake/refund_flag/payout_amount/profit/fukusho_hit_validated/recommend_rank/EV_lower/EV_upper）。(2) `src/db/backtest_load.py::BACKTEST_COLUMNS`（L77-116）は16列相当の DB カラムを定義。(3) CLAUDE.md「本ファイルと要件に乖離がある場合は要件定義書を優先する」→ 要件§16.2 原典（16列）が正。Plan 01 の `BACKTEST_CSV_COLUMNS` は16列（RESEARCH Pitfall 3 で確定済・test_csv_columns.py::test_backtest_csv_columns_count で `assert len(BACKTEST_CSV_COLUMNS) == 16` を機械検証）。CONTEXT D-04 の「14列」表記は Phase 7 全 plan で一貫して errata 扱い（実装には影響しない・`BACKTEST_CSV_COLUMNS` が唯一の正）。

Purpose: Phase 7 全体の前提（列定数・マッピング・依存・テスト土俵）を確立し、Plan 02/03 が専念できるようにする。
Output: `src/ui/{__init__.py,csv_columns.py,jyocd_map.py}`・`.streamlit/config.toml`・`tests/ui/` 4ファイル・`pyproject.toml`/`uv.lock` 更新
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-presentation/07-CONTEXT.md
@.planning/phases/07-presentation/07-RESEARCH.md
@.planning/phases/07-presentation/07-PATTERNS.md
@.planning/phases/07-presentation/07-UI-SPEC.md

# ソースコード契約（PATTERNS analog・実装の正）
@src/ev/report.py
@src/config/code_tables.yaml
@src/db/schema.py
@src/db/backtest_load.py
@pyproject.toml
</context>

<artifacts_this_phase_produces>
本 plan が新規作成するシンボル・ファイル（source-grounding pass の drift 対象除外用）:

**新規ファイル**:
- `src/ui/__init__.py`（空・パッケージ化のみ）
- `src/ui/csv_columns.py` — `PREDICTION_CSV_COLUMNS: tuple[str, ...]`（20列・fukusho_odds_lower/fukusho_odds_upper を含む・REVIEW HIGH-1 canonical 外部名）/ `BACKTEST_CSV_COLUMNS: tuple[str, ...]`（16列）
- `src/ui/jyocd_map.py` — `def load_jyocd_map() -> dict[str, str]`
- `.streamlit/config.toml`（`[theme]` base/primaryColor）
- `tests/ui/__init__.py`（空・パッケージ化のみ）
- `tests/ui/test_csv_columns.py` — テスト関数: `test_prediction_csv_columns_count`/`test_prediction_csv_has_all_stamps`/`test_prediction_csv_columns_match_spec`/`test_backtest_csv_columns_count`/`test_backtest_csv_columns_match_spec`/`test_ui_cli_share_columns`
- `tests/ui/test_readonly_guarantee.py` — `test_ui_has_no_write_ddl_sql`/`test_ui_uses_only_readonly_pool`（SQL リテラルのみ検査・REVIEW LOW-2）
- `tests/ui/test_segment_schema.py` — `test_all_axes_present`/`test_segment_schema_contract`
- `tests/ui/test_streamlit_api_usage.py` — `test_no_legacy_selection_arg`/`test_dataframe_uses_selection_mode`（緩和基準・REVIEW LOW-1）

**変更ファイル**:
- `pyproject.toml`（dependencies に `streamlit==1.58.0`・`[tool.hatch.build.targets.wheel] packages` に `"src/ui"` 追加）
- `uv.lock`（streamlit 追加で更新）

**docstring 方針（REVIEW HIGH-1/MEDIUM-2）**: csv_columns.py docstring に「外部公開 canonical 名 = fukusho_odds_*・内部 JODDS snapshot 名 = fuku_odds_*（loaders 側で normalize）・odds_snapshot_at は JODDS snapshot の happyo_datetime から取得（backtest JOIN でない）」を明記。
</artifacts_this_phase_produces>

<tasks><task type="auto" tdd="true">
  <name>Task 1: 依存追加・パッケージ登録・ディレクトリ骨架（D-04 前提・RESEARCH Open Question #3 解決）</name>
  <files>pyproject.toml, uv.lock, src/ui/__init__.py</files>
  <read_first>
    - pyproject.toml（編集対象・現状 `plotly>=6.8.0` L24・`packages = ["src/config", "src/db"]` L33 を確認）
    - CLAUDE.md（推奨 `streamlit==1.58.0`・§17.1 技術スタック固定）
    - 07-RESEARCH.md（§Standard Stack L84「pyproject.toml に未追加・uv add streamlit==1.58.0」・§Open Questions #3「src/ui/ を推奨・hatch wheel packages に src/ui を追加」）
    - 07-PATTERNS.md（§pyproject.toml L455-471「修正1: dependencies に streamlit 追加・修正2: wheel packages に src/ui 追加」）
  </read_first>
  <behavior>
    - Test 1: `uv run python -c "import streamlit; print(streamlit.__version__)"` が `1.58.0` を出力する
    - Test 2: `uv run python -c "from src.ui.csv_columns import PREDICTION_CSV_COLUMNS"` が ModuleNotFoundError なく成功する（src/ui が import 可能・wheel packages 登録の proxy 検証）
    - Test 3: `grep -c 'src/ui' pyproject.toml` が 1 以上（packages 行に src/ui 追加）
  </behavior>
  <action>
    1. `uv add streamlit==1.58.0` を実行し dependencies に `streamlit==1.58.0` を追加・uv.lock を更新（CLAUDE.md 推奨バージョン固定・§17.1）。`uv add` が自動で pyproject.toml を編集する。
    2. pyproject.toml の `[tool.hatch.build.targets.wheel] packages` を `["src/config", "src/db", "src/ui"]` に更新（src/ui 追加・Open Question #3 解決・PATTERNS L465-469）。`uv add` は packages 行を触らないため手動 Edit が必要。
    3. `src/ui/__init__.py` を空ファイル（パッケージ化のみ・`from __future__ import annotations` すら不要・src/db/__init__.py と同一慣例）で作成。
    4. `uv sync --frozen` は行わない（uv add 直後は lock と pyproject が一致している・RESEARCH §Standard Stack L105-108）。
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v3 && uv run python -c "import streamlit; assert streamlit.__version__ == '1.58.0', streamlit.__version__; print('streamlit OK')" && grep -q '"src/ui"' pyproject.toml && echo "packages OK"</automated>
  </verify>
  <acceptance_criteria>
    - `uv pip show streamlit | head -2` が `Version: 1.58.0` を含む
    - `grep '"src/ui"' pyproject.toml` が packages 行に一致する（1行）
    - `grep 'streamlit==1.58.0' pyproject.toml` が dependencies に一致する（1行）
    - `src/ui/__init__.py` が存在する
    - `uv run python -c "from src.ui.csv_columns import PREDICTION_CSV_COLUMNS"` が成功する（Task 2 完了後・ただし import 可能であることは Task 1 の packages 登録で保証される）
  </acceptance_criteria>
  <done>streamlit==1.58.0 が依存追加済み・pyproject.toml packages に src/ui が追加済み・src/ui/__init__.py が存在し src.ui パッケージが import 可能</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CSV 列定数 DRY・jyocd マッピング helper・Wave 0 テスト集群（D-04・UI-SPEC CSV Export Contract・LOW-05 再利用）</name>
  <files>src/ui/csv_columns.py, src/ui/jyocd_map.py, tests/ui/__init__.py, tests/ui/test_csv_columns.py, tests/ui/test_readonly_guarantee.py, tests/ui/test_segment_schema.py, tests/ui/test_streamlit_api_usage.py</files>
  <read_first>
    - src/ev/report.py（analog・`REPORT_COLUMNS` L41-53 の tuple[str,...] 定数定義パターン）
    - tests/ev/test_run_backtest_e2e.py（analog・L300-324 `test_report_columns_present` の LOW-05 presence assert パターン・`grep 否定でなく in で存在検証`）
    - src/config/code_tables.yaml（入力・L9-21 jyocd 01-10 マッピングの実データ）
    - src/db/schema.py（L59-94 prediction DDL・L144-202 backtest DDL・`odds_snapshot_at` L182・`as_of_datetime` L65 の存在確認）
    - src/db/backtest_load.py（L77-116 BACKTEST_COLUMNS・OUT-02 CSV 16列のソース）
    - 07-RESEARCH.md（§CSV Export Contract L540-566・§Code Examples L498-538 read-only 保証テスト・§Validation Architecture L670-682 Wave 0 テスト群）
    - 07-UI-SPEC.md（§CSV Export Contract L244-258・§Component Inventory L222-238）
    - 07-PATTERNS.md（§src/ui/csv_columns.py L36-82・§src/ui/jyocd_map.py L184-211・§tests/ui/* L488-594）
  </read_first>
  <behavior>
    - Test 1 (test_csv_columns.py): `len(PREDICTION_CSV_COLUMNS) == 20` かつ `len(BACKTEST_CSV_COLUMNS) == 16` かつ再現性スタンプ5項目が PREDICTION_CSV_COLUMNS に含まれる（odds_snapshot_policy/odds_snapshot_at/model_version/feature_snapshot_id・§19.1）
    - Test 2 (test_csv_columns.py): PREDICTION_CSV_COLUMNS が §16.2 原典20項目の過不足なく一致する（race_id/race_date/race_start_datetime/競馬場/レース番号/horse_id/horse_name/枠番/馬番/p_fukusho_hit/**fukusho_odds_lower/fukusho_odds_upper**/EV_lower/EV_upper/recommend_rank/odds_snapshot_policy/odds_snapshot_at/model_version/feature_snapshot_id/prediction_created_at・REVIEW HIGH-1 canonical 外部名 `fukusho_*` を使用・`fuku_*` でない）
    - Test 3 (test_csv_columns.py): BACKTEST_CSV_COLUMNS が §16.2 原典16項目に一致（backtest_id/backtest_strategy_version/train_period/validation_period/odds_snapshot_policy/race_id/horse_id/selected_flag/stake/refund_flag/payout_amount/profit/fukusho_hit_validated/recommend_rank/EV_lower/EV_upper）
    - Test 4 (test_readonly_guarantee.py): src/ui/ 配下の .py ファイルに書き込み/DDL SQL キーワード（insert into/update /delete from/truncate /create table/drop table/alter table・小文字統一比較）が含まれない（Task 2 時点では src/ui 配下に csv_columns.py/jyocd_map.py のみ・両者とも SQL 文字列を含まないため green）
    - Test 5 (test_segment_schema.py): reports/06-segments/{year,month,jyocd,entry_count,ninki,odds_band}.json の6軸ファイルが全て存在し・各 JSON が `axis_name` + `segments[]`（curve=count/frac_pos/mean_pred・scalar=ece_quantile/ece_uniform/max_dev_guarded/mce_guarded/n_samples・segment_value）構造を持つ（Phase 6 D-10/D-11/D-12 生成済みの stamped データを検証）
    - Test 6 (test_streamlit_api_usage.py): src/ui/ 配下で `st.dataframe(` 呼出が `selection=` 引数（古い API）を一切持たないことを検証する（AST ベース・全域走査）。加えて・`selection_mode=` を持つ `st.dataframe` が**少なくとも1箇所**存在することを検証する（REVIEW LOW-1 解決・全 st.dataframe に selection_mode を要求するのでなく・selection_mode が出現するか・selection= が一件もないかを問う緩和基準・詳細/スカラー表の st.dataframe は selection_mode 不要）。Task 2 時点では src/ui に st.dataframe 呼出がないため green。
  </behavior>
  <action>
    1. **src/ui/csv_columns.py を作成**（D-04 LOCKED・§16.2 pin・PATTERNS L61-82 を正に実装）:
       - `from __future__ import annotations` のみの import（定数モジュール・他 import 不要）
       - `PREDICTION_CSV_COLUMNS: tuple[str, ...] = (...)` で20列を定義（順序は §16.2 原典 L1092-1112・race_id/race_date/race_start_datetime/競馬場/レース番号/horse_id/horse_name/枠番/馬番/p_fukusho_hit/fukusho_odds_lower/fukusho_odds_upper/EV_lower/EV_upper/recommend_rank/odds_snapshot_policy/odds_snapshot_at/model_version/feature_snapshot_id/prediction_created_at）。列名に日本語（競馬場/レース番号/枠番/馬番）を含むのは §16.2 原典どおり・OUT-01 pin。
       - `BACKTEST_CSV_COLUMNS: tuple[str, ...] = (...)` で16列を定義（順序は §16.2 原典 L1118-1133・backtest_id/backtest_strategy_version/train_period/validation_period/odds_snapshot_policy/race_id/horse_id/selected_flag/stake/refund_flag/payout_amount/profit/fukusho_hit_validated/recommend_rank/EV_lower/EV_upper）。RESEARCH Pitfall 3 で16列確定・「14列」表記は誤記。
       - モジュール docstring で「§16.2 pin・UI/CLI/test で DRY 共有」を明記。**odds 列名 canonical 方針（REVIEW HIGH-1 解決）**: 外部公開（CSV ヘッダ・UI 表示）は `fukusho_odds_lower`/`fukusho_odds_upper` を canonical とし・loaders 内部の JODDS snapshot 由来 DataFrame 列名（src/ev/odds_snapshot.py が返す `fuku_odds_lower`/`fuku_odds_upper`）は `normalize_prediction_export_columns(df)` helper で外部名に rename してから CSV/UI に渡す（rename map: `{"fuku_odds_lower": "fukusho_odds_lower", "fuku_odds_upper": "fukusho_odds_upper"}`）。docstring に `odds_snapshot_at` の取得元は「JODDS snapshot の happyo_datetime（07-02 loaders で取得）」と記載し・**backtest テーブル JOIN とは書かない**（REVIEW MEDIUM-2 解決・backtest テーブルに odds 値カラムは不存在のため構造的に不可能・07-02 revision で確定済）。
    2. **src/ui/jyocd_map.py を作成**（PATTERNS L184-211 を正に実装）:
       - `load_jyocd_map() -> dict[str, str]` 関数を定義。`src/config/code_tables.yaml` を PyYAML `yaml.safe_load` で読み込み・`data["jyocd"]` を `dict()` で返す。
       - `_CODE_TABLES_PATH = Path("src/config/code_tables.yaml")`（repo root 相対・Streamlit は repo root から `streamlit run` する前提）。
       - UI 側に jyocd dict をハードコードしない（DRY・code_tables.yaml が単一ソース）。
    3. **tests/ui/__init__.py を空ファイル**で作成（パッケージ化）。
    4. **tests/ui/test_csv_columns.py を作成**（LOW-05 presence assert 再利用・PATTERNS L488-524）:
       - `test_prediction_csv_columns_count`: `assert len(PREDICTION_CSV_COLUMNS) == 20`
       - `test_prediction_csv_has_all_stamps`: `for stamp in ("odds_snapshot_policy","odds_snapshot_at","model_version","feature_snapshot_id"): assert stamp in PREDICTION_CSV_COLUMNS`
       - `test_prediction_csv_columns_match_spec`: 期待20項目（§16.2 原典）の tuple を期待値として `assert PREDICTION_CSV_COLUMNS == EXPECTED`（順序含む完全一致）
       - `test_backtest_csv_columns_count`: `assert len(BACKTEST_CSV_COLUMNS) == 16`（Pitfall 3・「14列」でない）
       - `test_backtest_csv_columns_match_spec`: 期待16項目の tuple と完全一致
       - `test_ui_cli_share_columns`: `PREDICTION_CSV_COLUMNS`/`BACKTEST_CSV_COLUMNS` が `src.ui.csv_columns` から import 可能（同一ソース・DRY 検証）
    5. **tests/ui/test_readonly_guarantee.py を作成**（RESEARCH Code Examples L498-538・PATTERNS L528-558）:
       - `test_ui_has_no_write_ddl_sql`: `UI_DIR = Path("src/ui")` 配下の `*.py` を走査し・**SQL 文字列リテラル（AST で `ast.Constant` の value が str かつ親が `cur.execute` Call の第一引数）のみを検査対象とする**（REVIEW LOW-2 解決・comment/docstring に "update" 等の英単語が現れる false positive を回避）。検査キーワード: `insert into`/`update `（`UPDATE <table> SET` 形式）/`delete from`/`truncate `（`TRUNCATE <table>` 形式）/`create table`/`drop table`/`alter table`（小文字統一比較）。SQL リテラル抽出が困難な場合は `# planner-discipline-allow: <kw>` マーカー行で例外的に許容（test_readonly_guarantee.py 自身の検査キーワード文字列はマーカーで許容）。
       - `test_ui_uses_only_readonly_pool`: src/ui 配下の AST を走査し `make_pool` Call ノードの `role` keyword を検証（`role='readonly'` または role 引数なし=既定のみ許可・Task 2 時点では make_pool 呼出しがないため green・Plan 02/03 で実装時に検出）
    6. **tests/ui/test_segment_schema.py を作成**（PATTERNS L564-594・RESEARCH Validation Architecture L681）:
       - `SEGMENTS_DIR = Path("reports/06-segments")`・`AXES = ("year","month","jyocd","entry_count","ninki","odds_band")`（D-12 の6軸）
       - `test_all_axes_present`: `for axis in AXES: assert (SEGMENTS_DIR / f"{axis}.json").exists()`（Phase 6 D-10 生成済み・skiplist が必要な場合は `pytest.skip` でなく素直に fail・stamped 成果物の前提検証）
       - `test_segment_schema_contract`: 各軸 JSON が `axis_name` + `segments[]`（`curve` に count/frac_pos/mean_pred・`scalar` に ece_quantile/ece_uniform/max_dev_guarded/mce_guarded/n_samples・`segment_value`）を持つ（`REQUIRED_CURVE_KEYS <= set(seg["curve"])` で部分集合検証）
    7. **tests/ui/test_streamlit_api_usage.py を作成**（RESEARCH Pitfall 1・PATTERNS L30・spec-driven）:
       - `test_no_legacy_selection_arg`: src/ui/ 配下の .py ファイルを AST 走査し `st.dataframe` 呼出の `selection=` キーワード引数が存在しないことを検証（古い API・Task 2 時点では st.dataframe 呼出がないため green・Plan 03 実装時に検出）
       - `test_dataframe_uses_selection_mode`: `st.dataframe` 呼出が `selection_mode=` を持つことを検証する土俵（Plan 03 実装後に対象 st.dataframe が存在する場合のみ・存在しない場合は skip）
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v3 && KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/ui/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/ui/ -x -q` が exit 0（全 test green）
    - `uv run python -c "from src.ui.csv_columns import PREDICTION_CSV_COLUMNS as P, BACKTEST_CSV_COLUMNS as B; assert len(P)==20 and len(B)==16; print('cols OK')"` が成功
    - `uv run python -c "from src.ui.jyocd_map import load_jyocd_map; m=load_jyocd_map(); assert m['01']=='札幌' and m['10']=='小倉'; print('jyocd OK')"` が成功
    - `grep -c 'PREDICTION_CSV_COLUMNS' src/ui/csv_columns.py` が 1 以上
    - `grep -c 'def load_jyocd_map' src/ui/jyocd_map.py` が 1
    - `grep -v '^#' tests/ui/test_csv_columns.py | grep -c 'len(PREDICTION_CSV_COLUMNS) == 20'` が 1
    - `grep -v '^#' tests/ui/test_readonly_guarantee.py | grep -c 'insert into'` が1以上（検査対象キーワードとしての言及があること・実装コードではない）
    - `ls tests/ui/test_csv_columns.py tests/ui/test_readonly_guarantee.py tests/ui/test_segment_schema.py tests/ui/test_streamlit_api_usage.py` が全ファイル存在
  </acceptance_criteria>
  <done>src/ui/csv_columns.py が20/16列の DRY 単一定数を持つ・src/ui/jyocd_map.py が code_tables.yaml からマッピングを読込・tests/ui/ 4テストファイルが green・read-only 保証と segment スキーマ契約と Streamlit API 正引数がテスト土俵として固定化</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Streamlit テーマ設定・Task 1/2 統合ゲート（UI-SPEC Color/Theme Contract）</name>
  <files>.streamlit/config.toml</files>
  <read_first>
    - 07-UI-SPEC.md（§Color L146-173・`[theme] base="light"` `primaryColor="#FF4B4B"`・その他は Streamlit 既定で上書きしない）
    - 07-PATTERNS.md（§`.streamlit/config.toml` L476-484・spec-driven・既存コード analog なし・UI-SPEC が正）
  </read_first>
  <behavior>
    - Test 1: `.streamlit/config.toml` が存在し `[theme]` セクション下に `base = "light"` と `primaryColor = "#FF4B4B"` を持つ
    - Test 2: `streamlit run` がファイル存在で theme を読込（本 Task では `streamlit run` の実起動は行わない・ファイル存在と内容の構造的検証のみ・実起動は Plan 03 のスコープ）
  </behavior>
  <action>
    1. `.streamlit/config.toml` を作成（UI-SPEC Color Contract・PATTERNS L478-483 を正に実装）:
       - `[theme]` セクション・`base = "light"`・`primaryColor = "#FF4B4B"`
       - `secondaryBackgroundColor`/`backgroundColor`/`font` 等は上書きしない（UI-SPEC「その他は Streamlit 既定」・L153 secondary background は既定のまま）
       - コメントで「UI-SPEC Color Contract・accent reserved for st.download_button/rank S のみ」と明記
    2. 統合ゲート: `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/ui/ -q` を実行し Task 1/2 の成果物が全て green であることを確認（Task 3 自体は .streamlit/config.toml のみ・統合確認が主目的）。
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v3 && test -f .streamlit/config.toml && grep -q 'base = "light"' .streamlit/config.toml && grep -q 'primaryColor = "#FF4B4B"' .streamlit/config.toml && KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/ui/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `.streamlit/config.toml` が存在する
    - `grep 'base = "light"' .streamlit/config.toml` が一致（1行）
    - `grep 'primaryColor = "#FF4B4B"' .streamlit/config.toml` が一致（1行）
    - `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/ui/ -q` が exit 0（Plan 01 全テスト green）
    - `uv run python -c "import streamlit; from src.ui.csv_columns import PREDICTION_CSV_COLUMNS; from src.ui.jyocd_map import load_jyocd_map; load_jyocd_map(); print('integration OK')"` が成功
  </acceptance_criteria>
  <done>.streamlit/config.toml が UI-SPEC Color Contract どおり作成・Plan 01 全成果物の統合ゲート green・Plan 02/03 が専念できる基盤完成</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner→executor（PLAN.md）| 本 PLAN の記述が実装に正しく反映されること・特に CSV 列数 20/16 と再現性スタンプ5項目の欠落がないこと |
| code→test（presence assert）| PREDICTION_CSV_COLUMNS/BACKTEST_CSV_COLUMNS の列定義と §16.2 原典の 1:1 対応が test で機械検証されること |
| src/ui→src/db（将来）| src/ui/ 配下に書き込み/DDL SQL 経路が存在しないこと（read-only 保証・Plan 02/03 で実装時に test_readonly_guarantee.py が検出） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering（データ完全性） | src/ui/csv_columns.py・PREDICTION_CSV_COLUMNS/BACKTEST_CSV_COLUMNS | mitigate | tests/ui/test_csv_columns.py で §16.2 原典20/16項目との完全一致（順序含む）と再現性スタンプ5項目の presence を assert（LOW-05 再利用・grep 否定でない）。列揺れを構造的にブロック。 |
| T-07-02 | Tampering（スコープ逸脱） | BACKTEST_CSV_COLUMNS 列数 | mitigate | tests/ui/test_csv_columns.py::test_backtest_csv_columns_count で `assert len(BACKTEST_CSV_COLUMNS) == 16` を固定（RESEARCH Pitfall 3・「14列」誤記を機械的に排除・要件§16.2 原典優先） |
| T-07-03 | Information Disclosure | pyproject.toml・uv.lock | accept | 依存追加情報（streamlit==1.58.0）は秘密情報でない・公開可能。DSN/パスワードは pyproject に含まれない（src/config/settings.py が pydantic-settings で別管理） |
| T-07-04 | Tampering（スコープ逸脱） | .streamlit/config.toml・サードパーティコンポーネント | mitigate | UI-SPEC Registry Safety に従い Streamlit 公式 + Plotly 公式のみ採用・サードパーティ Streamlit コンポーネントを依存に追加しない（pyproject.toml dependencies が streamlit==1.58.0 のみ追加で証明される） |
| T-07-SC | Tampering | uv add streamlit install | mitigate | streamlit は CLAUDE.md（§17.1）と docs.streamlit.io（公式）で権威付け・[CITED] 扱い・RESEARCH §Package Legitimacy Audit で Approved 判定・blocking human checkpoint 不要（ASVS L1・確立された公式パッケージ） |
</threat_model>

<verification>
Plan 01 の検証（Phase 7 全体の基盤）:

1. **依存とパッケージ**: `uv pip show streamlit | head -2` が `Version: 1.58.0`・`grep '"src/ui"' pyproject.toml` が packages 行に一致
2. **CSV 列定数 DRY**: `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/ui/test_csv_columns.py -x -q` が green・`PREDICTION_CSV_COLUMNS` 20列・`BACKTEST_CSV_COLUMNS` 16列・再現性スタンプ5項目 presence
3. **read-only 保証土俵**: `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/ui/test_readonly_guarantee.py -x -q` が green（Task 2 時点では src/ui 配下が csv_columns.py/jyocd_map.py のみで SQL を含まないため）
4. **segment スキーマ契約**: `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/ui/test_segment_schema.py -x -q` が green（Phase 6 D-10/D-11/D-12 生成済み JSON の6軸構造検証）
5. **Streamlit API 正引数土俵**: `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/ui/test_streamlit_api_usage.py -x -q` が green（Pitfall 1 検出準備完了）
6. **jyocd マッピング**: `uv run python -c "from src.ui.jyocd_map import load_jyocd_map; m=load_jyocd_map(); assert m['01']=='札幌'"` が成功
7. **テーマ設定**: `.streamlit/config.toml` が `base = "light"` + `primaryColor = "#FF4B4B"` を持つ
8. **統合ゲート**: `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/ui/ -q` が exit 0
</verification>

<success_criteria>
- streamlit==1.58.0 が pyproject.toml dependencies と uv.lock に追加済み
- pyproject.toml `[tool.hatch.build.targets.wheel] packages` に "src/ui" が追加済み
- src/ui/csv_columns.py が PREDICTION_CSV_COLUMNS (20列) / BACKTEST_CSV_COLUMNS (16列) を定義
- src/ui/jyocd_map.py が code_tables.yaml から jyocd→競馬場名マッピングを読込
- tests/ui/ の4テストファイルが green（CSV 列定数 presence assert・read-only 保証・segment スキーマ契約・Streamlit API 正引数）
- .streamlit/config.toml が UI-SPEC Color Contract どおり作成
- OUT-01/OUT-02/UI-01（列定数契約部分）が基盤として確立
</success_criteria>

<output>
Create `.planning/phases/07-presentation/07-01-SUMMARY.md` when done
</output>
