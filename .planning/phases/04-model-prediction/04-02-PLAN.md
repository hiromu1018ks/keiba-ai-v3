---
phase: 04-model-prediction
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/model/__init__.py
  - src/model/data.py
  - src/model/calibrator.py
  - src/model/artifact.py
  - tests/model/test_data.py
  - tests/model/test_calibrator.py
autonomous: true
requirements: [MODL-01, MODL-03]
tags: [phase-04, data-layer, calibrator, sc1, sc4]

must_haves:
  truths:
    - "src/model/data.py が snapshots/feature_matrix_20260620-1a-postreview-v2.parquet からのみ feature matrix を読込し live DB の feature 系テーブルを一切参照しない（per SC#1・D-01）"
    - "data.py が raw ID 原列4列（kisyucode/chokyosicode/ketto3infohansyokunum1/2）をモデル入力から除外する（per Pitfall 4 / MODL-01）"
    - "data.py が assert_matrix_columns_registered で feature allowlist を検査し banned_features が空であることを保証する（per D-07 / SC#1）"
    - "data.py が train 2016-07..2023 / calib 2024-H1 / test 2024-H2 で3way分割し 同一 race_id が train/calib/test を跨がない（per D-02b）"
    - "src/model/calibrator.py が fit_prefit_calibrator を薄く wrap し calib sample >=1000 で isotonic・<1000 で sigmoid を切替える（per SC#4・§15.2）"
    - "src/model/artifact.py が LightGBM .txt / CatBoost .cbm / sklearn joblib を models/{model_version}/ 配下に atomic に保存する（per D-06）"
  artifacts:
    - path: "src/model/data.py"
      provides: "load_feature_matrix / join_labels / filter_eligible / drop_raw_ids / prepare_model_matrix / split_3way"
      contains: "SNAPSHOT_PATH"
      min_lines: 60
    - path: "src/model/calibrator.py"
      provides: "calibrate_model (fit_prefit_calibrator wrapper・isotonic/sigmoid 切替)"
      contains: "fit_prefit_calibrator"
    - path: "src/model/artifact.py"
      provides: "save_native_artifact / load_native_artifact / write_metadata_json (atomic)"
      contains: "save_native_artifact"
    - path: "src/model/__init__.py"
      provides: "package marker"
      min_lines: 1
  key_links:
    - from: "src/model/data.py"
      to: "snapshots/feature_matrix_20260620-1a-postreview-v2.parquet"
      via: "pq.read_table(SNAPSHOT_PATH).to_pandas()"
      pattern: "feature_matrix_20260620-1a-postreview-v2"
    - from: "src/model/data.py"
      to: "src/features/availability.py"
      via: "assert_matrix_columns_registered(spec, output_columns) で banned features 検査"
      pattern: "assert_matrix_columns_registered"
    - from: "src/model/calibrator.py"
      to: "src/utils/calibrator.py"
      via: "fit_prefit_calibrator 呼出・再実装しない"
      pattern: "from src.utils.calibrator import fit_prefit_calibrator"
    - from: "src/model/data.py"
      to: "src/utils/group_split.py"
      via: "race_id 単位 disjoint 3way 分割（race_id_time_series_split 補完・暦年 mask）"
      pattern: "race_id"
---

<objective>
src/model/ の service 層基盤を構築: (a) data.py が stamped Parquet から feature matrix を読込し label.fukusho_label と PK join し is_model_eligible フィルタ・raw ID 除外・allowlist 検査・3way race_id disjoint 分割を行う（per SC#1・D-02b・MODL-01）、(b) calibrator.py が既存 fit_prefit_calibrator を薄く wrap し calib sample 件数で isotonic/sigmoid 切替（per SC#4・§15.2・再実装禁止）、(c) artifact.py が LightGBM .txt / CatBoost .cbm / sklearn joblib を models/{model_version}/ に atomic 保存（per D-06）。test_data.py / test_calibrator.py の該当 stub を GREEN 化。

Purpose: 後続 PLAN 03 (trainer/baseline)・PLAN 04 (predict/prediction_load/evaluator) が依存する「feature matrix + label + 3way split + calibrated estimator」の service interface を先に確立。リーク防止プリミティブ（availability/calibrator/category_map）は再実装せず既存契約を消費する。
Output: src/model/{__init__,data,calibrator,artifact}.py + GREEN 化された test_data.py/test_calibrator.py（データ/キャリブレーション契約）
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-model-prediction/04-CONTEXT.md
@.planning/phases/04-model-prediction/04-RESEARCH.md
@.planning/phases/04-model-prediction/04-PATTERNS.md

@src/features/availability.py
@src/features/category_map_consumer.py
@src/utils/calibrator.py
@src/utils/group_split.py
@src/etl/fukusho_label.py
@src/features/snapshot.py
@src/config/feature_availability.yaml
@src/config/label_spec.yaml

@snapshots/feature_matrix_20260620-1a-postreview-v2.manifest.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/model/data.py — stamped Parquet 読込 + label join + allowlist + 3way split（SC#1/MODL-01）</name>
  <files>src/model/__init__.py, src/model/data.py, tests/model/test_data.py</files>
  <behavior>
    - Test test_load_from_parquet_only: load_feature_matrix() が返す DataFrame の shape が (554267, 62+)・pyarrow.parquet 由来・live DB 接続しない（関数シグネチャに DB pool 引数なし）
    - Test test_raw_ids_excluded: prepare_model_matrix() の出力カラムリストに kisyucode/chokyosicode/ketto3infohansyokunum1/ketto3infohansyokunum2 が含まれない
    - Test test_no_banned_features: assert_matrix_columns_registered を呼出し banned_features(spec) が空リストを返す
    - Test test_race_id_disjoint_3way: split_3way() の train/calib/test の race_id 集合が pairwise disjoint・race_date で strict chronological (train_max < calib_min < calib_max < test_min)
  </behavior>
  <read_first>
    - src/features/availability.py:296-340（registered_feature_columns, assert_matrix_columns_registered, _RAW_ID_KEPT_COLUMNS:131-137, banned_features:220）
    - src/features/category_map_consumer.py:275-310（load_category_maps・再 fit 禁止）
    - src/utils/group_split.py:29-128（race_id_time_series_split・race_id disjoint guard・3 ValueError）
    - src/etl/fukusho_label.py:523-527（_RACE_KEY = year,jyocd,kaiji,nichiji,racenum）, 629-645（merge on _RACE_KEY + astype(str) 型整列パターン）, 880-919（_LABEL_TABLE_COLUMNS 参考）
    - src/config/feature_availability.yaml（registry・banned source 確認）
    - src/config/label_spec.yaml（is_model_eligible / fukusho_hit_validated / label_validation_status フィルタ条件）
    - snapshots/feature_matrix_20260620-1a-postreview-v2.manifest.yaml（feature_snapshot_id / train_period / validation_period / feature_count=62 / fa_version=0.3.0）
    - .planning/phases/04-model-prediction/04-RESEARCH.md「入力 snapshot の確定 feature 仕様」セクション（42 実質 feature・5低基数 categorical・5 _code int32・4 raw ID 除外・16 meta/key）
    - .planning/phases/04-model-prediction/04-PATTERNS.md「src/model/data.py」「Pattern 2: 3way race_id-grouped 時系列分割」セクション
    - tests/features/test_allowlist.py（assert_matrix_columns_registered 呼出例）
    - tests/model/test_data.py（Wave 0 で作成した RED stub 4件）
  </read_first>
  <action>
    1. 【src/model/__init__.py】package marker docstring 1行作成（`"""Phase 4 model layer: data/calibrator/trainer/baseline/evaluator/predict/artifact."""`）。
    2. 【モジュール冒頭 docstring + from __future__ import annotations】PATTERNS.md Shared Pattern 1 踏襲。SC#1/§13.4/D-01/D-02b の参照を docstring に明記。
    3. 【定数】SNAPSHOT_PATH = "snapshots/feature_matrix_20260620-1a-postreview-v2.parquet"（D-01 正）。LABEL_PK_COLUMNS = ["year","jyocd","kaiji","nichiji","racenum"]（fukusho_label.py:524 と同一）。RAW_ID_COLUMNS = frozenset({"kisyucode","chokyosicode","ketto3infohansyokunum1","ketto3infohansyokunum2"})（availability._RAW_ID_KEPT_COLUMNS と整合・import して流用可）。META_KEY_COLUMNS（16カラム・manifest 参照）。
    4. 【load_feature_matrix() -> pd.DataFrame】`pyarrow.parquet.read_table(SNAPSHOT_PATH).to_pandas()` で読込・`race_date` を `pd.to_datetime` 化・live DB 引数を持たない（SC#1）。戻り値に race_id/race_nkey があれば保持（split_3way で使用）。feature_snapshot_id 列も保持（provenance 用）。
    5. 【join_labels(feature_df, readonly_cur) -> pd.DataFrame】`label.fukusho_label` から SELECT（fukusho_hit_validated, is_model_eligible, label_validation_status, race_date, _RACE_KEY + umaban + kettonum + 補助列 sales_start_entry_count/fukusho_payout_places を BL-1 用に取得）。feature_df と PK で left merge・merge 前 に _RACE_KEY + umaban + kettonum を `astype(str)` で両側型整列（fukusho_label.py:641-644 パターン）。silent NaN 禁止・label が無い行は ValueError（fail-loud）。
    6. 【filter_eligible(df) -> pd.DataFrame】`is_model_eligible == True` かつ `label_validation_status in {'ok','computed'}`（label_spec.yaml で確定した条件）でフィルタ。欠損行は fail-loud。
    7. 【drop_raw_ids(df) -> pd.DataFrame】RAW_ID_COLUMNS を drop（Pitfall 4）。unit test で除外を assert 可能な形で `_RAW_ID_KEPT_COLUMNS` を import して利用（availability から再利用・重複定義しない）。
    8. 【prepare_model_matrix(df) -> tuple[pd.DataFrame, pd.Series]】上記 join/filter/drop を統合し、(X, y) を返す。y = df["fukusho_hit_validated"].astype(int)。X は feature matrix。feature_availability.yaml を load_feature_availability で読み・assert_matrix_columns_registered(spec, X.columns) を呼出（SC#1）・banned_features(spec) が空でなければ ValueError。
    9. 【split_3way(df) -> dict[str, pd.DataFrame]】D-02b 推奨案: train = df[df.race_date.between("2016-07-01","2023-12-31")] / calib = df[df.race_date.between("2024-01-01","2024-06-30")] / test = df[df.race_date.between("2024-07-01","2024-12-31")]。holdout_2025_plus（df.race_date >= "2025-01-01"）は Phase 5 BT 温存用に返すが学習/評価に使わない（docstring 明記）。3way の race_id 集合が pairwise disjoint であることを ValueError guard で保証（PATTERNS Shared Pattern 2: assert でなく raise ValueError）。race_id 列が無い場合は race_nkey で代用（manifest 確認）。
    10. 【評価窓の戻り値型】dict のキーは "train" / "calib" / "test" / "holdout_2025_plus"。各値は sort_values(["race_start_datetime","race_id"]) 済み（CatBoost has_time=True 前提 sort）。
    11. 【frozen category map】load_category_maps("snapshots/category_map_20260620-1a-postreview-v2.json") を呼べる helper `load_frozen_maps()` を提供（trainer が消費・本 task では map 自体の適用はしない・snapshot 内に既に _code int32 化済み・PATTERNS data.py 注意差分）。
    12. 【Test GREEN 化】tests/model/test_data.py の 4 stub を実装。test_load_from_parquet_only は load_feature_matrix() を呼出し shape と live DB 引数不在を assert。test_raw_ids_excluded は prepare_model_matrix で RAW_ID_COLUMNS が除外を assert。test_no_banned_features は banned_features(load_feature_availability()) == [] を assert。test_race_id_disjoint_3way は split_3way 結果の race_id pairwise disjoint + strict chronological を assert。DB 接続を要する test（join/filter）は合成 DataFrame で unit test 化・requires_db マークは最小限。
  </action>
  <verify>
    <automated>uv run pytest tests/model/test_data.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/model/test_data.py の4 test が全て GREEN
    - src/model/data.py に `pq.read_table` 呼出があり live DB cursor 引数が load_feature_matrix シグネチャに無い（grep で `def load_feature_matrix` が `()` のみ引数）
    - src/model/data.py に `assert_matrix_columns_registered` import がある
    - src/model/data.py に `from src.features.availability import _RAW_ID_KEPT_COLUMNS` または等価の raw ID 除外ロジックがある
    - split_3way に `raise ValueError` による race_id disjoint guard がある（assert でない・`python -O` で生存）
    - train_max_date < calib_min_date < calib_max_date < test_min_date が妥当（2023-12-31 < 2024-01-01 < 2024-06-30 < 2024-07-01）
    - snapshots/feature_matrix_20260620-1a-postreview-v2.parquet の SHA256 が `26c685f0…ecbdd2` と一致（load 時の整合性確認・D-01）
  </acceptance_criteria>
  <done>data.py が stamped Parquet のみ読込・raw ID 除外・allowlist 検査・3way race_id disjoint 分割を行い4 test が GREEN（SC#1/MODL-01/D-02b 達成）</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: src/model/calibrator.py + src/model/artifact.py — prefit wrapper + native artifact save（SC#4/D-06）</name>
  <files>src/model/calibrator.py, src/model/artifact.py, tests/model/test_calibrator.py</files>
  <behavior>
    - Test test_strict_later_disjoint: max(train.race_date) >= min(calib.race_date) の入力で calibrate_model() が fit_prefit_calibrator 経由で ValueError を raise・正常入力（train_max < calib_min）では CalibratedClassifierCV を返す
    - Test test_isotonic_vs_sigmoid_threshold: calib sample >=1000 で method="isotonic"・<1000 で method="sigmoid" が選択される（合成 DataFrame で境界 999/1000 を検証）
    - Test test_reproduce_bit_identical: この PLAN では trainer が未実装のため SKIP マークまたは lightgbm LogisticRegression 小モデルで代用検証（trainer 実装後に PLAN 05 で本格化）・docstring に「trainer 実装後に強化」を明記
  </behavior>
  <read_first>
    - src/utils/calibrator.py:1-130（fit_prefit_calibrator 完全体・FrozenEstimator・CalibratedClassifierCV・ValueError guard:86-94）
    - src/features/snapshot.py:206-269（byte-reproducible Parquet 書込・決定論的パターン参考）
    - src/features/category_map_consumer.py:243-272（persist_category_maps・atomic write: tmp → os.replace）
    - tests/utils/test_calibrator.py（smoke + ValueError サブプロセステスト:1-60）
    - .planning/phases/04-model-prediction/04-RESEARCH.md Pattern 4「FrozenEstimator prefit」+ D-06「SC#4 reproduce smoke」+ D-09 ハイパラ
    - .planning/phases/04-model-prediction/04-PATTERNS.md「src/model/calibrator.py」「src/model/artifact.py」セクション
    - tests/model/test_calibrator.py（Wave 0 RED stub 3件）
  </read_first>
  <action>
    1. 【calibrator.py モジュール docstring】SC#4/§15.2/D-06 を明記。fit_prefit_calibrator を再実装しない旨を明記（薄い wrapper・既存契約消費）。
    2. 【calibrate_model(base_estimator, X_calib, y_calib, race_dates_calib, train_max_date) -> CalibratedClassifierCV】RESEARCH Pattern 4 の実装例を直接採用: `calib_method = "isotonic" if len(X_calib) >= 1000 else "sigmoid"`（CLAUDE.md §15.2）→ `from src.utils.calibrator import fit_prefit_calibrator` → `return fit_prefit_calibrator(base_estimator=base_estimator, X_calib=X_calib, y_calib=y_calib, race_dates_calib=race_dates_calib, train_max_date=train_max_date, method=calib_method)`。戻り値の calibrated estimator と共に `calib_method` を呼出元が取得できるよう NamedTuple 又は dict で返す（provenance 用・predict.py が model_version/calib_method を書込）。シグネチャ: 戻り値 = `CalibrationResult(calibrated, calib_method)`。
    3. 【artifact.py モジュール docstring】D-06/§19.1/PATTHRNS atomic write 参照。
    4. 【save_native_artifact(model, model_type, model_version, out_dir, *, feature_snapshot_id, hyperparams, seed, train_calib_test_periods) -> Path】PATTERNS artifact.py セクション手順に従う: out_dir = Path(f"models/{model_version}") を `mkdir(parents=True, exist_ok=True)`（snapshot.py:274-275 パターン）。model_type で分岐: "lightgbm" → `model.booster_.save_model(out_dir / "lgb_model.txt")`（LGBMClassifier の場合は .booster_）・"catboost" → `model.save_model(out_dir / "cb_model.cbm")`・"logreg"/"calibrator" → `joblib.dump(model, out_dir / "calibrator.joblib")`。metadata.json を `json.dumps({...}, sort_keys=True)`（byte-reproducible・category_map_consumer.py:268 パターン）で atomic write（tmp → os.replace: category_map_consumer.py:263-272）。<!-- planner-discipline-allow: pickle -->Python 標準のオブジェクト直列化モジュール（pickle）は ACE リスクのため使用禁止・Phase 3 CR-04 で廃止した思想を継承しネイティブ形式（.txt/.cbm）と joblib のみ使用。
    5. 【load_native_artifact(model_type, model_version, out_dir) -> Any】model_type で分岐: "lightgbm" → `lgb.Booster(model_file=str(path))` または `lgb.LGBMClassifier` 復元・"catboost" → `CatBoostClassifier().load_model(path)`・"calibrator" → `joblib.load(path)`。戻り値型は Any で呼出元が型確認。
    6. 【write_metadata_json(out_dir, metadata_dict) -> Path】atomic write helper（save_native_artifact 内部で使用・単体 test 可能）。
    7. 【Test GREEN 化】tests/model/test_calibrator.py の3 stub を実装:
       - test_strict_later_disjoint: 合成 DataFrame で train_max_date >= calib_min の入力を calibrate_model に渡し `pytest.raises(ValueError)`・正常入力で CalibrationResult.calibrated が `CalibratedClassifierCV` インスタンス・base_estimator は sklearn LogisticRegression の合成モデル（trainer 未実装のため LogisticRegression で代用・docstring 明記）。
       - test_isotonic_vs_sigmoid_threshold: calib DataFrame を 999行/1000行 で作成（race_id 単位で 999/1000 行になるよう合成）・calibrate_model の戻り CalibrationResult.calib_method が "sigmoid"/"isotonic" になることを assert。
       - test_reproduce_bit_identical: この PLAN では lightgbm 小モデル（n_estimators=10・合成50行）で seed=42 で2回 calibrate_model → predict_proba して np.array_equal を assert。docstring に「PLAN 05 で trainer 本体 + 3way 実データで再強化」と明記。lightgbm が PLAN 01 で pin 済みなので import 可能。
  </action>
  <verify>
    <automated>uv run pytest tests/model/test_calibrator.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/model/test_calibrator.py の3 test が全て GREEN
    - src/model/calibrator.py に `from src.utils.calibrator import fit_prefit_calibrator` があり fit_prefit_calibrator を再実装していない（grep で `def fit_prefit_calibrator` が src/model/calibrator.py に無い）
    - src/model/calibrator.py の calibrate_model に `isotonic if len(X_calib) >= 1000 else "sigmoid"` 切替がある
    - src/model/artifact.py に `os.replace` を使った atomic write がある（grep `os.replace`）
    - src/model/artifact.py に pickle 呼出がない（`grep -c 'pickle' src/model/artifact.py` == 0・joblib は可）
    - src/model/artifact.py が LightGBM (.txt)・CatBoost (.cbm)・joblib の3形式を model_type で分岐保存
    - metadata.json が `sort_keys=True` で書かれる（grep `sort_keys=True`）
  </acceptance_criteria>
  <done>calibrator.py が fit_prefit_calibrator を薄く wrap し isotonic/sigmoid 切替・artifact.py が3形式を atomic 保存・3 test が GREEN（SC#4/D-06 達成）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stamped Parquet → DataFrame | 不変 snapshot の読込境界・live DB 再計算を防ぐ境界（SC#1） |
| label.fukusho_label → DataFrame merge | DB readonly 読込と Parquet の PK join 境界・型不一致で silent 0 行 merge を防ぐ |
| feature_availability.yaml registry → assert | banned feature 混入（odds 系・当日情報）を検出する境界 |
| calibrated estimator → models/{version}/ artifact | ネイティブ形式保存・pickle ACE を防ぐ境界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Information Disclosure | load_feature_matrix が live DB から feature 再計算 | mitigate | 関数シグネチャが DB cursor 引数を持たない・pq.read_table(SNAPSHOT_PATH) のみ・unit test で引数 arity を assert（SC#1） |
| T-04-07 | Tampering | drop_raw_ids 忘れによる raw ID 原列のモデル入力混入 | mitigate | prepare_model_matrix が明示的に drop・unit test で kisyucode/chokyosicode/ketto3infohansyokunum1/2 が X.columns に含まれないことを assert（Pitfall 4） |
| T-04-08 | Tampering | banned feature（odds/ninki）の feature 混入 | mitigate | assert_matrix_columns_registered 呼出 + banned_features(spec) == [] を assert（D-07 保証・BL-3 は独立ベンチマーク） |
| T-04-09 | Tampering | split_3way で同一 race_id が train/calib/test に跨る | mitigate | race_id pairwise disjoint を raise ValueError で guard（PATTERNS Shared Pattern 2・`python -O` で生存）・unit test で検証 |
| T-04-10 | Information Disclosure | calib slice が train と過去/重複 | mitigate | calibrate_model が fit_prefit_calibrator 経由で strict-later ValueError guard を継承・unit test で max(train) >= min(calib) 入力を検証 |
| T-04-11 | Tampering | model artifact の pickle ACE | mitigate | save_native_artifact は LightGBM .txt / CatBoost .cbm / joblib のみ・pickle 不使用（D-06・Phase 3 CR-04 思想） |
| T-04-12 | Repudiation | metadata.json の非決定論的書込 | mitigate | sort_keys=True + atomic write（os.replace）で再現性保証 |
</threat_model>

<verification>
- Wave 2 完了: `uv run pytest tests/model/test_data.py tests/model/test_calibrator.py -x -q` が全 GREEN
- 後続 PLAN 03 (trainer/baseline) は data.py の prepare_model_matrix/split_3way/load_frozen_maps を消費・PLAN 04 (predict) は calibrate_model の戻り値と artifact save を消費
</verification>

<success_criteria>
- data.py が stamped Parquet のみ読込・raw ID 除外・allowlist 検査・3way race_id disjoint 分割を行う（SC#1/MODL-01/D-02b）
- calibrator.py が fit_prefit_calibrator を再実装せず薄く wrap し isotonic/sigmoid 切替（SC#4/§15.2）
- artifact.py がネイティブ形式で atomic 保存（D-06）
- 7 test（test_data.py 4 + test_calibrator.py 3）が GREEN
</success_criteria>

<output>
Create `.planning/phases/04-model-prediction/04-02-SUMMARY.md` when done

## Artifacts this phase produces
- src/model/__init__.py: package marker
- src/model/data.py: `load_feature_matrix()`, `join_labels(feature_df, readonly_cur)`, `filter_eligible(df)`, `drop_raw_ids(df)`, `prepare_model_matrix(df) -> (X, y)`, `split_3way(df) -> {"train","calib","test","holdout_2025_plus"}`, `load_frozen_maps()`・定数 `SNAPSHOT_PATH`, `LABEL_PK_COLUMNS`, `RAW_ID_COLUMNS`, `META_KEY_COLUMNS`
- src/model/calibrator.py: `calibrate_model(...) -> CalibrationResult(calibrated, calib_method)` NamedTuple・`CalibrationResult` 型
- src/model/artifact.py: `save_native_artifact(model, model_type, model_version, out_dir, *, feature_snapshot_id, hyperparams, seed, train_calib_test_periods) -> Path`, `load_native_artifact(model_type, model_version, out_dir) -> Any`, `write_metadata_json(out_dir, metadata_dict) -> Path`
- tests/model/test_data.py: 4 test GREEN
- tests/model/test_calibrator.py: 3 test GREEN
</output>
