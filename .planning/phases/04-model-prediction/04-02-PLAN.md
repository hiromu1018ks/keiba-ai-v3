---
phase: 04-model-prediction
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/model/__init__.py
  - src/model/data.py
  - src/model/calibrator.py
  - src/model/artifact.py
  - tests/model/test_data.py
  - tests/model/test_calibrator.py
autonomous: true
requirements: [MODL-01, MODL-03]
tags: [phase-04, data-layer, calibrator, sc1, sc4]

must_haves:
  truths:
    - "src/model/data.py が snapshots/feature_matrix_20260620-1a-postreview-v2.parquet からのみ feature matrix を読込し live DB の feature 系テーブルを一切参照しない（per SC#1・D-01）"
    - "data.py の API が明示的に分割される: load_feature_matrix()（Parquet・DB引数なし）/ load_labels(readonly_cur)（DB）/ build_training_frame(feature_df, label_df)（純粋 join）/ make_X_y(frame)（feature 選択+X,y 抽出）・prepare_model_matrix(df) はこれらを統合する thin orchestrator に過ぎない（review HIGH#9: 契約混乱の解消・fake-green 防止）"
    - "FEATURE_COLUMNS 定数が registry から導出される明示的 allowlist であり metadata/raw-ID/label 列を除外し・make_X_y が返す X は厳密に FEATURE_COLUMNS のみを含む（review HIGH#9: assert_matrix_columns_registered の誤適用防止）"
    - "data.py が raw ID 原列4列（kisyucode/chokyosicode/ketto3infohansyokunum1/2）をモデル入力から除外する（per Pitfall 4 / MODL-01）"
    - "data.py が assert_matrix_columns_registered で feature allowlist を検査し banned_features が空であることを保証する（per D-07 / SC#1）"
    - "data.py の race 識別キーが正準 race_key = (year,jyocd,kaiji,nichiji,racenum) に統一され・ad-hoc な race_nkey フォールバックを廃止する（review HIGH#9: race_id 安定性）"
    - "data.py が train 2016-07..2023 / calib 2024-H1 / test 2024-H2 で3way分割し 同一 race_id が train/calib/test を跨がない・厳密時系列順 train_max < calib_min < calib_max < test_min <= test_max を raise ValueError で保証する（per D-02b・review MEDIUM#5: test_min<=test_max 含む完全条件）"
    - "data.py が snapshot manifest の完全 SHA256 を読込み hash scope=parquet_data_only_metadata_excluded で検証する（review MEDIUM#6: 略記でなく完全 hash・Phase 3 と同一 scope）"
    - "src/model/calibrator.py が fit_prefit_calibrator を薄く wrap し calib sample >=1000 で isotonic・<1000 で sigmoid を切替える（per SC#4・§15.2）"
    - "src/model/artifact.py が CalibratedClassifierCV を正しく保存する: base estimator をネイティブ形式（lgb_model.txt / cb_model.cbm）・calibrator wrapper を calibrator.joblib・metadata を metadata.json として**別々に**保存し・load_native_artifact が base+calibrator から CalibratedClassifierCV を再構築する（review HIGH#5: native save of CalibratedClassifierCV wrapper の失敗回避）"
    - "load_native_artifact が native base ファイルから base estimator を真正読込し base+calibrators から CalibratedClassifierCV を真正再構築する（Cycle 2 NEW-5: 単なる型 assertion でなく予測の中心が native base 復元にある・joblib の Python マイナーバージョン非互換で calibrator.joblib が読めない場合の fallback 経路を docstring で担保）"
  prohibitions:
    - "prepare_model_matrix が metadata/label 列（race_date/feature_snapshot_id/fukusho_hit_validated/sales_start_entry_count 等）を X に含めることは許容しない（review HIGH#9）"
    - "artifact.py が CalibratedClassifierCV を LightGBM .txt / CatBoost .cbm で直接保存することは許容しない（review HIGH#5: wrapper と base は別形式で保存）"
    - "race_id 列が無い場合に race_nkey で静かに代用することは許容しない（review HIGH#9: 正準 race_key ビルダ必須）"
    - "SHA256 検証が略記（先頭+末尾数文字のみ）で完了扱いすることは許容しない（review MEDIUM#6: manifest の完全 hash 必須）"
  artifacts:
    - path: "src/model/data.py"
      provides: "load_feature_matrix / join_labels / filter_eligible / drop_raw_ids / prepare_model_matrix / split_3way"
      contains: "SNAPSHOT_PATH"
      min_lines: 60
    - path: "src/model/calibrator.py"
      provides: "calibrate_model (fit_prefit_calibrator wrapper・isotonic/sigmoid 切替)"
      contains: "fit_prefit_calibrator"
    - path: "src/model/artifact.py"
      provides: "save_native_artifact / load_native_artifact / write_metadata_json (atomic)"
      contains: "save_native_artifact"
    - path: "src/model/__init__.py"
      provides: "package marker"
      min_lines: 1
  key_links:
    - from: "src/model/data.py"
      to: "snapshots/feature_matrix_20260620-1a-postreview-v2.parquet"
      via: "pq.read_table(SNAPSHOT_PATH).to_pandas()"
      pattern: "feature_matrix_20260620-1a-postreview-v2"
    - from: "src/model/data.py"
      to: "src/features/availability.py"
      via: "assert_matrix_columns_registered(spec, output_columns) で banned features 検査"
      pattern: "assert_matrix_columns_registered"
    - from: "src/model/calibrator.py"
      to: "src/utils/calibrator.py"
      via: "fit_prefit_calibrator 呼出・再実装しない"
      pattern: "from src.utils.calibrator import fit_prefit_calibrator"
    - from: "src/model/data.py"
      to: "src/utils/group_split.py"
      via: "race_id 単位 disjoint 3way 分割（race_id_time_series_split 補完・暦年 mask）"
      pattern: "race_id"
---

<objective>
src/model/ の service 層基盤を構築: (a) data.py が stamped Parquet から feature matrix を読込し label.fukusho_label と PK join し is_model_eligible フィルタ・raw ID 除外・allowlist 検査・3way 正準 race_key disjoint 分割を行う（per SC#1・D-02b・MODL-01）・**review HIGH#9 対応: データ API を load_feature_matrix/load_labels/build_training_frame/make_X_y/prepare_model_matrix に明示分離し FEATURE_COLUMNS を registry から導出した明示的 allowlist で定義・make_X_y が X.columns == FEATURE_COLUMNS を完全一致 assert**（契約混乱と fake-green を防止）・**review MEDIUM#5/MEDIUM#6 対応: 完全時系列条件 train_max<calib_min<calib_max<test_min<=test_max を guard・manifest の完全 SHA256 と hash scope を検証**、(b) calibrator.py が既存 fit_prefit_calibrator を薄く wrap し calib sample 件数で isotonic/sigmoid 切替（per SC#4・§15.2・再実装禁止）、(c) artifact.py が **review HIGH#5 対応: CalibratedClassifierCV を base estimator（lgb_model.txt / cb_model.cbm ネイティブ形式）と calibrator wrapper（calibrator.joblib）に分離保存し load_native_artifact が再構築**（D-06・wrapper を直接ネイティブ形式で保存する失敗を回避）。test_data.py / test_calibrator.py の該当 stub を GREEN 化。

Purpose: 後続 PLAN 03 (trainer/baseline)・PLAN 04 (predict/prediction_load/evaluator) が依存する「feature matrix + label + 3way split + calibrated estimator」の service interface を先に確立。リーク防止プリミティブ（availability/calibrator/category_map）は再実装せず既存契約を消費する。review HIGH#9 の「prepare_model_matrix 契約混乱で label join が未実装のままテストが GREEN になる fake-green」を API 分離と合成 label DataFrame injection で防止する。
Output: src/model/{__init__,data,calibrator,artifact}.py + GREEN 化された test_data.py/test_calibrator.py（データ/キャリブレーション契約）
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-model-prediction/04-CONTEXT.md
@.planning/phases/04-model-prediction/04-RESEARCH.md
@.planning/phases/04-model-prediction/04-PATTERNS.md

@src/features/availability.py
@src/features/category_map_consumer.py
@src/utils/calibrator.py
@src/utils/group_split.py
@src/etl/fukusho_label.py
@src/features/snapshot.py
@src/config/feature_availability.yaml
@src/config/label_spec.yaml

@snapshots/feature_matrix_20260620-1a-postreview-v2.manifest.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/model/data.py — stamped Parquet 読込 + label join + allowlist + 3way split（SC#1/MODL-01）</name>
  <files>src/model/__init__.py, src/model/data.py, tests/model/test_data.py</files>
  <behavior>
    - Test test_load_from_parquet_only: load_feature_matrix() が返す DataFrame の shape が (554267, 62+)・pyarrow.parquet 由来・live DB 接続しない（関数シグネチャ arity 0・引数なし）・verify_snapshot_sha256 が完全 SHA256（64 hex・略記でない・review MEDIUM#6）で PASS
    - Test test_raw_ids_excluded: make_X_y(frame) の出力 X.columns が FEATURE_COLUMNS と**完全一致（集合 + 順序）**し kisyucode/chokyosicode/ketto3infohansyokunum1/ketto3infohansyokunum2 が含まれない（review HIGH#9: 厳密 feature 選択・metadata/label/raw-ID 列の混入防止）
    - Test test_no_banned_features: assert_matrix_columns_registered を呼出し banned_features(spec) が空リストを返す
    - Test test_race_id_disjoint_3way: split_3way() の train/calib/test の **正準 race_key**（race_nkey でなく）集合が pairwise disjoint・完全時系列条件 train_max < calib_min < calib_max < test_min <= test_max（review MEDIUM#5: test_min<=test_max 含む）
  </behavior>
  <read_first>
    - src/features/availability.py:296-340（registered_feature_columns, assert_matrix_columns_registered, _RAW_ID_KEPT_COLUMNS:131-137, banned_features:220）
    - src/features/category_map_consumer.py:275-310（load_category_maps・再 fit 禁止）
    - src/utils/group_split.py:29-128（race_id_time_series_split・race_id disjoint guard・3 ValueError）
    - src/etl/fukusho_label.py:523-527（_RACE_KEY = year,jyocd,kaiji,nichiji,racenum）, 629-645（merge on _RACE_KEY + astype(str) 型整列パターン）, 880-919（_LABEL_TABLE_COLUMNS 参考）
    - src/config/feature_availability.yaml（registry・banned source 確認）
    - src/config/label_spec.yaml（is_model_eligible / fukusho_hit_validated / label_validation_status フィルタ条件）
    - snapshots/feature_matrix_20260620-1a-postreview-v2.manifest.yaml（feature_snapshot_id / train_period / validation_period / feature_count=62 / fa_version=0.3.0）
    - .planning/phases/04-model-prediction/04-RESEARCH.md「入力 snapshot の確定 feature 仕様」セクション（42 実質 feature・5低基数 categorical・5 _code int32・4 raw ID 除外・16 meta/key）
    - .planning/phases/04-model-prediction/04-PATTERNS.md「src/model/data.py」「Pattern 2: 3way race_id-grouped 時系列分割」セクション
    - tests/features/test_allowlist.py（assert_matrix_columns_registered 呼出例）
    - tests/model/test_data.py（Wave 0 で作成した RED stub 4件）
  </read_first>
  <action>
    1. 【src/model/__init__.py】package marker docstring 1行作成（`"""Phase 4 model layer: data/calibrator/trainer/baseline/evaluator/predict/artifact."""`）。
    2. 【モジュール冒頭 docstring + from __future__ import annotations】PATTERNS.md Shared Pattern 1 踏襲。SC#1/§13.4/D-01/D-02b の参照を docstring に明記。
    3. 【定数】SNAPSHOT_PATH = "snapshots/feature_matrix_20260620-1a-postreview-v2.parquet"（D-01 正）。SNAPSHOT_MANIFEST_PATH = "snapshots/feature_matrix_20260620-1a-postreview-v2.manifest.yaml"。EXPECTED_FEATURE_COUNT = 62。LABEL_PK_COLUMNS = ["year","jyocd","kaiji","nichiji","racenum"]（fukusho_label.py:524 と同一）。RAW_ID_COLUMNS = frozenset({"kisyucode","chokyosicode","ketto3infohansyokunum1","ketto3infohansyokunum2"})（availability._RAW_ID_KEPT_COLUMNS と整合・import して流用可）。META_KEY_COLUMNS（16カラム・manifest 参照）。
    4. 【FEATURE_COLUMNS 定数 — review HIGH#9 明示的 allowlist】`feature_availability.yaml` を load_feature_availability で読み・`registered_feature_columns(spec)` を起点にして **FEATURE_COLUMNS = registered から (META_KEY_COLUMNS ∪ RAW_ID_COLUMNS ∪ label 系列集合) を差し引いたリスト** を定数として構築（docstring で「registry derived・metadata/raw-ID/label 除外」を明記・trainer/predict が消費）。FEATURE_COLUMNS を定義する際に LEN(expected feature) == 42（RESEARCH 確定）を検証・不一致は ValueError。
    5. 【make_race_key(df) -> pd.Series — review HIGH#9 正準 race_key】`(df["year"].astype(str)+"-"+df["jyocd"].astype(str)+"-"+df["kaiji"].astype(str)+"-"+df["nichiji"].astype(str)+"-"+df["racenum"].astype(str))` を返す。snapshot 内に race_id 列があればそれも保持するが、disjoint 検査は正準 race_key で実施する（race_id が無い場合の race_nkey 静かフォールバックは廃止・race_nkey が存在すれば docstring に「参考専用・disjoint 検査には使わない」注記）。
    6. 【load_feature_matrix() -> pd.DataFrame】`pyarrow.parquet.read_table(SNAPSHOT_PATH).to_pandas()` で読込・`race_date` を `pd.to_datetime` 化・live DB 引数を持たない（SC#1）。戻り値に make_race_key で導出した `race_key` 列を追加（正準キー・review HIGH#9）・feature_snapshot_id 列も保持（provenance 用）。
    7. 【verify_snapshot_sha256(df) — review MEDIUM#6 完全 hash】SNAPSHOT_MANIFEST_PATH から yaml.safe_load で `sha256` と `byte_reproducible_scope` を読込。scope が `parquet_data_only_metadata_excluded` であることを assert（Phase 3 D-08 と一致）。SNAPSHOT_PATH の実際 bytes を hashlib.sha256 で計算し manifest の**完全 hash 文字列**（64 hex・略記でない）と `secrets.compare_digest` で比較・不一致は ValueError（fail-loud）。load_feature_matrix の末尾で呼出。
    8. 【load_labels(readonly_cur) -> pd.DataFrame — review HIGH#9 DB 分離】`label.fukusho_label` から SELECT（fukusho_hit_validated, is_model_eligible, label_validation_status, race_date, _RACE_KEY + umaban + kettonum + 補助列 sales_start_entry_count/fukusho_payout_places を BL-1 用に取得）。戻り値は label DataFrame のみ（feature との join は build_training_frame で別途）。DB 引数を明示的に取る（load_feature_matrix は DB 引数を持たない・懸念分離）。
    9. 【build_training_frame(feature_df, label_df) -> pd.DataFrame — review HIGH#9 純粋 join】feature_df と label_df を make_race_key + umaban + kettonum で left merge・merge 前に _RACE_KEY + umaban + kettonum を `astype(str)` で両側型整列（fukusho_label.py:641-644 パターン）。silent NaN 禁止・label が無い行は ValueError（fail-loud）。filter_eligible を内部呼出（is_model_eligible == True かつ label_validation_status in {'ok','computed'}）。DB 引数を持たない純粋関数（テスト容易性・fake-green 回避）。
    10. 【make_X_y(frame) -> tuple[pd.DataFrame, pd.Series] — review HIGH#9 厳密 feature 選択】frame から厳密に FEATURE_COLUMNS のみを X として抽出（feature 順序は FEATURE_COLUMNS に固定）。y = frame["fukusho_hit_validated"].astype(int)。load_feature_availability で spec を読み・assert_matrix_columns_registered(spec, X.columns) を呼出（SC#1・FEATURE_COLUMNS が registry 登録済みであることの二重検査）・banned_features(spec) が空でなければ ValueError。X.columns が FEATURE_COLUMNS と**完全一致（集合 + 順序）**であることを assert（metadata/label/raw-ID 列混入防止・review HIGH#9）。
    11. 【prepare_model_matrix(feature_df_or_frame, *, readonly_cur=None) -> tuple[pd.DataFrame, pd.Series] — review HIGH#9 thin orchestrator】(a) feature_df のみ渡された場合は load_labels(readonly_cur) で label を取得して build_training_frame で join・(b) 既に join 済みの frame が渡された場合はそのまま・(c) いずれの場合も make_X_y を呼出して (X, y) を返す。戻り値の X.columns == FEATURE_COLUMNS（厳密）。この関数は thin orchestrator に過ぎず、実ロジックは load_feature_matrix/load_labels/build_training_frame/make_X_y に分離済み（テストは各純粋関数を直接叩く・fake-green 回避）。
    12. 【split_3way(frame) -> dict[str, pd.DataFrame] — review MEDIUM#5 完全条件】D-02b 推奨案: train = frame[frame.race_date.between("2016-07-01","2023-12-31")] / calib = frame[frame.race_date.between("2024-01-01","2024-06-30")] / test = frame[frame.race_date.between("2024-07-01","2024-12-31")]。holdout_2025_plus（frame.race_date >= "2025-01-01"）は Phase 5 BT 温存用に返すが学習/評価に使わない（docstring 明記）。**完全時系列条件** `train_max <= calib_min < calib_max <= test_min <= test_max`（review MEDIUM#5: train_max_date < calib_min_date < calib_max_date < test_min_date に加え test_min <= test_max を含む）を raise ValueError guard で保証。3way の race_key 集合が pairwise disjoint であることを別の raise ValueError guard で保証（PATTERNS Shared Pattern 2: assert でなく raise ValueError・review HIGH#9: race_nkey でなく正準 race_key で検査）。
    13. 【評価窓の戻り値型】dict のキーは "train" / "calib" / "test" / "holdout_2025_plus"。各値は sort_values(["race_start_datetime","race_key"]) 済み（CatBoost has_time=True 前提 sort・trainer.py の _prepare_catboost_pool が再 sort するが入力も整列済みを保証）。
    14. 【frozen category map】load_category_maps("snapshots/category_map_20260620-1a-postreview-v2.json") を呼べる helper `load_frozen_maps()` を提供（trainer が消費・本 task では map 自体の適用はしない・snapshot 内に既に _code int32 化済み・PATTERNS data.py 注意差分）。
    15. 【Test GREEN 化】tests/model/test_data.py の 4 stub を実装。test_load_from_parquet_only は load_feature_matrix() を呼出し shape (554267, 62+) と live DB 引数不在（シグネチャ arity 0）を assert・verify_snapshot_sha256 が完全 hash で PASS することもここで検証。test_raw_ids_excluded は make_X_y で RAW_ID_COLUMNS が除外を assert・X.columns が FEATURE_COLUMNS と完全一致を assert（review HIGH#9）。test_no_banned_features は banned_features(load_feature_availability()) == [] を assert。test_race_id_disjoint_3way は split_3way 結果の race_key pairwise disjoint + 完全時系列条件（train_max < calib_min < calib_max < test_min <= test_max・review MEDIUM#5）を assert。DB 接続を要する test（load_labels/build_training_frame）は合成 label DataFrame を inject できる形で unit test 化（review HIGH#9: DB 依存をテストから排除・fake-green 防止）・requires_db マークは最小限。
  </action>
  <verify>
    <automated>uv run pytest tests/model/test_data.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/model/test_data.py の4 test が全て GREEN
    - src/model/data.py に `pq.read_table` 呼出があり live DB cursor 引数が load_feature_matrix シグネチャに無い（grep で `def load_feature_matrix` が `()` のみ引数）
    - src/model/data.py に FEATURE_COLUMNS 定数があり `len(FEATURE_COLUMNS)` が RESEARCH 確定の実質 feature 数（42）と一致する（review HIGH#9）
    - src/model/data.py に load_feature_matrix / load_labels / build_training_frame / make_X_y / prepare_model_matrix / verify_snapshot_sha256 / make_race_key の7関数が分離定義されている（review HIGH#9: 契約混乱解消）
    - make_X_y が返す X.columns が FEATURE_COLUMNS と完全一致（`list(X.columns) == FEATURE_COLUMNS`）を unit test で assert（review HIGH#9）
    - src/model/data.py に `assert_matrix_columns_registered` import がある
    - src/model/data.py に `from src.features.availability import _RAW_ID_KEPT_COLUMNS` または等価の raw ID 除外ロジックがある
    - split_3way に `raise ValueError` による race_key disjoint guard と完全時系列条件 guard がある（assert でない・`python -O` で生存・review MEDIUM#5: test_min<=test_max 含む）
    - 完全時系列条件 train_max_date < calib_min_date < calib_max_date < test_min_date <= test_max_date が妥当（2023-12-31 < 2024-01-01 < 2024-06-30 < 2024-07-01 <= 2024-12-31）
    - verify_snapshot_sha256 が manifest の完全 SHA256（64 hex・略記でない）と hashlib.sha256(file bytes) を secrets.compare_digest で比較する（review MEDIUM#6）
    - `byte_reproducible_scope` フィールドが `parquet_data_only_metadata_excluded`（Phase 3 D-08 と一致）であることを assert する検査がある
  </acceptance_criteria>
  <done>data.py が stamped Parquet のみ読込・raw ID 除外・allowlist 検査・3way race_id disjoint 分割を行い4 test が GREEN（SC#1/MODL-01/D-02b 達成）</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: src/model/calibrator.py + src/model/artifact.py — prefit wrapper + native artifact save（SC#4/D-06）</name>
  <files>src/model/calibrator.py, src/model/artifact.py, tests/model/test_calibrator.py</files>
  <behavior>
    - Test test_strict_later_disjoint: max(train.race_date) >= min(calib.race_date) の入力で calibrate_model() が fit_prefit_calibrator 経由で ValueError を raise・正常入力（train_max < calib_min）では CalibrationResult(calibrated=CalibratedClassifierCV, calib_method=...) を返す
    - Test test_isotonic_vs_sigmoid_threshold: calib sample >=1000 で method="isotonic"・<1000 で method="sigmoid" が選択される（合成 DataFrame で境界 999/1000 を検証）
    - Test test_reproduce_bit_identical: この PLAN では trainer が未実装のため SKIP マークまたは lightgbm LogisticRegression 小モデルで代用検証（trainer 実装後に PLAN 05 で本格化）・docstring に「trainer 実装後に強化」を明記
    - Test（追加）test_artifact_save_load_roundtrip: 合成 CalibratedClassifierCV（sklearn LogisticRegression base・isotonic）を save_native_artifact で保存・lgb_model.txt 等でなく calibrator.joblib が作成されること・load_native_artifact で再構築した estimator が元と predict_proba 一致（np.allclose）すること（review HIGH#5: base+calibrator 分離保存の検証）・**Cycle 2 NEW-5**: native base ファイル（lgb_model.txt/cb_model.cbm）が真正再構築の入力になっていること（calibrator.joblib を一時退避しても native base から再構築可能・又は docstring に fallback 手順明記）を assert し純粋 joblib 依存でないことを実証
  </behavior>
  <read_first>
    - src/utils/calibrator.py:1-130（fit_prefit_calibrator 完全体・FrozenEstimator・CalibratedClassifierCV・ValueError guard:86-94）
    - src/features/snapshot.py:206-269（byte-reproducible Parquet 書込・決定論的パターン参考）
    - src/features/category_map_consumer.py:243-272（persist_category_maps・atomic write: tmp → os.replace）
    - tests/utils/test_calibrator.py（smoke + ValueError サブプロセステスト:1-60）
    - .planning/phases/04-model-prediction/04-RESEARCH.md Pattern 4「FrozenEstimator prefit」+ D-06「SC#4 reproduce smoke」+ D-09 ハイパラ
    - .planning/phases/04-model-prediction/04-PATTERNS.md「src/model/calibrator.py」「src/model/artifact.py」セクション
    - tests/model/test_calibrator.py（Wave 0 RED stub 3件）
  </read_first>
  <action>
    1. 【calibrator.py モジュール docstring】SC#4/§15.2/D-06 を明記。fit_prefit_calibrator を再実装しない旨を明記（薄い wrapper・既存契約消費）。
    2. 【calibrate_model(base_estimator, X_calib, y_calib, race_dates_calib, train_max_date) -> CalibratedClassifierCV】RESEARCH Pattern 4 の実装例を直接採用: `calib_method = "isotonic" if len(X_calib) >= 1000 else "sigmoid"`（CLAUDE.md §15.2）→ `from src.utils.calibrator import fit_prefit_calibrator` → `return fit_prefit_calibrator(base_estimator=base_estimator, X_calib=X_calib, y_calib=y_calib, race_dates_calib=race_dates_calib, train_max_date=train_max_date, method=calib_method)`。戻り値の calibrated estimator と共に `calib_method` を呼出元が取得できるよう NamedTuple 又は dict で返す（provenance 用・predict.py が model_version/calib_method を書込）。シグネチャ: 戻り値 = `CalibrationResult(calibrated, calib_method)`。
    3. 【artifact.py モジュール docstring】D-06/§19.1/PATTHRNS atomic write 参照。
    4. 【save_native_artifact(calibrated_estimator, base_model_type, model_version, *, feature_snapshot_id, hyperparams, seed, train_calib_test_periods, out_dir=None) -> Path — review HIGH#5: CalibratedClassifierCV を base+calibrator に分離】PATTERNS artifact.py セクション + review HIGH#5 対応。out_dir = Path(f"models/{model_version}")（out_dir None の場合）を `mkdir(parents=True, exist_ok=True)`（snapshot.py:274-275 パターン）。calibrated_estimator は sklearn CalibratedClassifierCV であり・そのまま LightGBM .txt / CatBoost .cbm で保存すると失敗または誤オブジェクト保存になる（review HIGH#5）。次のように**別々に保存**:
       - **base estimator 抽出**: `base = calibrated_estimator.estimator`（sklearn 1.9 では FrozenEstimator で包まれた LGBMClassifier または CatBoostClassifier・`.estimator` 属性で取り出し可能）
       - base_model_type で分岐: "lightgbm" → `base.booster_.save_model(out_dir / "lgb_model.txt")`（LGBMClassifier の場合は .booster_）・"catboost" → `base.save_model(out_dir / "cb_model.cbm")`
       - **calibrator wrapper 保存**: `joblib.dump(calibrated_estimator, out_dir / "calibrator.joblib")`（CalibratedClassifierCV 全体・base への参照含む・再構築に必要）
       - metadata.json: `json.dumps({"model_version":..., "base_model_type":..., "feature_snapshot_id":..., "calib_method":..., "seed":..., "hyperparams":..., "train_calib_test_periods":..., "saved_components":["base_native","calibrator_joblib"]}, sort_keys=True)`（byte-reproducible・category_map_consumer.py:268 パターン）で atomic write（tmp → os.replace: category_map_consumer.py:263-272）。<!-- planner-discipline-allow: pickle -->Python 標準のオブジェクト直列化モジュール（pickle）は ACE リスクのため使用禁止・Phase 3 CR-04 で廃止した思想を継承しネイティブ形式（.txt/.cbm）と joblib のみ使用。
       - 戻り値 = out_dir（Path）
    5. 【load_native_artifact(base_model_type, model_version, out_dir=None) -> CalibratedClassifierCV — review HIGH#5: base+calibrator から真正再構築・Cycle 2 NEW-5: native base 依存強化で joblib 依存軽減】out_dir = Path(f"models/{model_version}")（None の場合）。真正再構築パイプライン（3ステップ）:
       - **(a) native base ファイルから base estimator を読込む**: base_model_type=="lightgbm" → `base = lgb.Booster(model_file=str(out_dir / "lgb_model.txt"))`（又は LGBMClassifier 復元可능なパス・本プロジェクトでは予測に必要なのは predict_proba のみなので Booster で十分な場合は sklearn ラッパを再構築）・base_model_type=="catboost" → `base = CatBoostClassifier(); base.load_model(str(out_dir / "cb_model.cbm"))`。native ファイルが存在しない場合は FileNotFoundError。
       - **(b) calibrator.joblib から calibrators を読込む**: `cal_data = joblib.load(out_dir / "calibrator.joblib")`。cal_data は CalibratedClassifierCV 又は calibrators リスト＋ calib_method 等の metadata dict のいずれか（save_native_artifact Step 4 の保存形式に依存・joblib 依存部分は calibrators の純粋数値オブジェクトのみに最小化）。
       - **(c) base + calibrators から CalibratedClassifierCV を真正再構築（Cycle 2 NEW-5）**: sklearn.calibration が内部に持つ `CalibratedClassifierCV(estimator=base, method=calib_method)` を構築後・保存済み calibrators を `_calibrators` 属性に注入する再構築ヘルパを用意・又は CalibratedClassifierCV の公開 API 範囲で `.calibrated_classifiers_` に base+各 calibrator を再 bind。**native base（lgb_model.txt/cb_model.cbm）が真正に再構築の入力になること**が本ステップの核心・単なる型 assertion 用確証オブジェクトでない・joblib が Python マイナーバージョン非互換で読めない場合でも native base + calibrator 数値から予測可能経路を担保。
       - base_model_type と再構築後 CalibratedClassifierCV.estimator の型が整合することを assert・不一致は RuntimeError。戻り値は predict_proba 可能な CalibratedClassifierCV。
       - **docstring 明記**: "Cycle 2 NEW-5: native base ファイルから真正再構築。joblib は calibrators（純粋数値）の運び屋としてのみ使用し・予測の中心は native base から復元した estimator。joblib の Python マイナーバージョン非互換で calibrator.joblib が読めない場合の fallback docstring も併記（手動で isotonic/sigmoid 関数を再適用する手順）"。
    6. 【write_metadata_json(out_dir, metadata_dict) -> Path】atomic write helper（save_native_artifact 内部で使用・単体 test 可能）。
    7. 【Test GREEN 化】tests/model/test_calibrator.py の3 stub を実装:
       - test_strict_later_disjoint: 合成 DataFrame で train_max_date >= calib_min の入力を calibrate_model に渡し `pytest.raises(ValueError)`・正常入力で CalibrationResult.calibrated が `CalibratedClassifierCV` インスタンス・base_estimator は sklearn LogisticRegression の合成モデル（trainer 未実装のため LogisticRegression で代用・docstring 明記）。
       - test_isotonic_vs_sigmoid_threshold: calib DataFrame を 999行/1000行 で作成（race_id 単位で 999/1000 行になるよう合成）・calibrate_model の戻り CalibrationResult.calib_method が "sigmoid"/"isotonic" になることを assert。
       - test_reproduce_bit_identical: この PLAN では lightgbm 小モデル（n_estimators=10・合成50行）で seed=42 で2回 calibrate_model → predict_proba して np.array_equal を assert。docstring に「PLAN 05 で trainer 本体 + 3way 実データで再強化」と明記。lightgbm が PLAN 01 で pin 済みなので import 可能。
  </action>
  <verify>
    <automated>uv run pytest tests/model/test_calibrator.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/model/test_calibrator.py の3 test と test_artifact_save_load_roundtrip（追加）が GREEN
    - src/model/calibrator.py に `from src.utils.calibrator import fit_prefit_calibrator` があり fit_prefit_calibrator を再実装していない（grep で `def fit_prefit_calibrator` が src/model/calibrator.py に無い）
    - src/model/calibrator.py の calibrate_model に `isotonic if len(X_calib) >= 1000 else "sigmoid"` 切替がある
    - src/model/artifact.py に `os.replace` を使った atomic write がある（grep `os.replace`）
    - src/model/artifact.py に pickle 呼出がない（`grep -c 'pickle' src/model/artifact.py` == 0・joblib は可）
    - save_native_artifact が CalibratedClassifierCV を受け取り `calibrated_estimator.estimator`（base）をネイティブ形式で保存しつつ `calibrator.joblib` に wrapper 全体を保存する（review HIGH#5: base+calibrator 分離・metadata.json の saved_components=["base_native","calibrator_joblib"]）
    - load_native_artifact が calibrator.joblib を読込み CalibratedClassifierCV を再構築し・base ネイティブファイルとの型整合を assert する（review HIGH#5）
    - **load_native_artifact が native base ファイル（lgb_model.txt/cb_model.cbm）から base estimator を真正読込し base+calibrators から CalibratedClassifierCV を真正再構築する**（Cycle 2 NEW-5）: 3ステップ（native base 読込 → calibrator.joblib 読込 → 再構築）で・joblib 依存を calibrators 純粋数値に最小化・docstring に joblib 非依存 fallback 手順明記・test_artifact_save_load_roundtrip が純粋 joblib 依存でないことを実証
    - metadata.json が `sort_keys=True` で書かれる（grep `sort_keys=True`）
  </acceptance_criteria>
  <done>calibrator.py が fit_prefit_calibrator を薄く wrap し isotonic/sigmoid 切替・artifact.py が3形式を atomic 保存・3 test が GREEN（SC#4/D-06 達成）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stamped Parquet → DataFrame | 不変 snapshot の読込境界・live DB 再計算を防ぐ境界（SC#1） |
| label.fukusho_label → DataFrame merge | DB readonly 読込と Parquet の PK join 境界・型不一致で silent 0 行 merge を防ぐ |
| feature_availability.yaml registry → assert | banned feature 混入（odds 系・当日情報）を検出する境界 |
| calibrated estimator → models/{version}/ artifact | ネイティブ形式保存・pickle ACE を防ぐ境界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Information Disclosure | load_feature_matrix が live DB から feature 再計算 | mitigate | 関数シグネチャが DB cursor 引数を持たない・pq.read_table(SNAPSHOT_PATH) のみ・unit test で引数 arity を assert（SC#1） |
| T-04-07 | Tampering | drop_raw_ids 忘れによる raw ID 原列のモデル入力混入 | mitigate | prepare_model_matrix が明示的に drop・unit test で kisyucode/chokyosicode/ketto3infohansyokunum1/2 が X.columns に含まれないことを assert（Pitfall 4） |
| T-04-08 | Tampering | banned feature（odds/ninki）の feature 混入 | mitigate | assert_matrix_columns_registered 呼出 + banned_features(spec) == [] を assert（D-07 保証・BL-3 は独立ベンチマーク） |
| T-04-09 | Tampering | split_3way で同一 race_id が train/calib/test に跨る | mitigate | 正準 race_key（race_nkey でなく）pairwise disjoint を raise ValueError で guard（PATTERNS Shared Pattern 2・`python -O` で生存・review HIGH#9）・完全時系列条件 train_max<calib_min<calib_max<test_min<=test_max を同様に guard（review MEDIUM#5） |
| T-04-10 | Information Disclosure | calib slice が train と過去/重複 | mitigate | calibrate_model が fit_prefit_calibrator 経由で strict-later ValueError guard を継承・unit test で max(train) >= min(calib) 入力を検証 |
| T-04-11 | Tampering | model artifact の pickle ACE・及び load_native_artifact が native base を真正再構築に使わず純粋 joblib 依存で Python マイナーバージョン非互換時に予測不能（Cycle 2 NEW-5） | mitigate | save_native_artifact は base estimator をネイティブ形式（lgb_model.txt / cb_model.cbm）・calibrator を calibrator.joblib・metadata を metadata.json として別々保存（review HIGH#5: CalibratedClassifierCV を直接 .txt/.cbm で保存しない）・pickle 不使用（D-06・Phase 3 CR-04 思想）・**load_native_artifact が native base から base estimator を真正読込し base+calibrators から CalibratedClassifierCV を再構築（Cycle 2 NEW-5）・joblib は calibrators 純粋数値の運び屋・fallback docstring 明記** |
| T-04-12 | Repudiation | metadata.json の非決定論的書込 | mitigate | sort_keys=True + atomic write（os.replace）で再現性保証 |
| T-04-12b | Information Disclosure | prepare_model_matrix 契約混乱で label join が未実装のままテスト GREEN（fake-green） | mitigate | データ API を load_feature_matrix/load_labels/build_training_frame/make_X_y/prepare_model_matrix に明示分離（review HIGH#9）・DB 依存をテストから排除し合成 label DataFrame を inject できる形で unit test 化 |
| T-04-12c | Tampering | metadata/label/raw-ID 列が X に混入し assert_matrix_columns_registered が誤適用 | mitigate | FEATURE_COLUMNS を registry から明示的に導出（metadata/raw-ID/label 除外）し make_X_y が X.columns == FEATURE_COLUMNS を完全一致 assert（review HIGH#9） |
</threat_model>

<verification>
- Wave 2 完了: `uv run pytest tests/model/test_data.py tests/model/test_calibrator.py -x -q` が全 GREEN
- 後続 PLAN 03 (trainer/baseline) は data.py の prepare_model_matrix/split_3way/load_frozen_maps を消費・PLAN 04 (predict) は calibrate_model の戻り値と artifact save を消費
</verification>

<success_criteria>
- data.py が stamped Parquet のみ読込・raw ID 除外・allowlist 検査・3way 正準 race_key disjoint 分割を行う（SC#1/MODL-01/D-02b）
- data.py の API が load_feature_matrix/load_labels/build_training_frame/make_X_y/prepare_model_matrix に明示分離され FEATURE_COLUMNS allowlist で厳密 feature 選択（review HIGH#9）
- data.py が manifest の完全 SHA256 と hash scope を検証（review MEDIUM#6）
- 完全時系列条件 train_max<calib_min<calib_max<test_min<=test_max を raise ValueError で保証（review MEDIUM#5）
- calibrator.py が fit_prefit_calibrator を再実装せず薄く wrap し isotonic/sigmoid 切替（SC#4/§15.2）
- artifact.py が CalibratedClassifierCV を base ネイティブ + calibrator.joblib に分離保存し再構築可能（review HIGH#5・D-06）
- 8 test（test_data.py 4 + test_calibrator.py 3 + test_artifact_save_load_roundtrip 1）が GREEN
</success_criteria>

<output>
Create `.planning/phases/04-model-prediction/04-02-SUMMARY.md` when done

## Artifacts this phase produces
- src/model/__init__.py: package marker
- src/model/data.py: `load_feature_matrix() -> pd.DataFrame`, `verify_snapshot_sha256(df)`, `load_labels(readonly_cur) -> pd.DataFrame`, `build_training_frame(feature_df, label_df) -> pd.DataFrame`, `make_X_y(frame) -> (X, y)`, `prepare_model_matrix(feature_df_or_frame, *, readonly_cur=None) -> (X, y)`（thin orchestrator）, `make_race_key(df) -> pd.Series`, `split_3way(frame) -> {"train","calib","test","holdout_2025_plus"}`, `load_frozen_maps()`・定数 `SNAPSHOT_PATH`, `SNAPSHOT_MANIFEST_PATH`, `EXPECTED_FEATURE_COUNT`, `FEATURE_COLUMNS`（registry derived allowlist・metadata/raw-ID/label 除外）, `LABEL_PK_COLUMNS`, `RAW_ID_COLUMNS`, `META_KEY_COLUMNS`
- src/model/calibrator.py: `calibrate_model(...) -> CalibrationResult(calibrated, calib_method)` NamedTuple・`CalibrationResult` 型
- src/model/artifact.py: `save_native_artifact(calibrated_estimator, base_model_type, model_version, *, feature_snapshot_id, hyperparams, seed, train_calib_test_periods, out_dir=None) -> Path`（review HIGH#5: base を lgb_model.txt/cb_model.cbm・calibrator を calibrator.joblib に分離保存）, `load_native_artifact(base_model_type, model_version, out_dir=None) -> CalibratedClassifierCV`（Cycle 2 NEW-5: native base から base estimator を真正読込し base+calibrators から真正再構築・joblib 依存を calibrators 純粋数値に最小化・fallback docstring 明記）, `write_metadata_json(out_dir, metadata_dict) -> Path`
- tests/model/test_data.py: 4 test GREEN
- tests/model/test_calibrator.py: 3 test GREEN + test_artifact_save_load_roundtrip GREEN（review HIGH#5）
</output>
