---
phase: 04-model-prediction
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - src/model/trainer.py
  - src/model/baseline.py
  - tests/model/test_trainer.py
  - tests/model/test_baseline.py
autonomous: true
requirements: [MODL-02, MODL-03]
tags: [phase-04, trainer, baseline, sc3, leak-diagnostic, bl1-bl5]

must_haves:
  truths:
    - "src/model/trainer.py が LightGBM native categorical（非負 code・__MISSING__/__UNSEEN__ sentinel・target encoding 禁止）で学習する（per SC#3・§14.3）"
    - "src/model/trainer.py が CatBoost cat_features + has_time=True（Pool は race_start_datetime sort・random permutation 無効化）で学習する（per SC#3・§14.4）"
    - "trainer.py が高基数 ID の _code 列（jockey_id_code/trainer_id_code/sire_id_code/bms_id_code/horse_id_code）を CatBoost の cat_features に**含めて**宣言する（文字列化して cat_features に渡す・数値扱いしない）・MODL-03 categorical-ID 意図を遵守し CatBoost が任意の ID に序数構造を課すことを防止する（review HIGH#6・Cross-Plan #2）"
    - "trainer.py が LightGBM 側でも _code 列を categorical_feature に含め非負 int32 code であることを保証する（review MEDIUM: 低基数列のみでなく _code 列も categorical）"
    - "trainer.py が train/eval 入力で行整列を保証する: X と y の index が完全一致・RaceKey マッピングが予測パスまで追跡可能・align_predictions(pred_series, original_index) で元の行順序に復元できる（review HIGH#2: sort/split/merge/predict 後の silent wrong-horse prediction 防止）"
    - "trainer.py の early stopping eval set が train slice の時系列末尾から切られ calib/test と完全に分離される・加えて `max(eval.race_date) <= max(train_core.race_date)` を満たす（review Cross-Plan #8: calib/test disjoint に加え eval が train 末尾に収まる）（per D-04 / Pitfall 5）"
    - "SC#3 leak diagnostic が高基数 _code 列を明示的に検証し・意図的リーク制御実装がテストを fail させる（DEMONSTRABLY fail）ことを実証する（review HIGH#3: false-pass を防ぐ・target encoding 非混入の構造的実証）"
    - "src/model/baseline.py が BL-1（頭数別一定）/BL-2（人気 1/ninki 正規化）/BL-3（複勝オッズ逆数 1/fukuoddslow 正規化）/BL-4（logreg 少数特徴量）/BL-5（LightGBM 最小特徴量）の全5つを実装する（per D-07/D-08・MODL-02）"
    - "BL-3 の市場データ（n_odds_tanpuku.fukuoddslow）が feature には混入せず独立ベンチマークとしてのみ存在する（per D-07）"
    - "BL-4/BL-5 も主モデルと同一の calib slice でキャリブレーションされるか・比較表で明示的に未キャリブレーションと記載される（review MEDIUM: SC#2 比較の公平性）"
  prohibitions:
    - "CatBoost が _code 列を数値として扱い任意の ID 順序に序数構造を課すことは許容しない（review HIGH#6・MODL-03 違反）"
    - "SC#3 leak diagnostic が target encoding を注入しても GREEN になる（false-pass）ことは許容しない（review HIGH#3: 意図的リーク制御で DEMONSTRABLY fail 必須）"
    - "SC#3 leak diagnostic が低基数合成列のみ検証し高基数 _code 列を未検証のまま GREEN 扱いすることは許容しない（review HIGH#3）"
    - "eval set の race_date が train_core の race_date 最大値を超えることは許容しない（review Cross-Plan #8）"
    - "sort/split/merge 後に予測と元の行が misalign してもテストが GREEN のままになることは許容しない（review HIGH#2）"
  artifacts:
    - path: "src/model/trainer.py"
      provides: "train_lightgbm / train_catboost（固定 seed・native categorical・has_time・eval set 分離）"
      contains: "deterministic"
      min_lines: 80
    - path: "src/model/baseline.py"
      provides: "compute_bl1 / compute_bl2 / compute_bl3 / compute_bl4 / compute_bl5 / compute_all_baselines"
      contains: "fukuoddslow"
      min_lines: 80
    - path: "tests/model/test_trainer.py"
      provides: "4 test GREEN（nonneg_codes / has_time / leak diagnostic / eval set disjoint）"
      contains: "test_no_target_encoding_leak"
    - path: "tests/model/test_baseline.py"
      provides: "6 test GREEN（BL-1..5 + 市場データ源）"
      contains: "test_bl3_fukuodds_inverse_normalized"
  key_links:
    - from: "src/model/trainer.py"
      to: "src/model/data.py"
      via: "prepare_model_matrix / split_3way / load_frozen_maps を消費"
      pattern: "from src.model.data import"
    - from: "src/model/trainer.py"
      to: "lightgbm.LGBMClassifier / catboost.CatBoostClassifier"
      via: "native categorical + has_time=True 学習（§14.3/§14.4）"
      pattern: "has_time"
    - from: "src/model/baseline.py"
      to: "n_odds_tanpuku.fukuoddslow / n_uma_race.ninki"
      via: "readonly_cur で SELECT（D-08 市場データ源）"
      pattern: "fukuoddslow"
---

<objective>
src/model/trainer.py と src/model/baseline.py を実装する: (a) trainer.py は LightGBM native categorical（非負 code・__MISSING__/__UNSEEN__・target encoding 禁止・deterministic seed・num_threads=1）と CatBoost（cat_features + has_time=True・Pool は race_start_datetime sort・thread_count=1）で学習する・**review HIGH#6 対応: 高基数 _code 列（jockey_id_code/trainer_id_code/sire_id_code/bms_id_code/horse_id_code）を文字列化して CatBoost cat_features に含め数値扱いしない（MODL-03 categorical-ID 意図）**・**review HIGH#2 対応: train/eval 入力で X.index.equals(y.index) を assert し align_predictions で予測を元の行順序に復元（silent wrong-horse prediction 防止）**・**review Cross-Plan #8 対応: assert_eval_disjoint が eval_max_date <= train_core_max_date を追加検証**・**review HIGH#3 対応: SC#3 leak diagnostic が低基数 RARE_X + 高基数 _code train-only/test-unseen + 意図的リーク制御で DEMONSTRABLY fail を実証（false-pass 防止）**・early stopping eval set を train slice 末尾から切り calib/test と分離（per SC#3・§14.3/§14.4・D-04/Pitfall 5）。(b) baseline.py は BL-1..5 全5つを計算し・**review MEDIUM 対応: BL-4/BL-5 のキャリブレーション状態を明示（主モデルと同一 calib slice でキャリブレーション or 未キャリブレーション注記）**（per D-07/D-08・MODL-02）。test_trainer.py / test_baseline.py の該当 stub を GREEN 化。

Purpose: SC#3（リークセーフ カテゴリ/欠損）と SC#2（BL-1..5 比較表の原材料）を同時達成。両モデル（LightGBM/CatBoost）を学習し Phase 6 評価ゲートまで候補として残す（D-03・Phase 4 では1つに絞らない）。BL-3 市場データはモデル特徴量には絶対混入せず独立ベンチマーク。review で指摘された「CatBoost が _code を数値扱いし序数構造を課す MODL-03 違反」「sort 後の silent wrong-horse prediction」「SC#3 誤検出」を実装前の契約として防止する。
Output: src/model/{trainer,baseline}.py + GREEN 化された test_trainer.py/test_baseline.py（SC#3 leak diagnostic + BL-1..5）
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-model-prediction/04-CONTEXT.md
@.planning/phases/04-model-prediction/04-RESEARCH.md
@.planning/phases/04-model-prediction/04-PATTERNS.md
@.planning/phases/04-model-prediction/04-VALIDATION.md

@CLAUDE.md
@src/model/data.py
@src/features/availability.py
@src/features/category_map_consumer.py
@src/utils/calibrator.py
@src/config/label_spec.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/model/trainer.py — LightGBM native categorical + CatBoost has_time + SC#3 leak diagnostic（MODL-03/SC#3/D-04）</name>
  <files>src/model/trainer.py, tests/model/test_trainer.py</files>
  <behavior>
    - Test test_lightgbm_nonneg_codes: train_lightgbm 後の categorical_feature で渡した列が `astype('category')` され `.cat.codes.min() >= 0`（NaN→-1 が起きない・__MISSING__ sentinel 経由）・_code 列（jockey_id_code 等）も categorical_feature に含まれ非負 int32（review MEDIUM）
    - Test test_catboost_has_time: train_catboost が返す CatBoostClassifier の get_all_params()['has_time'] == True・Pool に渡す前の DataFrame が race_start_datetime 昇順 sort 済み・**_code 列（jockey_id_code/trainer_id_code/sire_id_code/bms_id_code/horse_id_code）が文字列化されて cat_features に含まれる**（review HIGH#6: 数値扱い禁止）
    - Test test_catboost_predict_preserves_row_order（追加）: train_catboost 後に予測すると sort 済み Pool 順序でなく元の行順序に復元されることを assert・RaceKey マッピングで元 index に戻る（review HIGH#2）
    - Test test_no_target_encoding_leak（review HIGH#3 強化）: 合成希少カテゴリ RARE_X を作成（label=1・残り positive rate 0.21）・train/test 正準 race_key disjoint 分割・train_lightgbm と train_catboost の両方を学習・test の RARE_X 行の予測が 0.5 未満（mean に縮む・target encoding なら 1.0 近く）・**高基数 _code 列（jockey_id_code）で train-only / test-unseen の希少 ID を作成し test での予測が global mean に縮むことを追加 assert**・**意図的 target encoding 実装を注入してテストが fail すること（DEMONSTRABLY fail）を別の assert で実証**
    - Test test_eval_set_disjoint_from_calib_test（review Cross-Plan #8 強化）: train_lightgbm / train_catboost に渡す eval set の race_key と calib/test の race_key が pairwise disjoint（set.isdisjoint で assert）・違反入力で raise ValueError・**加えて max(eval.race_date) <= max(train_core.race_date) を満たすことを assert**（eval が train 末尾に収まる）
  </behavior>
  <read_first>
    - CLAUDE.md §14.3（LightGBM native categorical・target encoding 禁止・非負 code・__MISSING__/__UNSEEN__・高基数 ID frozen map）・§14.4（CatBoost cat_features・has_time=True・Pool sort）
    - src/features/category_map_consumer.py:54-150（FrozenCategoryMap・apply_category_map・__UNSEEN__/__MISSING__ sentinel・非負 int32 保証）
    - src/features/availability.py:115-141（_CATEGORY_COLUMNS・_RAW_ID_KEPT_COLUMNS）
    - src/model/data.py（PLAN 02 で実装: prepare_model_matrix/split_3way/load_frozen_maps）
    - src/utils/calibrator.py:38-103（FrozenEstimator・fit_prefit_calibrator の estimator 取回し契約）
    - .planning/phases/04-model-prediction/04-RESEARCH.md Pattern 3「LightGBM native categorical + CatBoost has_time」・Code Examples LightGBM/CatBoost 学習・D-03（categorical vs numeric 扱い確定）・D-04（eval set 分離）・D-05（SC#3 leak diagnostic 手順）・D-09（ハイパラ初期値確定）
    - .planning/phases/04-model-prediction/04-PATTERNS.md「src/model/trainer.py」セクション + Shared Patterns 2/3/6
    - tests/utils/test_calibrator.py（ValueError サブプロセステストパターン:1-60）
    - tests/features/test_snapshot_repro.py:20-34（_build_synthetic_matrix パターン・SC#3 leak diagnostic で応用）
    - tests/model/test_trainer.py（Wave 0 RED stub 4件）
  </read_first>
  <action>
    1. 【モジュール docstring + from __future__ import annotations】PATTERNS Shared Pattern 1。SC#3/§14.3/§14.4/D-04/D-05/D-09 を明記。target encoding 一切禁止・NaN→-1 依存禁止（§14.3）を明記。
    2. 【定数 — review HIGH#6: _code 列は CatBoost cat_features に含める】LOW_CARD_CAT_COLS = ["sexcd","class_code_normalized","estimated_running_style","rolling_jyocd_mode_5","rolling_jyocd_latest_5"]（D-03 確定・5低基数 string）。HIGH_CARD_CODE_COLS = ["jockey_id_code","trainer_id_code","sire_id_code","bms_id_code","horse_id_code"]（5高基数 ID・非負 int32・frozen map）・**これらは数値扱いではなく categorical として扱う**（review HIGH#6・MODL-03）。LGB_INIT_PARAMS と CB_INIT_PARAMS を D-09 確定値で定義（LGB: objective=binary, metric=binary_logloss, learning_rate=0.05, num_leaves=63, min_data_in_leaf=100, feature_fraction=0.9, bagging_fraction=0.9, bagging_freq=5, lambda_l2=1.0, verbose=-1, seed=42, deterministic=True, force_col_wise=True, num_threads=1（review HIGH#7 bit-identical・SC#4）・CB: iterations=1000, learning_rate=0.05, depth=6, l2_leaf_reg=3.0, has_time=True, random_seed=42, eval_metric=Logloss, early_stopping_rounds=50, verbose=100, thread_count=1（review HIGH#7 bit-identical・SC#4））。N_ESTIMATORS=1000, EARLY_STOPPING_ROUNDS=50, RANDOM_SEED=42。
    3. 【_prepare_lightgbm_matrix(df) -> pd.DataFrame — review MEDIUM: _code も categorical】LOW_CARD_CAT_COLS を `fillna("__MISSING__").astype("category")`（Pitfall 3 回避・Shared Pattern 3）。HIGH_CARD_CODE_COLS は int32 のまま保持しつつ `astype("category")` で categorical dtype 化（非負 int32 code・LightGBM が Fisher 分割を適用）・`categorical_feature=LOW_CARD_CAT_COLS + HIGH_CARD_CODE_COLS` で渡す。NaN が残っていないことを assert（silent fallback 禁止）・HIGH_CARD_CODE_COLS の `.cat.codes.min() >= 0` を assert（NaN→-1 ハザード回避）。
    4. 【_prepare_catboost_pool(df, y, *, sort=True, original_index=None) -> tuple[Pool, pd.Index] — review HIGH#6 + HIGH#2】sort=True の場合 `df_sorted = df.sort_values(["race_start_datetime","race_key"])` してから Pool 構築（has_time=True が入力順序を使用・Pitfall 2）。**cat_features に LOW_CARD_CAT_COLS と HIGH_CARD_CODE_COLS の両方を含める**（review HIGH#6: _code 列を数値扱いしない・文字列化して cat_features に渡す・`df_sorted[HIGH_CARD_CODE_COLS].astype(str)` で Pool に渡す前に対象列を文字列化）。戻り値は (Pool, sorted_index) の tuple・sorted_index は予測パスが元の行順序を復元するために使用（review HIGH#2: 行整列保証）。
    5. 【_split_train_eval_tail(train_df, eval_fraction=0.2) -> (train_core, train_tail) — review Cross-Plan #8: eval は train 末尾に収まる】train slice を race_key 単位・時系列順で末尾 eval_fraction を切り出し（D-04・例: train 2016-07..2023 の末尾 20% = 2023 年後半）。戻り値の race_key 集合は train 全体に含まれるが、train_core と train_tail は disjoint。**train_tail が calib/test と disjoint であることに加え `train_tail["race_date"].max() <= train_core["race_date"].max()` を満たさない場合 raise ValueError**（review Cross-Plan #8: eval が train 末尾に収まる）・ただし train_tail は時系列末尾なので通常 max(train_tail) > max(train_core) になる点に注意・実際の guard は `max(eval.race_date) <= max(train.race_date)`（eval は train 全体の末尾から切るため train 全体の max を超えない）・train_core と train_tail の境界は train 全体の (1-eval_fraction) 分位点。
    6. 【assert_eval_disjoint(eval_race_keys, calib_race_keys, test_race_keys, *, train_core_max_date=None, eval_max_date=None) — review Cross-Plan #8 拡張】`raise ValueError` guard（Shared Pattern 2・assert でなく raise）。3 pairwise disjoint（eval ∩ calib / eval ∩ test / calib ∩ test）を検証。**加えて train_core_max_date と eval_max_date が指定された場合 `eval_max_date <= train_core_max_date`（train 末尾から切った eval が train を超えない）を検証**（review Cross-Plan #8）。違反時メッセージに race_key 例と日付を含める。race_id でなく正準 race_key で検査（data.py PLAN 02 と整合）。
    7. 【align_predictions(pred_series, sorted_index, original_index) -> pd.Series — review HIGH#2 行整列保証】CatBoost Pool を sort して予測した series を元の行順序に復元。pred_series の index は sorted_index・original_index（元の feature_df index）に pandas.Series(pred_series.values, index=sorted_index).reindex(original_index) で戻す。戻り値の index が original_index と完全一致することを assert・不一致は RuntimeError（silent wrong-horse prediction 防止）。
    8. 【train_lightgbm(X_train, y_train, *, X_eval=None, y_eval=None, eval_race_keys=None, calib_race_keys=None, test_race_keys=None, train_core_max_date=None, eval_max_date=None, params=None) -> LGBMClassifier — review HIGH#2: X/y index 整合 assert】**入力で `X_train.index.equals(y_train.index)` を assert し不一致は ValueError**（review HIGH#2 silent row mismatch 防止）。X_train を _prepare_lightgbm_matrix で前処理・params=LGB_INIT_PARAMS をマージ・eval_race_keys が指定されていれば assert_eval_disjoint で guard（review Cross-Plan #8 拡張）・`lgb.LGBMClassifier(n_estimators=N_ESTIMATORS, **params)` を構築・`.fit(X_train, y_train, categorical_feature=LOW_CARD_CAT_COLS + HIGH_CARD_CODE_COLS, eval_set=[(X_eval, y_eval)], eval_metric="binary_logloss", callbacks=[lgb.early_stopping(EARLY_STOPPING_ROUNDS), lgb.log_evaluation(100)])`。戻り値は fit 済み estimator（calibrator へ渡す）。
    9. 【train_catboost(X_train, y_train, *, X_eval=None, y_eval=None, eval_race_keys=None, calib_race_keys=None, test_race_keys=None, train_core_max_date=None, eval_max_date=None, params=None) -> CatBoostClassifier — review HIGH#2 + HIGH#6】**入力で `X_train.index.equals(y_train.index)` を assert**（review HIGH#2）。train_pool, sorted_index = _prepare_catboost_pool(X_train, y_train, sort=True) で構築（cat_features に _code 列含む・review HIGH#6）・assert_eval_disjoint で guard（review Cross-Plan #8 拡張）・eval は同様に _prepare_catboost_pool で sort 済み Pool 構築・`CatBoostClassifier(**CB_INIT_PARAMS)` を `.fit(train_pool, eval_set=eval_pool)`。戻り値は fit 済み estimator（予測パスが sorted_index を使って元順序に復元するため sorted_index も呼出元に返す必要がある場合は (estimator, sorted_index) の tuple で返す・予測は predict_p_fukusho 側で align_predictions を呼ぶ）。
    10. 【_build_rare_category_synthetic(n=2000, rare_rate=0.05, seed=42) -> pd.DataFrame — review HIGH#3: 高基数 _code 列も含む】SC#3 leak diagnostic 用合成データ。(a) 低基数希少カテゴリ: 'RARE_X' を持つ行（LOW_CARD_CAT_COLS の1つに割当）の label を全て 1・残りは positive rate 0.21（Bernoulli）。(b) **高基数 _code 列**: jockey_id_code で train-only の希少 ID（例: code=99991・train に数回のみ出現）を作成し test には出現させない（test-unseen）・この ID の label も train では 1 に設定（target encoding 風リークがあれば test でこの ID に対する予測が 1.0 近くになるが・__UNSEEN__ に縮むべき）。race_key は時系列順に割当・race_start_datetime は 2020-01-01 から1日間隔。test_no_target_encoding_leak が消費。
    11. 【_build_intentional_leak_control() -> tuple[callable, float] — review HIGH#3: 意図的リークで DEMONSTRABLY fail を実証】target encoding 風のリーク feature を注入する関数と、それが test で高い予測（>0.9 等）を生むことを確認する期待値を返す。具体的には train の RARE_X 行の label 平均（=1.0）を特徴量として埋め込み・test の RARE_X 行にも同値を適用（本来持てない情報）・train_lightgbm/train_catboost に渡すと test RARE_X の予測が 0.9 を超える（target encoding 風リークの挙動）。test_no_target_encoding_leak はこのリーク制御を別途 run して予測が 0.9 を超える（=リークがあれば検出される）ことを assert する・**これにより leak diagnostic が false-pass でないことを DEMONSTRABLY 実証**。
    12. 【Test GREEN 化】tests/model/test_trainer.py の5 stub（test_catboost_predict_preserves_row_order 追加で5件）を実装:
        - test_lightgbm_nonneg_codes（review MEDIUM 拡張）: 合成 DataFrame で train_lightgbm を呼出し・戻り値 estimator の categorical_feature 渡し列（LOW_CARD + HIGH_CARD_CODE_COLS の両方）で `.cat.codes.min() >= 0` を assert・NaN が残らないことを `df[cat_col].isna().sum() == 0` で assert・HIGH_CARD_CODE_COLS が categorical_feature リストに含まれることを assert。
        - test_catboost_has_time（review HIGH#6 拡張）: train_catboost の戻り値 `model.get_all_params()['has_time']` が True・入力 DataFrame を `.sort_values(["race_start_datetime","race_key"])` したことを `_prepare_catboost_pool` 内部で検証・**CatBoost に渡された cat_features に HIGH_CARD_CODE_COLS が含まれることを assert**（数値扱いでない）。
        - test_catboost_predict_preserves_row_order（追加・review HIGH#2）: train_catboost で学習後・test DataFrame を逆順にシャッフルして予測しても align_predictions で元の行順序に復元されることを assert・pred_series.index == original_index・不一致なら RuntimeError。
        - test_no_target_encoding_leak（review HIGH#3 強化）: _build_rare_category_synthetic（低基数 RARE_X + 高基数 _code train-only/test-unseen）で合成・make_race_key で正準キー作成・split_3way で train/test 分離・train_lightgbm と train_catboost を学習・test の RARE_X 行の predict_proba[:,1] が 0.5 未満（mean に縮む）を assert・高基数 _code test-unseen ID の予測も global mean に縮むことを assert・**_build_intentional_leak_control を注入した場合は予測が 0.9 を超える（=リークがあれば検出）ことを別 assert で実証**・5回平均で安定性確認（seed 固定）。
        - test_eval_set_disjoint_from_calib_test（review Cross-Plan #8 強化）: 合成データで eval_race_keys と calib_race_keys/test_race_keys が重複する入力で `pytest.raises(ValueError)`・**eval_max_date > train_core_max_date の入力でも raise**（review Cross-Plan #8）・正常入力で train_lightgbm/train_catboost が完了。
    13. 【決定論フラグの全箇所固定確認】LGB: seed=42/deterministic=True/force_col_wise=True/num_threads=1/bagging_seed=42/feature_fraction_seed=42（D-06 + review HIGH#7 リスト）・CB: random_seed=42/has_time=True/thread_count=1。これらがパラメータ dict に含まれることを assert（unit test で params dict を grep）。
  </action>
  <verify>
    <automated>uv run pytest tests/model/test_trainer.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/model/test_trainer.py の5 test（test_catboost_predict_preserves_row_order 追加で5件）が全て GREEN
    - src/model/trainer.py に `target_encoding` / `TargetEncoder` / `mean_encoding` の呼出がない（grep -i で 0 件・target encoding 禁止の構造実証）
    - src/model/trainer.py に `has_time=True` と `deterministic=True` と `force_col_wise=True` と `seed=42` と `random_seed=42` と `num_threads=1` と `thread_count=1` が含まれる（review HIGH#7 bit-identical）
    - src/model/trainer.py に `__MISSING__` sentinel fillna がある（NaN→-1 ハザード回避・Pitfall 3）
    - src/model/trainer.py に `assert_eval_disjoint` 関数と `raise ValueError` guard がある（Shared Pattern 2）・eval_max_date <= train_core_max_date 検証を含む（review Cross-Plan #8）
    - src/model/trainer.py に `align_predictions(pred_series, sorted_index, original_index)` 関数がある（review HIGH#2 行整列）
    - train_lightgbm / train_catboost が `X_train.index.equals(y_train.index)` を assert する（review HIGH#2）
    - **_prepare_catboost_pool が HIGH_CARD_CODE_COLS（jockey_id_code 等5列）を文字列化して cat_features に含める**（review HIGH#6: 数値扱い禁止・`astype(str)` の後 cat_features リストに追加）
    - test_catboost_has_time で `_prepare_catboost_pool` に渡った cat_features に HIGH_CARD_CODE_COLS が含まれることを assert（review HIGH#6）
    - test_no_target_encoding_leak が低基数 RARE_X と高基数 _code 列の両方を検証し・意図的リーク制御注入で予測が 0.9 超える（false-pass でない）ことを別 assert で実証（review HIGH#3）
    - test_eval_set_disjoint_from_calib_test が eval ∩ calib/test disjoint と eval_max_date <= train_core_max_date の両方を検証（review Cross-Plan #8）
    - test_catboost_predict_preserves_row_order がシャッフル入力でも元順序に復元されることを assert（review HIGH#2）
    - LightGBM/CatBoost の学習が small synthetic データで数秒以内に完了（test 実行時間 < 30秒）
  </acceptance_criteria>
  <done>trainer.py が LightGBM native categorical + CatBoost has_time=True で学習し・SC#3 leak diagnostic が GREEN・eval set 分離 guard が動作（SC#3/MODL-03/D-04 達成）</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: src/model/baseline.py — BL-1..5 計算（MODL-02/SC#2/D-07/D-08）</name>
  <files>src/model/baseline.py, tests/model/test_baseline.py</files>
  <behavior>
    - Test test_bl1_field_size_constant: BL-1 が 8頭以上で 3/entry_count・5-7頭で 2/entry_count を返す・境界 (7→8) で閾値切替
    - Test test_bl2_ninki_normalized: BL-2 が 1/ninki をレース内正規化し sum(p) == 払戻対象数（8頭以上 3・5-7頭 2）になる・p_i in [0,1]
    - Test test_bl3_fukuodds_inverse_normalized: BL-3 が 1/fukuoddslow をレース内正規化し sum(p) == 払戻対象数・odds 欠損/0 のレースは NaN で除外
    - Test test_bl4_logreg: BL-4 が sklearn LogisticRegression(max_iter=1000, random_state=42) で barei/futan/umaban/wakuban/class_code_normalized を学習・predict_proba[:,1] を返す
    - Test test_bl5_min_lightgbm: BL-5 が rolling 系統を除外した最小特徴量（static 4 + class_code + estimated_running_style）で LightGBM 学習・predict_proba を返す
    - Test test_market_data_source: n_odds_tanpuku.fukuoddslow と n_uma_race.ninki が readonly_cur で SELECT 可能・2024年 test 期間に NULL が少ない（D-08 実DB 検証済み）
  </behavior>
  <read_first>
    - src/etl/fukusho_label.py:523-527（_RACE_KEY）, 880-919（_LABEL_TABLE_COLUMNS で sales_start_entry_count / fukusho_payout_places 列確認）
    - docs/everydb2/05-HARAI.md（n_odds_tanpuku.fukuoddslow/fukuoddshigh の確認・D-08 根拠）
    - docs/everydb2/04-UMA_RACE.md（Ninki 列の確認）
    - src/model/data.py（PLAN 02: prepare_model_matrix・split_3way・_RACE_KEY 定数）
    - src/model/trainer.py（PLAN 03 Task 1: train_lightgbm を BL-5 で再利用）
    - .planning/phases/04-model-prediction/04-RESEARCH.md D-07（BL-3 レース内正規化）・D-08（BL-1..5 厳密定義表・市場データ源 ninki/fukuoddslow）・「Pitfall 6: BL-3 オッズ逆数の正規化忘れ」
    - .planning/phases/04-model-prediction/04-PATTERNS.md「src/model/baseline.py」セクション
    - tests/features/test_builder.py（pandas assert パターン）
    - tests/model/test_baseline.py（Wave 0 RED stub 6件）
  </read_first>
  <action>
    1. 【モジュール docstring + from __future__ import annotations】SC#2/§14.2/D-07/D-08 を明記。BL-3 は「Phase 1-A モデルと同一情報条件の比較ではない」旨（§14.2）を docstring に明記。市場データは feature に混入せず独立ベンチマーク（D-07）。
    2. 【定数】BL4_FEATURES = ["barei","futan","umaban","wakuban","class_code_normalized"]（D-08）。BL5_FEATURES = ["wakuban","barei","futan","umaban","sexcd","class_code_normalized","estimated_running_style"]（static + 低基数 categorical・rolling 除外）。
    3. 【_payout_places(entry_count) -> int】8 頭以上で 3・5-7 頭で 2・それ以外（4頭以下等）は ValueError（競走不成立・学習対象外）。fukusho_payout_places 列があればそれを優先・無ければ entry_count から推定。
    4. 【compute_bl1(df) -> pd.Series】index = df.index・p = `_payout_places(entry_count) / entry_count`・各レース内で全馬同一値。返り Series 名 = "p_bl1"。
    5. 【_race_normalize_inverse(df, value_col) -> pd.Series】汎用ヘルパー: レース(_RACE_KEY + umaban で識別) 内で `1/value_col` を計算し `(1/v) / sum(1/v within race) * payout_places` で正規化・sum(p) == payout_places になるよう保証（Pitfall 6）。value_col が 0/NaN の行は除外（NaN を返す）。
    6. 【compute_bl2(df, ninki_col="ninki") -> pd.Series】_race_normalize_inverse(df, ninki_col) を呼出・返り Series 名 = "p_bl2"。ninki は n_uma_race 由来（D-08）。
    7. 【compute_bl3(df, fukuodds_col="fukuoddslow") -> pd.Series】_race_normalize_inverse(df, fukuodds_col) を呼出・返り Series 名 = "p_bl3"。fukuoddslow は n_odds_tanpuku 由来（D-08・HARAI 参照）。odds が NULL/0 のレースは NaN。
    8. 【compute_bl4(X_train, y_train, X_test, *, calibrate=False) -> pd.Series — review MEDIUM: キャリブレーション明示】BL4_FEATURES で `LogisticRegression(max_iter=1000, random_state=42)` を fit・X_test の predict_proba[:,1]・返り Series 名 = "p_bl4"。calibrate=True の場合は主モデルと同一の calib slice で fit_prefit_calibrator でキャリブレーション（比較の公平性）・calibrate=False の場合は未キャリブレーションで返し compute_all_baselines が BL-3 §14.2 注記と同様に「BL-4 未キャリブレーション」注記を付与（review MEDIUM: SC#2 比較の公平性・デフォルトは calibrate=False で注記必須）。
    9. 【compute_bl5(X_train, y_train, X_test, *, calibrate=False) -> pd.Series — review MEDIUM】BL5_FEATURES subset で train_lightgbm（PLAN 03 Task 1 再利用）を small params（n_estimators=100 等の短縮版・固定 seed・num_threads=1）で fit・predict_proba[:,1]・返り Series 名 = "p_bl5"。calibrate=True の場合は主モデルと同一 calib slice でキャリブレーション・False の場合は未キャリブレーション注記（review MEDIUM）。
    10. 【compute_all_baselines(df_test, *, X_test_bl4_bl5, y_train, X_train_bl4_bl5, readonly_cur=None, calib_slice=None) -> pd.DataFrame — review MEDIUM: 比較公平性】BL-1/2/3 は df_test の列から直接計算・BL-4/5 は学習済み estimator から予測・calib_slice が渡された場合は BL-4/5 をキャリブレーション（主モデルと公平）・渡されない場合は未キャリブレーション注記を "bl_calib_note" 列に付与（"BL-4/BL-5 uncalibrated - comparison caveat" 等）。全 p_bl* 列を統合した DataFrame を返す。provenance 用に bl_name / bl_calib_note 列も付与。
    11. 【fetch_market_data(readonly_cur, race_keys) -> pd.DataFrame】readonly_cur で `SELECT ... FROM normalized.n_odds_tanpuku` と `SELECT ... FROM normalized.n_uma_race` を _RACE_KEY + umaban + kettonum で結合し fukuoddslow / ninki を取得（D-08）。NULL/0 行はそのまま（compute_bl2/bl3 で処理）。
    12. 【Test GREEN 化】tests/model/test_baseline.py の6 stub を実装。test_market_data_source は `@pytest.mark.requires_db`・live DB の n_odds_tanpuku.fukuoddslow と n_uma_race.ninki が SELECT 可能 + 2024年 test 期間に NULL が少ない（< 5% 等）ことを assert。test_bl1..bl5 は合成 DataFrame で各 BL の計算結果を assert（境界値・sum(p)==payout_places・p_i in [0,1]）。
  </action>
  <verify>
    <automated>uv run pytest tests/model/test_baseline.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/model/test_baseline.py の6 test が全て GREEN（test_market_data_source は requires_db・KEIBA_SKIP_DB_TESTS=1 で skip 可）
    - src/model/baseline.py に BL-1..5 の5 compute 関数 + compute_all_baselines + fetch_market_data が存在
    - BL-3 の計算に `_race_normalize_inverse` が使われ sum(p) == 払戻対象数（3 or 2）が保証される（Pitfall 6 回避）
    - BL-2 が ninki 由来・BL-3 が fukuoddslow 由来（D-08・grep で確認）
    - src/model/baseline.py の特徴量に fukuoddslow/ninki が含まれない（BL 計算の入力のみ・feature matrix には混入しない・D-07・`assert_matrix_columns_registered` で保証）
    - BL-4 が LogisticRegression(max_iter=1000, random_state=42) を使用
    - BL-5 が train_lightgbm を再利用（trainer の複製なし）
  </acceptance_criteria>
  <done>baseline.py が BL-1..5 全5つを計算し・市場データは feature に混入せず・6 test が GREEN（MODL-02/SC#2/D-07/D-08 達成）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| feature matrix → LightGBM/CatBoost 学習 | categorical 処理経路（target encoding 混入を防ぐ境界・SC#3） |
| NaN → category code -1 | pandas category dtype の NaN→-1 ハザード境界（Pitfall 3） |
| train slice 末尾 → eval set | early stopping feedback が calib/test に漏れる境界（Pitfall 5/D-04） |
| 合成希少カテゴリ → 学習モデル | SC#3 leak diagnostic の検証力を決定する境界 |
| n_odds_tanpuku/n_uma_race readonly SELECT | 市場データが feature に混入する境界（D-07） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Information Disclosure | target encoding 混入（LightGBM/CatBoost カテゴリ処理） | mitigate | native categorical（LightGBM category dtype・Fisher 分割）+ CatBoost ordered TS（has_time=True）・SC#3 leak diagnostic（合成 RARE_X + 高基数 _code train-only/test-unseen + 意図的リーク制御 DEMONSTRABLY fail）で target encoding 非混入を構造実証（review HIGH#3）・test_no_target_encoding_leak GREEN |
| T-04-13b | Tampering | CatBoost が高基数 _code 列を数値として扱い任意 ID 順序に序数構造を課す（MODL-03 違反） | mitigate | _prepare_catboost_pool が HIGH_CARD_CODE_COLS を文字列化して cat_features に含める（review HIGH#6・Cross-Plan #2）・unit test で cat_features に _code 列が含まれることを assert |
| T-04-14 | Tampering | NaN→code -1（pandas category）で LightGBM C++ が missing bucket に非決定論的に振分 | mitigate | __MISSING__ sentinel で fillna 後に category 化・HIGH_CARD_CODE_COLS は frozen map で非負保証・unit test で LOW_CARD + HIGH_CARD_CODE_COLS の両方で .cat.codes.min() >= 0 を assert（Pitfall 3・review MEDIUM） |
| T-04-15 | Information Disclosure | CatBoost random permutation で未来行がカテゴリ符号化に混入 | mitigate | has_time=True で permutation 無効化・Pool は race_start_datetime sort 済み前提・unit test で get_all_params()['has_time']==True を assert（Pitfall 2） |
| T-04-15b | Tampering | CatBoost Pool sort 後の予測が元の行順序に戻らず silent wrong-horse prediction | mitigate | _prepare_catboost_pool が (Pool, sorted_index) を返し align_predictions が元 index に復元・train/predict の全段で X.index.equals(y.index) を assert・test_catboost_predict_preserves_row_order でシャッフル入力でも復元を検証（review HIGH#2） |
| T-04-16 | Information Disclosure | early stopping eval set が calib/test を覗く・または train 全体を超える | mitigate | assert_eval_disjoint で race_key pairwise disjoint を raise ValueError guard・eval は train slice 末尾から切る・**加えて eval_max_date <= train_core_max_date を検証**（review Cross-Plan #8）・unit test で違反入力を検証（D-04/Pitfall 5） |
| T-04-17 | Tampering | BL-3 市場データ（fukuoddslow/ninki）が feature matrix に混入 | mitigate | baseline.py は BL 計算専用・feature matrix には混入しない・assert_matrix_columns_registered で banned_features が空を保証・unit test で odds/ninki 列不在を assert（D-07） |
| T-04-18 | Tampering | BL-3 オッズ逆数のレース内正規化忘れで sum(p) ≠ 払戻対象数 | mitigate | _race_normalize_inverse で sum == payout_places を保証・unit test で sum(p) per race を assert（Pitfall 6） |
| T-04-19 | Spoofing | BL-4/BL-5 学習の random_state 未固定で非再現 | mitigate | LogisticRegression(random_state=42)・train_lightgbm(seed=42, deterministic=True, num_threads=1) で固定・SC#4 reproduce smoke が最終保証 |
| T-04-19b | Information Disclosure | BL-4/BL-5 未キャリブレーションで主モデル（キャリブレーション済み）との比較が不公平 | mitigate | compute_bl4/bl5 が calibrate 引数を持ち・calib_slice 渡せばキャリブレーション・渡さなければ bl_calib_note 列で「未キャリブレーション」注記（review MEDIUM: SC#2 公平性） |
</threat_model>

<verification>
- Wave 3 完了（PLAN 03 と PLAN 04 並列）: `uv run pytest tests/model/test_trainer.py tests/model/test_baseline.py -x -q` が全 GREEN
- SC#3 leak diagnostic GREEN = target encoding 非混入の構造的実証（Phase 8 adversarial audit の監査対象）
- trainer/baseline の estimator と p_bl* Series は PLAN 04 (predict/evaluator) と PLAN 05 (run_train_predict) が消費
</verification>

<success_criteria>
- trainer.py が LightGBM native categorical + CatBoost has_time=True で学習（SC#3/§14.3/§14.4）
- trainer.py が高基数 _code 列を CatBoost cat_features に含め数値扱いしない（review HIGH#6・MODL-03）
- trainer.py が行整列を保証し align_predictions で元順序に復元（review HIGH#2）
- early stopping eval set が train 末尾から切り calib/test と完全分離・eval_max_date <= train_core_max_date（review Cross-Plan #8・D-04/Pitfall 5）
- SC#3 leak diagnostic が低基数 RARE_X + 高基数 _code + 意図的リーク制御で false-pass でないことを実証（review HIGH#3・MODL-03）
- baseline.py が BL-1..5 全5つを計算（MODL-02/SC#2・D-07）
- BL-3 市場データは feature に混入せず独立ベンチマーク（D-07・§14.2 明記）・BL-4/5 キャリブレーション状態が注記される（review MEDIUM）
- 11 test（test_trainer.py 5 + test_baseline.py 6）が GREEN
</success_criteria>

<output>
Create `.planning/phases/04-model-prediction/04-03-SUMMARY.md` when done

## Artifacts this phase produces
- src/model/trainer.py: `train_lightgbm(X_train, y_train, *, X_eval, y_eval, eval_race_keys, calib_race_keys, test_race_keys, train_core_max_date, eval_max_date, params) -> LGBMClassifier`, `train_catboost(...) -> CatBoostClassifier`, `assert_eval_disjoint(eval_race_keys, calib_race_keys, test_race_keys, *, train_core_max_date, eval_max_date)`（review Cross-Plan #8 拡張）, `align_predictions(pred_series, sorted_index, original_index) -> pd.Series`（review HIGH#2）, `_prepare_lightgbm_matrix(df)`, `_prepare_catboost_pool(df, y, *, sort, original_index) -> tuple[Pool, pd.Index]`（review HIGH#6: cat_features に _code 列含む・review HIGH#2: sorted_index 返却）, `_split_train_eval_tail(train_df, eval_fraction=0.2)`, `_build_rare_category_synthetic(n, rare_rate, seed)`, `_build_intentional_leak_control()`（review HIGH#3: DEMONSTRABLY fail 実証）・定数 `LOW_CARD_CAT_COLS`, `HIGH_CARD_CODE_COLS`, `LGB_INIT_PARAMS`（num_threads=1 含む）, `CB_INIT_PARAMS`（thread_count=1 含む）, `N_ESTIMATORS`, `EARLY_STOPPING_ROUNDS`, `RANDOM_SEED`
- src/model/baseline.py: `compute_bl1(df)`, `compute_bl2(df, ninki_col)`, `compute_bl3(df, fukuodds_col)`, `compute_bl4(X_train, y_train, X_test, *, calibrate=False)`, `compute_bl5(X_train, y_train, X_test, *, calibrate=False)`, `compute_all_baselines(df_test, *, X_test_bl4_bl5, y_train, X_train_bl4_bl5, readonly_cur, calib_slice)`, `_race_normalize_inverse(df, value_col)`, `_payout_places(entry_count)`, `fetch_market_data(readonly_cur, race_keys)`・定数 `BL4_FEATURES`, `BL5_FEATURES`
- tests/model/test_trainer.py: 5 test GREEN（test_catboost_predict_preserves_row_order 追加）
- tests/model/test_baseline.py: 6 test GREEN
</output>
