---
phase: 04-model-prediction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - .gitignore
  - src/config/settings.py
  - src/db/schema.py
  - src/db/connection.py
  - scripts/run_apply_schema.py
  - tests/model/__init__.py
  - tests/model/test_data.py
  - tests/model/test_trainer.py
  - tests/model/test_calibrator.py
  - tests/model/test_baseline.py
  - tests/model/test_predict.py
  - tests/model/test_prediction_load.py
  - .planning/PROJECT.md
  - .planning/STATE.md
autonomous: true
requirements: [MODL-01]
tags: [phase-04, foundation, schema, deps, red-stubs, drift-fix]

must_haves:
  truths:
    - "lightgbm==4.6.0 と catboost==1.2.10 が uv.lock に固定され uv sync --frozen で再現可能インストールできる（per D-11）"
    - "prediction.fukusho_prediction テーブルが DDL 定数として src/db/schema.py に存在し ETL ロールに USAGE+CREATE+書込が GRANT される（per D-05/D-12）"
    - "tests/model/ 配下に7ファイル（init含む）の RED test stub が存在し、全 test が pytest で collection される（import error なし）"
    - "PROJECT.md/STATE.md の v3(63・fa0.2.0) 参照が postreview-v2(62・fa0.3.0) に修正されている（per D-01）"
  artifacts:
    - path: "pyproject.toml"
      provides: "lightgbm==4.6.0, catboost==1.2.10 pin（D-11）"
      contains: "lightgbm==4.6.0"
    - path: "src/db/schema.py"
      provides: "PREDICTION_TABLE_DDL + GRANT_ETL_SQL/GRANT_READER_SQL 拡張"
      contains: "prediction.fukusho_prediction"
    - path: "src/config/settings.py"
      provides: "db_schema_prediction='prediction' フィールド"
      contains: "db_schema_prediction"
    - path: "tests/model/__init__.py"
      provides: "package marker"
      min_lines: 1
    - path: "tests/model/test_data.py"
      provides: "RED stubs: test_load_from_parquet_only / test_raw_ids_excluded / test_no_banned_features / test_race_id_disjoint_3way"
      contains: "test_load_from_parquet_only"
  key_links:
    - from: "src/db/connection.py"
      to: "src/config/settings.py"
      via: "make_pool(role='etl') の search_path に settings.db_schema_prediction を追加"
      pattern: "db_schema_prediction"
    - from: "src/db/schema.py"
      to: "prediction.fukusho_prediction テーブル"
      via: "PREDICTION_TABLE_DDL + APPLY_ORDER 追加"
      pattern: "PREDICTION_TABLE_DDL"
---

<objective>
Phase 4 の基盤を整える: (a) LightGBM 4.6.0 / CatBoost 1.2.10 を pyproject.toml + uv.lock に pin しインストール（per D-11）、(b) prediction.fukusho_prediction テーブル DDL + ETL/reader ロール GRANT 拡張 + Settings.db_schema_prediction + connection.py search_path 拡張（per D-05/D-12）、(c) tests/model/ 配下に7ファイルの RED test stub を作成（SC#1/#3/#4/MODL-02/D-05 の検証契約を先に固定・後続 wave で GREEN 化）、(d) PROJECT.md/STATE.md の v3(63) 参照ドリフトを postreview-v2(62) に修正（per D-01）。

Purpose: 後続 wave の trainer/calibrator/baseline/predict/prediction_load 実装が依存する DB スキーマ・ロール権限・依存ライブラリ・test コレクションを先に確立し、実装 task が全て「GREEN 化」に専念できるようにする。Wave 0 = 基盤。
Output: pin 済み pyproject.toml/uv.lock・拡張された schema.py/connection.py/settings.py・7 RED test stub・ドリフト修正済み PROJECT.md/STATE.md
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-model-prediction/04-CONTEXT.md
@.planning/phases/04-model-prediction/04-RESEARCH.md
@.planning/phases/04-model-prediction/04-PATTERNS.md
@.planning/phases/04-model-prediction/04-VALIDATION.md

@pyproject.toml
@src/db/schema.py
@src/db/connection.py
@src/config/settings.py
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: 依存ライブラリ pin + prediction スキーマ DDL/GRANT/Settings 拡張</name>
  <files>pyproject.toml, uv.lock, src/config/settings.py, src/db/schema.py, src/db/connection.py, scripts/run_apply_schema.py, .gitignore</files>
  <read_first>
    - pyproject.toml（dependencies 配列の現状・requires-python 制約確認）
    - src/db/schema.py（SCHEMAS, CREATE_SCHEMAS_SQL, GRANT_READER_SQL, GRANT_ETL_SQL, APPLY_ORDER, __all__）
    - src/db/connection.py（make_pool:21-54 の role='etl' ブロック search_path 構築）
    - src/config/settings.py（Settings クラス:19-, db_schema_label:41, db_schema_raw, db_reader_role:46）
    - scripts/run_apply_schema.py（GRANT 適用の idempotent 実行方法）
    - .gitignore（models/ エントリの有無確認）
    - .planning/phases/04-model-prediction/04-RESEARCH.md D-12 セクション（prediction.fukusho_prediction DDL 完全定義）
    - .planning/phases/04-model-prediction/04-PATTERNS.md「src/db/schema.py」「src/db/connection.py」セクション
  </read_first>
  <action>
    1. 【D-11 pin】`uv add "lightgbm==4.6.0" "catboost==1.2.10"` を実行。pyproject.toml [project].dependencies に両者が追加され uv.lock が更新されることを確認。`requires-python = ">=3.12,<3.13"` は維持。`uv sync --frozen` で再現可能インストールを検証。
    2. 【.gitignore】`models/` エントリが無ければ追加（D-06 model artifact は git 管理外・§19.1 再現性は code+lock+snapshot+provenance）。行末改行・重複回避。
    3. 【Settings.db_schema_prediction】src/config/settings.py の Settings クラスに db_schema_label:41 と同パターンで `db_schema_prediction: str = "prediction"` フィールドを追加。
    4. 【connection.py search_path 拡張】src/db/connection.py make_pool の `role == "etl"` ブロック（line 40-44）で search_path 構築を `f"{settings.db_schema_label},{settings.db_schema_prediction},{settings.db_schema_normalized},public"` に変更（prediction を label と normalized の間に挿入）。
    5. 【PREDICTION_TABLE_DDL】src/db/schema.py に D-12 完全定義の CREATE TABLE 文を `PREDICTION_TABLE_DDL` 定数として追加。テーブル名=`prediction.fukusho_prediction`。列: model_type varchar(16) NOT NULL / model_version varchar(64) NOT NULL / feature_snapshot_id varchar(64) NOT NULL / as_of_datetime timestamp NOT NULL / calib_method varchar(16) NOT NULL / year int / jyocd varchar(2) / kaiji int / nichiji varchar(2) / racenum int / umaban int / kettonum int / p_fukusho_hit double precision NOT NULL / race_date date / split varchar(16)。PRIMARY KEY = (model_type, model_version, year, jyocd, kaiji, nichiji, racenum, umaban, kettonum)（9カラム）。COMMENT ON TABLE で D-05 provenance と staging-swap idempotent 由来を明記。
    6. 【GRANT_ETL_SQL 拡張】既存 label ブロック（schema.py:103-108）の直後に prediction ブロックを追記: `GRANT USAGE, CREATE ON SCHEMA prediction TO {etl}` / `GRANT SELECT, INSERT, UPDATE, DELETE, TRUNCATE ON ALL TABLES IN SCHEMA prediction TO {etl}` / `ALTER DEFAULT PRIVILEGES IN SCHEMA prediction GRANT SELECT, INSERT, UPDATE, DELETE, TRUNCATE ON TABLES TO {etl}`。
    7. 【GRANT_READER_SQL 拡張】既存 label reader ブロック（schema.py:88-90）の直後に prediction reader ブロックを追記: `GRANT USAGE ON SCHEMA prediction TO {reader}` / `GRANT SELECT ON ALL TABLES IN SCHEMA prediction TO {reader}` / `ALTER DEFAULT PRIVILEGES IN SCHEMA prediction GRANT SELECT ON TABLES TO {reader}`。
    8. 【APPLY_ORDER】`("prediction_table", PREDICTION_TABLE_DDL)` を grant_etl の直前（CREATE_SCHEMAS_SQL の後・GRANT_ETL_SQL の前）に挿入。
    9. 【__all__】`PREDICTION_TABLE_DDL` を __all__ に追加。
    10. 【idempotent 適用】`uv run python scripts/run_apply_schema.py` を実行し GRANT 拡張を live DB に反映（CREATE TABLE IF NOT EXISTS + idempotent GRANT）。psql で `\dt prediction.*` と `\dp prediction.fukusho_prediction` を確認。
  </action>
  <verify>
    <automated>uv run python -c "import lightgbm, catboost; assert lightgbm.__version__=='4.6.0'; assert catboost.__version__=='1.2.10'; from src.db.schema import PREDICTION_TABLE_DDL, GRANT_ETL_SQL, GRANT_READER_SQL; assert 'prediction.fukusho_prediction' in PREDICTION_TABLE_DDL; assert 'USAGE, CREATE ON SCHEMA prediction' in GRANT_ETL_SQL; assert 'USAGE ON SCHEMA prediction' in GRANT_READER_SQL; from src.config.settings import Settings; assert Settings().db_schema_prediction=='prediction'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `uv pip show lightgbm` が Version: 4.6.0 を出力
    - `uv pip show catboost` が Version: 1.2.10 を出力
    - `grep -c 'lightgbm==4.6.0' pyproject.toml` == 1 / `grep -c 'catboost==1.2.10' pyproject.toml` == 1
    - `grep -c 'models/' .gitignore` >= 1
    - `uv run python scripts/run_apply_schema.py` が exit 0 で完了
    - `psql -c "\dt prediction.*"` が `prediction | fukusho_prediction | table | keiba_etl` を含む
    - `psql -c "\dp prediction.fukusho_prediction"` が keiba_etl に arwdDxt（insert/update/delete/truncate）を表示
    - connection.py の grep で `db_schema_prediction` が etl search_path 行に出現
    - Settings().db_schema_prediction == "prediction" を Python で確認
  </acceptance_criteria>
  <done>両ライブラリ pin 済み・prediction テーブル live DB に作成済み・ETL ロール USAGE+CREATE+書込 GRANT 済み・reader ロール SELECT GRANT 済み・Settings.db_schema_prediction 設定済み・etl search_path 拡張済み・models/ gitignore 済み</done>
</task>

<task type="auto">
  <name>Task 2: tests/model/ RED stubs 作成（SC#1/#3/#4/MODL-02/D-05 検証契約の固定）</name>
  <files>tests/model/__init__.py, tests/model/test_data.py, tests/model/test_trainer.py, tests/model/test_calibrator.py, tests/model/test_baseline.py, tests/model/test_predict.py, tests/model/test_prediction_load.py</files>
  <read_first>
    - tests/conftest.py（settings / pg_pool / readonly_cur / write_pool / write_cur / pytest_collection_modifyitems / KEIBA_SKIP_DB_TESTS の既存 fixtures）
    - tests/utils/test_calibrator.py（smoke + ValueError guard 検証パターン・サブプロセステスト）
    - tests/features/test_snapshot_repro.py（_build_synthetic_matrix + 2回呼出 hash 比較パターン）
    - tests/features/test_allowlist.py（allowlist assert パターン）
    - tests/test_fukusho_label.py（idempotent verify・requires_db マーク）
    - pyproject.toml [tool.pytest.ini_options]（markers=requires_db・testpaths=["tests"]）
    - .planning/phases/04-model-prediction/04-VALIDATION.md Per-Task Verification Map（13 test 契約の full list）
    - .planning/phases/04-model-prediction/04-RESEARCH.md D-05/D-06（SC#3 leak diagnostic / SC#4 reproduce smoke の手順）
  </read_first>
  <action>
    1. 【__init__.py】空の package marker のみ作成（docstring 1行 + `"""Phase 4 model layer tests."""`）。
    2. 【test_data.py】RESEARCH D-02b/PATTERNS data.py セクションに基づき以下の RED stub（関数名 + docstring + `pytest.fail("Wave 0 stub - implemented in PLAN 02/03")` のみ）を作成:
       - `test_load_from_parquet_only`（SC#1: snapshots/feature_matrix_20260620-1a-postreview-v2.parquet から読込・live DB 非使用）
       - `test_raw_ids_excluded`（kisyucode/chokyosicode/ketto3infohansyokunum1/2 がモデル入力に含まれない）
       - `test_no_banned_features`（assert_matrix_columns_registered で banned_features が空）
       - `test_race_id_disjoint_3way`（3way 分割で同一 race_id が train/calib/test を跨がない・BACK-01 前置）
    3. 【test_trainer.py】以下の RED stub:
       - `test_lightgbm_nonneg_codes`（SC#3: category dtype の code が非負・NaN→-1 禁止）
       - `test_catboost_has_time`（SC#3: CatBoostClassifier が has_time=True・Pool は race_start_datetime sort）
       - `test_no_target_encoding_leak`（SC#3 leak diagnostic: 合成希少カテゴリ RARE_X で予測が mean に縮む）
       - `test_eval_set_disjoint_from_calib_test`（D-04: eval_races ∩ calib_races == ∅ / ∩ test_races == ∅）
    4. 【test_calibrator.py】以下の RED stub:
       - `test_strict_later_disjoint`（SC#4: max(train.race_date) < min(calib.race_date) 違反で fit_prefit_calibrator が ValueError）
       - `test_reproduce_bit_identical`（SC#4 smoke: seed=42 で2回 train_and_predict → np.array_equal(pred1, pred2)）
       - `test_isotonic_vs_sigmoid_threshold`（calib sample >=1000 で isotonic・<1000 で sigmoid）
    5. 【test_baseline.py】以下の RED stub:
       - `test_bl1_field_size_constant`（BL-1: 8頭以上 3/count・5-7頭 2/count）
       - `test_bl2_ninki_normalized`（BL-2: 1/ninki レース内正規化で sum=払戻対象数）
       - `test_bl3_fukuodds_inverse_normalized`（BL-3: 1/fukuoddslow レース内正規化・D-07）
       - `test_bl4_logreg`（BL-4: LogisticRegression 少数特徴量）
       - `test_bl5_min_lightgbm`（BL-5: rolling 除外 LightGBM 最小特徴量）
       - `test_market_data_source`（D-08: n_odds_tanpuku.fukuoddslow / n_uma_race.ninki が存在）
    6. 【test_predict.py】以下の RED stub:
       - `test_provenance_columns`（model_type/model_version/feature_snapshot_id/as_of_datetime/calib_method が存在・NOT NULL）
       - `test_model_version_numbering`（D-10: 20260620-1a-lgb-v1 / 20260620-1a-cb-v1 形式）
    7. 【test_prediction_load.py】`@pytest.mark.requires_db` を付与した以下の RED stub:
       - `test_idempotent_checksum_match`（D-05: _idempotent_load_prediction を2回実行で checksum 一致）
    8. 【stub 実装規約】各 stub は `import pytest` + 関数定義 + docstring（検証する振る舞いを明記）+ `pytest.fail("Wave 0 RED stub - implemented in PLAN 0X")` のみ。実装・import 完了後の production モジュール import は書かない（後続 wave で実装）。これにより全 stub が collection され RED 状態で実行される。
    9. 【実行検証】`uv run pytest tests/model/ --collect-only -q` で13+ test が collection error なくリストされることを確認。各 stub を実行すると `Wave 0 RED stub` で fail することを確認（RED 状態の確立）。
  </action>
  <verify>
    <automated>uv run pytest tests/model/ --collect-only -q 2>&1 | grep -c "::test_" | awk '{ if ($1 >= 13) { exit 0 } else { exit 1 } }' && echo "COLLECT_OK" && uv run pytest tests/model/ -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/model/ 配下に7ファイル（__init__.py 含む）が存在
    - `uv run pytest tests/model/ --collect-only -q` が import error なく 13 件以上の test をリスト
    - `uv run pytest tests/model/ -q` の結果が "13 failed"（全 RED）・collection error なし・setup/teardown error なし
    - 各 stub 関数が docstring で SC#/D-XX 検証契約を明記
    - test_prediction_load.py の test に `@pytest.mark.requires_db` が付与されている
    - conftest.py の KEIBA_SKIP_DB_TESTS=1 時に requires_db マークの test が skip される（既存挙動の回帰なし）
  </acceptance_criteria>
  <done>7 RED stub ファイルが存在・13+ test が collection される・全 RED・後続 wave が GREEN 化する検証契約を docstring で固定</done>
</task>

<task type="auto">
  <name>Task 3: PROJECT.md / STATE.md の v3(63) ドリフトを postreview-v2(62) に修正（per D-01）</name>
  <files>.planning/PROJECT.md, .planning/STATE.md</files>
  <read_first>
    - .planning/PROJECT.md（v3 参照箇所の grep: `grep -n '20260619-1a-v3\|63 feature\|fa 0.2.0\|feature_count.*63\|v3.*snapshot'`）
    - .planning/STATE.md（同上の v3 参照箇所）
    - .planning/phases/04-model-prediction/04-CONTEXT.md D-01 セクション（postreview-v2 の完全仕様）
    - .planning/phases/04-model-prediction/04-RESEARCH.md D-02 確定事項（rolling_jyocd_mean→mode rename + sd remove）
  </read_first>
  <action>
    1. 【ドリフト箇所特定】`.planning/PROJECT.md` と `.planning/STATE.md` を `grep -n '20260619-1a-v3\|63 feature\|fa.*0\.2\.0\|feature_count.*63'` で走査。CONTEXT.md D-01 が指摘する「v3(63・fa0.2.0) 参照ドリフト」の全出現箇所をリスト化。
    2. 【PROJECT.md 修正】該当箇所の snapshot id を `20260619-1a-v3` → `20260620-1a-postreview-v2` に更新。feature_count を `63` → `62` に更新。fa_version `0.2.0` → `0.3.0` に更新。SHA256 が記載されていれば `26c685f0…ecbdd2`（postreview-v2）に更新。修正は Edit (scoped replacement) のみで実行・全ファイル Write は不可。
    3. 【STATE.md 修正】同様に v3 参照を postreview-v2 に更新。current_phase: 4 / status: planning は維持。progress.completed_plans 等のメトリクスは変更しない（本 task はドリフト修正のみ）。
    4. 【ドリフト解消確認】修正後 `grep -rn '20260619-1a-v3' .planning/PROJECT.md .planning/STATE.md` が空であることを確認（v3 参照の完全消除）。
    5. 【D-02 由来注記】PROJECT.md の snapshot 記述箇所に 1 行注記追加: 「feature 63→62 は CR-02 で rolling_jyocd_mean→mode rename + sd remove（commit 43bd81f・§13.4 odds-free allowlist 違反なし）」。これは D-01 で研究者が確定済みの内容を文書に反映。
  </action>
  <verify>
    <automated>! grep -q '20260619-1a-v3' .planning/PROJECT.md .planning/STATE.md && grep -q '20260620-1a-postreview-v2' .planning/PROJECT.md && grep -q '20260620-1a-postreview-v2' .planning/STATE.md && echo "DRIFT_FIX_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn '20260619-1a-v3' .planning/PROJECT.md .planning/STATE.md` が空（v3 参照 0 件）
    - `grep -c '20260620-1a-postreview-v2' .planning/PROJECT.md` >= 1
    - `grep -c '20260620-1a-postreview-v2' .planning/STATE.md` >= 1
    - PROJECT.md/STATE.md に `63` feature 数言及が残っていない（grep `63 feature` が空・ただし行番号や無関係な 63 は除く）
    - PROJECT.md に rolling_jyocd rename 由来の注記が追加されている
  </acceptance_criteria>
  <done>両ファイルから v3 参照が完全に消除され postreview-v2(62・fa0.3.0) が正として記載される（per D-01 §19.1 再現性聖域の文書整合）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| パッケージインストール（uv add）→ venv | 外部 PyPI から LightGBM/CatBoost wheel を取り込む境界 |
| ETL ロール GRANT → prediction スキーマ | DB ロール権限の拡張点・過剰権限付与を避ける境界 |
| ETL pool search_path → prediction.fukusho_prediction | search_path injection で意図しないテーブルを書き替える攻撃面 |
| Live DB DDL 適用（run_apply_schema.py） | 既存スキーマへの副作用境界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-SC | Tampering | uv add lightgbm/catboost | mitigate | Package Legitimacy Audit 済（RESEARCH.md・PyPI 確認・Microsoft/Yandex 公式・要件固定版）。blocking human checkpoint 不要（[VERIFIED: PyPI] で Approved） |
| T-04-01 | Elevation of Privilege | GRANT_ETL_SQL prediction ブロック | mitigate | `GRANT USAGE, CREATE ON SCHEMA prediction` + `GRANT SELECT, INSERT, UPDATE, DELETE, TRUNCATE` のみ付与・SUPERUSER/CREATEDB 不与・既存 label ブロックと同一権限 scope |
| T-04-02 | Spoofing | connection.py etl search_path | mitigate | search_path に settings.db_schema_prediction を明示追加・動的文字列結合なし・`options=-c search_path=...` で固定（既存パターン踏襲） |
| T-04-03 | Tampering | PREDICTION_TABLE_DDL | mitigate | CREATE TABLE IF NOT EXISTS（idempotent）+ 9カラム PRIMARY KEY 制約 + NOT NULL provenance 列（model_type/model_version/feature_snapshot_id/as_of_datetime/calib_method）で不正レコード挿入を DB 側で拒否 |
| T-04-04 | Information Disclosure | run_apply_schema.py DDL エラーログ | accept | 既存 logger 使用・DSN は dsn_masked で出力（既存 CR-06）・新規 PII なし |
| T-04-05 | Repudiation | tests/model/ stub が後続 wave で GREEN 化される順序 | mitigate | 各 test が docstring で SC#/D-XX を明記・VALIDATION.md Per-Task Map が task 割当を追跡・後続 PLAN が「どの stub を GREEN 化するか」を action に明示 |
</threat_model>

<verification>
- Wave 1 完了時点: `uv run pytest tests/model/ --collect-only` で13+ RED test が collection される・`uv run pytest tests/ -q`（既存 24 ファイル）が全 GREEN（回帰なし）・`uv run python scripts/run_apply_schema.py` が exit 0・`psql -c "\dt prediction.*"` で fukusho_prediction が見える
- 後続 wave は本 PLAN で作成した test stub を GREEN 化することが検証契約
</verification>

<success_criteria>
- lightgbm==4.6.0 / catboost==1.2.10 が uv.lock + pyproject.toml に固定されインストール済み（D-11）
- prediction.fukusho_prediction テーブルが D-12 完全 DDL で live DB に作成済み・ETL/reader GRANT 済み（D-05）
- Settings.db_schema_prediction + connection.py etl search_path 拡張済み
- tests/model/ に7ファイル 13+ RED stub が存在し collection される
- PROJECT.md/STATE.md の v3 ドリフトが完全解消（D-01 §19.1）
- 既存テストスイート（24 ファイル）の回帰なし
</success_criteria>

<output>
Create `.planning/phases/04-model-prediction/04-01-SUMMARY.md` when done

## Artifacts this phase produces
- pyproject.toml: `lightgbm==4.6.0`, `catboost==1.2.10` pin 追加
- src/db/schema.py: `PREDICTION_TABLE_DDL` 定数・`GRANT_ETL_SQL`/`GRANT_READER_SQL` の prediction ブロック・`APPLY_ORDER` への `("prediction_table", PREDICTION_TABLE_DDL)` 追加・`__all__` への `PREDICTION_TABLE_DDL` 追加
- src/config/settings.py: `Settings.db_schema_prediction: str = "prediction"` フィールド
- src/db/connection.py: `make_pool(role='etl')` の search_path に `settings.db_schema_prediction` 追加
- DB: `prediction.fukusho_prediction` テーブル（9カラム PK・provenance 列）
- tests/model/__init__.py, test_data.py, test_trainer.py, test_calibrator.py, test_baseline.py, test_predict.py, test_prediction_load.py（13+ RED stub）
- .gitignore: `models/` エントリ
- .planning/PROJECT.md / .planning/STATE.md: v3 → postreview-v2 ドリフト修正
</output>
