---
phase: 04-model-prediction
plan: 05
type: execute
wave: 4
depends_on: [04-03, 04-04]
files_modified:
  - scripts/run_train_predict.py
  - src/model/calibrator.py
  - tests/model/test_calibrator.py
autonomous: true
requirements: [MODL-01, MODL-02, MODL-03]
tags: [phase-04, integration, run-train-predict, sc4-reproduce, both-models, end-to-end]

must_haves:
  truths:
    - "scripts/run_train_predict.py が LightGBM と CatBoost の両方を学習し calibrate_model でキャリブレーションし predict_p_fukusho で予測し load_predictions で prediction.fukusho_prediction に書込む（per D-03・両モデル候補として並列）"
    - "両モデルの artifact が models/20260620-1a-postreview-v2-lgb-v1/ と models/20260620-1a-postreview-v2-cb-v1/ にネイティブ形式（lgb_model.txt/cb_model.cbm/calibrator.joblib/metadata.json）で保存される（per D-06/D-10）"
    - "evaluator.evaluate_all_models が reports/04-eval.md に LightGBM/CatBoost + BL-1..5 の比較表を出力する（per SC#2）"
    - "SC#4 reproduce smoke（固定 seed=42 で2回 train_and_predict → np.array_equal(pred1, pred2)）が GREEN になる（per D-06・SC#4 構造的ブロック）"
    - "prediction.fukusho_prediction に両モデル（model_type='lightgbm' と 'catboost'）の予測が provenance 列付きで永続化される（per D-05・§19.1）"
  artifacts:
    - path: "scripts/run_train_predict.py"
      provides: "Phase 4 エントリポイント（学習→キャリブレーション→予測→書込→評価・両モデル・--model-type {lightgbm,catboost,both}）"
      contains: "run_train_predict"
      min_lines: 100
    - path: "src/model/calibrator.py"
      provides: "train_and_predict orchestrator（trainer + calibrate_model + predict_p_fukusho 統合・reproduce smoke 用）"
      contains: "train_and_predict"
  key_links:
    - from: "scripts/run_train_predict.py"
      to: "src/model/trainer.py"
      via: "train_lightgbm / train_catboost 呼出"
      pattern: "train_lightgbm"
    - from: "scripts/run_train_predict.py"
      to: "src/model/baseline.py"
      via: "compute_all_baselines 呼出"
      pattern: "compute_all_baselines"
    - from: "scripts/run_train_predict.py"
      to: "src/db/prediction_load.py"
      via: "load_predictions で prediction.fukusho_prediction に書込"
      pattern: "load_predictions"
    - from: "src/model/calibrator.py"
      to: "src/model/trainer.py + predict.py"
      via: "train_and_predict orchestrator（reproduce smoke 用）"
      pattern: "predict_p_fukusho"
---

<objective>
Phase 4 のエンドツーエンド統合を実装: (a) scripts/run_train_predict.py が data → trainer（LightGBM + CatBoost 両方）→ calibrate_model → predict_p_fukusho → load_predictions → evaluator.evaluate_all_models の全 pipeline を実行する（per D-03 両モデル候補・Phase 5 で両方 backtest）、(b) src/model/calibrator.py に train_and_predict orchestrator を追加し trainer + calibrate_model + predict_p_fukusho を統合（reproduce smoke test が消費）、(c) SC#4 reproduce smoke test（seed=42 で2回 train_and_predict → np.array_equal(pred1, pred2)）を GREEN 化（per D-06・構造的ブロック・失敗時 Phase 完了不可）。reports/04-eval.md に比較表を出力し prediction.fukusho_prediction に両モデル予測を永続化。

Purpose: Phase 4 の成果物（両モデル予測 + BL 比較表 + リーク防止実証）を単一エントリポイントで完結させる。SC#4 reproduce smoke が GREEN で固定 seed の bit-identical 再現性を実証（§19.1 聖域）。Phase 5/6/7 は prediction テーブルと artifact を消費。
Output: scripts/run_train_predict.py + train_and_predict orchestrator + SC#4 reproduce smoke GREEN + reports/04-eval.md + prediction.fukusho_prediction 両モデル予測
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-model-prediction/04-CONTEXT.md
@.planning/phases/04-model-prediction/04-RESEARCH.md
@.planning/phases/04-model-prediction/04-PATTERNS.md
@.planning/phases/04-model-prediction/04-VALIDATION.md

@src/model/data.py
@src/model/trainer.py
@src/model/calibrator.py
@src/model/baseline.py
@src/model/predict.py
@src/model/evaluator.py
@src/model/artifact.py
@src/db/prediction_load.py
@src/db/connection.py
@src/config/settings.py
@scripts/run_feature_build.py
@scripts/run_label_etl.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/model/calibrator.py に train_and_predict orchestrator 追加（SC#4 reproduce smoke 用）</name>
  <files>src/model/calibrator.py, tests/model/test_calibrator.py</files>
  <behavior>
    - Test test_reproduce_bit_identical（強化）: train_and_predict(model_type, feature_df, *, seed=42) を2回呼出し・戻り prediction DataFrame の p_fukusho_hit 列が np.array_equal（bit-identical）・calibrated estimator も再現性あり・3way split が安定
  </behavior>
  <read_first>
    - src/model/calibrator.py（PLAN 02: calibrate_model・CalibrationResult）
    - src/model/data.py（PLAN 02: prepare_model_matrix / split_3way / load_frozen_maps）
    - src/model/trainer.py（PLAN 03: train_lightgbm / train_catboost / _split_train_eval_tail / assert_eval_disjoint）
    - src/model/predict.py（PLAN 04: predict_p_fukusho / make_model_version）
    - src/model/artifact.py（PLAN 02: save_native_artifact）
    - tests/model/test_calibrator.py（PLAN 02: test_reproduce_bit_identical は lightgbm 小モデルで代用済み・本 task で train_and_predict に強化）
    - .planning/phases/04-model-prediction/04-RESEARCH.md D-06「SC#4 reproduce smoke test の設計」（固定 seed リスト・np.array_equal 比較）・Code Examples SC#4 Reproduce Smoke Test
    - tests/features/test_snapshot_repro.py:40-52（2回呼出 hash 比較パターン）
  </read_first>
  <action>
    1. 【train_and_predict(feature_df, *, model_type, feature_snapshot_id, version_n=1, seed=42, eval_fraction=0.2, params_override=None) -> dict】orchestrator: 
       - `X, y = prepare_model_matrix(feature_df)`（data.py）
       - `splits = split_3way(feature_df)`（data.py・train/calib/test）
       - `train_core, train_tail = _split_train_eval_tail(splits["train"], eval_fraction)`（trainer.py・eval set 分離）
       - model_type で分岐: "lightgbm" → `estimator = train_lightgbm(X_train_core, y_train_core, X_eval=X_train_tail, y_eval=y_train_tail, eval_race_ids=..., calib_race_ids=..., test_race_ids=..., params=params_override)` / "catboost" → `train_catboost(...)`（同様）
       - `calib_result = calibrate_model(estimator, X_calib, y_calib, race_dates_calib=splits["calib"]["race_date"], train_max_date=splits["train"]["race_date"].max())`（calibrator.py・isotonic/sigmoid 切替）
       - `model_version = make_model_version(feature_snapshot_id, model_type, version_n)`（predict.py・D-10）
       - `pred_df_test = predict_p_fukusho(calib_result.calibrated, X_test, model_type=..., model_version=..., feature_snapshot_id=..., calib_method=calib_result.calib_method, race_df=splits["test"], split_label="test")`（predict.py）
       - 戻り値 dict = {"estimator": estimator, "calibrated": calib_result.calibrated, "calib_method": calib_result.calib_method, "pred_df": pred_df_test, "model_version": model_version, "splits": splits}
       - seed は trainer の RANDOM_SEED と lgb/cb params で固定済み・orchestrator 側では seed 引数を持ちつつ trainer 呼出で params_override["seed"]/["random_seed"] を上書き（既定 seed=42）
    2. 【determinism 検証ヘルパー _assert_deterministic(model_type, feature_df)】train_and_predict を2回呼出し np.array_equal(pred1["pred_df"]["p_fukusho_hit"].values, pred2["pred_df"]["p_fukusho_hit"].values) を assert・違反で RuntimeError（非決定論的要素あり）。内部ヘルパー・test と run_train_predict の --check-reproduce が使用。
    3. 【Test 強化 GREEN 化】tests/model/test_calibrator.py の test_reproduce_bit_identical を train_and_predict を使う形に強化（PLAN 02 で lightgbm 小モデル代用だったものを置換）:
       - 合成 feature_df（race_id 時系列・categorical 含む・n=1000 程度）で train_and_predict(model_type="lightgbm", ...) を seed=42 で2回呼出・np.array_equal(pred1, pred2) を assert
       - model_type="catboost" でも同様（has_time=True で決定論的）
       - docstring から「trainer 実装後に強化」の注記を削除（本格化完了）
  </action>
  <verify>
    <automated>uv run pytest tests/model/test_calibrator.py::test_reproduce_bit_identical -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/model/test_calibrator.py::test_reproduce_bit_identical が GREEN（lightgbm + catboost 両方で bit-identical）
    - src/model/calibrator.py に `def train_and_predict` orchestrator がある
    - train_and_predict が prepare_matrix/split_3way/_split_train_eval_tail/train_lightgbm or train_catboost/calibrate_model/predict_p_fukusho を順に呼出
    - np.array_equal で pred1 と pred2 が bit-identical を assert（SC#4・§19.1）
    - train_and_predict が seed 引数を持ち trainer params の seed/random_seed を上書き可能
    - catboost 呼出で has_time=True が維持（train_catboost 内部で保証・orchestrator では上書きしない）
  </acceptance_criteria>
  <done>train_and_predict orchestrator が trainer + calibrate_model + predict_p_fukusho を統合し・SC#4 reproduce smoke（両モデル bit-identical）が GREEN（SC#4/D-06 構造的ブロック達成）</done>
</task>

<task type="auto">
  <name>Task 2: scripts/run_train_predict.py — エンドツーエンド pipeline エントリポイント</name>
  <files>scripts/run_train_predict.py</files>
  <read_first>
    - scripts/run_feature_build.py（構造が最も近い: sys.path 挿入:38-41 / argparse:65-103 / pool 構築 / try-except-finally / masked DSN ログ:117）
    - scripts/run_label_etl.py（logging.basicConfig:39-43 / masked DSN:50-51 / make_pool role='readonly'+role='etl':53-54 / try-except PsycopgError / finally pool.close:56-127 / idempotent verify 2回実行:90-99）
    - src/model/calibrator.py（PLAN 05 Task 1: train_and_predict）
    - src/model/baseline.py（PLAN 03: compute_all_baselines / fetch_market_data）
    - src/model/predict.py（PLAN 04: predict_p_fukusho / make_model_version）
    - src/model/evaluator.py（PLAN 04: evaluate_all_models）
    - src/model/artifact.py（PLAN 02: save_native_artifact）
    - src/db/prediction_load.py（PLAN 04: load_predictions）
    - src/db/connection.py（make_pool / readonly_cursor / write_cursor）
    - src/config/settings.py（dsn_masked / etl_dsn_masked / db_reader_role）
    - .planning/phases/04-model-prediction/04-PATTERNS.md「scripts/run_train_predict.py」セクション・Shared Patterns 4/8
  </read_first>
  <action>
    1. 【モジュール docstring + from __future__ import annotations】Phase 4 エントリポイント・D-01/D-03/D-05/D-06 を明記。`uv run python scripts/run_train_predict.py [--snapshot-id 20260620-1a-postreview-v2] [--model-type {lightgbm,catboost,both}] [--version-n 1] [--check-reproduce]`。
    2. 【sys.path 挿入】run_feature_build.py:38-41 パターンで repo-root を sys.path へ。
    3. 【logging.basicConfig】run_label_etl.py:39-43 と同一フォーマット。
    4. 【masked DSN ログ】`logger.info("readonly DSN: %s", settings.dsn_masked)` と `logger.info("etl DSN: %s", settings.etl_dsn_masked)`（生 DSN 絶対出力禁止・Shared Pattern 8・MEDIUM #1）。
    5. 【parse_args】argparse で: --snapshot-id（デフォルト "20260620-1a-postreview-v2"・D-01）・--model-type {lightgbm,catboost,both}（デフォルト both・D-03）・--version-n（デフォルト 1・D-10）・--check-reproduce（SC#4 smoke を実行）・--no-write-db（prediction 書込を skip・dry run）。
    6. 【pool 構築】`readonly_pool = make_pool(settings, role="readonly")`（feature/label/odds/ninki SELECT・run_label_etl.py:53 と同一）+ `etl_pool = make_pool(settings, role="etl")`（prediction 書込・search_path 拡張済み）。
    7. 【データ準備】`feature_df = load_feature_matrix()`（data.py・SNAPSHOT_PATH デフォルト = D-01）→ `readonly_cur` で label join（join_labels）・BL 市場データ取得（fetch_market_data for BL-2/BL-3）。
    8. 【両モデル学習】args.model_type でループ: model_type in ["lightgbm","catboost"]（both の場合両方）: `result = train_and_predict(feature_df, model_type=model_type, feature_snapshot_id=args.snapshot_id, version_n=args.version_n, seed=42)`（calibrator.py orchestrator・eval set 分離済み）。各モデルの calibrated estimator と pred_df を収集。
    9. 【artifact 保存】`save_native_artifact(result["calibrated"], model_type, result["model_version"], out_dir=f"models/{result['model_version']}", feature_snapshot_id=args.snapshot_id, hyperparams=..., seed=42, train_calib_test_periods=...)`（artifact.py・D-06・.gitignore 対象）。各モデルで実行。
    10. 【BL 計算】`baseline_df = compute_all_baselines(test_df, X_test_bl4_bl5=..., y_train=..., X_train_bl4_bl5=..., readonly_cur=readonly_cur)`（baseline.py・BL-1..5・市場データは readonly SELECT）。
    11. 【prediction 書込】args.no_write_db でなければ: 両モデルの pred_df を統合し `load_predictions(write_cur, combined_pred_df, reader_role=settings.db_reader_role)`（prediction_load.py・staging-swap idempotent・2回目実行で checksum 一致）。
    12. 【評価レポート】`metrics = evaluate_all_models({"lightgbm": lgb_pred, "catboost": cb_pred, "bl1": ..., ..., "bl5": ...}, y_true_by_split={"test": y_test}, out_path="reports/04-eval.md")`（evaluator.py・SC#2 比較表・§15.2 sum(p) 検査）。
    13. 【SC#4 --check-reproduce】args.check_reproduce の場合: 各 model_type で train_and_predict を seed=42 で2回呼出し np.array_equal を assert（calibrator.py の _assert_deterministic を使用）・失敗で sys.exit(1)（構造的ブロック・SC#4）。
    14. 【try / except PsycopgError / finally pool.close】run_label_etl.py:56-127 と同一構造。PsycopgError を catch して logger.error・finally で readonly_pool.close() / etl_pool.close()。
    15. 【idempotent verify】prediction 書込後 load_predictions をもう1回実行し checksum が前回と一致することを logger.info で報告（run_label_etl.py:90-99 の2回実行パターン）。
    16. 【終了サマリ】logger.info で: 両モデル model_version / artifact path / prediction row count / checksum / reports/04-eval.md path / SC#4 reproduce 結果 を出力。
  </action>
  <verify>
    <automated>uv run python scripts/run_train_predict.py --help | grep -E "snapshot-id|model-type|check-reproduce" && echo "ARGPARSE_OK"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/run_train_predict.py が --snapshot-id / --model-type / --version-n / --check-reproduce / --no-write-db の5 argparse を持つ
    - デフォルト --snapshot-id が "20260620-1a-postreview-v2"（D-01）
    - デフォルト --model-type が "both"（D-03・両モデル候補）
    - scripts/run_train_predict.py --help が exit 0
    - sys.path 挿入 / logging.basicConfig / masked DSN ログ（dsn_masked/etl_dsn_masked）が run_label_etl.py と同一パターン
    - 生 DSN 文字列がスクリプト内に埋込まれていない（grep で password= 等が無い・settings.dsn_masked のみ使用）
    - try/except PsycopgError/finally pool.close 構造がある
    - 両モデル（lightgbm + catboost）の学習→キャリブレーション→予測→artifact 保存→prediction 書込→評価レポート の全ステップが順序立てて実装
    - load_predictions が staging-swap idempotent・2回実行で checksum 報告
  </acceptance_criteria>
  <done>run_train_predict.py が両モデルのエンドツーエンド pipeline を単一エントリポイントで実行し・reports/04-eval.md と prediction.fukusho_prediction（両モデル）と models/{version}/ artifact を生成（D-01/D-03/D-05/D-06/SC#2 達成）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI 引数 → pipeline 実行 | --snapshot-id / --model-type / --check-reproduce 等の信頼できない入力境界 |
| readonly pool → DB SELECT | feature/label/odds/ninki 読込境界 |
| etl pool → prediction.fukusho_prediction 書込 | staging-swap atomic 替換境界 |
| 固定 seed → 2回 train_and_predict → np.array_equal | SC#4 reproduce smoke の検証力を決定する境界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-26 | Tampering | CLI 引数 injection（--snapshot-id で不正パス読込） | mitigate | argparse choices/型検証・SNAPSHOT_PATH は data.py 定数と一致確認・任意パス読込不可 |
| T-04-27 | Information Disclosure | スクリプトログへの生 DSN 出力 | mitigate | settings.dsn_masked / etl_dsn_masked のみ使用・生 DSN 埋込なし・Shared Pattern 8（MEDIUM #1・run_label_etl.py パターン踏襲） |
| T-04-28 | Denial of Service | pool close 忘れで DB 接続枯渇 | mitigate | try/except/finally で readonly_pool.close() / etl_pool.close() を保証（run_label_etl.py:56-127 パターン） |
| T-04-29 | Repudiation | 非決定論的要素で SC#4 reproduce 失敗を silent 無視 | mitigate | --check-reproduce で np.array_equal 違反時 sys.exit(1)（構造的ブロック・SC#4・Phase 完了不可） |
| T-04-30 | Tampering | prediction 書込の部分失敗 | mitigate | load_predictions staging-swap atomic・2回実行 checksum 報告・idempotent（PATTERNS Shared Pattern 5） |
| T-04-31 | Repudiation | artifact 保存の非決定論的書込 | mitigate | save_native_artifact の metadata.json が sort_keys=True + atomic write・SC#4 reproduce smoke が最終保証 |
</threat_model>

<verification>
- Wave 4 完了: `uv run pytest tests/model/test_calibrator.py::test_reproduce_bit_identical -x -q` が GREEN・`uv run python scripts/run_train_predict.py --help` が exit 0
- 実データ実行（PLAN 06 で実施）: `uv run python scripts/run_train_predict.py --check-reproduce` が両モデルで bit-identical・reports/04-eval.md 生成・prediction.fukusho_prediction 両モデル予測永続化
</verification>

<success_criteria>
- train_and_predict orchestrator が trainer + calibrate_model + predict_p_fukusho を統合（SC#4 smoke 用）
- SC#4 reproduce smoke（両モデル bit-identical）が GREEN（D-06・§19.1 構造的ブロック）
- run_train_predict.py が両モデル pipeline を実行し reports/04-eval.md + prediction.fukusho_prediction + models/ artifact を生成（D-01/D-03/D-05/D-06/SC#2）
- masked DSN ログ・try/finally pool close・idempotent verify が既存パターンと一致
</success_criteria>

<output>
Create `.planning/phases/04-model-prediction/04-05-SUMMARY.md` when done

## Artifacts this phase produces
- src/model/calibrator.py: `train_and_predict(feature_df, *, model_type, feature_snapshot_id, version_n=1, seed=42, eval_fraction=0.2, params_override=None) -> dict` orchestrator, `_assert_deterministic(model_type, feature_df)` helper（PLAN 02 の calibrate_model に追加）
- scripts/run_train_predict.py: Phase 4 エントリポイント・CLI flags `--snapshot-id`（デフォルト 20260620-1a-postreview-v2）, `--model-type {lightgbm,catboost,both}`（デフォルト both）, `--version-n`（デフォルト 1）, `--check-reproduce`, `--no-write-db`
- tests/model/test_calibrator.py: test_reproduce_bit_identical 強化 GREEN（lightgbm + catboost bit-identical）
- 実行生成物: reports/04-eval.md（比較表）, models/20260620-1a-postreview-v2-lgb-v1/{lgb_model.txt, calibrator.joblib, metadata.json}, models/20260620-1a-postreview-v2-cb-v1/{cb_model.cbm, calibrator.joblib, metadata.json}, prediction.fukusho_prediction（両モデル予測行）
</output>
