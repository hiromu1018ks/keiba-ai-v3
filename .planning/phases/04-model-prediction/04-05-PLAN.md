---
phase: 04-model-prediction
plan: 05
type: execute
wave: 4
depends_on: [04-03, 04-04]
files_modified:
  - scripts/run_train_predict.py
  - src/model/orchestrator.py
  - tests/model/test_orchestrator.py
autonomous: true
requirements: [MODL-01, MODL-02, MODL-03]
tags: [phase-04, integration, run-train-predict, sc4-reproduce, both-models, end-to-end]

must_haves:
  truths:
    - "scripts/run_train_predict.py が LightGBM と CatBoost の両方を学習し calibrate_model でキャリブレーションし predict_p_fukusho で予測し load_predictions で prediction.fukusho_prediction に書込む（per D-03・両モデル候補として並列）"
    - "train_and_predict orchestrator が src/model/orchestrator.py に配置され（src/model/calibrator.py でなく・review HIGH#12: 循環依存回避・純粋 utility と orchestration の分離）・trainer + calibrate_model + predict_p_fukusho を統合する"
    - "train_and_predict が単一の index 付き modeling frame を split/feature 選択/予測/出力の全段で運び・全 X/y/race_df merge の直前に index equality を assert する（review HIGH#2: sort/split/merge/predict 後の silent wrong-horse prediction 防止）"
    - "CatBoost 予測パスが Pool sort 後に trainer.align_predictions を呼び元の行順序を復元する（review HIGH#2 + HIGH#12: 学習時 sort は問題ないが予測時は sort しないか元 index を復元）"
    - "両モデルの artifact が models/20260620-1a-postreview-v2-lgb-v1/ と models/20260620-1a-postreview-v2-cb-v1/ に保存される・CalibratedClassifierCV を base ネイティブ（lgb_model.txt/cb_model.cbm）+ calibrator.joblib + metadata.json として別々に保存（per D-06/D-10・review HIGH#5: PLAN 02 artifact.py の分離保存を消費）"
    - "evaluator.evaluate_all_models が reports/04-eval.md と reports/04-eval.json に LightGBM/CatBoost + BL-1..5 の比較表を出力する（per SC#2）"
    - "SC#4 reproduce smoke（固定 seed=42 で2回 train_and_predict → np.array_equal(pred1, pred2)）が GREEN になる・固定 thread count（LightGBM num_threads=1・CatBoost thread_count=1）・固定 as_of_datetime（reproduce モードでは定数）・as_of_datetime が予測値 hash から除外される（per D-06・SC#4 構造的ブロック・review HIGH#7）"
    - "prediction.fukusho_prediction に両モデル（model_type='lightgbm' と 'catboost'）の予測が provenance 列付きで永続化される・load_predictions は1 model_type+model_version 単位で呼ばれ他 model_version の行を破壊しない（per D-05・§19.1・review HIGH#1/Cross-Plan #3）"
    - "run_train_predict の --snapshot-id 引数が実際に manifest/Parquet path/category map path/期待 hash へ解決される（review MEDIUM: 単なる provenance 文字列でなく実パス選択）"
  prohibitions:
    - "train_and_predict が src/model/calibrator.py に置かれ循環依存を生むことは許容しない（review HIGH#12）"
    - "sort/split/merge 後に予測と元の行が misalign してもテストが GREEN のままになることは許容しない（review HIGH#2）"
    - "CatBoost 予測が sort 済み Pool 順序のままで元の行に戻らないことは許容しない（review HIGH#2 + HIGH#12）"
    - "SC#4 reproduce smoke がデフォルト thread count・揮発性 as_of_datetime で非 bit-identical になることは許容しない（review HIGH#7）"
    - "artifact save が CalibratedClassifierCV を直接 .txt/.cbm で保存し失敗または誤オブジェクト保存することは許容しない（review HIGH#5）"
    - "--snapshot-id が実パス解決せず provenance 文字列に過ぎないことは許容しない（review MEDIUM）"
  artifacts:
    - path: "scripts/run_train_predict.py"
      provides: "Phase 4 エントリポイント（学習→キャリブレーション→予測→書込→評価・両モデル・--model-type {lightgbm,catboost,both}）"
      contains: "run_train_predict"
      min_lines: 100
    - path: "src/model/orchestrator.py"
      provides: "train_and_predict orchestrator（trainer + calibrate_model + predict_p_fukusho 統合・行整列保証・reproduce smoke 用・review HIGH#12 で calibrator.py から分離）"
      contains: "train_and_predict"
  key_links:
    - from: "scripts/run_train_predict.py"
      to: "src/model/orchestrator.py"
      via: "train_and_predict 呼出（review HIGH#12: calibrator.py でなく orchestrator.py）"
      pattern: "from src.model.orchestrator import train_and_predict"
    - from: "scripts/run_train_predict.py"
      to: "src/model/baseline.py"
      via: "compute_all_baselines 呼出"
      pattern: "compute_all_baselines"
    - from: "scripts/run_train_predict.py"
      to: "src/db/prediction_load.py"
      via: "load_predictions を各 model_type+model_version 単位で呼ぶ（review HIGH#1）"
      pattern: "load_predictions"
    - from: "src/model/orchestrator.py"
      to: "src/model/trainer.py + calibrator.py + predict.py"
      via: "train_and_predict orchestrator・一方向 import で循環依存回避（review HIGH#12）"
      pattern: "from src.model.trainer import"
    - from: "src/model/orchestrator.py"
      to: "src/model/trainer.align_predictions"
      via: "CatBoost 予測パスで元順序復元（review HIGH#2 + HIGH#12）"
      pattern: "align_predictions"
---

<objective>
Phase 4 のエンドツーエンド統合を実装: (a) scripts/run_train_predict.py が data → trainer（LightGBM + CatBoost 両方）→ calibrate_model → predict_p_fukusho → load_predictions → evaluator.evaluate_all_models の全 pipeline を実行する（per D-03 両モデル候補・Phase 5 で両方 backtest）、(b) **review HIGH#12 対応: train_and_predict orchestrator を src/model/calibrator.py でなく src/model/orchestrator.py に新設**（calibrator.py は純粋 utility のまま・data/trainer/predict/artifact 的関心を import する循環依存を回避）し trainer + calibrate_model + predict_p_fukusho を統合（reproduce smoke test が消費）、**review HIGH#2 対応: 単一 index 付き modeling frame を全段で運び X/y/race_df merge の直前に index equality を assert・CatBoost 予測パスが trainer.align_predictions を呼び元順序復元**、(c) **review HIGH#7 対応: SC#4 reproduce smoke test（seed=42 で2回 train_and_predict → np.array_equal）が固定 thread count（num_threads=1/thread_count=1）と固定 as_of_datetime で bit-identical になるよう GREEN 化**（per D-06・構造的ブロック・失敗時 Phase 完了不可）。reports/04-eval.md + .json に比較表を出力し prediction.fukusho_prediction に両モデル予測を永続化（**review HIGH#1: load_predictions は1 model_type+model_version 単位で呼ぶ**）。

Purpose: Phase 4 の成果物（両モデル予測 + BL 比較表 + リーク防止実証）を単一エントリポイントで完結させる。SC#4 reproduce smoke が GREEN で固定 seed の bit-identical 再現性を実証（§19.1 聖域）。review で指摘された「train_and_predict が calibrator.py にあり循環依存」「sort 後の silent wrong-horse prediction」「デフォルト thread/揮発性 timestamp で bit-identical 失敗」「CalibratedClassifierCV を直接 native save して失敗」を実装前の契約として防止する。
Output: scripts/run_train_predict.py + src/model/orchestrator.py（train_and_predict）+ SC#4 reproduce smoke GREEN + reports/04-eval.md + .json + prediction.fukusho_prediction 両モデル予測
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-model-prediction/04-CONTEXT.md
@.planning/phases/04-model-prediction/04-RESEARCH.md
@.planning/phases/04-model-prediction/04-PATTERNS.md
@.planning/phases/04-model-prediction/04-VALIDATION.md

@src/model/data.py
@src/model/trainer.py
@src/model/calibrator.py
@src/model/baseline.py
@src/model/predict.py
@src/model/evaluator.py
@src/model/artifact.py
@src/db/prediction_load.py
@src/db/connection.py
@src/config/settings.py
@scripts/run_feature_build.py
@scripts/run_label_etl.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/model/orchestrator.py 新設 — train_and_predict orchestrator + 行整列 + bit-identical 保証（review HIGH#2/#7/#12）</name>
  <files>src/model/orchestrator.py, tests/model/test_orchestrator.py</files>
  <behavior>
    - Test test_train_and_predict_row_alignment: train_and_predict が返す pred_df の index が入力 feature_df の該当行 index と完全一致し・誤馬予測がない（review HIGH#2）・CatBoost の場合 sort 後に align_predictions で元順序復元
    - Test test_reproduce_bit_identical（強化）: train_and_predict(model_type, feature_df, *, seed=42, as_of_datetime=FIXED) を2回呼出し・戻り prediction DataFrame の p_fukusho_hit 列が np.array_equal（bit-identical）・固定 thread count（LightGBM num_threads=1・CatBoost thread_count=1）・固定 as_of_datetime（review HIGH#7）・calibrated estimator も再現性あり・3way split が安定
    - Test test_no_circular_import: src/model/orchestrator.py を import しても src/model/calibrator.py との循環依存が起きない（review HIGH#12: orchestrator は calibrator/trainer/data/predict/artifact を一方向に import するのみ）
  </behavior>
  <read_first>
    - src/model/calibrator.py（PLAN 02: calibrate_model・CalibrationResult・純粋 utility のまま維持）
    - src/model/data.py（PLAN 02: prepare_model_matrix / split_3way / load_frozen_maps / make_race_key / FEATURE_COLUMNS）
    - src/model/trainer.py（PLAN 03: train_lightgbm / train_catboost / _split_train_eval_tail / assert_eval_disjoint / align_predictions・review HIGH#2）
    - src/model/predict.py（PLAN 04: predict_p_fukusho / make_model_version・review HIGH#4 D-10 形式）
    - src/model/artifact.py（PLAN 02: save_native_artifact・review HIGH#5 base+calibrator 分離）
    - tests/model/test_calibrator.py（PLAN 02: test_reproduce_bit_identical は lightgbm 小モデルで代用・本 task で orchestrator 版に強化しつつ PLAN 02 の lightgbm 代用版は残置）
    - .planning/phases/04-model-prediction/04-RESEARCH.md D-06「SC#4 reproduce smoke test の設計」（固定 seed リスト・np.array_equal 比較・thread count 固定）・Code Examples SC#4 Reproduce Smoke Test
    - tests/features/test_snapshot_repro.py:40-52（2回呼出 hash 比較パターン）
  </read_first>
  <action>
    1. 【src/model/orchestrator.py 新設 — review HIGH#12: calibrator.py でなく独立モジュール】モジュール docstring + from __future__ import annotations。本モジュールは orchestration 層であり・src.model.data / src.model.trainer / src.model.calibrator / src.model.predict / src.model.artifact を一方向に import する（逆方向の import なし・循環依存回避）。SC#4/D-06/review HIGH#2/#7/#12 を明記。
    2. 【train_and_predict(feature_df, *, model_type, feature_snapshot_id, version_n=1, seed=42, eval_fraction=0.2, params_override=None, as_of_datetime=None) -> dict — review HIGH#2 行整列 + review HIGH#7 bit-identical】orchestrator:
       - **index 保持の建模 frame 構築（review HIGH#2）**: feature_df から prepare_model_matrix で (X, y) を抽出する際・元 feature_df の index を保持した modeling_frame を構築（make_X_y が index を保持する前提・PLAN 02 で保証）。splits = split_3way(feature_df) で train/calib/test に分割した際も各 split の index は元 feature_df index の部分集合。
       - **index equality assert（review HIGH#2）**: 全 X_train/X_calib/X_test と y_train/y_calib/y_test と race_df の merge の直前に `X_train.index.equals(y_train.index)` 等 index equality を assert・不一致は RuntimeError（silent wrong-horse prediction 防止）。
       - `train_core, train_tail = _split_train_eval_tail(splits["train"], eval_fraction)`（trainer.py・eval set 分離・review Cross-Plan #8: eval_max_date <= train_core_max_date）
       - model_type で分岐: "lightgbm" → `estimator = train_lightgbm(X_train_core, y_train_core, X_eval=X_train_tail, y_eval=y_train_tail, eval_race_keys=..., calib_race_keys=..., test_race_keys=..., train_core_max_date=..., eval_max_date=..., params=_merge_params(LGB_INIT_PARAMS, params_override, seed))` / "catboost" → `train_catboost(...)`（同様・catboost の場合は戻り値 estimator と sorted_index を受け取り予測パスで使用）
       - `calib_result = calibrate_model(estimator, X_calib, y_calib, race_dates_calib=splits["calib"]["race_date"], train_max_date=splits["train"]["race_date"].max())`（calibrator.py・isotonic/sigmoid 切替・純粋関数呼出）
       - `model_version = make_model_version(feature_snapshot_id, model_type, version_n)`（predict.py・D-10 review HIGH#4）
       - **予測（review HIGH#2 + HIGH#12 CatBoost 行順序復元）**: "lightgbm" の場合は `pred_proba = calib_result.calibrated.predict_proba(X_test)[:,1]` で予測（X_test index は元のまま）・"catboost" の場合は `pred_proba_sorted = calib_result.calibrated.predict_proba(X_test_cb_pool)[:,1]`（sort 済み Pool）→ `pred_proba = align_predictions(pd.Series(pred_proba_sorted, index=sorted_index), sorted_index, X_test.index)` で元順序復元・両方とも pred_proba の index が X_test.index と完全一致することを assert。
       - `pred_df_test = predict_p_fukusho(calib_result.calibrated, X_test, model_type=..., model_version=..., feature_snapshot_id=..., calib_method=calib_result.calib_method, race_df=splits["test"], split_label="test", as_of_datetime=as_of_datetime)`（predict.py・review MEDIUM: as_of_datetime 制御）
       - 戻り値 dict = {"estimator": estimator, "calibrated": calib_result.calibrated, "calib_method": calib_result.calib_method, "pred_df": pred_df_test, "model_version": model_version, "splits": splits}
    3. 【_merge_params(default_params, override, seed) helper】default_params に override を deep merge し seed/random_seed/num_threads/thread_count を強制固定（review HIGH#7: thread count も seed も上書き可能だがデフォルトは 1/42）。
    4. 【_assert_deterministic(model_type, feature_df, *, as_of_datetime=FIXED_REPRODUCE_TS)】train_and_predict を2回呼出し np.array_equal(pred1["pred_df"]["p_fukusho_hit"].values, pred2["pred_df"]["p_fukusho_hit"].values) を assert・違反で RuntimeError（非決定論的要素あり）。as_of_datetime を固定値（例: datetime(2026,6,20,tzinfo=utc)）で渡すことで揮発性 timestamp が hash に混入しない（review HIGH#7）。内部ヘルパー・test と run_train_predict の --check-reproduce が使用。
    5. 【Test GREEN 化】tests/model/test_orchestrator.py を新設し以下を実装（test_calibrator.py の test_reproduce_bit_identical は PLAN 02 lightgbm 代用版を残置・orchestrator 版は test_orchestrator.py に新設）:
       - test_train_and_predict_row_alignment（review HIGH#2）: 合成 feature_df で train_and_predict(model_type="catboost") を呼出・戻り pred_df の index が splits["test"] の index と完全一致を assert・CatBoost の場合入力をシャッフルしても元順序に復元されることを assert。
       - test_reproduce_bit_identical（review HIGH#7 強化）: 合成 feature_df（race_key 時系列・categorical 含む・HIGH_CARD_CODE_COLS 含む・n=1000 程度）で train_and_predict(model_type="lightgbm", as_of_datetime=FIXED) を seed=42 で2回呼出・np.array_equal(pred1, pred2) を assert・model_type="catboost" でも同様（has_time=True + thread_count=1 で決定論的）。
       - test_no_circular_import（review HIGH#12）: `import src.model.orchestrator` と `import src.model.calibrator` の両方が成功し・sys.modules に循環参照がないことを assert（orchestrator が calibrator を import し・calibrator が orchestrator を import しない）。
  </action>
  <verify>
    <automated>uv run pytest tests/model/test_orchestrator.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/model/test_orchestrator.py の3 test（test_train_and_predict_row_alignment / test_reproduce_bit_identical / test_no_circular_import）が GREEN
    - src/model/orchestrator.py が存在し・`def train_and_predict` orchestrator がある・src.model.calibrator / data / trainer / predict / artifact を一方向 import する（review HIGH#12: 循環依存なし）
    - src/model/calibrator.py に train_and_predict が**無い**（review HIGH#12: orchestrator は orchestrator.py のみ）・`grep -c 'def train_and_predict' src/model/calibrator.py` == 0
    - train_and_predict が X/y/race_df の全 merge 直前に index equality を assert する（review HIGH#2）
    - CatBoost 予測パスが trainer.align_predictions を呼び元順序復元する（review HIGH#2 + HIGH#12）
    - np.array_equal で pred1 と pred2 が bit-identical を assert（SC#4・§19.1）・固定 thread count（num_threads=1/thread_count=1）・固定 as_of_datetime（review HIGH#7）
    - train_and_predict が seed 引数と as_of_datetime 引数を持つ・reproduce モードでは as_of_datetime を固定値で渡す（review HIGH#7）
    - catboost 呼出で has_time=True が維持（train_catboost 内部で保証・orchestrator では上書きしない）
  </acceptance_criteria>
  <done>src/model/orchestrator.py の train_and_predict が trainer + calibrate_model + predict_p_fukusho を統合し・行整列保証・固定 thread/as_of_datetime で bit-identical・循環依存なし（SC#4/D-06・review HIGH#2/#7/#12 達成）</done>
</task>

<task type="auto">
  <name>Task 2: scripts/run_train_predict.py — エンドツーエンド pipeline エントリポイント</name>
  <files>scripts/run_train_predict.py</files>
  <read_first>
    - scripts/run_feature_build.py（構造が最も近い: sys.path 挿入:38-41 / argparse:65-103 / pool 構築 / try-except-finally / masked DSN ログ:117）
    - scripts/run_label_etl.py（logging.basicConfig:39-43 / masked DSN:50-51 / make_pool role='readonly'+role='etl':53-54 / try-except PsycopgError / finally pool.close:56-127 / idempotent verify 2回実行:90-99）
    - src/model/calibrator.py（PLAN 05 Task 1: train_and_predict）
    - src/model/baseline.py（PLAN 03: compute_all_baselines / fetch_market_data）
    - src/model/predict.py（PLAN 04: predict_p_fukusho / make_model_version）
    - src/model/evaluator.py（PLAN 04: evaluate_all_models）
    - src/model/artifact.py（PLAN 02: save_native_artifact）
    - src/db/prediction_load.py（PLAN 04: load_predictions）
    - src/db/connection.py（make_pool / readonly_cursor / write_cursor）
    - src/config/settings.py（dsn_masked / etl_dsn_masked / db_reader_role）
    - .planning/phases/04-model-prediction/04-PATTERNS.md「scripts/run_train_predict.py」セクション・Shared Patterns 4/8
  </read_first>
  <action>
    1. 【モジュール docstring + from __future__ import annotations】Phase 4 エントリポイント・D-01/D-03/D-05/D-06 を明記。`uv run python scripts/run_train_predict.py [--snapshot-id 20260620-1a-postreview-v2] [--model-type {lightgbm,catboost,both}] [--version-n 1] [--check-reproduce]`。
    2. 【sys.path 挿入】run_feature_build.py:38-41 パターンで repo-root を sys.path へ。
    3. 【logging.basicConfig】run_label_etl.py:39-43 と同一フォーマット。
    4. 【masked DSN ログ】`logger.info("readonly DSN: %s", settings.dsn_masked)` と `logger.info("etl DSN: %s", settings.etl_dsn_masked)`（生 DSN 絶対出力禁止・Shared Pattern 8・MEDIUM #1）。
    5. 【parse_args — review MEDIUM: --snapshot-id は実パス解決】argparse で: --snapshot-id（デフォルト "20260620-1a-postreview-v2"・D-01）・--model-type {lightgbm,catboost,both}（デフォルト both・D-03）・--version-n（デフォルト 1・D-10）・--check-reproduce（SC#4 smoke を実行・固定 as_of_datetime で bit-identical 検証）・--no-write-db（prediction 書込を skip・dry run）・--as-of-datetime（予測 provenance の as_of_datetime を固定値で指定・未指定時は now()・reproduce では固定必須）。
    6. 【--snapshot-id 実パス解決 — review MEDIUM】args.snapshot_id から `f"snapshots/feature_matrix_{snapshot_id}.parquet"` / `f"snapshots/feature_matrix_{snapshot_id}.manifest.yaml"` / `f"snapshots/category_map_{snapshot_id}.json"` の3パスを構築し存在確認・存在しない snapshot_id は FileNotFoundError（単なる provenance 文字列でなく実パス選択・review MEDIUM）。data.py.load_feature_matrix は SNAPSHOT_PATH 定数を使うが・run_train_predict は args.snapshot_id と data.py.SNAPSHOT_PATH が一致することを assert（ドリフト検出）・一致しない場合は sys.exit(1)。
    7. 【pool 構築】`readonly_pool = make_pool(settings, role="readonly")`（feature/label/odds/ninki SELECT・run_label_etl.py:53 と同一）+ `etl_pool = make_pool(settings, role="etl")`（prediction 書込・search_path 拡張済み）。
    8. 【データ準備】`feature_df = load_feature_matrix()`（data.py・SNAPSHOT_PATH デフォルト = D-01）→ `readonly_cur` で label 取得（load_labels）+ build_training_frame で join・BL 市場データ取得（fetch_market_data for BL-2/BL-3）。
    9. 【両モデル学習 — review HIGH#2: orchestrator が行整列保証】args.model_type でループ: model_type in ["lightgbm","catboost"]（both の場合両方）: `result = train_and_predict(feature_df, model_type=model_type, feature_snapshot_id=args.snapshot_id, version_n=args.version_n, seed=42, as_of_datetime=args.as_of_datetime)`（orchestrator.py・review HIGH#12: calibrator.py でなく orchestrator.py・eval set 分離済み・行整列 assert 済み）。各モデルの calibrated estimator と pred_df を収集。
    10. 【artifact 保存 — review HIGH#5: base+calibrator 分離】`save_native_artifact(calibrated_estimator=result["calibrated"], base_model_type=model_type, model_version=result["model_version"], feature_snapshot_id=args.snapshot_id, hyperparams=..., seed=42, train_calib_test_periods=...)`（artifact.py・D-06・PLAN 02 review HIGH#5 で base を lgb_model.txt/cb_model.cbm・calibrator を calibrator.joblib に分離保存・.gitignore 対象）。各モデルで実行。**model_version は make_model_version で生成され D-10 例と完全一致**（review HIGH#4）。
    11. 【BL 計算】`baseline_df = compute_all_baselines(test_df, X_test_bl4_bl5=..., y_train=..., X_train_bl4_bl5=..., readonly_cur=readonly_cur, calib_slice=splits["calib"])`（baseline.py・BL-1..5・市場データは readonly SELECT・calib_slice 渡すで BL-4/5 キャリブレーション or 注記・review MEDIUM）。
    12. 【prediction 書込 — review HIGH#1: 1 model_type+model_version 単位で呼ぶ】args.no_write_db でなければ: **両モデルの pred_df を統合せず各 model_type ごとに `load_predictions(write_cur, pred_df_single_model, reader_role=settings.db_reader_role)` を呼ぶ**（review HIGH#1: load_predictions は単一 model_type+model_version を前提・統合 DataFrame で渡すと ValueError・各モデル別呼出で他 model の行を破壊しない）。
    13. 【評価レポート】`metrics = evaluate_all_models({"lightgbm": lgb_pred, "catboost": cb_pred, "bl1": ..., ..., "bl5": ...}, y_true_by_split={"test": y_test}, out_md_path="reports/04-eval.md", out_json_path="reports/04-eval.json")`（evaluator.py・SC#2 比較表・§15.2 sum(p) 診断検査・review MEDIUM/LOW）。
    14. 【SC#4 --check-reproduce — review HIGH#7: 固定 thread/as_of_datetime】args.check_reproduce の場合: 各 model_type で train_and_predict を seed=42・as_of_datetime=FIXED_REPRODUCE_TS（orchestrator._assert_deterministic 内部定数）で2回呼出し np.array_equal を assert（orchestrator.py の _assert_deterministic を使用）・失敗で sys.exit(1)（構造的ブロック・SC#4）。
    15. 【try / except PsycopgError / finally pool.close】run_label_etl.py:56-127 と同一構造。PsycopgError を catch して logger.error・finally で readonly_pool.close() / etl_pool.close()。
    16. 【idempotent verify】prediction 書込後 各 model_type の load_predictions をもう1回実行し checksum が前回と一致することを logger.info で報告（run_label_etl.py:90-99 の2回実行パターン）。
    17. 【終了サマリ】logger.info で: 両モデル model_version（D-10 形式確認）/ artifact path / prediction row count（model_type 毎）/ checksum（model_type 毎）/ reports/04-eval.md + .json path / SC#4 reproduce 結果 を出力。
  </action>
  <verify>
    <automated>uv run python scripts/run_train_predict.py --help | grep -E "snapshot-id|model-type|check-reproduce|as-of-datetime" && echo "ARGPARSE_OK"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/run_train_predict.py が --snapshot-id / --model-type / --version-n / --check-reproduce / --no-write-db / --as-of-datetime の6 argparse を持つ
    - デフォルト --snapshot-id が "20260620-1a-postreview-v2"（D-01）
    - デフォルト --model-type が "both"（D-03・両モデル候補）
    - --snapshot-id が実パス（snapshots/feature_matrix_{id}.parquet / .manifest.yaml / category_map_{id}.json）に解決される・存在しない snapshot_id は FileNotFoundError（review MEDIUM）
    - args.snapshot_id と data.py.SNAPSHOT_PATH の snapshot_id 部分が一致することを assert（ドリフト検出）
    - scripts/run_train_predict.py --help が exit 0
    - sys.path 挿入 / logging.basicConfig / masked DSN ログ（dsn_masked/etl_dsn_masked）が run_label_etl.py と同一パターン
    - 生 DSN 文字列がスクリプト内に埋込まれていない（grep で password= 等が無い・settings.dsn_masked のみ使用）
    - try/except PsycopgError/finally pool.close 構造がある
    - 両モデル（lightgbm + catboost）の学習→キャリブレーション→予測→artifact 保存→prediction 書込→評価レポート の全ステップが順序立てて実装
    - **load_predictions が各 model_type+model_version 単位で呼ばれる**（統合 DataFrame でなく各モデル別・review HIGH#1）・各モデルで2回実行で checksum 報告
    - artifact 保存が base ネイティブ + calibrator.joblib 分離（review HIGH#5・save_native_artifact の PLAN 02 契約を消費）
    - model_version が make_model_version で生成され D-10 例と完全一致（review HIGH#4）
    - SC#4 --check-reproduce が固定 as_of_datetime + thread count で bit-identical 検証（review HIGH#7）
  </acceptance_criteria>
  <done>run_train_predict.py が両モデルのエンドツーエンド pipeline を単一エントリポイントで実行し・reports/04-eval.md + .json と prediction.fukusho_prediction（両モデル・model_version スコープ）と models/{version}/ artifact（base+calibrator 分離）を生成（D-01/D-03/D-05/D-06/SC#2・review HIGH#1/#4/#5/#7 達成）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI 引数 → pipeline 実行 | --snapshot-id / --model-type / --check-reproduce 等の信頼できない入力境界 |
| readonly pool → DB SELECT | feature/label/odds/ninki 読込境界 |
| etl pool → prediction.fukusho_prediction 書込 | staging-swap atomic 替換境界 |
| 固定 seed → 2回 train_and_predict → np.array_equal | SC#4 reproduce smoke の検証力を決定する境界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-26 | Tampering | CLI 引数 injection（--snapshot-id で不正パス読込） | mitigate | argparse choices/型検証・--snapshot-id を snapshots/feature_matrix_{id}.parquet 等の実パスに解決し存在確認（review MEDIUM）・SNAPSHOT_PATH は data.py 定数と一致確認・任意パス読込不可 |
| T-04-27 | Information Disclosure | スクリプトログへの生 DSN 出力 | mitigate | settings.dsn_masked / etl_dsn_masked のみ使用・生 DSN 埋込なし・Shared Pattern 8（MEDIUM #1・run_label_etl.py パターン踏襲） |
| T-04-28 | Denial of Service | pool close 忘れで DB 接続枯渇 | mitigate | try/except/finally で readonly_pool.close() / etl_pool.close() を保証（run_label_etl.py:56-127 パターン） |
| T-04-29 | Repudiation | 非決定論的要素（デフォルト thread count・揮発性 as_of_datetime・非決定論的 categorical 順序）で SC#4 reproduce 失敗を silent 無視 | mitigate | --check-reproduce で np.array_equal 違反時 sys.exit(1)（構造的ブロック・SC#4・Phase 完了不可）・固定 thread count（num_threads=1/thread_count=1）・固定 as_of_datetime（review HIGH#7） |
| T-04-30 | Tampering | prediction 書込の部分失敗・及び全テーブル置換で他 model_version の行を破壊 | mitigate | load_predictions を各 model_type+model_version 単位で呼び（review HIGH#1）・staging-swap idempotent・2回実行 checksum 報告・model_version スコープ DELETE で他 model 保持 |
| T-04-31 | Repudiation | artifact 保存の非決定論的書込・及び CalibratedClassifierCV を直接 native save して失敗 | mitigate | save_native_artifact が base ネイティブ（lgb_model.txt/cb_model.cbm）+ calibrator.joblib に分離保存（review HIGH#5）・metadata.json が sort_keys=True + atomic write・SC#4 reproduce smoke が最終保証 |
| T-04-31b | Tampering | sort/split/merge 後に予測と元の行が misalign し silent wrong-horse prediction | mitigate | orchestrator.train_and_predict が単一 index 付き modeling frame を全段で運び・全 merge 直前に index equality を assert・CatBoost 予測パスが trainer.align_predictions を呼び元順序復元（review HIGH#2 + HIGH#12） |
| T-04-31c | Tampering | train_and_predict が calibrator.py にあり循環依存で utility が非純粋化 | mitigate | train_and_predict を src/model/orchestrator.py に配置し calibrator.py は純粋 utility のまま（review HIGH#12）・test_no_circular_import で検証 |
</threat_model>

<verification>
- Wave 4 完了: `uv run pytest tests/model/test_calibrator.py::test_reproduce_bit_identical -x -q` が GREEN・`uv run python scripts/run_train_predict.py --help` が exit 0
- 実データ実行（PLAN 06 で実施）: `uv run python scripts/run_train_predict.py --check-reproduce` が両モデルで bit-identical・reports/04-eval.md 生成・prediction.fukusho_prediction 両モデル予測永続化
</verification>

<success_criteria>
- src/model/orchestrator.py の train_and_predict が trainer + calibrate_model + predict_p_fukusho を統合（review HIGH#12: calibrator.py でなく独立モジュール・循環依存なし）
- 行整列が全段で保証され CatBoost 予測パスが align_predictions で元順序復元（review HIGH#2）
- SC#4 reproduce smoke（両モデル bit-identical・固定 thread count + 固定 as_of_datetime）が GREEN（review HIGH#7・D-06・§19.1 構造的ブロック）
- run_train_predict.py が両モデル pipeline を実行し reports/04-eval.md + .json + prediction.fukusho_prediction（model_version スコープ・review HIGH#1）+ models/ artifact（base+calibrator 分離・review HIGH#5）を生成（D-01/D-03/D-05/D-06/SC#2・review HIGH#4 model_version D-10 形式）
- masked DSN ログ・try/finally pool close・idempotent verify が既存パターンと一致
</success_criteria>

<output>
Create `.planning/phases/04-model-prediction/04-05-SUMMARY.md` when done

## Artifacts this phase produces
- src/model/orchestrator.py（新設・review HIGH#12）: `train_and_predict(feature_df, *, model_type, feature_snapshot_id, version_n=1, seed=42, eval_fraction=0.2, params_override=None, as_of_datetime=None) -> dict` orchestrator, `_merge_params(default_params, override, seed)` helper, `_assert_deterministic(model_type, feature_df, *, as_of_datetime=FIXED_REPRODUCE_TS)` helper（review HIGH#7 固定 as_of_datetime）
- scripts/run_train_predict.py: Phase 4 エントリポイント・CLI flags `--snapshot-id`（デフォルト 20260620-1a-postreview-v2・review MEDIUM: 実パス解決）, `--model-type {lightgbm,catboost,both}`（デフォルト both）, `--version-n`（デフォルト 1）, `--check-reproduce`, `--no-write-db`, `--as-of-datetime`
- tests/model/test_orchestrator.py（新設）: test_train_and_predict_row_alignment / test_reproduce_bit_identical / test_no_circular_import GREEN（review HIGH#2/#7/#12）
- 実行生成物: reports/04-eval.md + reports/04-eval.json（比較表）, models/20260620-1a-postreview-v2-lgb-v1/{lgb_model.txt, calibrator.joblib, metadata.json}（review HIGH#5 base+calibrator 分離）, models/20260620-1a-postreview-v2-cb-v1/{cb_model.cbm, calibrator.joblib, metadata.json}, prediction.fukusho_prediction（両モデル予測行・model_version スコープ swap で互いに破壊しない・review HIGH#1）
</output>
