---
phase: 04-model-prediction
plan: 06
type: execute
wave: 5
depends_on: [04-05]
files_modified:
  - .planning/ROADMAP.md
  - .planning/phases/04-model-prediction/04-VALIDATION.md
autonomous: true
requirements: [MODL-01, MODL-02, MODL-03]
tags: [phase-04, verification, sc3-leak-green, sc4-reproduce-green, roadmap-update, hybrid-gate]

must_haves:
  truths:
    - "SC#3 leak diagnostic（test_no_target_encoding_leak）と SC#4 reproduce smoke（test_reproduce_bit_identical）が live データ・両モデルで GREEN になり target encoding 非混入と固定 seed bit-identical 再現性を実証する（per SC#3/SC#4 構造的ブロック・Phase 1 D-01/Phase 2 D-02 hybrid gate）"
    - "uv run python scripts/run_train_predict.py --model-type both --check-reproduce が両モデルで完了し reports/04-eval.md と prediction.fukusho_prediction（両モデル）と models/{version}/ artifact を生成する"
    - "全 test スイート（tests/ 全体）が GREEN で Phase 4 完了を宣言できる状態になる"
    - "ROADMAP.md の Phase 4 エントリが Plans リスト（01..06）と成果物で更新され、SC#1-#4 の達成が明記される"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4 Plans リスト（01..06）+ 成果物 + SC#1-#4 達成記載"
      contains: "04-06-PLAN.md"
    - path: ".planning/phases/04-model-prediction/04-VALIDATION.md"
      provides: "nyquist_compliant: true・wave_0_complete: true・全 test status GREEN"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "scripts/run_train_predict.py --check-reproduce"
      to: "SC#4 構造的ブロック"
      via: "両モデル bit-identical 実証・失敗時 sys.exit(1)"
      pattern: "check-reproduce"
    - from: "tests/model/test_trainer.py::test_no_target_encoding_leak"
      to: "SC#3 構造的ブロック"
      via: "target encoding 非混入の対抗的実証（GREEN = 構造保証）"
      pattern: "test_no_target_encoding_leak"
---

<objective>
Phase 4 の最終検証と完了宣言: (a) scripts/run_train_predict.py --model-type both --check-reproduce を実データで実行し両モデル（LightGBM/CatBoost）の学習→キャリブレーション→予測→artifact 保存→prediction.fukusho_prediction 書込→reports/04-eval.md 生成を完結させる、(b) SC#3 leak diagnostic（test_no_target_encoding_leak）と SC#4 reproduce smoke（test_reproduce_bit_identical + --check-reproduce）が両モデルで GREEN になることを構造的ブロックとして確認（hybrid gate Phase 1 D-01/Phase 2 D-02・失敗時 Phase 完了不可）、(c) 全テストスイート（tests/ 全体）が GREEN であることを確認、(d) ROADMAP.md の Phase 4 エントリを Plans リスト（01..06）と成果物で更新し SC#1-#4 達成を明記、(e) 04-VALIDATION.md の nyquist_compliant/wave_0_complete を true に更新。

Purpose: Phase 4 の成果物（両モデル予測 + BL 比較表 + リーク防止実証）が本番データで動作することを実証し、Phase 5（EV/backtest）・Phase 6（評価ゲート）・Phase 7（Streamlit）・Phase 8（adversarial audit）への引き渡しを確立する。SC#3/SC#4 は構造的ブロックであり、この PLAN が GREEN でなければ Phase 4 完了を宣言できない。
Output: 実行済み pipeline（reports/04-eval.md + prediction テーブル両モデル + models/ artifact）・GREEN 全テスト・更新済み ROADMAP/VALIDATION
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-model-prediction/04-CONTEXT.md
@.planning/phases/04-model-prediction/04-RESEARCH.md
@.planning/phases/04-model-prediction/04-VALIDATION.md

@scripts/run_train_predict.py
@src/model/calibrator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 実データで run_train_predict.py --model-type both --check-reproduce を実行し SC#3/SC#4 構造的ブロック GREEN を確認</name>
  <files>.planning/phases/04-model-prediction/04-VALIDATION.md</files>
  <read_first>
    - scripts/run_train_predict.py（PLAN 05: --check-reproduce / --model-type both の実行方法）
    - .planning/phases/04-model-prediction/04-VALIDATION.md（Per-Task Verification Map・13 test の status 更新対象）
    - .planning/phases/04-model-prediction/04-RESEARCH.md D-05（SC#3 leak diagnostic）・D-06（SC#4 reproduce smoke）・SC#3/SC#4 構造的ブロックの定義
    - .planning/STATE.md（Phase 4 status 更新対象・PLAN 01 で postreview-v2 ドリフト修正済み）
  </read_first>
  <action>
    1. 【前提確認】Wave 1-4 で全 PLAN が完了していること: 依存ライブラリ pin・prediction DDL/GRANT・tests/model/ RED→GREEN・data/trainer/calibrator/baseline/predict/evaluator/prediction_load/artifact・run_train_predict.py。`uv run pytest tests/model/ -x -q` が全 GREEN（13+ test）であることを確認。
    2. 【SC#3 leak diagnostic GREEN 確認】`uv run pytest tests/model/test_trainer.py::test_no_target_encoding_leak -x -v` を実行。合成希少カテゴリ RARE_X で LightGBM + CatBoost の両方の予測が mean（0.21 程度）に縮み `< 0.5` を満たすこと（target encoding 混入なら 1.0 近くで失敗するはず）。GREEN = target encoding 非混入の構造実証。失敗時は Phase 4 完了不可（構造的ブロック）・原因調査は /gsd-debug で別途。
    3. 【SC#4 reproduce smoke GREEN 確認（unit）】`uv run pytest tests/model/test_calibrator.py::test_reproduce_bit_identical -x -v` を実行。両モデルで seed=42 の2回 train_and_predict が np.array_equal で bit-identical。GREEN = 固定 seed 再現性の構造実証。
    4. 【実データ pipeline 実行】`uv run python scripts/run_train_predict.py --model-type both --check-reproduce` を実行（D-01 snapshot_id デフォルト = 20260620-1a-postreview-v2・--version-n 1）。完了条件:
       - 両モデル（lightgbm + catboost）の学習が完了（LightGBM .txt / CatBoost .cbm 保存）
       - calibrate_model で isotonic（calib sample 24,884 > 1000）適用・calib_method が予測 provenance に記録
       - predict_p_fukusho で test slice（2024-H2）の p_fukusho_hit 算出・provenance 列 NOT NULL
       - artifact 保存: models/20260620-1a-postreview-v2-lgb-v1/{lgb_model.txt, calibrator.joblib, metadata.json} と models/20260620-1a-postreview-v2-cb-v1/{cb_model.cbm, calibrator.joblib, metadata.json}
       - load_predictions で prediction.fukusho_prediction に両モデル予測を永続化（staging-swap idempotent・2回実行で checksum 一致をログ確認）
       - evaluate_all_models で reports/04-eval.md 生成（LightGBM/CatBoost + BL-1..5 比較表・§15.2 sum(p) 検査結果・BL-3 §14.2 注記・D-04 Calibration 重視選定基準素材）
       - --check-reproduce で両モデル bit-identical 実証（SC#4 構造的ブロック）・失敗で sys.exit(1)
    5. 【出力検証】reports/04-eval.md が存在し比較表が揃う・`psql -c "SELECT model_type, count(*) FROM prediction.fukusho_prediction GROUP BY model_type"` で lightgbm/catboost 両方の行が存在・`ls models/20260620-1a-postreview-v2-lgb-v1/ models/20260620-1a-postreview-v2-cb-v1/` でネイティブ artifact が揃う。
    6. 【§15.2 sum(p) 受入基準確認】reports/04-eval.md の sum(p) 検査結果を確認: 8頭以上は [2.7,3.3]・5-7頭は [1.8,2.2]。違反 race_id があれば割合と共に記録（Phase 6 ゲートで最終判定・本 PLAN では warning・hybrid gate D-01/D-02 準拠）。
    7. 【全テストスイート GREEN 確認】`uv run pytest` を実行（tests/ 全体・24 既存 + tests/model/ 新設）。全 GREEN であることを確認・回帰なし。実行時間 < 120秒（VALIDATION.md sampling rate）。
    8. 【04-VALIDATION.md 更新】Per-Task Verification Map の13 test の Status 列を GREEN（✅）に更新・Task ID 列に PLAN/Task 番号を埋込（PLAN 後割当の解消）。frontmatter の `nyquist_compliant: false` → `true`・`wave_0_complete: false` → `true`・`status: draft` → `approved`。Validation Sign-Off の6チェック項目に全てチェック。
  </action>
  <verify>
    <automated>uv run python scripts/run_train_predict.py --model-type both --check-reproduce 2>&1 | tail -20 && uv run pytest tests/model/ -q 2>&1 | tail -3 && test -f reports/04-eval.md && psql -c "SELECT model_type, count(*) FROM prediction.fukusho_prediction GROUP BY model_type"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/model/test_trainer.py::test_no_target_encoding_leak` が GREEN（SC#3 構造的ブロック・target encoding 非混入実証）
    - `uv run pytest tests/model/test_calibrator.py::test_reproduce_bit_identical` が GREEN（SC#4 構造的ブロック・bit-identical 実証）
    - `uv run python scripts/run_train_predict.py --model-type both --check-reproduce` が exit 0（両モデル pipeline 完了・SC#4 構造的ブロック GREEN）
    - reports/04-eval.md が存在し LightGBM/CatBoost/BL-1..5 の比較表 + sum(p) 検査結果を含む
    - `psql -c "SELECT model_type, count(*) FROM prediction.fukusho_prediction GROUP BY model_type"` が lightgbm と catboost の両方の行数を返す
    - models/20260620-1a-postreview-v2-lgb-v1/ と models/20260620-1a-postreview-v2-cb-v1/ に lgb_model.txt/cb_model.cbm + calibrator.joblib + metadata.json が存在
    - `uv run pytest` が全 GREEN（tests/ 全体・回帰なし）
    - 04-VALIDATION.md の frontmatter が `nyquist_compliant: true` / `wave_0_complete: true` / `status: approved`
    - 04-VALIDATION.md の Per-Task Map の13 test が全て ✅ GREEN・Task ID 列が埋込済み
  </acceptance_criteria>
  <done>実データ pipeline が両モデルで完了・SC#3/SC#4 構造的ブロックが GREEN・reports/04-eval.md + prediction テーブル + artifact が生成・全テスト GREEN・VALIDATION 更新（SC#3/SC#4 構造的ブロック達成・Phase 4 完了条件充足）</done>
</task>

<task type="auto">
  <name>Task 2: ROADMAP.md Phase 4 エントリを Plans リスト + 成果物 + SC#1-#4 達成で更新</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md（Phase 4 エントリ現状: Goal/Depends on/Requirements/Success Criteria#1-#4/Plans: TBD）
    - .planning/phases/04-model-prediction/04-01..06-PLAN.md（各 PLAN の objective / must_haves / Artifacts this phase produces）
    - .planning/phases/04-model-prediction/04-CONTEXT.md（D-01..D-08 locked decisions・Phase 4 境界）
    - .planning/STATE.md（Phase 4 status 更新対象）
  </read_first>
  <action>
    1. 【Plans 行更新】ROADMAP.md の Phase 4 エントリの `**Plans**: TBD` を `**Plans**: 6 plans` に更新（Edit scoped replacement・全ファイル Write 不可）。
    2. 【Plan リスト追加】Phase 4 エントリの Plans セクションに以下のリストを追加（未チェック → 全 PLAN 完了後は checked にする・本 PLAN 完了時点で checked）:
       ```
       Plans:
       - [x] 04-01-PLAN.md — 基盤（lightgbm/catboost pin + prediction DDL/GRANT + RED stubs + v3 ドリフト修正）
       - [x] 04-02-PLAN.md — data.py + calibrator.py + artifact.py（SC#1 stamped Parquet・3way split・prefit wrapper）
       - [x] 04-03-PLAN.md — trainer.py + baseline.py（SC#3 leak diagnostic・LightGBM native + CatBoost has_time・BL-1..5）
       - [x] 04-04-PLAN.md — predict.py + prediction_load.py + evaluator.py（provenance・staging-swap・比較表）
       - [x] 04-05-PLAN.md — run_train_predict.py + SC#4 reproduce smoke（両モデル統合・bit-identical）
       - [x] 04-06-PLAN.md — SC#3/SC#4 構造的ブロック GREEN + ROADMAP 更新（Phase 4 完了宣言）
       ```
    3. 【SC#1-#4 達成記載】Success Criteria#1-#4 の直後に各 SC の達成根拠（PLAN 番号 + test 名 + 生成物）を1行ずつ追記:
       - SC#1（stamped Parquet のみ学習）→ "Achieved: PLAN 01-02・test_load_from_parquet_only/test_raw_ids_excluded/test_no_banned_features GREEN・models/{version}/ artifact 生成"
       - SC#2（BL-1..5 比較表）→ "Achieved: PLAN 03-05・test_baseline.py 6 test GREEN・reports/04-eval.md 生成"
       - SC#3（リークセーフ カテゴリ/欠損）→ "Achieved: PLAN 03・test_no_target_encoding_leak GREEN（target encoding 非混入の構造実証）・test_lightgbm_nonneg_codes/test_catboost_has_time GREEN"
       - SC#4（prefit isotonic + reproduce smoke）→ "Achieved: PLAN 02/05・test_strict_later_disjoint/test_reproduce_bit_identical GREEN・run_train_predict --check-reproduce 両モデル bit-identical"
    4. 【D-01..D-08 反応記載】Phase 4 エントリ末尾に locked decisions の反応を1行集約: "Locked decisions D-01..D-08 全て実装（snapshot postreview-v2・prediction DB 永続化・ネイティブ artifact・BL-1..5 全部・市場データ ninki/fukuoddslow）"
    5. 【STATE.md status 更新】`.planning/STATE.md` の `status: planning` → `status: phase-4-complete`（又は後続フェーズ移行を示す値）・current_phase_name / progress.completed_phases（4 → 5・percent 更新）・last_activity_desc に "Phase 4 complete, transitioned to Phase 5" を追記。progress.completed_plans に 6 追加（17 → 23）。Edit scoped replacement のみ。
    6. 【Phase 5 引き渡し確認】Phase 4 成果物（prediction.fukusho_prediction 両モデル・reports/04-eval.md・models/ artifact・D-04 事前登録選定基準）が Phase 5（EV/backtest）の入力として明記されていることを確認・Phase 5 エントリの Depends on が Phase 4 を指すことを確認。
  </action>
  <verify>
    <automated>grep -c '04-0[1-6]-PLAN.md' .planning/ROADMAP.md | awk '{ if ($1 >= 6) { exit 0 } else { exit 1 } }' && grep -q 'Achieved: PLAN' .planning/ROADMAP.md && grep -q 'status: phase-4-complete\|Phase 4 complete' .planning/STATE.md && echo "ROADMAP_UPDATE_OK"</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md の Phase 4 エントリが `**Plans**: 6 plans` を表示
    - ROADMAP.md に 04-01..06-PLAN.md の6行が checked ([x]) でリスト
    - ROADMAP.md に SC#1-#4 各々の "Achieved: PLAN ..." 行が存在
    - ROADMAP.md に D-01..D-08 反応の1行集約が存在
    - STATE.md の status が phase-4-complete（又は等価）・progress.completed_plans が 23（17+6）・last_activity_desc が Phase 4 complete を示す
    - Phase 5 エントリの Depends on が Phase 4 を指す
    - 全更新が Edit scoped replacement で実行（Write 全文上書きではない・Phase 5-8 エントリが破壊されていない）
  </acceptance_criteria>
  <done>ROADMAP.md と STATE.md が Phase 4 完了を反映し SC#1-#4 達成と D-01..D-08 反応が明記・Phase 5 への引き渡しが確立（Phase 4 完了宣言）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 実データ pipeline 実行 → prediction テーブル + artifact | 本番データでの副作用境界・部分失敗で破損 |
| SC#3/SC#4 GREEN 宣言 → Phase 4 完了 | 構造的ブロックの検証力を決定する境界（偽陽性 GREEN を防ぐ） |
| ROADMAP/STATE 更新 → 後続フェーズ | 文書と実態の整合を保つ境界（ドリフト再発防止） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-32 | Repudiation | SC#3/SC#4 が silent skip で GREEN 扱い | mitigate | 構造的ブロック宣言（hybrid gate Phase 1 D-01/Phase 2 D-02）・--check-reproduce 失敗で sys.exit(1)・各 test が docstring で SC#/D-XX を明記・VALIDATION Per-Task Map で追跡 |
| T-04-33 | Tampering | 実データ pipeline の部分失敗で破損した予測が Phase 5 に渡る | mitigate | load_predictions staging-swap atomic・2回実行 checksum 一致・run_train_predict の try/except/finally・reports/04-eval.md の sum(p) 検査で異常検出 |
| T-04-34 | Repudiation | ROADMAP/STATE が実態と乖離（ドリフト再発） | mitigate | Edit scoped replacement のみ（Write 全文上書き禁止）・SC 達成根拠（PLAN 番号 + test 名）を明記・D-01 ドリフトの再発防止パターン確立 |
| T-04-35 | Information Disclosure | 実行ログへの生 DSN 出力 | mitigate | run_train_predict の masked DSN ログ（settings.dsn_masked）・本 PLAN でも生 DSN を出力しない |
| T-04-36 | Tampering | D-04 事前登録選定基準を結果を見てすり替え | mitigate | reports/04-eval.md の comment 列で「Calibration 重視」を結果前に固定（PLAN 04 evaluator）・Phase 6 ゲートで最終判定時にすり替え検知 |
</threat_model>

<verification>
- Wave 5 完了 = Phase 4 完了: 
  - `uv run python scripts/run_train_predict.py --model-type both --check-reproduce` が exit 0
  - `uv run pytest` が全 GREEN（tests/ 全体）
  - reports/04-eval.md 存在・prediction.fukusho_prediction 両モデル行・models/ artifact 揃う
  - ROADMAP.md/STATE.md が Phase 4 完了を反映
  - 04-VALIDATION.md が nyquist_compliant: true
</verification>

<success_criteria>
- SC#3 leak diagnostic が GREEN（target encoding 非混入の構造実証）
- SC#4 reproduce smoke が GREEN（両モデル bit-identical・§19.1 構造的ブロック）
- run_train_predict.py --model-type both --check-reproduce が実データで完了
- reports/04-eval.md + prediction.fukusho_prediction（両モデル）+ models/ artifact が生成
- 全テストスイート GREEN（回帰なし）
- ROADMAP/STATE が Phase 4 完了 + SC#1-#4 達成 + D-01..D-08 反応で更新
- 04-VALIDATION.md が approved/nyquist_compliant: true
</success_criteria>

<output>
Create `.planning/phases/04-model-prediction/04-06-SUMMARY.md` when done

## Artifacts this phase produces
- 実行生成物（再確認）: reports/04-eval.md（LightGBM/CatBoost + BL-1..5 比較表・§15.2 sum(p) 検査・D-04 事前登録選定基準素材）, models/20260620-1a-postreview-v2-lgb-v1/{lgb_model.txt, calibrator.joblib, metadata.json}, models/20260620-1a-postreview-v2-cb-v1/{cb_model.cbm, calibrator.joblib, metadata.json}, prediction.fukusho_prediction テーブル（model_type='lightgbm' + 'catboost' の両方の行）
- .planning/phases/04-model-prediction/04-VALIDATION.md: frontmatter nyquist_compliant: true / wave_0_complete: true / status: approved・Per-Task Verification Map 13 test 全 ✅ GREEN・Task ID 埋込済み・Validation Sign-Off 6 項目チェック済み
- .planning/ROADMAP.md: Phase 4 エントリ `**Plans**: 6 plans`・6 PLAN リスト checked・SC#1-#4 各 "Achieved: PLAN ..." 根拠・D-01..D-08 反応集約
- .planning/STATE.md: status 更新（phase-4-complete 系）・progress.completed_plans=23・last_activity_desc "Phase 4 complete"
</output>
