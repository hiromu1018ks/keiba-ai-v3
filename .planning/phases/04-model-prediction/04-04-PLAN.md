---
phase: 04-model-prediction
plan: 04
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - src/model/predict.py
  - src/model/evaluator.py
  - src/db/prediction_load.py
  - tests/model/test_predict.py
  - tests/model/test_prediction_load.py
autonomous: true
requirements: [MODL-01, MODL-02]
tags: [phase-04, predict, evaluator, prediction-load, staging-swap, provenance, sc2-table]

must_haves:
  truths:
    - "src/model/predict.py が calibrated estimator の predict_proba から p_fukusho_hit を算出し provenance 列（model_type/model_version/feature_snapshot_id/as_of_datetime/calib_method）を付与する（per D-05/D-10・§19.1）"
    - "predict.py の make_model_version が D-10 例と**完全一致**する形式 `{feature_snapshot_id}-{short}-v{N}` を返す（例: feature_snapshot_id='20260620-1a-postreview-v2'・lightgbm・1 → '20260620-1a-postreview-v2-lgb-v1'）・後続 PLAN 05/06 と artifact パス・prediction provenance・テストが全てこの形式に依存（review HIGH#4/Cross-Plan #1）"
    - "src/db/prediction_load.py の staging-swap が **model_version スコープ**（同一 model_type+model_version の行のみ置換）で動作し・他の model_type/model_version の行を破壊しない（review HIGH#1/Cross-Plan #3: 全テーブル置換で LightGBM 実行後 CatBoost 実行が前者を削除する silent 履歴破壊を防止）"
    - "src/model/evaluator.py が Brier/LogLoss/Calibration/sum(p) を計算し BL-1..5 と主モデルの比較表を reports/04-eval.md と reports/04-eval.json に分離出力する（per SC#2・§15.1/§15.2・review LOW: .md と .json 分離）"
    - "evaluator が sum(p) を独立二値確率の診断（厳密合計制約でなく）として扱い・理論値チェック（8頭以上 [2.7,3.3]・5-7頭 [1.8,2.2]）を機械検査する（per §15.2・review MEDIUM: 複勝確率は払戻対象数に厳密合計しない・診断として扱う）"
    - "evaluator が calibration curve の binning 契約（bin 数・strategy・最小 bin count・単調性処理）を明示する（review MEDIUM: 再現性のため binning 固定）"
    - "prediction_load.py の checksum が as_of_datetime を含む全列で計算され ORDER BY PK で安定（review HIGH#1・PARTITION 互換）"
  prohibitions:
    - "make_model_version が '20260620-1a-postreview-v2-lgb-v1' 以外の形式（例: '20260620-1a-postreview-v2-postreview-v2-lgb-v1'・二重 postfix）を返すことは許容しない（review HIGH#4）"
    - "prediction_load.py が全テーブルを TRUNCATE/DROP して他 model_type/version の行を破壊することは許容しない（review HIGH#1/Cross-Plan #3）"
    - "evaluator が sum(p) を厳密合計制約として扱い複勝確率の独立二値性質で false alarm を出すことは許容しない（review MEDIUM）"
    - "as_of_datetime = datetime.now() が bit-identical hash に混入することは許容しない（review MEDIUM: hash scope は予測値のみ）"
  artifacts:
    - path: "src/model/predict.py"
      provides: "predict_p_fukusho（calibrated estimator → provenance DataFrame）"
      contains: "model_version"
      min_lines: 50
    - path: "src/model/evaluator.py"
      provides: "compute_metrics / build_comparison_table / evaluate_all_models"
      contains: "sum(p)"
      min_lines: 60
    - path: "src/db/prediction_load.py"
      provides: "_idempotent_load_prediction / _df_to_prediction_tuples"
      contains: "pg_advisory_xact_lock"
      min_lines: 60
    - path: "tests/model/test_predict.py"
      provides: "2 test GREEN（provenance 列 / model_version 採番）"
      contains: "test_provenance_columns"
    - path: "tests/model/test_prediction_load.py"
      provides: "1 test GREEN・requires_db（idempotent checksum match）"
      contains: "test_idempotent_checksum_match"
  key_links:
    - from: "src/model/predict.py"
      to: "src/model/calibrator.py"
      via: "CalibrationResult.calibrated.predict_proba → p_fukusho_hit + provenance"
      pattern: "predict_proba"
    - from: "src/db/prediction_load.py"
      to: "src/etl/fukusho_label.py"
      via: "_idempotent_load_label の staging-swap 9ステップを1:1 複製（対象テーブルのみ書換）"
      pattern: "pg_advisory_xact_lock"
    - from: "src/model/evaluator.py"
      to: "reports/04-eval.md"
      via: "JSON + Markdown 比較表出力（§15.1/§15.2）"
      pattern: "reports/04-eval"
---

<objective>
src/model/predict.py・src/model/evaluator.py・src/db/prediction_load.py を実装する: (a) predict.py は calibrated estimator の predict_proba から p_fukusho_hit を算出し provenance 列（model_type/model_version/feature_snapshot_id/as_of_datetime/calib_method）を付与した予測 DataFrame を構築する（per D-05/D-10・§19.1 再現性）・**review HIGH#4 対応: model_version 採番は D-10 例と完全一致 `{feature_snapshot_id}-{short}-v{N}`（例: 20260620-1a-postreview-v2-lgb-v1）**（後続 PLAN 05/06・artifact パス・prediction provenance が全てこの形式に依存）、(b) prediction_load.py は src/etl/fukusho_label.py::_idempotent_load_label の staging-swap パターンを基にするが・**review HIGH#1/Cross-Plan #3 対応: model_version スコープ置換（同一 model_type+model_version の行のみ DELETE → INSERT・他 model_type/version の行は保持）で全テーブル破壊を防止**・2回実行で checksum 一致を保証（per D-05・PATTERNS Shared Pattern 5 変形）、(c) evaluator.py は Brier/LogLoss/Calibration Curve/sum(p) を計算し BL-1..5 と主モデルの比較表を reports/04-eval.md と reports/04-eval.json に分離出力する（per SC#2・§15.1/§15.2・**review MEDIUM: sum(p) は独立二値確率の診断として扱い binning 契約を明示**）。test_predict.py / test_prediction_load.py の該当 stub を GREEN 化。

Purpose: MODL-01（provenance 付き予測）と SC#2（比較表）の完成要素を提供。prediction テーブル永続化により Phase 5/6/7 が SQL で予測を照会できる。review で指摘された「全テーブル置換で LightGBM 実行後 CatBoost 実行が前者を削除する silent 履歴破壊」「model_version 形式の D-10 例との不一致で provenance 一意性崩壊」を実装前の契約として防止する。
Output: src/model/{predict,evaluator}.py + src/db/prediction_load.py + GREEN 化された test_predict.py/test_prediction_load.py
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-model-prediction/04-CONTEXT.md
@.planning/phases/04-model-prediction/04-RESEARCH.md
@.planning/phases/04-model-prediction/04-PATTERNS.md
@.planning/phases/04-model-prediction/04-VALIDATION.md

@src/etl/fukusho_label.py
@src/db/schema.py
@src/db/connection.py
@src/model/data.py
@src/model/calibrator.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/model/predict.py — provenance 付き予測 DataFrame 構築（MODL-01/D-05/D-10）</name>
  <files>src/model/predict.py, tests/model/test_predict.py</files>
  <behavior>
    - Test test_provenance_columns: predict_p_fukusho の戻り DataFrame が model_type/model_version/feature_snapshot_id/as_of_datetime/calib_method + 7カラム PK + p_fukusho_hit + race_date + split 列を持ち・全 provenance 列が NOT NULL（NaN なし）・p_fukusho_hit が [0,1] 区間
    - Test test_model_version_numbering: model_version が "20260620-1a-lgb-v1" / "20260620-1a-cb-v1" 形式（{feature_snapshot_id}-{short}-v{N}）・feature_snapshot_id="20260620-1a-postreview-v2" と整合
  </behavior>
  <read_first>
    - src/model/calibrator.py（PLAN 02: CalibrationResult(calibrated, calib_method) NamedTuple）
    - src/model/data.py（PLAN 02: SNAPSHOT_PATH・META_KEY_COLUMNS・feature_snapshot_id 列）
    - src/db/schema.py（PLAN 01: PREDICTION_TABLE_DDL・9カラム PK・列名・型）
    - src/etl/fukusho_label.py:882-888（_RACE_KEY + umaban + kettonum の PK 7カラム）, 880-919（_LABEL_TABLE_COLUMNS の列順）
    - .planning/phases/04-model-prediction/04-RESEARCH.md D-05（prediction provenance 列）・D-10（model_version 採番方式）・Pattern「src/model/predict.py」
    - .planning/phases/04-model-prediction/04-PATTERNS.md「src/model/predict.py」セクション
    - tests/features/test_snapshot_repro.py（provenance / hash assert パターン）
    - tests/model/test_predict.py（Wave 0 RED stub 2件）
  </read_first>
  <action>
    1. 【モジュール docstring + from __future__ import annotations】SC#1/D-05/D-10/§19.1 を明記。provenance で再現性を保証する旨。
    2. 【定数】PREDICTION_COLUMNS（schema.py PREDICTION_TABLE_DDL の列順と同一）・MODEL_TYPE_TO_SHORT = {"lightgbm":"lgb","catboost":"cb","logreg":"logreg"}（D-10 短縮形）・PK_COLUMNS = ["year","jyocd","kaiji","nichiji","racenum","umaban","kettonum"]（label と同一7カラム・provenance と組み合わせて9カラム PK）。
    3. 【make_model_version(feature_snapshot_id, model_type, version_n=1) -> str — review HIGH#4: D-10 例と完全一致】D-10 形式: `f"{feature_snapshot_id}-{MODEL_TYPE_TO_SHORT[model_type]}-v{version_n}"`。例: feature_snapshot_id="20260620-1a-postreview-v2"・model_type="lightgbm"・version_n=1 → **"20260620-1a-postreview-v2-lgb-v1"**（review HIGH#4/Cross-Plan #1: 旧版の "20260620-1a-postreview-v2-postreview-v2-lgb-v1" のような二重 postfix は絶対に出力しない）。**重要: feature_snapshot_id に既に '-v2' 等の version suffix が含まれていても feature_snapshot_id 全体をそのまま prefix として使い・再度 suffix を追加しない**。バージョン bump は呼出側で管理（手動・semver 的）。unit test で D-10 例の3パターン（lightgbm-v1 / catboost-v1 / lightgbm-v2）と "postreview-v2" を含む snapshot_id で二重 suffix が起きないことを assert。
    4. 【predict_p_fukusho(calibrated_estimator, X, *, model_type, model_version, feature_snapshot_id, calib_method, race_df, split_label, as_of_datetime=None) -> pd.DataFrame — review MEDIUM: as_of_datetime 制御可能】
       - `pred_proba = calibrated_estimator.predict_proba(X)[:, 1]` を算出
       - race_df（feature matrix の meta/key 列 + label join 済み・PK 7カラム + race_date + race_start_datetime）と予測を結合
       - provenance 列を付与: model_type / model_version / feature_snapshot_id / as_of_datetime（as_of_datetime 引数が None の場合は datetime.now(timezone.utc)・引数が渡された場合はそれを使用・reproduce smoke では固定値を渡して bit-identical を保証） / calib_method（calibrator の戻り値） / split（"train"/"calib"/"test"/"holdout_2025_plus"）
       - 戻り DataFrame の列は PREDICTION_TABLE_DDL の順序と一致（prediction_load.py が INSERT するため）
       - 検証: 全 provenance 列が NOT NULL・p_fukusho_hit が [0,1]・違反で ValueError（fail-loud・Shared Pattern silent fallback 禁止）
    5. 【_assert_valid_prediction_df(df)】予測 DataFrame の不変条件を検証: provenance 列 NOT NULL・p_fukusho_hit ∈ [0,1]・PK 11カラム（model_type/model_version/feature_snapshot_id/as_of_datetime + 7カラム RACE_KEY・PLAN 01 DDL と整合）で一意（duplicated().sum()==0）・列順序が PREDICTION_COLUMNS と一致。違反で ValueError。predict_p_fukusho の末尾で呼出。
    6. 【Test GREEN 化】tests/model/test_predict.py の2 stub を実装:
       - test_provenance_columns: 合成 calibrated_estimator（sklearn LogisticRegression・PLAN 02 test と同様）と合成 X/race_df で predict_p_fukusho を呼出・戻り df.columns が PREDICTION_COLUMNS と一致・provenance 5列が NOT NULL・p_fukusho_hit が [0,1]・PK 11カラムで一意を assert。
       - test_model_version_numbering（review HIGH#4 強化）: make_model_version("20260620-1a-postreview-v2","lightgbm",1)=="20260620-1a-postreview-v2-lgb-v1"・make_model_version("20260620-1a-postreview-v2","catboost",1)=="20260620-1a-postreview-v2-cb-v1"・version_n=2 で "-lgb-v2" を assert・**feature_snapshot_id="20260620-1a-postreview-v2" で "-postreview-v2-lgb" のような二重 version suffix が現れないことを assert**（review HIGH#4）。
  </action>
  <verify>
    <automated>uv run pytest tests/model/test_predict.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/model/test_predict.py の2 test が GREEN
    - src/model/predict.py に PREDICTION_COLUMNS 定数があり schema.py PREDICTION_TABLE_DDL の列順と一致
    - src/model/predict.py に model_type/model_version/feature_snapshot_id/as_of_datetime/calib_method の5 provenance 列付与がある
    - src/model/predict.py に `_assert_valid_prediction_df` と `raise ValueError` guard がある
    - make_model_version の出力が D-10 形式（{feature_snapshot_id}-{short}-v{N}）
    - p_fukusho_hit が predict_proba[:,1] 由来・[0,1] 区間を assert
  </acceptance_criteria>
  <done>predict.py が calibrated estimator から provenance 付き予測 DataFrame を構築し・2 test が GREEN（MODL-01/D-05/D-10 達成）</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: src/db/prediction_load.py — staging-swap idempotent load（D-05・1:1 複製）</name>
  <files>src/db/prediction_load.py, tests/model/test_prediction_load.py</files>
  <behavior>
    - Test test_idempotent_checksum_match（requires_db）: load_predictions を2回連続実行し・2回目の checksum が1回目と完全一致（row count + md5 string_agg）・同一レコードの重複挿入なし
    - Test test_model_version_scoped_swap_preserves_other_models（追加・review HIGH#1）: lightgbm v1 の予測を書込んだ後・catboost v1 の予測を別途書込んでも lightgbm v1 の行が破壊されず残ることを assert・各 model_type+model_version で row count が独立・全テーブル置換で他 model を削除しない
  </behavior>
  <read_first>
    - src/etl/fukusho_label.py:945-1019（_idempotent_load_label 完全体・advisory lock/空入力拒否/CREATE staging INCLUDING ALL/TRUNCATE/executemany INSERT/SELECT count(*) verify/DROP+RENAME swap/GRANT SELECT to reader/checksum 返却）, 1048-1087（_df_to_tuples_label・_BOOL_COLS/_INT_COLS/_is_na 変換ヘルパー）, 1153-1158（md5 string_agg checksum）
    - src/db/schema.py（PLAN 01: PREDICTION_TABLE_DDL・列名・reader ロール名は settings.db_reader_role="keiba_readonly"）
    - src/db/connection.py（write_cursor / make_pool role='etl'）
    - .planning/phases/04-model-prediction/04-PATTERNS.md「src/db/prediction_load.py」セクション・Shared Pattern 5「staging-swap idempotent load」
    - tests/test_fukusho_label.py（idempotent verify・requires_db パターン）
    - tests/model/test_prediction_load.py（Wave 0 RED stub 1件・requires_db）
  </read_first>
  <action>
    1. 【モジュール docstring + from __future__ import annotations】D-05/PATTERNS Shared Pattern 5 を明記。src/etl/fukusho_label.py::_idempotent_load_label のパターンを基にするが **review HIGH#1/Cross-Plan #3 対応: 全テーブル置換（DROP+RENAME）でなく model_version スコープ置換（DELETE WHERE model_type+model_version → INSERT）を採用**（LightGBM 実行後 CatBoost 実行が前者を削除する silent 履歴破壊を防止・Phase 5 永続化契約保全）。
    2. 【_PREDICTION_TABLE = "prediction.fukusho_prediction"】対象テーブル名定数。
    3. 【_df_to_prediction_tuples(df) -> list[tuple]】df を prediction テーブルの列順（PREDICTION_COLUMNS）の tuple list に変換。fukusho_label.py:1048-1087 の _df_to_tuples_label パターンを複製・型変換: model_type/model_version/feature_snapshot_id/calib_method/split は str・as_of_datetime は timestamp・PK int 列は int・p_fukusho_hit は float・race_date は date。_is_na で NaN/None を None に正規化。空リスト検出で後段の空入力拒否が発火。
    4. 【_idempotent_load_prediction(write_cur, rows, reader_role) -> str — review HIGH#1 model_version スコープ置換】fukusho_label.py の advisory lock + staging パターンを基にするが・**全テーブル DROP+RENAME でなく model_version スコープの DELETE → INSERT を採用**:
       - Step 1 advisory lock: `SELECT pg_advisory_xact_lock(hashtext('prediction.fukusho_prediction'))`
       - Step 2 空入力拒否: `if not rows: raise RuntimeError("prediction load: empty input - silent data loss prevented")`（CR-04(a)）
       - Step 3 model_version 抽出: rows から model_type と model_version を抽出・**単一の (model_type, model_version) のみ含むことを assert**（複数混在は ValueError・呼出側で1 model_version 単位で呼ぶ前提）・feature_snapshot_id と as_of_datetime も同様に単一であることを assert（review HIGH#1: model_version スコープの明確化）
       - Step 4 CREATE staging（COMMENT/INDEX/制約保全のため INCLUDING ALL で staging を作成し・本テーブルは破壊しない）: `CREATE TABLE IF NOT EXISTS prediction.fukusho_prediction_staging (LIKE prediction.fukusho_prediction INCLUDING ALL)`
       - Step 5 TRUNCATE staging: `TRUNCATE prediction.fukusho_prediction_staging`
       - Step 6 executemany INSERT into staging: `write_cur.executemany(f"INSERT INTO prediction.fukusho_prediction_staging ({cols_sql}) VALUES ({placeholders})", rows)`
       - Step 7 rowcount verify: `SELECT count(*) FROM prediction.fukusho_prediction_staging` で `count == len(rows)` を検証・違反で RuntimeError
       - Step 8 **model_version スコープ DELETE from 本テーブル（review HIGH#1: 全テーブル TRUNCATE でなく）**: `DELETE FROM prediction.fukusho_prediction WHERE model_type = %s AND model_version = %s`（psycopg2 sql.Identifier/Placeholder 使用・SQL injection 対策）・他 model_type/version の行は保持される
       - Step 9 INSERT staging → 本テーブル: `INSERT INTO prediction.fukusho_prediction SELECT * FROM prediction.fukusho_prediction_staging`
       - Step 10 DROP staging: `DROP TABLE prediction.fukusho_prediction_staging`（次回実行で再作成される・クリーンアップ）
       - Step 11 checksum 返却: `SELECT md5(string_agg(md5(row(cols_csv)::text), '' ORDER BY model_type, model_version, feature_snapshot_id, as_of_datetime, year, jyocd, kaiji, nichiji, racenum, umaban, kettonum))` WHERE model_type+model_version で scope（当該 model_version の行のみの checksum・review HIGH#1・ORDER BY PK 11カラムで安定）
       - 戻り値 = checksum（str）・呼出側が2回実行で一致を検証
    5. 【公開 API】`load_predictions(write_cur, prediction_df, reader_role=None) -> str` を提供（_idempotent_load_prediction の薄い wrapper・_df_to_prediction_tuples を内部呼出・reader_role が None なら settings.db_reader_role から取得）。**呼出側は1 model_type+model_version 単位で呼ぶ前提**（複数 model_version を統合 DataFrame で渡したら ValueError・run_train_predict は model_type 毎に呼出）。
    6. 【Test GREEN 化】tests/model/test_prediction_load.py の2 stub（test_model_version_scoped_swap_preserves_other_models 追加）を実装（両方 `@pytest.mark.requires_db`）:
       - test_idempotent_checksum_match: 合成 prediction DataFrame（10行・単一 model_type+model_version）で load_predictions を2回連続実行・1回目と2回目の checksum が完全一致を assert・`SELECT count(*) FROM prediction.fukusho_prediction WHERE model_type=... AND model_version=...` == len(rows)（重複なし）・staging テーブルが残らないことを `\dt prediction.*` で確認。teardown でテストデータ削除。
       - test_model_version_scoped_swap_preserves_other_models（review HIGH#1）: model_type='lightgbm' model_version='test-v1' の予測10行を書込・次に model_type='catboost' model_version='test-v1' の予測10行を別途書込・lightgbm の10行が破壊されず残ることを `SELECT count(*) ... WHERE model_type='lightgbm'` == 10 で assert・catboost も10行・全テーブル置換で lightgbm が削除されないことを実証。teardown で両方削除。
  </action>
  <verify>
    <automated>uv run pytest tests/model/test_prediction_load.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/model/test_prediction_load.py の2 test（test_idempotent_checksum_match + test_model_version_scoped_swap_preserves_other_models）が GREEN（KEIBA_SKIP_DB_TESTS=1 で skip 可）
    - src/db/prediction_load.py に `pg_advisory_xact_lock('prediction.fukusho_prediction')` がある
    - src/db/prediction_load.py に空入力拒否 `raise RuntimeError` がある
    - src/db/prediction_load.py に **`DELETE FROM prediction.fukusho_prediction WHERE model_type = %s AND model_version = %s`** がある（review HIGH#1: 全テーブル DROP/TRUNCATE でなく model_version スコープ DELETE）
    - src/db/prediction_load.py に `CREATE TABLE IF NOT EXISTS prediction.fukusho_prediction_staging (LIKE prediction.fukusho_prediction INCLUDING ALL)` がある
    - src/db/prediction_load.py に staging → 本テーブル INSERT と DROP staging クリーンアップがある
    - src/db/prediction_load.py に `GRANT SELECT ON prediction.fukusho_prediction TO {reader_role}` が適用される仕組みがある（初回 CREATE 時・又は run_apply_schema で付与済み・TO PUBLIC 不使用）
    - src/db/prediction_load.py に md5 string_agg checksum 返却がある・ORDER BY PK 11カラムで安定
    - `psycopg.sql.Identifier/Placeholder` を使い SQL injection 対策（生文字列埋込なし）
    - 単一 model_type+model_version で2回連続実行で checksum が bit-identical に一致（test で実証）
    - lightgbm 書込後 catboost 書込でも lightgbm 行が残る（review HIGH#1・test_model_version_scoped_swap_preserves_other_models で実証）
  </acceptance_criteria>
  <done>prediction_load.py が model_version スコープ staging-swap idempotent load を実装し 2回実行で checksum 一致・他 model_version の行を破壊しない・2 test が GREEN（D-05・review HIGH#1/Cross-Plan #3 達成）</done>
</task>

<task type="auto">
  <name>Task 3: src/model/evaluator.py — Brier/LogLoss/Calibration/sum(p) + 比較表（SC#2・§15.1/§15.2）</name>
  <files>src/model/evaluator.py</files>
  <read_first>
    - src/model/baseline.py（PLAN 03: compute_all_baselines・p_bl1..p_bl5 Series）
    - src/model/predict.py（PLAN 04 Task 1: predict_p_fukusho・p_fukusho_hit）
    - .planning/phases/04-model-prediction/04-RESEARCH.md「Pattern 1: stamped Parquet のみ学習」後半 Architecture Diagram（evaluator の位置）・D-08 BL-1..5 比較方法（SC#2 比較表）・「Common Pitfalls」「Pitfall 6: BL-3 オッズ逆数の正規化忘れ」
    - docs/keiba_ai_requirements_v1.3.md §15.1（評価指標: 的中率/Brier/LogLoss/Calibration Curve）・§15.2（確率品質受入基準: sum(p) 理論値 8頭以上 2.7-3.3 / 5-7頭 1.8-2.2・bin 単調増加・年別逆転なし）
    - .planning/phases/04-model-prediction/04-PATTERNS.md「src/model/evaluator.py」セクション
    - sklearn.metrics ドキュメント: brier_score_loss, log_loss, roc_auc_score
  </read_first>
  <action>
    1. 【モジュール docstring + from __future__ import annotations】SC#2/§15.1/§15.2/D-04（Calibration 重視選定基準）を明記。**review MEDIUM: sum(p) は複勝確率の独立二値性質により払戻対象数に厳密合計せず・診断的指標として扱う旨を明記**。
    2. 【定数 — review MEDIUM: binning 契約明示】SUM_P_BOUNDS = {"large": (2.7, 3.3), "small": (1.8, 2.2)}（§15.2・8頭以上と5-7頭・診断的閾値）。METRIC_COLUMNS = ["brier","logloss","auc","sum_p_mean","sum_p_median","sum_p_p10","sum_p_p90","calibration_max_dev"]。CALIBRATION_CURVE_BINS = 10・CALIBRATION_CURVE_STRATEGY = "uniform"・CALIBRATION_CURVE_MIN_BIN_COUNT = 30（review MEDIUM: binning 契約を固定値で明示・再現性保証）。
    3. 【compute_metrics(y_true, y_pred, *, race_keys=None, entry_counts=None) -> dict】`sklearn.metrics.brier_score_loss(y_true, y_pred)` / `log_loss(y_true, y_pred, labels=[0,1])` / `roc_auc_score(y_true, y_pred)` を計算。race_keys と entry_counts があれば sum(p) 分布（mean/median/std/p10/p90）を race_key groupby で計算（§15.2 受入基準・診断的）。calibration_max_dev は sklearn.calibration.calibration_curve で (mean_pred - frac_pos) の max abs・**CALIBRATION_CURVE_BINS/STRATEGY 定数を使用**（review MEDIUM: binning 契約固定）。戻り dict に `sum_p_note` キーで「独立二値確率のため厳密合計制約でない・診断的指標」注記を付与（review MEDIUM）。
    4. 【check_sum_p_distribution(df, p_col, entry_count_col) -> dict — review MEDIUM: 診断的扱い】race_key groupby で sum(p) を計算・8頭以上は [2.7,3.3]・5-7頭は [1.8,2.2] を機械検査（§15.2・診断的閾値）。違反 race_key のリストと割合を返す。fail-loud でなく warning（量的異常は参考レポート・hybrid gate D-01/D-02）。**戻り dict に "diagnostic_note" キーで「複勝確率は独立二値のため厳密合計制約でなく・参考診断」注記を付与**（review MEDIUM: evaluator で定量的診断と受入ゲートを分離）。
    5. 【build_comparison_table(metrics_dict) -> pd.DataFrame】metrics_dict = {"lightgbm": {...}, "catboost": {...}, "bl1": {...}, ..., "bl5": {...}} を比較表 DataFrame に統合。列 = METRIC_COLUMNS + model_name。BL-3 行に "market_reference" タグと「Phase 1-A モデルと同一情報条件の比較ではない（§14.2）」注記列。**review MEDIUM: BL-4/BL-5 未キャリブレーションの場合は bl_calib_note 列で注記**。Calibration 成分（brier の calibration term・sum(p) 分布適合度・calibration_max_dev）を強調表示するための列を追加（D-04 事前登録基準の素材）。
    6. 【write_eval_report(comparison_df, metrics_dict, *, out_md_path="reports/04-eval.md", out_json_path="reports/04-eval.json") -> tuple[Path, Path] — review LOW: .md と .json 分離】Markdown（reports/04-eval.md）は比較表 + §15.2 sum(p) 検査結果 + BL-3/BL-4/BL-5 注記・JSON（reports/04-eval.json）は metrics_dict を `json.dumps(sort_keys=True)` で atomic write（artifact.py の write_metadata_json を再利用可）。両パスのディレクトリを mkdir(parents=True, exist_ok=True)。戻り値 = (md_path, json_path)。
    7. 【evaluate_all_models(predictions_by_model, y_true_by_split, *, out_md_path="reports/04-eval.md", out_json_path="reports/04-eval.json") -> dict】predictions_by_model = {"lightgbm": df, "catboost": df, "bl1": df, ..., "bl5": df}・各モデルで compute_metrics を呼出・build_comparison_table で統合・write_eval_report で出力・戻り値 = metrics_dict（PLAN 05 が消費）。
    8. 【D-04 選定基準素材の事前登録】evaluator 出力に「Calibration 重視」の重み付け（calibration_max_dev と sum(p) 分布適合度を主要列・brier/logloss を次点・auc を参考）を comment 欄で明示。これは D-04「結果を見る前に固定」の素材。Phase 6 で最終選定基準として採用。
  </action>
  <verify>
    <automated>uv run python -c "from src.model.evaluator import compute_metrics, check_sum_p_distribution, build_comparison_table, write_eval_report, evaluate_all_models, SUM_P_BOUNDS, METRIC_COLUMNS, CALIBRATION_CURVE_BINS; import pandas as pd, numpy as np; y=np.array([0,1,0,1,1]); p=np.array([0.1,0.9,0.4,0.6,0.55]); m=compute_metrics(y,p); assert 'brier' in m and 'logloss' in m; assert CALIBRATION_CURVE_BINS == 10; print('EVALUATOR_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - src/model/evaluator.py に compute_metrics / check_sum_p_distribution / build_comparison_table / write_eval_report / evaluate_all_models の5関数が存在
    - SUM_P_BOUNDS = {"large": (2.7,3.3), "small": (1.8,2.2)} が §15.2 と一致
    - CALIBRATION_CURVE_BINS / CALIBRATION_CURVE_STRATEGY / CALIBRATION_CURVE_MIN_BIN_COUNT 定数が定義され compute_metrics/calibration_curve で使用される（review MEDIUM: binning 契約明示）
    - build_comparison_table が BL-3 行に §14.2 注記（同一情報条件でない）を含む・BL-4/BL-5 未キャリブレーション注記も含む（review MEDIUM）
    - write_eval_report が reports/04-eval.md と reports/04-eval.json に**分離**出力（review LOW）・json.dumps(sort_keys=True) で再現性
    - compute_metrics が brier/logloss/auc/sum_p 分布/calibration_max_dev を計算・sum_p_note キーで診断注記を付与（review MEDIUM）
    - check_sum_p_distribution が違反 race_key リストを返し diagnostic_note キーで独立二値性質の注記を付与（review MEDIUM・fail-loud でなく warning・hybrid gate 準拠）
    - evaluator 出力に D-04「Calibration 重視」選定基準素材が事前登録されている（comment/注記列）
  </acceptance_criteria>
  <done>evaluator.py が Brier/LogLoss/Calibration/sum(p) を計算し BL-1..5 と主モデルの比較表を reports/04-eval.md + reports/04-eval.json に分離出力・§15.2 sum(p) 機械検査（診断的）・binning 契約明示を含む（SC#2/§15.1/§15.2/D-04・review MEDIUM/LOW 達成）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| calibrated estimator → prediction DataFrame | predict_proba → p_fukusho_hit 変換境界・provenance 完全性 |
| prediction DataFrame → staging-swap → prediction.fukusho_prediction | atomic 替換の境界・部分失敗で破損 |
| prediction.fukusho_prediction → Phase 5/6/7 SQL 照会 | reader ロール SELECT 境界 |
| metrics 計算 → reports/04-eval.md | 比較表が D-04 事前登録基準の素材になる境界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-20 | Repudiation | provenance 列の欠損（model_version/feature_snapshot_id 等）で再現性崩壊 | mitigate | _assert_valid_prediction_df で全 provenance 列 NOT NULL を ValueError guard・unit test で NaN なしを assert（§19.1） |
| T-04-20b | Tampering | model_version 形式が D-10 例と不一致（二重 suffix 等）で provenance 一意性崩壊 | mitigate | make_model_version が D-10 例と完全一致 `{feature_snapshot_id}-{short}-v{N}` を返す・unit test で lightgbm/catboost/v2 と "postreview-v2" snapshot_id で二重 suffix が起きないことを assert（review HIGH#4/Cross-Plan #1） |
| T-04-21 | Tampering | prediction テーブルへの部分失敗書込で破損・及び全テーブル置換で他 model_version の行を破壊 | mitigate | **model_version スコープ置換**（advisory lock → staging INCLUDING ALL → TRUNCATE staging → INSERT staging → count verify → DELETE WHERE model_type+model_version from 本テーブル → INSERT staging → 本テーブル → DROP staging）で他 model_version の行を保持（review HIGH#1/Cross-Plan #3）・2回実行で checksum 一致を test で実証・test_model_version_scoped_swap_preserves_other_models で lightgbm 書込後 catboost 書込でも lightgbm 残ることを実証 |
| T-04-22 | Elevation of Privilege | reader ロールへの過剰 GRANT | mitigate | GRANT SELECT ON prediction.fukusho_prediction TO {reader_role} のみ・psycopg.sql.Identifier/Placeholder で SQL injection 対策・TO PUBLIC 不使用（HIGH #3） |
| T-04-23 | Tampering | checksum SQL の列順序依存で非決定論的 | mitigate | md5 string_agg で ORDER BY PK 11カラム（model_type/model_version/feature_snapshot_id/as_of_datetime + 7カラム RACE_KEY）安定・列順は PREDICTION_COLUMNS 固定・model_version スコープ WHERE 付き（fukusho_label.py パターン踏襲・review HIGH#1） |
| T-04-24 | Information Disclosure | evaluator が後知恵で選定基準をすり替え | mitigate | D-04 事前登録: comparison 表の comment 列で「Calibration 重視（calibration_max_dev + sum(p) 適合度を主要・brier/logloss 次点・auc 参考）」を結果を見る前に固定・Phase 6 で最終判定 |
| T-04-25 | Tampering | sum(p) 理論値違反を silent 無視・または独立二値性質で false alarm | mitigate | check_sum_p_distribution が違反 race_key リストと割合を返し・diagnostic_note で独立二値性質を明記（review MEDIUM）・reports/04-eval.md に記載・§15.2 受入基準違反は Phase 6 ゲートでブロック対象（本 PLAN では warning・hybrid gate D-01/D-02 準拠） |
| T-04-25b | Repudiation | bit-identical hash に揮発性 as_of_datetime = now() が混入 | mitigate | predict_p_fukusho が as_of_datetime 引数を受け取り・reproduce smoke では固定値を渡す・evaluator の hash 計算は予測値のみを scope（review MEDIUM） |
</threat_model>

<verification>
- Wave 3 完了（PLAN 03 と PLAN 04 並列・files_modified 非衝突: trainer/baseline vs predict/prediction_load/evaluator）: `uv run pytest tests/model/test_predict.py tests/model/test_prediction_load.py -x -q` が全 GREEN
- predict/evaluator/prediction_load は PLAN 05 (run_train_predict) が統合して消費
</verification>

<success_criteria>
- predict.py が provenance 付き予測 DataFrame を構築・make_model_version が D-10 例と完全一致（MODL-01/D-05/D-10・review HIGH#4）
- prediction_load.py が model_version スコープ staging-swap idempotent load を実装し 2回実行で checksum 一致・他 model_version の行を破壊しない（D-05・review HIGH#1/Cross-Plan #3）
- evaluator.py が Brier/LogLoss/Calibration/sum(p) を計算し BL-1..5 + 主モデル比較表を reports/04-eval.md + .json に分離出力・binning 契約明示（SC#2/§15.1/§15.2・review MEDIUM/LOW）
- D-04 事前登録基準素材が比較表に固定
- 4 test（test_predict.py 2 + test_prediction_load.py 2）が GREEN
</success_criteria>

<output>
Create `.planning/phases/04-model-prediction/04-04-SUMMARY.md` when done

## Artifacts this phase produces
- src/model/predict.py: `predict_p_fukusho(calibrated_estimator, X, *, model_type, model_version, feature_snapshot_id, calib_method, race_df, split_label, as_of_datetime=None) -> pd.DataFrame`（review MEDIUM: as_of_datetime 制御可能）, `make_model_version(feature_snapshot_id, model_type, version_n=1) -> str`（review HIGH#4: D-10 完全一致）, `_assert_valid_prediction_df(df)`・定数 `PREDICTION_COLUMNS`, `MODEL_TYPE_TO_SHORT`, `PK_COLUMNS`
- src/model/evaluator.py: `compute_metrics(y_true, y_pred, *, race_keys, entry_counts) -> dict`, `check_sum_p_distribution(df, p_col, entry_count_col) -> dict`（review MEDIUM: 診断的・diagnostic_note 付き）, `build_comparison_table(metrics_dict) -> pd.DataFrame`, `write_eval_report(comparison_df, metrics_dict, *, out_md_path, out_json_path) -> tuple[Path, Path]`（review LOW: .md+.json 分離）, `evaluate_all_models(predictions_by_model, y_true_by_split, *, out_md_path, out_json_path) -> dict`・定数 `SUM_P_BOUNDS`, `METRIC_COLUMNS`, `CALIBRATION_CURVE_BINS`, `CALIBRATION_CURVE_STRATEGY`, `CALIBRATION_CURVE_MIN_BIN_COUNT`
- src/db/prediction_load.py: `_idempotent_load_prediction(write_cur, rows, reader_role) -> str`（review HIGH#1: model_version スコープ DELETE→INSERT・全テーブル破壊しない）, `load_predictions(write_cur, prediction_df, reader_role=None) -> str`, `_df_to_prediction_tuples(df) -> list[tuple]`・定数 `_PREDICTION_TABLE`
- tests/model/test_predict.py: 2 test GREEN
- tests/model/test_prediction_load.py: 2 test GREEN（requires_db・test_model_version_scoped_swap_preserves_other_models 追加・review HIGH#1）
</output>
