---
phase: 02-fukusho-labels
plan: 04
type: tdd
wave: 3
depends_on: [02-03]
files_modified:
  - src/etl/label_reconcile.py
  - tests/test_label_reconcile.py
  - scripts/run_label_reconcile.py
autonomous: true
requirements: [LABEL-03]
tags: [phase-02, labels, reconcile, hybrid-gate, block-info, gt-999-pct, held-out]

must_haves:
  truths:
    - "src/etl/label_reconcile.py が存在し reconcile_against_payout(cur) -> dict を export する"
    - "§10.5 の6検査が全て CheckResult dataclass で実装され、severity が BLOCK/INFO に正しく分類される"
    - ">99.9% agreement テストが時系列ホールドアウト（最新10%）+ 層化で実装され、レース単位の馬集合完全一致を検証する"
    - "BLOCK 1件でも passed=False の場合 verdict='fail' になる（Phase 1 D-01 と同じ集計）"
    - "test_label_reconcile.py が §10.5 6検査 + agreement + BLOCK/INFO 分離 + degraded_checks_count + unresolved_fraction 報告（SC#3・W3）を網羅"
    - "scripts/run_label_reconcile.py が CLI から reconcile を実行し verdict を出力"
  artifacts:
    - path: "src/etl/label_reconcile.py"
      provides: "§10.5 払戻テーブル突合ゲート・CheckResult 再利用・reconcile_against_payout 統合エントリ"
      contains: "def reconcile_against_payout"
      min_lines: 300
    - path: "tests/test_label_reconcile.py"
      provides: "LABEL-03 unit/integration test・§10.5 6検査 BLOCK/INFO・>99.9% agreement"
      contains: "test_gt_999_pct_agreement"
      min_lines: 200
    - path: "scripts/run_label_reconcile.py"
      provides: "reconcile CLI・readonly_cur で実行・verdict を JSON/ログ出力"
      contains: "reconcile_against_payout"
  key_links:
    - from: "src/etl/label_reconcile.py"
      to: "src/etl/quality_gate.py CheckResult"
      via: "CheckResult dataclass を import または複製"
      pattern: "from src.etl.quality_gate import CheckResult|class CheckResult"
    - from: "src/etl/label_reconcile.py reconcile_against_payout"
      to: "label.fukusho_label + public.n_harai"
      via: "readonly_cur SELECT で両テーブルを比較"
      pattern: "FROM label.fukusho_label"
    - from: "src/etl/label_reconcile.py"
      to: "src/etl/filters.py PROJECT_WINDOW_FILTER"
      via: "JRA + 2015以降フィルタ適用"
      pattern: "PROJECT_WINDOW_FILTER"
---

<objective>
LABEL-03（払戻テーブル突合・>99.9% agreement）の品質ゲートを実装する。`src/etl/label_reconcile.py` を新規作成し、§10.5 の6検査（RESEARCH 対応表に基づき全て BLOCK）を Phase 1 `quality_gate.py` の `CheckResult` dataclass + `run_quality_gate` パターンで実装。`reconcile_against_payout(cur) -> dict` 統合エントリが BLOCK/INFO verdict 集計（D-02）を返す。`tests/test_label_reconcile.py` を TDD（RED→GREEN）で作成し、6検査・BLOCK/INFO 分離・>99.9% agreement（時系列ホールドアウト最新10% + 層化・レース単位馬集合完全一致）・degraded_checks_count を網羅。`scripts/run_label_reconcile.py` CLI エントリポイント作成。

Purpose: 予測目標 `fukusho_hit_validated` が払戻テーブル（HR PayFukusyoUmaban1..5）と整合することを聖域として保護。SC#2（>99.9% agreement）の直接実装。Phase 1 D-01 hybrid gate を LABEL-03 に踏襲（D-02）。silent mislabeling が下流に流出するのを防ぐ最終防波堤。

Output: 2新規ファイル + 1テストファイル。reconcile 実行で label.fukusho_label の全行が HR 払戻テーブルと整合することが検証される。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@$HOME/.claude/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-fukusho-labels/02-CONTEXT.md
@.planning/phases/02-fukusho-labels/02-RESEARCH.md
@.planning/phases/02-fukusho-labels/02-PATTERNS.md
@.planning/phases/02-fukusho-labels/02-03-PLAN.md
@src/etl/quality_gate.py
@src/etl/filters.py
@src/db/connection.py
@tests/test_quality_gate.py
@tests/conftest.py
</context>

<tasks>

<task type="tdd" tdd_phase="red">
  <name>Task 1: test_label_reconcile.py 作成（§10.5 6検査 + agreement + BLOCK/INFO + degraded・RED）</name>
  <files>tests/test_label_reconcile.py</files>

  <behavior>
    - Test test_reconcile_module_imports: src.etl.label_reconcile から reconcile_against_payout / CheckResult / 各 _check_* 関数が import 可能
    - Test test_block_info_separation: 6検査の severity が BLOCK/INFO に正しく分類される（§10.5 対応表通り全6検査が BLOCK）
    - Test test_check_payout_precision: 検査1（fukusho_hit=1 の馬が払戻テーブルに存在）・mock cursor で不一致行を返し CheckResult.passed=False・severity='block'
    - Test test_check_payout_recall: 検査2（払戻テーブル対象馬が fukusho_hit=1）・同様
    - Test test_check_dead_heat_integrity: 検査3（同着で対象馬数 > 理論値でも破綻しない）・mock で正常 dead_heat を返し passed=True
    - Test test_check_no_scratch_mislabeled: 検査4（取消/除外馬が誤って fukusho_hit=1）・mock で scratch馬+hit=1 を返し passed=False
    - Test test_check_dead_loss_not_excluded: 検査5（競走中止馬が誤って除外）・mock で dead_loss馬+is_model_eligible=False を返し passed=False
    - Test test_check_no_fukusho_sale_not_in_training: 検査6（複勝発売なしレースが学習対象に混入）・mock で is_model_eligible=True & no_fukusho_sale を返し passed=False
    - Test test_gt_999_pct_agreement: 時系列ホールドアウト最新10% + 層化（年/競馬場/頭数帯）でレース単位馬集合完全一致を assert・agreement >= 99.9%
    - Test test_verdict_fail_when_block_failed: BLOCK 検査1件でも passed=False で verdict='fail'
    - Test test_verdict_pass_when_all_block_pass: 全 BLOCK 検査 passed=True で verdict='pass'
    - Test test_degraded_checks_count: INFO 検査で error キーを含む場合 degraded_checks_count > 0（WR-05）
    - Test test_unresolved_fraction_reported_in_status_distribution: _check_label_status_distribution の戻り値 detail が `unresolved_fraction`（float）と `unresolved_threshold`（0.01）を含み・mock cursor で unresolved_count=376/total_count=553891 の場合 `unresolved_fraction ≈ 0.000678`（< 0.01）かつ `threshold_exceeded == False` となることを assert（W3・SC#3 明示報告）
    - Test test_no_auth_info_in_check_detail: 各 check dict が name/passed/severity/detail のみを含み DSN/password 等を含まない（T-02-02）
  </behavior>

  <read_first>
    - tests/test_quality_gate.py:11-61（import パターン・_mock_cursor ファクトリ・fetch_map 部分文字列マッチ）
    - tests/test_quality_gate.py:69-146（BLOCK verdict fail テスト・test_blocking_checks_fail_when_no_2015_data / test_n_race_pk_duplicate_fails / test_table_missing_fails）
    - tests/test_quality_gate.py:262-285（verdict pass テスト・test_verdict_logic_all_block_pass・check dict key 検証）
    - tests/test_quality_gate.py:462-555（degraded_checks_count テスト・WR-05）
    - src/etl/quality_gate.py:91-106（CheckResult dataclass・name/passed/severity/detail）
    - src/etl/quality_gate.py:160-175（_check_table_exists 最小テンプレ）
    - src/etl/quality_gate.py:533-617（run_quality_gate 統合エントリ・verdict 集計・degraded_checks_count・T-02-02 security）
    - .planning/phases/02-fukusho-labels/02-RESEARCH.md:336-351（§10.5 6検査 BLOCK/INFO 対応表・全6検査 BLOCK）
    - .planning/phases/02-fukusho-labels/02-RESEARCH.md:594-597（Open Question #3・held-out 10% + 層化・レース単位馬集合完全一致）
    - .planning/phases/02-fukusho-labels/02-PATTERNS.md:92-139（label_reconcile.py 構造指針・CheckResult 再利用・6検査関数テンプレ・reconcile_against_payout）
    - .planning/phases/02-fukusho-labels/02-CONTEXT.md:29-34（D-02 hybrid gate 踏襲・構造=BLOCK/量化=INFO）
    - tests/conftest.py:22-78（readonly_cur fixture・requires_db policy）
  </read_first>

  <action>
    `tests/test_quality_gate.py:11-26` の import・`_mock_cursor`（test_quality_gate.py:33-61）ファクトリを再利用可能な形で `tests/test_label_reconcile.py` を新規作成。ファイルヘッダ docstring に「LABEL-03 払戻テーブル突合ゲート・§10.5 6検査 BLOCK/INFO・D-02 hybrid gate 踏襲・WR-05 degraded visibility・T-02-02 認証情報非含有」と明記。

    **共通 helper**: `tests/test_quality_gate.py:33-61` の `_mock_cursor(fetch_map)` をそのまま本ファイルに複製（同一パターン・依存避けるため）。fetch_map は SQL 部分文字列 → fetchone 戻り値の辞書。複数 fetchall を取る検査がある場合は fetchall 用の拡張 `_mock_cursor_with_fetchall(fetchone_map, fetchall_map)` も定義。

    **test_reconcile_module_imports**: `from src.etl import label_reconcile` で `callable(label_reconcile.reconcile_against_payout)` と各 `_check_*` 関数（`_check_payout_precision` / `_check_payout_recall` / `_check_dead_heat_integrity` / `_check_no_scratch_mislabeled` / `_check_dead_loss_not_excluded` / `_check_no_fukusho_sale_not_in_training`）の callable を assert。Plan 04 Task 2 GREEN まで ImportError/AttributeError で RED。

    **test_block_info_separation**: 6検査関数をそれぞれ正常データの mock cursor で呼出し、戻り値 CheckResult の `.severity` を検証:
    - `_check_payout_precision` → 'block'
    - `_check_payout_recall` → 'block'
    - `_check_dead_heat_integrity` → 'block'
    - `_check_no_scratch_mislabeled` → 'block'
    - `_check_dead_loss_not_excluded` → 'block'
    - `_check_no_fukusho_sale_not_in_training` → 'block'
    （RESEARCH 対応表通り全6検査が BLOCK・量化 drift は INFO 別関数）

    **test_check_payout_precision**: mock cursor で `label.fukusho_label.fukusho_hit_validated=1 AND umaban NOT IN payout_set` の不一致行を N>0 で返す。`_check_payout_precision(cur)` が `CheckResult(passed=False, severity='block')` を返すことを assert。N=0 の場合は `passed=True`。

    **test_check_payout_recall**: 逆方向・`umaban IN payout_set AND fukusho_hit_validated=0` の不一致行。同様。

    **test_check_dead_heat_integrity**: mock で dead_heat レースの payout_count > fukusho_payout_places が正常に処理される場合を返し、`_check_dead_heat_integrity` が `passed=True` を返すことを assert（97レース相当・正常状態）。

    **test_check_no_scratch_mislabeled**: mock で `bataijyu='000' AND fukusho_hit_validated=1` の行を返し、`_check_no_scratch_mislabeled` が `passed=False` を返すことを assert（実DBでは0件だが guard）。

    **test_check_dead_loss_not_excluded**: mock で `harontimel3='999.0' AND timediff='9999' AND time 有 AND is_model_eligible=False` の行を返し、`_check_dead_loss_not_excluded` が `passed=False` を返す（競走中止馬は学習に残す・§10.6）。

    **test_check_no_fukusho_sale_not_in_training**: mock で `is_model_eligible=True AND is_fukusho_sale_available=False` の行を返し、`_check_no_fukusho_sale_not_in_training` が `passed=False` を返す。

    **test_gt_999_pct_agreement**: integration test（`@pytest.mark.requires_db`）。実DB で:
    1. 全レースを race_date 昇順でソート
    2. 最新10%（race_id単位）をホールドアウト
    3. 層化（年/競馬場 jyocd/頭数帯 5-7・8以上）で各層のレースも抽出
    4. 各ホールドアウトレースで: label.fukusho_label の馬集合（fukusho_hit_validated=1 の umaban set）と public.n_harai.PayFukusyoUmaban1..5 の馬集合を比較
    5. agreement =（両者が完全一致したレース数 / ホールドアウト総レース数）* 100
    6. agreement >= 99.9 を assert
    本テストは mock で実DB相当の分布を再現するのは難しいため `@pytest.mark.requires_db` 必須。Plan 04 Task 2 GREEN 時点では reconcile_against_payout 内にヘルパー `_compute_race_level_agreement(cur, sample_pct=0.1) -> dict` を実装して呼出す。

    **test_verdict_fail_when_block_failed**: `_mock_cursor` で1検査だけ passed=False を返す状況を作り、`reconcile_against_payout(cur)` の `result['verdict'] == 'fail'` を assert。

    **test_verdict_pass_when_all_block_pass**: 全 BLOCK 検査 passed=True を返す mock で `result['verdict'] == 'pass'` を assert。

    **test_degraded_checks_count**: WR-05・`reconcile_against_payout` の戻り値が `degraded_checks_count` フィールドを含むことを assert。

    **test_no_auth_info_in_check_detail**: T-02-02・各 check dict が `{name, passed, severity, detail}` のみをキーに持ち、`detail` の値に DSN/password/secret 等の文字列を含まないことを assert（`test_verdict_pass_when_all_block_pass` の result を使用・'password' / 'dsn' / 'secret' の部分文字列が detail 内に無いことを確認）。

    全テストのうち unit test（mock cursor 使用）は `@pytest.mark.requires_db` なし・`test_gt_999_pct_agreement` のみ `@pytest.mark.requires_db` 付き。Plan 04 Task 2 GREEN まで ImportError/AttributeError で RED。
  </action>

  <verify>
    <automated>test -f tests/test_label_reconcile.py &amp;&amp; uv run pytest tests/test_label_reconcile.py --collect-only 2>&amp;1 | grep -c "test_" | awk '{if ($1 >= 14) print "OK"; else print "NG"}' | grep -q "^OK$"</automated>
  </verify>

  <acceptance_criteria>
    - `tests/test_label_reconcile.py` が存在する
    - `uv run pytest tests/test_label_reconcile.py --collect-only` が14テスト以上を収集: test_reconcile_module_imports / test_block_info_separation / test_check_payout_precision / test_check_payout_recall / test_check_dead_heat_integrity / test_check_no_scratch_mislabeled / test_check_dead_loss_not_excluded / test_check_no_fukusho_sale_not_in_training / test_gt_999_pct_agreement / test_verdict_fail_when_block_failed / test_verdict_pass_when_all_block_pass / test_degraded_checks_count / test_no_auth_info_in_check_detail / test_unresolved_fraction_reported_in_status_distribution
    - `test_gt_999_pct_agreement` に `@pytest.mark.requires_db` マークが付く（integration・実DB）
    - 他の12テストは `@pytest.mark.requires_db` なし（unit・mock cursor）
    - この時点で全テスト RED（ImportError/AttributeError・src.etl.label_reconcile が Task 2 で実装されるまで）
    - `from src.etl.label_reconcile import reconcile_against_payout` を含む（Task 2 GREEN の契約）
    - 実カラム名 `bataijyu` / `harontimel3` / `timediff` / `payfukusyounmaban1..5` / `is_fukusho_sale_available` / `is_model_eligible` / `fukusho_hit_validated` が使用される（Pitfall 1 + LABEL-03 6検査項目）
  </acceptance_criteria>

  <done>tests/test_label_reconcile.py が作成され14テスト以上が collection される。全 RED。§10.5 6検査・BLOCK/INFO 分離・>99.9% agreement・WR-05・W3 unresolved fraction 報告・T-02-02 を網羅。</done>
</task>

<task type="tdd" tdd_phase="green">
  <name>Task 2: label_reconcile.py 実装 + run_label_reconcile.py CLI（GREEN・Task 1 RED を通す）</name>
  <files>src/etl/label_reconcile.py, scripts/run_label_reconcile.py</files>

  <behavior>
    - src/etl/label_reconcile.py: 6検査関数（_check_payout_precision / _check_payout_recall / _check_dead_heat_integrity / _check_no_scratch_mislabeled / _check_dead_loss_not_excluded / _check_no_fukusho_sale_not_in_training）+ reconcile_against_payout(cur) -> dict（verdict 集計 + degraded_checks_count）+ _compute_race_level_agreement(cur, sample_pct=0.1) -> dict（時系列ホールドアウト + 層化）
    - scripts/run_label_reconcile.py: CLI エントリポイント・readonly_cur で reconcile_against_payout を実行・verdict を JSON / INFO ログ出力・masked DSN 使用
  </behavior>

  <read_first>
    - src/etl/quality_gate.py:91-106（CheckResult dataclass・name/passed/severity/detail）
    - src/etl/quality_gate.py:114-152（_load_allowed_codes・YAML ロード・RuntimeError fail-fast）
    - src/etl/quality_gate.py:160-253（_check_table_exists / _check_jra_since_2015 / _check_n_race_pk_unique の各 BLOCK 検査テンプレ）
    - src/etl/quality_gate.py:533-617（run_quality_gate 統合エントリ・verdict 集計・_has_error 走査・WR-05 degraded_checks_count）
    - src/etl/filters.py:31-39（PROJECT_WINDOW_FILTER・JRA_FILTER）
    - src/db/connection.py:21-52（make_pool role='readonly'・search_path）
    - .planning/phases/02-fukusho-labels/02-RESEARCH.md:336-351（§10.5 6検査 BLOCK/INFO 対応表・実装メモ）
    - .planning/phases/02-fukusho-labels/02-RESEARCH.md:594-597（Open Question #3・held-out 10% + 層化・レース単位馬集合完全一致）
    - .planning/phases/02-fukusho-labels/02-PATTERNS.md:92-139（label_reconcile.py の CheckResult 再利用・6検査関数構造・reconcile_against_payout・T-02-02 セキュリティ）
    - .planning/phases/02-fukusho-labels/02-CONTEXT.md:29-34（D-02 hybrid gate 踏襲・quality_gate.py CheckResult パターン再利用）
    - tests/test_label_reconcile.py（Task 1 で作成・13テスト GREEN 目標）
    - scripts/run_quality_report.py（参考・CLI エントリポイント構造・allowlist filter で認証情報除去）
    - src/etl/fukusho_label.py（Plan 03 で作成・label.fukusho_label テーブル構造・カラム名参照元）
  </read_first>

  <action>
    `src/etl/quality_gate.py:91-617` の構造（CheckResult dataclass → 各検査関数 → run_quality_gate 統合エントリ → WR-05 degraded）を直接踏襲して `src/etl/label_reconcile.py` を新規作成。

    **imports**: `from __future__ import annotations` / `from dataclasses import asdict, dataclass, field` / `from typing import Any` / `from psycopg import Cursor` / `from src.etl.filters import PROJECT_WINDOW_FILTER` / `from src.etl.quality_gate import CheckResult`（dataclass を再利用・PATTERNS.md:98-107） / `import logging`。`logger = logging.getLogger(__name__)`。

    **6検査関数**（各 `cur: Cursor` を受け取り `CheckResult` を返す・quality_gate.py:160-175 のテンプレ構造）:

    1. `_check_payout_precision(cur) -> CheckResult`: 検査1（§10.5 #1）・`fukusho_hit_validated=1` の馬が HR PayFukusyoUmaban1..5 に存在するか。SQL: `label.fukusho_label` と `public.n_harai` を JOIN し、`fukusho_hit_validated=1 AND umaban NOT IN (payfukusyounmaban1..5)` の行数を COUNT。N=0 なら passed=True・N>0 なら passed=False。severity='block'。detail に `count` / `sample_size` を格納。

    2. `_check_payout_recall(cur) -> CheckResult`: 検査2（§10.5 #2）・逆方向。`umaban IN (payfukusyounmaban1..5) AND fukusho_hit_validated=0` の行数。severity='block'。

    3. `_check_dead_heat_integrity(cur) -> CheckResult`: 検査3（§10.5 #3）・同着レースで払戻対象馬数 > 理論値でも正しく `dead_heat` status が付いているか。SQL: `label_validation_status='dead_heat'` の行で `is_dead_heat=True AND fukusho_hit_validated IN (0,1)`（dead_heat でも非対象馬は0）。passed=True で正常・異常があれば False。severity='block'。

    4. `_check_no_scratch_mislabeled(cur) -> CheckResult`: 検査4（§10.5 #4）・取消/除外馬が誤って `fukusho_hit_validated=1` になっていないか。SQL: `is_scratch_cancel=True AND fukusho_hit_validated=1` の行数。実DBでは0件だが guard。severity='block'。

    5. `_check_dead_loss_not_excluded(cur) -> CheckResult`: 検査5（§10.5 #5）・競走中止馬が誤って除外されていないか。SQL: `is_dead_loss=True AND is_model_eligible=False` の行数。N>0 なら passed=False（競走中止馬は is_model_eligible=True で学習に残す・§10.6）。severity='block'。

    6. `_check_no_fukusho_sale_not_in_training(cur) -> CheckResult`: 検査6（§10.5 #6）・複勝発売なしレースが学習対象に混入していないか。SQL: `is_model_eligible=True AND is_fukusho_sale_available=False` の行数。N>0 なら passed=False。severity='block'。

    **量化 INFO 検査**: `_check_label_status_distribution(cur) -> CheckResult`・severity='info'・detail に status_values 別件数 / **unresolved 割合（`unresolved_count / total_count`・SC#3 明示報告・実測 ≈0.07% はレース全体中止376行/553,891行）** / dead_heat レース数 / drift 行数（fukusho_hit_raw vs fukusho_hit_validated の不一致）を格納（D-02 量化レポート）。**detail に `unresolved_fraction`（float）と `unresolved_threshold`（0.01）を明示格納し・SC#3 の「unresolved fraction reported」を直接満たす（W3 対応）**。実測 ≈0.07% < 1% 閾値を大幅に下回るため severity='info' で保持し、閾値超過時は detail に `threshold_exceeded: True` フラグを立てて監視強化（ただし verdict は BLOCK 検査のみで決定・D-02 一貫）。

    **_compute_race_level_agreement(cur, sample_pct: float = 0.1) -> dict[str, Any]**: 時系列ホールドアウト + 層化・レース単位馬集合完全一致:
    1. `label.fukusho_label` と `public.n_harai` を race_id 単位で JOIN
    2. race_date 昇順でソートし、最新 `sample_pct * 100`%（例: 10%）のレースをホールドアウト
    3. 層化（year / jyocd / 頭数帯 5-7・8以上）で各層からも抽出（層化サンプリング・stratified）
    4. 各ホールドアウトレースで:
       - `label` 側の馬集合: `{umaban for fukusho_hit_validated=1}`
       - `HR` 側の馬集合: `{payfukusyounmaban1..5 の非NA値}`
       - 完全一致（== 比較）なら agree_count += 1
    5. `agreement = agree_count / total_held_out * 100.0`
    6. 戻り値: `{"agreement_pct": float, "agree_count": int, "total_held_out": int, "disagree_races": [...]}`
    Open Question #3 の推奨実装・レース単位の precision/recall 両方 1.0 を意味する完全一致。

    **reconcile_against_payout(cur) -> dict[str, Any]**: `quality_gate.py:533-617` の `run_quality_gate` パターンを複製:
    1. `results: list[CheckResult] = []` に6検査 BLOCK + 1量化 INFO を順次 append
    2. `verdict = "pass" if all(r.passed for r in results if r.severity == "block") else "fail"`（quality_gate.py:583 と同じ）
    3. WR-05: `_has_error(detail)` 走査ロジックを quality_gate.py:596-609 から複製・`degraded_checks_count = sum(1 for r in results if r.severity == "info" and _has_error(r.detail))`
    4. `_compute_race_level_agreement(cur, sample_pct=0.1)` を呼出して agreement を result に付属
    5. 戻り値: `{"verdict": "pass"|"fail", "checks": [asdict(r) for r in results], "degraded_checks_count": int, "agreement": {...}}`
    T-02-02 セキュリティ: 各 check dict は `name/passed/severity/detail` のみをキーに持つ・DSN/password 等は一切含めない（quality_gate.py:554-555 と同じ制約）。

    **__all__**: `["reconcile_against_payout", "CheckResult", "_check_payout_precision", "_check_payout_recall", "_check_dead_heat_integrity", "_check_no_scratch_mislabeled", "_check_dead_loss_not_excluded", "_check_no_fukusho_sale_not_in_training", "_check_label_status_distribution", "_compute_race_level_agreement"]`

    **scripts/run_label_reconcile.py 新規作成**: `scripts/run_quality_report.py` の構造（参考・allowlist filter で認証情報除去）を写しつつ簡略化:
    - `from __future__ import annotations` / argparse / logging / sys / pathlib.Path / `from dotenv import load_dotenv`
    - `_REPO_ROOT = Path(__file__).resolve().parent.parent` / `sys.path.insert(0, str(_REPO_ROOT / "src"))`
    - `load_dotenv(_REPO_ROOT / ".env")`
    - main(): `from src.config.settings import Settings` / `from src.db.connection import make_pool` / `from src.etl.label_reconcile import reconcile_against_payout`
    - `settings = Settings()` / `read_pool = make_pool(settings, role='readonly')`
    - `with read_pool.connection() as conn: with conn.cursor() as cur: result = reconcile_against_payout(cur)`
    - verdict / 各 check の passed・severity・count / agreement_pct を INFO ログ出力・JSON 形式でも stdout に出力可能（--json フラグ）
    - masked DSN ログ（`settings.dsn_masked`・T-01-01）
    - verdict=='fail' の場合 exit code 1・'pass' の場合 0

    実装後 `uv run pytest tests/test_label_reconcile.py -x` で13テスト全て GREEN になるまで反復。
  </action>

  <verify>
    <automated>uv run pytest tests/test_label_reconcile.py -x 2>&amp;1 | tail -5 | grep -E "passed|failed" | grep -qE "(14 passed|1[45] passed|1[4-9] passed)"</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tests/test_label_reconcile.py -x` が14テスト全て passed（Plan 04 Task 1 RED が GREEN に）
    - `src/etl/label_reconcile.py` が存在し `python3 -c "from src.etl.label_reconcile import reconcile_against_payout, CheckResult, _check_payout_precision, _check_payout_recall, _check_dead_heat_integrity, _check_no_scratch_mislabeled, _check_dead_loss_not_excluded, _check_no_fukusho_sale_not_in_training, _compute_race_level_agreement"` が成功
    - ファイル内に `from src.etl.quality_gate import CheckResult` を含む（PATTERNS.md:98-107 再利用）
    - ファイル内に `PROJECT_WINDOW_FILTER` の import を含む（CR-06）
    - ファイル内に6検査関数名（_check_payout_precision / _check_payout_recall / _check_dead_heat_integrity / _check_no_scratch_mislabeled / _check_dead_loss_not_excluded / _check_no_fukusho_sale_not_in_training）を含む
    - ファイル内に `FROM label.fukusho_label` と `FROM public.n_harai` を含む（両テーブル JOIN）
    - ファイル内に `severity='block'` を6箇所以上含む（6検査全て BLOCK）
    - ファイル内に `verdict = "pass" if all` パターンを含む（Phase 1 D-01 と同じ）
    - ファイル内に `degraded_checks_count` を含む（WR-05）
    - ファイル内に `unresolved_fraction` と `unresolved_threshold` を含む（W3・SC#3 明示報告・_check_label_status_distribution の detail に格納）
    - ファイル内に `_compute_race_level_agreement` と `sample_pct` を含む（Open Question #3 実装）
    - ファイル内に 'password' / 'dsn=' / 'secret' のリテラルを含まない（T-02-02 security・各 check dict の detail には認証情報を格納しない）
    - `scripts/run_label_reconcile.py` が存在し `from src.etl.label_reconcile import reconcile_against_payout` を含む
    - `scripts/run_label_reconcile.py` に `dsn_masked` を含む（認証情報マスク・T-01-01）
    - `scripts/run_label_reconcile.py` に `verdict` と exit code 分岐（0 or 1）を含む
  </acceptance_criteria>

  <done>src/etl/label_reconcile.py が実装され Plan 04 Task 1 の14テストが全て GREEN。reconcile_against_payout が6検査 BLOCK + 1量化 INFO（unresolved_fraction 含む・W3）+ agreement + verdict + degraded_checks_count を返す。scripts/run_label_reconcile.py が CLI から実行可能。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 実DB で reconcile を実行し verdict=pass と >99.9% agreement を確認</name>
  <files>scripts/run_label_reconcile.py</files>

  <what-built>
    src/etl/label_reconcile.py が実装され、Plan 04 Task 1 の13テスト（unit + integration）が GREEN。ただし実DB の 553,891 馬行に対する reconcile 実行と >99.9% agreement 実測値は未確認。
  </what-built>

  <how-to-verify>
    1. Plan 03 の label ETL が既に実行済み（label.fukusho_label が 553,891 行前後で populated）であることを確認
    2. `uv run python scripts/run_label_reconcile.py` を実行:
       - masked DSN ログ出力
       - 各 check の name / passed / severity / count が INFO ログに出る
       - `verdict: pass` を期待（実DB では §10.5 検査1-6 が全て passed=True のはず・実測 drift 7行は dead_heat status で正常処理）
       - `agreement.agreement_pct` が 99.9 以上（実測 99.9987% のはず）
       - `agreement.agree_count` / `agreement.total_held_out` が出る
       - exit code 0
    3. agreement が 99.9% 未満だった場合・または verdict='fail' だった場合:
       - 各 check の detail を確認・どの検査が passed=False かを特定
       -実DB のラベル生成ロジック（Plan 03）にバグがある可能性・SUMMARY の issues に記録して Plan 03 に差し戻し
    4. 結果の JSON を `reports/02-label-reconcile-{YYYYMMDD}.json` に保存（オプション・後続分析用・gitignore 対象）
    5. unit test 再確認: `uv run pytest tests/test_label_reconcile.py -x` が13 passed
  </how-to-verify>

  <resume-signal>Type "approved" または verdict / agreement_pct / 各 check の count の実測値を貼付。verdict=fail の場合は原因を記載。</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| readonly pool → label.fukusho_label + public.n_harai | 読取境界・両テーブルを SELECT JOIN で比較。書込は一切発行しない |
| check dict detail → downstream（JSON / ログ） | 認証情報非含有境界・T-02-02 |
| checkpoint:human-verify（Task 3） | 実DB reconcile 実行境界・`human_verify_mode=end-of-phase` から逸脱（W5・理由: 実DB against の reconcile と held-out 10% agreement 検証は agent 自律完了不可・VALIDATION.md に理由明記） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-15 | Tampering | ラベル改ざんの silent flow through | mitigate | §10.5 6検査 BLOCK で不整合を unresolved / is_model_eligible=False に隔離。reconcile_against_payout の verdict='fail' で CI / gate が止まる（D-02・Phase 1 D-01 踏襲） |
| T-02-16 | Information Disclosure | check dict detail への DSN/password 混入 | mitigate | T-02-02: 各 check dict は name/passed/severity/detail のみ。detail に DSN/password/secret 等の文字列を含めない。scripts/run_label_reconcile.py は dsn_masked のみ INFO ログ出力（T-01-01）。test_no_auth_info_in_check_detail で検証 |
| T-02-17 | Tampering | agreement 計算の後知恵バイアス（test set への overfit） | mitigate | Open Question #3 の時系列ホールドアウト（最新10%）+ 層化で未来データ混入を防止。レース単位の馬集合完全一致で precision/recall 両方を検証。dead_heat レースも層に明示的に含める |
| T-02-18 | Repudiation | degraded INFO check の silent error | mitigate | WR-05: degraded_checks_count で INFO check の query 例外を可視化。verdict は pass のままでも downstream で監視可能（Phase 1 quality_gate.py:585-611 と同じ） |
| T-02-19 | Tampering | raw/normalized テーブルへの reconcile による誤書込 | mitigate | reconcile は readonly pool で SELECT のみ。`UPDATE label.fukusho_label` / `INSERT INTO label` 等の書込 SQL を含まない（reconcile は監査専用・Phase 1 raw_fingerprint.py と同じ read-only helper 性質） |
| T-02-SC | Tampering | パッケージインストール | accept | 新規外部パッケージインストール無し |
</threat_model>

<verification>
- `uv run pytest tests/test_label_reconcile.py -x` が14 passed（Plan 04 Task 1 GREEN・W3 含む）
- `uv run python scripts/run_label_reconcile.py` が exit 0（verdict='pass' を期待）
- agreement_pct >= 99.9（実測 99.9987% を期待・SC#2 直接証明）
- `SELECT count(*) FROM label.fukusho_label WHERE fukusho_hit_validated=1 AND label_validation_status IS NULL` が 0（全 validated 行に status 付与・Plan 03 と連動）
- `ruff check src/etl/label_reconcile.py scripts/run_label_reconcile.py tests/test_label_reconcile.py` が clean
</verification>

<success_criteria>
- SC#2: 払戻テーブル突合検査が >99.9% agreement で通過（時系列ホールドアウト + 層化・レース単位馬集合完全一致）
- SC#3: `unresolved` 割合が `_check_label_status_distribution` で `unresolved_fraction` として明示報告され、閾値 1% 以下（実測 ≈0.07%）であることが検証される（W3・sales_start_entry_count の unresolved も本検査で可視化）
- LABEL-03: §10.5 の6検査が全て BLOCK severity で実装され、不整合行が unresolved / is_model_eligible=False に隔離される
- D-02: 構造的欠陥 = BLOCK（verdict='fail'）/ 量的ドリフト = INFO（参考レポート）の分離が Phase 1 D-01 と一貫
- WR-05: degraded_checks_count で INFO check の silent error を可視化
- T-02-02: check dict に認証情報を含めない（セキュリティ・ASVS V8）
</success_criteria>

<output>
Create `.planning/phases/02-fukusho-labels/02-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

### 新規シンボル・関数（本 plan が作成）

**関数（src/etl/label_reconcile.py）:**
- `reconcile_against_payout(cur: Cursor) -> dict[str, Any]`
- `_check_payout_precision(cur: Cursor) -> CheckResult`
- `_check_payout_recall(cur: Cursor) -> CheckResult`
- `_check_dead_heat_integrity(cur: Cursor) -> CheckResult`
- `_check_no_scratch_mislabeled(cur: Cursor) -> CheckResult`
- `_check_dead_loss_not_excluded(cur: Cursor) -> CheckResult`
- `_check_no_fukusho_sale_not_in_training(cur: Cursor) -> CheckResult`
- `_check_label_status_distribution(cur: Cursor) -> CheckResult`（INFO・量化レポート）
- `_compute_race_level_agreement(cur: Cursor, sample_pct: float = 0.1) -> dict[str, Any]`

**再利用（src/etl/quality_gate.py から import）:**
- `CheckResult` dataclass（name / passed / severity / detail）

**新規テスト関数（tests/test_label_reconcile.py）:**
- `test_reconcile_module_imports`
- `test_block_info_separation`
- `test_check_payout_precision`
- `test_check_payout_recall`
- `test_check_dead_heat_integrity`
- `test_check_no_scratch_mislabeled`
- `test_check_dead_loss_not_excluded`
- `test_check_no_fukusho_sale_not_in_training`
- `test_gt_999_pct_agreement`（@pytest.mark.requires_db）
- `test_verdict_fail_when_block_failed`
- `test_verdict_pass_when_all_block_pass`
- `test_degraded_checks_count`
- `test_no_auth_info_in_check_detail`
- `test_unresolved_fraction_reported_in_status_distribution`（W3・SC#3）

**ヘルパー（tests/test_label_reconcile.py 内・private）:**
- `_mock_cursor(fetch_map)`（test_quality_gate.py:33-61 と同じパターンを複製）

**戻り値 dict 構造（reconcile_against_payout）:**
- `{verdict: "pass"|"fail", checks: [...], degraded_checks_count: int, agreement: {agreement_pct: float, agree_count: int, total_held_out: int, disagree_races: [...]}}`

**新規スクリプト:**
- `scripts/run_label_reconcile.py`（CLI・readonly_cur で実行・verdict で exit code 0 or 1）
