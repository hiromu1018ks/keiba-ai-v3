---
phase: 02-fukusho-labels
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_fukusho_label.py
autonomous: true
requirements: [LABEL-01, LABEL-02, LABEL-04]
tags: [phase-02, labels, tdd, unit-test, mock-cursor, edge-cases, reviews-rev1]

must_haves:
  truths:
    - "test_fukusho_label.py が存在し LABEL-01/02/04 + W4 複勝特払（tokubaraiflag2）の各シナリオを unit test として網羅する"
    - "fukusho_hit_raw と fukusho_hit_validated の drift が dead_heat 境界のみで発生することを検証するテストが存在"
    - "sales_start_entry_count = n_harai.TorokuTosu かつ sales_start_entry_count_source='torokutosu_proxy' / sales_start_entry_count_confidence='inferred' が label_validation_status と独立して格納されることを検証（REVIEWS HIGH #1）"
    - "D-04 の5エッジ（同着/取消/競走中止/レース全体中止/複勝不成立）+ W4 複勝特払（tokubaraiflag2 対象馬あり/なし 2シナリオ）の各ケースで label_validation_status と is_model_eligible を assert するテストが存在"
    - "D-03 §7.2 適格性ルール（障害/新馬/複勝発売なし/class_below_minimum/unresolved + 未勝利 syubetucd precision）を網羅するテストが存在"
    - "marker canonicalization（raw '999.0' 文字列 / 999.0 数値キャスト後 が同一判定）を両表現で assert するテストが存在（REVIEWS HIGH #5）"
    - "race_cancelled (DataKubun='9') の SE 行が _select_se_state で落とされずラベル生成対象になることを assert するテストが存在（REVIEWS HIGH #4）"
    - "全 unit test が mock cursor / 合成 DataFrame を使用し DB 接続不要（@pytest.mark.requires_db 付与対象を最小化）"
  artifacts:
    - path: "tests/test_fukusho_label.py"
      provides: "LABEL-01/02/04 unit test・mock cursor・合成 DataFrame シナリオ・marker canonicalization 両表現検証"
      contains: "test_drift_is_dead_heat_only"
      min_lines: 280
  key_links:
    - from: "tests/test_fukusho_label.py"
      to: "src/etl/fukusho_label.py (Plan 03 で作成)"
      via: "compute_fukusho_labels / classify_status / compute_is_model_eligible / _canonicalize_markers 関数を直接 import"
      pattern: "from src.etl.fukusho_label import"
    - from: "tests/test_fukusho_label.py"
      to: "src/etl/label_reconcile.py (Plan 04 で作成・参照しない)"
      via: "（参照しない・Plan 04 の test_label_reconcile.py が独立）"
      pattern: "（該当なし）"
---

<objective>
LABEL-01/02/04 を網羅する unit test ファイル `tests/test_fukusho_label.py` を TDD RED フェーズで作成する。本 plan の時点では `src/etl/fukusho_label.py`（Plan 03）が未実装のため、全テストが ImportError または AttributeError で fail する状態（RED）を作る。Plan 03 の GREEN 実装がこれらのテストを通す形で進む。

Purpose: ラベルは予測目標の正であり、実DBの実測分布（drift 7行・取消956・中止3554・全体中止376・dead_heat 97）を synthetic DataFrame で再現した unit test を先に書くことで、Plan 03 実装が silent mislabeling に陥るのを防ぐ。RESEARCH Validation Architecture の Wave 0 gap を閉じる。

**Reviews REV1 対応（本 plan が扱う HIGH 懸念）:**
- **HIGH #1 (inferred conflation):** `test_sales_start_entry_count_proxy` を拡張し、`label_validation_status`（HR DataKubun='2' → 'validated'）と `sales_start_entry_count_confidence='inferred'`（TorokuTosu 代理値）が**独立した列**に格納されることを assert するテストを追加。
- **HIGH #4 (race-cancelled rows dropped):** `test_race_cancelled_all_unresolved` を強化し、`_select_se_state` の SQL が `datakubun IN ('7','9')` で SELECT すること（regression テスト: SQL 文字列 grep）を追加。合成 DataFrame でも DataKubun='9' の SE 行が compute_fukusho_labels に入力されることを assert。
- **HIGH #5 (brittle markers):** marker canonicalization の両表現テストを追加: `test_canonicalize_markers_raw_string_form`（'999.0'/'000' 文字列）+ `test_canonicalize_markers_numeric_cast_form`（999.0/0 数値）が同一の is_dead_loss/is_scratch_cancel/is_race_cancelled 判定を返す。

Output: テストファイル1つ（LABEL-01/02/04 の合成シナリオ・mock cursor・pure transform・marker canonicalization 両表現）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@$HOME/.claude/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-fukusho-labels/02-CONTEXT.md
@.planning/phases/02-fukusho-labels/02-RESEARCH.md
@.planning/phases/02-fukusho-labels/02-PATTERNS.md
@.planning/phases/02-fukusho-labels/02-REVIEWS.md
@.planning/phases/02-fukusho-labels/02-01-PLAN.md
@tests/conftest.py
@tests/test_quality_gate.py
@tests/test_normalized_etl.py
</context>

<tasks>

<task type="tdd" tdd_phase="red">
  <name>Task 1: LABEL-01/02 unit test シナリオ（raw vs validated drift・sales_start_entry_count・payout 境界・HIGH #1 source/confidence 分離・HIGH #5 canonicalization）</name>
  <files>tests/test_fukusho_label.py</files>

  <behavior>
    - Test test_fukusho_module_imports: src.etl.fukusho_label から compute_fukusho_labels / classify_status / compute_is_model_eligible / _canonicalize_markers / run_label_etl が import 可能
    - Test test_raw_vs_validated_basic_8_horses: 8頭レース・KakuteiJyuni=1,2,3 は fukusho_hit_raw=1・KakuteiJyuni=4..8 は fukusho_hit_raw=0・PayFukusyoUmaban1..3 と整合して fukusho_hit_validated=1 の馬が3頭
    - Test test_raw_vs_validated_basic_6_horses: 6頭レース・payout_places=2・KakuteiJyuni=1,2 が raw=1・PayFukusyoUmaban1..2 と整合
    - Test test_drift_is_dead_heat_only: 合成シナリオで KakuteiJyuni-based raw と PayFukusyoUmaban-based validated の不一致を発生させ、不一致行が全て dead_heat status（label_validation_status='dead_heat'）に分類されることを assert。drift 行の fukusho_payout_places を超えて PayFukusyoUmaban4/5 に馬番が存在する（同着拡張）
    - Test test_sales_start_entry_count_proxy_and_source_confidence_separated_from_status (**REVIEWS HIGH #1**): n_harai.TorokuTosu='8' のレースで sales_start_entry_count=8 / sales_start_entry_count_source='torokutosu_proxy' / sales_start_entry_count_confidence='inferred' を assert。**加えて** 同レースの label_validation_status は HR DataKubun='2' のため 'validated' であること（inferred ではない）を assert。両者が独立した列に格納されることを証明。
    - Test test_unresolved_triggers_hr_missing: HR レコードが欠損したレース（torokutosu IS NULL）の馬行は label_validation_status='unresolved' かつ is_model_eligible=False
    - Test test_no_fukusho_sale_under_5_horses: TorokuTosu='4' のレースは is_fukusho_sale_available=False・fukusho_payout_places=0・is_model_eligible=False（no_fukusho_sale）
    - Test test_payout_places_uses_torokutosu_not_syussotosu: SyussoTosu='7' だが TorokuTosu='8' のレース（取消1頭）は fukusho_payout_places=3（Pitfall 3・SyussoTosu で境界を決めない）
    - Test test_canonicalize_markers_raw_string_form (**REVIEWS HIGH #5**): harontimel3='999.0'（文字列）/ timediff='9999' / time='9990.0' / bataijyu='000' / datakubun='9' の合成行で _canonicalize_markers が正しく is_dead_loss / is_scratch_cancel / is_race_cancelled を判定することを assert。
    - Test test_canonicalize_markers_numeric_cast_form (**REVIEWS HIGH #5**): 同シナリオで raw 値を pandas.to_numeric で数値キャストした後（harontimel3=999.0 float / timediff=9999 int / time=9990.0 float / bataijyu=0 int / datakubun='9' string のまま）でも _canonicalize_markers が同一の is_dead_loss / is_scratch_cancel / is_race_cancelled を返すことを assert（文字列表現と数値表現で同一結果）。
  </behavior>

  <read_first>
    - tests/test_quality_gate.py:11-26（import パターン・private 関数直接 import 慣行）
    - tests/test_quality_gate.py:33-61（_mock_cursor ファクトリ・fetch_map 部分文字列マッチ・MagicMock 構築）
    - tests/test_normalized_etl.py:20-48（module signature test の idiom・callable assert）
    - tests/conftest.py:22-78（settings/pg_pool/readonly_cur/write_cur fixture・KEIBA_SKIP_DB_TESTS policy）
    - .planning/phases/02-fukusho-labels/02-RESEARCH.md:255-296（Pattern 2 compute_fukusho_labels 実測ロジック）
    - .planning/phases/02-fukusho-labels/02-RESEARCH.md:353-398（Anti-Patterns・Pitfall 3 SyussoTosu 禁止）
    - .planning/phases/02-fukusho-labels/02-PATTERNS.md:240-269（test_fukusho_label.py の構造指針・fixture 再利用）
    - .planning/phases/02-fukusho-labels/02-REVIEWS.md:37-50（HIGH #1/#5 詳細・suggestions）
    - src/config/label_spec.yaml（Plan 01 で作成・label_generation_version・payout_places_rules・se_marker_rules・se_marker_canonicalization・sales_start_entry_count.source_confidence を参照値として使用）
  </read_first>

  <action>
    `tests/test_quality_gate.py:11-26` と `tests/test_normalized_etl.py:20-26` の import・signature test パターンを踏襲して `tests/test_fukusho_label.py` を新規作成。

    **ファイルヘッダ**: docstring に「LABEL-01/02/04 unit test・RESEARCH Validation Architecture 準拠・mock cursor と合成 DataFrame で DB 不要・@pytest.mark.requires_db は integration test のみ・REVIEWS HIGH #1/#4/#5/#6 対応」と明記。`from __future__ import annotations` / `import pandas as pd` / `import pytest` / `from unittest.mock import MagicMock` を import。

    **共通 helper・合成 DataFrame ビルダー** をファイル先頭に定義（test_quality_gate.py:33-61 の _mock_cursor を参考にしつつ、本ファイルは純粋な合成 DataFrame でテストするため mock cursor は最小限）:
    - `_build_hr_row(**overrides) -> dict`: n_harai 1レース分の辞書を返す。デフォルト: `year='2023', monthday='0101', jyocd='05', kaiji='01', nichiji='01', racenum='01', datakubun='2', torokutosu='8', syussotosu='8', fuseirituflag2='0', tokubaraiflag2='0', henkanflag2='0', payfukusyounmaban1='01', payfukusyounmaban2='02', payfukusyounmaban3='03', payfukusyounmaban4='', payfukusyounmaban5=''`
    - `_build_se_row(umaban: int, **overrides) -> dict`: n_uma_race 1馬行分。デフォルト: `year='2023', monthday='0101', jyocd='05', kaiji='01', nichiji='01', racenum='01', umaban=f'{umaban:02d}', kettonum=f'{umaban:03d}', kakuteijyuni=f'{umaban:02d}', bataijyu='057', harontimel3='1.0', timediff='0', time='90.0', datakubun='7', dochachukubun='0', dochachutosu='0'`（time は pandas float で扱う前提）
    - `_build_label_input_df(n_horses: int, hr_overrides: dict | None = None, se_overrides: dict | None = None, syubetucd: str = '00', class_level_numeric: int = 2) -> tuple[pd.DataFrame, pd.DataFrame, pd.DataFrame]`: (hr_df, se_df, race_df) を返す。`n_horses` で1..N の SE 行を生成・race_df は syubetucd / class_level_numeric を含む1行の normalized.n_race 相当

    **test_fukusho_module_imports**: `from src.etl import fukusho_label` を import して `callable(fukusho_label.compute_fukusho_labels)` / `callable(fukusho_label.classify_status)` / `callable(fukusho_label.compute_is_model_eligible)` / `callable(fukusho_label._canonicalize_markers)` / `callable(fukusho_label.run_label_etl)` を assert（Plan 03 が未実装なので ImportError で fail = RED）。

    **test_raw_vs_validated_basic_8_horses**: `_build_label_input_df(8)` で8頭レース構築。`compute_fukusho_labels(hr_df, se_df, race_df)` を呼出。結果 df で:
    - umaban 1,2,3 の `fukusho_hit_raw == 1`
    - umaban 4..8 の `fukusho_hit_raw == 0`
    - umaban 1,2,3 の `fukusho_hit_validated == 1`（PayFukusyoUmaban1..3='01','02','03' と整合）
    - 全行の `fukusho_payout_places == 3`
    - 全行の `is_fukusho_sale_available == True`（torokutosu=8 >= 5・fuseirituflag2='0'）

    **test_raw_vs_validated_basic_6_horses**: `_build_label_input_df(6)` で6頭。`hr_overrides={'torokutosu':'6','syussotosu':'6','payfukusyounmaban3':''}` を渡して payout_places=2 を検証。

    **test_drift_is_dead_heat_only**: dead_heat シナリオを合成: hr 行で `payfukusyounmaban1..4='01','02','03','04'`（4頭・理論値3を超える）かつ `torokutosu='8'`。SE 側で KakuteiJyuni=1,2,3 の3頭のみ raw=1 になるが、validated は4頭=1（PayFukusyoUmaban1..4 に属する）→ umaban 4 の馬が drift（raw=0, valid=1）。このレースの全馬行の `label_validation_status == 'dead_heat'` を assert（Plan 03 GREEN で classify_status が dead_heat を返す）。

    **test_sales_start_entry_count_proxy_and_source_confidence_separated_from_status (REVIEWS HIGH #1)**: hr 行 `torokutosu='12', datakubun='2'` の8頭レース（SE KakuteiJyuni=1..8、PayFukusyoUmaban1..3='01','02','03'）。compute_fukusho_labels 結果で:
    - 全行 `sales_start_entry_count == 12`
    - 全行 `sales_start_entry_count_source == 'torokutosu_proxy'`
    - **全行 `sales_start_entry_count_confidence == 'inferred'`（HIGH #1: 独立列）**
    - **全行 `label_validation_status == 'validated'`（HR DataKubun='2'・status は inferred ではない・HIGH #1: conflation 無し）**
    - assert: `sales_start_entry_count_confidence` という列名が結果 df.columns に含まれる（label_validation_status とは別列）

    **test_unresolved_triggers_hr_missing**: hr 行を空にする（HR レコード欠損）。SE 8頭に対し compute_fukusho_labels に空の hr_df を渡す。結果で全馬行の `label_validation_status == 'unresolved'` かつ `is_model_eligible == False`（D-03・reason='unresolved'）を assert。

    **test_no_fukusho_sale_under_5_horses**: hr 行 `torokutosu='4'`・`payfukusyounmaban1='00'`（発売なしマーカー）・SE 4頭。結果で `is_fukusho_sale_available == False`・`fukusho_payout_places == 0`・`is_model_eligible == False`・`ineligibility_reason == 'no_fukusho_sale'` を assert。

    **test_payout_places_uses_torokutosu_not_syussotosu**: hr 行 `torokutosu='8', syussotosu='7'`（取消1頭反映済み）・SE 8頭。`fukusho_payout_places == 3`（SyussoTosu=7 で2にならない・Pitfall 3）を assert。

    **test_canonicalize_markers_raw_string_form (REVIEWS HIGH #5)**: 合成 SE 行を raw varchar として構築:
    - 行A: `harontimel3='999.0', timediff='9999', time='9990.0', bataijyu='057', datakubun='7'`（競走中止・raw 文字列）
    - 行B: `harontimel3='1.0', timediff='0', time='90.0', bataijyu='000', datakubun='7'`（出走取消・raw 文字列）
    - 行C: `harontimel3='1.0', timediff='0', time='90.0', bataijyu='057', datakubun='9'`（レース全体中止・raw 文字列）
    se_df として _canonicalize_markers(df, spec) を呼出（Plan 03 GREEN で実装）。結果で:
    - 行A: `is_dead_loss == True` / `is_scratch_cancel == False` / `is_race_cancelled == False`
    - 行B: `is_dead_loss == False` / `is_scratch_cancel == True` / `is_race_cancelled == False`
    - 行C: `is_race_cancelled == True` / `is_dead_loss == False` / `is_scratch_cancel == False`

    **test_canonicalize_markers_numeric_cast_form (REVIEWS HIGH #5)**: 同シナリオで raw 値を `pd.to_numeric(errors='coerce')` で数値化した DataFrame を構築:
    - 行A: `harontimel3=999.0 (float), timediff=9999 (int), time=9990.0 (float), bataijyu=57 (int・'057' をキャスト), datakubun='7' (str のまま)`
    - 行B: `harontimel3=1.0, timediff=0, time=90.0, bataijyu=0 (int・'000' をキャスト), datakubun='7'`
    - 行C: `harontimel3=1.0, timediff=0, time=90.0, bataijyu=57, datakubun='9'`
    _canonicalize_markers(df, spec) の結果が test_canonicalize_markers_raw_string_form と同一（行A/B/C の is_dead_loss / is_scratch_cancel / is_race_cancelled が全て一致）であることを assert。これが sentinel 集合ベース判定の核心（HIGH #5）。

    各テストは純粋関数 `compute_fukusho_labels(hr_df, se_df, race_df, spec=...)` / `_canonicalize_markers(df, spec)` を直接呼び DB 不要。`@pytest.mark.requires_db` は付けない（unit test）。Plan 03 GREEN まで全テスト ImportError/AttributeError で fail = RED 状態を維持。
  </action>

  <verify>
    <automated>test -f tests/test_fukusho_label.py &amp;&amp; uv run pytest tests/test_fukusho_label.py --collect-only 2>&amp;1 | head -40 | grep -q "test_raw_vs_validated_basic_8_horses" &amp;&amp; uv run pytest tests/test_fukusho_label.py --collect-only 2>&amp;1 | grep -q "test_canonicalize_markers_numeric_cast_form" &amp;&amp; uv run pytest tests/test_fukusho_label.py --collect-only 2>&amp;1 | grep -q "test_sales_start_entry_count_proxy_and_source_confidence_separated_from_status"</automated>
  </verify>

  <acceptance_criteria>
    - `tests/test_fukusho_label.py` が存在する
    - `uv run pytest tests/test_fukusho_label.py --collect-only` が上記10テスト関数名を収集対象として出力する（test_fukusho_module_imports / test_raw_vs_validated_basic_8_horses / test_raw_vs_validated_basic_6_horses / test_drift_is_dead_heat_only / test_sales_start_entry_count_proxy_and_source_confidence_separated_from_status / test_unresolved_triggers_hr_missing / test_no_fukusho_sale_under_5_horses / test_payout_places_uses_torokutosu_not_syussotosu / test_canonicalize_markers_raw_string_form / test_canonicalize_markers_numeric_cast_form）
    - この時点で `uv run pytest tests/test_fukusho_label.py -x` が全テスト fail または error（ImportError・AttributeError・ModuleNotFoundError）になる（RED・src.etl.fukusho_label 未実装のため）
    - ファイル内に `_build_hr_row` / `_build_se_row` / `_build_label_input_df` の3つのヘルパー関数が定義される
    - `from src.etl.fukusho_label import` を含む（Plan 03 GREEN 実装の契約・`_canonicalize_markers` も含む）
    - `@pytest.mark.requires_db` マークが付与されていない（unit test・DB 不要・RESEARCH Validation Architecture 準拠）
    - `KakuteiJyuni` ベースと `PayFukusyoUmaban1..5` ベースの両方の検証を含む（LABEL-01 2層ラベル要件）
    - **HIGH #1:** `test_sales_start_entry_count_proxy_and_source_confidence_separated_from_status` が `sales_start_entry_count_confidence` と `label_validation_status` の両方を独立して assert する
    - **HIGH #5:** `test_canonicalize_markers_raw_string_form` と `test_canonicalize_markers_numeric_cast_form` の両方が存在し、同一シナリオで同一結果を期待する
  </acceptance_criteria>

  <done>tests/test_fukusho_label.py が作成され10テストが collection される。実行すると ImportError/AttributeError で RED（src.etl.fukusho_label が Plan 03 で実装されるまで）。LABEL-01/02 の合成シナリオ・Pitfall 3 反証テスト・HIGH #1 source/confidence 分離・HIGH #5 marker canonicalization 両表現を網羅。</done>
</task>

<task type="tdd" tdd_phase="red">
  <name>Task 2: LABEL-04 エッジケース + D-03 §7.2 適格性 unit test（5エッジ×7適格性理由・HIGH #4 race_cancelled rows 検証・HIGH #6 dead_loss 除外理由 precision・未勝利 precision）</name>
  <files>tests/test_fukusho_label.py</files>

  <behavior>
    - Test test_dead_heat_all_payout_positive: 同着で PayFukusyoUmaban1..4 に4馬番存在するレース・fukusho_payout_places=3 の場合、4頭全て fukusho_hit_validated=1 かつ label_validation_status='dead_heat' かつ is_model_eligible=True
    - Test test_scratch_cancel_excluded: SE bataijyu='000' の馬行は is_scratch_cancel=True・label_validation_status='validated'（outcomeは確定・ただし予測対象外）・is_model_eligible=False・ineligibility_reason='race_or_horse_cancelled'
    - Test test_dead_loss_in_training: SE harontimel3='999.0' AND timediff='9999' AND time='9990.0'（time 有り）の馬行は is_dead_loss=True・fukusho_hit_validated=0・fukusho_hit_raw=0・label_validation_status='validated'・is_model_eligible=True（学習に残して0・§10.6）
    - Test test_dead_loss_in_obstacle_race_excluded_for_obstacle_reason (**REVIEWS HIGH #6**): 障害レース（syubetucd='18'）で競走中止（is_dead_loss=True）の馬行は is_model_eligible=False だが、ineligibility_reason='obstacle'（dead_loss が理由ではない）。compute_is_model_eligible の適用順序で syubetucd 障害が先に評価されることを assert。
    - Test test_race_cancelled_all_unresolved (**REVIEWS HIGH #4 強化**): HR/SE datakubun='9' のレースの全馬行は is_race_cancelled=True・label_validation_status='unresolved'・is_model_eligible=False。**加えて** Plan 03 の `_select_se_state` が SQL `datakubun IN ('7','9')` を含むことをファイル grep ベースの regression test で assert（regression・race_cancelled rows が SELECT から落とされない保証）。
    - Test test_select_se_state_includes_datakubun_9 (**REVIEWS HIGH #4・regression**): `src.etl.fukusho_label` モジュールのソースを `inspect.getsource` で取得し、`datakubun` に対する WHERE 句が `'7'` と `'9'` の両方を含むことを assert（例: `datakubun IN ('7', '9')` または `datakubun IN ('7','9')`）。`datakubun = '7'` 単独は HIGH #4 違反で fail。
    - Test test_fuseiritu_flag2_unresolved: HR fuseirituflag2='1' のレースの全馬行は label_validation_status='unresolved'・is_model_eligible=False（将来DB更新用・D-13 保持）
    - Test test_tokubaraiflag2_with_payout_validated: HR tokubaraiflag2='1'（複勝特払）かつ PayFukusyoUmaban1..3 に馬番が存在する（payout_umaban_set 非空）レースの該当馬は label_validation_status='validated'・is_model_eligible=True（D-04・実DB0件だが分岐保持）
    - Test test_tokubaraiflag2_without_payout_unresolved: HR tokubaraiflag2='1'（複勝特払）かつ PayFukusyoUmaban1..5 が全て空/'00'（payout_umaban_set 空）のレースの全馬行は label_validation_status='unresolved'・is_model_eligible=False（D-04・D-13 silent fallback 禁止）
    - Test test_is_model_eligible_obstacle_syubetucd: normalized.n_race syubetucd='18' のレースの全馬行は is_model_eligible=False・ineligibility_reason='obstacle'（§7.3）
    - Test test_is_model_eligible_newcomer_syubetucd: syubetucd='11'（2歳新馬）は is_model_eligible=False・reason='newcomer'
    - Test test_is_model_eligible_maiden_syubetucd_included (**REVIEWS MEDIUM open question 解決**): syubetucd='13'（2歳未勝利・class_level_numeric=0）は §7.2「2歳未勝利以上」により is_model_eligible=True・ineligibility_reason IS None（class_level_numeric=0 でも syubetucd が maiden_syubetucd に含まれるため適格）
    - Test test_is_model_eligible_class_below_minimum: class_level_numeric=0 かつ syubetucd NOT IN maiden_syubetucd（例: class_level_numeric=0 で新馬扱いだが syubetucd='99' の異常ケース）は is_model_eligible=False・reason='class_below_minimum'
    - Test test_is_model_eligible_validated_normal: 通常レース（障害/新馬でない・torokutosu>=5・class_level_numeric>=1・datakubun='2'・bataijyu!='000'・marker なし）の馬行は is_model_eligible=True・ineligibility_reason IS NULL
    - Test test_dochachukubun_dead_heat_detection: SE で dochachutosu='1'（同着）の馬を2頭用意。compute_fukusho_labels 結果で `is_dead_heat == True` または `label_validation_status == 'dead_heat'` になることを検証（実測 dead_heat=97レース相当・slot4 使用）。**ただし payout slot4/5 非使用で DochacoTosu='1' のみの場合は dead_heat にならないことも assert（payout-table authoritative・MEDIUM #2）**
  </behavior>

  <read_first>
    - tests/test_fukusho_label.py（Task 1 で作成・同じヘルパー _build_hr_row / _build_se_row / _build_label_input_df を再利用）
    - .planning/phases/02-fukusho-labels/02-RESEARCH.md:300-334（Pattern 3 classify_status 実測ロジック）
    - .planning/phases/02-fukusho-labels/02-RESEARCH.md:405-412（Pitfall 4 SE marker 識別条件表・実カラム名 bataijyu/harontimel3/timediff/time）
    - .planning/phases/02-fukusho-labels/02-RESEARCH.md:522-557（compute_is_model_eligible 実装例・syubetucd 実測値）
    - .planning/phases/02-fukusho-labels/02-CONTEXT.md:44-51（D-04 locked decision・5エッジ観測事実ベース分類）
    - .planning/phases/02-fukusho-labels/02-CONTEXT.md:36-42（D-03 is_model_eligible §7.2 + status 写像）
    - .planning/phases/02-fukusho-labels/02-REVIEWS.md:45-48, 53-61, 89（HIGH #4 race_cancelled DataKubun='9' / HIGH #6 dead_loss reason precision / MEDIUM §7.2 未勝利）
    - src/config/label_spec.yaml（Plan 01・se_marker_rules / se_marker_canonicalization / model_ineligibility_syubetucd / newcomer_syubetucd / class_eligibility.maiden_syubetucd / se_datakubun_inclusion を参照）
    - src/config/class_normalization.yaml:23-48（jyokencd5_map・class_level_numeric=0 は新馬(701)/未勝利(703)・=1 は1勝クラス(005)）
    - docs/keiba_ai_requirements_v1.3.md §7.2-7.3（未勝利以上・新馬除外の正）
  </read_first>

  <action>
    Task 1 で作成した `tests/test_fukusho_label.py` に追記。同じヘルパー（`_build_hr_row` / `_build_se_row` / `_build_label_input_df`）を再利用し、LABEL-04 エッジケース（D-04 5状態）と D-03 §7.2 適格性の unit test を追加。

    **test_dead_heat_all_payout_positive**: `_build_label_input_df(8)` + hr_overrides=`{'payfukusyounmaban1':'01','payfukusyounmaban2':'02','payfukusyounmaban3':'03','payfukusyounmaban4':'04','payfukusyounmaban5':''}`。compute_fukusho_labels 結果で:
    - umaban 1,2,3,4 の4頭が全て `fukusho_hit_validated == 1`（PayFukusyoUmaban4='04' も対象）
    - 全行 `label_validation_status == 'dead_heat'`
    - 全行 `is_dead_heat == True`
    - 全行 `is_model_eligible == True`（dead_heat は学習対象・払戻テーブル優先）

    **test_scratch_cancel_excluded**: `_build_label_input_df(8)` で SE umaban=5 の bataijyu='000'・harontimel3='999.0'・timediff='9999'・time='0'（取消マーカー）。compute_fukusho_labels 結果で umaban=5 の行:
    - `is_scratch_cancel == True`
    - `label_validation_status` は 'validated'（HR DataKubun=2・outcome は確定している・ただし予測対象外）
    - `is_model_eligible == False`
    - `ineligibility_reason == 'race_or_horse_cancelled'`
    他の馬行は通常通り。

    **test_dead_loss_in_training**: SE umaban=7 で `bataijyu='054', harontimel3='999.0', timediff='9999', time='9990.0'`（time 有り＝発走後停止＝競走中止）。結果で umaban=7 の行:
    - `is_dead_loss == True`
    - `fukusho_hit_validated == 0`（中止馬は的中対象外）
    - `fukusho_hit_raw == 0`
    - `label_validation_status == 'validated'`（HR DataKubun=2・outcome 確定）
    - `is_model_eligible == True`（学習に残す・§10.6 除外禁止）

    **test_dead_loss_in_obstacle_race_excluded_for_obstacle_reason (REVIEWS HIGH #6)**: syubetucd='18'（障害）のレースで SE umaban=3 が `harontimel3='999.0', timediff='9999', time='9990.0'`（競走中止）。compute_fukusho_labels 結果で umaban=3 の行:
    - `is_dead_loss == True`
    - `is_model_eligible == False`（障害レースのため不適格）
    - **`ineligibility_reason == 'obstacle'`（HIGH #6: dead_loss が理由ではなく obstacle が理由・compute_is_model_eligible の適用順序で syubetucd が先に評価される）**
    - assert: `ineligibility_reason != 'dead_loss'` かつ `ineligibility_reason != 'dead_loss_only'`（dead_loss 単独では不適格理由にならない）

    **test_race_cancelled_all_unresolved (REVIEWS HIGH #4 強化)**: hr_overrides=`{'datakubun':'9'}`・SE 全行 datakubun='9'。結果で全8頭:
    - `is_race_cancelled == True`
    - `label_validation_status == 'unresolved'`
    - `is_model_eligible == False`
    - `ineligibility_reason == 'unresolved'`
    このテストは Plan 03 GREEN で compute_fukusho_labels が datakubun='9' の SE 行を処理することを前提とする（_select_se_state が落とさなければ OK）。

    **test_select_se_state_includes_datakubun_9 (REVIEWS HIGH #4・regression・ファイル grep)**: `import inspect` で `src.etl.fukusho_label._select_se_state` のソースを取得（Plan 03 GREEN 後に成功）。文字列パターン `datakubun IN ('7'` または `datakubun IN ('7',` または `datakubun IN ('7', '9'` などの variation を許容する正規表現で `'9'` が含まれることを assert。逆に `datakubun = '7'` や `datakubun='7'` 単独（'9' を含まない）の場合は `AssertionError` で fail。Plan 03 GREEN まで AttributeError/ImportError で RED。

    **test_fuseiritu_flag2_unresolved**: hr_overrides=`{'fuseirituflag2':'1','payfukusyounmaban1':'00','payfukusyounmaban2':'','payfukusyounmaban3':''}`。結果で全馬行:
    - `label_validation_status == 'unresolved'`
    - `is_model_eligible == False`
    （実DBでは発生0件だが将来DB更新用に分岐保持・D-13）

    **test_tokubaraiflag2_with_payout_validated**（W4 対応・D-04 複勝特払）: hr_overrides=`{'tokubaraiflag2':'1','fuseirituflag2':'0','payfukusyounmaban1':'01','payfukusyounmaban2':'02','payfukusyounmaban3':'03','payfukusyounmaban4':'','payfukusyounmaban5':''}`（複勝特払フラグ+対象馬3頭）・SE 8頭。compute_fukusho_labels 結果で:
    - umaban 1,2,3 の3頭が `fukusho_hit_validated == 1`（payout_umaban_set 非空・正当な正例）
    - umaban 1,2,3 の `label_validation_status == 'validated'`（D-04: 特払で対象馬あれば正当 outcome）
    - umaban 1,2,3 の `is_model_eligible == True`
    （実DBでは tokubaraiflag2='1' 0件・D-13 silent fallback 禁止で分岐保持）

    **test_tokubaraiflag2_without_payout_unresolved**（W4 対応・D-04・D-13）: hr_overrides=`{'tokubaraiflag2':'1','fuseirituflag2':'0','payfukusyounmaban1':'00','payfukusyounmaban2':'','payfukusyounmaban3':'','payfukusyounmaban4':'','payfukusyounmaban5':''}`（複勝特払フラグのみ・対象馬なし）・SE 8頭。compute_fukusho_labels 結果で全馬行:
    - `payout_umaban_set` が空集合（payout_count == 0）
    - `label_validation_status == 'unresolved'`（outcome 非確定・D-04）
    - `is_model_eligible == False`
    （classify_status の tokubaraiflag2=='1' 分岐が payout_set 空で unresolved を返すことを検証・Plan 02-03 の classify_status と契約）

    **test_is_model_eligible_obstacle_syubetucd**: `_build_label_input_df(8, syubetucd='18')`（障害）。結果で全馬行 `is_model_eligible == False`・`ineligibility_reason == 'obstacle'`。

    **test_is_model_eligible_newcomer_syubetucd**: 同上で syubetucd='11'（2歳新馬）。`is_model_eligible == False`・`reason == 'newcomer'`。

    **test_is_model_eligible_maiden_syubetucd_included (REVIEWS MEDIUM open question 解決)**: `_build_label_input_df(8, syubetucd='13', class_level_numeric=0)`（2歳未勝利・class_level_numeric=0）。compute_is_model_eligible の結果:
    - **`is_model_eligible == True`**（§7.2「2歳未勝利以上」により適格・class_level_numeric=0 でも syubetucd='13' が maiden_syubetucd に含まれるため）
    - `ineligibility_reason IS None`
    docstring に「REVIEWS MEDIUM 解決: class_level_numeric_minimum=1 と未勝利 class_level_numeric=0 の整合は syubetucd maiden list で解決。新馬(11,12)は newcomer_syubetucd で除外・未勝利(13,14)/条件戦(15)は maiden_syubetucd で適格。」と明記。

    **test_is_model_eligible_class_below_minimum**: compute_is_model_eligible に class_level_numeric=0 かつ syubetucd='99'（異常・未勝利でも新馬でもない）の行を渡し、`is_model_eligible == False`・`reason == 'class_below_minimum'` を assert。class_level_numeric=1（1勝クラス）・syubetucd='00' の場合は True（class_level_numeric_minimum=1・label_spec.yaml）。

    **test_is_model_eligible_validated_normal**: `_build_label_input_df(8, syubetucd='00', class_level_numeric=2)` の通常レース。任意の馬行で `is_model_eligible == True`・`ineligibility_reason IS None` を assert。

    **test_dochachukubun_dead_heat_detection**: SE で dochachutosu='1'（同着）の馬を2頭用意。compute_fukusho_labels 結果で `is_dead_heat == True` または `label_validation_status == 'dead_heat'` になることを検証（実測 dead_heat=97レース相当・slot4 使用）。**ただし payout slot4/5 非使用で DochacoTosu='1' のみの場合は dead_heat 判定にならないことも assert する（payout-table authoritative・MEDIUM #2 対応・DochacoTosu は参考値）**。

    全テスト純粋 unit test・DB 不要・`@pytest.mark.requires_db` 付けない（`test_select_se_state_includes_datakubun_9` は `inspect.getsource` のみ・DB 不要）。Plan 03 GREEN まで RED（ImportError/AttributeError）。
  </action>

  <verify>
    <automated>uv run pytest tests/test_fukusho_label.py --collect-only 2>&amp;1 | grep -c "test_" | awk '{if ($1 >= 24) print "OK"; else print "NG"}' | grep -q "^OK$"</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tests/test_fukusho_label.py --collect-only` が Task 1 の10テスト + Task 2 の15テスト = 計25テスト以上を収集
    - 収集されるテスト名に `test_dead_heat_all_payout_positive` / `test_scratch_cancel_excluded` / `test_dead_loss_in_training` / `test_dead_loss_in_obstacle_race_excluded_for_obstacle_reason` / `test_race_cancelled_all_unresolved` / `test_select_se_state_includes_datakubun_9` / `test_fuseiritu_flag2_unresolved` / `test_tokubaraiflag2_with_payout_validated` / `test_tokubaraiflag2_without_payout_unresolved` / `test_is_model_eligible_obstacle_syubetucd` / `test_is_model_eligible_newcomer_syubetucd` / `test_is_model_eligible_maiden_syubetucd_included` / `test_is_model_eligible_class_below_minimum` / `test_is_model_eligible_validated_normal` / `test_dochachukubun_dead_heat_detection` が全て含まれる
    - 実カラム名 `bataijyu`（NOT bataiju）/ `harontimel3` / `timediff`（NOT timedifn）/ `time` / `datakubun` / `fuseirituflag2` / `payfukusyounmaban1..5` が使用される（Pitfall 1）
    - `bataijyu='000'` で取消・`harontimel3='999.0' & timediff='9999' & time='9990.0'` で競走中止・`datakubun='9'` でレース全体中止・の3状態を区別する assert が存在
    - 競走中止馬が `is_model_eligible == True` かつ `fukusho_hit_validated == 0` になる assert が存在（§10.6・バックテスト除外禁止）
    - **HIGH #4:** `test_select_se_state_includes_datakubun_9` が存在し `_select_se_state` のソースに `datakubun IN ('7', '9')` 相当が含まれることを `inspect.getsource` で assert
    - **HIGH #6:** `test_dead_loss_in_obstacle_race_excluded_for_obstacle_reason` が存在し、競走中止+障害の馬行が `ineligibility_reason == 'obstacle'`（dead_loss 由来ではない）になることを assert
    - **MEDIUM 解決:** `test_is_model_eligible_maiden_syubetucd_included` が syubetucd='13' で `is_model_eligible == True` を assert（未勝利は §7.2 対象）
    - 取消馬が `is_model_eligible == False` になる assert が存在
    - この時点で全テストが RED（ImportError/AttributeError・src.etl.fukusho_label 未実装）
  </acceptance_criteria>

  <done>LABEL-04 の5エッジケース（同着/取消/中止/全体中止/不成立）+ W4 複勝特払（tokubaraiflag2）2シナリオ と D-03 §7.2 適格性の6理由（障害/新馬/複勝発売なし/class_below_minimum/unresolved/正常・未勝利 precision 含む）+ HIGH #4 race_cancelled rows regression + HIGH #6 dead_loss reason precision を網羅する unit test が追加され、計25テスト以上が collection される。全て RED 状態。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| テスト fixture → src.etl.fukusho_label 実装 | 合成 DataFrame が実DBに依存せず deterministic にラベル計算ロジックを検証 |
| mock cursor → 検証対象 | fetch_map 部分文字列マッチで合成 SELECT 結果を返す（DB 接続不要・CI で実行可能） |
| raw 文字列 vs numeric キャスト後表現 | **REVIEWS HIGH #5: sentinel 集合ベース判定が両表現で同一結果を返すことを unit test で保証** |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | LABEL-04 エッジケースの silent mislabeling | mitigate | 5状態（同着/取消/中止/全体中止/不成立）の合成シナリオを必ず網羅。実測識別条件（bataijyu='000' / harontimel3='999.0' & timediff='9999' & time有 / datakubun='9' / fuseirituflag2='1'）を hard-code した DataFrame を構築して assert |
| T-02-06 | Tampering | Pitfall 1 実カラム名誤用（timedifn / bataiju） | mitigate | テスト内で必ず `timediff` と `bataijyu` を使用。`timedifn` / `bataiju` を含む合成行があれば検知できるよう grep で file scan を avoidance check に含める |
| T-02-07 | Repudiation | drift 行が dead_heat 以外で発生した場合の未検知 | mitigate | test_drift_is_dead_heat_only で drift 行を全て dead_heat status に分類することを assert。将来のロジック変更で drift 行が別 status に流れた場合テストが fail |
| T-02-RV1 | Tampering | label_validation_status='inferred' と sales_start_entry_count_confidence='inferred' の conflation | mitigate | **REVIEWS HIGH #1:** test_sales_start_entry_count_proxy_and_source_confidence_separated_from_status で `sales_start_entry_count_confidence` が独立列・`label_validation_status` とは別値であることを assert |
| T-02-RV4 | Tampering | _select_se_state が datakubun='9' (race_cancelled) の SE 行を落とす silent data loss | mitigate | **REVIEWS HIGH #4:** test_select_se_state_includes_datakubun_9 で `_select_se_state` のソースが `datakubun IN ('7','9')` を含むことを regression assert。将来 `datakubun='7'` 単独に戻るとテストが fail |
| T-02-RV5 | Tampering | marker 列の raw '999.0' 文字列と numeric 999.0 で判定が変わる brittle 等価 | mitigate | **REVIEWS HIGH #5:** test_canonicalize_markers_raw_string_form と test_canonicalize_markers_numeric_cast_form の両者が同一シナリオで同一結果を assert。canonicalization sentinel 集合が両表現を許容 |
| T-02-RV6 | Tampering | 競走中止馬が dead_loss 単独で不適格にされ §10.6 違反 | mitigate | **REVIEWS HIGH #6:** test_dead_loss_in_obstacle_race_excluded_for_obstacle_reason で「dead_loss + 障害」の馬行の ineligibility_reason が 'obstacle' であることを assert・純粋 dead_loss 除外を検知するテストは Plan 04 側で実装 |
| T-02-SC | Tampering | パッケージインストール | accept | 新規外部パッケージインストール無し（pytest/pandas は Phase 1 依存） |
</threat_model>

<verification>
- `test -f tests/test_fukusho_label.py` 成功
- `uv run pytest tests/test_fukusho_label.py --collect-only` が25テスト以上を収集（W4 tokubaraiflag2 2件 + REVIEWS HIGH #1/#4/#5/#6 + MEDIUM 追加分含む）
- `uv run pytest tests/test_fukusho_label.py -x` が RED（ImportError または AttributeError・src.etl.fukusho_label が Plan 03 で実装されるまで）
- ファイル内に `from src.etl.fukusho_label import` を含む（`_canonicalize_markers` 含む）
- LABEL-01/02/04 の全シナリオと D-03 §7.2 適格性（未勝利 precision 含む）のテストを含む
</verification>

<success_criteria>
- LABEL-01/02/04 を網羅する25テスト以上の unit test ファイルが存在（SC#1/SC#4 直接前提）
- D-04 の5エッジケースが合成 DataFrame で再現され、label_validation_status と is_model_eligible の正しい写像を assert
- D-03 §7.2 適格性ルール（障害/新馬/複勝発売なし/class_below_minimum/unresolved/正常・未勝利 precision）を網羅
- 全テスト DB 不要（@pytest.mark.requires_db なし・test_select_se_state_includes_datakubun_9 は inspect.getsource のみ）で CI 実行可能
- Plan 03 GREEN 実装の契約になる（RED→GREEN→REFACTOR サイクル）
- **REVIEWS 解決:** HIGH #1（source/confidence 分離テスト）/ #4（_select_se_state regression）/ #5（canonicalization 両表現）/ #6（dead_loss+obstacle reason precision）/ MEDIUM（未勝利適格）がテストとして存在
</success_criteria>

<output>
Create `.planning/phases/02-fukusho-labels/02-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

### 新規シンボル・ファイル（本 plan が作成）

**テスト関数（tests/test_fukusho_label.py）:**
- `test_fukusho_module_imports`
- `test_raw_vs_validated_basic_8_horses`
- `test_raw_vs_validated_basic_6_horses`
- `test_drift_is_dead_heat_only`
- `test_sales_start_entry_count_proxy_and_source_confidence_separated_from_status`（**HIGH #1**）
- `test_unresolved_triggers_hr_missing`
- `test_no_fukusho_sale_under_5_horses`
- `test_payout_places_uses_torokutosu_not_syussotosu`
- `test_canonicalize_markers_raw_string_form`（**HIGH #5**）
- `test_canonicalize_markers_numeric_cast_form`（**HIGH #5**）
- `test_dead_heat_all_payout_positive`
- `test_scratch_cancel_excluded`
- `test_dead_loss_in_training`
- `test_dead_loss_in_obstacle_race_excluded_for_obstacle_reason`（**HIGH #6**）
- `test_race_cancelled_all_unresolved`（**HIGH #4 強化**）
- `test_select_se_state_includes_datakubun_9`（**HIGH #4 regression**）
- `test_fuseiritu_flag2_unresolved`
- `test_tokubaraiflag2_with_payout_validated`
- `test_tokubaraiflag2_without_payout_unresolved`
- `test_is_model_eligible_obstacle_syubetucd`
- `test_is_model_eligible_newcomer_syubetucd`
- `test_is_model_eligible_maiden_syubetucd_included`（**MEDIUM 解決**）
- `test_is_model_eligible_class_below_minimum`
- `test_is_model_eligible_validated_normal`
- `test_dochachukubun_dead_heat_detection`

**ヘルパー関数（tests/test_fukusho_label.py 内・同じファイル内 private）:**
- `_build_hr_row(**overrides) -> dict`
- `_build_se_row(umaban: int, **overrides) -> dict`
- `_build_label_input_df(n_horses: int, hr_overrides: dict | None = None, se_overrides: dict | None = None, syubetucd: str = '00', class_level_numeric: int = 2) -> tuple[pd.DataFrame, pd.DataFrame, pd.DataFrame]`

**import 契約（Plan 03 GREEN が満たすべき）:**
- `from src.etl.fukusho_label import compute_fukusho_labels, classify_status, compute_is_model_eligible, _canonicalize_markers, run_label_etl`
