---
phase: 02-fukusho-labels
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/label_spec.yaml
  - src/db/schema.py
  - src/db/connection.py
  - src/config/settings.py
  - scripts/apply_schema.sql
autonomous: true
requirements: [LABEL-01, LABEL-02, LABEL-03, LABEL-04]
tags: [phase-02, labels, schema, grant, config, foundation, reviews-rev1]

must_haves:
  truths:
    - "label_spec.yaml が Git 管理下に存在し label_generation_version='v1.0.0' を含む（D-07）"
    - "ETL ロールが label スキーマに USAGE+CREATE+書込権を持つ（Pitfall 6・SC#1 前提）"
    - "reader ロールが label スキーマに USAGE+SELECT を持つ（下流 Phase 3-5・T-02-03）"
    - "make_pool(role='etl') の search_path が label,normalized,public を含む"
    - "run_apply_schema.py の再実行が exit 0 で GRANT 拡張を実DBに反映"
    - "label_validation_status の 4 値と sales_start_entry_count_source_confidence の 'inferred' が明示分離される（REVIEWS HIGH #1: conflation 回避）"
    - "marker canonicalization ルールが raw文字列・数値キャスト後の両表現を許容する sentinel 集合として定義される（REVIEWS HIGH #5: brittle marker 回避）"
  artifacts:
    - path: "src/config/label_spec.yaml"
      provides: "ラベル定義の静的正（label_generation_version/status 定義/payout 境界/SE marker canonicalization/§7.2 適格性）"
      contains: "label_generation_version"
    - path: "src/db/schema.py"
      provides: "GRANT_ETL_SQL と GRANT_READER_SQL の label スキーマ拡張"
      contains: "GRANT USAGE, CREATE ON SCHEMA label"
    - path: "src/config/settings.py"
      provides: "db_schema_label='label' フィールド"
      contains: "db_schema_label"
  key_links:
    - from: "src/db/schema.py GRANT_ETL_SQL"
      to: "scripts/run_apply_schema.py apply()"
      via: "APPLY_ORDER 内の grant_etl ステップ実行"
      pattern: "GRANT USAGE, CREATE ON SCHEMA label"
    - from: "src/db/connection.py make_pool(role='etl')"
      to: "src/config/settings.py db_schema_label"
      via: "search_path 構築で参照"
      pattern: "db_schema_label"
---

<objective>
Phase 2 の基盤を整える: (1) `src/config/label_spec.yaml` を作成しラベル定義の静的正（label_generation_version=payout 境界・SE marker rules・marker canonicalization・§7.2 適格性・status 分類・`sales_start_entry_count` source/confidence 分離）を Git 管理下に置く（D-07）、(2) `src/db/schema.py` の GRANT_ETL_SQL / GRANT_READER_SQL を label スキーマ向けに拡張し `scripts/apply_schema.sql` も同期、(3) `src/config/settings.py` に `db_schema_label` を追加し `src/db/connection.py` の etl ロール search_path に label を組み込む、(4) `scripts/run_apply_schema.py` を再実行して GRANT 拡張を実DBに反映（Pitfall 6）。

Purpose: 後続 Plan 02/03/04 が label_spec.yaml の値を参照し、ETL ロールで label.fukusho_label へ idempotent 書込ができるようにする。Phase 1 01-03 と同じ GRANT 拡張パターンの再適用（HIGH #5/#6 互換）。ラベルは予測目標の正であり、設定/権限の欠陥は下流すべてを汚染する聖域の前提。

**Reviews REV1 対応（本 plan が扱う HIGH 懸念）:**
- **HIGH #1 (inferred conflation):** `label_validation_status='inferred'`（D-04 速報成績 DataKubun=1 用・本DBでは0件）と `sales_start_entry_count` の代理値信頼度（'inferred'）を明示的に別物として YAML に定義。後者は `sales_start_entry_count_source='torokutosu_proxy'` + `sales_start_entry_count_confidence='inferred'`（独立キー）で格納し、`label_validation_status` は触らない。Plan 03 は `label.fukusho_label` に `sales_start_entry_count_confidence` カラムを別途作成。
- **HIGH #5 (brittle marker comparisons):** marker 識別条件（harontimel3/timediff/time/bataijyu/datakubun）を「文字列表現と数値表現の両方を受け入れる sentinel 集合」として `se_marker_canonicalization` セクションで定義。Plan 03 の `_canonicalize_markers` がこれに従い、raw 文字列 '999.0' と数値キャスト後 999.0 の両方を sentinel 扱いする。

Output: 5 ファイル修正/新規。実DBの label スキーマ GRANT 反映。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-fukusho-labels/02-CONTEXT.md
@.planning/phases/02-fukusho-labels/02-RESEARCH.md
@.planning/phases/02-fukusho-labels/02-PATTERNS.md
@.planning/phases/02-fukusho-labels/02-REVIEWS.md
@src/db/schema.py
@src/db/connection.py
@src/config/settings.py
@src/config/class_normalization.yaml
@src/config/code_tables.yaml
@src/etl/filters.py
@scripts/run_apply_schema.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: label_spec.yaml 作成（ラベル定義の静的正・D-07 Git 管理・marker canonicalization・source/confidence 分離）</name>
  <files>src/config/label_spec.yaml</files>

  <read_first>
    - src/config/class_normalization.yaml（1-131 行・ヘッダコメント慣行・`post_2019_class_system_reform_date` / jyokencd5_map 構造・unresolved_strategy パターンの写し・特に "703" 未勝利 class_level_numeric=0 と "005" 1勝クラス class_level_numeric=1 の確認）
    - src/config/code_tables.yaml（1-42 行・`syubetucd` 実測値 '00'/'11'/'12'/'13'..'19' の確認）
    - .planning/phases/02-fukusho-labels/02-RESEARCH.md:334-350（§10.5 の6検査 BLOCK/INFO 対応表）
    - .planning/phases/02-fukusho-labels/02-RESEARCH.md:405-412（取消/中止/除外識別条件表・実カラム名 bataijyu/harontimel3/timediff）
    - .planning/phases/02-fukusho-labels/02-RESEARCH.md:582-587（Open Question #1・`label_generation_version: "v1.0.0"` 推奨）
    - .planning/phases/02-fukusho-labels/02-CONTEXT.md:24-51（D-01..D-04 locked decisions・status 4値と is_model_eligible 写像）
    - .planning/phases/02-fukusho-labels/02-REVIEWS.md:37-50（HIGH #1 inferred conflation・HIGH #5 brittle marker・該当 suggestion）
    - docs/keiba_ai_requirements_v1.3.md §7.2-7.3（適格性ルールの正・特に §7.2「2歳未勝利以上 / 3歳未勝利以上 / 1勝クラス以上」と "703" 未勝利 class_level_numeric=0 の関係）
    - docs/keiba_ai_requirements_v1.3.md §10.2-10.6（ラベル仕様の正）
  </read_first>

  <action>
    `src/config/class_normalization.yaml:1-15` のヘッダコメント慣行（由来・実測根拠・CODE.md 不使用明記・D-13 silent fallback 禁止）を写した `label_spec.yaml` を作成。由来は「PostgreSQL everydb2 実測（2026-06-17・JRA 2015+ 39,580 レース/553,891 馬行）+ 要件 §10.1-10.6 + §7.2-7.3 + CONTEXT.md D-01..D-04 + REVIEWS HIGH #1/#5 対応」と明記。

    以下のトップレベルキーを定義（実測値・CONTEXT.md locked decisions・REVIEWS HIGH #1/#5 に厳密合致）:

    1. `label_generation_version: "v1.0.0"`（Open Question #1 推奨・セマンティック採番・ラベルロジック変更時に bump・Phase 3 snapshot metadata に埋め込まれる）

    2. `status_values: ["validated", "inferred", "dead_heat", "unresolved"]`（D-04 固定4値・`inferred` は HR/SE 速報成績 DataKubun='1' が存在する場合のみ・本DBでは発生せず）

    3. `payout_places_rules`（D-01 厳格・学習除外対象）:
       - `min_torokutosu_for_fukusho_sale: 5`（5頭未満は複勝発売なし・§7.3）
       - `places_5_to_7_horses: 2`
       - `places_8_or_more_horses: 3`
       - `no_sale_marker_value: 0`
       - コメント: 「境界は HR PayFukusyoUmaban/FuseirituFlag2 の観測事実で最終確定（D-01 厳格）。TorokuTosu は補助」

    4. **`sales_start_entry_count`（D-01 寛容・REVIEWS HIGH #1: label_validation_status から明示分離）**:
       - `proxy_source: "n_harai.TorokuTosu"`（label.fukusho_label.sales_start_entry_count_source カラムに格納）
       - `source_confidence: "inferred"`（label.fukusho_label.sales_start_entry_count_confidence カラムに格納・`label_validation_status` とは独立・`status_values` とは無関係）
       - `source_note: "AV (TORIKESI_JYOGAI) テーブルは本DBで0行・HappyoTime 復元は技術的に不可能（RESEARCH Pitfall 2）。TorokuTosu は SE 馬番数と 99.97% 一致（39,570/39,580）。この 'inferred' は sales_start_entry_count の信頼度表示のみで、当該レースの label_validation_status（HR DataKubun='2' なら 'validated'）とは直交する概念"`
       - **明示的分離の表明（HIGH #1 回避）:** 当該キーの docstring に「`source_confidence='inferred'` は `label_validation_status` ではなく `sales_start_entry_count` カラム群の信頼度。下流が `WHERE label_validation_status != 'inferred'` でフィルタしても sales_start_entry_count の proxy 性は隠れない」と明記。

    5. `model_ineligibility_syubetucd: ["18", "19"]`（§7.3 障害・code_tables.yaml 実測値）
    6. `newcomer_syubetucd: ["11", "12"]`（§7.3 新馬・2歳/3歳）
    7. **`class_eligibility`（REVIEWS MEDIUM open question 解決・§7.2 精密化）**:
       - `minimum_class_level_numeric: 1`（1勝クラス以上・jyokencd5='005'）
       - `maiden_syubetucd: ["13", "14", "15"]`（2歳未勝利=13 / 3歳未勝利=14 / 条件戦=15・class_level_numeric=0 だが §7.2「未勝利以上」に含まれるため **class_level_numeric=0 単独では不適格としない**・syubetucd で識別）
       - `maiden_ineligible_syubetucd: ["11", "12"]`（新馬戦=class_level_numeric=0 かつ §7.3 除外・上記 newcomer_syubetucd と重複するが理由コード分離のため明示）
       - 適用順序の docstring: 「compute_is_model_eligible は (a) syubetucd が obstacle/newcomer なら不適格 → (b) 複勝発売なし → (c) unresolved → (d) cancel系マーカー → (e) class_level_numeric < 1 AND syubetucd NOT IN maiden_syubetucd → class_below_minimum。これにより未勝利（class_level_numeric=0・syubetucd='13'/'14'）は §7.2 対象として残り、新馬（class_level_numeric=0・syubetucd='11'/'12'）はステップ(a)で除外される」

    8. **`se_marker_canonicalization`（REVIEWS HIGH #5: brittle marker 回避・raw 文字列と数値キャスト後の両表現を許容）**:
       - `enabled: true`
       - `harontimel3_sentinels: ["999", "999.0", "999.00"]`（raw varchar '999' / pandas to_numeric 後 999.0 / 文字列表現 '999.0' すべてを「ハロンタイム異常 sentinel」として同一視）
       - `timediff_sentinels: ["9999", "9999.0"]`（同上）
       - `time_sentinels_absent: ["0", "0.0", "", "9999", "9999.0"]`（time がこれらの値なら「発走前停止/除外」扱い）
       - `time_sentinels_present_hint: "non-zero numeric not in time_sentinels_absent"`（time が数値で sentinel 集合外なら「発走後停止=競走中止」扱い）
       - `bataijyu_sentinels_scratch: ["000", "0", "0.0"]`（raw '000' / 数値キャスト 0 を同一視）
       - `datakubun_sentinels_race_cancelled: ["9", "09"]`（HR/SE 両方で DataKubun='9' を「レース全体中止」扱い・先頭0埋め許容）
       - docstring: 「`_canonicalize_markers`（Plan 03）は必ずこの sentinel 集合で判定する。`harontimel3 == '999.0'` のような単一文字列等価比較は禁止・sentinel 集合への `in` 判定のみ許可。raw が varchar で normalized が numeric の両方で同一結果を返す。」

    9. **`se_marker_rules`（D-04・RESEARCH Pitfall 4 実測識別条件表・実カラム名 + 上記 canonicalization sentinel 使用）**:
       - `scratch_cancel: {condition: "canonicalized_bataijyu in bataijyu_sentinels_scratch", expected_count: 956, label: "予測対象外", is_model_eligible: false}`
       - `dead_loss_individual: {condition: "canonicalized_harontimel3 in harontimel3_sentinels AND canonicalized_timediff in timediff_sentinels AND canonicalized_time NOT in time_sentinels_absent", expected_count: 3554, label: "fukusho_hit=0 で学習残", is_model_eligible: true}`（注: `timediff` は EveryDB2 マニュアル TimeDIFN だが本DB実カラム名・RESEARCH Pitfall 1）
       - `race_excluded_before_start: {condition: "canonicalized_harontimel3 in harontimel3_sentinels AND canonicalized_timediff in timediff_sentinels AND canonicalized_time in time_sentinels_absent", expected_count: 0, label: "予測対象外（本DBでは発生なし）", is_model_eligible: false}`
       - `race_cancelled: {condition: "canonicalized_datakubun in datakubun_sentinels_race_cancelled", expected_count_se: 376, label: "全馬 unresolved", is_model_eligible: false}`
       - 各 condition は `_canonicalize_markers` が生成する `canonicalized_*` 列を参照する（Plan 03 が実装）

    10. `se_datakubun_inclusion`（REVIEWS HIGH #4: race-cancelled rows が _select_se_state で落とされないことを YAML で表明）:
        - `required_se_datakubun_values: ["7", "9"]`（confirmed='7' と race_cancelled='9' の両方を SELECT 対象とする・PLAN 03 _select_se_state の WHERE 句で固定）
        - docstring: 「Plan 03 の `_select_se_state` は `datakubun IN ('7','9')` で SELECT する。`datakubun='7'` のみに絞ると race_cancelled の 376 SE 行がラベル生成前に消失し silent data loss になる（HIGH #4）。」

    11. `hr_status_rules`（D-04・RESEARCH 実測）:
        - `monday_confirmed_datakubun: "2"`（本DBでは全件・validated）
        - `flash_report_datakubun: "1"`（本DBでは0件・将来用 inferred 分岐保持・D-13）
        - `fuseiritu_flag2_value: {value: "1", meaning: "複勝不成立", status: "unresolved", expected_count: 0}`
        - `tokubarai_flag2_value: {value: "1", meaning: "複勝特払", status: "validated_if_payout_exists_else_unresolved", expected_count: 0}`

    12. `dead_heat_rules`（D-04・LABEL-04・payout-table authoritative・REVIEWS MEDIUM #2 対応）:
        - `detection: "payout_count > fukusho_payout_places AND fukusho_payout_places > 0"`（**payout slot4/5 使用が唯一の権威ある同着検出・DochacoTosu は参考値に過ぎない**）
        - `slot4_used_count: 97`（実測・PayFukusyoUmaban4 使用）
        - `slot5_used_count: 1`（実測・PayFukusyoUmaban5 使用）
        - `status: "dead_heat"`
        - `is_model_eligible: true`（学習対象・払戻テーブル全対象馬を正例）
        - docstring: 「DochacoTosu='1' は別途監査列として保持するが、dead_heat 判定の権威は払戻テーブル slot 拡張のみ。DochacoTosu の意味が EveryDB2 で確定していないため、payout-table-derived の is_dead_heat を唯一の正とする。」

    13. `ineligibility_reason_codes`（D-03・RESEARCH `label.fukusho_label.ineligibility_reason` 値域・REVIEWS HIGH #6 で "dead_loss_only" を追加）:
        `[obstacle, newcomer, no_fukusho_sale, unresolved, race_or_horse_cancelled, class_below_minimum, status_not_eligible]`
        - docstring: 「競走中止馬（is_dead_loss=True）がその他の理由で不適格になる場合、ineligibility_reason には本来の理由（obstacle / newcomer / no_fukusho_sale / unresolved / race_or_horse_cancelled / class_below_minimum）を格納し、競走中止単独では不適格にしない。Plan 04 `_check_dead_loss_not_excluded`（HIGH #6 修正版）は「ineligibility_reason が存在する かつ それが上記の非 dead_loss 理由である」場合のみ passed=True とし、純粋な dead_loss 除外（reason が存在しないのに is_model_eligible=False）を passed=False とする。」

    14. `unresolved_strategy: "error_and_isolate"`（class_normalization.yaml:130 と同形式・D-13）

    YAML 形式・インデント2スペース・全文字列値をクォート（class_normalization.yaml:23-47 の `"701":` 形式に準拠）。fenced code block を action 内に置かないこと。
  </action>

  <verify>
    <automated>test -f src/config/label_spec.yaml &amp;&amp; grep -c "label_generation_version" src/config/label_spec.yaml | grep -q "^1$" &amp;&amp; python3 -c "import yaml; cfg = yaml.safe_load(open('src/config/label_spec.yaml')); assert cfg['label_generation_version'] == 'v1.0.0'; assert set(cfg['status_values']) == {'validated','inferred','dead_heat','unresolved'}; assert cfg['model_ineligibility_syubetucd'] == ['18','19']; assert 'source_confidence' in cfg['sales_start_entry_count'], 'HIGH #1: source_confidence must be separate key'; assert cfg['sales_start_entry_count']['source_confidence'] == 'inferred'; assert 'se_marker_canonicalization' in cfg, 'HIGH #5: canonicalization config missing'; assert 'harontimel3_sentinels' in cfg['se_marker_canonicalization']; assert '999.0' in cfg['se_marker_canonicalization']['harontimel3_sentinels']; assert set(cfg['se_datakubun_inclusion']['required_se_datakubun_values']) == {'7','9'}, 'HIGH #4: SE datakubun inclusion'; assert 'maiden_syubetucd' in cfg['class_eligibility'], 'MEDIUM: class_eligibility precision'; print('label_spec.yaml OK (REVIEWS HIGH #1/#4/#5 + MEDIUM resolved)')"</automated>
  </verify>

  <acceptance_criteria>
    - src/config/label_spec.yaml が存在し `grep -c "label_generation_version" src/config/label_spec.yaml` が 1 を返す
    - `python3 -c "import yaml; yaml.safe_load(open('src/config/label_spec.yaml'))"` が例外なく成功（YAML 構文 valid）
    - `label_generation_version: "v1.0.0"` が含まれる（grep で一致）
    - `status_values` が正確に `["validated", "inferred", "dead_heat", "unresolved"]`（D-04）
    - **HIGH #1 解決:** `sales_start_entry_count` セクションに `source_confidence: "inferred"` が独立キーとして存在（grep `source_confidence` で一致）・かつ `proxy_status` という同義キーは存在しない（`grep -c "proxy_status" src/config/label_spec.yaml` が 0）
    - **HIGH #5 解決:** `se_marker_canonicalization` セクションが存在し `harontimel3_sentinels` に `"999"` と `"999.0"` の両方が含まれる（grep で両方一致）・`timediff_sentinels` に `"9999"` と `"9999.0"` の両方が含まれる
    - **HIGH #4 予防:** `se_datakubun_inclusion.required_se_datakubun_values` が `["7", "9"]`（race_cancelled DataKubun='9' が含まれる）
    - `se_marker_rules.dead_loss_individual.condition` 文字列が `timediff`（NOT `timedifn`）と `canonicalized_` を含む（Pitfall 1 + HIGH #5）
    - `se_marker_rules.scratch_cancel.condition` が `canonicalized_bataijyu in bataijyu_sentinels_scratch` を含む（HIGH #5・実カラム名 bataijyu・NOT bataiju）
    - `model_ineligibility_syubetucd` が `["18", "19"]`（code_tables.yaml 実測値）
    - `unresolved_strategy: "error_and_isolate"` が含まれる（D-13）
    - **MEDIUM 解決:** `class_eligibility.maiden_syubetucd` が `["13", "14", "15"]` を含み（未勝利は class_level_numeric=0 だが §7.2 対象）、`minimum_class_level_numeric: 1` と併記される
  </acceptance_criteria>

  <done>label_spec.yaml が作成され label_generation_version='v1.0.0'・D-04 status 4値・D-01 payout 境界ルール・Pitfall 1/4 実カラム名識別条件・§7.2/7.3 適格性ルール（未勝利 precision 含む）を網羅。REVIEWS HIGH #1（source_confidence 分離）/#4（SE DataKubun='9' 包含表明）/#5（marker canonicalization sentinels）解決。python yaml.safe_load が成功。</done>
</task>

<task type="auto">
  <name>Task 2: schema.py GRANT 拡張 + settings.py db_schema_label + db_reader_role + connection.py search_path 拡張 + apply_schema.sql 同期（HIGH #3 支援）</name>
  <files>src/db/schema.py, src/config/settings.py, src/db/connection.py, scripts/apply_schema.sql</files>

  <read_first>
    - src/db/schema.py:76-117（GRANT_READER_SQL / GRANT_ETL_SQL / REVOKE_RAW_WRITES_*_SQL の現状）
    - src/db/schema.py:131-139（APPLY_ORDER の順序）
    - src/config/settings.py:32-39（db_schema_raw / db_schema_normalized の現状）
    - src/db/connection.py:21-52（make_pool role='etl' の search_path 構築・role='readonly' の DSN 構築・KEIBA_DB_USER/KEIBA_DB_USER 環境変数と db_reader_role の整合確認）
    - scripts/run_apply_schema.py:114-128（apply() が APPLY_ORDER の各ステップを実行する仕組み）
    - .planning/phases/02-fukusho-labels/02-PATTERNS.md:176-211（schema.py GRANT 拡張の具体3行・connection.py search_path 拡張）
    - .planning/phases/02-fukusho-labels/02-RESEARCH.md:682-687（AC 変更点3点・run_apply_schema.py 再実行必須）
    - .planning/phases/02-fukusho-labels/02-REVIEWS.md:41-42, 64（HIGH #3 staging-swap GRANT TO PUBLIC 禁止・MEDIUM #3 reader role 明示使用）
    - scripts/apply_schema.sql（現状の GRANT_ETL_SQL/GRANT_READER_SQL レンダリング結果確認）
  </read_first>

  <action>
    **修正1 — src/db/schema.py GRANT_READER_SQL 拡張**: 現行 `GRANT_READER_SQL`（schema.py:76-86）の `GRANT USAGE ON SCHEMA normalized TO {reader};` 行の直後に3行追加:
    - `GRANT USAGE ON SCHEMA label TO {reader};`
    - `GRANT SELECT ON ALL TABLES IN SCHEMA label TO {reader};`
    - `ALTER DEFAULT PRIVILEGES IN SCHEMA label GRANT SELECT ON TABLES TO {reader};`
    （下流 Phase 3-5 の readonly ロールが label.fukusho_label を schema 修飾 SELECT するため・T-02-03・REVIEWS HIGH #3/MEDIUM #3: reader ロール明示付与・PUBLIC には付与しない）

    **修正2 — src/db/schema.py GRANT_ETL_SQL 拡張**: 現行 `GRANT_ETL_SQL`（schema.py:88-98）の `GRANT USAGE, CREATE ON SCHEMA normalized TO {etl};` 行の直後に3行追加（PATTFALL 6 対策・PATTERNS.md:194-200 の通り）:
    - `GRANT USAGE, CREATE ON SCHEMA label TO {etl};`
    - `GRANT SELECT, INSERT, UPDATE, DELETE, TRUNCATE ON ALL TABLES IN SCHEMA label TO {etl};`
    - `ALTER DEFAULT PRIVILEGES IN SCHEMA label GRANT SELECT, INSERT, UPDATE, DELETE, TRUNCATE ON TABLES TO {etl};`
    public/raw_everydb2 に対する REVOKE は変更しない（D-06 二重保護維持・REVOKE_RAW_WRITES_PUBLIC_SQL/REVOKE_RAW_WRITES_VIEW_SQL はそのまま）。APPLY_ORDER（schema.py:131-139）の順序・ステップ名は変更不要（grant_reader/grant_etl の SQL 文字列の中身が変わるだけ）。

    **修正3 — src/config/settings.py db_schema_label + db_reader_role 追加**（**REVIEWS HIGH #3 支援**）: `db_schema_normalized: str = "normalized"`（settings.py:39）の直後に2フィールドを追加: (a) `db_schema_label: str = "label"`（db_schema_raw / db_schema_normalized と対称）・(b) `db_reader_role: str = "keiba_readonly"`（Plan 03 の `_idempotent_load_label` が staging RENAME 後に `GRANT SELECT ON label.fukusho_label TO {reader_role}` を発行する際に reader role 名をハードコードせず settings から取得するため・HIGH #3: PUBLIC ではなく明示的 reader ロール付与をコード上でも保証）。db_reader_role のデフォルト値は `.env` の `KEIBA_DB_USER` と一致することを docstring に明記。

    **修正4 — src/db/connection.py search_path 拡張**: `make_pool(role='etl')` 分岐（connection.py:40-42）の `search_path = f"{settings.db_schema_normalized},public"` を `search_path = f"{settings.db_schema_label},{settings.db_schema_normalized},public"` に変更。`role='readonly'` 側（connection.py:38-39）は変更しない（readonly は schema 修飾 `label.fukusho_label` で SELECT するため search_path 依存性が低い・PATTERNS.md:234 通り）。

    **修正5 — scripts/apply_schema.sql 同期**: `scripts/apply_schema.sql` が schema.py の SQL 定数をレンダリングした成果物であるため、run_apply_schema.py を dry-run して生成される SQL を確認し、手動で apply_schema.sql にも同じ3行×2箇所（reader/etl）を追記。または次タスクの `run_apply_schema.py` 実行が schema.py の定数を直接使うため apply_schema.sql は参照用・更新は schema.py が source of truth であることを確認（run_apply_schema.py:117-128 は schema_module の定数を直接実行）。

    **REVIEWS HIGH #3 制約（Plan 03 staging-swap が従うべき事前表明）:** schema.py の GRANT_READER_SQL が `keiba_readonly` ロール明示付与（`{reader}` placeholder）であること。Plan 03 の `_idempotent_load_label` は `GRANT SELECT ON label.fukusho_label TO PUBLIC` を使わず、staging RENAME 後に `ALTER DEFAULT PRIVILEGES` と明示的 reader role GRANT で対応する（本 Task は事前表明のみ・実際の修正は Plan 03 Task 1）。

    SQL 文字列結合による GRANT 構築は禁止・psycopg.sql.Identifier による {reader}/{etl} プレースホルダ置換は既存のまま維持（run_apply_schema.py:47-64 が処理・MEDIUM #3）。
  </action>

  <verify>
    <automated>grep -q "GRANT USAGE, CREATE ON SCHEMA label TO {etl};" src/db/schema.py &amp;&amp; grep -q "GRANT USAGE ON SCHEMA label TO {reader};" src/db/schema.py &amp;&amp; grep -q "db_schema_label: str = \"label\"" src/config/settings.py &amp;&amp; grep -q "db_schema_label" src/db/connection.py &amp;&amp; python3 -c "from src.config.settings import Settings; from src.db.schema import GRANT_ETL_SQL, GRANT_READER_SQL; assert 'SCHEMA label' in GRANT_ETL_SQL, 'GRANT_ETL_SQL に label が無い'; assert 'SCHEMA label' in GRANT_READER_SQL, 'GRANT_READER_SQL に label が無い'; assert 'TO PUBLIC' not in GRANT_READER_SQL or GRANT_READER_SQL.count('TO PUBLIC') == 0, 'HIGH #3: reader は PUBLIC ではなく明示ロール'; print('schema/connection/settings OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "GRANT USAGE, CREATE ON SCHEMA label TO {etl};" src/db/schema.py` が 1 を返す
    - `grep -c "GRANT SELECT, INSERT, UPDATE, DELETE, TRUNCATE ON ALL TABLES IN SCHEMA label TO {etl};" src/db/schema.py` が 1 を返す
    - `grep -c "GRANT USAGE ON SCHEMA label TO {reader};" src/db/schema.py` が 1 を返す
    - `grep -c "ALTER DEFAULT PRIVILEGES IN SCHEMA label" src/db/schema.py` が 2 を返す（etl+reader の2箇所）
    - `grep -c "REVOKE UPDATE, DELETE, TRUNCATE ON ALL TABLES IN SCHEMA public" src/db/schema.py` が 2 を返すまま（D-06 二重保護維持・変更なし）
    - **HIGH #3 / MEDIUM #3:** `python3 -c "from src.db.schema import GRANT_READER_SQL; assert 'TO PUBLIC' not in GRANT_READER_SQL"` が成功（reader 権限は PUBLIC ではなく明示的 reader ロール・Plan 03 もこれに従う）
    - `python3 -c "from src.config.settings import Settings; import inspect; src = inspect.getsource(Settings); assert 'db_schema_label' in src; assert 'db_reader_role' in src"` が成功（HIGH #3 支援: Plan 03 が settings.db_reader_role で明示的 reader ロール名を取得）
    - `python3 -c "from src.config.settings import Settings; assert Settings.model_fields['db_reader_role'].default == 'keiba_readonly'"` が成功（HIGH #3: デフォルトが keiba_readonly・.env KEIBA_DB_USER と一致）
    - `python3 -c "from src.db.connection import make_pool; import inspect; src = inspect.getsource(make_pool); assert 'db_schema_label' in src and 'db_schema_normalized' in src"` が成功（label が normalized の前に来る検証）
    - `python3 -c "from src.db.schema import GRANT_ETL_SQL, GRANT_READER_SQL; assert GRANT_ETL_SQL.count('SCHEMA label') >= 3 and GRANT_READER_SQL.count('SCHEMA label') >= 2"` が成功
  </acceptance_criteria>

  <done>GRANT_ETL_SQL と GRANT_READER_SQL に label スキーマ権限が追加され（reader は明示ロール・PUBLIC ではない）、settings.py に db_schema_label='label' が定義され、connection.py の etl ロール search_path が label,normalized,public になる。public/raw_everydb2 の REVOKE は変更なし（D-06 維持）。</done>
</task>

<task type="auto">
  <name>Task 3: run_apply_schema.py 再実行で GRANT 拡張を実DBに反映（Pitfall 6）</name>
  <files>scripts/apply_schema.sql</files>

  <read_first>
    - scripts/run_apply_schema.py:106-146（apply() 関数・CREATE SCHEMA → CREATE ROLE → CREATE VIEW → GRANT → REVOKE の順序・psycopg.sql.Identifier 置換）
    - scripts/run_apply_schema.py:149-185（main()・KEIBA_ADMIN_DSN/KEIBA_DB_USER/KEIBA_ETL_DB_USER 必須・RuntimeError fail-fast）
    - src/db/schema.py:42-44（CREATE_SCHEMAS_SQL が `CREATE SCHEMA IF NOT EXISTS label` を含む・idempotent）
    - .env（KEIBA_ADMIN_DSN / KEIBA_DB_USER=keiba_readonly / KEIBA_ETL_DB_USER=keiba_etl が設定済みであることを確認・Phase 1 完了済み前提）
    - .planning/phases/02-fukusho-labels/02-RESEARCH.md:422-427（Pitfall 6・permission denied for schema label を避ける）
    - .planning/phases/02-fukusho-labels/02-REVIEWS.md:41-42（HIGH #3: GRANT TO PUBLIC 禁止・reader ロール明示使用）
  </read_first>

  <action>
    `scripts/apply_schema.sql` を最新の schema.py 定数と同期するため、まず dry-run で出力を確認:
    `uv run python scripts/run_apply_schema.py --dry-run`
    dry-run 出力の `grant_etl` ステップに `GRANT USAGE, CREATE ON SCHEMA label TO "keiba_etl";` が3行含まれること・`grant_reader` に `GRANT USAGE ON SCHEMA label TO "keiba_readonly";` 等が含まれること・**`TO PUBLIC` が含まれないこと**（HIGH #3）を確認。

    apply_schema.sql を dry-run 出力で上書き更新（参照用・run_apply_schema.py は schema.py 定数を直接実行するが、sql ファイルも最新状態を保つ慣行・Phase 1 01-01/01-03 と同じ）。または `scripts/apply_schema.sql` が既に存在しない場合は新規作成。

    実行: `uv run python scripts/run_apply_schema.py` （--dry-run 無し）
    - KEIBA_ADMIN_DSN で管理者接続・autocommit=True（run_apply_schema.py:115）
    - 全ステップが順次実行され、最後に `schema applied successfully` が INFO ログで出る
    - 既存テーブル/ロールは idempotent（CREATE SCHEMA IF NOT EXISTS / DO $$ BEGIN IF NOT EXISTS）
    - 既存 GRANT は重複発行されるが PostgreSQL は冪等（同一権限の重複 GRANT は無害）

    実行後、psql または readonly_cur で GRANT が反映されたことを検証クエリで確認:
    `SELECT has_schema_privilege('keiba_etl', 'label', 'USAGE, CREATE')` → true
    `SELECT has_schema_privilege('keiba_readonly', 'label', 'USAGE')` → true
    `SELECT has_schema_privilege('keiba_readonly', 'label', 'SELECT')` → true（HIGH #3 の実効確認）

    失敗した場合（RuntimeError・権限エラー・接続エラー）は plan を止めて SUMMARY の blockers に記録。.env の KEIBA_ADMIN_DSN 未設定は Phase 1 の時点で要件違反なので前提確認。
  </action>

  <verify>
    <automated>uv run python scripts/run_apply_schema.py 2>&amp;1 | tail -5 | grep -q "schema applied successfully"</automated>
  </verify>

  <acceptance_criteria>
    - `uv run python scripts/run_apply_schema.py` の終了コードが 0
    - 標準出力/ログの最後に `schema applied successfully` が含まれる
    - `scripts/apply_schema.sql` 内の `SCHEMA label` の出現回数が5以上（reader: USAGE/SELECT/DEFAULT PRIVILEGES = 3行、etl: USAGE,CREATE/書込4種/DEFAULT PRIVILEGES = 3行 = 合計6箇所以上の "SCHEMA label" 文字列）
    - **HIGH #3:** `grep -c "TO PUBLIC" scripts/apply_schema.sql` が 0（PUBLIC 付与は一切ない・reader ロール明示付与のみ）
    - dry-run 実行時の出力に `"keiba_etl"` と `"keiba_readonly"` の Identifier quote が含まれる（MEDIUM #3・SQL injection 回避）
    - D-06 二重保護が維持される: `grep -c "REVOKE UPDATE, DELETE, TRUNCATE" scripts/apply_schema.sql` が4以上（public×2 + raw_everydb2×2）
  </acceptance_criteria>

  <done>run_apply_schema.py が exit 0 で完了し、keiba_etl ロールが label スキーマに USAGE+CREATE+書込権を持ち、keiba_readonly が label スキーマに USAGE+SELECT を持つ（PUBLIC には付与しない・HIGH #3）。public/raw_everydb2 への書込権剥奪は維持。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ETL ロール → label スキーマ | 書込境界・今回新規追加。ETL ロールのみが INSERT/UPDATE/TRUNCATE を持つ |
| reader ロール → label スキーマ | 読取境界・下流 Phase 3-5 が使用。SELECT のみ・**明示的 reader ロール付与（HIGH #3: PUBLIC 付与禁止）** |
| ETL/reader → public/raw_everydb2 | 既存の raw 不変性境界（D-06）。本 plan で変更しない |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | GRANT_ETL_SQL label 拡張 | mitigate | ETL ロールに label のみ USAGE+CREATE+書込を付与。public/raw_everydb2 への書込権は付与しない（REVOKE 維持・D-06）。GRANT 文は schema.py のプレースホルダ {etl} を psycopg.sql.Identifier で安全に置換（run_apply_schema.py:59-64・MEDIUM #3） |
| T-02-02 | Tampering | label_spec.yaml の改ざん | mitigate | D-07 Git 管理下に配置。label_generation_version='v1.0.0' でバージョン管理・ラベルロジック変更時に bump。Phase 3 snapshot metadata に埋め込まれ再現性保証（§19.1） |
| T-02-03 | Information Disclosure | reader ロールの過剰権限 / PUBLIC 付与 | mitigate | **REVIEWS HIGH #3 対応:** GRANT_READER_SQL は label スキーマに SELECT のみ付与（USAGE+SELECT）・**明示的 reader ロール（{reader} placeholder = keiba_readonly）のみ・`TO PUBLIC` は一切使用しない**。INSERT/UPDATE/DELETE/TRUNCATE は付与しない。下流 Phase 3-5 は読取専用 |
| T-02-04 | Tampering | 設定値の silent fallback / marker 誤判定 | mitigate | label_spec.yaml のロード失敗時は RuntimeError で fail-fast（D-13・quality_gate.py:131-133 のパターン踏襲）。未知の status 値や syubetucd は unresolved/is_model_eligible=False で隔離。**HIGH #5:** se_marker_canonicalization sentinel 集合で raw/numeric 両表現を許容し `'999.0' == 999` のような brittle 等価を回避 |
| T-02-RV1 | Tampering | label_validation_status と sales_start_entry_count confidence の conflation | mitigate | **REVIEWS HIGH #1:** label_spec.yaml で `sales_start_entry_count.source_confidence` を独立キーとして定義。Plan 03 は `label.fukusho_label.sales_start_entry_count_confidence` カラムを別途作成し `label_validation_status` とは独立して格納・下流フィルタが誤解しない |
| T-02-RV5 | Tampering | marker 列の文字列/数値表現揺れによる silent mislabeling | mitigate | **REVIEWS HIGH #5:** `se_marker_canonicalization` sentinel 集合が raw '999'/'999.0'/'999.00' と numeric 999.0 を同一視。Plan 03 `_canonicalize_markers` が sentinel `in` 判定のみを行い単一文字列等価を禁止。Plan 02 unit test が両表現で同一結果を assert |
| T-02-SC | Tampering | パッケージインストール | accept | 本 plan は新規外部パッケージインストール無し（Phase 1 の依存関係で完結・RESEARCH Package Legitimacy Audit 検証済み） |
</threat_model>

<verification>
- `test -f src/config/label_spec.yaml` 成功
- `python3 -c "import yaml; yaml.safe_load(open('src/config/label_spec.yaml'))"` 成功
- `uv run python scripts/run_apply_schema.py` の終了コード 0
- `grep -c "SCHEMA label" src/db/schema.py` >= 6
- `python3 -c "from src.config.settings import Settings; assert Settings.model_fields['db_schema_label'].default == 'label'"`
- 事後 psql 検証: `SELECT has_schema_privilege('keiba_etl', 'label', 'USAGE, CREATE')` が true を返す（オプション・blocker でなければ省略可）
- **HIGH #3:** `grep -c "TO PUBLIC" scripts/apply_schema.sql` が 0
</verification>

<success_criteria>
- label_spec.yaml が静的正として作成され label_generation_version='v1.0.0' でバージョン管理（SC#1 前提・LABEL-01/02/03/04 の定義参照元）
- ETL ロールが label スキーマに書込権を持つ（SC#1 前提・LABEL-01 ラベル生成が実行可能）
- reader ロールが label スキーマに SELECT 権を持つ（下流 Phase 3-5 準備・明示的 reader ロール付与・PUBLIC ではない）
- run_apply_schema.py が exit 0 で実DBに反映（Pitfall 6 回避）
- **REVIEWS 解決:** HIGH #1（source_confidence 分離）/ #3（PUBLIC 禁止）/ #4（SE DataKubun='9' 包含）/ #5（marker canonicalization）+ MEDIUM（class_eligibility 未勝利 precision）が全て label_spec.yaml / schema.py に組み込まれる
</success_criteria>

<output>
Create `.planning/phases/02-fukusho-labels/02-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

### 新規シンボル・ファイル・カラム（本 plan が作成）

**YAML keys（src/config/label_spec.yaml）:**
- `label_generation_version: "v1.0.0"`
- `status_values: ["validated", "inferred", "dead_heat", "unresolved"]`
- `payout_places_rules.{min_torokutosu_for_fukusho_sale, places_5_to_7_horses, places_8_or_more_horses, no_sale_marker_value}`
- `sales_start_entry_count.{proxy_source, source_confidence, source_note}`（**HIGH #1: source_confidence が label_validation_status から分離**）
- `model_ineligibility_syubetucd: ["18", "19"]`
- `newcomer_syubetucd: ["11", "12"]`
- `class_eligibility.{minimum_class_level_numeric, maiden_syubetucd, maiden_ineligible_syubetucd}`（**MEDIUM 解決: 未勝利は syubetucd で識別**）
- `se_marker_canonicalization.{enabled, harontimel3_sentinels, timediff_sentinels, time_sentinels_absent, time_sentinels_present_hint, bataijyu_sentinels_scratch, datakubun_sentinels_race_cancelled}`（**HIGH #5: sentinel 集合**）
- `se_marker_rules.{scratch_cancel, dead_loss_individual, race_excluded_before_start, race_cancelled}`（各 .condition は canonicalized_* 列を参照）
- `se_datakubun_inclusion.{required_se_datakubun_values}`（**HIGH #4 予防: ["7","9"]**）
- `hr_status_rules.{monday_confirmed_datakubun, flash_report_datakubun, fuseiritu_flag2_value, tokubarai_flag2_value}`
- `dead_heat_rules.{detection, slot4_used_count, slot5_used_count, status, is_model_eligible}`（**payout-table-authoritative・MEDIUM #2 対応**）
- `ineligibility_reason_codes: [obstacle, newcomer, no_fukusho_sale, unresolved, race_or_horse_cancelled, class_below_minimum, status_not_eligible]`（**HIGH #6 docstring: dead_loss は単独では不適格理由にならない**）
- `unresolved_strategy: "error_and_isolate"`

**新規フィールド（src/config/settings.py）:**
- `Settings.db_schema_label: str = "label"`
- `Settings.db_reader_role: str = "keiba_readonly"`（**HIGH #3 支援**: Plan 03 `_idempotent_load_label` が staging RENAME 後に `GRANT SELECT ON label.fukusho_label TO {reader_role}` を発行する際のロール名・PUBLIC ではなく明示的 reader ロール）

**SQL 文（src/db/schema.py の GRANT_ETL_SQL/GRANT_READER_SQL 内）:**
- `GRANT USAGE, CREATE ON SCHEMA label TO {etl}`
- `GRANT SELECT, INSERT, UPDATE, DELETE, TRUNCATE ON ALL TABLES IN SCHEMA label TO {etl}`
- `ALTER DEFAULT PRIVILEGES IN SCHEMA label GRANT SELECT, INSERT, UPDATE, DELETE, TRUNCATE ON TABLES TO {etl}`
- `GRANT USAGE ON SCHEMA label TO {reader}`（**HIGH #3: 明示的 reader ロール・PUBLIC ではない**）
- `GRANT SELECT ON ALL TABLES IN SCHEMA label TO {reader}`
- `ALTER DEFAULT PRIVILEGES IN SCHEMA label GRANT SELECT ON TABLES TO {reader}`

**search_path 構成:**
- `make_pool(role='etl')` → `search_path = "label,normalized,public"`

**ファイル:**
- `src/config/label_spec.yaml`（新規）
- `scripts/apply_schema.sql`（更新・dry-run 出力で同期・`TO PUBLIC` が含まれないことを保証）
