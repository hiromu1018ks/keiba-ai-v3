---
phase: 02-fukusho-labels
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/label_spec.yaml
  - src/db/schema.py
  - src/db/connection.py
  - src/config/settings.py
  - scripts/apply_schema.sql
autonomous: true
requirements: [LABEL-01, LABEL-02, LABEL-03, LABEL-04]
tags: [phase-02, labels, schema, grant, config, foundation]

must_haves:
  truths:
    - "label_spec.yaml が Git 管理下に存在し label_generation_version='v1.0.0' を含む（D-07）"
    - "ETL ロールが label スキーマに USAGE+CREATE+書込権を持つ（Pitfall 6・SC#1 前提）"
    - "reader ロールが label スキーマに USAGE+SELECT を持つ（下流 Phase 3-5・T-02-03）"
    - "make_pool(role='etl') の search_path が label,normalized,public を含む"
    - "run_apply_schema.py の再実行が exit 0 で GRANT 拡張を実DBに反映"
  artifacts:
    - path: "src/config/label_spec.yaml"
      provides: "ラベル定義の静的正（label_generation_version/status 定義/payout 境界/SE marker/§7.2 適格性）"
      contains: "label_generation_version"
    - path: "src/db/schema.py"
      provides: "GRANT_ETL_SQL と GRANT_READER_SQL の label スキーマ拡張"
      contains: "GRANT USAGE, CREATE ON SCHEMA label"
    - path: "src/config/settings.py"
      provides: "db_schema_label='label' フィールド"
      contains: "db_schema_label"
  key_links:
    - from: "src/db/schema.py GRANT_ETL_SQL"
      to: "scripts/run_apply_schema.py apply()"
      via: "APPLY_ORDER 内の grant_etl ステップ実行"
      pattern: "GRANT USAGE, CREATE ON SCHEMA label"
    - from: "src/db/connection.py make_pool(role='etl')"
      to: "src/config/settings.py db_schema_label"
      via: "search_path 構築で参照"
      pattern: "db_schema_label"
---

<objective>
Phase 2 の基盤を整える: (1) `src/config/label_spec.yaml` を作成しラベル定義の静的正（label_generation_version=payout 境界・SE marker rules・§7.2 適格性・status 分類）を Git 管理下に置く（D-07）、(2) `src/db/schema.py` の GRANT_ETL_SQL / GRANT_READER_SQL を label スキーマ向けに拡張し `scripts/apply_schema.sql` も同期、(3) `src/config/settings.py` に `db_schema_label` を追加し `src/db/connection.py` の etl ロール search_path に label を組み込む、(4) `scripts/run_apply_schema.py` を再実行して GRANT 拡張を実DBに反映（Pitfall 6）。

Purpose: 後続 Plan 02/03/04 が label_spec.yaml の値を参照し、ETL ロールで label.fukusho_label へ idempotent 書込ができるようにする。Phase 1 01-03 と同じ GRANT 拡張パターンの再適用（HIGH #5/#6 互換）。ラベルは予測目標の正であり、設定/権限の欠陥は下流すべてを汚染する聖域の前提。

Output: 5 ファイル修正/新規。実DBの label スキーマ GRANT 反映。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-fukusho-labels/02-CONTEXT.md
@.planning/phases/02-fukusho-labels/02-RESEARCH.md
@.planning/phases/02-fukusho-labels/02-PATTERNS.md
@src/db/schema.py
@src/db/connection.py
@src/config/settings.py
@src/config/class_normalization.yaml
@src/config/code_tables.yaml
@src/etl/filters.py
@scripts/run_apply_schema.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: label_spec.yaml 作成（ラベル定義の静的正・D-07 Git 管理）</name>
  <files>src/config/label_spec.yaml</files>

  <read_first>
    - src/config/class_normalization.yaml（1-131 行・ヘッダコメント慣行・`post_2019_class_system_reform_date` / jyokencd5_map 構造・unresolved_strategy パターンの写し）
    - src/config/code_tables.yaml（1-42 行・`syubetucd` 実測値 '00'/'11'/'12'/'13'..'19' の確認）
    - .planning/phases/02-fukusho-labels/02-RESEARCH.md:334-350（§10.5 の6検査 BLOCK/INFO 対応表）
    - .planning/phases/02-fukusho-labels/02-RESEARCH.md:405-412（取消/中止/除外識別条件表・実カラム名 bataijyu/harontimel3/timediff）
    - .planning/phases/02-fukusho-labels/02-RESEARCH.md:582-587（Open Question #1・`label_generation_version: "v1.0.0"` 推奨）
    - .planning/phases/02-fukusho-labels/02-CONTEXT.md:24-51（D-01..D-04 locked decisions・status 4値と is_model_eligible 写像）
    - docs/keiba_ai_requirements_v1.3.md §7.2-7.3（適格性ルールの正）
    - docs/keiba_ai_requirements_v1.3.md §10.2-10.6（ラベル仕様の正）
  </read_first>

  <action>
    `src/config/class_normalization.yaml:1-15` のヘッダコメント慣行（由来・実測根拠・CODE.md 不使用明記・D-13 silent fallback 禁止）を写した `label_spec.yaml` を作成。由来は「PostgreSQL everydb2 実測（2026-06-17・JRA 2015+ 39,580 レース/553,891 馬行）+ 要件 §10.1-10.6 + §7.2-7.3 + CONTEXT.md D-01..D-04」と明記。

    以下のトップレベルキーを定義（実測値・CONTEXT.md locked decisions に厳密合致）:

    1. `label_generation_version: "v1.0.0"`（Open Question #1 推奨・セマンティック採番・ラベルロジック変更時に bump・Phase 3 snapshot metadata に埋め込まれる）

    2. `status_values: ["validated", "inferred", "dead_heat", "unresolved"]`（D-04 固定4値）

    3. `payout_places_rules`（D-01 厳格・学習除外対象）:
       - `min_torokutosu_for_fukusho_sale: 5`（5頭未満は複勝発売なし・§7.3）
       - `places_5_to_7_horses: 2`
       - `places_8_or_more_horses: 3`
       - `no_sale_marker_value: 0`
       - コメント: 「境界は HR PayFukusyoUmaban/FuseirituFlag2 の観測事実で最終確定（D-01 厳格）。TorokuTosu は補助」

    4. `sales_start_entry_count`（D-01 寛容・inferred）:
       - `proxy_source: "n_harai.TorokuTosu"`
       - `proxy_status: "inferred"`
       - `source_note: "AV (TORIKESI_JYOGAI) テーブルは本DBで0行・HappyoTime 復元は技術的に不可能（RESEARCH Pitfall 2）。TorokuTosu は SE 馬番数と 99.97% 一致（39,570/39,580）"`

    5. `model_ineligibility_syubetucd: ["18", "19"]`（§7.3 障害・code_tables.yaml 実測値）
    6. `newcomer_syubetucd: ["11", "12"]`（§7.3 新馬・2歳/3歳）
    7. `class_level_numeric_minimum: 1`（§7.2「1勝クラス以上」・class_normalization.yaml jyokencd5_map の class_level_numeric=1 (005/1勝クラス) が最小）

    8. `se_marker_rules`（D-04・RESEARCH Pitfall 4 実測識別条件表・実カラム名を使用）:
       - `scratch_cancel: {condition: "bataijyu == '000'", expected_count: 956, label: "予測対象外", is_model_eligible: false}`
       - `dead_loss_individual: {condition: "harontimel3 == '999.0' AND timediff == '9999' AND time NOT IN ('0.0', '', '9999')", expected_count: 3554, label: "fukusho_hit=0 で学習残", is_model_eligible: true}`（注: `timediff` は EveryDB2 マニュアル TimeDIFN だが本DB実カラム名・RESEARCH Pitfall 1）
       - `race_excluded_before_start: {condition: "harontimel3 == '999.0' AND timediff == '9999' AND time IN ('0.0', '', '9999')", expected_count: 0, label: "予測対象外（本DBでは発生なし）", is_model_eligible: false}`
       - `race_cancelled: {condition: "datakubun == '9'", expected_count_se: 376, label: "全馬 unresolved", is_model_eligible: false}`

    9. `hr_status_rules`（D-04・RESEARCH 実測）:
       - `monday_confirmed_datakubun: "2"`（本DBでは全件・validated）
       - `flash_report_datakubun: "1"`（本DBでは0件・将来用 inferred 分岐保持・D-13）
       - `fuseiritu_flag2_value: {value: "1", meaning: "複勝不成立", status: "unresolved", expected_count: 0}`
       - `tokubarai_flag2_value: {value: "1", meaning: "複勝特払", status: "validated_if_payout_exists_else_unresolved", expected_count: 0}`

    10. `dead_heat_rules`（D-04・LABEL-04）:
        - `detection: "payout_count > fukusho_payout_places AND fukusho_payout_places > 0"`
        - `slot4_used_count: 97`（実測・PayFukusyoUmaban4 使用）
        - `slot5_used_count: 1`（実測・PayFukusyoUmaban5 使用）
        - `status: "dead_heat"`
        - `is_model_eligible: true`（学習対象・払戻テーブル全対象馬を正例）

    11. `ineligibility_reason_codes`（D-03・RESEARCH `label.fukusho_label.ineligibility_reason` 値域）:
        `[obstacle, newcomer, no_fukusho_sale, unresolved, race_or_horse_cancelled, class_below_minimum, status_not_eligible]`

    12. `unresolved_strategy: "error_and_isolate"`（class_normalization.yaml:130 と同形式・D-13）

    YAML 形式・インデント2スペース・全文字列値をクォート（class_normalization.yaml:23-47 の `"701":` 形式に準拠）。fenced code block を action 内に置かないこと。
  </action>

  <verify>
    <automated>test -f src/config/label_spec.yaml &amp;&amp; grep -c "label_generation_version" src/config/label_spec.yaml | grep -q "^1$" &amp;&amp; python3 -c "import yaml; cfg = yaml.safe_load(open('src/config/label_spec.yaml')); assert cfg['label_generation_version'] == 'v1.0.0'; assert set(cfg['status_values']) == {'validated','inferred','dead_heat','unresolved'}; assert cfg['model_ineligibility_syubetucd'] == ['18','19']; assert cfg['se_marker_rules']['scratch_cancel']['condition'] == \"bataijyu == '000'\"; assert 'timediff' in cfg['se_marker_rules']['dead_loss_individual']['condition']; print('label_spec.yaml OK')"</automated>
  </verify>

  <acceptance_criteria>
    - src/config/label_spec.yaml が存在し `grep -c "label_generation_version" src/config/label_spec.yaml` が 1 を返す
    - `python3 -c "import yaml; yaml.safe_load(open('src/config/label_spec.yaml'))"` が例外なく成功（YAML 構文 valid）
    - `label_generation_version: "v1.0.0"` が含まれる（grep で一致）
    - `status_values` が正確に `["validated", "inferred", "dead_heat", "unresolved"]`（D-04）
    - `se_marker_rules.dead_loss_individual.condition` 文字列が `timediff`（NOT `timedifn`）を含む（Pitfall 1）
    - `se_marker_rules.scratch_cancel.condition` が `bataijyu == '000'`（実カラム名・NOT `bataiju`）
    - `model_ineligibility_syubetucd` が `["18", "19"]`（code_tables.yaml 実測値）
    - `unresolved_strategy: "error_and_isolate"` が含まれる（D-13）
  </acceptance_criteria>

  <done>label_spec.yaml が作成され label_generation_version='v1.0.0'・D-04 status 4値・D-01 payout 境界ルール・Pitfall 1/4 実カラム名識別条件・§7.2/7.3 適格性ルールを網羅。python yaml.safe_load が成功。</done>
</task>

<task type="auto">
  <name>Task 2: schema.py GRANT 拡張 + settings.py db_schema_label + connection.py search_path 拡張 + apply_schema.sql 同期</name>
  <files>src/db/schema.py, src/config/settings.py, src/db/connection.py, scripts/apply_schema.sql</files>

  <read_first>
    - src/db/schema.py:76-117（GRANT_READER_SQL / GRANT_ETL_SQL / REVOKE_RAW_WRITES_*_SQL の現状）
    - src/db/schema.py:131-139（APPLY_ORDER の順序）
    - src/config/settings.py:32-39（db_schema_raw / db_schema_normalized の現状）
    - src/db/connection.py:21-52（make_pool role='etl' の search_path 構築）
    - scripts/run_apply_schema.py:114-128（apply() が APPLY_ORDER の各ステップを実行する仕組み）
    - .planning/phases/02-fukusho-labels/02-PATTERNS.md:176-211（schema.py GRANT 拡張の具体3行・connection.py search_path 拡張）
    - .planning/phases/02-fukusho-labels/02-RESEARCH.md:682-687（AC 変更点3点・run_apply_schema.py 再実行必須）
    - scripts/apply_schema.sql（現状の GRANT_ETL_SQL/GRANT_READER_SQL レンダリング結果確認）
  </read_first>

  <action>
    **修正1 — src/db/schema.py GRANT_READER_SQL 拡張**: 現行 `GRANT_READER_SQL`（schema.py:76-86）の `GRANT USAGE ON SCHEMA normalized TO {reader};` 行の直後に3行追加:
    - `GRANT USAGE ON SCHEMA label TO {reader};`
    - `GRANT SELECT ON ALL TABLES IN SCHEMA label TO {reader};`
    - `ALTER DEFAULT PRIVILEGES IN SCHEMA label GRANT SELECT ON TABLES TO {reader};`
    （下流 Phase 3-5 の readonly ロールが label.fukusho_label を schema 修飾 SELECT するため・T-02-03）

    **修正2 — src/db/schema.py GRANT_ETL_SQL 拡張**: 現行 `GRANT_ETL_SQL`（schema.py:88-98）の `GRANT USAGE, CREATE ON SCHEMA normalized TO {etl};` 行の直後に3行追加（PATTFALL 6 対策・PATTERNS.md:194-200 の通り）:
    - `GRANT USAGE, CREATE ON SCHEMA label TO {etl};`
    - `GRANT SELECT, INSERT, UPDATE, DELETE, TRUNCATE ON ALL TABLES IN SCHEMA label TO {etl};`
    - `ALTER DEFAULT PRIVILEGES IN SCHEMA label GRANT SELECT, INSERT, UPDATE, DELETE, TRUNCATE ON TABLES TO {etl};`
    public/raw_everydb2 に対する REVOKE は変更しない（D-06 二重保護維持・REVOKE_RAW_WRITES_PUBLIC_SQL/REVOKE_RAW_WRITES_VIEW_SQL はそのまま）。APPLY_ORDER（schema.py:131-139）の順序・ステップ名は変更不要（grant_reader/grant_etl の SQL 文字列の中身が変わるだけ）。

    **修正3 — src/config/settings.py db_schema_label 追加**: `db_schema_normalized: str = "normalized"`（settings.py:39）の直後に `db_schema_label: str = "label"` を追加。db_schema_raw / db_schema_normalized と対称。

    **修正4 — src/db/connection.py search_path 拡張**: `make_pool(role='etl')` 分岐（connection.py:40-42）の `search_path = f"{settings.db_schema_normalized},public"` を `search_path = f"{settings.db_schema_label},{settings.db_schema_normalized},public"` に変更。`role='readonly'` 側（connection.py:38-39）は変更しない（readonly は schema 修飾 `label.fukusho_label` で SELECT するため search_path 依存性が低い・PATTERNS.md:234 通り）。

    **修正5 — scripts/apply_schema.sql 同期**: `scripts/apply_schema.sql` が schema.py の SQL 定数をレンダリングした成果物であるため、run_apply_schema.py を dry-run して生成される SQL を確認し、手動で apply_schema.sql にも同じ3行×2箇所（reader/etl）を追記。または次タスクの `run_apply_schema.py` 実行が schema.py の定数を直接使うため apply_schema.sql は参照用・更新は schema.py が source of truth であることを確認（run_apply_schema.py:117-128 は schema_module の定数を直接実行）。

    SQL 文字列結合による GRANT 構築は禁止・psycopg.sql.Identifier による {reader}/{etl} プレースホルダ置換は既存のまま維持（run_apply_schema.py:47-64 が処理・MEDIUM #3）。
  </action>

  <verify>
    <automated>grep -q "GRANT USAGE, CREATE ON SCHEMA label TO {etl};" src/db/schema.py &amp;&amp; grep -q "GRANT USAGE ON SCHEMA label TO {reader};" src/db/schema.py &amp;&amp; grep -q "db_schema_label: str = \"label\"" src/config/settings.py &amp;&amp; grep -q "db_schema_label" src/db/connection.py &amp;&amp; python3 -c "from src.config.settings import Settings; from src.db.schema import GRANT_ETL_SQL, GRANT_READER_SQL; assert 'SCHEMA label' in GRANT_ETL_SQL, 'GRANT_ETL_SQL に label が無い'; assert 'SCHEMA label' in GRANT_READER_SQL, 'GRANT_READER_SQL に label が無い'; print('schema/connection/settings OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "GRANT USAGE, CREATE ON SCHEMA label TO {etl};" src/db/schema.py` が 1 を返す
    - `grep -c "GRANT SELECT, INSERT, UPDATE, DELETE, TRUNCATE ON ALL TABLES IN SCHEMA label TO {etl};" src/db/schema.py` が 1 を返す
    - `grep -c "GRANT USAGE ON SCHEMA label TO {reader};" src/db/schema.py` が 1 を返す
    - `grep -c "ALTER DEFAULT PRIVILEGES IN SCHEMA label" src/db/schema.py` が 2 を返す（etl+reader の2箇所）
    - `grep -c "REVOKE UPDATE, DELETE, TRUNCATE ON ALL TABLES IN SCHEMA public" src/db/schema.py` が 2 を返すまま（D-06 二重保護維持・変更なし）
    - `python3 -c "from src.config.settings import Settings; import inspect; src = inspect.getsource(Settings); assert 'db_schema_label' in src"` が成功
    - `python3 -c "from src.db.connection import make_pool; import inspect; src = inspect.getsource(make_pool); assert 'db_schema_label' in src and 'db_schema_normalized' in src"` が成功（label が normalized の前に来る検証）
    - `python3 -c "from src.db.schema import GRANT_ETL_SQL, GRANT_READER_SQL; assert GRANT_ETL_SQL.count('SCHEMA label') >= 3 and GRANT_READER_SQL.count('SCHEMA label') >= 2"` が成功
  </acceptance_criteria>

  <done>GRANT_ETL_SQL と GRANT_READER_SQL に label スキーマ権限が追加され、settings.py に db_schema_label='label' が定義され、connection.py の etl ロール search_path が label,normalized,public になる。public/raw_everydb2 の REVOKE は変更なし（D-06 維持）。</done>
</task>

<task type="auto">
  <name>Task 3: run_apply_schema.py 再実行で GRANT 拡張を実DBに反映（Pitfall 6）</name>
  <files>scripts/apply_schema.sql</files>

  <read_first>
    - scripts/run_apply_schema.py:106-146（apply() 関数・CREATE SCHEMA → CREATE ROLE → CREATE VIEW → GRANT → REVOKE の順序・psycopg.sql.Identifier 置換）
    - scripts/run_apply_schema.py:149-185（main()・KEIBA_ADMIN_DSN/KEIBA_DB_USER/KEIBA_ETL_DB_USER 必須・RuntimeError fail-fast）
    - src/db/schema.py:42-44（CREATE_SCHEMAS_SQL が `CREATE SCHEMA IF NOT EXISTS label` を含む・idempotent）
    - .env（KEIBA_ADMIN_DSN / KEIBA_DB_USER=keiba_readonly / KEIBA_ETL_DB_USER=keiba_etl が設定済みであることを確認・Phase 1 完了済み前提）
    - .planning/phases/02-fukusho-labels/02-RESEARCH.md:422-427（Pitfall 6・permission denied for schema label を避ける）
  </read_first>

  <action>
    `scripts/apply_schema.sql` を最新の schema.py 定数と同期するため、まず dry-run で出力を確認:
    `uv run python scripts/run_apply_schema.py --dry-run`
    dry-run 出力の `grant_etl` ステップに `GRANT USAGE, CREATE ON SCHEMA label TO "keiba_etl";` が3行含まれること・`grant_reader` に `GRANT USAGE ON SCHEMA label TO "keiba_readonly";` 等が含まれることを確認。

    apply_schema.sql を dry-run 出力で上書き更新（参照用・run_apply_schema.py は schema.py 定数を直接実行するが、sql ファイルも最新状態を保つ慣行・Phase 1 01-01/01-03 と同じ）。または `scripts/apply_schema.sql` が既に存在しない場合は新規作成。

    実行: `uv run python scripts/run_apply_schema.py` （--dry-run 無し）
    - KEIBA_ADMIN_DSN で管理者接続・autocommit=True（run_apply_schema.py:115）
    - 全ステップが順次実行され、最後に `schema applied successfully` が INFO ログで出る
    - 既存テーブル/ロールは idempotent（CREATE SCHEMA IF NOT EXISTS / DO $$ BEGIN IF NOT EXISTS）
    - 既存 GRANT は重複発行されるが PostgreSQL は冪等（同一権限の重複 GRANT は無害）

    実行後、psql または readonly_cur で GRANT が反映されたことを検証クエリで確認:
    `SELECT has_schema_privilege('keiba_etl', 'label', 'USAGE, CREATE')` → true
    `SELECT has_schema_privilege('keiba_readonly', 'label', 'USAGE')` → true

    失敗した場合（RuntimeError・権限エラー・接続エラー）は plan を止めて SUMMARY の blockers に記録。.env の KEIBA_ADMIN_DSN 未設定は Phase 1 の時点で要件違反なので前提確認。
  </action>

  <verify>
    <automated>uv run python scripts/run_apply_schema.py 2>&amp;1 | tail -5 | grep -q "schema applied successfully"</automated>
  </verify>

  <acceptance_criteria>
    - `uv run python scripts/run_apply_schema.py` の終了コードが 0
    - 標準出力/ログの最後に `schema applied successfully` が含まれる
    - `scripts/apply_schema.sql` 内の `SCHEMA label` の出現回数が5以上（reader: USAGE/SELECT/DEFAULT PRIVILEGES = 3行、etl: USAGE,CREATE/書込4種/DEFAULT PRIVILEGES = 3行 = 合計6箇所以上の "SCHEMA label" 文字列）
    - dry-run 実行時の出力に `"keiba_etl"` と `"keiba_readonly"` の Identifier quote が含まれる（MEDIUM #3・SQL injection 回避）
    - D-06 二重保護が維持される: `grep -c "REVOKE UPDATE, DELETE, TRUNCATE" scripts/apply_schema.sql` が4以上（public×2 + raw_everydb2×2）
  </acceptance_criteria>

  <done>run_apply_schema.py が exit 0 で完了し、keiba_etl ロールが label スキーマに USAGE+CREATE+書込権を持ち、keiba_readonly が label スキーマに USAGE+SELECT を持つ。public/raw_everydb2 への書込権剥奪は維持。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ETL ロール → label スキーマ | 書込境界・今回新規追加。ETL ロールのみが INSERT/UPDATE/TRUNCATE を持つ |
| reader ロール → label スキーマ | 読取境界・下流 Phase 3-5 が使用。SELECT のみ |
| ETL/reader → public/raw_everydb2 | 既存の raw 不変性境界（D-06）。本 plan で変更しない |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | GRANT_ETL_SQL label 拡張 | mitigate | ETL ロールに label のみ USAGE+CREATE+書込を付与。public/raw_everydb2 への書込権は付与しない（REVOKE 維持・D-06）。GRANT 文は schema.py のプレースホルダ {etl} を psycopg.sql.Identifier で安全に置換（run_apply_schema.py:59-64・MEDIUM #3） |
| T-02-02 | Tampering | label_spec.yaml の改ざん | mitigate | D-07 Git 管理下に配置。label_generation_version='v1.0.0' でバージョン管理・ラベルロジック変更時に bump。Phase 3 snapshot metadata に埋め込まれ再現性保証（§19.1） |
| T-02-03 | Information Disclosure | reader ロールの過剰権限 | mitigate | GRANT_READER_SQL は label スキーマに SELECT のみ付与（USAGE+SELECT）。INSERT/UPDATE/DELETE/TRUNCATE は付与しない。下流 Phase 3-5 は読取専用 |
| T-02-04 | Tampering | 設定値の silent fallback | mitigate | label_spec.yaml のロード失敗時は RuntimeError で fail-fast（D-13・quality_gate.py:131-133 のパターン踏襲）。未知の status 値や syubetucd は unresolved/is_model_eligible=False で隔離 |
| T-02-SC | Tampering | パッケージインストール | accept | 本 plan は新規外部パッケージインストール無し（Phase 1 の依存関係で完結・RESEARCH Package Legitimacy Audit 検証済み） |
</threat_model>

<verification>
- `test -f src/config/label_spec.yaml` 成功
- `python3 -c "import yaml; yaml.safe_load(open('src/config/label_spec.yaml'))"` 成功
- `uv run python scripts/run_apply_schema.py` の終了コード 0
- `grep -c "SCHEMA label" src/db/schema.py` >= 6
- `python3 -c "from src.config.settings import Settings; assert Settings.model_fields['db_schema_label'].default == 'label'"`
- 事後 psql 検証: `SELECT has_schema_privilege('keiba_etl', 'label', 'USAGE, CREATE')` が true を返す（オプション・blocker でなければ省略可）
</verification>

<success_criteria>
- label_spec.yaml が静的正として作成され label_generation_version='v1.0.0' でバージョン管理（SC#1 前提・LABEL-01/02/03/04 の定義参照元）
- ETL ロールが label スキーマに書込権を持つ（SC#1 前提・LABEL-01 ラベル生成が実行可能）
- reader ロールが label スキーマに SELECT 権を持つ（下流 Phase 3-5 準備）
- run_apply_schema.py が exit 0 で実DBに反映（Pitfall 6 回避）
</success_criteria>

<output>
Create `.planning/phases/02-fukusho-labels/02-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

### 新規シンボル・ファイル・カラム（本 plan が作成）

**YAML keys（src/config/label_spec.yaml）:**
- `label_generation_version: "v1.0.0"`
- `status_values: ["validated", "inferred", "dead_heat", "unresolved"]`
- `payout_places_rules.{min_torokutosu_for_fukusho_sale, places_5_to_7_horses, places_8_or_more_horses, no_sale_marker_value}`
- `sales_start_entry_count.{proxy_source, proxy_status, source_note}`
- `model_ineligibility_syubetucd: ["18", "19"]`
- `newcomer_syubetucd: ["11", "12"]`
- `class_level_numeric_minimum: 1`
- `se_marker_rules.{scratch_cancel, dead_loss_individual, race_excluded_before_start, race_cancelled}`（各 .condition/.expected_count/.label/.is_model_eligible）
- `hr_status_rules.{monday_confirmed_datakubun, flash_report_datakubun, fuseiritu_flag2_value, tokubarai_flag2_value}`
- `dead_heat_rules.{detection, slot4_used_count, slot5_used_count, status, is_model_eligible}`
- `ineligibility_reason_codes: [obstacle, newcomer, no_fukusho_sale, unresolved, race_or_horse_cancelled, class_below_minimum, status_not_eligible]`
- `unresolved_strategy: "error_and_isolate"`

**新規フィールド（src/config/settings.py）:**
- `Settings.db_schema_label: str = "label"`

**SQL 文（src/db/schema.py の GRANT_ETL_SQL/GRANT_READER_SQL 内）:**
- `GRANT USAGE, CREATE ON SCHEMA label TO {etl}`
- `GRANT SELECT, INSERT, UPDATE, DELETE, TRUNCATE ON ALL TABLES IN SCHEMA label TO {etl}`
- `ALTER DEFAULT PRIVILEGES IN SCHEMA label GRANT SELECT, INSERT, UPDATE, DELETE, TRUNCATE ON TABLES TO {etl}`
- `GRANT USAGE ON SCHEMA label TO {reader}`
- `GRANT SELECT ON ALL TABLES IN SCHEMA label TO {reader}`
- `ALTER DEFAULT PRIVILEGES IN SCHEMA label GRANT SELECT ON TABLES TO {reader}`

**search_path 構成:**
- `make_pool(role='etl')` → `search_path = "label,normalized,public"`

**ファイル:**
- `src/config/label_spec.yaml`（新規）
- `scripts/apply_schema.sql`（更新・dry-run 出力で同期）
