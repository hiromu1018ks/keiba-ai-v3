---
phase: 02-fukusho-labels
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - src/etl/fukusho_label.py
  - scripts/run_label_etl.py
  - tests/test_raw_immutability.py
autonomous: true
requirements: [LABEL-01, LABEL-02, LABEL-04]
tags: [phase-02, labels, etl, idempotent-staging-swap, raw-immutability, pandas, psycopg3, reviews-rev1, reviews-rev2-null-safe-padded-umaban, reviews-rev2-no-row-multiplication, reviews-rev2-pd-isna-missing-time]

must_haves:
  truths:
    - "src/etl/fukusho_label.py が存在し compute_fukusho_labels / classify_status / compute_is_model_eligible / _canonicalize_markers / run_label_etl を export する"
    - "run_label_etl が readonly pool で raw を SELECT のみし、etl pool で label.fukusho_label に idempotent 書込する"
    - "label.fukusho_label テーブルが §10.2 保持項目 + D-03 is_model_eligible + D-04 状態マーカー + label_generation_version + sales_start_entry_count_confidence を網羅する（REVIEWS HIGH #1）"
    - "raw vs valid drift が dead_heat 境界のみ（実測7行/553,891・99.9987%一致）"
    - "取消/中止/レース全体中止/不成立が観測事実ベース（D-04）で正しく分類される"
    - "_select_se_state が datakubun IN ('7','9') で SELECT し race_cancelled (DataKubun='9') 376行が落とされない（REVIEWS HIGH #4）"
    - "_canonicalize_markers が label_spec.yaml の se_marker_canonicalization sentinel 集合のみを使用し単一文字列等価比較を行わない（REVIEWS HIGH #5）"
    - "_canonicalize_markers が pd.isna guard で missing/null time を '__MISSING__' sentinel にマップし time_present=False に分類する・NaN/pd.NA/None が誤って is_dead_loss=True になる silent corruption を防止（REVIEWS NEW HIGH #3）"
    - "_select_se_state の timediff merge が両側 datakubun IN ('7','9') でフィルタ + merge キーに datakubun を含め 1:1 merge となり row-multiplication を構造的に防止する（REVIEWS NEW HIGH #2）"
    - "ETL 前後で raw(public.n_*) が不変であることが test_raw_immutability.py の拡張で直接証明される"
    - "ETL が idempotent・再実行で行重複しない（staging-swap）。staging RENAME 後に主キー・インデックス・GRANT・コメントが保存される（REVIEWS HIGH #3）"
    - "GRANT SELECT ... TO PUBLIC を使用しない・reader ロール明示付与でステージ swap 後の権限を再構築する（REVIEWS HIGH #3）"
  artifacts:
    - path: "src/etl/fukusho_label.py"
      provides: "複勝ラベル ETL 本体・compute_fukusho_labels / classify_status / compute_is_model_eligible / _canonicalize_markers / run_label_etl / _select_raw_harai / _select_se_state / _idempotent_load_label"
      contains: "def run_label_etl"
      min_lines: 450
    - path: "scripts/run_label_etl.py"
      provides: "ETL エントリポイント・readonly/etl pool 構築・run_label_etl 呼出"
      contains: "run_label_etl"
    - path: "tests/test_raw_immutability.py"
      provides: "label ETL 追加後の raw 不変性拡張テスト"
      contains: "run_label_etl"
  key_links:
    - from: "src/etl/fukusho_label.py run_label_etl"
      to: "src/etl/normalize.py _idempotent_load（再利用または label 版）"
      via: "staging-swap パターン + post-rename 再 GRANT・index 再構築"
      pattern: "_staging|RENAME TO|GRANT SELECT ON label.fukusho_label TO"
    - from: "src/etl/fukusho_label.py _select_raw_harai"
      to: "public.n_harai (raw)"
      via: "readonly pool SELECT"
      pattern: "FROM public.n_harai"
    - from: "src/etl/fukusho_label.py _select_se_state"
      to: "normalized.n_uma_race + public.n_uma_race.timediff (raw)"
      via: "readonly pool SELECT・両側 datakubun IN ('7','9') + merge キー datakubun 含む 1:1 merge・race_cancelled 376行を含める（NEW HIGH #2）"
      pattern: "FROM normalized.n_uma_race|FROM public.n_uma_race|datakubun IN"
    - from: "src/etl/fukusho_label.py compute_fukusho_labels"
      to: "src/config/label_spec.yaml"
      via: "load_label_spec() で読込・payout_places_rules / se_marker_rules / se_marker_canonicalization / class_eligibility 参照"
      pattern: "load_label_spec"
---

<objective>
`src/etl/fukusho_label.py` を新規作成し、複勝ラベル ETL 本体を実装する。Plan 02 で RED 状態にした25テスト（test_fukusho_label.py）を GREEN にする。HR（n_harai raw 直読み・明示キャスト）と SE（normalized.n_uma_race + 新規 raw.timediff）を読込、pandas で `fukusho_hit_raw`（KakuteiJyuni-based）/ `fukusho_hit_validated`（PayFukusyoUmaban1..5-based）/ `label_validation_status`（D-04 4値）/ `is_model_eligible`（D-03 §7.2）を計算し、`label.fukusho_label` テーブルへ staging-swap idempotent 書込する。Phase 1 `normalize.py` の `_idempotent_load` を label スキーマ向けに適用（汎用化または新規同関数）。`scripts/run_label_etl.py` エントリポイント作成。`tests/test_raw_immutability.py` に label ETL 追加後の raw 不変性テストを拡張。

Purpose: 予測目標の正 `fukusho_hit_validated` を実DBの553,891馬行に生成する。SC#1（2層ラベル）/ SC#3（sales_start_entry_count）/ SC#4（同着/取消/中止/除外）の直接実装。リーク防止聖域。

**Reviews REV1 対応（本 plan が扱う HIGH 懸念）:**
- **HIGH #1 (inferred conflation):** `_create_label_table` / `_idempotent_load_label` が作成する `label.fukusho_label` スキーマに `sales_start_entry_count_confidence`（独立 varchar(16)・値 'inferred'）と `sales_start_entry_count_source`（'torokutosu_proxy'）の2カラムを明示的に含める。`label_validation_status` とは独立して格納される。Plan 02 の `test_sales_start_entry_count_proxy_and_source_confidence_separated_from_status` が両列の存在と値を assert。
- **HIGH #3 (staging-swap privilege/idempotency):** (1) `GRANT SELECT ON label.fukusho_label TO PUBLIC` を **使用しない**。代わりに staging RENAME 後に `GRANT SELECT ON label.fukusho_label TO keiba_readonly` を明示的に再発行（`settings.db_reader_role` 経由でロール名を取得・ハードコード禁止）。(2) インデックス・主キー・コメントが DROP+RENAME で失われないよう、staging 表は `CREATE TABLE ... (LIKE label.fukusho_label INCLUDING ALL)` で作成し、RENAME 後の PK/インデックスは INCLUDING ALL によって継承される。(3) 2回連続実行で row count と content checksum が一致することを `_idempotent_load_label` の rowcount 検証で保証。
- **HIGH #4 (race-cancelled rows dropped):** `_select_se_state` の SQL は `WHERE {PROJECT_WINDOW_FILTER} AND datakubun IN ('7','9')` を含む。`datakubun='7'`（confirmed SE）のみに絞ると race_cancelled 376 SE 行がラベル生成前に消失する silent data loss になる。Plan 02 `test_select_se_state_includes_datakubun_9` が `inspect.getsource` でこの条件を regression assert。
- **HIGH #5 (brittle marker comparisons):** `_canonicalize_markers(df, spec)` 関数を新規実装し、`label_spec.yaml` の `se_marker_canonicalization` sentinel 集合のみで判定する。`harontimel3 == '999.0'` のような単一文字列等価比較は禁止・sentinel 集合への `in` 判定のみ許可。raw が varchar で normalized が numeric の両方で同一結果を返す。Plan 02 `test_canonicalize_markers_raw_string_form` / `test_canonicalize_markers_numeric_cast_form` が両表現で同一結果を assert。
- **NEW HIGH #2 (timediff merge row multiplication):** `_select_se_state` が `public.n_uma_race.timediff` を取得する際、両側（normalized 側 + public 側）で `datakubun IN ('7','9')` をフィルタし、merge キーに `datakubun` を含めることで 1:1 merge を保証する。public 側が複数 DataKubun 行を持つ場合でも PK+datakubun で厳密 1:1 となり SE 行の増殖を構造的に防止。merge 前後で `len(se_df) == len(merged_df)` をコード内で assert（不一致は RuntimeError・D-13）。Plan 02 `test_select_se_state_no_row_multiplication_on_timediff_merge` が regression assert。
- **NEW HIGH #3 (missing time misclassified as present):** `_canonicalize_markers` の marker 列正規化で、`str()` 変換の前に `pd.isna(v)` guard を必ず実行し、missing/null（NaN / pd.NA / None）を sentinel `"__MISSING__"`（`label_spec.yaml.se_marker_canonicalization.missing_value_sentinel`・Plan 01 追加）にマップする。`time_present` 判定は `canonicalized_time != missing_value_sentinel` を最初の条件とし、missing time は `time_present=False` → dead_loss にならず is_race_excluded/unresolved に分類される。これにより「time 欠損馬が誤って競走中止判定で fukusho_hit=0 学習混入」の silent corruption を防止。Plan 02 `test_canonicalize_markers_missing_time`（None/NaN/pd.NA 3 variant）が regression assert。

Output: 2新規ファイル + 1テスト拡張。label.fukusho_label テーブルに全馬行のラベルが格納される。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-fukusho-labels/02-CONTEXT.md
@.planning/phases/02-fukusho-labels/02-RESEARCH.md
@.planning/phases/02-fukusho-labels/02-PATTERNS.md
@.planning/phases/02-fukusho-labels/02-01-PLAN.md
@.planning/phases/02-fukusho-labels/02-02-PLAN.md
@.planning/phases/02-fukusho-labels/02-REVIEWS.md
@src/etl/normalize.py
@src/etl/quality_gate.py
@src/etl/class_normalize.py
@src/etl/filters.py
@src/etl/raw_fingerprint.py
@src/db/connection.py
@src/db/schema.py
@src/config/label_spec.yaml
@src/config/settings.py
@tests/test_fukusho_label.py
@tests/test_raw_immutability.py
@scripts/run_normalized_etl.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fukusho_label.py ETL 本体（compute_fukusho_labels / classify_status / compute_is_model_eligible / _canonicalize_markers / _select_raw_harai / _select_se_state / run_label_etl・HIGH #1/#3/#4/#5 対応）</name>
  <files>src/etl/fukusho_label.py</files>

  <behavior>
    - compute_fukusho_labels(hr_df, se_df, race_df, *, spec) -> pd.DataFrame: 合成 DataFrame に対し fukusho_hit_raw / fukusho_hit_validated / label_validation_status / is_model_eligible / 状態マーカーを計算して1行/馬を返す（test_fukusho_label.py の25テスト GREEN）
    - classify_status(row, *, spec) -> str: D-04 4値 status を返す（validated/inferred/dead_heat/unresolved）
    - compute_is_model_eligible(row, *, spec) -> tuple[bool, str | None]: §7.2 + status で (is_eligible, reason) を返す・syubetucd 障害/新馬が先に評価され dead_loss は単独では不適格理由にならない（HIGH #6 契約）
    - _canonicalize_markers(df, *, spec) -> pd.DataFrame: label_spec.yaml se_marker_canonicalization sentinel 集合のみで marker 判定・raw varchar と numeric cast と missing/null の3表現で同一結果（HIGH #5・**NEW HIGH #3**: pd.isna で missing を先に捕捉し "__MISSING__" sentinel にマップ→ time_present=False）
    - _select_raw_harai(read_cur) -> pd.DataFrame: public.n_harai から JRA 限定 SELECT・実カラム名 PayFukusyoUmaban1..5 / DataKubun / FuseirituFlag2 / TorokuTosu / SyussoTosu / HenkanFlag2
    - _select_se_state(read_cur) -> pd.DataFrame: normalized.n_uma_race + public.n_uma_race.timediff（新規 raw SELECT）を統合・両側とも WHERE datakubun IN ('7','9') でフィルタし merge キーに datakubun を含めて race_cancelled 376行を含める（HIGH #4）・**NEW HIGH #2**: 1:1 merge で row-multiplication を構造的に防止・merge 前後で行数一致を assert
    - _idempotent_load_label(write_cur, rows, columns, *, reader_role) -> int: staging-swap で label.fukusho_label を atomic 替換・INCLUDING ALL で PK/インデックス継承・RENAME 後に明示的 reader role GRANT（PUBLIC 不使用・HIGH #3）
    - run_label_etl(read_pool, write_pool) -> dict: ETL エントリポイント・{"rows_inserted": int, "label_unresolved_count": int, "raw_touched": False, "checksum": str}
  </behavior>

  <read_first>
    - src/etl/normalize.py:1-675（全3層構造・_select_raw_race / _transform_race_df / _idempotent_load / _df_to_tuples / run_normalized_etl の完全な実装）
    - src/etl/normalize.py:178-240（_select_raw_race / _select_raw_uma_race の SELECT パターン・明示キャスト・WHERE 句構築）
    - src/etl/normalize.py:356-441（_idempotent_load staging-swap・CR-04 advisory_xact_lock/空入力拒否/rowcount 検証・INCLUDING ALL 指定箇所）
    - src/etl/normalize.py:520-585（_df_to_tuples / _row_to_tuple・pd.NaT/pd.NA/Int64→int変換）
    - src/etl/normalize.py:624-663（run_normalized_etl エントリポイント・戻り値 dict 構造）
    - src/etl/filters.py:31-39（PROJECT_WINDOW_FILTER / JRA_FILTER・CR-06 single source of truth）
    - src/etl/class_normalize.py:36-51（load_class_config YAML ロード・ValueError fail-fast パターン）
    - src/etl/quality_gate.py:91-106（CheckResult dataclass・参考）
    - src/etl/quality_gate.py:114-152（_load_allowed_codes YAML ロード・RuntimeError fail-fast）
    - src/config/label_spec.yaml（Plan 01 で作成・全キーを参照・特に sales_start_entry_count.source_confidence / se_marker_canonicalization / se_marker_rules / se_datakubun_inclusion / class_eligibility）
    - src/config/class_normalization.yaml:23-48（jyokencd5_map class_level_numeric の意味）
    - src/db/connection.py:21-52（make_pool role='readonly'/'etl'・search_path）
    - src/db/schema.py:76-117（GRANT_READER_SQL / GRANT_ETL_SQL の {reader}/{etl} プレースホルダ・PUBLIC 使用箇所の有無確認・HIGH #3）
    - src/config/settings.py（db_reader_role フィールドの有無確認・無ければ追加すべきか Plan 01 に差戻し）
    - .planning/phases/02-fukusho-labels/02-RESEARCH.md:213-296（Pattern 1 HR raw 直読み・Pattern 2 compute_fukusho_labels 実測ロジック）
    - .planning/phases/02-fukusho-labels/02-RESEARCH.md:300-351（Pattern 3 classify_status・§10.5 6検査対応表）
    - .planning/phases/02-fukusho-labels/02-RESEARCH.md:405-412（Pitfall 4 SE marker 識別条件表・実カラム名）
    - .planning/phases/02-fukusho-labels/02-RESEARCH.md:473-517（label.fukusho_label CREATE TABLE 推奨スキーマ）
    - .planning/phases/02-fukusho-labels/02-RESEARCH.md:522-557（compute_is_model_eligible 実装例）
    - .planning/phases/02-fukusho-labels/02-PATTERNS.md:23-89（fukusho_label.py の構造指針・Imports / HR SELECT / transform / idempotent load / ETL entrypoint）
    - .planning/phases/02-fukusho-labels/02-REVIEWS.md:37-50（HIGH #1/#3/#4/#5 詳細・suggestions）
    - tests/test_fukusho_label.py（Plan 02 で作成・25テスト・GREEN 目標・_canonicalize_markers / datakubun IN ('7','9') / sales_start_entry_count_confidence の各 assert に合致させる）
    - scripts/run_normalized_etl.py（エントリポイント・raw fingerprint 前後比較・masked DSN ログ）
  </read_first>

  <action>
    `src/etl/normalize.py` と同じ3層構造（select → transform → idempotent load）で `src/etl/fukusho_label.py` を新規作成。`from __future__ import annotations` / logging / typing.Any / pandas / psycopg.Cursor / psycopg_pool.ConnectionPool / `from src.etl.filters import PROJECT_WINDOW_FILTER, JRA_FILTER` / `from src.config.settings import Settings`（reader role 名取得のため） / `from src.etl.label_spec_loader import load_label_spec`（後述）を import。module-level `logger = logging.getLogger(__name__)`。

    **label_spec.yaml loader**: `load_label_spec(path: str | Path = _DEFAULT_CONFIG_PATH) -> dict[str, Any]` を定義。`src/etl/class_normalize.py:36-51` の `load_class_config` パターンを踏襲。`Path(path).open(encoding="utf-8")` + `yaml.safe_load(f)`。必須キー（label_generation_version / status_values / payout_places_rules / se_marker_rules / se_marker_canonicalization / se_datakubun_inclusion / sales_start_entry_count / model_ineligibility_syubetucd / newcomer_syubetucd / class_eligibility / unresolved_strategy）が欠損時は `ValueError`（D-13 silent fallback 禁止）。戻り値のキー存在検証。

    **_select_raw_harai(read_cur: Cursor) -> pd.DataFrame**: `src/etl/normalize.py:178-184` の `_select_raw_race` パターンを写し。`_HARAI_SELECT_COLUMNS = ["year", "jyocd", "kaiji", "nichiji", "racenum", "monthday", "datakubun", "torokutosu", "syussotosu", "fuseirituflag2", "tokubaraiflag2", "henkanflag2", "payfukusyounmaban1", "payfukusyounmaban2", "payfukusyounmaban3", "payfukusyounmaban4", "payfukusyounmaban5"]`。`f"SELECT {cols} FROM public.n_harai WHERE {PROJECT_WINDOW_FILTER}"`（CR-06・NAR + 2015以前を除外）。

    **_select_se_state(read_cur: Cursor) -> pd.DataFrame**（**REVIEWS HIGH #4 対応**）: normalized.n_uma_race から SELECT。`_SE_SELECT_COLUMNS = ["year", "jyocd", "kaiji", "nichiji", "racenum", "umaban", "kettonum", "kakuteijyuni", "nyusenjyuni", "bataijyu", "harontimel3", "harontimel4", "time", "dochakukubun", "dochakutosu", "datakubun"]`。SQL: `SELECT ... FROM normalized.n_uma_race WHERE {PROJECT_WINDOW_FILTER} AND datakubun IN ('7', '9')`。**`datakubun IN ('7', '9')` は HIGH #4 の核心** — `datakubun='7'` 単独では race_cancelled 376行が SELECT から落とされ silent data loss になる。`timediff` は normalized.n_uma_race に未格納（Phase 1 normalize.py が未 SELECT）のため public.n_uma_race.timediff を別 SELECT で取得し merge: `SELECT year, jyocd, kaiji, nichiji, racenum, umaban, kettonum, datakubun, timediff FROM public.n_uma_race WHERE {PROJECT_WINDOW_FILTER} AND datakubun IN ('7','9')` を発行し、PK (year, jyocd, kaiji, nichiji, racenum, umaban, kettonum, datakubun) で merge する（**REVIEWS NEW HIGH #2 対応**）。**public.n_uma_race 側も必ず `datakubun IN ('7','9')` でフィルタし、merge キーに datakubun を含める**（HIGH #2: public 側に複数 DataKubun 行が存在すると PK (year,jyocd,kaiji,nichiji,racenum,umaban,kettonum) のみの merge は one-to-many になり SE 行を増殖させる。datakubun で両側を等価制約すれば normalized 側 1 行 ↔ public 側 1 行の strict 1:1 になる・row-multiplication を構造的に防止）。**安全性の主表明**: `merge` 前後で `len(se_df) == len(merged_df)`（high #2: no-row-multiplication assertion）をコード内で assert・不一致時は RuntimeError で fail-fast（D-13 silent fallback 禁止）。Plan 02 `test_select_se_state_no_row_multiplication_on_timediff_merge` が両フレームの行数が一致することを regression assert する。

    **race-level 適格性情報 SELECT**: `_select_race_meta(read_cur) -> pd.DataFrame` で normalized.n_race から `["year", "jyocd", "kaiji", "nichiji", "racenum", "syubetucd", "class_code_normalized", "class_level_numeric", "class_normalization_status", "race_date"]` を SELECT。compute_fukusho_labels に渡して syubetucd / class_level_numeric 適格性判定に使用。

    **_canonicalize_markers(df: pd.DataFrame, *, spec: dict) -> pd.DataFrame**（**REVIEWS HIGH #5 対応**）: 入力 df の marker 列（bataijyu / harontimel3 / harontimel4 / timediff / time / datakubun）を、label_spec.yaml の `se_marker_canonicalization` sentinel 集合のみで判定し、`is_scratch_cancel` / `is_dead_loss` / `is_race_excluded` / `is_race_cancelled` の boolean 列を追加して df を返す。
    1. 各 marker 列を正規化。**REVIEWS NEW HIGH #3 対応: 必ずまず `pd.isna(v)` で missing/null を判定し、欠損時は sentinel `"__MISSING__"`（label_spec.yaml `se_marker_canonicalization.missing_value_sentinel`）にマップしてから `str()` に回す**。具体的には「`canonicalized = "__MISSING__" if pd.isna(v) else str(v).strip()`」とする。これにより `NaN`→`str(NaN)='nan'`・`pd.NA`→`str(pd.NA)='<NA>'`・`None`→`str(None)='None'` が sentinel 集合外と誤判定され「time_present=True」となる HIGH #3 バグを構造的に回避する。numeric 型の値も str() でキャストして sentinel 比較に回す（例: float 999.0 → '999.0'・int 0 → '0'）。これが「raw varchar と numeric cast と missing の3表現で同一結果」の核心。
    2. spec['se_marker_canonicalization'] の各 sentinel 集合を取得: `bataijyu_sentinels_scratch` / `harontimel3_sentinels` / `timediff_sentinels` / `time_sentinels_absent` / `datakubun_sentinels_race_cancelled` / `missing_value_sentinel`（=`"__MISSING__"`）。
    3. `is_scratch_cancel = canonicalized_bataijyu in bataijyu_sentinels_scratch`
    4. `is_race_cancelled = canonicalized_datakubun in datakubun_sentinels_race_cancelled`
    5. `marker_active = canonicalized_harontimel3 in harontimel3_sentinels AND canonicalized_timediff in timediff_sentinels`
    6. **HIGH #3 核心**: `time_present = (canonicalized_time != missing_value_sentinel) AND (canonicalized_time not in time_sentinels_absent) AND (canonicalized_time != '')`（missing/null time は time_present=False → dead_loss にならず is_race_excluded または unresolved に分類される）。これにより missing time の馬が誤って「発走後停止=競走中止」と判定され `fukusho_hit=0` で学習に混入する silent corruption を防ぐ。
    7. `is_dead_loss = marker_active AND time_present`（発走後停止＝競走中止）
    8. `is_race_excluded = marker_active AND not time_present`（発走前除外・本DBでは0件）
    **禁止**: `harontimel3 == '999.0'` のような単一文字列等価比較・`time NOT IN ('0.0','','9999')` のような固定値リスト。sentinel 集合への `in` 判定のみ許可。Plan 02 `test_canonicalize_markers_raw_string_form` / `test_canonicalize_markers_numeric_cast_form` / `test_canonicalize_markers_missing_time`（**NEW HIGH #3**）が3表現（raw 文字列・numeric キャスト・missing/null）で同一結果を assert するため、str() 変換で両者を統一し `pd.isna` で missing を捕捉することが必須。

    **compute_fukusho_labels(hr_df, se_df, race_df, *, spec: dict | None = None) -> pd.DataFrame**: RESEARCH Pattern 2（RESEARCH.md:255-296）を実装:
    1. `spec = spec or load_label_spec()` をデフォルト（明示的に渡された場合はそれを使用）
    2. hr_df / se_df / race_df の明示キャスト（`pd.to_numeric(errors='coerce')` for int 系: kakuteijyuni / torokutosu / syussotosu / bataijyu / kettonum / umaban）。`payfukusyounmaban1..5` は `'00'`/`''` を `pd.NA` に変換（A3・silent mislabeling 防止）
    3. hr_df から `payout_umaban_set`（馬番集合）を構築: `payfukusyounmaban1..5` の非NA値を set に格納
    4. se_df と hr_df を PK で left merge
    5. se_df と race_df を PK で left merge（syubetucd / class_level_numeric / race_date 追加）
    6. `_canonicalize_markers(merged, spec=spec)` を呼出して `is_scratch_cancel` / `is_dead_loss` / `is_race_excluded` / `is_race_cancelled` を付与（HIGH #5）
    7. `fukusho_payout_places` 計算: `torokutosu >= 8 → 3` / `5 <= torokutosu <= 7 → 2` / その他 → 0（label_spec.yaml payout_places_rules 参照）
    8. `is_fukusho_sale_available`: `torokutosu >= 5 AND fuseirituflag2 != '1'`
    9. `fukusho_hit_raw`: `(fukusho_payout_places > 0) AND (kakuteijyuni >= 1) AND (kakuteijyuni <= fukusho_payout_places)` を int（0/1）
    10. `fukusho_hit_validated`: `umaban（zfill(2)文字列）in payout_umaban_set` を int（0/1）
    11. `sales_start_entry_count` = `torokutosu`・`sales_start_entry_count_source = 'torokutosu_proxy'`・`sales_start_entry_count_confidence = spec['sales_start_entry_count']['source_confidence']`（='inferred'・HIGH #1: label_validation_status とは独立列）
    12. `final_starter_count` = `syussotosu`
    13. `is_dead_heat`: `(payout_count > fukusho_payout_places) AND (fukusho_payout_places > 0)`（payout_count は PayFukusyoUmaban1..5 の非NA件数・payout-table-authoritative・MEDIUM #2 対応）
    14. 各行に `classify_status(row, spec=spec)` を適用して `label_validation_status`
    15. 各行に `compute_is_model_eligible(row, spec=spec)` を適用して `is_model_eligible` / `ineligibility_reason`
    16. `label_generation_version` 列を spec['label_generation_version'] から全行に付与
    17. 1行/馬の DataFrame を返す

    **classify_status(row, *, spec) -> str**: RESEARCH Pattern 3（RESEARCH.md:300-334）を実装。順序依存あり（早期 return）。**D-04 観測事実ベース・D-13 silent fallback 禁止**・将来のDB更新（FuseirituFlag2/TokubaraiFlag2 の発生）に備え全分岐を明示:
    1. `is_race_cancelled` または `datakubun_harai == '9'`（spec['datakubun_sentinels_race_cancelled'] で判定）→ 'unresolved'
    2. `fuseirituflag2 == '1'`（複勝不成立）→ 'unresolved'
    3. `tokubaraiflag2 == '1'`（複勝特払・D-04・label_spec.yaml `hr_status_rules.tokubarai_flag2_value` 参照）→ payout_umaban_set が非空（`payout_count > 0`）なら 'validated'（対象馬あり・正当な正例）/ 空（`payout_count == 0`）なら 'unresolved'（対象馬なし・outcome 非確定）。実DBでは0件だが分岐保持は D-13 の必須要件・fuseirituflag2 分岐の直後に配置してフラグ順序を仕様に一致させる
    4. HR レコード欠損（`torokutosu_i` が pd.NA）→ 'unresolved'
    5. `is_dead_heat`（payout_count > fukusho_payout_places AND fukusho_payout_places > 0）→ 'dead_heat'
    6. `datakubun_harai == '1'`（速報）→ 'inferred'
    7. それ以外 → 'validated'

    **compute_is_model_eligible(row, *, spec) -> tuple[bool, str | None]**（**REVIEWS HIGH #6 契約**）: RESEARCH Code Examples（RESEARCH.md:522-557）+ label_spec.yaml class_eligibility を実装。順序依存（syubetucd 障害/新馬が先に評価され、dead_loss は単独では不適格理由にならない）:
    1. `syubetucd in spec['model_ineligibility_syubetucd']`（['18','19']）→ (False, 'obstacle')
    2. `syubetucd in spec['newcomer_syubetucd']`（['11','12']）→ (False, 'newcomer')
    3. `not is_fukusho_sale_available` → (False, 'no_fukusho_sale')
    4. `label_validation_status == 'unresolved'` → (False, 'unresolved')
    5. `is_race_cancelled or is_scratch_cancel or is_race_excluded` → (False, 'race_or_horse_cancelled')
    6. `class_level_numeric is not None AND class_level_numeric < spec['class_eligibility']['minimum_class_level_numeric']`（1）AND `syubetucd NOT IN spec['class_eligibility']['maiden_syubetucd']`（['13','14','15']）→ (False, 'class_below_minimum')。**未勝利（class_level_numeric=0・syubetucd='13'/'14'/'15'）は §7.2「2歳未勝利以上」により適格**・MEDIUM open question 解決
    7. `label_validation_status in ('validated', 'inferred', 'dead_heat')` → (True, None)
    8. それ以外 → (False, 'status_not_eligible')
    **HIGH #6 コメント**: 「is_dead_loss は適用順序の判定対象に入らない。競走中止馬が他の理由で不適格になる場合（例: 障害レースで競走中止）、ineligibility_reason には 'obstacle' が格納され 'dead_loss' 由来にはならない。Plan 04 `_check_dead_loss_not_excluded` は "dead_loss 由来で除外された" 行のみを passed=False とする。」

    **_create_label_table(write_cur: Cursor, *, reader_role: str) -> None**（**HIGH #1 + HIGH #3 対応**）: `_idempotent_load_label` の初回起動時に IF NOT EXISTS で `label.fukusho_label` を作成。カラムは RESEARCH.md:477-509 の推奨スキーマ + HIGH #1 拡張:
    - PK `(year, jyocd, kaiji, nichiji, racenum, umaban, kettonum)`
    - §10.2 保持項目: race_date / sales_start_entry_count / sales_start_entry_count_source / **sales_start_entry_count_confidence varchar(16) NOT NULL**（HIGH #1 独立列） / final_starter_count / fukusho_payout_places / is_fukusho_sale_available / fukusho_hit_raw / fukusho_hit_validated / label_validation_status
    - D-03: is_model_eligible / ineligibility_reason
    - D-04 状態マーカー: is_scratch_cancel / is_race_excluded / is_dead_loss / is_race_cancelled / is_dead_heat
    - label_generation_version NOT NULL
    - COMMENT ON TABLE label.fukusho_label IS 'Fukusho label table (Phase 2). label_generation_version-managed.'（HIGH #3 コメント保存）
    初回作成直後に `GRANT SELECT ON label.fukusho_label TO {reader_role}` を発行（psycopg.sql.Identifier で安全に置換・HIGH #3: PUBLIC 不使用・明示的 reader ロール）。

    **_idempotent_load_label(write_cur, rows, columns, *, reader_role: str) -> int**（**REVIEWS HIGH #3 対応**）: `normalize.py:356-441` の `_idempotent_load` を `label` スキーマ向けに複製（汎用化せず schema='label' を hard-code）。同ステップ順序:
    1. `SELECT pg_advisory_xact_lock(hashtext('label.fukusho_label'))`（CR-04(b)）
    2. 空入力 RuntimeError（CR-04(a)・rows=[] 拒否）
    3. `_create_label_table(write_cur, reader_role=reader_role)` を呼出（IF NOT EXISTS・既存なら no-op）
    4. `CREATE TABLE IF NOT EXISTS label.fukusho_label_staging (LIKE label.fukusho_label INCLUDING ALL)`（**INCLUDING ALL が HIGH #3 の核心** — PK / インデックス / NOT NULL 制約 / コメントが staging 表に継承され、RENAME 後も失われない）
    5. `TRUNCATE label.fukusho_label_staging`
    6. `executemany INSERT INTO label.fukusho_label_staging (...) VALUES (...)`（rowcount 検証 CR-04(c)）
    7. `DROP TABLE IF EXISTS label.fukusho_label`
    8. `ALTER TABLE label.fukusho_label_staging RENAME TO fukusho_label`
    9. **`GRANT SELECT ON label.fukusho_label TO {reader_role}`**（**HIGH #3: RENAME 後に明示的 reader role GRANT を再発行・PUBLIC は使用しない**・psycopg.sql.Identifier で安全に置換・normalize.py:439 の `GRANT ... TO PUBLIC` とは異なる label 専用ロジック）
    10. `SELECT count(*) FROM label.fukusho_label` で rowcount を返り値として返す（idempotent 実行の checksum 基準）

    **_df_to_tuples_label(df, columns) -> list[tuple]**: `normalize.py:520-585` の `_df_to_tuples` を label カラム型（boolean / smallint / varchar / date / int）向けに適用。boolean カラム（is_model_eligible / is_fukusho_sale_available / is_scratch_cancel 等）は `normalize.py:576-580` の is_grade_race 分岐を参考に `bool(v)` 変換。

    **run_label_etl(read_pool, write_pool, *, settings: Settings | None = None) -> dict[str, Any]**: `normalize.py:624-663` の `run_normalized_etl` パターンを踏襲:
    1. `settings = settings or Settings()` で reader role 名を取得: `reader_role = settings.db_reader_role or 'keiba_readonly'`（settings にフィールドがない場合は Plan 01 で追加済みを前提・無ければ default 'keiba_readonly' で logger.warning で明示）
    2. `read_pool.connection()` → `_select_raw_harai(cur)` + `_select_se_state(cur)` + `_select_race_meta(cur)`
    3. `compute_fukusho_labels(hr_df, se_df, race_df)` で変換
    4. `write_pool.connection()` → `_idempotent_load_label(wcur, rows, columns, reader_role=reader_role)` で staging-swap 書込
    5. `SELECT md5(string_agg(md5(row(r.*)::text), '' ORDER BY year, jyocd, kaiji, nichiji, racenum, umaban)) FROM label.fukusho_label r` で content checksum を取得（idempotent 実行確認用・HIGH #3）
    6. `conn.commit()`
    7. 戻り値: `{"rows_inserted": int, "label_unresolved_count": int, "raw_touched": False, "checksum": str}`（raw_touched は常に False・成功基準#2・checksum は idempotent 実行確認用）

    **__all__**: `["run_label_etl", "compute_fukusho_labels", "classify_status", "compute_is_model_eligible", "_canonicalize_markers", "_select_raw_harai", "_select_se_state", "_select_race_meta", "_idempotent_load_label", "_create_label_table", "load_label_spec"]`

    実装後 `uv run pytest tests/test_fukusho_label.py -x` で25テスト全て GREEN になるまで反復。
  </action>

  <verify>
    <automated>uv run pytest tests/test_fukusho_label.py -x 2>&amp;1 | tail -5 | grep -E "passed|failed" | grep -qE "(2[5-9] passed|[3-9][0-9] passed)"</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tests/test_fukusho_label.py -x` が25テスト以上全て passed で完了（Plan 02 の RED テストが GREEN に）
    - `src/etl/fukusho_label.py` が存在し `python3 -c "from src.etl.fukusho_label import run_label_etl, compute_fukusho_labels, classify_status, compute_is_model_eligible, _canonicalize_markers, _select_raw_harai, _select_se_state, _idempotent_load_label, load_label_spec"` が成功
    - ファイル内に `PROJECT_WINDOW_FILTER` の import（from src.etl.filters）を含む（CR-06 single source of truth・三重定義禁止）
    - ファイル内に `FROM public.n_harai` と `FROM normalized.n_uma_race` と `FROM public.n_uma_race`（timediff 用）を含む
    - **HIGH #4:** ファイル内に `datakubun IN ('7', '9')` または `datakubun IN ('7','9')` のいずれかの表現を含む（`datakubun = '7'` 単独は違反・`grep -E "datakubun[^=]*IN.*'7'.*'9'|datakubun[^=]*IN.*'9'" src/etl/fukusho_label.py` で一致）
    - ファイル内に `timediff`（NOT `timedifn`）を含む（Pitfall 1）
    - ファイル内に `bataijyu`（NOT `bataiju`）を含む
    - ファイル内に `payfukusyounmaban1`..`payfukusyounmaban5` を含む（HR 払戻馬番・LABEL-01 valid 原料）
    - ファイル内に `tokubaraiflag2` と `payout_umaban_set`（または payout_count）の両方を含む（D-04 複勝特払分岐・D-13 silent fallback 禁止・W4 対応）
    - ファイル内に `SyussoTosu` / `syussotosu` は参照するが `fukusho_payout_places` 計算には `torokutosu` のみ使用（Pitfall 3・grep で境界計算箇所を確認）
    - **HIGH #5:** ファイル内に `_canonicalize_markers` 関数定義を含み、関数内に `se_marker_canonicalization` sentinel 集合の参照を含む（`spec['se_marker_canonicalization']` または `spec.get('se_marker_canonicalization')`）。`== '999.0'` や `== '9999'` のような単一値等価比較は _canonicalize_markers 内に出現しない（`grep -cE "(harontimel3|timediff|bataijyu|time) *== *'" src/etl/fukusho_label.py` が 0 または docstring/comment 行のみ）
    - **NEW HIGH #3:** `python3 -c "import inspect; from src.etl.fukusho_label import _canonicalize_markers; src = inspect.getsource(_canonicalize_markers); assert 'pd.isna' in src, 'NEW HIGH #3: _canonicalize_markers must guard pd.isna before str()'; assert 'missing_value_sentinel' in src or '__MISSING__' in src, 'NEW HIGH #3: missing sentinel mapping missing'; assert 'time_present' in src"` が成功（pd.isna guard + missing sentinel + time_present を含む）
    - **NEW HIGH #2:** `python3 -c "import inspect; from src.etl.fukusho_label import _select_se_state; src = inspect.getsource(_select_se_state); import re; assert re.search(r"datakubun[^=]*IN[^)]*'7'[^)]*'9'", src) or re.search(r"datakubun[^=]*IN[^)]*'7'[^)]*,[^)]*'9'", src), 'NEW HIGH #2: timediff source must filter datakubun IN (\'7\',\'9\')'; assert 'timediff' in src; assert 'merge' in src or '.merge' in src or 'join' in src, 'NEW HIGH #2: merge logic missing"` が成功（timediff SELECT に datakubun IN ('7','9') フィルタ + merge を含む）
    - **HIGH #1:** ファイル内に `sales_start_entry_count_confidence` カラム名を含む（CREATE TABLE または INSERT 列リスト）。`sales_start_entry_count_source` と独立して定義される。
    - **HIGH #3:** ファイル内に `INCLUDING ALL` を含む（staging 表が PK/インデックスを継承）
    - **HIGH #3:** ファイル内に `GRANT SELECT ON label.fukusho_label TO` を含み、`TO PUBLIC` を含まない（`grep -c "TO PUBLIC" src/etl/fukusho_label.py` が 0）
    - **HIGH #3:** ファイル内に `reader_role` パラメータを含む（`_idempotent_load_label` と `_create_label_table` の両方のシグネチャ）
    - ファイル内に `_staging` / `RENAME TO` / `DROP TABLE IF EXISTS label` を含む（HIGH #3 staging-swap・label 版）
    - ファイル内に `pg_advisory_xact_lock(hashtext('label.fukusho_label'))` を含む（CR-04(b)）
    - ファイル内に `UPDATE public.n_` / `DELETE FROM public.n_` を含まない（D-06 raw 不変性・成功基準#2）
    - `import duckdb` を含まない（§12.1・Phase 2 では補助すら不要）
    - run_label_etl の戻り値が `{"rows_inserted": ..., "label_unresolved_count": ..., "raw_touched": False, "checksum": ...}` の構造（normalize.py:659-663 と対称 + checksum）
  </acceptance_criteria>

  <done>src/etl/fukusho_label.py が実装され Plan 02 の25テストが全て GREEN。compute_fukusho_labels / classify_status / compute_is_model_eligible / _canonicalize_markers / run_label_etl が定義され、HR/SE の raw 直読みと label スキーマへの staging-swap idempotent 替換（reader role 明示 GRANT・PUBLIC 不使用・INCLUDING ALL・datakubun IN ('7','9')）を実装。REVIEWS HIGH #1（sales_start_entry_count_confidence 独立列）/ #3（PUBLIC 禁止・reader role 明示・INCLUDING ALL）/ #4（datakubun IN ('7','9')）/ #5（_canonicalize_markers sentinel 集合）解決。</done>
</task>

<task type="auto">
  <name>Task 2: scripts/run_label_etl.py エントリポイント作成 + raw 不変性テスト拡張 + idempotent 実行検証</name>
  <files>scripts/run_label_etl.py, tests/test_raw_immutability.py</files>

  <behavior>
    - scripts/run_label_etl.py: KEIBA_SKIP_DB_TESTS に関わらず CLI 実行で ETL を起動・settings 読込・readonly/etl pool 構築・run_label_etl 呼出・raw fingerprint 前後比較で masked DSN ログ出力
    - test_raw_immutability.py 拡張: label ETL 追加後も raw(public.n_*) が不変であることを test_raw_unchanged_after_label_etl で直接証明
    - HIGH #3 idempotent 検証: ETL を2回連続実行しても rows_inserted と checksum が一致することを test_label_etl_idempotent で assert
  </behavior>

  <read_first>
    - scripts/run_normalized_etl.py（全構造・sys.path セットアップ・load_dotenv・Settings 読込・make_pool(role='readonly'/'etl')・compute_raw_fingerprint 前後比較・masked DSN ログ・main() / argparse）
    - tests/test_raw_immutability.py:46-67（test_raw_unchanged_after_etl の現状・run_normalized_etl 呼出パターン）
    - tests/conftest.py:22-78（settings / pg_pool / readonly_cur / write_cur fixture）
    - src/etl/raw_fingerprint.py:36-105（compute_raw_fingerprint / assert_raw_unchanged）
    - src/etl/fukusho_label.py（Task 1 で作成・run_label_etl を呼出・戻り値 checksum フィールド）
    - .planning/phases/02-fukusho-labels/02-REVIEWS.md:41-42, 83（HIGH #3 staging-swap idempotency・preserve indexes/grants/comments・2回連続実行で同一 row count + checksum）
  </read_first>

  <action>
    **scripts/run_label_etl.py 新規作成**: `scripts/run_normalized_etl.py` の構造を直接写す:
    - `from __future__ import annotations` / argparse / logging / sys / pathlib.Path / `from dotenv import load_dotenv` / psycopg 必要なし（pool 経由）
    - `_REPO_ROOT = Path(__file__).resolve().parent.parent` / `sys.path.insert(0, str(_REPO_ROOT / "src"))`
    - `load_dotenv(_REPO_ROOT / ".env")`
    - main(): Settings() 読込・`read_pool = make_pool(settings, role='readonly')` / `write_pool = make_pool(settings, role='etl')`・`from src.etl.fukusho_label import run_label_etl`・`from src.etl.raw_fingerprint import compute_raw_fingerprint, assert_raw_unchanged`
    - raw fingerprint before 取得（readonly pool connection）・connection rollback・`result1 = run_label_etl(read_pool, write_pool, settings=settings)` 実行・`result2 = run_label_etl(read_pool, write_pool, settings=settings)` を**2回目実行**（HIGH #3 idempotent 検証）・`assert result1["rows_inserted"] == result2["rows_inserted"], f"idempotent violation: {result1['rows_inserted']} != {result2['rows_inserted']}"` で行数一致・`assert result1["checksum"] == result2["checksum"], "idempotent checksum violation"` で内容一致・after fingerprint 取得・`assert_raw_unchanged(before, after)` で raw 不変性をスクリプト実行時にも検証
    - 結果 dict の rows_inserted / label_unresolved_count / raw_touched / checksum を INFO ログ出力・masked DSN（`settings.dsn_masked` / `settings.etl_dsn_masked`）を使用
    - exit code 0 で完了・例外時は 1

    **tests/test_raw_immutability.py 拡張**: 既存の `test_raw_unchanged_after_etl`（行46-67・normalized ETL 版）の後に新しいテストを2つ追加:

    1. `test_raw_unchanged_after_label_etl`（raw 不変性・label ETL 版）:
       - `@pytest.mark.requires_db`
       - `before = compute_raw_fingerprint(readonly_cur)` / `readonly_cur.connection.rollback()`
       - `from src.etl.fukusho_label import run_label_etl` / `run_label_etl(pg_pool, write_pool, settings=settings)`
       - `after = compute_raw_fingerprint(readonly_cur)` / `readonly_cur.connection.rollback()`
       - `assert_raw_unchanged(before, after)`
       - docstring に「label ETL 追加後の raw 不変性拡張（SC#2・D-06・Plan 02-03・HIGH #3 reader role 明示 GRANT 後も raw 不変）」と明記

    2. `test_label_etl_idempotent`（**REVIEWS HIGH #3 対応**）:
       - `@pytest.mark.requires_db`
       - `result1 = run_label_etl(pg_pool, write_pool, settings=settings)` / `result2 = run_label_etl(pg_pool, write_pool, settings=settings)`
       - `assert result1["rows_inserted"] == result2["rows_inserted"]`（行数 idempotent）
       - `assert result1["checksum"] == result2["checksum"]`（内容 checksum idempotent・staging-swap が PK/インデックス/GRANT を保存して2回目も同一結果）
       - `assert result2["raw_touched"] is False`（raw 不変・2回実行で raw に触れない）
       - docstring に「staging-swap idempotency・INCLUDING ALL で PK/インデックス継承・reader role 明示 GRANT 再発行・PUBLIC 不使用（REVIEWS HIGH #3）」と明記

    `test_etl_role_cannot_write_public`（test_raw_immutability.py:84-109）は既存のまま・label ETL が ETL ロールで `label.fukusho_label` に INSERT できること（Plan 01 GRANT 拡張の実効性）は `test_raw_unchanged_after_label_etl` の中で `run_label_etl` が例外なく完了すること自体が GRANT 反映の証明になる。
  </action>

  <verify>
    <automated>test -f scripts/run_label_etl.py &amp;&amp; grep -q "from src.etl.fukusho_label import run_label_etl" scripts/run_label_etl.py &amp;&amp; grep -q "test_raw_unchanged_after_label_etl" tests/test_raw_immutability.py &amp;&amp; grep -q "test_label_etl_idempotent" tests/test_raw_immutability.py &amp;&amp; uv run pytest tests/test_raw_immutability.py --collect-only 2>&amp;1 | grep -q "test_raw_unchanged_after_label_etl" &amp;&amp; uv run pytest tests/test_raw_immutability.py --collect-only 2>&amp;1 | grep -q "test_label_etl_idempotent"</automated>
  </verify>

  <acceptance_criteria>
    - `scripts/run_label_etl.py` が存在し `from src.etl.fukusho_label import run_label_etl` を含む
    - `scripts/run_label_etl.py` に `compute_raw_fingerprint` と `assert_raw_unchanged` の import を含む（raw 不変性の実行時検証）
    - `scripts/run_label_etl.py` に `make_pool(settings, role='readonly')` と `make_pool(settings, role='etl')` の両方を含む（HIGH #6 2ロール pool）
    - `scripts/run_label_etl.py` に `dsn_masked` を含む（認証情報マスク・T-01-01・MEDIUM #1）
    - `scripts/run_label_etl.py` に `INSERT INTO public.n_` / `UPDATE public.n_` / `DELETE FROM public.n_` を含まない（raw 書込禁止・D-06）
    - **HIGH #3:** `scripts/run_label_etl.py` に2回連続 `run_label_etl` 呼出と `result1["checksum"] == result2["checksum"]` assertion を含む（idempotent 検証）
    - `tests/test_raw_immutability.py` に `test_raw_unchanged_after_label_etl` 関数が追加される
    - **HIGH #3:** `tests/test_raw_immutability.py` に `test_label_etl_idempotent` 関数が追加される
    - `uv run pytest tests/test_raw_immutability.py --collect-only` が両方の新しいテスト名を収集する
    - 両方の新しいテストに `@pytest.mark.requires_db` マークが付く（実DB integration test）
  </acceptance_criteria>

  <done>scripts/run_label_etl.py が作成され raw fingerprint 前後比較機能と2回実行 idempotent 検証を持つ。test_raw_immutability.py に label ETL 版の raw 不変性テストと idempotent 検証テストが追加される。REVIEWS HIGH #3 解決。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 実DB で label ETL を実行しラベル生成と raw 不変性と idempotent 実行を確認</name>
  <files>scripts/run_label_etl.py</files>

  <what-built>
    src/etl/fukusho_label.py が実装され、Plan 02 の25テストが GREEN、scripts/run_label_etl.py が作成され raw fingerprint 前後比較 + 2回連続実行 idempotent 検証を備える。ただし unit test は合成 DataFrame を使用し、実DB の 39,580 レース / 553,891 馬行に対する実行は未検証。
  </what-built>

  <how-to-verify>
    1. 実DB（PostgreSQL everydb2）が起動し `.env` に KEIBA_DB_USER / KEIBA_ETL_DB_USER / KEIBA_ADMIN_DSN が設定されていることを確認
    2. Plan 01 Task 3 の `uv run python scripts/run_apply_schema.py` が既に成功していることを確認（label スキーマ GRANT 拡張済み・HIGH #3 reader role 反映済み）
    3. `uv run python scripts/run_label_etl.py` を実行:
       - 標準出力に masked DSN で `postgresql://keiba_readonly:***@...` / `postgresql://keiba_etl:***@...` が INFO ログで出る
       - `compute_raw_fingerprint before` が取得される
       - `run_label_etl` 1回目実行（数分要する場合あり・55万行処理）
       - `run_label_etl` 2回目実行（idempotent 検証・HIGH #3）
       - `rows_inserted: 553891`（±数件・実測値と整合）・`label_unresolved_count: 376`（レース全体中止376行に近い・datakubun=9 の SE 行数）・`raw_touched: False`・`checksum: <md5 hash>` が INFO ログに出る
       - 2回実行で rows_inserted と checksum が完全一致すること（HIGH #3 idempotent・ INCLUDING ALL で PK/インデックス継承済み・reader role 明示 GRANT 再発行済み）
       - `compute_raw_fingerprint after` が取得される
       - `assert_raw_unchanged` が例外なく完了（raw 不変性証明）
       - exit code 0
    4. label.fukusho_label テーブルの内容を psql または readonly_cur で spot check:
       `SELECT count(*) FROM label.fukusho_label;` は 553,891 ± 数件
       `SELECT count(*) FROM label.fukusho_label WHERE label_validation_status IS NULL;` は 0（全行 status 付与）
       `SELECT count(*) FROM label.fukusho_label WHERE is_model_eligible IS NULL;` は 0
       `SELECT count(*) FROM label.fukusho_label WHERE sales_start_entry_count_confidence IS NULL;` は 0（HIGH #1 全行 populated）
       `SELECT sales_start_entry_count_confidence, count(*) FROM label.fukusho_label GROUP BY 1;` は inferred のみ（HIGH #1・label_validation_status とは別列）
       `SELECT label_validation_status, count(*) FROM label.fukusho_label GROUP BY 1;` は validated ≈ 550,000+ / dead_heat 約 97レース分の馬行 / unresolved 約 376+ / inferred 0件（本DBでは速報 DataKubun=1 なし）
       `SELECT count(*) FROM label.fukusho_label WHERE is_model_eligible = true;` は通常レースの大半
       `SELECT count(*) FROM label.fukusho_label WHERE is_dead_loss = true;` は約 3,554（競走中止・学習残）
       `SELECT count(*) FROM label.fukusho_label WHERE is_scratch_cancel = true;` は約 956（取消・予測対象外）
       `SELECT count(*) FROM label.fukusho_label WHERE is_race_cancelled = true;` は約 376（HIGH #4・datakubun='9' 行が落とされず含まれる）
       **HIGH #6 確認**: `SELECT count(*) FROM label.fukusho_label WHERE is_dead_loss = true AND is_model_eligible = false;` の結果のうち、`ineligibility_reason != 'obstacle' AND ineligibility_reason != 'newcomer' AND ineligibility_reason != 'no_fukusho_sale' AND ineligibility_reason != 'unresolved' AND ineligibility_reason != 'race_or_horse_cancelled' AND ineligibility_reason != 'class_below_minimum'` な行が0件であること（dead_loss 単独で除外された行は存在しない・Plan 04 がこの不変条件を検査する）
       **HIGH #3 GRANT 確認**: `SELECT has_table_privilege('keiba_readonly', 'label.fukusho_label', 'SELECT')` が true を返す（reader role 明示 GRANT 反映）
    5. raw 不変性: `uv run pytest tests/test_raw_immutability.py::test_raw_unchanged_after_label_etl -x` が passed
    6. idempotent: `uv run pytest tests/test_raw_immutability.py::test_label_etl_idempotent -x` が passed（HIGH #3）
    7. Plan 02 の unit test も再確認: `uv run pytest tests/test_fukusho_label.py -x` が25+ passed
  </how-to-verify>

  <resume-signal>Type "approved" またはラベル件数・status 分布・HIGH #3 idempotent checksum（2回一致）/ HIGH #6 dead_loss+ineligible の実測値を貼付。件数が大幅にずれる場合は issues として記載。</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| readonly pool → raw/normalized | 読取境界。public.n_harai / public.n_uma_race.timediff / normalized.n_uma_race / normalized.n_race から SELECT のみ |
| etl pool → label.fukusho_label | 書込境界・staging-swap で atomic 替換。public/raw_everydb2 には書込まない。RENAME 後に reader role 明示 GRANT（HIGH #3） |
| pandas transform 内部 | 計算境界・合成 DataFrame と実DB DataFrame の両方で deterministic に振る舞う必要 |
| checkpoint:human-verify（Task 3） | 実DB実行境界・`human_verify_mode=end-of-phase` から逸脱（W5・理由: 実DB against の ETL 実行は agent 自律完了不可・CI で実DB接続利用不能・VALIDATION.md に理由明記） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Tampering | raw(public.n_*) への誤書込 | mitigate | readonly pool で SELECT のみ・write_pool は label スキーマのみに GRANT（Plan 01・D-06）。_select_raw_harai / _select_se_state は readonly_cur を使用。test_raw_unchanged_after_label_etl で raw 不変性を直接証明 |
| T-02-09 | Tampering | Pitfall 3: SyussoTosu で payout_places を決定 | mitigate | compute_fukusho_labels 内で `fukusho_payout_places` 計算は `torokutosu` のみ使用。`syussotosu` は `final_starter_count`（参考）に格納するのみ。test_payout_places_uses_torokutosu_not_syussotosu で検証 |
| T-02-10 | Tampering | Pitfall 1: timedifn / bataiju カラム名誤用 | mitigate | _select_se_state で `timediff`（実カラム）を SELECT。`bataijyu`（実カラム）を使用。実行時 psycopg.errors.UndefinedColumn を検知した場合は即座に fail（D-13） |
| T-02-11 | Tampering | HR PayFukusyoUmaban1..5 の '00'/'空' を 0 扱いして誤正例化 | mitigate | A3: payfukusyounmaban1..5 の '00' と '' を pd.NA に変換してから payout_umaban_set を構築。test_drift_is_dead_heat_only で drift 行が dead_heat に分類されることを検証 |
| T-02-12 | Information Disclosure | label ETL ログからの DSN/password 漏洩 | mitigate | scripts/run_label_etl.py は settings.dsn_masked / etl_dsn_masked のみを INFO ログ出力。生 DSN は psycopg.connect() のみに渡す（settings.py:48-68 と同じ・T-01-01） |
| T-02-13 | Tampering | label_spec.yaml の未知 status / syubetucd / tokubaraiflag2 での silent fallback | mitigate | load_label_spec / compute_is_model_eligible で未知値は unresolved / is_model_eligible=False / ineligibility_reason='status_not_eligible' で明示隔離。classify_status は tokubaraiflag2=='1' を payout_set 非空で validated / 空で unresolved に明示分岐（D-04・D-13・W4） |
| T-02-14 | Denial of Service | ETL が空 rows を staging に swap して silent data loss | mitigate | _idempotent_load_label で rows=[] を RuntimeError で拒否（CR-04(a)・normalize.py:398-402 と同じ）。executemany rowcount 検証（CR-04(c)） |
| T-02-RV1 | Tampering | label_validation_status と sales_start_entry_count_confidence の conflation | mitigate | **REVIEWS HIGH #1:** `_create_label_table` が `sales_start_entry_count_confidence` を独立 varchar(16) NOT NULL カラムとして作成。`sales_start_entry_count_source` とも独立・label_validation_status とも独立。Plan 02 test_sales_start_entry_count_proxy_and_source_confidence_separated_from_status が両列の存在と値を assert |
| T-02-RV3 | Elevation of Privilege | staging-swap DROP+RENAME が indexes/grants/comments を失う / GRANT TO PUBLIC で過剰権限付与 | mitigate | **REVIEWS HIGH #3:** (1) `CREATE TABLE ... (LIKE label.fukusho_label INCLUDING ALL)` で PK/インデックス/NOT NULL/コメントが staging 表に継承される。(2) RENAME 後に `GRANT SELECT ON label.fukusho_label TO {reader_role}` で明示的 reader ロール付与（`TO PUBLIC` は一切使用しない）。(3) 2回連続実行で rows_inserted と checksum が一致することを test_label_etl_idempotent と scripts/run_label_etl.py で検証 |
| T-02-RV4 | Tampering | _select_se_state が datakubun='9' (race_cancelled) の SE 行を落とす silent data loss | mitigate | **REVIEWS HIGH #4:** _select_se_state の SQL は `WHERE {PROJECT_WINDOW_FILTER} AND datakubun IN ('7', '9')` を含む。Plan 02 test_select_se_state_includes_datakubun_9 が inspect.getsource で regression assert。Task 3 実DB確認で `count(*) WHERE is_race_cancelled=true` が 376 ± 数件になることを検証 |
| T-02-RV5 | Tampering | marker 列の raw '999.0' 文字列と numeric 999.0 で判定が変わる brittle 等価 | mitigate | **REVIEWS HIGH #5:** `_canonicalize_markers` 関数が label_spec.yaml の `se_marker_canonicalization` sentinel 集合のみを使用し、str() キャストで raw varchar と numeric cast を統一してから `in` 判定する。単一文字列等価比較は禁止。Plan 02 test_canonicalize_markers_raw_string_form / test_canonicalize_markers_numeric_cast_form が両表現で同一結果を assert |
| T-02-RV8 | Tampering | _select_se_state の timediff merge が one-to-many で SE 行を増殖させ marker/classification を破壊 | mitigate | **REVIEWS NEW HIGH #2:** public.n_uma_race.timediff SELECT を datakubun IN ('7','9') でフィルタし、merge キーに datakubun を含めて両側 1:1 を保証。merge 前後で len(se_df) == len(merged_df) をコード内 assert・不一致時は RuntimeError（D-13）。Plan 02 test_select_se_state_no_row_multiplication_on_timediff_merge が行数不変を regression assert |
| T-02-RV9 | Tampering | _canonicalize_markers が missing/null time を str() で 'nan'/'<NA>'/'None' に変換し sentinel 集合外と誤判定→ time_present=True → 誤 is_dead_loss=True → fukusho_hit=0 で学習汚染 | mitigate | **REVIEWS NEW HIGH #3:** `pd.isna(v)` guard で missing を先に捕捉し `missing_value_sentinel='__MISSING__'` にマップ→ time_present=False（dead_loss にならない）・label_spec.yaml に missing_value_sentinel を Plan 01 で追加。Plan 02 test_canonicalize_markers_missing_time（None/NaN/pd.NA 3 variant）が regression assert |
| T-02-SC | Tampering | パッケージインストール | accept | 新規外部パッケージインストール無し |
</threat_model>

<verification>
- `uv run pytest tests/test_fukusho_label.py -x` が25+ passed（Plan 02 GREEN）
- `uv run pytest tests/test_raw_immutability.py -x` が全 passed（label ETL 追加後も raw 不変・SC#2 + HIGH #3 idempotent）
- `uv run python scripts/run_label_etl.py` が exit 0・rows_inserted が 553,891 ± 数件・label_unresolved_count が 376 ± 数件（実測のレース全体中止376 SE行に由来）・2回実行で checksum 一致（HIGH #3）
- `SELECT count(*) FROM label.fukusho_label WHERE label_validation_status IS NULL` が 0
- `SELECT count(*) FROM label.fukusho_label WHERE is_model_eligible IS NULL` が 0
- **HIGH #1:** `SELECT count(*) FROM label.fukusho_label WHERE sales_start_entry_count_confidence IS NULL` が 0
- **HIGH #4:** `SELECT count(*) FROM label.fukusho_label WHERE is_race_cancelled = true` が 376 ± 数件（datakubun='9' が落とされず含まれる）
- `ruff check src/etl/fukusho_label.py scripts/run_label_etl.py tests/test_raw_immutability.py` が clean
</verification>

<success_criteria>
- SC#1: `label.fukusho_label` 行が `fukusho_hit_raw` と `fukusho_hit_validated` の両方を持ち、全行に `label_validation_status` が付与される（NULL 0件）
- SC#3: `sales_start_entry_count` が全行に格納され（TorokuTosu 代理値・source='torokutosu_proxy'・confidence='inferred'）、`unresolved` 行は学習除外（is_model_eligible=False）
- SC#4: 同着（dead_heat）・取消（is_scratch_cancel）・競走中止（is_dead_loss）・レース全体中止（is_race_cancelled）が観測事実ベースで分類され、それぞれの label / is_model_eligible が §10.6 に合致
- raw 不変性: ETL 前後で public.n_* の row-hash + row-count が完全一致
- idempotent: run_label_etl を2回連続実行しても件数・ハッシュが同一（HIGH #3）
- **REVIEWS 解決:** HIGH #1（sales_start_entry_count_confidence 独立列）/ #3（INCLUDING ALL・reader role 明示 GRANT・PUBLIC 禁止・idempotent checksum）/ #4（datakubun IN ('7','9')）/ #5（_canonicalize_markers sentinel 集合）/ **NEW HIGH #2（timediff merge 1:1・datakubun IN ('7','9') 両側・row-multiplication 防止 assertion）** / **NEW HIGH #3（pd.isna guard・missing '__MISSING__' sentinel・time_present=False で dead_loss 汚染防止）** が実装・テスト・実DB確認で解決
</success_criteria>

<output>
Create `.planning/phases/02-fukusho-labels/02-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

### 新規シンボル・関数・テーブル（本 plan が作成）

**関数（src/etl/fukusho_label.py）:**
- `load_label_spec(path: str | Path = _DEFAULT_CONFIG_PATH) -> dict[str, Any]`
- `compute_fukusho_labels(hr_df, se_df, race_df, *, spec: dict | None = None) -> pd.DataFrame`
- `classify_status(row: pd.Series, *, spec: dict) -> str`
- `compute_is_model_eligible(row: pd.Series, *, spec: dict) -> tuple[bool, str | None]`（HIGH #6: syubetucd が先に評価され dead_loss は単独では不適格理由にならない）
- `_canonicalize_markers(df: pd.DataFrame, *, spec: dict) -> pd.DataFrame`（**HIGH #5**: sentinel 集合ベース判定・**NEW HIGH #3**: pd.isna guard + '__MISSING__' sentinel・time_present=False for missing）
- `_select_raw_harai(read_cur: Cursor) -> pd.DataFrame`
- `_select_se_state(read_cur: Cursor) -> pd.DataFrame`（**HIGH #4**: `datakubun IN ('7', '9')`・**NEW HIGH #2**: 両側 datakubun IN ('7','9') + merge キーに datakubun 含む 1:1 merge・row-multiplication 防止 assert）
- `_select_race_meta(read_cur: Cursor) -> pd.DataFrame`
- `_create_label_table(write_cur: Cursor, *, reader_role: str) -> None`（**HIGH #1**: sales_start_entry_count_confidence カラム含む / **HIGH #3**: INCLUDING ALL・明示 reader GRANT）
- `_idempotent_load_label(write_cur, rows, columns, *, reader_role: str) -> int`（**HIGH #3**: PUBLIC 禁止・reader role 明示 GRANT・INCLUDING ALL・checksum 返却）
- `_df_to_tuples_label(df, columns) -> list[tuple]`
- `run_label_etl(read_pool, write_pool, *, settings: Settings | None = None) -> dict[str, Any]`

**モジュール定数（src/etl/fukusho_label.py）:**
- `_HARAI_SELECT_COLUMNS`（n_harai SELECT 列リスト）
- `_SE_SELECT_COLUMNS`（normalized.n_uma_race SELECT 列リスト）
- `_SE_TIMEDIFF_SELECT_COLUMNS`（public.n_uma_race.timediff 用）
- `_RACE_META_SELECT_COLUMNS`（normalized.n_race 適格性判定用）
- `_LABEL_TABLE_COLUMNS`（label.fukusho_label INSERT 列リスト・sales_start_entry_count_confidence 含む）
- `_DEFAULT_CONFIG_PATH = Path("src/config/label_spec.yaml")`

**SQL 文（src/etl/fukusho_label.py 内）:**
- `SELECT ... FROM public.n_harai WHERE {PROJECT_WINDOW_FILTER}`
- `SELECT ... FROM normalized.n_uma_race WHERE {PROJECT_WINDOW_FILTER} AND datakubun IN ('7', '9')`（**HIGH #4**）
- `SELECT ... FROM public.n_uma_race WHERE {PROJECT_WINDOW_FILTER} AND datakubun IN ('7', '9')`（timediff 用・**NEW HIGH #2**: datakubun フィルタ追加・両側 1:1 merge 用）
- `SELECT ... FROM normalized.n_race WHERE {PROJECT_WINDOW_FILTER}`
- `CREATE TABLE IF NOT EXISTS label.fukusho_label (...)`（**HIGH #1**: sales_start_entry_count_confidence 列含む）
- `COMMENT ON TABLE label.fukusho_label IS '...'`（HIGH #3 コメント保存）
- `CREATE TABLE IF NOT EXISTS label.fukusho_label_staging (LIKE label.fukusho_label INCLUDING ALL)`（**HIGH #3**: INCLUDING ALL）
- `SELECT pg_advisory_xact_lock(hashtext('label.fukusho_label'))`
- `TRUNCATE label.fukusho_label_staging`
- `INSERT INTO label.fukusho_label_staging (...) VALUES (...)`
- `DROP TABLE IF EXISTS label.fukusho_label`
- `ALTER TABLE label.fukusho_label_staging RENAME TO fukusho_label`
- `GRANT SELECT ON label.fukusho_label TO {reader_role}`（**HIGH #3**: reader ロール明示・`TO PUBLIC` 禁止）
- `SELECT md5(string_agg(md5(row(r.*)::text), '' ORDER BY ...)) FROM label.fukusho_label r`（checksum・idempotent 検証）

**新規テーブル・カラム:**
- `label.fukusho_label`（PK: year, jyocd, kaiji, nichiji, racenum, umaban, kettonum）
  - カラム: race_date / sales_start_entry_count / sales_start_entry_count_source / **sales_start_entry_count_confidence varchar(16) NOT NULL**（**HIGH #1**・独立列） / final_starter_count / fukusho_payout_places / is_fukusho_sale_available / fukusho_hit_raw / fukusho_hit_validated / label_validation_status / is_model_eligible / ineligibility_reason / is_scratch_cancel / is_race_excluded / is_dead_loss / is_race_cancelled / is_dead_heat / label_generation_version

**新規テスト（tests/test_raw_immutability.py 拡張）:**
- `test_raw_unchanged_after_label_etl`
- `test_label_etl_idempotent`（**HIGH #3**）

**新規スクリプト:**
- `scripts/run_label_etl.py`（CLI エントリポイント・2回連続実行で idempotent checksum 検証）

**run_label_etl 戻り値 dict 構造:**
- `{rows_inserted: int, label_unresolved_count: int, raw_touched: False, checksum: str}`
</content>
</invoke>
