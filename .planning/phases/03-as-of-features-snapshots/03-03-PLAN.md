---
phase: 03-as-of-features-snapshots
plan: 03
type: tdd
wave: 2
depends_on: ['03-01']
files_modified:
  - src/features/rolling.py
  - src/features/running_style.py
  - src/features/builder.py
autonomous: true
requirements: [FEAT-01, FEAT-02]
tags: [phase-03, features, tdd, green, builder, rolling, running-style, pit-join-consumer]

must_haves:
  truths:
    - "src/features/builder.py::build_feature_matrix が readonly pool から SELECT のみで PIT-correct feature matrix を構築する（raw_touched=False・D-06）"
    - "各 feature 行が as_of_datetime / feature_cutoff_datetime / feature_snapshot_id / feature_availability_version を持つ（SC#1・§13.2 必須5項目）"
    - "feature_cutoff_datetime == race_date - 1 day が厳格適用され、同日別レースの成績が rolling feature に混入しない（D-06・Pitfall 3.1）"
    - "過去走 rolling 9系統が mean/latest/sd の3軸で算出され、5走未満の馬は __MISSING__ sentinel で明示される（D-03/D-04/D-13）"
    - "推定脚質が過去走 jyuni3c/jyuni4c のみから導出され、当日 kyakusitukubun を使用しない（D-05・Pitfall 3.2）"
    - "_HISTORY_SELECT_COLUMNS が明示的で SELECT * を使わず、禁止カラム（kyakusitukubun/当日 bataijyu/ninki/odds/sibababacd/dirtbabacd/tenkocd/harontimel4）を含まない（SC#2・Pitfall 1/3.6）"
    - "pit_join_backward 呼出前に builder/rolling が .sort_values() でソート済み frame を渡す（pit_join.py の契約・HIGH #1/#3）"
    - "feature matrix の行 key が (race_nkey, kettonum) で一意（umaban は key にしない・Pitfall・29件重複回避）"
    - "feature 値は train/val/test 境界に依存しない（同一 feature 値・全期間1枚 D-09 前提）"
  artifacts:
    - path: "src/features/rolling.py"
      provides: "build_rolling_features(observations, history, lookback=5) 関数・9系統 × 3軸 rolling feature 構築・pit_join_backward 消費・__MISSING__ sentinel"
      contains: "def build_rolling_features"
      min_lines: 180
    - path: "src/features/running_style.py"
      provides: "estimate_running_style(history_5starts) 関数・過去走 jyuni3c/jyuni4c 閾値ルール分類（逃/先/差/追）"
      contains: "def estimate_running_style"
      min_lines: 60
    - path: "src/features/builder.py"
      provides: "build_feature_matrix(read_pool, *, snapshot_id, label_version, fa_version, train_period, validation_period) 関数・observations/static/rolling/running_style 統合・§13.2 metadata stamp"
      contains: "def build_feature_matrix"
      min_lines: 280
  key_links:
    - from: "src/features/builder.py"
      to: "src/features/rolling.py"
      via: "build_rolling_features(observations, history) の呼出"
      pattern: "from src\\.features\\.rolling import"
    - from: "src/features/builder.py"
      to: "src/features/running_style.py"
      via: "estimate_running_style の適用"
      pattern: "from src\\.features\\.running_style import"
    - from: "src/features/rolling.py"
      to: "src/utils/pit_join.py"
      via: "pit_join_backward（sort 済み frame を渡す・HIGH #1/#3 契約）"
      pattern: "from src\\.utils\\.pit_join import pit_join_backward"
    - from: "src/features/builder.py"
      to: "src/features/availability.py"
      via: "load_feature_availability / assert_all_entries_allowed（Plan 03-01 作成）"
      pattern: "from src\\.features\\.availability import"
    - from: "src/features/builder.py"
      to: "normalized.n_uma_race / normalized.n_race / public.n_uma (PostgreSQL)"
      via: "readonly SELECT（明示カラム・SELECT * 禁止・PROJECT_WINDOW_FILTER）"
      pattern: "FROM normalized\\.n_uma_race|FROM normalized\\.n_race|FROM public\\.n_uma"
---

<objective>
Plan 03-01 で RED で立てた unit test 集群（test_pit_cutoff.py / test_rolling.py / test_running_style.py / test_builder.py）を GREEN にする。feature matrix 構築の中核3モジュールを実装:

1. **`src/features/rolling.py`** — 過去走9系統 rolling feature（kakuteijyuni / timediff / harontimel3 / jyuni3c_jyuni4c / kyori / babacd / jyocd / days_since_prev）を mean/latest/sd の3軸で構築。`pit_join_backward`（`merge_asof(direction='backward')`）を消費し、cutoff 以前の最新5走のみ集約。5走未満は `__MISSING__` sentinel（D-13）。
2. **`src/features/running_style.py`** — 推定脚質（逃/先/差/追）を過去走 `jyuni3c`/`jyuni4c` の平均位置から閾値ルールで算出。当日 `kyakusitukubun` は絶対に使わない（D-05・post_race_only）。
3. **`src/features/builder.py`** — `build_feature_matrix(read_pool, *, snapshot_id, label_version, fa_version, train_period, validation_period)` 公開 API。observations frame 構築（cutoff 計算 D-06）→ 静的属性 SELECT → rolling features → 推定脚質 → §13.2 metadata stamp。readonly ロールのみ・明示カラム SELECT・禁止カラム構造的排除。

Purpose: SC#1（PIT correctness）と SC#2（banned feature 排除）と SC#4 前提（horse_id カテゴリ化可能状態）を実装で保証する。Phase 4 モデルが stamped Parquet のみから学習できる PIT-correct matrix を届ける。Plan 03-01 の RED テストが GREEN 化をもって契約履行を証明する。

Output:
- `src/features/rolling.py`（9系統 × 3軸・pit_join_backward 消費・__MISSING__ sentinel）
- `src/features/running_style.py`（過去走 jyuni3c/jyuni4c 閾値ルール）
- `src/features/builder.py`（公開 API build_feature_matrix・readonly SELECT・§13.2 metadata stamp）
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@$HOME/.claude/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-as-of-features-snapshots/03-CONTEXT.md
@.planning/phases/03-as-of-features-snapshots/03-RESEARCH.md
@.planning/phases/03-as-of-features-snapshots/03-PATTERNS.md
@.planning/phases/03-as-of-features-snapshots/03-VALIDATION.md
@.planning/phases/03-as-of-features-snapshots/03-01-PLAN.md
@.planning/phases/02-fukusho-labels/02-03-PLAN.md
@src/utils/pit_join.py
@src/utils/category_map.py
@src/etl/normalize.py
@src/etl/fukusho_label.py
@src/etl/filters.py
@src/db/connection.py
@src/config/settings.py
@src/features/availability.py
@tests/features/conftest.py
@tests/features/test_pit_cutoff.py
@tests/features/test_rolling.py
@tests/features/test_running_style.py
@tests/features/test_builder.py
</context>

<tasks>

<task type="tdd" tdd_phase="green">
  <name>Task 1: src/features/rolling.py（過去走9系統 × 3軸 rolling・pit_join_backward 消費・__MISSING__ sentinel）</name>
  <files>src/features/rolling.py</files>

  <read_first>
    - src/utils/pit_join.py（pit_join_backward 完全 docstring・line 19-104・sort 前入力検査・HIGH #1/#3 契約）
    - src/utils/category_map.py（`MISSING = "__MISSING__"` sentinel・line 21）
    - .planning/phases/03-as-of-features-snapshots/03-RESEARCH.md Code Example 1 line 384-439（rolling 3軸・defense-in-depth・sort 契約）
    - .planning/phases/03-as-of-features-snapshots/03-PATTERNS.md「src/features/rolling.py」節 line 167-212（呼出契約・3軸集約・sd は n<2 で __MISSING__）
    - .planning/phases/03-as-of-features-snapshots/03-PATTERNS.md「silent fallback 禁止」節 line 463-478
    - tests/features/test_rolling.py（Plan 03-01 で RED 立てた契約・test_under_5_starts_uses_missing_sentinel / test_rolling_three_axes_present / test_target_race_timediff_not_in_history / test_harontimel4_not_in_feature_matrix）
    - tests/features/conftest.py（_build_se_history_row / _build_race_obs_row のデフォルト値・as_of_datetime・feature_cutoff_datetime）
  </read_first>

  <behavior>
    - Test 1 `test_under_5_starts_uses_missing_sentinel`: starts_count==0（新馬・全 NaN）の馬の rolling_*_mean_5 / latest_5 / sd_5 が全て `MISSING`（"__MISSING__"）になる
    - Test 2 `test_rolling_three_axes_present`: 各9系統で mean / latest / sd の3列が存在し、sd は n<2 の場合 `MISSING`（Pitfall 3.3）
    - Test 3 `test_target_race_timediff_not_in_history`: 対象レース自身（race_date == observation の race_date）の `timediff` が history に入らない（defense-in-depth・Pitfall 3.1）
    - Test 4 `test_harontimel4_not_in_feature_matrix`: feature matrix に `harontimel4` 列が存在しない（Pitfall 3.6）
    - Test 5 `test_pit_join_backward_called_with_sorted_input`: `inspect.getsource(rolling)` に `.sort_values(...)` が pit_join_backward 呼出前に存在する（HIGH #1/#3 regression）
  </behavior>

  <action>
    **`src/features/rolling.py`** — RESEARCH Example 1（line 384-439）と PATTERNS.md line 167-212 を統合した実装:

    - import: `from __future__ import annotations`, `import pandas as pd`, `import numpy as np`, `from src.utils.pit_join import pit_join_backward`, `from src.utils.category_map import MISSING`
    - `_ROLLING_SYSTEMS = ("kakuteijyuni", "timediff", "harontimel3", "jyuni3c_jyuni4c", "kyori", "babacd", "jyocd", "days_since_prev")` — 9系統（`harontimel4` は絶対に含めない・Pitfall 3.6）
    - `LOOKBACK = 5`（D-03 定数）
    - `def build_rolling_features(observations: pd.DataFrame, history: pd.DataFrame, *, lookback: int = LOOKBACK) -> pd.DataFrame:`
      1. **defense-in-depth pre-filter（Pitfall 3.1）** — history 側で各 observation の `feature_cutoff_datetime` **以前のみ**残す明示的 pre-filter を実装する: `history = history[history["as_of_datetime"] < observations.loc[obs_idx, "feature_cutoff_datetime"]]`（実装上は kettonum 毎に `as_of_datetime < feature_cutoff_datetime` で filter してから `merge_asof` で cutoff 以前の最新 lookback 件を取得）。**`pit_join_backward` の backward direction は最終防壁だが、これ単独に頼らない（Pitfall 3.1 defense-in-depth）** — target 行自身は cutoff == race_date-1day で race_date(target) は常に cutoff より未来なので構造的に到達不能だが、history 構築で「馬の全出走」を取る際に当日行も混入する後段 row_number での漏れを、history 側での明示的時系列 filter で二重に防ぐ。
      2. **事前 sort（HIGH #1/#3 契約）** — `history_sorted = history.sort_values(["kettonum", "as_of_datetime"])` / `obs_sorted = observations.sort_values(["kettonum", "feature_cutoff_datetime"])` を必ず呼出（pit_join_backward の契約）。
      3. **kettonum 毎に直近5走の snapshot を取得** — `pit_join_backward(observations=obs_sorted, history=history_sorted, on_cutoff="feature_cutoff_datetime", on_asof="as_of_datetime", by="kettonum")` を消費。lookback=5 の window は groupby(kettonum).head(5) または事前 row_number 付与で管理（`tolerance` は設定しない・5走 window は別途管理）。
      4. **3軸集約（D-04）** — 各系統で `rolling_<system>_mean_5 = recent5.mean(axis=1)` / `rolling_<system>_latest_5 = recent5["<system>_1"]`（最新値） / `rolling_<system>_sd_5 = recent5.std(axis=1, ddof=1)`（標準偏差）。pandas `mean()` は NaN 無視なので、5走未満でも available な走で算出（D-04）。
      5. **5走未満 sentinel（D-13・Pitfall 3.3）** — `starts_count = recent5.notna().sum(axis=1)` を `rolling_<system>_count_5` 列に格納（何走分で集約したか）。`starts_count == 0`（新馬）の3軸は `MISSING` sentinel 文字列で上書き。`sd` は `n<2` で定義不能のため `starts_count < 2` で `MISSING`（pandas std が自動で NaN になるのを sentinel で明示・silent fill 禁止）。
      6. **9系統全てに上記 3軸 + count 適用** — 各系統で `recent5` の列名を `_1`〜`_5` で揃える（例: `kakuteijyuni_1`〜`kakuteijyuni_5`）。`jyuni3c_jyuni4c` は `jyuni3c`/`jyuni4c` の両方から位置平均を取った上で rolling。
    - **禁止事項（実装上明示）** — `harontimel4` を source 列にしない・当日行を history に混ぜない・`mean()` の結果を NaN のまま放置しない（`__MISSING__` sentinel 必須）。
  </action>

  <verify>
    <automated>KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/features/test_rolling.py -q</automated>
  </verify>

  <acceptance_criteria>
    - src/features/rolling.py に `def build_rolling_features(observations, history, *, lookback=5)` が存在する
    - `LOOKBACK = 5` と `_ROLLING_SYSTEMS` 定数が定義され、`_ROLLING_SYSTEMS` に `"harontimel4"` が含まれない（`grep -c 'harontimel4' src/features/rolling.py` == 0・Pitfall 3.6）
    - `inspect.getsource(build_rolling_features)` に `pit_join_backward(` と `.sort_values(["kettonum", "as_of_datetime"])` と `.sort_values(["kettonum", "feature_cutoff_datetime"])` が含まれる（HIGH #1/#3・pit_join.py 契約）
    - `from src.utils.category_map import MISSING` が import されており、新馬（starts_count==0）の3軸列が `MISSING` で上書きされる
    - tests/features/test_rolling.py の全テストが GREEN（5テスト全て）
    - `test_rolling.py::test_target_race_timediff_not_in_history` が history 側の明示的 pre-filter（`as_of_datetime < feature_cutoff_datetime`）を検査すること（Pitfall 3.1 defense-in-depth・本 task の Step 1 明示 pre-filter が検証対象）・GREEN
    - `inspect.getsource(build_rolling_features)` に `as_of_datetime` と `feature_cutoff_datetime` の比較式（`<`）が含まれる（history 側明示的 pre-filter の regression guard・pit_join_backward の backward direction 単独依存でないことの証明）
    - `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/features/test_rolling.py -q` が GREEN
  </acceptance_criteria>

  <done>
    9系統 × 3軸 rolling feature 構築が GREEN。pit_join_backward 消費・sort 契約遵守・5走未満 __MISSING__ sentinel・harontimel4 除外が全て unit test で証明される。
  </done>
</task>

<task type="tdd" tdd_phase="green">
  <name>Task 2: src/features/running_style.py（推定脚質・過去走 jyuni3c/jyuni4c 閾値ルール・当日不使用）</name>
  <files>src/features/running_style.py</files>

  <read_first>
    - .planning/phases/03-as-of-features-snapshots/03-RESEARCH.md Code Example 2 line 444-481（推定脚質アルゴリズム・閾値 2.0/3.5/5.5）
    - .planning/phases/03-as-of-features-snapshots/03-PATTERNS.md「src/features/running_style.py」節 line 218-240
    - src/etl/class_normalize.py line 63-118（dict/閾値ルール分類の analog・未対象は unresolved 相当）
    - .planning/phases/03-as-of-features-snapshots/03-RESEARCH.md Pitfall 3.2 line 346-350（jyuni1c 短距離で57% 0/NULL・主軸にしない）
    - tests/features/test_running_style.py（Plan 03-01 RED 契約）
  </read_first>

  <behavior>
    - Test 1 `test_estimate_running_style_uses_past_races_only`: `inspect.getsource(running_style)` に `kyakusitukubun` が出現しない（当日リーク防止・D-05 regression）
    - Test 2 `test_short_distance_jyuni1c_zero_is_missing`: 過去走 `jyuni1c=0`（短距離・1コーナー不存在）のみの馬でも、`jyuni3c`/`jyuni4c` > 0 が1件でもあれば `__MISSING__` でなく推定脚質を返す（Pitfall 3.2）
    - Test 3 `test_threshold_classification`: avg_position ≤2.0 → '逃'、≤3.5 → '先'、≤5.5 → '差'、>5.5 → '追' の parametrize 4ケース
    - Test 4 `test_new_horse_returns_missing`: history_5starts が空（len==0）の場合 `__MISSING__` を返す（D-13）
    - Test 5 `test_all_invalid_returns_missing`: 全走で `jyuni3c`/`jyuni4c` が 0/NULL の場合 `__MISSING__`（Pitfall 3.2）
  </behavior>

  <action>
    **`src/features/running_style.py`** — RESEARCH Example 2（line 444-481）をほぼそのまま実装:

    - import: `from __future__ import annotations`, `import pandas as pd`, `from src.utils.category_map import MISSING`
    - `def estimate_running_style(history_5starts: pd.DataFrame) -> str:`
      1. `if len(history_5starts) == 0: return MISSING`（新馬・D-13）
      2. `valid = history_5starts[(history_5starts["jyuni3c"] > 0) & (history_5starts["jyuni4c"] > 0)]`（0 と NULL を除外・短距離 pitfall 回避）
      3. `if len(valid) == 0: return MISSING`（全走で3/4コーナー位置不明・稀・Pitfall 3.2）
      4. `avg_jyuni3c = valid["jyuni3c"].mean()` / `avg_jyuni4c = valid["jyuni4c"].mean()` / `avg_position = (avg_jyuni3c + avg_jyuni4c) / 2`（両コーナー平均で安定化）
      5. 閾値ルール: `avg_position <= 2.0 → "逃"` / `<= 3.5 → "先"` / `<= 5.5 → "差"` / `else → "追"`
    - **絶対使用禁止:** `kyakusitukubun`（SE #73・post_race_only・当日リーク・D-05）。`jyuni1c` を主軸にしない（57% 0/NULL・Pitfall 3.2・補助信号にも使わない・実装上 `valid` filter で除外）
    - vectorized 版（`def estimate_running_style_batch(history_by_horse: pd.DataFrameGroupBy) -> pd.Series:`）も提供して builder で groupby 適用可能にする。戻り値は str（"逃"/"先"/"差"/"追"/`MISSING`）。
  </action>

  <verify>
    <automated>KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/features/test_running_style.py -q</automated>
  </verify>

  <acceptance_criteria>
    - src/features/running_style.py に `def estimate_running_style(history_5starts: pd.DataFrame) -> str:` が存在する
    - `inspect.getsource(running_style)` に `kyakusitukubun` が出現しない（`grep -c 'kyakusitukubun' src/features/running_style.py` == 0・D-05）
    - `inspect.getsource(estimate_running_style)` に `jyuni1c` が主軸として出現しない（`valid` filter で `jyuni3c`/`jyuni4c` > 0 のみ）
    - tests/features/test_running_style.py の全5テストが GREEN
  </acceptance_criteria>

  <done>
    推定脚質アルゴリズムが GREEN。当日 kyakusitukubun 不使用・過去走 jyuni3c/jyuni4c 閾値ルール・5走未満/全 invalid の __MISSING__ が unit test で証明される。
  </done>
</task>

<task type="tdd" tdd_phase="green">
  <name>Task 3: src/features/builder.py（build_feature_matrix 公開 API・observations/static/rolling/running_style 統合・§13.2 metadata stamp）</name>
  <files>src/features/builder.py</files>

  <read_first>
    - src/etl/normalize.py line 178-184, 234-240（readonly SELECT・明示カラム・`PROJECT_WINDOW_FILTER`・`_JRA_FILTER`）
    - src/etl/normalize.py line 316-350（`_transform_uma_race_df` キャストイディオム・`pd.to_numeric(errors="coerce").astype("Int64")` / `.astype(float)`）
    - src/etl/normalize.py line 624-663（`run_normalized_etl` 公開 API シグネチャの analog）
    - src/etl/fukusho_label.py line 178-250（readonly SELECT helpers のパターン・columns list・fetchall → DataFrame）
    - src/etl/filters.py line 31-60（`PROJECT_WINDOW_FILTER` / `project_window_filter(alias)`・CR-06 single source・JOIN で alias 衝突回避）
    - src/db/connection.py line 21-54（`make_pool` role='readonly'・search_path 'public,normalized,label'）
    - src/features/availability.py（load_feature_availability / assert_all_entries_allowed・Plan 03-01）
    - src/features/rolling.py（build_rolling_features・Task 1 で作成）
    - src/features/running_style.py（estimate_running_style・Task 2 で作成）
    - .planning/phases/03-as-of-features-snapshots/03-RESEARCH.md Architecture Diagram line 116-192・Don't Hand-Roll line 308-319・Pitfall 3.1 line 338-344（明示カラム SELECT 義務）
    - .planning/phases/03-as-of-features-snapshots/03-PATTERNS.md「src/features/builder.py」節 line 103-164（public API シグネチャ・明示カラムリスト・cutoff 計算・禁止カラム select 義務）
    - docs/keiba_ai_requirements_v1.3.md §13.2（feature_cutoff_datetime = race_date - 1 day・必須5項目）・§13.4（当日禁止リスト）
    - tests/features/test_pit_cutoff.py / tests/features/test_builder.py（Plan 03-01 RED 契約）
  </read_first>

  <behavior>
    - Test 1 `test_cutoff_excludes_same_day_races`: 対象レース race_date='2023-06-04' の馬に、同日別レース（race_date='2023-06-04'・別 jyocd）の成績が rolling feature に混入しない（SC#1・Pitfall 3.1）
    - Test 2 `test_pit_join_backward_called_with_sorted_input`: builder で pit_join_backward 呼出前に `.sort_values()` がある（HIGH #1/#3 regression）
    - Test 3 `test_feature_cutoff_is_race_date_minus_one_day`: observations frame の `feature_cutoff_datetime == race_date - 1 day`（D-06）
    - Test 4 `test_split_independence`: 同一 snapshot を train/val/test に任意の race_date 境界で分割しても、境界付近の馬の feature 値が同一（D-09 全期間1枚）
    - Test 5 `test_no_select_star_in_builder`: `inspect.getsource(builder)` に `SELECT *` が出現しない（明示カラム・Pitfall 1）
    - Test 6 `test_banned_columns_not_selected`: `_HISTORY_SELECT_COLUMNS` 定数に `kyakusitukubun` / `bataijyu`（当日） / `ninki` / `odds` / `sibababacd` / `dirtbabacd` / `tenkocd` / `harontimel4` が含まれない（SC#2）
    - Test 7 `test_canonical_key_is_race_nkey_kettonum`: feature matrix の行 key が (race_nkey, kettonum) で一意（umaban は key にしない）
    - Test 8 `test_metadata_columns_stamped`: feature matrix に `as_of_datetime` / `feature_cutoff_datetime` / `feature_snapshot_id` / `feature_availability_version` の4列が全行に付与される（SC#1・§13.2）
  </behavior>

  <action>
    **`src/features/builder.py`** — PATTERNS.md line 103-164 を統合した公開 API:

    - import: `from __future__ import annotations`, `import logging`, `from typing import Any`, `import pandas as pd`, `from psycopg import Cursor`, `from psycopg_pool import ConnectionPool`, `from src.etl.filters import PROJECT_WINDOW_FILTER, project_window_filter`, `from src.features.availability import load_feature_availability, assert_all_entries_allowed`, `from src.features.rolling import build_rolling_features`, `from src.features.running_style import estimate_running_style`
    - **明示カラム定数（SC#2 前提）**:
      - `_HISTORY_SELECT_COLUMNS = (...)` — `normalized.n_uma_race` から取得する過去走 history カラム。`kettonum` / `race_nkey` / `race_date` / `as_of_datetime`（= race_date）/ `kakuteijyuni` / `timediff` / `harontimel3` / `jyuni3c` / `jyuni4c` / `jyuni1c` / `kyori` / `jyocd` / `datakubun`（フィルタ用）等。**絶対に含めない:** `kyakusitukubun`（post_race_only・SE #73）/ 当日 `bataijyu`（race_day）/ `ninki`/`odds`（odds_snapshot_available）/ `sibababacd`/`dirtbabacd`/`tenkocd`（race_day_morning/post_race_only・馬場状態・天候）/ `harontimel4`（Pitfall 3.6）/ 当日 `timediff` 当日行（cutoff で構造排除だが SELECT しないのが望ましい・過去走 timediff のみ）
      - `_OBS_SELECT_COLUMNS = (...)` — 対象レース行（observations）。`race_nkey` / `kettonum` / `race_date` / `race_start_datetime` / `jyocd` / `kyori` / `umaban` / `wakuban` / `barei` / `sexcd` / `futan` / `kisyucode` / `chokyosicode` / `class_code_normalized` 等
      - `_RACE_CONTEXT_COLUMNS = (...)` — `normalized.n_race` から取得する race-level 文脈。`race_nkey` / `race_date` / `race_start_datetime` / `jyocd` / `kyori` / `trackcd`（`raw_everydb2.n_race` VIEW から readonly SELECT・Open Question #1 (a)）/ `coursekubuncd`（同）/ `class_code_normalized`
      - `_BLOODLINE_COLUMNS = (...)` — `public.n_uma` から取得する3代血統。`kettonum` / `ketto3infohansyokunum1`（sire_id）/ `ketto3infohansyokunum2`（bms_id）
    - **禁止カラム（実装上明示）**: `_BANNED_COLUMNS = frozenset({"kyakusitukubun", "ninki", "odds", "sibababacd", "dirtbabacd", "tenkocd", "harontimel4", "bataijyu"})` — `_HISTORY_SELECT_COLUMNS` と intersect が空であることを `assert _BANNED_COLUMNS.isdisjoint(_HISTORY_SELECT_COLUMNS)` で起動時に検証（SC#2 構造的防御）。
    - `def build_feature_matrix(read_pool: ConnectionPool, *, snapshot_id: str, label_version: str, fa_version: str, train_period: tuple[str, str] = ("2016-07-01", "2023-12-31"), validation_period: tuple[str, str] = ("2024-01-01", "2024-12-31")) -> dict[str, Any]:`
      1. `spec = load_feature_availability()` / `assert_all_entries_allowed(spec)` — 許可タイミング強制（Plan 03-01 作成済 loader）
      2. readonly SELECT で observations / history / race_context / bloodline を取得（明示カラム・`PROJECT_WINDOW_FILTER` / `project_window_filter(alias)` で JRA フィルタ・CR-06）
      3. **cutoff 計算（D-06）**: `observations["feature_cutoff_datetime"] = pd.to_datetime(observations["race_date"]) - pd.Timedelta(days=1)` / `observations["as_of_datetime"] = observations["race_start_datetime"]`（§13.2 必須項目・cutoff 基準は race_date-1day のみ・race_start_datetime は保持するが cutoff 基準にしない）
      4. 静的属性 transform（normalize.py:_transform_uma_race_df line 316-350 のキャストイディオム）— int 系 `pd.to_numeric(errors="coerce").astype("Int64")`、real 系 `.astype(float)`
      5. **rolling features 統合** — `rolling_df = build_rolling_features(observations, history)` を呼出（Task 1 実装・sort 契約は rolling 側で処理）
      6. **推定脚質** — `history.groupby("kettonum").apply(estimate_running_style)` で各馬に付与（Task 2 実装）
      7. **§13.2 metadata stamp** — feature matrix の全行に `feature_snapshot_id = snapshot_id` / `feature_availability_version = fa_version` / `label_generation_version = label_version` / `prediction_timing = "1A"` の定数列を付与
      8. **canonical key 検証** — `assert feature_matrix.groupby(["race_nkey", "kettonum"]).ngroups == len(feature_matrix)` で (race_nkey, kettonum) 一意性を保証（umaban 重複29件回避・Pitfall・実 DB 554267 = 554267 unique）
      9. 戻り値: `{"feature_matrix": pd.DataFrame, "snapshot_id": snapshot_id, "raw_touched": False, "feature_count": int, "row_count": int}`
    - **絶対禁止（実装上明示）**: `SELECT *`（明示カラムのみ・Pitfall 1・PATTERNS line 552-559）/ raw/normalized への INSERT/UPDATE/CREATE（readonly ロール・D-06）/ target race 当日行の history 混入（defense-in-depth・rolling 側でも cutoff filter 二重化）
  </action>

  <verify>
    <automated>KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/features/test_builder.py tests/features/test_pit_cutoff.py -q</automated>
  </verify>

  <acceptance_criteria>
    - src/features/builder.py に `def build_feature_matrix(read_pool, *, snapshot_id, label_version, fa_version, train_period=..., validation_period=...)` が存在する
    - `_HISTORY_SELECT_COLUMNS` と `_BANNED_COLUMNS = frozenset({"kyakusitukubun", "ninki", "odds", "sibababacd", "dirtbabacd", "tenkocd", "harontimel4", "bataijyu"})` の isdisjoint assertion が起動時に実行される
    - `inspect.getsource(builder)` に `SELECT *` が出現しない（`grep -c 'SELECT \*' src/features/builder.py` == 0）
    - `inspect.getsource(builder)` に `pd.Timedelta(days=1)` が含まれる（D-06 cutoff）
    - `inspect.getsource(builder)` に `feature_snapshot_id` / `feature_cutoff_datetime` / `feature_availability_version` の §13.2 metadata stamp が含まれる
    - `inspect.getsource(builder)` に `pit_join_backward` 呼出しがない（builder 自身は呼ばない・rolling.py 内で消費・builder は rolling.py を呼ぶ）または builder から `from src.features.rolling import build_rolling_features` で rolling を経由
    - **feature_matrix の列名に `jockey_id` / `trainer_id` / `sire_id` / `bms_id` / `horse_id` が存在する（category_map_consumer 契約・W-2）** — `inspect.getsource(build_feature_matrix)` に `.rename(columns={"kisyucode": "jockey_id", "chokyosicode": "trainer_id", "ketto3infohansyokunum1": "sire_id", "ketto3infohansyokunum2": "bms_id", "kettonum": "horse_id"})` が含まれる（grep 検査可能: `grep -c 'kisyucode.*jockey_id' src/features/builder.py` ≥ 1）。Plan 03-04 の `_CATEGORY_COLUMNS` と整合・rename 後も canonical feature-row key `(year, jyocd, kaiji, nichiji, racenum, kettonum)` は壊れない（rename は value 用カラムのみ・key 用の `kettonum`/`racenum` 等は元名で保持）
    - tests/features/test_builder.py の全8テストと tests/features/test_pit_cutoff.py の全テストが GREEN
    - `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/features/ -q` で test_allowlist / test_rolling / test_running_style / test_pit_cutoff / test_builder が全て GREEN（test_snapshot_repro / test_category_map_consumer は Plan 03-04 で GREEN 化）
  </acceptance_criteria>

  <done>
    build_feature_matrix 公開 API が GREEN。readonly SELECT・明示カラム・禁止カラム構造的排除・cutoff D-06・§13.2 metadata stamp・rolling/running_style 統合・canonical key 一意性が unit test で証明される。SC#1/SC#2 達成。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| readonly role → normalized/raw/public SELECT | builder は `make_pool(role='readonly')` のみ・raw/normalized/label へ USAGE+SELECT・UPDATE/INSERT/CREATE は REVOKE で物理拒否 |
| feature_availability registry → builder SELECT list | `_HISTORY_SELECT_COLUMNS` が `_BANNED_COLUMNS` と disjoint・allowlist が監査可能 map |
| history DataFrame → pit_join_backward | sort 契約・未ソートは raise ValueError（HIGH #1/#3）・target race 当日行は cutoff で構造排除・defense-in-depth pre-filter で二重化 |
| rolling aggregation → __MISSING__ sentinel | pandas `mean()` の NaN 無視を sentinel で明示化（silent fill 禁止・D-13） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Tampering (future-info leak) | rolling feature に対象レース当日行が混入 | mitigate | `feature_cutoff_datetime = race_date - 1 day` filter・pit_join_backward direction='backward'・defense-in-depth pre-filter・test_cutoff_excludes_same_day_races (SC#1・Pitfall 3.1) |
| T-03-11 | Tampering (banned feature sneak-in) | `_HISTORY_SELECT_COLUMNS` に `kyakusitukubun`/`bataijyu` 等が混入 | mitigate | `_BANNED_COLUMNS.isdisjoint(_HISTORY_SELECT_COLUMNS)` 起動時 assert・test_banned_columns_not_selected (SC#2)・test_no_select_star_in_builder inspect |
| T-03-12 | Tampering (silent mislabeling) | 5走未満の rolling feature を NaN/0 で埋める | mitigate | `__MISSING__` sentinel（`from src.utils.category_map import MISSING`）・`rolling_*_count_5` 列で何走分か明示・test_under_5_starts_uses_missing_sentinel (D-13・Pitfall 3.3) |
| T-03-13 | Tampering (same-day race leak) | D-06 cutoff が時刻粒度で同日午前/午後混入を許す | mitigate | cutoff は `race_date - 1 day` の日付粒度（D-06）・同日全レースを一律排除・test_cutoff_excludes_same_day_races |
| T-03-14 | Tampering (post_race leak via kyakusitukubun) | 推定脚質に当日 `kyakusitukubun` (SE #73) を使用 | mitigate | estimate_running_style は `jyuni3c`/`jyuni4c` のみ・`inspect.getsource(running_style)` で `kyakusitukubun` 出現しないことを regression (D-05・Pitfall 3.2) |
| T-03-15 | Tampering (column alias collision) | JOIN で `race_date` 等の alias が衝突し誤列を SELECT | mitigate | `project_window_filter('fl')` / `project_window_filter('nr')` で alias 明示（CR-06）・`_OBS_SELECT_COLUMNS` と `_RACE_CONTEXT_COLUMNS` を明示的に管理 |
| T-03-16 | Tampering (canonical key duplication) | `umaban` を行 key にして重複29件で row multiplication | mitigate | canonical key = (race_nkey, kettonum)・`assert groupby(["race_nkey", "kettonum"]).ngroups == len(feature_matrix)`・test_canonical_key_is_race_nkey_kettonum |
| T-03-17 | Information disclosure | ログに生 DSN や PII が出る | accept | builder は logger に row_count / feature_count / snapshot_id のみ出力・masked DSN は CLI 側（Plan 03-04）で管理・ローカル single-user |
</threat_model>

<verification>
- `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/features/test_rolling.py tests/features/test_running_style.py tests/features/test_builder.py tests/features/test_pit_cutoff.py tests/features/test_allowlist.py -q` 全て GREEN
- `grep -c 'harontimel4' src/features/rolling.py` == 0（Pitfall 3.6）
- `grep -c 'kyakusitukubun' src/features/running_style.py` == 0（D-05）
- `grep -c 'SELECT \*' src/features/builder.py` == 0（明示カラム・SC#2）
- `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/features/ -q` で test_snapshot_repro と test_category_map_consumer を除く6ファイルが GREEN
</verification>

<success_criteria>
- build_feature_matrix 公開 API が readonly pool から PIT-correct feature matrix を構築
- 各 feature 行に §13.2 必須5項目（as_of_datetime / feature_cutoff_datetime / feature_snapshot_id / feature_availability_version / prediction_timing）が stamp 済み
- 過去走 rolling 9系統 × 3軸が mean/latest/sd で算出され、5走未満は __MISSING__ sentinel
- 推定脚質が過去走 jyuni3c/jyuni4c のみから導出（当日 kyakusitukubun 不使用）
- 明示カラム SELECT のみ・禁止カラム構造的排除（_BANNED_COLUMNS.isdisjoint assertion）
- SC#1 と SC#2 の unit test が GREEN
</success_criteria>

<output>
Create `.planning/phases/03-as-of-features-snapshots/03-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol / Path | Kind | Purpose |
|---------------|------|---------|
| `src/features/rolling.py::build_rolling_features` | function | 9系統 × 3軸 rolling feature 構築（pit_join_backward 消費・__MISSING__ sentinel） |
| `src/features/rolling.py::_ROLLING_SYSTEMS` | tuple constant | rolling 対象9系統（harontimel4 除外・Pitfall 3.6） |
| `src/features/rolling.py::LOOKBACK` | int constant | `5`（D-03） |
| `src/features/running_style.py::estimate_running_style` | function | 過去走 jyuni3c/jyuni4c 閾値ルール分類（逃/先/差/追） |
| `src/features/running_style.py::estimate_running_style_batch` | function | groupby 適用 vectorized 版 |
| `src/features/builder.py::build_feature_matrix` | function | 公開 API・readonly SELECT → PIT-correct feature matrix・§13.2 metadata stamp |
| `src/features/builder.py::_HISTORY_SELECT_COLUMNS` | tuple constant | 明示カラムリスト（禁止カラム除外・SC#2） |
| `src/features/builder.py::_OBS_SELECT_COLUMNS` | tuple constant | observations 明示カラムリスト |
| `src/features/builder.py::_RACE_CONTEXT_COLUMNS` | tuple constant | normalized.n_race / raw_everydb2.n_race 明示カラム |
| `src/features/builder.py::_BLOODLINE_COLUMNS` | tuple constant | public.n_uma 3代血統 明示カラム |
| `src/features/builder.py::_BANNED_COLUMNS` | frozenset constant | `{kyakusitukubun, ninki, odds, sibababacd, dirtbabacd, tenkocd, harontimel4, bataijyu}` |
