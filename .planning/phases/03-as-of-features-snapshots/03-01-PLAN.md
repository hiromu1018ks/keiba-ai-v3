---
phase: 03-as-of-features-snapshots
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/features/__init__.py
  - src/config/feature_availability.yaml
  - src/features/availability.py
  - tests/features/__init__.py
  - tests/features/conftest.py
  - tests/features/test_allowlist.py
  - tests/features/test_pit_cutoff.py
  - tests/features/test_rolling.py
  - tests/features/test_running_style.py
  - tests/features/test_snapshot_repro.py
  - tests/features/test_category_map_consumer.py
  - tests/features/test_builder.py
autonomous: true
requirements: [FEAT-01, FEAT-02]
tags: [phase-03, features, tdd, wave-0-red, allowlist, availability-loader, conftest, reviews-rev2]

must_haves:
  truths:
    - "feature_availability.yaml が §13.5 全25 feature の実エントリ（静的15 + rolling 9系統 × 3軸 = 27 + 推定脚質1 = 計43エントリ）を持ち、schema_version が '0.2.0' に bump されている（D-01・Q5）"
    - "feature_availability.yaml の全エントリが available_from_timing ∈ {entry_confirmed, post_position_confirmed} のみ（D-07）・禁止5種（race_day_morning/body_weight_announced/odds_snapshot_available/post_race_only/same_day_aggregate）のエントリが0件（SC#2 前提）"
    - "feature_availability.yaml が target-observation 禁止カラム（kyakusitukubun/bataijyu/ninki/odds/sibababacd/dirtbabacd/tenkocd/harontimel4）と historical-rolling 許可カラム（babacd as historical rolling source 等）を区別する `source_role` フィールド（target_obs_banned / history_allowed_post_race / both_banned）をエントリ毎に持つ（REVIEWS HIGH #4）"
    - "load_feature_availability() が必須キー（schema_version/feature_schema/features）欠損時に raise ValueError する（Phase 1 D-13 silent fallback 禁止・fukusho_label.py:load_label_spec パターン踏襲）"
    - "feature_cutoff_datetime のセマンティクスが仕様化され: `race_date` は JST 00:00:00 midnight 解釈・`feature_cutoff_datetime = race_date - 1 day`（midnight JST）・PIT join は strict `< feature_cutoff_datetime + 1 day` すなわち同日 race_date 未満（`< midnight of race_date`）で filter（REVIEWS HIGH #2）"
    - "tests/features/ 配下に8つのテストファイル stub が存在し、各々が対応する SC#1-#4 / D-03/D-04/D-05/D-06/D-09 + REVIEWS HIGH #1-#6 振る舞いを assert しようとして ImportError または AttributeError で RED になる（Phase 2 Plan 02-02 パターン踏襲）"
    - "tests/features/conftest.py が合成 n_uma_race history 行・合成 n_race observations 行・mock readonly cursor・feature_availability.yaml 読込 fixture・5行 adversarial rolling-row builder（target / same-day-prior / same-day-later / previous-day / future）を提供する（REVIEWS HIGH #1）"
    - "banned_features(spec) ヘルパーが禁止 timing に該当する feature 名の list を返す（SC#2 fail-loud test が消費）"
    - "registry が出力 feature-matrix カラムと監査可能に対応付けられ、unregistered 出力カラムは assert_fail_loud で ValueError になる（REVIEWS HIGH #3）"
  artifacts:
    - path: "src/features/__init__.py"
      provides: "features package marker（Phase 3 で新設・Phase 1 は features/ 未存在）"
      contains: "\"Keiba AI v3 feature engineering\""
      min_lines: 5
    - path: "src/config/feature_availability.yaml"
      provides: "§13.5 全 feature の available_from_timing + source_role registry（D-07 allowlist 境界・SC#2 fail-loud test の入力・REVIEWS HIGH #4 taxonomy）"
      contains: "schema_version: \"0.2.0\""
      min_lines: 220
    - path: "src/features/availability.py"
      provides: "load_feature_availability（YAML→dict）・BANNED_TIMINGS frozenset・ALLOWED_TIMINGS frozenset・banned_features(spec) helper・TARGET_OBS_BANNED_COLUMNS・HISTORY_ALLOWED_POST_RACE_COLUMNS・assert_matrix_columns_registered(spec, output_columns)（REVIEWS HIGH #3）・CUTOFF_SEMANTICS 定数（REVIEWS HIGH #2）"
      contains: "def load_feature_availability"
      min_lines: 90
    - path: "tests/features/conftest.py"
      provides: "合成 se_history_row / race_obs_row builder・5-row adversarial rolling builder・feature_availability 読込 fixture・mock readonly_cur"
      contains: "def _build_se_history_row"
      min_lines: 90
    - path: "tests/features/test_allowlist.py"
      provides: "SC#2 fail-loud test（禁止 timing feature 0件・target-obs banned / history-allowed 区別・出力カラム全登録検査・inspect.getsource で SELECT * 検査）"
      contains: "test_no_banned_timing_features"
      min_lines: 60
    - path: "tests/features/test_pit_cutoff.py"
      provides: "SC#1 + D-06 + REVIEWS HIGH #2 cutoff enforcement（strict < cutoff・同日レース排除・cutoff 境界 JST midnight・merge_asof direction='backward'）"
      contains: "test_cutoff_excludes_same_day_races"
      min_lines: 50
    - path: "tests/features/test_rolling.py"
      provides: "D-03 lookback=5 / D-04 mean+latest+sd 3軸 / 5走未満 __MISSING__ sentinel / REVIEWS HIGH #1 per-observation latest-5 algorithm 対抗テスト"
      contains: "test_under_5_starts_uses_missing_sentinel"
      min_lines: 70
    - path: "tests/features/test_running_style.py"
      provides: "D-05 推定脚質（過去走 jyuni3c/jyuni4c のみ・当日 kyakusitukubun 不使用）"
      contains: "test_estimate_running_style_uses_past_races_only"
      min_lines: 40
    - path: "tests/features/test_snapshot_repro.py"
      provides: "SC#3 byte-reproducibility（同一 DataFrame → 同一 SHA256）・REVIEWS HIGH #6 SHA256 scope（Parquet bytes のみ・manifest created_at は除外）"
      contains: "test_byte_reproducible_by_hash"
      min_lines: 50
    - path: "tests/features/test_category_map_consumer.py"
      provides: "SC#4 train-only fit + __UNSEEN__ sentinel・REVIEWS HIGH #5 raw ID 列 drop 検査"
      contains: "test_unseen_maps_to_sentinel"
      min_lines: 50
    - path: "tests/features/test_builder.py"
      provides: "D-09 全期間1枚 + 分割非依存 + SELECT * 禁止 inspection guard・REVIEWS HIGH #3 出力カラム全登録検査"
      contains: "test_split_independence"
      min_lines: 50
  key_links:
    - from: "src/features/availability.py"
      to: "src/config/feature_availability.yaml"
      via: "yaml.safe_load + 必須キー検査"
      pattern: "feature_availability\\.yaml"
    - from: "tests/features/test_allowlist.py"
      to: "src/features/availability.py"
      via: "load_feature_availability + banned_features(spec) + assert_matrix_columns_registered"
      pattern: "from src\\.features\\.availability import"
    - from: "tests/features/test_pit_cutoff.py"
      to: "src/utils/pit_join.py (Phase 1 実装済)"
      via: "pit_join_backward direction='backward'（Plan 03-03 が消費）+ REVIEWS HIGH #2 strict < cutoff semantics"
      pattern: "from src\\.utils\\.pit_join import"
---

<objective>
Phase 3 の Wave 1 基盤を構築する: (1) `src/features/` package を新設、(2) `src/config/feature_availability.yaml` を枠（`features: []` 空・Phase 1 D-15）から §13.5 全 feature エントリに拡充（schema_version 0.1.0 → 0.2.0）、(3) `src/features/availability.py` loader + allowlist helpers を実装、(4) `tests/features/` 配下に8つのテストファイル stub を RED（ImportError / AttributeError で fail）で集群する。

**REVIEWS HIGH #1-#6 への対応（本 Plan が主担当: #2, #3, #4）:**
- **HIGH #2 (`feature_cutoff_datetime` セマンティクス)** — cutoff の比較演算子（strict `<`）と JST midnight 解釈を availability.py の `CUTOFF_SEMANTICS` 定数に文書化し、feature_availability.yaml と Parquet §12.4 metadata の `feature_cutoff_rule` フィールドに反映。test_pit_cutoff.py に境界 adversarial RED テストを追加。
- **HIGH #3 (出力カラム全登録検査)** — availability.py に `assert_matrix_columns_registered(spec, output_columns)` helper を追加し、registry 未登録の出力 feature-matrix カラムが存在すると ValueError で fail-loud。test_allowlist.py と test_builder.py が本 helper を消費。
- **HIGH #4 (target-obs banned vs history-allowed)** — feature_availability.yaml に `source_role` フィールド（target_obs_banned / history_allowed_post_race / both_banned）を追加し、`sibababacd`/`dirtbabacd` は target_obs_banned・`babacd`（rolling source としての過去走馬場状態）は history_allowed_post_race と明示的に区別。

Purpose: 後続 Plan 03-03（builder 本体）と 03-04（snapshot+category_map+CLI）が依存する「feature_availability registry と unit test 契約」を先に固定する。Phase 2 Plan 02-02 で確立した RED-集群パターンを再利用し、Plan 03-03 の GREEN 実装が silent leak / silent fill / banned カラム混入に陥るのを防ぐ。FEAT-01/FEAT-02 の検証ゲート（SC#1-#4）を構造化する。

Output:
- `src/features/__init__.py`（package marker）
- `src/features/availability.py`（loader + BANNED_TIMINGS + ALLOWED_TIMINGS + banned_features + assert_matrix_columns_registered + CUTOFF_SEMANTICS + TARGET_OBS_BANNED_COLUMNS + HISTORY_ALLOWED_POST_RACE_COLUMNS）
- `src/config/feature_availability.yaml`（25 feature を per-feature 粒度で網羅・schema_version '0.2.0'・source_role フィールド付き）
- `tests/features/__init__.py` / `conftest.py`（合成 DataFrame builder・mock cursor・availability fixture・5-row adversarial rolling builder）
- `tests/features/test_allowlist.py` / `test_pit_cutoff.py` / `test_rolling.py` / `test_running_style.py` / `test_snapshot_repro.py` / `test_category_map_consumer.py` / `test_builder.py`（全 RED stub）
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@$HOME/.claude/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-as-of-features-snapshots/03-CONTEXT.md
@.planning/phases/03-as-of-features-snapshots/03-RESEARCH.md
@.planning/phases/03-as-of-features-snapshots/03-PATTERNS.md
@.planning/phases/03-as-of-features-snapshots/03-VALIDATION.md
@.planning/phases/03-as-of-features-snapshots/03-REVIEWS.md
@.planning/phases/02-fukusho-labels/02-02-PLAN.md
@src/config/feature_availability.yaml
@src/config/label_spec.yaml
@src/etl/fukusho_label.py
@src/etl/class_normalize.py
@src/utils/pit_join.py
@src/utils/category_map.py
@tests/conftest.py
@tests/test_fukusho_label.py
</context>

<tasks>

<task type="tdd" tdd_phase="red">
  <name>Task 1: src/features/ package 新設 + feature_availability.yaml 25 feature エントリ拡充（source_role 付き・HIGH #4）+ availability.py loader + cutoff semantics（HIGH #2）+ matrix-column registry（HIGH #3）</name>
  <files>src/features/__init__.py, src/config/feature_availability.yaml, src/features/availability.py</files>

  <read_first>
    - src/config/feature_availability.yaml（現状の枠・schema_version '0.1.0'・features: [] 空・6項目スキーマ）
    - src/config/label_spec.yaml（YAML loader が従う analog・Phase 1 D-07）
    - src/etl/fukusho_label.py（load_label_spec 関数 line 82-110・必須キー検査パターン）
    - src/etl/class_normalize.py（load_class_config line 37-51・map 形式 YAML の analog）
    - .planning/phases/03-as-of-features-snapshots/03-RESEARCH.md Example 3 line 485-621（25 feature の drop-in YAML エントリ一覧・Q5 解決）
    - .planning/phases/03-as-of-features-snapshots/03-RESEARCH.md line 614（rolling source に babacd / sibababacd+dirtbabacd が言及されている箇所・HIGH #4 で source_role 区分を導入）
    - .planning/phases/03-as-of-features-snapshots/03-RESEARCH.md line 652-655（sibababacd/dirtbabacd は禁止だが raw_everydb2 VIEW からの SELECT を許容・babacd 系は rolling では過去走 source として許可されるべき根拠）
    - .planning/phases/03-as-of-features-snapshots/03-PATTERNS.md 「src/features/availability.py」節 line 47-99（loader 実装テンプレート・banned_features ヘルパー）
    - .planning/phases/03-as-of-features-snapshots/03-CONTEXT.md decisions D-01/D-06/D-07（25種網羅・cutoff=race_date-1day・allowlist 境界）
    - .planning/phases/03-as-of-features-snapshots/03-REVIEWS.md Concern HIGH #2/#3/#4 + Suggestions line 144-181（cutoff semantics pinning / matrix-column allowlist / banned-split taxonomy）
    - docs/keiba_ai_requirements_v1.3.md §13.2（feature_cutoff_datetime 定義・strict 比較の根拠）・§13.3（6項目スキーマ）・§13.4（Phase 1-A 禁止リスト）・§13.5（25 feature 候補・推定脚質注記）
    - ./CLAUDE.md Leakage-prevention configuration #3 Point-in-time / as-of feature joins（`direction='backward'` の leak-safe invariant・`< feature_cutoff_datetime` 厳格適用）
  </read_first>

  <action>
    **(a) `src/features/__init__.py`** — package marker のみ。docstring に「Phase 3 で新設・特徴量マトリクス構築と snapshot 書込を担う（§17.2 レイアウト）」を明記。`__all__` は空（明示的 import を強制・`from src.features.builder import build_feature_matrix` 形式）。`src/etl/__init__.py` / `src/utils/__init__.py` を analog とする。

    **(b) `src/config/feature_availability.yaml`** — 既存の枠（schema_version '0.1.0'・features: []）を上書き:
    - `schema_version: "0.2.0"` に bump（Phase 3 で初めて features を埋めた印・snapshot metadata の feature_availability_version に同値を埋込・§19.1 再現性）
    - **REVIEWS HIGH #2: cutoff semantics の仕様化** — トップレベルに `cutoff_semantics:` ブロックを追加:
      - `comparison_operator: "strict_less_than"` — PIT join は `history.as_of_datetime < observation.feature_cutoff_datetime`（`<`・`<=` でない）
      - `timezone: "Asia/Tokyo"` — race_date は JST midnight（00:00:00）解釈
      - `cutoff_definition: "feature_cutoff_datetime = pd.to_datetime(race_date) - pd.Timedelta(days=1) (JST midnight)"`
      - `boundary_rule: "race row is included in history IFF race.as_of_datetime < feature_cutoff_datetime (strict <); a race on the same calendar date as feature_cutoff_datetime IS excluded"`
      - `documented_in: "Parquet §12.4 metadata field feature_cutoff_rule"`
    - `feature_schema:` 7項目スキーマに拡張（従来6項目 + `source_role`）: feature_name / feature_group / available_from_timing / source_role / source_table / cutoff_rule / leakage_risk_level
    - `source_role` の取り得る値（**REVIEWS HIGH #4**）:
      - `target_obs_banned` — 当日 observation の SELECT に現れてはならないカラム（race-day 情報: kyakusitukubun / 当日 bataijyu / ninki / odds / sibababacd / dirtbabacd / tenkocd / harontimel4 等）。これらは feature matrix の出力カラムとしても登録しない（registry から除外・fail-loud 対象）
      - `history_allowed_post_race` — 過去走 history SELECT のみ許可される source カラム。`babacd`（過去走の馬場状態・rolling_*_babacd_* 派生 feature の source）・`timediff`（過去走の勝馬差）・`harontimel3`（過去走の上がり3F）・`jyuni3c`/`jyuni4c`（過去走の通過順）等。これらは rolling feature を通じて feature matrix 出力カラム（`rolling_babacd_mean_5` 等）として登録
      - `both_allowed` — target observation・history 両方で SELECT 可能な静的カラム（barei / sexcd / kyori / jyocd / kisyucode / chokyosicode / kettonum / ketto3infohansyokunum1 / ketto3infohansyokunum2 / trackcd / coursekubuncd 等）
    - `features:` リストに RESEARCH Example 3 に基づく **約43エントリ**（静的15 + rolling 9系統 × 3軸 = 27 + estimated_running_style 1）を追加。各エントリ7項目全て埋める:
      - 静的15種: `barei`/`sexcd`/`futan`/`jockey_id`(kisyucode)/`trainer_id`(chokyosicode)/`sire_id`(ketto3infohansyokunum1 via public.n_uma JOIN)/`bms_id`(ketto3infohansyokunum2)/`jyocd`/`kyori`/`trackcd`/`course_kubun`(coursekubuncd via raw_everydb2 VIEW)/`class_code_normalized`/`umaban`/`wakuban`/`horse_id`(kettonum・D-02 採用・leakage_risk_level=medium)。これら全て `source_role: both_allowed`
      - rolling 9系統 × 3軸: `kakuteijyuni` / `timediff`(=TimeDIFN, SE#66, 勝馬差) / `harontimel3`(上がり3F・harontimel4 は絶対にエントリにしない・Pitfall 3.6) / `jyuni3c_jyuni4c`(通過順) / `kyori`(距離) / `babacd`(過去走馬場状態・target race 当日の sibababacd/dirtbabacd とは区別・HIGH #4) / `jyocd`(競馬場) / `days_since_prev`(間隔) — 各系統で `rolling_<system>_mean_5` / `rolling_<system>_latest_5` / `rolling_<system>_sd_5` の3エントリ。これら全て `source_role: history_allowed_post_race`（source となる過去走カラムは history SELECT のみ許可・target race 当日行は cutoff で構造排除＋target_obs_banned の二重防御）
      - `estimated_running_style` 1エントリ（推定脚質・feature_group='running_style'・leakage_risk_level=low・source_role: history_allowed_post_race）
    - **全エントリ制約（D-07 厳守）:** `available_from_timing` は `entry_confirmed` または `post_position_confirmed` のみ。静的属性の `futan`/`jockey_id`/`umaban`/`wakuban` は `post_position_confirmed`（騎手/馬番/枠番確定後）、他は `entry_confirmed`。禁止5種（race_day_morning/body_weight_announced/odds_snapshot_available/post_race_only/same_day_aggregate）のエントリは一切作らない。
    - **全エントリ制約（D-06 厳守）:** `cutoff_rule: "race_date - 1 day (strict < cutoff, JST midnight)"` で一律（HIGH #2 semantics 反映）。
    - `source_table` は `normalized.n_uma_race` / `normalized.n_race` / `public.n_uma` / `raw_everydb2.n_race`（trackcd/coursekubuncd のみ readonly SELECT・Open Question #1 (a) 採用）のいずれか。target_obs_banned カラム（kyakusitukubun/bataijyu 当日行/当日 timediff/sibababacd/dirtbabacd/tenkocd/odds/ninki）は registry に feature として一切登録しない。

    **(c) `src/features/availability.py`** — PATTERNS.md line 47-99 テンプレートを拡張実装:
    - import: `from __future__ import annotations`, `from pathlib import Path`, `from typing import Any`, `import yaml`
    - `_DEFAULT_CONFIG_PATH = Path("src/config/feature_availability.yaml")`
    - `_REQUIRED_SPEC_KEYS = ("schema_version", "feature_schema", "features", "cutoff_semantics")` — HIGH #2: cutoff_semantics を必須キーに昇格（欠損で ValueError）
    - `CUTOFF_SEMANTICS = {` 定数 dict — HIGH #2:
      - `"comparison_operator": "strict_less_than"`
      - `"timezone": "Asia/Tokyo"`
      - `"cutoff_definition": "feature_cutoff_datetime = pd.to_datetime(race_date) - pd.Timedelta(days=1) (JST midnight)"`
      - `"pit_filter": "history.as_of_datetime < observation.feature_cutoff_datetime"`
      - `"boundary_rule": "a race whose as_of_datetime equals feature_cutoff_datetime midnight is EXCLUDED (strict <)"`
    - `BANNED_TIMINGS = frozenset({"race_day_morning", "body_weight_announced", "odds_snapshot_available", "post_race_only", "same_day_aggregate"})` — D-07
    - `ALLOWED_TIMINGS = frozenset({"entry_confirmed", "post_position_confirmed"})` — D-07
    - **HIGH #4: target-obs vs history-allowed カラム taxonomy** — 定数 frozenset で明示:
      - `TARGET_OBS_BANNED_COLUMNS = frozenset({"kyakusitukubun", "bataijyu", "ninki", "odds", "sibababacd", "dirtbabacd", "tenkocd", "harontimel4"})` — 当日 observation SELECT 禁止（race-day 情報）
      - `HISTORY_ALLOWED_POST_RACE_COLUMNS = frozenset({"kakuteijyuni", "timediff", "harontimel3", "jyuni3c", "jyuni4c", "jyuni1c", "babacd", "datakubun", "harontimel4"})` — 過去走 history SELECT 許可（post_race_only だが rolling source としては許容）。`harontimel4` は history SELECT では許可しても feature matrix 出力カラムには絶対にしない（rolling.py で `_ROLLING_SYSTEMS` が harontimel4 を含まない・別防御）
      - `TARGET_OBS_AND_HISTORY_BANNED = TARGET_OBS_BANNED_COLUMNS - HISTORY_ALLOWED_POST_RACE_COLUMNS` — 両文脈で禁止（odds/ninki/sibababacd/dirtbabacd/tenkocd/kyakusitukubun/bataijyu 等）
    - `def load_feature_availability(path: str | Path = _DEFAULT_CONFIG_PATH) -> dict[str, Any]:` — `yaml.safe_load` → dict 型チェック → 必須キー検査（欠損時 `raise ValueError`・fukusho_label.py:load_label_spec パターン踏襲・Phase 1 D-13 silent fallback 禁止）→ `cutoff_semantics` ブロック検査（HIGH #2）
    - `def banned_features(spec: dict[str, Any]) -> list[str]:` — `spec["features"]` を走査し `available_from_timing` が `BANNED_TIMINGS` に含まれる feature_name の list を返す（SC#2 fail-loud test が消費）。空リスト期待。
    - `def assert_all_entries_allowed(spec: dict[str, Any]) -> None:` — 全 feature の timing が ALLOWED_TIMINGS に属することを検査（違反時 `raise ValueError`）。banned でなくても未知 timing（typo 等）も検出する防御層。
    - **HIGH #3: 出力カラム全登録検査 helper**:
      - `def registered_feature_columns(spec: dict[str, Any]) -> set[str]:` — `spec["features"]` の `feature_name` を集合で返す（出力 feature-matrix のカラムとして期待される名前）。これが feature matrix の「許可リスト」
      - `_RESERVED_NON_FEATURE_COLUMNS = frozenset({"race_nkey", "kettonum", "year", "jyocd", "kaiji", "nichiji", "racenum", "race_date", "race_start_datetime", "as_of_datetime", "feature_cutoff_datetime", "feature_snapshot_id", "feature_availability_version", "label_generation_version", "prediction_timing", "rolling_<system>_count_5"})` — feature でない管理/key/audit カラム（registry 対象外・明示的に許可）
      - `def assert_matrix_columns_registered(spec: dict[str, Any], output_columns: list[str] | set[str], *, reserved: frozenset[str] = _RESERVED_NON_FEATURE_COLUMNS) -> None:` — output_columns の各要素が `registered_feature_columns(spec) ∪ reserved ∪ {c + "_code" for c in _CATEGORY_COLUMNS}` のいずれかに属することを検査。1件でも未登録（registry に無い・reserved でない・`_code` suffix で category map 由来でない）出力カラムがあれば `raise ValueError(f"unregistered feature-matrix column: {col}; allowed = registry features + reserved keys + <col>_code from frozen category map")`。test_allowlist.py と test_builder.py が消費（HIGH #3）。
  </action>

  <verify>
    <automated>KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/features/test_allowlist.py -q</automated>
  </verify>

  <acceptance_criteria>
    - src/features/__init__.py が存在し docstring に "Phase 3" と "snapshot" を含む
    - src/config/feature_availability.yaml の `schema_version:` が `"0.2.0"` である（`grep -c 'schema_version: "0.2.0"' src/config/feature_availability.yaml` == 1）
    - src/config/feature_availability.yaml にトップレベル `cutoff_semantics:` ブロックが存在し、`comparison_operator: "strict_less_than"` と `timezone: "Asia/Tokyo"` を含む（REVIEWS HIGH #2・`grep -c 'comparison_operator: "strict_less_than"' src/config/feature_availability.yaml` == 1）
    - src/config/feature_availability.yaml の `features:` 配下に `feature_name:` エントリが ≥ 43 件存在する（静的15 + rolling 27 + 推定脚質1 = 計43・`grep -c 'feature_name:' src/config/feature_availability.yaml` ≥ 43・must_haves.truths と整合）
    - src/config/feature_availability.yaml の `feature_schema:` が7項目（feature_name / feature_group / available_from_timing / source_role / source_table / cutoff_rule / leakage_risk_level）を含む（HIGH #4・`grep -c 'source_role' src/config/feature_availability.yaml` ≥ 1）
    - src/config/feature_availability.yaml に `available_from_timing: post_race_only` / `odds_snapshot_available` / `body_weight_announced` / `race_day_morning` / `same_day_aggregate` のいずれも出現しない（`grep -cE 'available_from_timing: (race_day_morning|body_weight_announced|odds_snapshot_available|post_race_only|same_day_aggregate)' src/config/feature_availability.yaml` == 0・D-07）
    - src/config/feature_availability.yaml に `harontimel4` / `kyakusitukubun` / 当日 `bataijyu` が feature_name として出現しない（Pitfall 3.6・D-05）
    - src/config/feature_availability.yaml に `rolling_babacd_mean_5` / `rolling_babacd_latest_5` / `rolling_babacd_sd_5` の3エントリが存在し、各々 `source_role: history_allowed_post_race` である（HIGH #4・過去走 babacd は許可・`grep -A3 'feature_name: rolling_babacd_mean_5' src/config/feature_availability.yaml | grep -c 'history_allowed_post_race'` == 1）
    - src/config/feature_availability.yaml に `sibababacd` / `dirtbabacd` が feature_name として出現しない（target_obs_banned なので registry 登録対象外・HIGH #4・`grep -cE 'feature_name: (sibababacd|dirtbabacd)' src/config/feature_availability.yaml` == 0）
    - src/features/availability.py に `def load_feature_availability` / `def banned_features` / `def registered_feature_columns` / `def assert_matrix_columns_registered` / `CUTOFF_SEMANTICS` / `TARGET_OBS_BANNED_COLUMNS` / `HISTORY_ALLOWED_POST_RACE_COLUMNS` / `BANNED_TIMINGS = frozenset` / `ALLOWED_TIMINGS = frozenset` が存在する
    - src/features/availability.py の `_REQUIRED_SPEC_KEYS` が `"cutoff_semantics"` を含む（HIGH #2）
    - `assert_matrix_columns_registered(spec, ["unregistered_evil_column"])` が `ValueError("unregistered feature-matrix column: unregistered_evil_column")` を raise することを `uv run python -c "..."` で簡易検証可能（本格テストは test_allowlist.py）
  </acceptance_criteria>

  <done>
    feature_availability.yaml が25 feature を網羅（source_role 付き）し schema_version='0.2.0'・禁止 timing エントリ0件・cutoff_semantics ブロックで strict < を明示化（HIGH #2/#4）。availability.py が loader / banned_features / assert_all_entries_allowed / assert_matrix_columns_registered / CUTOFF_SEMANTICS / TARGET_OBS_BANNED_COLUMNS / HISTORY_ALLOWED_POST_RACE_COLUMNS helper を提供（HIGH #3）。tests/features/test_allowlist.py が GREEN になる（依存する builder.py は Plan 03-03 で作成・他の test_*.py は引き続き RED）。
  </done>
</task>

<task type="tdd" tdd_phase="red">
  <name>Task 2: tests/features/ package + conftest.py（5-row adversarial rolling builder・HIGH #1）+ 8つの RED テスト stub 集群（SC#1-#4 + D-03/D-04/D-05/D-06/D-09 + REVIEWS HIGH #1-#6）</name>
  <files>tests/features/__init__.py, tests/features/conftest.py, tests/features/test_allowlist.py, tests/features/test_pit_cutoff.py, tests/features/test_rolling.py, tests/features/test_running_style.py, tests/features/test_snapshot_repro.py, tests/features/test_category_map_consumer.py, tests/features/test_builder.py</files>

  <read_first>
    - tests/conftest.py（ルート conftest・DB skip policy を継承・再定義不要・KEIBA_SKIP_DB_TESTS=1 挙動）
    - tests/test_fukusho_label.py line 50-96（`_build_se_row` synthetic DataFrame builder の analog・必ず読む）
    - tests/utils/test_pit_join.py（pit_join_backward の unit test パターン・未ソート raise ValueError 検査 line 81-97, 167-172）
    - tests/test_normalized_etl.py line 29-53（idempotent load の空入力拒否・mock cursor・`refusing to swap to empty` 検査）
    - .planning/phases/02-fukusho-labels/02-02-PLAN.md（RED-集群パターン・module-level import 遅延化・Task 1/2 構成）
    - .planning/phases/03-as-of-features-snapshots/03-VALIDATION.md（task ↔ test 対応表 line 42-52・Wave 0 Gaps line 712-724）
    - .planning/phases/03-as-of-features-snapshots/03-PATTERNS.md「pytest パターン」節 line 516-569（synthetic DataFrame・mock cursor・inspect.getsource guard・parametrize）
    - .planning/phases/03-as-of-features-snapshots/03-RESEARCH.md Code Examples line 382-481（rolling 3軸・推定脚質）
    - .planning/phases/03-as-of-features-snapshots/03-REVIEWS.md Suggestions line 144-186（5-row adversarial: target / same-day-prior / same-day-later / previous-day / future・HIGH #1）
    - src/features/availability.py（Plan 03-01 Task 1 で作成済・CUTOFF_SEMANTICS / TARGET_OBS_BANNED_COLUMNS / HISTORY_ALLOWED_POST_RACE_COLUMNS / assert_matrix_columns_registered を参照）
  </read_first>

  <action>
    **(a) `tests/features/__init__.py`** — 空パッケージ marker（`tests/utils/__init__.py` analog）。docstring 1行。

    **(b) `tests/features/conftest.py`** — features テスト共通 fixtures:
    - `def _build_se_history_row(kettonum: int, race_date: str, **overrides) -> dict:` — 合成 `normalized.n_uma_race` history 行（test_fukusho_label.py:50-96 パターン）。デフォルト値: `kettonum`/`race_date`/`kakuteijyuni=1`/`timediff=0`/`harontimel3=36.0`/`jyuni3c=1`/`jyuni4c=1`/`jyuni1c=0`（短距離 pitfall 再現用）/`kyori=1600`/`jyocd="05"`/`as_of_datetime=race_date`/`datakubun="7"`/`babacd="01"`（過去走馬場状態・HIGH #4 history_allowed）。`**overrides` で上書き可能。
    - `def _build_race_obs_row(race_nkey: str, kettonum: int, race_date: str, **overrides) -> dict:` — 合成 observations 行（対象レースの馬）。デフォルト: `race_nkey`/`kettonum`/`race_date`/`feature_cutoff_datetime = pd.to_datetime(race_date) - pd.Timedelta(days=1)`（D-06 + HIGH #2 semantics・`race_date` は JST midnight 解釈）/`race_start_datetime`/`jyocd`/`kyori`/`umaban=1`/`wakuban=1`/`barei=4`/`sexcd="1"`/`futan=57.0`/`kisyucode="01001"`/`chokyosicode="01001"`/`ketto3infohansyokunum1`/`ketto3infohansyokunum2`。
    - **REVIEWS HIGH #1: 5-row adversarial rolling builder** — `def _build_adversarial_rolling_rows(obs_race_date: str = "2023-06-04", kettonum: int = 1001) -> pd.DataFrame:` — 1頭の馬に対し、対象レース（target）と周辺の5状況を生成する合成 history 行:
      1. `target_row`: `as_of_datetime == obs_race_date`（対象レース当日・必ず rolling window から除外されるべき・feature_cutoff_datetime = obs_race_date - 1 day より未来）
      2. `same_day_prior_row`: `as_of_datetime == obs_race_date`（同日別レース・必ず除外・HIGH #1 / Pitfall 3.1）
      3. `same_day_later_row`: `as_of_datetime == obs_race_date`（同日午後レース・必ず除外）
      4. `previous_day_row`: `as_of_datetime == obs_race_date - 1 day`（前日レース・strict `<` なので必ず除外・HIGH #2 境界 adversarial）
      5. `future_row`: `as_of_datetime > obs_race_date`（未来レース・必ず除外）
      6. `eligible_history_rows`: 3行の `obs_race_date - 2 days / -3 days / -4 days` レース（window に含まれるべき正当な過去走）
      - 各行に `timediff` を区別可能な値（target=99.99, same_day_prior=88.88, same_day_later=77.77, previous_day=66.66, future=55.55, eligible=-1.0/-2.0/-3.0）で設定。rolling 結果の `rolling_timediff_mean_5` が eligible 3行の平均（=-2.0）のみを含み、target/same_day/previous_day/future の異常値を含まないことを test_rolling.py が機械的に検査（HIGH #1 対抗テスト・GREEN 化は Plan 03-03 で rolling.py が厳密 per-observation `< cutoff` algorithm を実装して達成）。
    - `@pytest.fixture def synthetic_availability():` — `load_feature_availability()` の結果を返す（Plan 03-01 Task 1 で作成済）。
    - `@pytest.fixture def mock_readonly_cur():` — `unittest.mock.MagicMock()` cursor（test_normalized_etl.py:29 パターン）。`fetchall()` が合成 list-of-tuples を返すよう `return_value` を設定可能。

    **(c) 各 test_*.py は module-level import を遅延化（Phase 2 Plan 02-02 パターン踏襲）:** 各テスト関数内で `from src.features.X import Y` を実行し、`src/features/builder.py` 等が未実装のため ImportError で RED になる。唯一 `test_allowlist.py` は Task 1 で作成した `src/features/availability.py` に依存するため GREEN になる（例外）。

    **(d) `tests/features/test_allowlist.py`** — SC#2 fail-loud + HIGH #3 出力カラム検査 + HIGH #4 taxonomy（GREEN 想定・availability.py のみ依存）:
    - `def test_no_banned_timing_features(synthetic_availability):` — `banned_features(synthetic_availability)` が `[]` を返すことを assert。違反時は failing assert message に違反 feature 名を含む。
    - `@pytest.mark.parametrize("timing", [...BANNED_TIMINGS])` で5禁止 timing の parametrize 版も追加（spec 内に当該 timing が0件）。
    - `def test_all_entries_in_allowed_set(synthetic_availability):` — `assert_all_entries_allowed(synthetic_availability)` が raise しない（未知 timing 含まない）。
    - **HIGH #3 出力カラム全登録検査**:
      - `def test_matrix_columns_all_registered(synthetic_availability):` — `assert_matrix_columns_registered(synthetic_availability, output_columns=[...全43 feature + reserved keys...])` が raise しない。
      - `def test_matrix_rejects_unregistered_column(synthetic_availability):` — `assert_matrix_columns_registered(synthetic_availability, output_columns=["unregistered_evil_column"])` が `ValueError("unregistered feature-matrix column: unregistered_evil_column")` を raise すること（banned source が allowed feature 名で偽装されて混入するのを機械的に検出・HIGH #3 GREEN 前提）。
      - `def test_matrix_rejects_banned_timing_column_under_allowed_name(synthetic_availability):` — `assert_matrix_columns_registered(synthetic_availability, output_columns=["same_day_track_condition"])` が `ValueError` を raise（HIGH #3・banned カラムが alias 名で潜入するのを検出）。
    - **HIGH #4 taxonomy 検査**:
      - `def test_target_obs_banned_columns_not_in_registry_features(synthetic_availability):` — registry の feature_name 一覧に `sibababacd` / `dirtbabacd` / `kyakusitukubun` / `bataijyu` / `ninki` / `odds` / `tenkocd` / `harontimel4` が含まれない（target_obs_banned は登録外・alias 名でも名前が一致すれば reject）。
      - `def test_history_allowed_babacd_rolling_present(synthetic_availability):` — registry の feature_name 一覧に `rolling_babacd_mean_5` / `rolling_babacd_latest_5` / `rolling_babacd_sd_5` が含まれる（過去走 babacd は history_allowed_post_race・HIGH #4 で許可）。

    **(e) `tests/features/test_pit_cutoff.py`** — SC#1 + D-06 + **HIGH #2 cutoff semantics**（RED・builder.py 未実装に依存）:
    - `def test_cutoff_excludes_same_day_races():` — 対象レース race_date='2023-06-04' の馬に、同日別レース（race_date='2023-06-04'・別 jyocd）の成績が rolling feature に混入しないことを assert（Pitfall 3.1・5-row adversarial builder の same_day_prior/same_day_later 行が除外）。
    - `def test_cutoff_excludes_previous_day_race_strict_less_than():` — **HIGH #2 境界 adversarial** — feature_cutoff_datetime = '2023-06-03' (JST midnight) に対し、previous_day_row の `as_of_datetime == '2023-06-03'`（cutoff と同日）が rolling feature に混入しない（strict `<` なので同日 == cutoff は除外・`<=` だと混入してしまう。これが RED で検出できれば strict `<` の証明）。
    - `def test_cutoff_excludes_future_race():` — 5-row adversarial builder の `future_row`（`as_of_datetime > race_date`）が rolling feature に混入しない。
    - `def test_pit_join_backward_called_with_sorted_input():` — `src/features/builder.py` 内で `pit_join_backward` 呼出前に `.sort_values()` があることを `inspect.getsource` で regression assert（test_pit_join.py:167-172 パターン）。
    - `def test_feature_cutoff_is_race_date_minus_one_day():` — observations frame の `feature_cutoff_datetime == pd.to_datetime(race_date) - pd.Timedelta(days=1)` であること（HIGH #2・JST midnight 解釈）。
    - `def test_cutoff_semantics_documented_in_metadata():` — builder が生成する feature matrix の `feature_cutoff_rule` 列（または Parquet metadata・Plan 03-04 側）に `"strict_less_than"` と `"Asia/Tokyo"` が含まれることを assert（HIGH #2・本テストは builder + snapshot 統合なので GREEN は Plan 03-03/03-04 完了後）。

    **(f) `tests/features/test_rolling.py`** — D-03/D-04/D-13 + **HIGH #1 per-observation latest-5 algorithm**（RED・rolling.py 未実装）:
    - `def test_under_5_starts_uses_missing_sentinel():` — 5走未満（例: 3走のみ）の馬の rolling_*_mean_5 が `__MISSING__` でなく平均値（3走分で算出）だが、starts_count=0（新馬）は3軸とも `__MISSING__`。`from src.utils.category_map import MISSING` で sentinel 一致性を検査。
    - `def test_rolling_three_axes_present():` — 各系統で mean / latest / sd の3列が存在し、sd は n<2 の場合 `__MISSING__`（Pitfall 3.3）。
    - `def test_target_race_timediff_not_in_history():` — 対象レース自身の `timediff` が rolling 入力 history に含まれない（defense-in-depth・Pitfall 3.1）。
    - `def test_harontimel4_not_in_feature_matrix():` — feature matrix に `harontimel4` 列が存在しない（Pitfall 3.6）。
    - **HIGH #1 per-observation latest-5 algorithm 対抗テスト:**
      - `def test_per_observation_latest_5_excludes_target_same_day_previous_future():` — `_build_adversarial_rolling_rows` で生成した5行（target / same_day_prior / same_day_later / previous_day / future）+ eligible 3行の計8行 history に対し、`build_rolling_features` の戻り `rolling_timediff_mean_5` が eligible 3行の平均（=-2.0）のみを含むことを assert。target (99.99) / same_day_prior (88.88) / same_day_later (77.77) / previous_day (66.66) / future (55.55) のどの異常値も rolling 結果に現れないこと（5行のどれか1つでも混入すれば mean ≠ -2.0 で検出可能）。
      - `def test_latest_5_window_ranked_by_race_start_datetime_desc():` — `inspect.getsource(build_rolling_features)` に `sort_values(... race_start_datetime ..., ascending=False)` と `.head(5)`（または `nlargest(5, ...)`）が含まれることを regression assert（HIGH #1・単一 backward merge_asof では到達不能・per-observation window algorithm の証明）。
      - `def test_history_pre_filtered_strict_less_than_cutoff():` — `inspect.getsource(build_rolling_features)` に `history["as_of_datetime"] < ... feature_cutoff_datetime` の strict `<` filter が含まれることを regression assert（HIGH #1/#2・pit_join_backward の単一方向依存でない defense-in-depth）。

    **(g) `tests/features/test_running_style.py`** — D-05（RED・running_style.py 未実装）:
    - `def test_estimate_running_style_uses_past_races_only():` — `inspect.getsource(running_style)` に `kyakusitukubun` が出現しないことを regression assert（当日リーク防止・D-05）。
    - `def test_short_distance_jyuni1c_zero_is_missing():` — 過去走 `jyuni1c=0`（短距離・1コーナー不存在）のみの馬の推定脚質が `__MISSING__` でなく、`jyuni3c`/`jyuni4c` から算出されること（Pitfall 3.2）。
    - `def test_threshold_classification():` — avg_position ≤2.0 → '逃'、≤3.5 → '先'、≤5.5 → '差'、>5.5 → '追' の parametrize 4ケース。
    - `def test_new_horse_returns_missing():` — history=[] の馬は `__MISSING__`。

    **(h) `tests/features/test_snapshot_repro.py`** — SC#3 + **HIGH #6 SHA256 scope**（RED・snapshot.py 未実装）:
    - `def test_byte_reproducible_by_hash():` — 同一合成 DataFrame から2回 write_snapshot を呼び、SHA256 hexstring が完全一致することを assert（Pitfall 3.5）。
    - `def test_metadata_contains_12_4_keys():` — Parquet schema metadata に `dataset_version`/`feature_snapshot_id`/`label_version`/`prediction_timing`/`feature_cutoff_rule`/`train_period`/`validation_period`/`created_at`/`feature_availability_version` の9キーが含まれること（§12.4）。
    - `def test_created_at_is_deterministic():` — manifest 側で実タイムスタンプ管理・schema metadata の `created_at` は関数引数で固定（Pitfall 3.5・再現性の要）。
    - **HIGH #6 SHA256 scope 検査**:
      - `def test_sha256_covers_parquet_bytes_only():` — 同一 DataFrame から2回 write_snapshot を呼び、それぞれ manifest の `created_at`（実タイムスタンプ）が異なっても、Parquet ファイルの SHA256 が一致することを assert（manifest bytes は SHA256 scope 外・HIGH #6）。実装は: 2回 write_snapshot を異なる `created_at` 引数（例: '2026-06-18T10:00:00' と '2026-06-19T15:30:00'）で呼び、戻り sha256 を比較。manifest の `created_at` が異なっても Parquet sha256 が一致することを検証。
      - `def test_manifest_created_at_varies_between_runs():` — 2回の run で manifest YAML の `created_at` フィールドが異なる値を持つこと（実タイムスタンプ・HIGH #6・manifest は byte-reproducible でないことを明示）。

    **(i) `tests/features/test_category_map_consumer.py`** — SC#4 + **HIGH #5 raw ID drop**（RED・category_map_consumer.py 未実装）:
    - `def test_unseen_maps_to_sentinel():` — train 窓に無い horse_id を val/test に適用した場合、`apply_category_map` の結果が `code["__UNSEEN__"]` になること。
    - `def test_fit_called_on_train_window_only():` — `inspect.getsource(category_map_consumer)` で train 窓 mask（`race_date` で '2016-07-01'〜'2023-12-31' filter）が存在することを regression assert（Pitfall 3.4・D-09）。
    - `def test_codes_are_non_negative_int32():` — `apply_category_map` の戻り値 dtype が `int32` で min ≥ 0（LightGBM §14.3 非負要件・`-1` 禁止）。
    - **HIGH #5 raw ID drop 検査**:
      - `def test_apply_frozen_maps_drops_raw_id_columns():` — `apply_frozen_category_maps` 適用後の feature matrix に `jockey_id` / `trainer_id` / `sire_id` / `bms_id` / `horse_id` の生 ID 文字列列が残っていないこと（`_code` suffix 列のみ残る）を assert。具体: 合成 feature_matrix に `horse_id` 文字列列 + `horse_id_code` int32 列を渡して apply 後、`assert "horse_id" not in result.columns and "horse_id_code" in result.columns`（5カラム全て）。
      - `def test_apply_frozen_maps_no_raw_id_in_output_schema():` — `_CATEGORY_COLUMNS` に列挙された5カラム（`jockey_id` / `trainer_id` / `sire_id` / `bms_id` / `horse_id`）のうち1つでも非 `_code` 形式で apply 後 DataFrame に残っていれば fail（HIGH #5・Phase 4 モデルが raw ID で refit するのを構造的に防止）。

    **(j) `tests/features/test_builder.py`** — D-09 + SELECT * 禁止 + **HIGH #3 出力カラム全登録検査**（RED・builder.py 未実装）:
    - `def test_split_independence():` — 同一 snapshot を train/val/test に任意の race_date 境界で分割しても、境界付近の馬の feature 値が同一であることを property-based で assert（D-09・全期間1枚の根拠）。
    - `def test_no_select_star_in_builder():` — `inspect.getsource(builder)` に `SELECT *` が出現しない（明示カラムのみ・Pitfall 1・PATTERNS.md line 552-559）。
    - `def test_banned_columns_not_selected():` — `_HISTORY_SELECT_COLUMNS` 定数に `kyakusitukubun`/当日 `bataijyu`/`ninki`/`odds`/`sibababacd`/`dirtbabacd`/`tenkocd`/`harontimel4` が含まれないことを regression assert。**HIGH #4 区別:** `_HISTORY_SELECT_COLUMNS` には過去走 source として `babacd` / `timediff` / `harontimel3` / `jyuni3c` / `jyuni4c` を含めること（target_obs_banned でなく history_allowed_post_race・registry taxonomy と整合）。
    - `def test_canonical_key_is_race_nkey_kettonum():` — feature matrix の行 key が (race_nkey, kettonum) で一意（`umaban` は key にしない・Pitfall・29件重複回避）。
    - **HIGH #3 builder → availability 連携検査**:
      - `def test_builder_output_columns_all_registered_in_registry():` — `build_feature_matrix` の戻り feature_matrix の `columns` が `assert_matrix_columns_registered(spec, list(feature_matrix.columns))` で全て合格すること（registry 未登録カラムが builder 出力に含まれない・HIGH #3 GREEN 化は Plan 03-03）。本テストは builder 内部で `assert_matrix_columns_registered` を呼ぶことを期待する実装契約。

    全テストで `@pytest.mark.requires_db` は real-DB 統合テスト（test_builder.py::test_real_db_build 等）のみに付与。合成 DataFrame を使う unit test は付与しない（Phase 2 慣行）。
  </action>

  <verify>
    <automated>KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/features/ -q 2>&1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - tests/features/__init__.py が存在する
    - tests/features/conftest.py に `_build_se_history_row` / `_build_race_obs_row` / `_build_adversarial_rolling_rows` / `synthetic_availability` fixture / `mock_readonly_cur` fixture が定義される
    - `_build_adversarial_rolling_rows` が5種の異常行（target / same_day_prior / same_day_later / previous_day / future）と3行の eligible history を生成し、各行の `timediff` が区別可能な値を持つ（HIGH #1・grep で `"target"` / `"same_day_prior"` / `"same_day_later"` / `"previous_day"` / `"future"` の各識別子が conftest.py 内に登場）
    - tests/features/test_allowlist.py::test_no_banned_timing_features / test_all_entries_in_allowed_set / test_matrix_columns_all_registered / test_matrix_rejects_unregistered_column / test_matrix_rejects_banned_timing_column_under_allowed_name / test_target_obs_banned_columns_not_in_registry_features / test_history_allowed_babacd_rolling_present が全て GREEN（availability.py のみ依存・HIGH #3/#4 GREEN 前提）
    - tests/features/test_pit_cutoff.py::test_cutoff_excludes_previous_day_race_strict_less_than が RED（HIGH #2 境界 adversarial・builder.py 未実装に依存）
    - tests/features/test_pit_cutoff.py の全テストが RED（ImportError or AttributeError on src.features.builder）
    - tests/features/test_rolling.py::test_per_observation_latest_5_excludes_target_same_day_previous_future / test_latest_5_window_ranked_by_race_start_datetime_desc / test_history_pre_filtered_strict_less_than_cutoff が RED（HIGH #1・rolling.py 未実装）
    - tests/features/test_rolling.py の全テストが RED（src.features.rolling 未実装）
    - tests/features/test_running_style.py の全テストが RED（src.features.running_style 未実装）
    - tests/features/test_snapshot_repro.py::test_sha256_covers_parquet_bytes_only / test_manifest_created_at_varies_between_runs が RED（HIGH #6・snapshot.py 未実装）
    - tests/features/test_snapshot_repro.py の全テストが RED（src.features.snapshot 未実装）
    - tests/features/test_category_map_consumer.py::test_apply_frozen_maps_drops_raw_id_columns / test_apply_frozen_maps_no_raw_id_in_output_schema が RED（HIGH #5・category_map_consumer.py 未実装）
    - tests/features/test_category_map_consumer.py の全テストが RED（src.features.category_map_consumer 未実装）
    - tests/features/test_builder.py::test_builder_output_columns_all_registered_in_registry が RED（HIGH #3・builder.py 未実装）
    - tests/features/test_builder.py の全テストが RED（src.features.builder 未実装）
    - `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/features/ -q` の summary に「N passed, M failed」の形式で RED 件数が可視化される（M は各 stub のテスト数合計）
    - 各 test_*.py が module-level ではなく関数内 import を使用し（Phase 2 Plan 02-02 パターン）、collection 時ではなく実行時に RED になる
  </acceptance_criteria>

  <done>
    8テストファイル + conftest.py（5-row adversarial rolling builder 含む）が存在し、test_allowlist.py のみ GREEN、残り7ファイルが ImportError/AttributeError で RED。後続 Plan 03-03/03-04 がこれらのテストを GREEN にする形で実装を進める。REVIEWS HIGH #1-#6 の各 adversarial / structural テストが RED stub として存在。Nyquist Validation Strategy の Wave 0 gap が閉じた状態。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML config → loader | `src/config/feature_availability.yaml` → `load_feature_availability()`（未検証入力・schema validation 必須・`cutoff_semantics` ブロック含む・HIGH #2） |
| registry → allowlist test | `banned_features(spec)` + `assert_matrix_columns_registered(spec, ...)` が SC#2 fail-loud pytest に検査対象を供給（banned timing の structural block + 未登録出力カラムの structural block・HIGH #3） |
| target-obs vs history taxonomy | `TARGET_OBS_BANNED_COLUMNS` / `HISTORY_ALLOWED_POST_RACE_COLUMNS` が source_role 区分を定数化（HIGH #4・sibababacd は target_obs_banned・babacd は history_allowed_post_race） |
| conftest fixture → unit test | 合成 DataFrame が production データ形状を正しく再現する（実データ漏洩リスクなし・逆に実 DB のNULL/cardinality分布を模擬できていないと silent mislabeling） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering (Elevation) | `feature_availability.yaml` への禁止 timing エントリ混入 | mitigate | `banned_features(spec)` + `assert_all_entries_allowed(spec)` + test_allowlist.py::test_no_banned_timing_features で fail-loud 検査（SC#2・D-07） |
| T-03-02 | Tampering (Reproducibility) | YAML schema_version 不一致・loader が古い schema を通す | mitigate | `_REQUIRED_SPEC_KEYS` 検査（`cutoff_semantics` 含む・HIGH #2）+ schema_version を snapshot metadata に埋込（§19.1・Plan 03-04） |
| T-03-03 | Information disclosure | conftest.py の合成行に実データの PII（馬名/騎手名でなく ID のみ）が残る | accept | Phase 3 は ID のみ使用・実馬名は保持しない・ローカル single-user |
| T-03-04 | Tampering | test_collection_modifyitems の skip policy 継承が壊れて KEIBA_SKIP_DB_TESTS=1 でも DB テストが走る | mitigate | tests/features/conftest.py で再定義せず・ルート tests/conftest.py を継承（PATTERNS.md 指示） |
| T-03-R1 | Tampering (future-info leak via ambiguous cutoff) | `feature_cutoff_datetime = race_date - 1 day` の `<` vs `<=` / midnight vs end-of-day 曖昧さで未来/同日情報が混入（REVIEWS HIGH #2） | mitigate | `CUTOFF_SEMANTICS` 定数（`comparison_operator: "strict_less_than"` / `timezone: "Asia/Tokyo"` / `boundary_rule`）+ YAML `cutoff_semantics` ブロック + test_pit_cutoff.py::test_cutoff_excludes_previous_day_race_strict_less_than 境界 adversarial テスト（HIGH #2・5-row adversarial builder の previous_day_row が除外されることを機械的検証） |
| T-03-R3 | Tampering (unregistered column sneak-in) | banned source カラムが allowed feature 名の alias で feature matrix に混入（REVIEWS HIGH #3） | mitigate | `assert_matrix_columns_registered(spec, output_columns)` が registry 未登録カラムを全て ValueError で reject + test_allowlist.py::test_matrix_rejects_unregistered_column / test_matrix_rejects_banned_timing_column_under_allowed_name / test_builder.py::test_builder_output_columns_all_registered_in_registry（HIGH #3 GREEN 前提） |
| T-03-R4 | Tampering (column taxonomy conflation) | `sibababacd`/`dirtbabacd`（target_obs_banned）と `babacd`（history_allowed_post_race）が混同され、過去走 rolling source が誤って reject または target race 当日行が誤って許可（REVIEWS HIGH #4） | mitigate | feature_availability.yaml の `source_role` フィールド + availability.py の `TARGET_OBS_BANNED_COLUMNS` / `HISTORY_ALLOWED_POST_RACE_COLUMNS` 定数 + test_allowlist.py::test_target_obs_banned_columns_not_in_registry_features / test_history_allowed_babacd_rolling_present + test_builder.py::test_banned_columns_not_selected（HIGH #4・過去走 babacd rolling 許可・target race sibababacd/dirtbabacd 拒否） |
</threat_model>

<verification>
- `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/features/ -q` で test_allowlist.py が GREEN・他7ファイルが期待通り RED
- `uv run python -c "from src.features.availability import load_feature_availability, banned_features, BANNED_TIMINGS, CUTOFF_SEMANTICS, TARGET_OBS_BANNED_COLUMNS, HISTORY_ALLOWED_POST_RACE_COLUMNS, assert_matrix_columns_registered; spec = load_feature_availability(); print('features:', len(spec['features']), 'banned:', banned_features(spec), 'cutoff_op:', CUTOFF_SEMANTICS['comparison_operator'])"` で features 数が約43件・banned が空 list・cutoff_op が 'strict_less_than'
- `grep -c 'comparison_operator: "strict_less_than"' src/config/feature_availability.yaml` == 1（HIGH #2）
- `grep -c 'source_role' src/config/feature_availability.yaml` ≥ 1（HIGH #4）
- `grep -cE 'available_from_timing: (race_day_morning|body_weight_announced|odds_snapshot_available|post_race_only|same_day_aggregate)' src/config/feature_availability.yaml` == 0
</verification>

<success_criteria>
- feature_availability.yaml が25 feature を per-feature 粒度で網羅（静的15 + rolling 27 + 推定脚質1 = 計43）し schema_version '0.2.0'・禁止 timing エントリ0件・source_role taxonomy 付き（HIGH #4）
- cutoff_semantics ブロックが strict_less_than / JST midnight を文書化（HIGH #2）
- availability.py が loader / BANNED_TIMINGS / ALLOWED_TIMINGS / banned_features / assert_all_entries_allowed / assert_matrix_columns_registered / CUTOFF_SEMANTICS / TARGET_OBS_BANNED_COLUMNS / HISTORY_ALLOWED_POST_RACE_COLUMNS を提供（HIGH #2/#3/#4）
- tests/features/ に8つのテストファイル stub と conftest.py（5-row adversarial rolling builder 含む）が存在し、test_allowlist.py のみ GREEN・残り RED（Phase 2 RED-集群パターン踏襲）
- 後続 Plan 03-03/03-04 が各 RED テストを GREEN にする契約を確立（HIGH #1/#5/#6 対応含む）
</success_criteria>

<output>
Create `.planning/phases/03-as-of-features-snapshots/03-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol / Path | Kind | Purpose |
|---------------|------|---------|
| `src/features/__init__.py` | package marker | features package 新設（Phase 3 境界） |
| `src/features/availability.py::load_feature_availability` | function | YAML→dict loader（必須キー検査・ValueError fail-fast・cutoff_semantics 含む・HIGH #2） |
| `src/features/availability.py::BANNED_TIMINGS` | frozenset constant | `{race_day_morning, body_weight_announced, odds_snapshot_available, post_race_only, same_day_aggregate}`（D-07） |
| `src/features/availability.py::ALLOWED_TIMINGS` | frozenset constant | `{entry_confirmed, post_position_confirmed}`（D-07） |
| `src/features/availability.py::banned_features(spec)` | function | spec 内の禁止 timing feature 名 list を返す（SC#2 fail-loud 検査入力） |
| `src/features/availability.py::assert_all_entries_allowed(spec)` | function | 全 feature timing が ALLOWED_TIMINGS に属することを検査（防御層） |
| `src/features/availability.py::registered_feature_columns(spec)` | function | registry 登録済 feature_name の set を返す（HIGH #3 出力カラム検査入力） |
| `src/features/availability.py::assert_matrix_columns_registered(spec, output_columns)` | function | 出力カラムが全て registry または reserved/category_map 由来であることを検査（HIGH #3 fail-loud） |
| `src/features/availability.py::CUTOFF_SEMANTICS` | dict constant | `{comparison_operator: "strict_less_than", timezone: "Asia/Tokyo", cutoff_definition, pit_filter, boundary_rule}`（HIGH #2） |
| `src/features/availability.py::TARGET_OBS_BANNED_COLUMNS` | frozenset constant | `{kyakusitukubun, bataijyu, ninki, odds, sibababacd, dirtbabacd, tenkocd, harontimel4}`（HIGH #4） |
| `src/features/availability.py::HISTORY_ALLOWED_POST_RACE_COLUMNS` | frozenset constant | `{kakuteijyuni, timediff, harontimel3, jyuni3c, jyuni4c, jyuni1c, babacd, datakubun}`（HIGH #4） |
| `src/features/availability.py::_RESERVED_NON_FEATURE_COLUMNS` | frozenset constant | feature でない管理/key/audit カラム（registry 対象外・明示許可） |
| `src/config/feature_availability.yaml` | config file | schema_version '0.2.0'・cutoff_semantics ブロック・約43 feature エントリ（source_role 付き・監査可能 map・§13.3） |
| `tests/features/__init__.py` | package marker | features test package |
| `tests/features/conftest.py::_build_se_history_row` | fixture helper | 合成 n_uma_race history 行 builder（test_fukusho_label.py:50-96 パターン） |
| `tests/features/conftest.py::_build_race_obs_row` | fixture helper | 合成 observations 行 builder（JST midnight cutoff・HIGH #2） |
| `tests/features/conftest.py::_build_adversarial_rolling_rows` | fixture helper | 5-row adversarial rolling builder（target / same_day_prior / same_day_later / previous_day / future + eligible 3行・HIGH #1） |
| `tests/features/conftest.py::synthetic_availability` | fixture | `load_feature_availability()` の結果 |
| `tests/features/conftest.py::mock_readonly_cur` | fixture | `unittest.mock.MagicMock()` cursor |
| `tests/features/test_allowlist.py::test_no_banned_timing_features` | test | SC#2 fail-loud（GREEN 想定） |
| `tests/features/test_allowlist.py::test_matrix_columns_all_registered` | test | HIGH #3 出力カラム全登録検査（GREEN 想定） |
| `tests/features/test_allowlist.py::test_matrix_rejects_unregistered_column` | test | HIGH #3 未登録カラム reject（GREEN 想定） |
| `tests/features/test_allowlist.py::test_target_obs_banned_columns_not_in_registry_features` | test | HIGH #4 taxonomy（GREEN 想定） |
| `tests/features/test_allowlist.py::test_history_allowed_babacd_rolling_present` | test | HIGH #4 taxonomy（GREEN 想定） |
| `tests/features/test_pit_cutoff.py::test_cutoff_excludes_same_day_races` | test stub (RED) | SC#1 + D-06・Plan 03-03 が GREEN 化 |
| `tests/features/test_pit_cutoff.py::test_cutoff_excludes_previous_day_race_strict_less_than` | test stub (RED) | HIGH #2 境界 adversarial・Plan 03-03 が GREEN 化 |
| `tests/features/test_pit_cutoff.py::test_cutoff_excludes_future_race` | test stub (RED) | HIGH #1 future row 排除・Plan 03-03 が GREEN 化 |
| `tests/features/test_pit_cutoff.py::test_cutoff_semantics_documented_in_metadata` | test stub (RED) | HIGH #2 metadata 文書化・Plan 03-03/03-04 が GREEN 化 |
| `tests/features/test_rolling.py::test_under_5_starts_uses_missing_sentinel` | test stub (RED) | D-03/D-04/D-13・Plan 03-03 が GREEN 化 |
| `tests/features/test_rolling.py::test_per_observation_latest_5_excludes_target_same_day_previous_future` | test stub (RED) | HIGH #1 per-observation latest-5 algorithm・Plan 03-03 が GREEN 化 |
| `tests/features/test_rolling.py::test_latest_5_window_ranked_by_race_start_datetime_desc` | test stub (RED) | HIGH #1 regression guard・Plan 03-03 が GREEN 化 |
| `tests/features/test_rolling.py::test_history_pre_filtered_strict_less_than_cutoff` | test stub (RED) | HIGH #1/#2 defense-in-depth・Plan 03-03 が GREEN 化 |
| `tests/features/test_running_style.py::test_estimate_running_style_uses_past_races_only` | test stub (RED) | D-05・Plan 03-03 が GREEN 化 |
| `tests/features/test_snapshot_repro.py::test_byte_reproducible_by_hash` | test stub (RED) | SC#3・Plan 03-04 が GREEN 化 |
| `tests/features/test_snapshot_repro.py::test_sha256_covers_parquet_bytes_only` | test stub (RED) | HIGH #6 SHA256 scope・Plan 03-04 が GREEN 化 |
| `tests/features/test_snapshot_repro.py::test_manifest_created_at_varies_between_runs` | test stub (RED) | HIGH #6 manifest 非再現性・Plan 03-04 が GREEN 化 |
| `tests/features/test_category_map_consumer.py::test_unseen_maps_to_sentinel` | test stub (RED) | SC#4・Plan 03-04 が GREEN 化 |
| `tests/features/test_category_map_consumer.py::test_apply_frozen_maps_drops_raw_id_columns` | test stub (RED) | HIGH #5 raw ID drop・Plan 03-04 が GREEN 化 |
| `tests/features/test_category_map_consumer.py::test_apply_frozen_maps_no_raw_id_in_output_schema` | test stub (RED) | HIGH #5 raw ID 残存検出・Plan 03-04 が GREEN 化 |
| `tests/features/test_builder.py::test_split_independence` | test stub (RED) | D-09・Plan 03-03 が GREEN 化 |
| `tests/features/test_builder.py::test_builder_output_columns_all_registered_in_registry` | test stub (RED) | HIGH #3 builder 連携・Plan 03-03 が GREEN 化 |
