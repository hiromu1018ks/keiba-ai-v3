---
phase: 03-as-of-features-snapshots
plan: 04
type: tdd
wave: 3
depends_on: ['03-03']
files_modified:
  - src/features/snapshot.py
  - src/features/category_map_consumer.py
  - scripts/run_feature_build.py
  - snapshots/.gitignore
autonomous: false
requirements: [FEAT-01, FEAT-02]
tags: [phase-03, features, tdd, green, snapshot, byte-reproducible, parquet, category-map, cli, human-verify, reviews-rev2]

must_haves:
  truths:
    - "src/features/snapshot.py::write_snapshot が PyArrow 決定論的書込オプション（use_dictionary=False / compression='zstd' / sorted input / write_statistics=True / row_group_size=100_000 rows (REVIEWS MEDIUM #10: row count not bytes)）で byte-reproducible な Parquet を生成する（SC#3・Pitfall 3.5）"
    - "Parquet schema metadata に §12.4 9項目（dataset_version / feature_snapshot_id / label_version / prediction_timing / feature_cutoff_rule / train_period / validation_period / created_at / feature_availability_version）が埋め込まれる（SC#3・§12.4）"
    - "同一 DataFrame から2回 write_snapshot を呼ぶと SHA256 hexstring が完全一致する（SC#3 byte-reproducibility・Pitfall 3.5・RESEARCH 実証済）"
    - "manifest YAML（snapshots/feature_matrix_<id>.manifest.yaml）が sha256 / byte_size / row_count / feature_count / label_version / feature_availability_version / prediction_timing / feature_cutoff_rule / train_period / validation_period / created_at / category_map_artifact を保持する"
    - "frozen category map が train 窓（2016H2-2023・D-09）でのみ fit され、val/test の未知 ID は __UNSEEN__ に map される（SC#4・Pitfall 3.4）"
    - "apply_category_map の戻り値 dtype が int32 で min ≥ 0（LightGBM §14.3 非負要件・pandas category code -1 ハザード回避）"
    - "category_map artefact が snapshots/category_map_<snapshot_id>.joblib に Parquet snapshot と同 snapshot_id で紐付け保存される"
    - "scripts/run_feature_build.py が readonly pool で feature matrix を構築し snapshot+manifest+category_map を生成する（masked DSN・raw_touched=False）"
    - "snapshots/*.parquet と snapshots/*.joblib が .gitignore 対象・manifest YAML のみ git 管理候補（D-08・セキュリティ）"
  artifacts:
    - path: "src/features/snapshot.py"
      provides: "write_snapshot(df, snapshot_id, parquet_path, *, label_version, fa_version, train_period, validation_period, created_at) -> str (sha256) 関数・PyArrow 決定論的書込・§12.4 metadata 埋込"
      contains: "def write_snapshot"
      min_lines: 120
    - path: "src/features/category_map_consumer.py"
      provides: "build_frozen_category_maps(feature_matrix, train_mask) -> dict[str, dict[str, int]] と apply_frozen_category_maps(feature_matrix, maps) -> pd.DataFrame 関数・train窓fit/__UNSEEN__/__MISSING__/非負int32"
      contains: "def build_frozen_category_maps"
      min_lines: 100
    - path: "scripts/run_feature_build.py"
      provides: "CLI entrypoint・readonly pool → build_feature_matrix → category_maps → write_snapshot → manifest YAML・2回書込で sha256 一致 verify"
      contains: "def main"
      min_lines: 130
    - path: "snapshots/.gitignore"
      provides: "*.parquet と *.joblib を git 管理外にする（manifest YAML は git 管理候補）"
      contains: "*.parquet"
      min_lines: 5
  key_links:
    - from: "src/features/snapshot.py"
      to: "pyarrow.parquet.write_table + pyarrow.Schema.with_metadata"
      via: "決定論的書込 + §12.4 9項目 schema metadata"
      pattern: "pq\\.write_table|schema\\.with_metadata"
    - from: "src/features/snapshot.py"
      to: "hashlib.sha256"
      via: "byte-reproducibility hash 計算・manifest YAML に保存"
      pattern: "hashlib\\.sha256"
    - from: "src/features/category_map_consumer.py"
      to: "src/utils/category_map.py"
      via: "fit_category_map（train窓）/ apply_category_map（val/test・__UNSEEN__）の消費"
      pattern: "from src\\.utils\\.category_map import fit_category_map, apply_category_map"
    - from: "scripts/run_feature_build.py"
      to: "src/features/builder.py::build_feature_matrix"
      via: "readonly pool で feature matrix 構築"
      pattern: "from src\\.features\\.builder import build_feature_matrix"
    - from: "scripts/run_feature_build.py"
      to: "src/features/snapshot.py::write_snapshot"
      via: "Parquet 書込 + sha256 verify"
      pattern: "from src\\.features\\.snapshot import write_snapshot"
---

<objective>
Plan 03-01 で RED で立てた残り2テストファイル（test_snapshot_repro.py / test_category_map_consumer.py）を GREEN にする。Phase 3 の最終成果物である「不変 versioned Parquet スナップショット」と「frozen category map」を実装:

1. **`src/features/snapshot.py`** — PyArrow 決定論的書込（`use_dictionary=False` / `compression='zstd'` / sorted input / `write_statistics=True` / `row_group_size=100_000 rows (REVIEWS MEDIUM #10: row count not bytes)`）で byte-reproducible な Parquet を生成。§12.4 9項目を `schema.with_metadata` で埋め込み、SHA256 を manifest YAML に保存。`created_at` は関数引数で固定（実タイムスタンプは manifest 側で別管理・Pitfall 3.5）。
2. **`src/features/category_map_consumer.py`** — `fit_category_map`（train窓のみ）と `apply_category_map`（val/test・`__UNSEEN__`/`__MISSING__`）を feature matrix 全体に適用。対象: jockey_id / trainer_id / sire_id / bms_id / horse_id（D-02 採用）。train窓 mask は D-09（2016H2-2023）。
3. **`scripts/run_feature_build.py`** — CLI entrypoint。readonly pool → `build_feature_matrix` → category_maps → `write_snapshot` → manifest YAML。2回書込で SHA256 一致を assert する byte-reproducibility 自己検証を実装。
4. **`snapshots/.gitignore`** — Parquet バイナリと joblib を git 管理外にする（D-08・セキュリティ）。

Purpose: SC#3（byte-reproducibility）と SC#4（frozen category map train-only fit）を実装で保証する。Phase 4 モデルが stamped Parquet のみから学習できる最終状態を届ける。実 DB からの feature matrix 構築を伴うため `autonomous: false` とし Task 3 で checkpoint:human-verify を挟む（Phase 2 Plan 02-03/02-04 の実DB停止パターン踏襲）。

Output:
- `src/features/snapshot.py`（write_snapshot + sha256 + §12.4 metadata）
- `src/features/category_map_consumer.py`（build_frozen_category_maps + apply_frozen_category_maps）
- `scripts/run_feature_build.py`（CLI・2回書込 sha256 verify・manifest YAML 書出）
- `snapshots/.gitignore`（Parquet/joblib 除外）
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@$HOME/.claude/gsd-core/references/tdd.md
@$HOME/.claude/gsd-core/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-as-of-features-snapshots/03-CONTEXT.md
@.planning/phases/03-as-of-features-snapshots/03-RESEARCH.md
@.planning/phases/03-as-of-features-snapshots/03-PATTERNS.md
@.planning/phases/03-as-of-features-snapshots/03-VALIDATION.md
@.planning/phases/03-as-of-features-snapshots/03-01-PLAN.md
@.planning/phases/03-as-of-features-snapshots/03-03-PLAN.md
@.planning/phases/02-fukusho-labels/02-04-PLAN.md
@src/utils/category_map.py
@src/features/builder.py
@src/db/connection.py
@src/config/settings.py
@scripts/run_label_etl.py
@scripts/run_label_reconcile.py
@tests/features/test_snapshot_repro.py
@tests/features/test_category_map_consumer.py
</context>

<tasks>

<task type="tdd" tdd_phase="green">
  <name>Task 1: src/features/snapshot.py（PyArrow 決定論的書込 + §12.4 metadata + SHA256・byte-reproducible）</name>
  <files>src/features/snapshot.py</files>

  <read_first>
    - .planning/phases/03-as-of-features-snapshots/03-RESEARCH.md Pattern 2 line 254-296（byte-reproducible Parquet 実装テンプレート・use_dictionary=False・compression='zstd'・row_group_size=100_000 rows (REVIEWS MEDIUM #10: row count not bytes)・schema.with_metadata）
    - .planning/phases/03-as-of-features-snapshots/03-RESEARCH.md Pitfall 3.5 line 366-372（非決定論的書込の4原因と対策・created_at 実タイムスタンプ問題）
    - .planning/phases/03-as-of-features-snapshots/03-PATTERNS.md「src/features/snapshot.py」節 line 244-326（実装テンプレート・manifest YAML 構造・.gitignore 対応）
    - src/utils/category_map.py（`MISSING` sentinel・本 task では直接使わないが参照）
    - tests/features/test_snapshot_repro.py（Plan 03-01 RED 契約・test_byte_reproducible_by_hash / test_metadata_contains_12_4_keys / test_created_at_is_deterministic）
    - docs/keiba_ai_requirements_v1.3.md §12.4（学習用データセット保存・8項目メタデータ）
    - docs/keiba_ai_requirements_v1.3.md §19.1（再現性・モデルバージョン・特徴量スナップショット）
  </read_first>

  <behavior>
    - Test 1 `test_byte_reproducible_by_hash`: 同一合成 DataFrame から2回 write_snapshot を呼び、SHA256 hexstring が完全一致する（Pitfall 3.5・SC#3）
    - Test 2 `test_metadata_contains_12_4_keys`: Parquet schema metadata に `dataset_version` / `feature_snapshot_id` / `label_version` / `prediction_timing` / `feature_cutoff_rule` / `train_period` / `validation_period` / `created_at` / `feature_availability_version` の9キーが含まれる（§12.4）
    - Test 3 `test_created_at_is_deterministic`: schema metadata の `created_at` は関数引数で固定（実タイムスタンプは manifest 側で別管理・再現性の要）
    - **Test 4 `test_sha256_covers_parquet_bytes_only` (REVIEWS HIGH #6)**: 同一 DataFrame + 同一 `created_at` 引数で2回 write_snapshot を呼ぶと sha256 が完全一致すること。更に manifest 側 `created_at_real` が異なっても Parquet sha256 は同一 `created_at` 引数なら一致することを検証（HIGH #6: SHA256 scope が Parquet bytes のみであることの証明・manifest bytes は SHA256 計算対象外）。
    - **Test 5 `test_manifest_created_at_varies_between_runs` (REVIEWS HIGH #6)**: 2回の run で manifest YAML の `created_at_real` フィールドが異なる値を持つこと（実タイムスタンプ・manifest は byte-reproducible でないことを明示）・manifest の `byte_reproducible_scope` フィールドが `"parquet_bytes_only"` であること。
    - **Test 6 `test_row_group_size_is_row_count_not_bytes` (REVIEWS MEDIUM #10)**: `inspect.getsource(write_snapshot)` の `row_group_size` 引数が `100_000`（整数行数）であり `128 * 1024 * 1024`（bytes 誤解釈）でないことを regression assert。
  </behavior>

  <action>
    **`src/features/snapshot.py`** — RESEARCH Pattern 2（line 254-296）と PATTERNS.md line 244-326 を統合した実装:

    - import: `from __future__ import annotations`, `import hashlib`, `from datetime import datetime`, `from pathlib import Path`, `import pandas as pd`, `import pyarrow as pa`, `import pyarrow.parquet as pq`
    - `_SNAPSHOT_SORT_KEYS = ("race_date", "jyocd", "racenum", "kettonum")` — canonical row order（Pitfall 3.5・RESEARCH line 273）
    - `_METADATA_KEYS = ("dataset_version", "feature_snapshot_id", "label_version", "prediction_timing", "feature_cutoff_rule", "train_period", "validation_period", "created_at", "feature_availability_version")` — §12.4 + feature_availability_version（9項目）
    - `def write_snapshot(df: pd.DataFrame, snapshot_id: str, parquet_path: str, *, label_version: str, fa_version: str, train_period: str, validation_period: str, created_at: datetime, dataset_version: str = "v1.0.0", prediction_timing: str = "1A", feature_cutoff_rule: str = "race_date - 1 day") -> str:`
      1. **空入力拒否**: `if df.empty: raise RuntimeError("write_snapshot: refusing to write empty DataFrame (silent data loss prevention)")`
      2. **決定論的 sort（Pitfall 3.5・PATTERNS line 273-275）**: `df_sorted = df.sort_values(list(_SNAPSHOT_SORT_KEYS)).reset_index(drop=True)` — canonical row order を固定
      3. **schema metadata 埋込（§12.4・PATTERNS line 277-288）**: `schema = pa.Schema.from_pandas(df_sorted, preserve_index=False)` → `schema = schema.with_metadata({b"dataset_version": dataset_version.encode(), b"feature_snapshot_id": snapshot_id.encode(), b"label_version": label_version.encode(), b"prediction_timing": prediction_timing.encode(), b"feature_cutoff_rule": feature_cutoff_rule.encode(), b"train_period": train_period.encode(), b"validation_period": validation_period.encode(), b"created_at": created_at.isoformat().encode(), b"feature_availability_version": fa_version.encode()})` — 9項目（§12.4 の8項目 + feature_availability_version）
      4. **Table 構築**: `table = pa.Table.from_pandas(df_sorted, schema=schema, preserve_index=False)`
      5. **決定論的書込（Pitfall 3.5・RESEARCH line 283-290）**: `buf = pa.BufferOutputStream()` → `pq.write_table(table, buf, use_dictionary=False, compression="zstd", write_statistics=True, row_group_size=100_000  # rows, not bytes (REVIEWS MEDIUM #10))` — `use_dictionary=False` が必須（True だと辞書構築順で非決定論的になる可能性）
      6. **SHA256 計算（REVIEWS HIGH #6: SHA256 scope は Parquet bytes のみ）**: `data = buf.getvalue().to_pybytes()` / `sha256 = hashlib.sha256(data).hexdigest()`。**SHA256 scope の明示:** hash 対象は PyArrow `pq.write_table` が生成した Parquet バイト列（`buf.getvalue().to_pybytes()`）**のみ**。manifest YAML bytes / schema metadata に渡す `created_at` 引数（固定文字列）/ 実行時 manifest `created_at_real`（実タイムスタンプ）は SHA256 計算対象外。これにより同一 DataFrame + 同一 `created_at` 引数なら常に同一 sha256 が再現される（HIGH #6・Pitfall 3.5）。manifest 側の `created_at_real` は run 毎に変わり得るが、Parquet sha256 は不変・これが byte-reproducibility の契約。
      7. **ファイル書込**: `Path(parquet_path).parent.mkdir(parents=True, exist_ok=True)` → `with open(parquet_path, "wb") as f: f.write(data)` — `snapshots/` ディレクトリが未存在なら作成
      8. 戻り値: `sha256` hexstring（manifest YAML 側で保存・sha256 は Parquet bytes のみ由来）
    - `def write_manifest(manifest_path: str, *, snapshot_id: str, parquet_path: str, sha256: str, byte_size: int, row_count: int, feature_count: int, label_version: str, fa_version: str, prediction_timing: str, feature_cutoff_rule: str, train_period: str, validation_period: str, created_at_real: datetime, category_map_artifact: str | None = None) -> None:`
      - **REVIEWS HIGH #6 契約の明文化:** manifest YAML は「Parquet byte hash（sha256・不変）」と「run 時刻（created_at_real・可変）」を分離:
        - `sha256` フィールドは `write_snapshot` の戻り値（Parquet bytes のみ由来・byte-reproducible）
        - `created_at_real` フィールドは実タイムスタンプ（`datetime.now()`・run 毎に可変・byte-reproducible 対象外）
        - `byte_reproducible_scope: "parquet_bytes_only"` フィールドを追加し、SHA256 scope を文書化（監査可能性）
      - `yaml.safe_dump` で manifest YAML を書出（label_spec.yaml / class_normalization.yaml の YAML dump パターン踏襲）・`sort_keys=True` で key 順序を固定（manifest YAML 自体も deterministic・ただし created_at_real は可変）
    - **禁止事項（実装上明示）**: `use_dictionary=True`（非決定論的リスク）・`compression` を `None`/`snappy` にする（安定性異なる）・schema metadata に実タイムスタンプ `datetime.now()` を直接入れる（非決定論的・Pitfall 3.5）・`row_group_size` を指定しない（DuckDB zero-copy 読込効率が落ちる）
  </action>

  <verify>
    <automated>KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/features/test_snapshot_repro.py -q</automated>
  </verify>

  <acceptance_criteria>
    - src/features/snapshot.py に `def write_snapshot(df, snapshot_id, parquet_path, *, label_version, fa_version, train_period, validation_period, created_at, ...)` と `def write_manifest(...)` が存在する
    - `inspect.getsource(write_snapshot)` に `use_dictionary=False` / `compression="zstd"` / `write_statistics=True` / `row_group_size=100_000`（行数・bytes でない・REVIEWS MEDIUM #10）が全て含まれる（Pitfall 3.5・RESEARCH Pattern 2）
    - **REVIEWS HIGH #6:** `inspect.getsource(write_snapshot)` の SHA256 計算が `hashlib.sha256(data).hexdigest()`（data = Parquet bytes）のみを対象とし、manifest bytes / 実行時 `created_at_real` を含まないこと・docstring または comment で SHA256 scope が "parquet_bytes_only" と明記されていること
    - `inspect.getsource(write_snapshot)` に `hashlib.sha256(data).hexdigest()` が含まれる
    - `inspect.getsource(write_snapshot)` に `df.sort_values(list(_SNAPSHOT_SORT_KEYS))` が含まれる（canonical row order・Pitfall 3.5）
    - schema metadata に9キー（dataset_version / feature_snapshot_id / label_version / prediction_timing / feature_cutoff_rule / train_period / validation_period / created_at / feature_availability_version）が `with_metadata` で埋め込まれる
    - `created_at` が関数引数（実タイムスタンプでない）・`datetime.now()` を schema metadata に直接入れない（Pitfall 3.5）
    - 空入力 DataFrame で `RuntimeError` を raise（CR-04(a) silent data loss 防止）
    - tests/features/test_snapshot_repro.py の全3テストが GREEN（同一 DataFrame → 同一 SHA256）
  </acceptance_criteria>

  <done>
    write_snapshot が GREEN。PyArrow 決定論的書込 + §12.4 9項目 metadata + SHA256 byte-reproducibility が unit test で証明される。SC#3 達成。
  </done>
</task>

<task type="tdd" tdd_phase="green">
  <name>Task 2: src/features/category_map_consumer.py（frozen category map train窓fit + __UNSEEN__ + 非負int32）</name>
  <files>src/features/category_map_consumer.py</files>

  <read_first>
    - src/utils/category_map.py（`fit_category_map` line 24-50・`apply_category_map` line 53-77・`UNSEEN`/`MISSING` sentinel line 20-21・非負 int32 保証）
    - .planning/phases/03-as-of-features-snapshots/03-RESEARCH.md Pitfall 3.4 line 359-364（horse_id category map の test 構成リーク・train窓 42395 distinct horses・全期間 60496）
    - .planning/phases/03-as-of-features-snapshots/03-PATTERNS.md「src/features/category_map_consumer.py」節 line 330-356（消費イディオム・train窓 mask・joblib 永続化）
    - .planning/phases/03-as-of-features-snapshots/03-CONTEXT.md decisions D-02（horse_id feature 化採用）・D-09（train 2016H2-2023 窓で fit）
    - docs/keiba_ai_requirements_v1.3.md §14.3（LightGBM native categorical・負値コード回避・__UNSEEN__ sentinel）・§14.4（CatBoost has_time）・§14.5（欠損理由区別）
    - tests/features/test_category_map_consumer.py（Plan 03-01 RED 契約・test_unseen_maps_to_sentinel / test_fit_called_on_train_window_only / test_codes_are_non_negative_int32）
  </read_first>

  <behavior>
    - Test 1 `test_unseen_maps_to_sentinel`: train 窓に無い horse_id を val/test に適用した場合、`apply_category_map` の結果が `code["__UNSEEN__"]` になる（SC#4・Pitfall 3.4）
    - Test 2 `test_fit_called_on_train_window_only`: `inspect.getsource(category_map_consumer)` で train 窓 mask（`race_date` で '2016-07-01'〜'2023-12-31' filter）が存在する（Pitfall 3.4・D-09 regression）
    - Test 3 `test_codes_are_non_negative_int32`: `apply_category_map` の戻り値 dtype が `int32` で min ≥ 0（LightGBM §14.3 非負要件・pandas category code -1 ハザード回避）
    - Test 4 (副): `test_horse_id_unseen_rate_within_theoretical_bound` — train窓外の 18101 馬（60496 - 42395）が `__UNSEEN__` になる理論上上限を unit test で検証（合成データで再現）
    - Test 5 (副): `test_missing_and_unseen_have_distinct_codes` — `__MISSING__` と `__UNSEEN__` が異なる int32 コードを持つ（§14.5 欠損理由区別）
    - **Test 6 `test_apply_frozen_maps_drops_raw_id_columns` (REVIEWS HIGH #5)**: `apply_frozen_category_maps` 適用後の feature matrix に `_CATEGORY_COLUMNS`（`jockey_id` / `trainer_id` / `sire_id` / `bms_id` / `horse_id`）の生 ID 文字列列が残っていないこと（`<col>_code` 列のみ残る）を assert。合成 feature_matrix に5カラムの文字列列 + `<col>_code` int32 列を渡して apply 後、`assert set(_CATEGORY_COLUMNS).isdisjoint(result.columns)`（5カラム全て不在）・`assert all(f"{c}_code" in result.columns for c in _CATEGORY_COLUMNS)`。
    - **Test 7 `test_apply_frozen_maps_no_raw_id_in_output_schema` (REVIEWS HIGH #5)**: `_CATEGORY_COLUMNS` に列挙された5カラムのうち1つでも非 `_code` 形式で apply 後 DataFrame に残っていれば fail（Phase 4 モデルが raw ID で refit するのを構造的に防止）。
  </behavior>

  <action>
    **`src/features/category_map_consumer.py`** — PATTERNS.md line 330-356 を統合した実装:

    - import: `from __future__ import annotations`, `from pathlib import Path`, `import joblib`, `import pandas as pd`, `from src.utils.category_map import fit_category_map, apply_category_map, UNSEEN, MISSING`
    - `_CATEGORY_COLUMNS = ("jockey_id", "trainer_id", "sire_id", "bms_id", "horse_id")` — frozen category map 対象の高基数 ID 列（D-02 で horse_id も feature 化採用）
    - `_TRAIN_WINDOW = ("2016-07-01", "2023-12-31")` — D-09 train 窓（frozen map fit 範囲）
    - **列名 rename 契約（builder ↔ category_map_consumer間・W-2）:** 03-03 builder の `_OBS_SELECT_COLUMNS` は EveryDB2 実カラム名（`kisyucode` / `chokyosicode` / `ketto3infohansyokunum1` / `ketto3infohansyokunum2` / `kettonum`）で SELECT するが、03-04 の `_CATEGORY_COLUMNS` は抽象名（`jockey_id` / `trainer_id` / `sire_id` / `bms_id` / `horse_id`）で消費する。**この rename を明文化:** builder（Plan 03-03 Task 3）が `_OBS_SELECT_COLUMNS` で SELECT した直後・feature matrix を `build_frozen_category_maps` / `apply_frozen_category_maps` に渡す前に、必ず `feature_matrix = feature_matrix.rename(columns={"kisyucode": "jockey_id", "chokyosicode": "trainer_id", "ketto3infohansyokunum1": "sire_id", "ketto3infohansyokunum2": "bms_id", "kettonum": "horse_id"})` を実行する。**rename は value 用カラムのみ**（canonical feature-row key `(year, jyocd, kaiji, nichiji, racenum, kettonum)` は壊さない・`kettonum` は key としては元の名前で残し、`horse_id` は category map 用の alias 列として追加生成する実装でも可）。本契約は `apply_category_map` の入力契約（`src/utils/category_map.py` 参照）に合致 — `apply_category_map(series, code)` は `series.astype(str)` を呼び value のみ参照し series.name に依存しないため、value が str 化可能なら動作するが、**`build_frozen_category_maps` / `apply_frozen_category_maps` は `feature_matrix[col]`（col ∈ `_CATEGORY_COLUMNS` 抽象名）で列参照するため rename が必須**（推測でなく `src/utils/category_map.py` line 73-76 実契約確認済）。
    - `def build_frozen_category_maps(feature_matrix: pd.DataFrame, *, train_window: tuple[str, str] = _TRAIN_WINDOW) -> dict[str, dict[str, int]]:`
      1. **train窓 mask 計算（D-09・Pitfall 3.4 回避）**: `train_mask = feature_matrix["race_date"].between(train_window[0], train_window[1])` — 絶対に val/test 行を含めない
      2. 各 `_CATEGORY_COLUMNS` について `train_series = feature_matrix.loc[train_mask, col].astype(str)` で訓練窓 series を抽出
      3. `maps[col] = fit_category_map(train_series)` を呼出（src/utils/category_map.py・訓練窓のみ fit・`__UNSEEN__`/`__MISSING__` sentinel 含む）
      4. 戻り値: `dict[str, dict[str, int]]` — 各カラムの frozen category map
    - `def apply_frozen_category_maps(feature_matrix: pd.DataFrame, maps: dict[str, dict[str, int]]) -> pd.DataFrame:`
      1. 各 `_CATEGORY_COLUMNS` について `feature_matrix[f"{col}_code"] = apply_category_map(feature_matrix[col].astype(str), maps[col])` を呼出（**再 fit 禁止**・`apply_category_map` のみ）
      2. train 窓に無い ID は `apply_category_map` 内で `code[UNSEEN]` にフォールバック（src/utils/category_map.py:76・`.fillna(code[UNSEEN])`）
      3. **REVIEWS HIGH #5: raw ID 列を drop** — coding 完了後、`feature_matrix = feature_matrix.drop(columns=list(_CATEGORY_COLUMNS))` で生 ID 文字列列を破棄（`_code` suffix 列のみ残す）。これにより Phase 4 モデルが raw ID で train/refit する経路を構造的に遮断（test_completeness: `assert set(_CATEGORY_COLUMNS).isdisjoint(result.columns)` で5カラム全てが存在しないことを機械的に保証）。**例外:** `horse_id`（kettonum alias）は canonical key 用途が終われば不要・builder の canonical key は `kettonum`（元の EveryDB2 カラム名）なので `horse_id` alias は drop 可能。実装上は `_CATEGORY_COLUMNS` 全て drop し、canonical key は `kettonum` のみ残す。
      4. 戻り値: 元 feature_matrix から `_CATEGORY_COLUMNS` を drop し `<col>_code` 列（int32・非負）を追加した DataFrame
    - `def persist_category_maps(maps: dict[str, dict[str, int]], artifact_path: str) -> None:`
      - `joblib.dump(maps, artifact_path)` — Parquet snapshot と同 snapshot_id で紐付け（例: `snapshots/category_map_<snapshot_id>.joblib`）
    - `def load_category_maps(artifact_path: str) -> dict[str, dict[str, int]]:`
      - `return joblib.load(artifact_path)` — Phase 4 モデル学習時に読込
    - **絶対禁止（実装上明示）**: `fit_category_map` に val/test series を渡す（test 構成リーク・Pitfall 3.4）・`apply_category_map` を呼ばずに自前で map 構築（非負 int32 保証が崩れる）・`__MISSING__` と `__UNSEEN__` を同コードにする（§14.5 欠損理由区別違反）
  </action>

  <verify>
    <automated>KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/features/test_category_map_consumer.py -q</automated>
  </verify>

  <acceptance_criteria>
    - src/features/category_map_consumer.py に `def build_frozen_category_maps(feature_matrix, *, train_window=("2016-07-01", "2023-12-31"))` と `def apply_frozen_category_maps(feature_matrix, maps)` と `def persist_category_maps(maps, artifact_path)` と `def load_category_maps(artifact_path)` が存在する
    - `_CATEGORY_COLUMNS` に `("jockey_id", "trainer_id", "sire_id", "bms_id", "horse_id")` が定義される（D-02）
    - `_TRAIN_WINDOW = ("2016-07-01", "2023-12-31")` が定数として存在（D-09）
    - `inspect.getsource(build_frozen_category_maps)` に `feature_matrix["race_date"].between(train_window[0], train_window[1])` の train窓 mask が含まれる（Pitfall 3.4 regression）
    - `inspect.getsource(build_frozen_category_maps)` に `fit_category_map(train_series)` が含まれ、`fit_category_map` に val/test series を直接渡さない
    - `inspect.getsource(apply_frozen_category_maps)` に `apply_category_map(` が含まれ、fit_category_map を再呼出しない（再 fit 禁止）
    - **REVIEWS HIGH #5:** `inspect.getsource(apply_frozen_category_maps)` に `.drop(columns=list(_CATEGORY_COLUMNS))` または同等の raw ID drop 処理が含まれる（raw ID 列が snapshot に残らない・Phase 4 が raw ID で refit するのを構造的に防止）
    - tests/features/test_category_map_consumer.py の全テストが GREEN（test_unseen_maps_to_sentinel / test_fit_called_on_train_window_only / test_codes_are_non_negative_int32 / test_horse_id_unseen_rate_within_theoretical_bound / test_missing_and_unseen_have_distinct_codes）
    - 合成 DataFrame で train窓外の horse_id が `code["__UNSEEN__"]` になることが検証される
  </acceptance_criteria>

  <done>
    category_map_consumer が GREEN。train窓fit / __UNSEEN__ / __MISSING__ / 非負 int32 / 欠損理由区別（§14.5）が unit test で証明される。SC#4 達成。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Task 1 と Task 2 で実装した snapshot/category_map_consumer を統合した CLI `scripts/run_feature_build.py` を実行し、実 DB から feature matrix を構築して不変 Parquet スナップショット + manifest + category_map を生成する。Phase 2 Plan 02-04（払戻テーブル突合ゲート）と Plan 02-03 Task 3（label ETL 実DB実行）の「実DB停止」パターンを踏襲: unit test が GREEN でも実データでのみ発覚する問題（NULL 残存・rowcount 不一致・SHA256 非再現・category map cardinality 異常）を人手で確認する。
  </what-built>

  <how-to-verify>
    1. CLI script `scripts/run_feature_build.py` を実装（masked DSN ログ・readonly pool → `build_feature_matrix` → `build_frozen_category_maps` → `apply_frozen_category_maps` → `write_snapshot` → `write_manifest` → `persist_category_maps`・2回書込で SHA256 一致 assert・run_label_etl.py:46-127 と同一構造・sys.path 調整含む）。
    2. `snapshots/.gitignore` を作成（`*.parquet` と `*.joblib` を git 管理外・manifest `*.yaml` は git 管理候補）。
    3. `uv run python scripts/run_feature_build.py --snapshot-id 20260618-1a-v1 --label-version v1.0.0 --fa-version 0.2.0` を実行。
    4. ログ出力を確認:
       - `readonly DSN: ...***...` が表示（masked・生 DSN は絶対に出力しない）
       - `feature matrix: rows=N features=M` で N が期待値（約 554267件・全期間）・M が約 80-100 feature 列
       - `category maps: jockey=K1 trainers=K2 sires=K3 bms=K4 horses=K5` で K5 が約 42395（train窓 distinct horses・Pitfall 3.4 実証）
       - `snapshot write #1: sha256=...` と `snapshot write #2 (byte-repro verify): sha256=...` で同一 hexstring
       - `byte-reproducibility verify: PASS` が表示される（SC#3）
       - `raw_touched=False` が表示される（D-06）
    5. `ls -la snapshots/` で `feature_matrix_20260618-1a-v1.parquet` / `feature_matrix_20260618-1a-v1.manifest.yaml` / `category_map_20260618-1a-v1.joblib` の3ファイルが生成されていることを確認。
    6. manifest YAML を目視:
       - `sha256:` / `byte_size:` / `row_count:` / `feature_count:` / `label_version: v1.0.0` / `feature_availability_version: 0.2.0` / `prediction_timing: 1A` / `feature_cutoff_rule: race_date - 1 day` / `train_period: 2016-07-01/2023-12-31` / `validation_period: 2024-01-01/2024-12-31` / `created_at:` / `category_map_artifact: snapshots/category_map_20260618-1a-v1.joblib` が含まれる
    7. DuckDB で Parquet 読込確認: `uv run python -c "import duckdb; r = duckdb.sql(\"SELECT count(*), count(distinct (race_nkey, kettonum)) FROM read_parquet('snapshots/feature_matrix_20260618-1a-v1.parquet')\").fetchone(); print(r)"` で total rows と distinct (race_nkey, kettonum) が一致（canonical key 一意性・Pitfall）。
    8. DuckDB で metadata 読込確認: `uv run python -c "import pyarrow.parquet as pq; s = pq.read_schema('snapshots/feature_matrix_20260618-1a-v1.parquet'); print(s.metadata)"` で §12.4 9項目が埋め込まれていることを確認。
    9. SC#2 fail-loud test を改めて実行: `uv run pytest tests/features/test_allowlist.py -q` が GREEN（実 feature matrix で禁止 timing feature が0件）。
    10. **OOM escape hatch（W-5）:** CLI 実行（554267行 × 80-100 feature 列・in-memory pandas frame が概算 1.5-2GB）で MemoryError / OOM kill が発生した場合は、chunk-based write（`feature_matrix.groupby("race_date").apply(lambda g: write_snapshot(g, ...))` 等で row_group 単位に分割して `pq.ParquetWriter` に追記）または DuckDB `postgres_scanner` 経由の direct Parquet materialize（`duckdb.sql("INSTALL postgres_scanner; LOAD postgres_scanner; COPY (SELECT ... FROM postgres_db.normalized.n_uma_race) TO 'snapshots/feature_matrix_<id>.parquet'")`・PyArrow 経由を迂回）に切り替え。byte-reproducibility の sha256 assert は chunk write 場合は row order を sort してから chunk 化することで保持（DuckDB direct materialize 場合は `ORDER BY race_date, jyocd, racenum, kettonum` 句で固定）。escape hatch 適用時は SUMMARY.md にその旨と理由（OOM 発生ログ）を記録する。
  </how-to-verify>

  <resume-signal>Type "approved" if all checks pass (3 files generated, sha256 reproducible, metadata embedded, SC#2 green), or describe the failure (sha256 mismatch, NULL rows, rowcount wrong, metadata missing, category cardinality off, raw mutated, etc.)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| feature matrix → PyArrow write | `write_snapshot` が sort + schema metadata + 決定論的書込で byte-reproducible Parquet を生成（Pitfall 3.5） |
| Parquet file → filesystem | `snapshots/*.parquet` は immutable・`.gitignore` で git 管理外（D-08） |
| train window → category map fit | `build_frozen_category_maps` が train窓（D-09）mask で fit・val/test は apply のみ（Pitfall 3.4） |
| filesystem → Phase 4 model | Phase 4 は stamped Parquet + category_map.joblib のみから学習・生 DB に触れない |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-18 | Tampering (reproducibility) | 同一 DataFrame から異なる SHA256 が生成される | mitigate | PyArrow 決定論的書込（use_dictionary=False / zstd / sorted input）・2回書込 sha256 assert・test_byte_reproducible_by_hash (SC#3・Pitfall 3.5) |
| T-03-R5B | Information disclosure (test-composition leak via raw IDs) | feature matrix snapshot に raw ID 文字列列（jockey_id / trainer_id / sire_id / bms_id / horse_id）が _code 列と併存し Phase 4 モデルが raw ID で refit して test 構成がリーク（REVIEWS HIGH #5） | mitigate | `apply_frozen_category_maps` が coding 完了後に `.drop(columns=list(_CATEGORY_COLUMNS))` で raw ID 列を破棄・`_code` suffix 列のみ残す・test_apply_frozen_maps_drops_raw_id_columns / test_apply_frozen_maps_no_raw_id_in_output_schema で5カラム全て不在を機械的検証・Phase 4 は stamped Parquet のみ参照し生 DB に触れない |
| T-03-R6B | Tampering (false reproducibility claim) | SHA256 hash が manifest bytes や実行時 created_at を含んでおり byte-reproducible でないのに再現性を主張（REVIEWS HIGH #6） | mitigate | SHA256 scope を Parquet bytes のみに明示的限定・`write_snapshot` の SHA256 計算は `buf.getvalue().to_pybytes()`（Parquet bytes）のみ・manifest 側 `created_at_real` は SHA256 計算対象外・manifest に `byte_reproducible_scope: "parquet_bytes_only"` フィールドで監査可能化・test_sha256_covers_parquet_bytes_only / test_manifest_created_at_varies_between_runs（HIGH #6） |
| T-03-19 | Tampering (reproducibility) | schema metadata に実タイムスタンプ `datetime.now()` が入る | mitigate | `created_at` は関数引数で固定・実タイムスタンプは manifest 側 `created_at_real` で別管理・test_created_at_is_deterministic |
| T-03-20 | Information disclosure (test-set composition leak) | category map に val/test の horse_id が漏れて test 構成がリーク | mitigate | `build_frozen_category_maps` は train窓 mask（2016H2-2023）でのみ fit・test_fit_called_on_train_window_only inspect・test_unseen_maps_to_sentinel (SC#4・Pitfall 3.4) |
| T-03-21 | Tampering (LightGBM NaN→-1 hazard) | category code に -1 が現れて LightGBM が missing と誤認 | mitigate | `apply_category_map` は `.astype("int32")` で非負保証・test_codes_are_non_negative_int32・src/utils/category_map.py 既存契約 |
| T-03-22 | Tampering (sentinel conflation) | `__MISSING__` と `__UNSEEN__` が同コードになり欠損理由が区別不能 | mitigate | `fit_category_map` が両 sentinel を別コードで追加・test_missing_and_unseen_have_distinct_codes (§14.5) |
| T-03-23 | Information disclosure (Parquet binary in git) | feature matrix Parquet が git に commit されて repo が膨張・再現性 story 破壊 | mitigate | `snapshots/.gitignore` で `*.parquet` と `*.joblib` を除外・manifest YAML のみ git 管理候補 (D-08・§19.1) |
| T-03-24 | Tampering (empty snapshot) | 空 DataFrame が write されて silent data loss | mitigate | `write_snapshot` が空入力で `RuntimeError` (CR-04(a)・PATTERNS silent fallback 禁止) |
| T-03-25 | Information disclosure (DSN leak) | CLI ログに生 DSN が出る | mitigate | `scripts/run_feature_build.py` は `settings.dsn_masked` のみログ出力・生 DSN は絶対に出さない (MEDIUM #1 / HIGH #6・run_label_etl.py パターン) |
| T-03-26 | Tampering (raw mutation via CLI) | CLI が誤って raw に書込む | mitigate | CLI は readonly pool のみ使用・`raw_touched=False` を戻り値で確認・`compute_raw_fingerprint` before/after は CLI でも assert 可能（run_label_etl.py 同様） |
</threat_model>

<verification>
- `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/features/test_snapshot_repro.py tests/features/test_category_map_consumer.py -q` 全て GREEN
- `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/features/ -q` 全8ファイル GREEN（Phase 3 完了状態）
- `uv run python scripts/run_feature_build.py --snapshot-id 20260618-1a-v1 --label-version v1.0.0 --fa-version 0.2.0` が exit 0・sha256 再現 PASS・3ファイル生成（人手確認）
- `cat snapshots/feature_matrix_20260618-1a-v1.manifest.yaml` で §12.4 9項目 + sha256 + category_map_artifact が含まれる（人手確認）
</verification>

<success_criteria>
- write_snapshot が PyArrow 決定論的書込で byte-reproducible Parquet を生成（同一 SHA256）
- §12.4 9項目 metadata が schema に埋め込まれる
- frozen category map が train窓（2016H2-2023）でのみ fit され、val/test の未知 ID は `__UNSEEN__`
- 非負 int32 / `__MISSING__` と `__UNSEEN__` のコード区別（§14.5）
- CLI が readonly pool で snapshot + manifest + category_map を生成
- snapshots/*.parquet と *.joblib が .gitignore 対象
- SC#3 と SC#4 達成・Phase 3 全 SC（#1-#4）GREEN
</success_criteria>

<output>
Create `.planning/phases/03-as-of-features-snapshots/03-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol / Path | Kind | Purpose |
|---------------|------|---------|
| `src/features/snapshot.py::write_snapshot` | function | PyArrow 決定論的書込 + §12.4 9項目 metadata + SHA256 byte-reproducible Parquet 生成（SHA256 scope = Parquet bytes のみ・REVIEWS HIGH #6） |
| `src/features/snapshot.py::write_manifest` | function | manifest YAML 書出（sha256 / 9項目 / category_map_artifact） |
| `src/features/snapshot.py::_SNAPSHOT_SORT_KEYS` | tuple constant | `("race_date", "jyocd", "racenum", "kettonum")` canonical row order |
| `src/features/snapshot.py::_METADATA_KEYS` | tuple constant | §12.4 + feature_availability_version の9キー |
| `src/features/category_map_consumer.py::build_frozen_category_maps` | function | train窓（D-09）mask で fit_category_map を呼び 5カラムの frozen map 構築 |
| `src/features/category_map_consumer.py::apply_frozen_category_maps` | function | apply_category_map で全行に map 適用後、raw ID 列を drop（__UNSEEN__ / 非負 int32 / REVIEWS HIGH #5 raw ID drop） |
| `src/features/category_map_consumer.py::persist_category_maps` | function | joblib.dump で snapshots/category_map_<id>.joblib に保存 |
| `src/features/category_map_consumer.py::load_category_maps` | function | Phase 4 用 joblib.load |
| `src/features/category_map_consumer.py::_CATEGORY_COLUMNS` | tuple constant | `("jockey_id", "trainer_id", "sire_id", "bms_id", "horse_id")`（D-02 含む） |
| `src/features/category_map_consumer.py::_TRAIN_WINDOW` | tuple constant | `("2016-07-01", "2023-12-31")`（D-09） |
| `scripts/run_feature_build.py::main` | CLI entrypoint | readonly pool → build_feature_matrix → category_maps → write_snapshot → manifest・2回書込 sha256 verify・masked DSN |
| `snapshots/.gitignore` | config file | `*.parquet` と `*.joblib` を git 管理外（D-08） |
| `snapshots/feature_matrix_<snapshot_id>.parquet` | data artifact | 不変 versioned Parquet・Phase 4 学習元（CLI 実行で生成・git 管理外） |
| `snapshots/feature_matrix_<snapshot_id>.manifest.yaml` | data artifact | sha256 + §12.4 metadata（git 管理候補） |
| `snapshots/category_map_<snapshot_id>.joblib` | data artifact | frozen category map（git 管理外） |
