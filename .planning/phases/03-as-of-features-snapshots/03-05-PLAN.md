---
phase: 03-as-of-features-snapshots
plan: 05
type: execute
wave: 4
depends_on: ['03-03', '03-04']
gap_closure: true
files_modified:
  - src/config/feature_availability.yaml
  - src/features/rolling.py
  - src/features/availability.py
  - src/features/builder.py
  - src/features/category_map_consumer.py
  - scripts/run_feature_build.py
  - tests/features/test_rolling.py
  - tests/features/test_builder.py
  - tests/features/test_category_map_consumer.py
  - .planning/phases/03-as-of-features-snapshots/03-CONTEXT.md
autonomous: true
requirements: [FEAT-01, FEAT-02]
tags: [phase-03, features, gap-closure, cr-01, cr-02, cr-03, cr-04, wr-01, pit-correctness, registry-parity, fail-loud, security, pickle-ace]

must_haves:
  truths:
    - "no registered feature column is 100% NaN (end-to-end builder path test green) — registry↔Parquet parity restored"
    - "manifest feature_count == populated feature column count (registry に登録された feature 列と実 Parquet 出力が silent 乖離しない)"
    - "feature_availability.yaml に rolling_babacd_{mean,latest,sd}_5 / rolling_timediff_{mean,latest,sd}_5 の6エントリが存在しない（CR-01 DELETE）"
    - "rolling.py の _ROLLING_SYSTEMS / _SYSTEM_SOURCE から timediff / babacd 系統が除外され、availability.py の _ROLLING_SYSTEMS_FOR_RESERVED と一致する（registry↔rolling↔reserved 三者 parity）"
    - "合成 DB-mock を通じた end-to-end builder path test が、登録済み feature 列のいずれも 100% NaN にならないことを assert する（CR-01 regression guard・現状 test_rolling.py は合成 dict fixture のみで end-to-end を検査しない hole を閉鎖）"
    - "builder.estimated_running_style が rolling と同一の PIT pre-filter（history.as_of_datetime < obs.feature_cutoff_datetime・per-observation）を groupby(kettonum) の前に適用する（WR-01 look-ahead leak 解消）"
    - "_fetch_feature_sources / _fetch_history の JOIN 右側 nr (normalized.n_race) に project_window_filter('nr') が適用される（CR-02・CR-06 契約・label_race_date_backfill.py と対称）"
    - "build_frozen_category_maps が race_date 欠損 frame で fail-loud（ValueError）し、silent all-train fallback しない（CR-03・D-13）"
    - "load_category_maps が joblib.load (pickle) を呼ばず、JSON 安全フォーマットで読み込む（CR-04・pickle ACE vector 解消）"
    - "category_map artifact の拡張子が .joblib から .json に移行し、manifest の category_map_artifact 参照も .json に更新される"
    - "rolling_timediff_* / rolling_babacd_* の6 feature は Deferred note にて再登録予約付きで削除され、Phase 2 ETL 拡張（別 phase・/gsd-phase --insert）で timediff/babacd source カラムが normalized 層に追加された後に再登録される（本 plan では Phase 2 ETL 拡張は実装しない）"
  artifacts:
    - path: "src/config/feature_availability.yaml"
      provides: "rolling_timediff_* / rolling_babacd_* 計6エントリ削除・registry↔実データ parity"
      contains: "rolling_kakuteijyuni_mean_5"
    - path: "src/features/rolling.py"
      provides: "_ROLLING_SYSTEMS / _SYSTEM_SOURCE から timediff / babacd を削除（6系統×3軸=18 rolling features）"
      contains: "_ROLLING_SYSTEMS"
    - path: "src/features/availability.py"
      provides: "_ROLLING_SYSTEMS_FOR_RESERVED を rolling.py と一致（6系統・parity）"
      contains: "_ROLLING_SYSTEMS_FOR_RESERVED"
    - path: "src/features/builder.py"
      provides: "WR-01 PIT pre-filter for estimated_running_style・CR-02 project_window_filter('nr') on JOIN right side"
      contains: "project_window_filter"
    - path: "src/features/category_map_consumer.py"
      provides: "CR-03 fail-loud race_date missing・CR-04 JSON load/persist（joblib 廃止）"
      contains: "def load_category_maps"
    - path: "scripts/run_feature_build.py"
      provides: "category_map artifact 拡張子 .joblib → .json 移行"
      contains: "category_map"
    - path: "tests/features/test_builder.py"
      provides: "CR-01 regression guard: 合成 DB-mock から build_feature_matrix 相当 path を通し、登録 feature 列が 100% NaN にならないことを assert"
      contains: "test_no_registered_feature_column_all_nan_end_to_end"
    - path: "tests/features/test_category_map_consumer.py"
      provides: "CR-03 fail-loud test・CR-04 JSON round-trip test"
      contains: "test_build_frozen_maps_raises_on_missing_race_date"
  key_links:
    - from: "src/features/rolling.py::_ROLLING_SYSTEMS"
      to: "src/config/feature_availability.yaml::features"
      via: "rolling 系統定義と registry feature エントリの 1:1 対応（parity）"
      pattern: "rolling_(timediff|babacd)"
    - from: "src/features/builder.py::estimated_running_style"
      to: "src/features/rolling.py (PIT pre-filter idiom)"
      via: "history.as_of_datetime < obs.feature_cutoff_datetime を per-observation で適用"
      pattern: "as_of_datetime.*feature_cutoff_datetime"
    - from: "src/features/builder.py::_fetch_feature_sources"
      to: "src/etl/label_race_date_backfill.py (CR-06 contract)"
      via: "project_window_filter('nr') を JOIN 右側に適用"
      pattern: "project_window_filter\\('nr'\\)"
    - from: "src/features/category_map_consumer.py::load_category_maps"
      to: "json.load (not joblib.load)"
      via: "JSON 安全フォーマットで category map を読込（pickle ACE 解消）"
      pattern: "json\\.load"
---

<objective>
Phase 3 verification が `gaps_found`（must-have #1 PARTIAL・2.5/3）を返した原因を閉鎖する。03-VERIFICATION.md が挙げた単一の実質 gap（CR-01 silent empty features）と、03-REVIEW.md が記録した4つの潜伏負債（WR-01 estimated_running_style look-ahead・CR-02 unfiltered JOIN・CR-03 silent all-train fallback・CR-04 pickle ACE）を、operator の BINDING decision に従って修正する。

本 plan は **ADD-only**（03-01..03-04 PLAN.md は変更しない）。修正は既存ソースへの surgical edit のみ。

目的別内訳:
1. **CR-01（registry↔実体 parity）**: rolling_timediff_* / rolling_babacd_* の6 registered feature column を registry・rolling 実装・availability reserved の三者から削除し、Phase 2 ETL 拡張（別 phase）で source カラムが normalized 層に揃った後に再登録する旨を Deferred note で明記。これにより manifest `feature_count` と実 populated feature 列数が一致する（現在は宣言63列のうち6列が 100% NaN・silent contract breach）。Phase 4 は 18 rolling features（6系統×3軸）で学習する。
2. **CR-01 回帰防止（MANDATORY）**: 合成 DB-mock を通じた end-to-end builder path test を追加し、「登録された feature column が1つでも 100% NaN になれば FAIL」を機械的に保証。現状 test_rolling.py は合成 dict fixture のみを検査し builder 経由の end-to-end をカバーしないため、この breach が検出されなかった。
3. **WR-01（look-ahead leak）**: builder.estimated_running_style が rolling と同一の PIT pre-filter（`history.as_of_datetime < obs.feature_cutoff_datetime`・per-observation）を groupby 前に適用する。registry は strict `< cutoff` を宣言し実装が違反していた矛盾を解消。
4. **CR-02（JOIN 右側 unfiltered）**: `_fetch_feature_sources` / `_fetch_history` の JOIN 右側 `nr` (normalized.n_race) に `project_window_filter('nr')` を付与（label_race_date_backfill.py の CR-06 契約と対称）。
5. **CR-03（silent all-train fallback）**: `build_frozen_category_maps` が `race_date` 欠損 frame で fail-loud（ValueError）する。D-13 silent fallback 禁止に合致。
6. **CR-04（pickle ACE）**: `load_category_maps` を `joblib.load`（pickle）から JSON 安全フォーマットに移行。§19.1 cross-machine 共有での arbitrary code execution vector を解消。artifact 拡張子を `.joblib` から `.json` に移行。

Purpose: VERIFICATION must-have #1「every feature row is point-in-time correct AND the feature matrix is complete」を PARTIAL → VERIFIED に押し上げ、WR-01/CR-02/CR-03/CR-04 anti-pattern を全て解消して Phase 3 を `passed` にする。

Output:
- `src/config/feature_availability.yaml`（6エントリ削除・Deferred note コメント）
- `src/features/rolling.py`（_ROLLING_SYSTEMS / _SYSTEM_SOURCE から timediff/babacd 削除）
- `src/features/availability.py`（_ROLLING_SYSTEMS_FOR_RESERVED parity）
- `src/features/builder.py`（WR-01 PIT pre-filter・CR-02 project_window_filter('nr')）
- `src/features/category_map_consumer.py`（CR-03 fail-loud・CR-04 JSON 移行）
- `scripts/run_feature_build.py`（artifact 拡張子 .json 移行）
- `tests/features/test_builder.py`（CR-01 regression guard end-to-end test）
- `tests/features/test_category_map_consumer.py`（CR-03/CR-04 tests）
- `.planning/phases/03-as-of-features-snapshots/03-CONTEXT.md`（## Deferred Ideas に timediff/babacd 再登録予約を追記）
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-as-of-features-snapshots/03-VERIFICATION.md
@.planning/phases/03-as-of-features-snapshots/03-REVIEW.md
@.planning/phases/03-as-of-features-snapshots/03-CONTEXT.md
@.planning/phases/03-as-of-features-snapshots/03-04-SUMMARY.md
@src/features/builder.py
@src/features/rolling.py
@src/features/availability.py
@src/features/category_map_consumer.py
@src/features/running_style.py
@src/etl/filters.py
@src/etl/label_race_date_backfill.py
@src/config/feature_availability.yaml
@tests/features/test_rolling.py
@tests/features/test_builder.py
@tests/features/test_category_map_consumer.py
@tests/features/conftest.py
@scripts/run_feature_build.py
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-01 — rolling_timediff_* / rolling_babacd_* の6エントリを registry・rolling・availability reserved から削除し三者 parity を復元 + end-to-end regression guard test を追加</name>
  <files>src/config/feature_availability.yaml, src/features/rolling.py, src/features/availability.py, tests/features/test_builder.py, .planning/phases/03-as-of-features-snapshots/03-CONTEXT.md</files>

  <read_first>
    - .planning/phases/03-as-of-features-snapshots/03-VERIFICATION.md（gap 権威・CR-01 実証: 6列とも non_null=0・reason・missing セクション）
    - .planning/phases/03-as-of-features-snapshots/03-REVIEW.md CR-01 セクション（line 62-94・3つの fix option・option (c) が最小修正・本 plan は option (c) を採用）
    - src/config/feature_availability.yaml（L164-305・8系統×3軸=24 rolling エントリ・うち timediff L193-213 / babacd L285-305 を削除対象・Deferred note コメント追加位置）
    - src/features/rolling.py（L46-75・`LOOKBACK`/`_ROLLING_SYSTEMS`/`_SYSTEM_SOURCE`・timediff/babacd を削除し6系統化）
    - src/features/availability.py（L127-160・`_ROLLING_SYSTEMS_FOR_RESERVED` と `_RESERVED_NON_FEATURE_COLUMNS`・IN-01 が指摘する重複定義を parity させる）
    - src/features/builder.py（L57-124・`_HISTORY_DB_SELECT_COLUMNS`/`_DERIVED_HISTORY_COLUMN_NAMES`・timediff/babacd は既に SELECT 対象外・docstring L65-66 の「sentinel 扱い」記述を更新）
    - tests/features/test_builder.py（既存テスト群・CR-01 regression guard を追加する位置・`_get_builder()` idiom）
    - tests/features/test_rolling.py（既存テスト・`test_target_race_timediff_not_in_history`/`test_per_observation_latest_5_excludes_target_same_day_previous_future` が timediff fixture に依存・削除後にこれらが壊れないか確認・必要なら kakuteijyuni 等の残存系統に切替）
    - tests/features/conftest.py（`_build_adversarial_rolling_rows`・timediff key を含む合成 dict・rolling.py から timediff を削除しても fixture 自体は rolling が `source_cols not in recent.columns` で skip するため壊れないはずだが確認）
    - .planning/phases/03-as-of-features-snapshots/03-CONTEXT.md（L137-148 `## Deferred Ideas`・再登録予約 note の追記位置）
  </read_first>

  <behavior>
    - Test 1 `test_no_registered_feature_column_all_nan_end_to_end`（新規・CR-01 regression guard・MANDATORY）: 合成 DB-mock（`_HISTORY_DB_SELECT_COLUMNS` 相当の列を持つ小 DataFrame・kettonum 毎に過去走を含む・cutoff 前後の mix）を構築し、builder の rolling/history/derived pipeline を通した end-to-end 相当の処理で rolling feature 列を算出。`load_feature_availability()` で取得した全 `rolling_*_mean_5` feature_name について、結果列が **100% NaN でない**ことを assert（少なくとも1行でも non-NaN があること・または sentinel `__MISSING__` 文字列であること・純粋な numpy NaN 100% を弾く）。CR-01 再発（registry に登録された系統の source が欠損し silent 全 NaN になる）を機械的に検出する。
    - Test 2 `test_registry_rolling_systems_match_rolling_impl`（新規・parity guard）: `load_feature_availability()` の features から `rolling_*_mean_5` feature_name を抽出し系統集合を算出。これが `rolling._ROLLING_SYSTEMS` と完全一致すること、`availability._ROLLING_SYSTEMS_FOR_RESERVED` とも完全一致することを assert（3者 parity・IN-01 重複定義の drift を機械検出）。
    - Test 3 `test_no_timediff_babacd_in_registry_or_rolling`（新規・CR-01 DELETE verify）: `rolling._ROLLING_SYSTEMS` に `"timediff"` と `"babacd"` が含まれないこと・`availability._ROLLING_SYSTEMS_FOR_RESERVED` にも含まれないこと・`load_feature_availability()` の features にも `rolling_timediff_*` / `rolling_babacd_*` が含まれないことを assert。
    - 既存テストの保全: `test_target_race_timediff_not_in_history` / `test_per_observation_latest_5_excludes_target_same_day_previous_future` が timediff fixture を使っている場合、削除された timediff 系統の代わりに残存系統（例: kakuteijyuni・実カラムとして存在し合成的に値を与えられる）に切替えて同等の PIT defense-in-depth を検証するよう修正。`test_under_5_starts_uses_missing_sentinel` が `timediff` 列で新馬検査している場合も残存系統に切替。
  </behavior>

  <action>
    **src/features/rolling.py**:
    - `_ROLLING_SYSTEMS` から `"timediff"` と `"babacd"` を削除し、6系統（`kakuteijyuni` / `harontimel3` / `jyuni3c_jyuni4c` / `kyori` / `jyocd` / `days_since_prev`）にする。
    - `_SYSTEM_SOURCE` dict から `"timediff"` と `"babacd"` の2エントリを削除。
    - module docstring（L6-8「8系統」表記・L47-52 コメント）を「6系統」に更新し、timediff/babacd が Phase 2 ETL 拡張待ちで Deferred である旨（CR-01・03-VERIFICATION.md 参照）を明記。

    **src/config/feature_availability.yaml**:
    - L192-213（`rolling_timediff_mean_5` / `rolling_timediff_latest_5` / `rolling_timediff_sd_5` の3エントリ）と L284-305（`rolling_babacd_mean_5` / `rolling_babacd_latest_5` / `rolling_babacd_sd_5` の3エントリ）の計6エントリを削除。
    - 削除位置にコメントブロックを追加: 「CR-01 (03-VERIFICATION.md): rolling_timediff_* / rolling_babacd_* は normalized 層に source カラム (timediff/babacd) が存在しないため全行 __MISSING__ → NaN となり registry↔実体 silent 乖離が生じていた。Phase 2 ETL 拡張（/gsd-phase --insert で Phase 4 挿入前に新設）で source カラムが normalized 層に揃った後に再登録する。それまで Phase 4 は 18 rolling features（6系統×3軸）で学習。」
    - ヘッダコメント（L164-167「8系統 × 3軸 = 24」表記）を「6系統 × 3軸 = 18」に更新。

    **src/features/availability.py**:
    - `_ROLLING_SYSTEMS_FOR_RESERVED`（L129-138）から `"timediff"` と `"babacd"` を削除し、rolling.py の `_ROLLING_SYSTEMS` と完全一致する6系統にする（IN-01 重複定義の drift リスクを parity で解消）。
    - L127-128 のコメント「rolling 8系統」を「6系統」に更新。

    **src/features/builder.py**:
    - docstring L65-66 の「`timediff` / `babacd` は normalized 層に存在しないため SELECT せず、当該 rolling 系統は rolling.py の D-13 sentinel 経路で `__MISSING__` 扱いとなる」を更新し、「Phase 3 gap-closure (03-05・CR-01) で rolling 系統から削除済み・Phase 2 ETL 拡張後に再登録予定」と改修。実コード（`_HISTORY_DB_SELECT_COLUMNS`/`_DERIVED_HISTORY_COLUMN_NAMES`）は既に timediff/babacd を含まないため変更不要（docstring と `_DERIVED_HISTORY_COLUMN_NAMES` frozenset の `timediff`/`babacd`/`datakubun` は derived 宣言専用で rolling source には使われないためそのまま残す・削除すると `_HISTORY_SELECT_COLUMNS` の filter 計算に影響するため注意）。

    **tests/features/test_builder.py**:
    - 新規 test `test_no_registered_feature_column_all_nan_end_to_end` を追加。合成 DB-mock frame（`_OBS_SELECT_COLUMN_NAMES` と `_HISTORY_SELECT_COLUMN_NAMES` 相当の列・2-3 馬 × 数レース・cutoff 前後の history を含む）を構築し、`builder._construct_derived_columns` → cutoff 計算 → `builder.build_rolling_features` を直接呼出して rolling feature 列を得る。`load_feature_availability()["features"]` から `feature_name.startswith("rolling_") and feature_name.endswith("_mean_5")` を抽出し、各列について `result[col].isna().all()` が False であること（純粋 NaN 100% でないこと・sentinel 文字列 `__MISSING__` は isna で False なので新馬行列も合格）を assert。万が一 rolling source が欠損した系統が再登録されると本 test が RED になる。
    - 新規 test `test_registry_rolling_systems_match_rolling_impl`（3者 parity・IN-01 regression guard）を追加。
    - 新規 test `test_no_timediff_babacd_in_registry_or_rolling`（CR-01 DELETE verify）を追加。

    **tests/features/test_rolling.py**:
    - 既存 test が timediff 系統に依存している場合（`test_target_race_timediff_not_in_history`・`test_per_observation_latest_5_excludes_target_same_day_previous_future`・`test_under_5_starts_uses_missing_sentinel`）、残存系統（`kakuteijyuni` 推奨・実カラムとして合成値を与えられる）に切替。adversarial row の `timediff` key を `kakuteijyuni` にリネームし、assertion の期待値（例: mean == -2.0）を同等の PIT defense を検証する値に調整。conftest.py の `_build_adversarial_rolling_rows` が timediff を emit する場合、timediff key は rolling.py が `source_cols not in recent.columns` で skip するため無害だが、test の intent を残存系統に寄せる方が健全。

    **.planning/phases/03-as-of-features-snapshots/03-CONTEXT.md**:
    - `## Deferred Ideas`（L137-148）に追記: 「**Phase 2 ETL 拡張（新規 phase・Phase 4 挿入前に /gsd-phase --insert で新設）**: EveryDB2 normalized ETL を拡張して `timediff`（勝馬差 TimeDIFN）・`babacd`（過去走馬場状態）の source カラムを normalized.n_uma_race に持ち込む。その後 Phase 3 の rolling_timediff_* / rolling_babacd_* 計6 feature を registry・rolling.py・availability reserved の三者から再登録し、Phase 1-A rolling features を 18 → 24（8系統×3軸）に拡張。Phase 3 gap-closure (03-05・CR-01) で silent 全 NaN breach を解消するため一時削除した。」
  </action>

  <verify>
    <automated>KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/features/test_rolling.py tests/features/test_builder.py tests/features/test_allowlist.py tests/features/test_pit_cutoff.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "rolling_timediff_\|rolling_babacd_" src/config/feature_availability.yaml` が `0` を返す（6エントリ削除・registry↔実体 parity）
    - `python -c "from src.features.rolling import _ROLLING_SYSTEMS; assert 'timediff' not in _ROLLING_SYSTEMS and 'babacd' not in _ROLLING_SYSTEMS and len(_ROLLING_SYSTEMS) == 6"` が exit 0
    - `python -c "from src.features.availability import _ROLLING_SYSTEMS_FOR_RESERVED as r; from src.features.rolling import _ROLLING_SYSTEMS as s; assert tuple(r) == tuple(s), (r, s)"` が exit 0（3者 parity・IN-01 drift 解消）
    - `python -c "from src.features.availability import load_feature_availability as f; feats=[e['feature_name'] for e in f()['features']]; assert not any('rolling_timediff_' in x or 'rolling_babacd_' in x for x in feats)"` が exit 0
    - `tests/features/test_builder.py::test_no_registered_feature_column_all_nan_end_to_end` が GREEN（CR-01 regression guard）
    - `tests/features/test_builder.py::test_registry_rolling_systems_match_rolling_impl` が GREEN（3者 parity）
    - `tests/features/test_builder.py::test_no_timediff_babacd_in_registry_or_rolling` が GREEN
    - `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/features/ -q` が全体 GREEN（既存テストの timediff 依存を残存系統に移行した結果）
    - `.planning/phases/03-as-of-features-snapshots/03-CONTEXT.md` の `## Deferred Ideas` に `rolling_timediff` または `rolling_babacd` の再登録予約 note が含まれる（`grep -c "rolling_timediff\|rolling_babacd" .planning/phases/03-as-of-features-snapshots/03-CONTEXT.md` >= 1）
  </acceptance_criteria>

  <done>
    CR-01 の silent empty features breach が閉鎖される。registry・rolling.py・availability reserved の三者が 6 rolling 系統（18 features）で完全一致し、end-to-end regression guard test が将来の再発を機械的に検出する。manifest `feature_count` と実 populated feature 列数が一致する（再 build 後・Task 3 の snapshot 再生成後に最終確認）。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-01 + CR-02 + CR-03 — estimated_running_style に PIT pre-filter 追加・JOIN 右側 nr に project_window_filter('nr') 適用・build_frozen_category_maps の race_date 欠損 fail-loud 化</name>
  <files>src/features/builder.py, src/features/category_map_consumer.py, tests/features/test_builder.py, tests/features/test_running_style.py, tests/features/test_category_map_consumer.py</files>

  <read_first>
    - .planning/phases/03-as-of-features-snapshots/03-VERIFICATION.md gap.artifacts（builder.py:330-339 WR-01・builder.py:392-397/439-444 CR-02・category_map_consumer.py:176-186 CR-03）
    - .planning/phases/03-as-of-features-snapshots/03-REVIEW.md WR-01 セクション（L200-220・registry は strict `< cutoff` を宣言し実装が違反・fix option (b) per-observation filter）・CR-02 セクション（L98-126・label_race_date_backfill.py が両側 filter で対称・CR-06 契約）・CR-03 セクション（L129-162・D-13 silent fallback 禁止・race_date 欠損で ValueError）
    - src/features/builder.py L296-339（cutoff 計算 Step 3・estimated_running_style 算出 Step 6・現状 `history.groupby("kettonum")` で未 filter）・L377-454（`_fetch_feature_sources`・`_fetch_history`・JOIN SQL・`project_window_filter('ur')` のみ）
    - src/features/rolling.py L179-208（expanded history 構築・`obs_id`/`feature_cutoff_datetime` を obs_keys に持たせ merge・`history.as_of_datetime < feature_cutoff_datetime` で PIT pre-filter・これと同一 idiom を estimated_running_style に適用）
    - src/features/category_map_consumer.py L152-195（`build_frozen_category_maps`・L176-186 の else branch が silent all-True mask・CR-03 で ValueError に置換）
    - src/etl/label_race_date_backfill.py L139-149（CR-06 契約の模範・`fl_filter` と `nr_filter` を AND 結合・これを builder の JOIN SQL に複製）
    - src/etl/filters.py（`project_window_filter(alias)` の契約・`f"{alias}.jyocd BETWEEN '01' AND '10' AND {alias}.year::int >= 2015"` を返す）
    - tests/features/test_running_style.py（既存テスト・WR-01 regression guard を追加する位置）
    - tests/features/test_category_map_consumer.py（既存テスト・CR-03 fail-loud test を追加する位置）
  </read_first>

  <behavior>
    - Test 1 `test_estimated_running_style_applies_pit_prefilter`（新規・WR-01 regression guard）: `inspect.getsource(builder.build_feature_matrix)` または estimated_running_style 算出 block に、`as_of_datetime` と `feature_cutoff_datetime` を用いた strict `<` filter（per-observation・rolling と同一 idiom）が含まれることを assert。更に、合成 feature_matrix（2 obs・同一 kettonum・cutoff 前後の history）で、cutoff 後の jyuni3c/jyuni4c が推定脚質に混入しないことを検証（例: cutoff 前は全て「逃」・cutoff 後に「追」を入れても結果が「逃」になること）。
    - Test 2 `test_fetch_history_and_feature_sources_filter_both_join_sides`（新規・CR-02 regression guard）: `inspect.getsource(builder._fetch_feature_sources)` と `inspect.getsource(builder._fetch_history)` の両方に `project_window_filter('nr')` が含まれることを assert（label_race_date_backfill.py L148 の `f"WHERE {fl_filter} AND {nr_filter}"` と対称）。
    - Test 3 `test_build_frozen_maps_raises_on_missing_race_date`（新規・CR-03 fail-loud）: `race_date` 列を持たない合成 DataFrame を `build_frozen_category_maps` に渡すと `ValueError` が raise されること（silent all-True mask に fall しない・D-13）を assert。エラーメッセージに `race_date` と `D-13` または `fail-loud` の言及が含まれること。
    - 既存テストの保全: `test_fit_called_on_train_window_only`（test_category_map_consumer.py L36-44）は `race_date` が存在する frame を前提とするため、`race_date` 列を追加した合成 frame を使うよう修正（CR-03 で race_date 必須化した影響）。`test_unseen_maps_to_sentinel` / `test_codes_are_non_negative_int32` は `fit_category_map`/`apply_category_map` 単体（`build_frozen_category_maps` 経由でない）を使うため影響なし。
  </behavior>

  <action>
    **src/features/builder.py — WR-01（estimated_running_style PIT pre-filter）**:
    - L329-339 の estimated_running_style 算出 block を修正。現状 `history.groupby("kettonum")` で未 filter だが、registry（feature_availability.yaml L355-361）は `cutoff_rule: "race_date - 1 day (strict < cutoff)"` を宣言する。rolling と同一の PIT idiom を適用:
      1. `feature_matrix` 側に `obs_keys = feature_matrix[["kettonum", "feature_cutoff_datetime"]].copy()`（index を obs 識別に使えるよう reset_index 済みを想定・`race_nkey` があれば obs_id 構築に使うが running_style は kettonum 単位で十分・per-horse style なので obs_id は不要）
      2. `expanded = history.merge(obs_keys, on="kettonum", how="inner")`（rolling.py L181 と同一・同一名衝突回避で suffixes）・`feature_cutoff_datetime_obs` → `feature_cutoff_datetime` の統合（rolling.py L184-186 と同一）
      3. `pit_filtered = expanded[expanded["as_of_datetime"] < expanded["feature_cutoff_datetime"]]`（strict `<`・rolling.py L193-195 と同一・CUTOFF_SEMANTICS と整合）
      4. `for kn, group in pit_filtered.groupby("kettonum"):` で推定脚質を算出（以降の `estimate_running_style(rows)` 呼出は不変）
      5. `feature_matrix["estimated_running_style"] = feature_matrix["kettonum"].map(style_map).fillna("__MISSING__")` は不変
    - docstring（L256・Step 6）を更新し、「過去走 jyuni3c/jyuni4c のみ・**cutoff 以前の過去走のみ**（rolling と同一 PIT pre-filter）・当日不使用・D-05・WR-01 fix」と明記。

    **src/features/builder.py — CR-02（JOIN 右側 filter）**:
    - `_fetch_feature_sources` の `obs_sql`（L392-397）と `_fetch_history` の `sql`（L439-444）の WHERE 句を `f"WHERE {project_window_filter('ur')}"` から `f"WHERE {project_window_filter('ur')} AND {project_window_filter('nr')}"` に変更（label_race_date_backfill.py L148 `f"WHERE {fl_filter} AND {nr_filter}"` と対称・CR-06 契約）。両関数の docstring に「CR-06: JOIN 両側に project_window_filter を適用（label_race_date_backfill.py と対称・03-05 CR-02 fix）」を追記。

    **src/features/category_map_consumer.py — CR-03（race_date 欠損 fail-loud）**:
    - L176-186 の `race_date_col` 計算と else branch を削除し、冒頭で `race_date` 列の存在を検査:
      ```
      if "race_date" not in feature_matrix.columns:
          raise ValueError(
              "build_frozen_category_maps: feature_matrix に race_date 列が無い "
              "(train 窓 mask を計算できない・Pitfall 3.4 / §14.3 / D-13 fail-loud・"
              "03-05 CR-03 fix)"
          )
      race_date_col = pd.to_datetime(feature_matrix["race_date"])
      train_mask = race_date_col.between(train_window[0], train_window[1])
      ```
    - docstring（L157-173）を更新し、「**race_date 必須**（D-13 fail-loud・03-05 CR-03）・race_date 欠損 frame は ValueError」と明記。L184-185 の「race_date が無い場合は全行を train 扱い（unit test の合成 DataFrame 向け）」comment を削除。

    **tests/features/test_running_style.py**:
    - 新規 test `test_estimated_running_style_applies_pit_prefilter` を追加（inspect-based regression guard + 合成 cutoff 前後 frame での実証）。

    **tests/features/test_builder.py**:
    - 新規 test `test_fetch_history_and_feature_sources_filter_both_join_sides` を追加（inspect-based・両関数の source に `project_window_filter('nr')` が含まれることを assert）。

    **tests/features/test_category_map_consumer.py**:
    - 新規 test `test_build_frozen_maps_raises_on_missing_race_date` を追加（`race_date` 列を持たない合成 DataFrame で `ValueError`・`pytest.raises(ValueError, match="race_date")`）。
    - 既存 `test_fit_called_on_train_window_only`（L36-44）は `build_frozen_category_maps` でなく module 全体の source inspect なので影響なし・ただし `race_date` 文字列が source に含まれることは CR-03 でも保持されるため GREEN 維持。
    - `test_unseen_maps_to_sentinel` / `test_codes_are_non_negative_int32` / `test_apply_frozen_maps_drops_raw_id_columns` は `fit_category_map`/`apply_category_map`/`apply_frozen_category_maps` 単体を使うため CR-03 影響なし。

    **禁止事項**: rolling.py の PIT idiom をコピーする際、`obs_id`（CYCLE-2 HIGH #1・`(race_nkey, kettonum)`）を構築しないこと。estimated_running_style は kettonum 単位の per-horse style を算出し、PIT は `as_of_datetime < feature_cutoff_datetime` の strict `<` だけで十分（race_nkey 単位の独立性は rolling のみで必要）。copy するのは PIT filter 行のみ。
  </action>

  <verify>
    <automated>KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/features/test_running_style.py tests/features/test_builder.py tests/features/test_category_map_consumer.py tests/features/test_pit_cutoff.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `python -c "import inspect; from src.features.builder import build_feature_matrix as b, _fetch_feature_sources, _fetch_history; s1=inspect.getsource(b); assert 'feature_cutoff_datetime' in s1; s2=inspect.getsource(_fetch_feature_sources); assert \"project_window_filter('nr')\" in s2; s3=inspect.getsource(_fetch_history); assert \"project_window_filter('nr')\" in s3"` が exit 0（WR-01 + CR-02）
    - `python -c "import inspect; from src.features.builder import _fetch_feature_sources, _fetch_history; s1=inspect.getsource(_fetch_feature_sources); s2=inspect.getsource(_fetch_history); assert s1.count(\"project_window_filter('nr')\") >= 1 and s2.count(\"project_window_filter('nr')\") >= 1"` が exit 0（両関数で nr filter が適用）
    - `tests/features/test_running_style.py::test_estimated_running_style_applies_pit_prefilter` が GREEN（WR-01 regression guard）
    - `tests/features/test_builder.py::test_fetch_history_and_feature_sources_filter_both_join_sides` が GREEN（CR-02 regression guard）
    - `tests/features/test_category_map_consumer.py::test_build_frozen_maps_raises_on_missing_race_date` が GREEN（CR-03 fail-loud）
    - `python -c "import inspect; from src.features.category_map_consumer import build_frozen_category_maps as b; s=inspect.getsource(b); assert 'raise ValueError' in s and 'race_date' in s"` が exit 0（CR-03 fail-loud・ValueError）
    - `python -c "import inspect; from src.features.category_map_consumer import build_frozen_category_maps as b; s=inspect.getsource(b); assert '全行を train 扱い' not in s and 'unit test の合成' not in s"` が exit 0（silent fallback comment 削除）
    - `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/features/ -q` が全体 GREEN
  </acceptance_criteria>

  <done>
    WR-01 look-ahead leak（estimated_running_style）・CR-02 JOIN 右側 unfiltered・CR-03 silent all-train fallback の3つの anti-pattern が解消される。registry 宣言（strict `< cutoff`）と実装が一致し、CR-06 契約（JOIN 両側 filter）が builder でも遵守される。race_date 欠損 frame が fail-loud で D-13 に合致する。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CR-04 — load_category_maps/persist_category_maps を joblib (pickle) から JSON 安全フォーマットに移行し、artifact 拡張子を .joblib → .json に変更</name>
  <files>src/features/category_map_consumer.py, scripts/run_feature_build.py, tests/features/test_category_map_consumer.py</files>

  <read_first>
    - .planning/phases/03-as-of-features-snapshots/03-REVIEW.md CR-04 セクション（L165-194・joblib.load が pickle で ACE vector・fix は JSON への移行・`FrozenCategoryMap` は `.items()`/dict access を持つため JSON round-trip は自明・byte-reproducible & human-auditable の副次効果）
    - src/features/category_map_consumer.py（全体・L54-108 `FrozenCategoryMap` class・`__getstate__`/`__setstate__` は joblib/pickle 用・JSON 移行で不要になるが class 自体は保持・L241-257 `persist_category_maps`/`load_category_maps` が joblib.dump/load を呼ぶ・これを json に置換）
    - src/features/category_map_consumer.py L36-41 import block（`import joblib` を削除し `import json` を追加）
    - src/utils/category_map.py（`fit_category_map`/`apply_category_map`・`UNSEEN`/`MISSING` sentinel・戻り値は `dict[str, int]` なので JSON serializable）
    - scripts/run_feature_build.py L40-50 import（`persist_category_maps` を import）・L220-230 付近の `category_map_path` 構築と `persist_category_maps` 呼出・manifest の `category_map_artifact` 参照
    - tests/features/test_category_map_consumer.py（既存テスト・CR-04 JSON round-trip test を追加する位置）
    - snapshots/feature_matrix_20260619-1a-v1.manifest.yaml（既存 manifest の `category_map_artifact: snapshots/category_map_20260619-1a-v1.joblib`・再生成後に .json になる）
    - CLAUDE.md「What NOT to Use」（Parquet 書込以外に pickle を使わない方針・CR-04 はこれを強化）
  </read_first>

  <behavior>
    - Test 1 `test_persist_and_load_category_maps_json_roundtrip`（新規・CR-04）: 合成 `dict[str, FrozenCategoryMap]` を構築し（`horse_id`/`jockey_id` 等5カラム・sentinel `__UNSEEN__`/`__MISSING__` 含む・非負 int）、`persist_category_maps(maps, tmp_path)` で永続化→`load_category_maps(tmp_path)` で読込。読込んだ map が元と同じ key 構成と code 値を持つこと（round-trip 等価）。永続化ファイルが JSON であること（`json.loads` で parse 可能・拡張子 `.json`）を assert。
    - Test 2 `test_load_category_maps_does_not_use_joblib`（新規・CR-04 regression guard）: `inspect.getsrcfile(category_map_consumer)` または `inspect.getsource(category_map_consumer)` に `joblib.load` が含まれないこと・`import joblib` が含まれないこと（module から joblib 依存が完全除去されていること）を assert。`json.load` が含まれることも assert。
    - Test 3 `test_persisted_artifact_is_human_auditable_json`（新規・CR-04 副次効果）: 永続化ファイルが `sort_keys=True` で dump された JSON であり、`Path(artifact_path).read_text()` が `__UNSEEN__` と `__MISSING__` の sentinel key を含むこと（human-auditable・byte-reproducible）を assert。
  </behavior>

  <action>
    **src/features/category_map_consumer.py**:
    - import block（L36-41）: `import joblib` を削除し、`import json` を追加。`from pathlib import Path` は既存。
    - `FrozenCategoryMap` class（L54-108）: そのまま保持（dict-style access と `.codes` attribute は JSON 移行でも需要あり・Phase 4 consumer が dict access を使うため）。`__getstate__`/`__setstate__`（L101-107）は joblib/pickle 専用なので削除（JSON 移行で不要・class は json dict から再構築される）。
    - `persist_category_maps`（L241-252）: `joblib.dump(maps, artifact_path)` を以下に置換:
      ```
      Path(artifact_path).parent.mkdir(parents=True, exist_ok=True)
      serialisable = {col: dict(m.items()) for col, m in maps.items()}
      Path(artifact_path).write_text(
          json.dumps(serialisable, sort_keys=True, ensure_ascii=False),
          encoding="utf-8",
      )
      ```
      （`sort_keys=True` で byte-reproducible・`ensure_ascii=False` で sentinel 文字列可読性確保・03-REVIEW.md L186-193 の fix テンプレートに合致）
    - `load_category_maps`（L255-257）: `joblib.load(artifact_path)` を以下に置換:
      ```
      raw = json.loads(Path(artifact_path).read_text(encoding="utf-8"))
      return {col: FrozenCategoryMap(m) for col, m in raw.items()}
      ```
      （`FrozenCategoryMap(m)` で dict から再構築・`__getstate__`/`__setstate__` 不要・03-REVIEW.md L190-193 と合致）
    - docstring（L244-250・L255-257）を更新: persist は「JSON（sort_keys=True・byte-reproducible・human-auditable）」・load は「JSON から読込（joblib/pickle 不使用・ACE vector 解消・03-05 CR-04 fix）」。両関数の docstring に「artifact 拡張子は `.json`（従来 `.joblib` から移行）」を明記。

    **scripts/run_feature_build.py**:
    - `category_map_path` 構築部（L220 付近・`_SNAPSHOTS_DIR / f"category_map_{snapshot_id}.joblib"` を想定）を `f"category_map_{snapshot_id}.json"` に変更。
    - `write_manifest` 呼出（manifest YAML 書出）の `category_map_artifact` 引数が `.json` 拡張子の path を指すよう更新。これにより manifest の `category_map_artifact: snapshots/category_map_<id>.json` になる。
    - docstring（L4-18・ステップ 7「`persist_category_maps` で frozen category map を joblib で永続化」）を「JSON で永続化（CR-04・pickle ACE 解消）」に更新。

    **tests/features/test_category_map_consumer.py**:
    - 新規 test `test_persist_and_load_category_maps_json_roundtrip` を追加（`tmp_path` fixture 使用・5カラムの合成 FrozenCategoryMap を round-trip）。
    - 新規 test `test_load_category_maps_does_not_use_joblib` を追加（inspect-based・`joblib.load` と `import joblib` が source に含まれないこと・`json.load` または `json.loads` が含まれること）。
    - 新規 test `test_persisted_artifact_is_human_auditable_json` を追加（`sort_keys=True` の JSON・sentinel key 可読）。
    - 既存テストは `fit_category_map`/`apply_category_map`/`apply_frozen_category_maps` 単体を使うため CR-04 影響なし。

    **実行後（Task 3 完了後・本 plan の verify には含めないが SUMMARY で記録）**: `scripts/run_feature_build.py` を再実行して `.json` category map artifact を生成し、既存 `.joblib` は削除可能（git 管理外のため影響なし・`.gitignore` の `*.joblib` 行は `*.json` category_map も除外するよう `snapshots/category_map_*.json` を追記するか検討・ただし manifest YAML と同じく git 管理候補とも考えられるため本 plan では .gitignore 修正は必須としない）。
  </action>

  <verify>
    <automated>KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/features/test_category_map_consumer.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `python -c "import inspect; from src.features import category_map_consumer as c; s=inspect.getsource(c); assert 'joblib' not in s, 'joblib 依存が残存（CR-04 違反）'; assert 'json.load' in s or 'json.loads' in s, 'JSON 読込が無い（CR-04）'"` が exit 0
    - `python -c "from src.features import category_map_consumer as c; import inspect; s=inspect.getsource(c.load_category_maps); assert 'joblib' not in s and 'json' in s"` が exit 0
    - `python -c "from src.features import category_map_consumer as c; import inspect; s=inspect.getsource(c.persist_category_maps); assert 'joblib' not in s and 'json' in s and 'sort_keys=True' in s"` が exit 0（byte-reproducible JSON dump）
    - `grep -c "\.joblib" scripts/run_feature_build.py` が `0` を返す（artifact 拡張子が .json に完全移行）
    - `grep -c "category_map_.*\.json" scripts/run_feature_build.py` が `>= 1` を返す（.json path 構築）
    - `tests/features/test_category_map_consumer.py::test_persist_and_load_category_maps_json_roundtrip` が GREEN
    - `tests/features/test_category_map_consumer.py::test_load_category_maps_does_not_use_joblib` が GREEN
    - `tests/features/test_category_map_consumer.py::test_persisted_artifact_is_human_auditable_json` が GREEN
    - `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/features/ -q` が全体 GREEN
  </acceptance_criteria>

  <done>
    CR-04 pickle ACE vector が解消される。category map artifact が JSON（byte-reproducible・human-auditable）に移行し、`load_category_maps` が `joblib.load` を呼ばない。§19.1 cross-machine 共有で tampered artifact による arbitrary code execution リスクが除去される。artifact 拡張子が `.json` に統一され、manifest 参照も更新される。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem artifact → Phase 4 model load | `load_category_maps` が JSON を読込む（pickle でない）・tampered artifact が code を実行できない（CR-04） |
| registry YAML → rolling impl → Parquet output | registry feature エントリと rolling 実装系統が 1:1 で一致し、登録された feature 列が silent に 100% NaN にならない（CR-01 parity） |
| history DataFrame → estimated_running_style | PIT pre-filter が cutoff 後レースを除外し、look-ahead leak を防ぐ（WR-01） |
| normalized.n_race (JOIN 右側) → feature matrix | project_window_filter('nr') が JRA/window 外行の混入を防ぐ（CR-02・CR-06） |
| feature_matrix → build_frozen_category_maps train mask | race_date 欠損で fail-loud し、silent all-train fallback による test 構成リークを防ぐ（CR-03・D-13） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-27 | Elevation of privilege (arbitrary code execution via pickle deserialization) | `category_map_consumer.load_category_maps` が `joblib.load`（pickle）で tampered `.joblib` artifact を読込 → `__reduce__` 経由で Phase 4 学習プロセス権限で任意コード実行（CR-04・§19.1 cross-machine 共有で悪用可能） | mitigate | `joblib.load` を `json.loads` に置換・`persist_category_maps` を `json.dumps(sort_keys=True)` で byte-reproducible な JSON 書出に変更・artifact 拡張子 `.joblib` → `.json`・module から `import joblib` を完全削除・`FrozenCategoryMap.__getstate__`/`__setstate__`（pickle 用）を削除・test_load_category_maps_does_not_use_joblib で回帰防止・test_persist_and_load_category_maps_json_roundtrip で等価性証明 |
| T-03-28 | Tampering (registry↔output silent divergence) | registry が `rolling_timediff_*`/`rolling_babacd_*` を実 feature として登録するが normalized 層に source カラムが無く、rolling.py が sentinel 経由で全行 NaN を出力 → manifest `feature_count` と実 populated 列数が乖離・再現性・監査性を損なう（CR-01） | mitigate | 6エントリを registry・rolling.py・availability reserved の三者から削除・Phase 2 ETL 拡張後に再登録（Deferred note）・test_no_registered_feature_column_all_nan_end_to_end で end-to-end builder path の回帰防止・test_registry_rolling_systems_match_rolling_impl で3者 parity を機械検証 |
| T-03-29 | Information disclosure (look-ahead leak via estimated_running_style) | `estimated_running_style` が cutoff 後の過去走 jyuni3c/jyuni4c を含めて算出され、target race の未来情報が feature に混入（WR-01・registry は strict `< cutoff` を宣言し実装が違反） | mitigate | rolling と同一の PIT pre-filter（`history.as_of_datetime < obs.feature_cutoff_datetime`・per-observation・strict `<`）を groupby(kettonum) 前に適用・test_estimated_running_style_applies_pit_prefilter で回帰防止 |
| T-03-30 | Tampering (unfiltered JOIN right side → bogus race rows) | `_fetch_feature_sources`/`_fetch_history` の JOIN 右側 `nr` (normalized.n_race) に `project_window_filter('nr')` 未適用・CR-06 契約違反・normalized ETL が n_race に pre-2015/NAR 行を混入させた場合に bogus 行が発生（CR-02・現状 INNER JOIN で未顕在化だが潜伏 vector） | mitigate | 両関数の WHERE 句を `project_window_filter('ur') AND project_window_filter('nr')` に変更（label_race_date_backfill.py と対称）・test_fetch_history_and_feature_sources_filter_both_join_sides で回帰防止 |
| T-03-31 | Information disclosure (silent all-train fallback → test composition leak) | `build_frozen_category_maps` が race_date 欠損 frame で silent に全行 train 扱いし、将来の refactor で val/test 行が fit に混入する潜伏 leak（CR-03・D-13 違反） | mitigate | race_date 欠損で `ValueError` を raise（fail-loud）・test_build_frozen_maps_raises_on_missing_race_date で回帰防止 |
| T-03-SC | Tampering (package legitimacy) | 本 plan は新規 package install を行わない（`json` は stdlib・既存 `pandas`/`joblib` 削除のみ） | accept | package install task 無し・slopcheck / blocking human checkpoint 不要・`json` は Python stdlib（信頼済み） |

</threat_model>

<verification>
- `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/features/ -q` が全ファイル GREEN（test_allowlist / test_builder / test_category_map_consumer / test_pit_cutoff / test_rolling / test_running_style / test_snapshot_repro）
- `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/ -q` が Phase 1-2 既存テストも含めて GREEN（CR-02 が builder SQL を変更するため Phase 3 以外への影響がないか回帰確認・label_race_date_backfill 等は別 module のため影響ないはず）
- `python -c "from src.features.availability import load_feature_availability as f; from src.features.rolling import _ROLLING_SYSTEMS as r; from src.features.availability import _ROLLING_SYSTEMS_FOR_RESERVED as a; feats=[e['feature_name'] for e in f()['features']]; rolling_in_registry={x.removeprefix('rolling_').removesuffix('_mean_5') for x in feats if x.startswith('rolling_') and x.endswith('_mean_5')}; assert rolling_in_registry == set(r) == set(a), (rolling_in_registry, set(r), set(a)); print('parity OK:', sorted(r))"` が exit 0 で `parity OK: ['days_since_prev', 'harontimel3', 'jyocd', 'jyuni3c_jyuni4c', 'kakuteijyuni', 'kyori']` を出力
- `python -c "import inspect; from src.features import category_map_consumer as c; assert 'joblib' not in inspect.getsource(c); print('joblib removed')"` が exit 0
- （推奨・任意）`uv run python scripts/run_feature_build.py --snapshot-id 20260619-1a-v2 --label-version v1.0.0 --fa-version 0.2.0` を実行して新 snapshot を生成し、manifest の `feature_count` が旧 63 から 63-6=57（または実 populated 列に基づく正確な値）に減少し `category_map_artifact: snapshots/category_map_<id>.json` になることを確認・さらに PyArrow probe で全 feature 列が non-null を持つことを確認（CR-01 parity の実データでの最終証明）。これは checkpoint:human-verify ではなく任意実行・SUMMARY に結果を記録。
</verification>

<success_criteria>
- CR-01: registry・rolling.py・availability reserved の三者から rolling_timediff_* / rolling_babacd_* 計6エントリが削除され、3者が6系統で完全一致する。end-to-end regression guard test が将来の silent empty feature 再発を検出する。Deferred note が Phase 2 ETL 拡張後の再登録を明記する。
- WR-01: estimated_running_style が rolling と同一の PIT pre-filter（strict `<`・per-observation）を適用し、registry 宣言と実装が一致する。
- CR-02: _fetch_feature_sources / _fetch_history の JOIN 両側に project_window_filter が適用され、CR-06 契約に合致する。
- CR-03: build_frozen_category_maps が race_date 欠損 frame で fail-loud（ValueError）し、D-13 silent fallback 禁止に合致する。
- CR-04: load_category_maps が joblib.load (pickle) を呼ばず JSON で読込む。persist_category_maps が byte-reproducible な JSON（sort_keys=True）を書出す。module から joblib 依存が完全に除去される。artifact 拡張子が `.json` に統一され manifest 参照も更新される。
- VERIFICATION must-have #1 が PARTIAL → VERIFIED に推移する（再 verify 時）。
- Phase 3 全 must-have（#1-#3）が VERIFIED になり Phase 3 が `passed` に推移する。
</success_criteria>

<output>
Create `.planning/phases/03-as-of-features-snapshots/03-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol / Path | Kind | Purpose |
|---------------|------|---------|
| `src/config/feature_availability.yaml`（6エントリ削除） | config edit | rolling_timediff_{mean,latest,sd}_5 / rolling_babacd_{mean,latest,sd}_5 計6エントリを削除・registry↔実体 parity 復元・Deferred note コメント追加（CR-01） |
| `src/features/rolling.py::_ROLLING_SYSTEMS` | tuple constant edit | `"timediff"` / `"babacd"` を削除し6系統化（kakuteijyuni / harontimel3 / jyuni3c_jyuni4c / kyori / jyocd / days_since_prev）・CR-01 |
| `src/features/rolling.py::_SYSTEM_SOURCE` | dict constant edit | `"timediff"` / `"babacd"` エントリを削除・CR-01 |
| `src/features/availability.py::_ROLLING_SYSTEMS_FOR_RESERVED` | tuple constant edit | rolling.py と一致する6系統に更新・IN-01 重複定義 drift 解消・CR-01 parity |
| `src/features/builder.py::build_feature_matrix`（estimated_running_style block） | function edit | rolling と同一の PIT pre-filter（`history.as_of_datetime < obs.feature_cutoff_datetime`・per-observation・strict `<`）を groupby(kettonum) 前に適用・WR-01 look-ahead leak 解消 |
| `src/features/builder.py::_fetch_feature_sources` | function edit | WHERE 句を `project_window_filter('ur') AND project_window_filter('nr')` に変更・CR-02・CR-06 契約 |
| `src/features/builder.py::_fetch_history` | function edit | 同上・JOIN 右側 nr に project_window_filter('nr') 適用・CR-02 |
| `src/features/category_map_consumer.py::build_frozen_category_maps` | function edit | race_date 欠損 frame で `ValueError` を raise（fail-loud）・CR-03・D-13・silent all-train fallback 削除 |
| `src/features/category_map_consumer.py::persist_category_maps` | function edit | `joblib.dump` → `json.dumps(sort_keys=True, ensure_ascii=False)` に置換・byte-reproducible・human-auditable・CR-04 |
| `src/features/category_map_consumer.py::load_category_maps` | function edit | `joblib.load` → `json.loads` に置換・pickle ACE vector 解消・CR-04・`FrozenCategoryMap(m)` で dict から再構築 |
| `src/features/category_map_consumer.py`（import block） | import edit | `import joblib` 削除・`import json` 追加・CR-04 |
| `src/features/category_map_consumer.py::FrozenCategoryMap.__getstate__`/`__setstate__` | method deletion | pickle/joblib 専用のため削除・JSON 移行で不要・CR-04 |
| `scripts/run_feature_build.py`（category_map_path） | path edit | `category_map_<id>.joblib` → `category_map_<id>.json`・manifest `category_map_artifact` 参照も .json に更新・CR-04 |
| `tests/features/test_builder.py::test_no_registered_feature_column_all_nan_end_to_end` | test (GREEN・新規) | CR-01 回帰防止・合成 DB-mock で end-to-end builder path を通し登録 feature 列が 100% NaN にならないことを assert |
| `tests/features/test_builder.py::test_registry_rolling_systems_match_rolling_impl` | test (GREEN・新規) | 3者 parity guard（registry・rolling.py・availability reserved）・IN-01 drift 検出 |
| `tests/features/test_builder.py::test_no_timediff_babacd_in_registry_or_rolling` | test (GREEN・新規) | CR-01 DELETE verify・timediff/babacd が registry と rolling から完全除去されたこと |
| `tests/features/test_builder.py::test_fetch_history_and_feature_sources_filter_both_join_sides` | test (GREEN・新規) | CR-02 regression guard・両関数の source に `project_window_filter('nr')` が含まれること |
| `tests/features/test_running_style.py::test_estimated_running_style_applies_pit_prefilter` | test (GREEN・新規) | WR-01 regression guard・PIT pre-filter 適用と cutoff 後レース除外の実証 |
| `tests/features/test_category_map_consumer.py::test_build_frozen_maps_raises_on_missing_race_date` | test (GREEN・新規) | CR-03 fail-loud・race_date 欠損 frame で ValueError |
| `tests/features/test_category_map_consumer.py::test_persist_and_load_category_maps_json_roundtrip` | test (GREEN・新規) | CR-04 JSON round-trip 等価性 |
| `tests/features/test_category_map_consumer.py::test_load_category_maps_does_not_use_joblib` | test (GREEN・新規) | CR-04 regression guard・joblib 依存完全除去 |
| `tests/features/test_category_map_consumer.py::test_persisted_artifact_is_human_auditable_json` | test (GREEN・新規) | CR-04 副次効果・sort_keys=True JSON・sentinel 可読 |
| `tests/features/test_rolling.py`（既存テスト修正） | test edit | timediff 系統依存の既存テストを残存系統（kakuteijyuni 等）に切替・PIT defense-in-depth の intent を保全 |
| `.planning/phases/03-as-of-features-snapshots/03-CONTEXT.md`（## Deferred Ideas） | doc edit | Phase 2 ETL 拡張後の rolling_timediff_* / rolling_babacd_* 再登録予約 note を追記 |
