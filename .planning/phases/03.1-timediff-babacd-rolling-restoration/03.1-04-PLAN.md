---
phase: 03.1
plan: 04
type: execute
wave: 3
depends_on: ["03.1-01", "03.1-02", "03.1-03"]
files_modified: []
autonomous: false
requirements: [FEAT-01]
user_setup:
  - service: PostgreSQL (live readonly/etl)
    why: "normalized ETL 再実行 + live snapshot rebuild（554,267行×列拡張）"
    env_vars:
      - name: KEIBA_DB_DSN_READONLY
        source: "host の ~/.pgpass / env（既存・Phase 1-3 で利用済み）"
      - name: KEIBA_DB_DSN_ETL
        source: "host の ~/.pgpass / env（既存・Phase 1-3 で利用済み）"

must_haves:
  truths:
    - "normalized ETL が再実行され、n_uma_race(554,267行)に timediff・n_race(39,593行)に baba3 が取り込まれる（raw 不変性維持・staging-swap idempotent）"
    - "新 snapshot の manifest feature_count が 63（全列数・rolling 32列=8系統×4軸 + 静的/label/PK/meta 31列）を反映する"
    - "新 snapshot で rolling_timediff_*/rolling_babacd_* の6 feature 列が source 存在行で non-null を持つ（silent-NaN breach 無し）"
    - "新 snapshot が byte-reproducible（write_snapshot 2回呼出で SHA256 一致・SC#3）"
    - "registry↔Parquet parity が保たれる（宣言 feature 数 == populated feature 列数・SC#4）"
    - "ETL 再実行で raw(public.n_*) が一切変更されない（raw_touched=False）"
  artifacts:
    - path: "snapshots/feature_matrix_<new-snapshot-id>.parquet"
      provides: "rolling 32 feature（8系統×4軸・timediff/babacd 含む）+ 静的/PK/label/meta 31列 の stamped Parquet・§12.4 metadata・feature_count=63"
      contains: "rolling_timediff"
    - path: "snapshots/feature_matrix_<new-snapshot-id>.manifest.yaml"
      provides: "SHA256・feature_count=63・feature_snapshot_id・dataset_version・category_map_artifact（repo-root 相対）"
      contains: "feature_count"
    - path: "snapshots/category_map_<new-snapshot-id>.json"
      provides: "frozen category map（sort_keys=True・byte-reproducible）"
      contains: "category_map"
  key_links:
    - from: "scripts/run_feature_build.py"
      to: "snapshots/feature_matrix_<new-id>.parquet"
      via: "persist→exists assert→manifest 順序（CR-01新・Plan 02）で stamped snapshot を生成"
      pattern: "feature_matrix"
    - from: "src/etl/normalize.py::run_normalized_etl"
      to: "normalized.n_uma_race.timediff / normalized.n_race.baba3"
      via: "staging-swap idempotent で raw から SELECT のみ書込・2回実行で同一 rowcount"
      pattern: "run_normalized_etl"
---

<objective>
Phase 3.1 の最終成果物: live-DB で normalized ETL 再実行 + snapshot rebuild を行い、SC#3（byte-reproducibility）・SC#4（registry↔Parquet parity）・SC#2（rolling 6→8系統化（新規 timediff/babacd）・新規6 feature non-null）を実証する。per D-04/Claude's Discretion（許可済み live-DB 操作は Claude 直接実行・VALIDATION.md Manual-Only 参照）。

Purpose: Phase 4 モデルが利用可能な stamped Parquet（rolling 32 feature=8系統×4軸・§12.4 metadata）を生成し、再現性（§19.1）と PIT-correctness を live-DB で実証する。Plan 01-03 のコード変更が実データで正しく機能することを確認する最終ゲート。
Output: snapshots/ 配下の新 snapshot（Parquet + manifest + category_map JSON・feature_count=63）。checkpoint:human-verify で実施・目視確認。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-timediff-babacd-rolling-restoration/03.1-CONTEXT.md
@.planning/phases/03.1-timediff-babacd-rolling-restoration/03.1-RESEARCH.md
@.planning/phases/03.1-timediff-babacd-rolling-restoration/03.1-VALIDATION.md
@scripts/run_feature_build.py
@src/etl/normalize.py
@src/features/snapshot.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: normalized ETL 再実行 + live snapshot rebuild + SC#3/SC#4 検証（許可済み live-DB）</name>
  <files>snapshots/feature_matrix_<new-snapshot-id>.parquet, snapshots/feature_matrix_<new-snapshot-id>.manifest.yaml, snapshots/category_map_<new-snapshot-id>.json</files>
  <action>
    Plan 01（normalize.py ETL 拡張: timediff + baba3）・Plan 02（run_feature_build.py CR-01新 persist→manifest 順序化）・Plan 03（builder/rolling/availability/yaml 拡張 + advisory hardening + テスト）が完了済み。本タスクは Claude が許可済み live-DB 操作（readonly/etl ロール）を直接実行し、人間が目視確認する。実行手順は `<how-to-verify>` の6ステップ（normalized ETL 再実行 → snapshot rebuild → SC#2 → SC#3 → SC#4 → full suite GREEN）。per D-04/Claude's Discretion・MEMORY.md run-authorized-ops-directly（許可済み live-DB は人手に回さず自分で実行）。
  </action>
  <what-built>
    Plan 01（normalize.py ETL 拡張: timediff + baba3）・Plan 02（run_feature_build.py CR-01新 persist→manifest 順序化）・Plan 03（builder/rolling/availability/yaml 拡張 + advisory hardening + テスト）が完了済み。本タスクは Claude が許可済み live-DB 操作（readonly/etl ロール）を直接実行し、人間が目視確認する。
  </what-built>
  <read_first>
    - scripts/run_feature_build.py（Plan 02 で変更後の persist→manifest 順序・`--snapshot-id` 引数・出力先 snapshots/）
    - src/etl/normalize.py（Plan 01 で変更後の timediff/baba3 定義・`run_normalized_etl` entry point・`_idempotent_load`）
    - src/features/snapshot.py（§12.4 metadata・`write_snapshot`・`_DETERMINISTIC_CREATED_AT`・SHA256 byte-repro 機構）
    - 03.1-VALIDATION.md（Manual-Only Verifications: live snapshot rebuild の手順・SHA256/feature_count/parity 確認項目）
    - MEMORY.md run-authorized-ops-directly（許可済み live-DB 操作は Claude が自分で実行・人手に回さない）
  </read_first>
  <how-to-verify>
    Claude が以下を順に実行（許可済み live-DB 操作・CLAUDE.md Core Value「再現性＝聖域」遵守）:

    1. **normalized ETL 再実行**: `uv run python -c "from src.etl.normalize import run_normalized_etl; run_normalized_etl()"`（または同等の entry point・etl ロール）。完了後、以下を確認:
       - `SELECT COUNT(*) FROM normalized.n_uma_race` == 554,267（既存と同一・idempotent）
       - `SELECT COUNT(*) FROM normalized.n_race` == 39,593（既存と同一・idempotent）
       - `SELECT COUNT(*) FROM normalized.n_uma_race WHERE timediff IS NOT NULL` == 554,267（全行 non-NULL・実データ検証通り）
       - `SELECT COUNT(*) FROM normalized.n_race WHERE sibababacd IS NOT NULL OR dirtbabacd IS NOT NULL OR trackcd IS NOT NULL` > 0（baba3 が取り込まれている）
       - raw 不変性: `SELECT COUNT(*) FROM public.n_uma_race` と `public.n_race` が ETL 前後で不変（raw_touched=False・host 側で事前/事後の checksum または信頼できる既知値との比較）

    2. **live snapshot rebuild**: 新 `feature_snapshot_id`（例: `2026-06-19-v03.1` 等・dataset_version 更新）で `uv run python scripts/run_feature_build.py --snapshot-id <new-id> ...` を実行。出力: `snapshots/feature_matrix_<new-id>.parquet` + `manifest.yaml` + `category_map_<new-id>.json`（persist→exists assert→manifest 順序・Plan 02）。

    3. **SC#2 確認（rolling 6→8系統化（新規 timediff/babacd）・新規6 feature non-null）**: 新 snapshot の Parquet を読み、以下を確認:
       - `rolling_timediff_mean_5`/`rolling_timediff_latest_5`/`rolling_timediff_sd_5`/`rolling_babacd_mean_5`/`rolling_babacd_latest_5`/`rolling_babacd_sd_5` の6列が存在
       - 各列が source 存在行（過去走を持つ馬）で non-null（`test_no_registered_feature_column_all_nan_end_to_end` 相当・silent-NaN breach 無し）
       - manifest の `feature_count` == 63（全列数・rolling 32列=8系統×4軸 含む）

    4. **SC#3 確認（byte-reproducibility）**: `write_snapshot` を同一入力で2回呼出し、両者の SHA256 が一致することを確認（`test_byte_reproducible_by_hash` 相当・_DETERMINISTIC_CREATED_AT sentinel で時刻依存性除去）。または run_feature_build.py を2回実行して SHA256 一致を確認。

    5. **SC#4 確認（registry↔Parquet parity）**: feature_availability.yaml の宣言 feature 数と Parquet の populated feature 列数が一致することを確認。`assert_matrix_columns_registered`(HIGH #3) が通ること。

    6. **full suite GREEN**: `uv run pytest -q` が全件 GREEN（Plan 02/03 のテスト更新含む・live-DB 参照 test 含む）。

    人間は Claude の実行ログと上記6項目の結果を目視確認する。
  </how-to-verify>
  <acceptance_criteria>
    - normalized.n_uma_race の COUNT == 554,267（idempotent・timediff 列が追加されている）
    - normalized.n_race の COUNT == 39,593（idempotent・baba3 列が追加されている）
    - normalized.n_uma_race.timediff が全行 non-NULL（554,267件）
    - raw(public.n_*) の COUNT が ETL 前後で不変（raw_touched=False）
    - 新 snapshot に rolling_timediff_*/rolling_babacd_* の6 feature 列が存在し、source 存在行で non-null
    - 新 snapshot の manifest feature_count == 63（全列数・rolling 8系統×4軸=32 + 静的/label/PK/meta=31）
    - write_snapshot 2回呼出で SHA256 一致（SC#3 byte-repro）
    - registry↔Parquet parity 成立（SC#4・宣言 feature 数 == populated feature 列数）
    - `uv run pytest -q` が全件 GREEN
  </acceptance_criteria>
  <resume-signal>Type "approved"（全6項目 GREEN の場合）または課題を記述（silent-NaN/parity 不成立/raw 変更等があれば）</resume-signal>
  <verify>
    <automated>uv run pytest -q</automated>
    <human-check>新 snapshot の feature_count=63・6 feature 列 non-null・SHA256 byte-repro・registry parity を目視確認</human-check>
  </verify>
  <done>SC#1(normalized 拡張)・SC#2(rolling 6→8系統化（新規 timediff/babacd）・新規6 feature non-null)・SC#3(byte-repro)・SC#4(registry parity) 全て成立・full suite GREEN・人間 approved。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw(public) → normalized | ETL 再実行で raw は readonly・staging-swap で normalized に idempotent 書込・timediff/baba3 も同一 |
| normalized → Parquet snapshot | readonly ロールで SELECT・PyArrow 決定論的書込・§12.4 metadata 埋め込み・SHA256 byte-repro |
| Parquet → Phase 4 モデル | stamped snapshot が再現性の単一の真の源・feature_count=63（全列数）で rolling 8系統×4軸（32列）が利用可能 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-13 | Tampering | ETL 再実行で raw が破壊的に更新される | mitigate | staging-swap idempotent（CREATE staging → TRUNCATE → INSERT → DROP/RENAME swap・advisory lock・rowcount verify）が raw に書込まない構造・raw COUNT 前後比較で検証 |
| T-03.1-14 | Tampering | silent-NaN breach 再発（6 feature が全 NaN） | mitigate | test_no_registered_feature_column_all_nan_end_to_end が GREEN・新 snapshot の6 feature 列が source 存在行で non-null を目視確認（SC#2） |
| T-03.1-15 | Tampering | byte-reproducibility 破壊（SC#3） | mitigate | write_snapshot 2回呼出 SHA256 一致・_DETERMINISTIC_CREATED_AT sentinel・PyArrow 決定論的書込 |
| T-03.1-16 | Tampering | registry↔Parquet parity 不成立（SC#4） | mitigate | assert_matrix_columns_registered(HIGH #3)・宣言 feature 数 == populated feature 列数の確認 |
| T-03.1-17 | Information Disclosure | live-DB DSN がログに漏洩 | mitigate | 既存の dsn_masked 機構（Phase 1）・DSN ログ出力厳禁 |
| T-03.1-PM | Tampering | npm/pip/cargo installs | accept | 本 Plan は新規パッケージインストールなし・Package Legitimacy Gate 対象外 |
</threat_model>

<verification>
- normalized ETL 再実行: n_uma_race(554,267)/n_race(39,593) が idempotent・timediff/baba3 追加・raw 不変
- 新 snapshot: feature_count=63・6 feature 列 non-null・SHA256 byte-repro（SC#3）・registry parity（SC#4）
- `uv run pytest -q` 全件 GREEN
- checkpoint:human-verify で人間が目視 approved
</verification>

<success_criteria>
- SC#1: normalized に timediff/baba3 が取り込まれた（Plan 01 コードが live-DB で機能）
- SC#2: rolling 6→8系統化（新規 timediff/babacd）・新規6 feature が registry/rolling/availability の3者で再登録され新 snapshot で non-null
- SC#3: 新 snapshot が byte-reproducible（SHA256 一致）
- SC#4: manifest feature_count=63・registry↔Parquet parity 成立
- advisory 4件（WR-01'/WR-02/CR-01新/WR-03）hardening 完了・full suite GREEN
- raw 不変性維持（raw_touched=False）
- 人間が checkpoint で approved
</success_criteria>

<output>
Create `.planning/phases/03.1-timediff-babacd-rolling-restoration/03.1-04-SUMMARY.md` when done（snapshot-id・SHA256・feature_count・parity 結果・advisory resolved 状態を記録）
</output>
