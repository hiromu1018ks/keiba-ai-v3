---
phase: 03.1
plan: 03
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - src/features/builder.py
  - src/features/rolling.py
  - src/features/availability.py
  - src/config/feature_availability.yaml
  - src/config/label_spec.yaml
  - tests/features/test_builder.py
  - tests/features/test_allowlist.py
autonomous: true
requirements: [FEAT-01]
user_setup: []

must_haves:
  truths:
    - "builder.py で timediff_raw(varchar) → timediff(real) parse が sentinel 0000/9999 を NaN 化して行われる"
    - "builder.py で babacd が trackcd 第1桁分岐(1/2→sibababacd・5→dirtbabacd・不能→__MISSING__)で派生される"
    - "rolling.py の _ROLLING_SYSTEMS が 6→8系統(timediff/babacd 追加)・_SYSTEM_SOURCE に両系統の source が定義される"
    - "availability.py の _ROLLING_SYSTEMS_FOR_RESERVED が rolling.py と順序含め完全一致(8系統)"
    - "feature_availability.yaml に rolling_timediff_{mean,latest,sd}_5 / rolling_babacd_{mean,latest,sd}_5 の6エントリが rolling_jyocd_* と同一 schema で再登録される"
    - "3者 parity test(test_registry_rolling_systems_match_rolling_impl)が 8系統で GREEN"
    - "WR-01'(builder.py L353-354 silent no-filter fallback)が fail-loud化される"
    - "WR-02(build_feature_matrix で空結果/全NaN をチョークポイント検出 fail-loud)が実装される"
    - "WR-03(rolling.py L236-240 groupby().apply)が vector化され count 計算が同一結果を維持する"
  artifacts:
    - path: "src/features/builder.py"
      provides: "timediff parse・babacd trackcd 分岐派生・WR-01'/WR-02 fail-loud・history SELECT に timediff_raw/baba3 追加"
      contains: "timediff"
    - path: "src/features/rolling.py"
      provides: "_ROLLING_SYSTEMS 8系統・_SYSTEM_SOURCE 拡張・WR-03 vector化"
      contains: "babacd"
    - path: "src/features/availability.py"
      provides: "_ROLLING_SYSTEMS_FOR_RESERVED 8系統（rolling.py と parity）"
      contains: "timediff"
    - path: "src/config/feature_availability.yaml"
      provides: "rolling_timediff_*/rolling_babacd_* 計6エントリ再登録"
      contains: "rolling_babacd"
    - path: "src/config/label_spec.yaml"
      provides: "timediff_sentinels に 0000/0000.0 追加"
      contains: "timediff_sentinels"
    - path: "tests/features/test_builder.py"
      provides: "parity 6→8・silent-NaN guard fixture 拡張・timediff_babacd 反転(存在assert化)・WR-01'/WR-02 fail-loud テスト"
      contains: "timediff"
    - path: "tests/features/test_allowlist.py"
      provides: "test_history_allowed_rolling_present を timediff/babacd 存在assert に切替"
      contains: "rolling_timediff"
  key_links:
    - from: "src/features/rolling.py::_ROLLING_SYSTEMS"
      to: "src/features/availability.py::_ROLLING_SYSTEMS_FOR_RESERVED"
      via: "tuple 比較で順序含め一致・test_registry_rolling_systems_match_rolling_impl が機械保証"
      pattern: "babacd"
    - from: "src/features/builder.py::_construct_derived_columns"
      to: "src/features/rolling.py::build_rolling_features"
      via: "timediff(real parse 済み)・babacd(trackcd 分岐派生済み) が rolling source として渡る"
      pattern: "_construct_derived_columns"
    - from: "src/config/feature_availability.yaml"
      to: "src/features/rolling.py::_ROLLING_SYSTEMS"
      via: "registry の feature_group(rolling_timediff/rolling_babacd) と rolling.py の system 名が一致"
      pattern: "rolling_timediff"
---

<objective>
Phase 3.1 の主作業: feature 層の core 拡張と advisory 4件の hardening を実施する。(1) builder.py で timediff parse + babacd trackcd 分岐派生 + history SELECT 拡張、(2) rolling.py で 8系統化 + WR-03 vector化、(3) availability.py/feature_availability.yaml で 3者 parity 再登録、(4) label_spec.yaml で sentinel 0000 追加、(5) builder.py で WR-01'/WR-02 fail-loud、(6) テスト5関数更新。per D-01〜D-04・SC#2（rolling 18→24）・SC#1/SC#4（parity）。

Purpose: Phase 4 モデルが馬場適性信号（timediff/babacd）を利用可能にする。既存 PIT pipeline（strict `< cutoff`・per-observation latest-K・3者 parity）を拡張し、新規 leak を生じさせない（03-05 regression guard が検出）。advisory hardening でリーク防止・再現性の defense-in-depth を強化。
Output: feature 層 5ファイル + テスト2ファイルの拡張。新規パッケージなし。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-timediff-babacd-rolling-restoration/03.1-CONTEXT.md
@.planning/phases/03.1-timediff-babacd-rolling-restoration/03.1-RESEARCH.md
@.planning/phases/03.1-timediff-babacd-rolling-restoration/03.1-PATTERNS.md
@.planning/todos/pending/phase3-advisory-hardening.md
@src/features/builder.py
@src/features/rolling.py
@src/features/availability.py
@src/config/feature_availability.yaml
@src/config/label_spec.yaml
@tests/features/test_builder.py
@tests/features/test_allowlist.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: builder.py で timediff parse + babacd trackcd 分岐派生 + history SELECT 拡張 + WR-01'/WR-02 fail-loud</name>
  <files>src/features/builder.py</files>
  <read_first>
    - src/features/builder.py（`_HISTORY_DB_SELECT_COLUMNS` L74-89・`_DERIVED_HISTORY_COLUMN_NAMES` L120-122・`_construct_derived_columns` L163-205・`days_since_prev` 派生 L192-204・`make_race_nkey` L136-160・estimated_running_style PIT filter L347-354・`_fetch_feature_sources` L455-457・`_fetch_history` L485-487・`build_feature_matrix` 呼出点 L297/L322・canonical key assert L378-381）
    - 03.1-PATTERNS.md（builder.py 拡張ポイント A/B/C/D・analog: days_since_prev 派生・make_race_nkey vector化・canonical key assert）
    - 03.1-RESEARCH.md（Pattern 1 派生 history カラム・Code Examples timediff parse・babacd trackcd 分岐・Pitfall 1/2/3・WR-01'/WR-02）
    - .planning/todos/pending/phase3-advisory-hardening.md（WR-01'/WR-02 修正方針・テスト条件）
    - src/features/availability.py（`_DERIVED_HISTORY_COLUMN_NAMES` と整合・TARGET_OBS_BANNED_COLUMNS の sibababacd/dirtbabacd 確認）
  </read_first>
  <behavior>
    - Test 1: timediff_raw="+001" → timediff=0.1(秒)・sentinel "0000"/"9999" → NaN
    - Test 2: trackcd="10"(芝)・sibababacd="1" → babacd="1"・trackcd="52"(ダ)・dirtbabacd="2" → babacd="2"・trackcd 欠損/"0" → babacd="__MISSING__"
    - Test 3: as_of_datetime 列を持たない frame で estimated_running_style が ValueError で fail（WR-01'）
    - Test 4: _fetch_feature_sources が空 DF を返す状態で build_feature_matrix が RuntimeError で fail（WR-02・チョークポイント検出）
    - timediff_raw/baba3(sibababacd/dirtbabacd/trackcd) が history SELECT に追加され、_HISTORY_SELECT_COLUMNS から derived 名(timediff/babacd) は除外される（Pitfall 3）
  </behavior>
  <action>
    (A) `_HISTORY_DB_SELECT_COLUMNS`(L74-89) に `"ur.timediff AS timediff_raw"`・`"nr.sibababacd AS sibababacd"`・`"nr.dirtbabacd AS dirtbabacd"`・`"nr.trackcd AS trackcd"` を追加（raw カラム名 `timediff` は派生名と衝突するため alias `timediff_raw`・Pitfall 3）。`_DERIVED_HISTORY_COLUMN_NAMES`(L120-122) に `timediff`/`babacd` は**既に含まれている**ことを確認（これにより `_HISTORY_SELECT_COLUMNS` から derived 名が除外され `timediff_raw`/baba3 のみが残る・L213-215 の `assert TARGET_OBS_BANNED_COLUMNS.isdisjoint(_HISTORY_SELECT_COLUMNS)` が sibababacd/dirtbababd を弾くため注意: babacd 派生は SELECT した baba3 から行う・babacd 自体は SELECT しない）。

    (B) `_construct_derived_columns`(L163-205) 内に `days_since_prev` 派生(L192-204) と同列の guard idiom（`if "X_raw" in result.columns and "X" not in result.columns:`）で timediff/babacd 派生を追加。timediff parse: `raw_td = result["timediff_raw"].astype(str)` → sentinel 集合 `{"0000","9999"}`（builder 側定数 `_TIMEDIFF_SENTINELS = frozenset({"0000","9999"})`・Open Q1 推奨・label_spec.timediff_sentinels とは別関心事）→ `is_sentinel = raw_td.isin(sentinels)` → `sign = raw_td.str[0]`・`digits = pd.to_numeric(raw_td.str.slice(1,4), errors="coerce")` → `signed = digits.where(sign != "-", -digits)` → `result["timediff"] = (signed / 10.0).where(~is_sentinel, np.nan)`（NNN/10 秒・per D-03・RESEARCH.md Code Example `parse_timediff_series`）。babacd trackcd 分岐派生: `tc = result["trackcd"].astype(str).str.strip()`・`lead = tc.str[0]` → 芝 mask `lead.isin(["1","2"])` で `sibababacd` → ダート mask `lead == "5"` で `dirtbabacd`（dirtbabacd='0'/欠損なら `__MISSING__`）→ その他/欠損/未設定('0'・第1桁 '0'/'3'/'4')→ `__MISSING__` sentinel（D-13・RESEARCH.md Code Example `derive_babacd`・Pitfall 2）。`MISSING` は `src/utils/category_map` から import（"`__MISSING__`"定数）。

    (C) WR-01' fail-loud（L347-354）: `else: pit_filtered_style = expanded_style` を削除し、`if "as_of_datetime" not in expanded_style.columns: raise ValueError("history に as_of_datetime 列が無い・PIT filter を適用できない (WR-01' fail-loud)")` に変更（PATTERNS.md 拡張ポイント C の変更後例通り・到達不能 silent 分岐を除去）。

    (D) WR-02 fail-loud（チョークポイント検出・D-04）: `_fetch_feature_sources`(L455-457) と `_fetch_history`(L485-487) の `except Exception → 空 DF 返却` は**維持**（D-04 採択）。代わりに `build_feature_matrix` 内の呼出点（L297 `feature_matrix = _fetch_feature_sources(read_pool)` の直後）に `if len(feature_matrix) == 0: raise RuntimeError("feature source fetch が空結果を返した・DB例外/0行結果の silent empty を検知 (WR-02)")` を追加（PATTERNS.md 拡張ポイント D の例通り・analog: L378-381 canonical key assert）。history 側(L322 直後)は history=0 が新馬のみ observation で正当なケースがあるため、feature_matrix 側の全NaN 検知と組み合わせる形で「feature_matrix が空でないのに全 rolling 列が NaN」を検知する条件を追加（チョークポイント単一化・D-04）。
  </action>
  <verify>
    <automated>uv run pytest tests/features/test_builder.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `_HISTORY_DB_SELECT_COLUMNS` に `ur.timediff AS timediff_raw`・`nr.sibababacd AS sibababacd`・`nr.dirtbabacd AS dirtbabacd`・`nr.trackcd AS trackcd` が含まれる
    - `_DERIVED_HISTORY_COLUMN_NAMES` に `timediff`/`babacd` が含まれる（既存確認・babacd は SELECT から除外される）
    - `_construct_derived_columns` に timediff parse（sentinel `{"0000","9999"}` → NaN・NNN/10秒）と babacd trackcd 第1桁分岐（1/2→sibababacd・5→dirtbabacd・不能→`__MISSING__`）が実装される
    - WR-01': `else: pit_filtered_style = expanded_style` が削除され `as_of_datetime` 不在で `ValueError` が raise される
    - WR-02: `build_feature_matrix` に `if len(feature_matrix) == 0: raise RuntimeError(...)` が追加される
    - `uv run pytest tests/features/test_builder.py -q` が GREEN（Task 3 で更新するテスト含め）
    - 構文エラー無し（`ast.parse` exit 0）
  </acceptance_criteria>
  <done>timediff parse・babacd 派生・history SELECT 拡張・WR-01'/WR-02 fail-loud が実装され、builder 単体テストが GREEN。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: rolling.py 8系統化 + _SYSTEM_SOURCE 拡張 + WR-03 vector化</name>
  <files>src/features/rolling.py</files>
  <read_first>
    - src/features/rolling.py（`_ROLLING_SYSTEMS` L63-70・`_SYSTEM_SOURCE` L74-81・`build_rolling_features` L84-280・count 計算 L236-240・mean/latest/sd L242-252・strict `< cutoff` pre-filter L199-201・MISSING 初期化 L174）
    - 03.1-PATTERNS.md（rolling.py 拡張ポイント A/B/C・analog: jyocd 序数扱い・WR-03 vector化）
    - 03.1-RESEARCH.md（rolling.py 再登録最小差分・WR-03 vector化・Pitfall 4 SHA256 drift）
    - .planning/todos/pending/phase3-advisory-hardening.md（WR-03 修正方針・テスト条件: 同一入力 SHA256 一致）
  </read_first>
  <behavior>
    - Test 1: `_ROLLING_SYSTEMS` に `timediff`/`babacd` が含まれ、計8系統になる
    - Test 2: `_SYSTEM_SOURCE["timediff"] == ("timediff",)`・`_SYSTEM_SOURCE["babacd"] == ("babacd",)`
    - Test 3: WR-03 vector化後、同一入力 DataFrame で count 計算結果が vector化前と完全一致（SHA256 維持・Pitfall 4）
  </behavior>
  <action>
    (A) `_ROLLING_SYSTEMS`(L63-70) に `"timediff"`, `"babacd"` を末尾に追加（6→8系統・PATTERNS.md 拡張ポイント A・RESEARCH.md Code Example）。(B) `_SYSTEM_SOURCE`(L74-81) に `"timediff": ("timediff",)`, `"babacd": ("babacd",)` を追加（analog: `"jyocd": ("jyocd",)` L79・単一 source 列の最も単純な系統・PATTERNS.md 拡張ポイント B）。`build_rolling_features`(L84-280) のループ(L218-279)は `_ROLLING_SYSTEMS` を iterate するため、系統追加だけで mean/latest/sd/count の4軸が自動生成（jyuni3c_jyuni4c 合成分岐 L224-229 以外は `pd.to_numeric(recent[system], errors="coerce")` L232 で処理・timediff(real parse済み)も babacd(序数コード)も同一 path）。

    (C) WR-03 vector化（L236-240・per D-04・Pitfall 4）: `count_per_obs = recent_sys.groupby("obs_id")["_sys_value"].apply(lambda s: int(s.notna().sum())).to_dict()` を `count_per_obs = recent_sys["_sys_value"].notna().groupby(recent_sys["obs_id"]).sum().astype(int).to_dict()` に置換（pandas 3.x 推奨・apply 廃止）。mean(L242-244)/latest(L246-248)/sd(L250-252) は**既に vector化済みのため維持**（これらを触ると SHA256 drift・Pitfall 4・WR-03 対象は count 計算 L236-240 のみ）。vector化前後で同一入力 DataFrame の count_per_obs が完全一致することを unit test で証明（同一プロセス同一 pandas では検出不可だが、vector化前後の結果比較で count 値の ±1 ズレが無いことを保証）。
  </action>
  <verify>
    <automated>uv run pytest tests/features/test_builder.py::test_registry_rolling_systems_match_rolling_impl -q && uv run python -c "from src.features.rolling import _ROLLING_SYSTEMS, _SYSTEM_SOURCE; assert 'timediff' in _ROLLING_SYSTEMS and 'babacd' in _ROLLING_SYSTEMS; assert _SYSTEM_SOURCE['timediff']==('timediff',) and _SYSTEM_SOURCE['babacd']==('babacd',); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `_ROLLING_SYSTEMS` に `timediff`/`babacd` が含まれ、長さが8（`len(_ROLLING_SYSTEMS) == 8`）
    - `_SYSTEM_SOURCE["timediff"] == ("timediff",)`・`_SYSTEM_SOURCE["babacd"] == ("babacd",)`
    - L236-240 の `groupby().apply(lambda)` が `notna().groupby().sum().astype(int)` vector形に置換される（`grep -c "lambda s: int(s.notna().sum())" src/features/rolling.py` が 0）
    - mean/latest/sd 計算(L242-252)は変更されていない（`grep -c "groupby.*mean\|groupby.*first\|groupby.*std" src/features/rolling.py` で既存 vector化が維持）
    - 構文エラー無し
  </acceptance_criteria>
  <done>rolling.py が 8系統化され、WR-03 count 計算が vector化され、mean/latest/sd は維持される（SHA256 drift 無し）。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: availability.py + feature_availability.yaml + label_spec.yaml の3者 parity 再登録 + テスト5関数更新</name>
  <files>src/features/availability.py, src/config/feature_availability.yaml, src/config/label_spec.yaml, tests/features/test_builder.py, tests/features/test_allowlist.py</files>
  <read_first>
    - src/features/availability.py（`_ROLLING_SYSTEMS_FOR_RESERVED` L130-137・`HISTORY_ALLOWED_POST_RACE_COLUMNS` L90-99・`TARGET_OBS_BANNED_COLUMNS` L79-88・`_RESERVED_NON_FEATURE_COLUMNS` L143-159 の内包表記）
    - src/config/feature_availability.yaml（rolling_jyocd_* 3軸エントリ L272-293・L197/L269 の deleted-entry placeholder）
    - src/config/label_spec.yaml（`timediff_sentinels` L130・`harontimel3_sentinels` L128）
    - tests/features/test_builder.py（`test_registry_rolling_systems_match_rolling_impl` L180-206・`test_no_timediff_babacd_in_registry_or_rolling` L209-240・`test_no_registered_feature_column_all_nan_end_to_end` L103-177・fixture L139-153）
    - tests/features/test_allowlist.py（`test_history_allowed_rolling_present` L88-107・L101-107 反転対象）
    - 03.1-PATTERNS.md（availability/feature_availability/label_spec/test_builder/test_allowlist の各拡張ポイント・analog: rolling_jyocd_*）
    - 03.1-RESEARCH.md（3者 parity・sentinel 0000・Wave 0 Gaps）
  </read_first>
  <behavior>
    - Test 1: `_ROLLING_SYSTEMS_FOR_RESERVED` が rolling.py `_ROLLING_SYSTEMS` と順序含め完全一致（8系統）
    - Test 2: feature_availability.yaml に rolling_timediff_{mean,latest,sd}_5 / rolling_babacd_{mean,latest,sd}_5 の6エントリが rolling_jyocd_* と同一 schema で存在
    - Test 3: label_spec.yaml の timediff_sentinels に "0000"/"0000.0" が追加される
    - Test 4: test_registry_rolling_systems_match_rolling_impl が 8系統で GREEN（自動追従）
    - Test 5: test_no_timediff_babacd_in_registry_or_rolling が反転（存在assert化）されて GREEN
    - Test 6: test_no_registered_feature_column_all_nan_end_to_end の fixture に timediff/babacd データが追加され GREEN（silent-NaN regression guard が新6 feature をカバー）
    - Test 7: test_history_allowed_rolling_present が timediff/babacd 存在assert に切替されて GREEN
  </behavior>
  <action>
    (A) availability.py: `_ROLLING_SYSTEMS_FOR_RESERVED`(L130-137) に `"timediff"`, `"babacd"` を rolling.py と**同一順序**で追加（8系統・`test_registry_rolling_systems_match_rolling_impl` が `tuple(...) == _ROLLING_SYSTEMS` で検査・順序不一致は RED）。`HISTORY_ALLOWED_POST_RACE_COLUMNS`(L90-99) の `timediff`(L92)/`babacd`(L97) は既存のため**変更不要**（確認のみ・CR-01 03-05 で rolling 系統だけ削除し taxonomy は保持）。`_RESERVED_NON_FEATURE_COLUMNS`(L143-159) の内包表記 `{f"rolling_{sys}_count_5" for sys in _ROLLING_SYSTEMS_FOR_RESERVED}` は、`_ROLLING_SYSTEMS_FOR_RESERVED` 拡張で `rolling_timediff_count_5`/`rolling_babacd_count_5` が自動的に reserved に追加（手動編集不要）。`TARGET_OBS_BANNED_COLUMNS`(L79-88) の `sibababacd`/`dirtbabacd`(L84-85) は target race 当日馬場=禁止として正しいため**維持**（babacd は history_allowed_post_race のみ・HIGH #4 taxonomy・PATTERNS.md Anti-Pattern）。

    (B) feature_availability.yaml: L197-198 の rolling_timediff_* placeholder・L269-270 の rolling_babacd_* placeholder を削除し、`rolling_jyocd_*`(L272-293) と**完全同一 schema** の3エントリ(系統)×2 = 6エントリを追加。各エントリのキー: `feature_name`(rolling_timediff_mean_5 等)・`feature_group`(rolling_timediff/rolling_babacd)・`available_from_timing: entry_confirmed`・`source_role: history_allowed_post_race`・`source_table: normalized.n_uma_race (history)`（babacd も同一・派生元は n_race だが rolling source は history frame 上の派生列）・`cutoff_rule: "race_date - 1 day (strict < cutoff, JST midnight)"`・`leakage_risk_level: low`（PATTERNS.md 拡張ポイント A/B）。

    (C) label_spec.yaml: `timediff_sentinels`(L130・現在 `["9999","9999.0"]`) に `"0000"`, `"0000.0"` を追加 → `["9999","9999.0","0000","0000.0"]`（実データ検証で 0000 は kakuteijyuni='00' と完全一致・376件・Pitfall 1・PATTERNS.md 拡張ポイント A）。注: この sentinel 集合は label 層(se_marker_canonicalization)用・rolling 層の timediff parse は builder 側別途定数（Task 1）。両者で "0000"/"9999" を含む点は整合。

    (D) tests/features/test_builder.py: (1) `test_registry_rolling_systems_match_rolling_impl`(L180-206) は `_ROLLING_SYSTEMS`/registry/`_ROLLING_SYSTEMS_FOR_RESERVED` の3者が8系統になれば自動 GREEN（**変更不要**の可能性大・確認のみ）。(2) `test_no_timediff_babacd_in_registry_or_rolling`(L209-240) を「含まれない」→「含まれる」に反転（関数名も `test_timediff_babacd_present_in_registry_or_rolling` 等に rename）: `assert "timediff" in _ROLLING_SYSTEMS`・`assert "babacd" in _ROLLING_SYSTEMS`・registry に `rolling_timediff_{mean,latest,sd}_5`/`rolling_babacd_{mean,latest,sd}_5` の6エントリが存在することを assert（PATTERNS.md test_builder 拡張ポイント B）。(3) `test_no_registered_feature_column_all_nan_end_to_end`(L103-177) の `history_rows` fixture(L139-153) に timediff_raw/baba3 データを追加: `"timediff_raw": f"+{val:03d}"`(例 "+001"→0.1秒)・`"sibababacd": "1"`・`"dirtbabacd": "0"`・`"trackcd": "10"`(第1桁1=芝・PATTERNS.md test_builder 拡張ポイント C)。ただし timediff/babacd は `_construct_derived_columns` で派生されるため、合成 history が `build_rolling_features` 直接呼出(L155)の場合は派生ロジックを test 内で適用するか `build_feature_matrix` 経由に切り替え（planner judgment・L155 analog）。(4) WR-01'/WR-02 用新規 unit test を追加: WR-01' は `as_of_datetime` 列を持たない合成 history で `ValueError` assert・WR-02 は `_fetch_feature_sources` が空 DF を返す状態(mock で DB例外)で `RuntimeError` assert（PATTERNS.md test_builder 拡張ポイント D）。

    (E) tests/features/test_allowlist.py: `test_history_allowed_rolling_present`(L88-107) の L101-107 の「rolling_timediff_mean_5/rolling_babacd_mean_5 が無いこと」assert を「6エントリが存在すること」assert に反転（PATTERNS.md test_allowlist 拡張ポイント A・Phase 3.1 存在assert化）。

    per D-01（完全復元）・D-02（序数扱い）・D-04（advisory）・SC#2（rolling 18→24）。
  </action>
  <verify>
    <automated>uv run pytest tests/features/test_builder.py tests/features/test_allowlist.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `_ROLLING_SYSTEMS_FOR_RESERVED` が8系統で rolling.py `_ROLLING_SYSTEMS` と順序含め完全一致（`tuple(_ROLLING_SYSTEMS_FOR_RESERVED) == _ROLLING_SYSTEMS`）
    - feature_availability.yaml に `rolling_timediff_mean_5`/`rolling_timediff_latest_5`/`rolling_timediff_sd_5`/`rolling_babacd_mean_5`/`rolling_babacd_latest_5`/`rolling_babacd_sd_5` の6エントリが存在し、各エントリが `feature_group`/`available_from_timing: entry_confirmed`/`source_role: history_allowed_post_race`/`cutoff_rule`/`leakage_risk_level: low` キーを持つ
    - label_spec.yaml の `timediff_sentinels` に `"0000"` と `"0000.0"` が含まれる
    - `test_no_timediff_babacd_in_registry_or_rolling` が反転（存在assert化・関数名 rename 含む）され GREEN
    - `test_no_registered_feature_column_all_nan_end_to_end` の fixture に timediff_raw/baba3 が追加され GREEN
    - WR-01'(ValueError)/WR-02(RuntimeError) の新規 unit test が追加され GREEN
    - `test_history_allowed_rolling_present` が6エントリ存在assert に切替され GREEN
    - `uv run pytest tests/features/test_builder.py tests/features/test_allowlist.py -q` が GREEN
  </acceptance_criteria>
  <done>3者 parity（rolling.py↔availability.py↔registry）が8系統で機械保証され、label_spec sentinel 0000 追加、テスト5関数が更新されて silent-NaN regression guard が新6 feature をカバーする。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| history row → derived column | trackcd/sibababacd/dirtbabacd から babacd を派生・timediff_raw から timediff(real) を parse・sentinel/欠損は `__MISSING__`/NaN で明示 |
| derived column → rolling source | timediff(real)/babacd(序数varchar) が rolling.py の per-observation latest-K に渡る・strict `< cutoff` で未来情報除外 |
| registry ↔ rolling.py ↔ availability.py | 3者 parity test が系統名/順序の一致を機械保証・1箇所でも更新漏れがあれば RED |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-06 | Tampering/Elevation | look-ahead leak（babacd 派生で使う過去レースの n_race 馬場が cutoff 以後） | mitigate | rolling.py strict `< feature_cutoff_datetime` pre-filter(L199-201) が不変・history 行が cutoff 以前なら馬場も cutoff 以前・timediff/babacd も同一 pipeline・03-05 regression guard が検出 |
| T-03.1-07 | Tampering | timediff sentinel 0000/9999 が 0.0/999.9 秒として rolling に混入 | mitigate | builder 側 `_TIMEDIFF_SENTINELS = frozenset({"0000","9999"})` で `isin` 判定→NaN 化（Pitfall 1）・label_spec.timediff_sentinels にも "0000" 追加で label/rolling 両層で整合 |
| T-03.1-08 | Tampering | babacd trackcd 第1桁5の芝障害(54/56)で dirtbabacd='0' → babacd=__MISSING__（信号欠損） | accept | 保守的・D-13 一貫・障害戦過去走を持つ馬は少数・GBDT が `__MISSING__` を処理・Phase 4 評価後に改善余地(D-08 deferred) |
| T-03.1-09 | Tampering | banned source sneak-in（sibababacd/dirtbabacd が target_obs に混入） | mitigate | `assert TARGET_OBS_BANNED_COLUMNS.isdisjoint(_HISTORY_SELECT_COLUMNS)`(L213-215) が機械保証・babacd は history SELECT から除外（derived扱い）・baba3 は history 側のみ |
| T-03.1-10 | Tampering | silent data loss（WR-01' silent no-filter・WR-02 except→空DF） | mitigate | WR-01' `as_of_datetime` 不在で ValueError・WR-02 build_feature_matrix チョークポイントで空結果/全NaN を RuntimeError fail-loud（D-04）・unit test で機械保証 |
| T-03.1-11 | Tampering | 3者 parity の更新漏れ（rolling.py 追加忘れで registry と不整合） | mitigate | test_registry_rolling_systems_match_rolling_impl が `tuple(...) == _ROLLING_SYSTEMS` で検査・順序含め一致しないと RED |
| T-03.1-12 | Tampering | WR-03 vector化で SHA256 drift（SC#3 byte-repro 破壊） | mitigate | count 計算のみ vector化・mean/latest/sd は既存 vector化維持・同一入力で count 結果完全一致を unit test で証明（Pitfall 4） |
| T-03.1-SC | Tampering | npm/pip/cargo installs | accept | 本 Plan は新規パッケージインストールなし |
</threat_model>

<verification>
- `uv run pytest tests/features/test_builder.py tests/features/test_allowlist.py -q` が GREEN
- `uv run pytest tests/features/test_builder.py::test_registry_rolling_systems_match_rolling_impl -q` が GREEN（8系統 parity）
- `uv run python -c "from src.features.rolling import _ROLLING_SYSTEMS; from src.features.availability import _ROLLING_SYSTEMS_FOR_RESERVED; assert tuple(_ROLLING_SYSTEMS_FOR_RESERVED)==_ROLLING_SYSTEMS; print('parity OK')"`
- WR-01'/WR-02/WR-03 の各新規 unit test が GREEN
</verification>

<success_criteria>
- timediff parse + babacd trackcd 分岐派生が builder.py に実装された（sentinel/欠損は `__MISSING__`/NaN）
- rolling.py が8系統化され WR-03 vector化された（SHA256 維持）
- 3者 parity（rolling.py↔availability.py↔registry）が8系統で機械保証される
- label_spec.yaml に timediff_sentinels 0000 が追加された
- advisory 3件（WR-01'/WR-02/WR-03）が fail-loud/vector化された（CR-01新は Plan 02）
- テスト5関数が更新され silent-NaN regression guard が新6 feature をカバーする
- 既存テストが GREEN を維持（PIT enforcement・3者 parity・byte-repro）
</success_criteria>

<output>
Create `.planning/phases/03.1-timediff-babacd-rolling-restoration/03.1-03-SUMMARY.md` when done
</output>
