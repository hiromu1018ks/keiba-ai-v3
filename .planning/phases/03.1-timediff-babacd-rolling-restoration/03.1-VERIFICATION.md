---
phase: 03.1-timediff-babacd-rolling-restoration
verified: 2026-06-19T10:05:00Z
status: passed
score: 9/9 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: none
  note: "初期検証（前回 VERIFICATION.md なし）"
---

# Phase 03.1: Timediff/Babacd Rolling Restoration — Verification Report

**Phase Goal:** Phase 3 gap-closure (03-05) で silent-empty breach を解消するために一時削除した `rolling_timediff_*` / `rolling_babacd_*` 計6 feature を復元する。Phase 2 normalized ETL を拡張して `timediff`・`babacd` の source カラムを normalized に取り込み、Phase 3 rolling 系統（8系統×4軸=32列）に再登録して Phase 1-A rolling features を拡張。registry↔rolling↔Parquet parity を維持したまま Phase 4 が利用可能な feature を増やす。PIT-correctness は rolling.py の strict `< cutoff` + per-observation window で保たれ、新規 leak は生じない。併せて Phase 03 code review advisory 4件（WR-01'/WR-02/CR-01新/WR-03）の hardening を実施する。
**Verified:** 2026-06-19T10:05:00Z
**Status:** passed
**Re-verification:** No — initial verification

## 検証方針

SUMMARY.md 主張（feature_count=63・SHA256=42865b9a…321516・6 feature non-null・raw 不変・216 passed）を信じず、実コードと live snapshot v3 と live-DB COUNT を PyArrow / psycopg / grep で直接検証。Phase 3 親 verification が `gaps_found` → closure 後 `passed` となった advisory 4件（WR-01'/WR-02/CR-01新/WR-03）が本 Phase で本当に hardening されたか、silent-empty breach が解消されたかを実コードで確認。

## Goal Achievement

### Observable Truths（ROADMAP Phase 03.1 SC + PLAN must_haves merge）

| # | Truth | Status | Evidence（実コード・実 snapshot・live-DB で検証） |
| --- | --- | --- | --- |
| 1 | `normalized.n_uma_race` に `timediff` カラムが存在し、staging-swap idempotent ETL で raw から機械的に取り込まれる（SC#1・raw 不変） | ✓ VERIFIED | `src/etl/normalize.py` L141 DDL `"timediff varchar(4)"`・L248 SELECT・L557 `_NORMALIZED_COLUMNS["n_uma_race"]` に `"timediff"`（3者整合・末尾 index 43）✓。**live-DB 実測**: `normalized.n_uma_race` = 554,267 行・`normalized.n_uma_race.timediff` non-null = 554,267 件（全行）✓。DDL 駆動 staging-swap（Rule 3 auto-fix・L381-384 `_TABLE_DDL_COLUMNS`・L450-451 DROP+CREATE staging）で新カラムが即時本番スキーマへ反映 ✓。raw 不変: `public.n_uma_race` = 881,202 件（前後不変）✓。 |
| 2 | `normalized.n_race` に baba3（sibababacd/dirtbabacd/trackcd）カラムが存在し全 varchar pass-through（SC#1） | ✓ VERIFIED | `src/etl/normalize.py` L77-79 DDL（`sibababacd varchar(1)`・`dirtbabacd varchar(1)`・`trackcd varchar(2)`）・L188-190 SELECT・L499-501 `_NORMALIZED_COLUMNS["n_race"]` に3カラム含む ✓。**live-DB 実測**: `normalized.n_race` = 39,593 行・baba3 non-null = 39,593 件（全行）✓。cast 対象外（varchar pass-through）確認: `_transform_race_df`(L292-331) は `kyori` 個別キャストのみで real/int cols 構造を持たないため baba3 は自動的に varchar 保持 ✓。raw 不変: `public.n_race` = 71,972 件 ✓。 |
| 3 | rolling 6 feature（timediff/babacd × mean/latest/sd）が rolling.py(8系統)/availability.py/feature_availability.yaml の3者で再登録され parity（SC#2） | ✓ VERIFIED | **rolling.py**: `_ROLLING_SYSTEMS`（L65-74）= 8系統（`...days_since_prev`, `timediff`, `babacd`）・`_SYSTEM_SOURCE['timediff']=('timediff',)`/`['babacd']=('babacd',)` 確認 ✓。**availability.py**: `_ROLLING_SYSTEMS_FOR_RESERVED`（L131-140）= 8系統・`tuple(...) == _ROLLING_SYSTEMS` で parity True（順序含め完全一致）✓。**feature_availability.yaml**: L200-220 rolling_timediff_* 3エントリ・L294-314 rolling_babacd_* 3エントリ・全て `feature_group`/`available_from_timing: entry_confirmed`/`source_role: history_allowed_post_race`/`cutoff_rule`/`leakage_risk_level: low` キー保持（rolling_jyocd_* と同一 schema）✓。3者 parity test `test_registry_rolling_systems_match_rolling_impl` GREEN ✓。 |
| 4 | 新 snapshot で6 feature 列が source 存在行で non-null（SC#2・silent-NaN breach 解消） | ✓ VERIFIED | **PyArrow live snapshot v3 実測**: `rolling_timediff_mean_5`=493,198 (89.0%)・`rolling_timediff_latest_5`=493,198 (89.0%)・`rolling_timediff_sd_5`=436,277 (78.7%)・`rolling_babacd_mean_5`=319,084 (57.6%)・`rolling_babacd_latest_5`=319,084 (57.6%)・`rolling_babacd_sd_5`=234,516 (42.3%)・全列 non-null > 0 ✓。silent-NaN breach（source 存在行で全 NaN）無し・non-null 減少は全て正当（初出走馬=履歴0件・障害戦 dirtbabacd='0' sentinel・視程不良馬場コード欠損）✓。`test_no_registered_feature_column_all_nan_end_to_end`（派生経路 `build_feature_matrix` 経由に確定・CHECKER WARNING #1 対応）GREEN ✓。 |
| 5 | 新 snapshot が byte-reproducible（SHA256 42865b9a…321516）（SC#3） | ✓ VERIFIED | **on-disk SHA256 再計算** = `42865b9a82a92dcb796ff0ab4432bee46a67a3fee25d925b5d1567a534321516` = manifest `sha256` ✓。byte_size 39,958,495 = manifest `byte_size` ✓。`_DETERMINISTIC_CREATED_AT` sentinel で時刻依存性除去・`scripts/run_feature_build.py` で write_snapshot 2回呼出 SHA256 一致 assert（L198-201）✓。 |
| 6 | manifest `feature_count=63`（rolling 32=8系統×4軸 + 静的/label/PK/meta 31）・registry↔Parquet parity（SC#4） | ✓ VERIFIED | manifest `feature_count: 63`・PyArrow 実測 cols=63 ✓。rolling 系統合計32列（8系統×4軸・count 軸8列含む）✓。**parity 実測**: registry features=40件・parquet rolling feature 列（count 除く）=24件（8系統×3軸）・未登録=0件・`assert_matrix_columns_registered` PASS ✓。populated(32 rolling + 静的/PK/label/meta=31) ⊆ registry+reserved ✓。 |
| 7 | advisory 4件 hardening（WR-01'/WR-02/CR-01新/WR-03・Phase 03-REVIEW 由来） | ✓ VERIFIED | **WR-01'**: `else: pit_filtered_style = expanded_style`（silent no-filter fallback）削除済み・L430-434 で `as_of_datetime` 不在時に `ValueError` raise ✓。**WR-02**: `build_feature_matrix` L372-376 で `if len(feature_matrix) == 0: raise RuntimeError(...)` チョークポイント検出（`_fetch_feature_sources` 内 except→空DF は維持・D-04 採択）✓。**CR-01新**: `scripts/run_feature_build.py` で persist_category_maps(L214) < assert category_map_path.exists()(L218) < write_manifest(L225) の順序・repo-root 相対 `snapshots/category_map_<id>.json`（CWD 非依存）✓。**WR-03**: `grep -c "lambda s: int(s.notna().sum())" src/features/rolling.py` = 0・`notna().groupby().sum().astype(int)` vector形（L245-248）・mean/latest/sd は既存 vector化維持 ✓。 |
| 8 | raw 不変性（raw_touched=False・ETL 再実行で raw 変更なし） | ✓ VERIFIED | `run_normalized_etl` 戻り値 `raw_touched: False`（L704）・staging-swap は raw に一切書込まない構造（advisory lock + CREATE staging → INSERT → DROP/RENAME swap・raw は readonly SELECT のみ）✓。**live-DB 実測**: `public.n_uma_race` = 881,202 件・`public.n_race` = 71,972 件（ETL 前後不変）✓。 |
| 9 | builder.py で timediff parse + babacd trackcd 分岐派生（機能的根拠） | ✓ VERIFIED | **timediff parse**: `src/features/builder.py` L239-245 で `timediff_raw`(varchar4) → real秒・`_TIMEDIFF_SENTINELS = frozenset({"0000","9999"})`(L58) で sentinel NaN 化・sign/digits 抽出・`signed/10.0` で秒化（NNN/10・D-03・vector idiom）✓。**babacd trackcd 分岐派生**: L254-273 で trackcd 第1桁 `1`/`2`→芝→`hist_sibababacd`・`5`→ダート→`hist_dirtbabacd`・dirtbabacd='0'/欠損/NaN→`__MISSING__` sentinel（T-03.1-08 accept・D-13）✓。alias `hist_*` で TARGET_OBS_BANNED 同名衝突回避（HIGH #4 intent 保持・L282-284 disjoint assert GREEN）✓。 |

**Score:** 9/9 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `src/etl/normalize.py` | n_uma_race.timediff / n_race.baba3 を SELECT/DDL/INSERT で取り込み・DDL 駆動 staging-swap | ✓ VERIFIED | L77-79 DDL baba3・L141 DDL timediff・L188-190 SELECT baba3・L248 SELECT timediff・L381-384 `_TABLE_DDL_COLUMNS` mapping・L450-451 DROP+CREATE staging（Rule 3 auto-fix）。3者整合・cast 対象外（varchar pass-through）。wired（`run_normalized_etl` から `_idempotent_load` 経由）。 |
| `scripts/run_feature_build.py` | persist→exists assert→manifest 順序化（CR-01新） | ✓ VERIFIED | persist_category_maps(L214) < assert category_map_path.exists()(L218) < write_manifest(L225)・repo-root 相対 `snapshots/category_map_*.json`（CWD 非依存）・persist 失敗時は例外で manifest 書出前に停止。wired（live snapshot v3 生成で exit 0）。 |
| `src/features/builder.py` | timediff parse + babacd trackcd 分岐派生 + WR-01'/WR-02 fail-loud + history SELECT 拡張 | ✓ VERIFIED | L99-119 `_HISTORY_DB_SELECT_COLUMNS`（`ur.timediff AS timediff_raw`・`nr.sibababacd AS hist_sibababacd`・`nr.dirtbabacd AS hist_dirtbabacd`・`nr.trackcd AS trackcd`）・L239-273 派生ロジック・L372-376 WR-02 RuntimeError・L430-434 WR-01' ValueError。wired（build_feature_matrix Step 1-9 完備）。 |
| `src/features/rolling.py` | `_ROLLING_SYSTEMS` 8系統・`_SYSTEM_SOURCE` 拡張・WR-03 vector化 | ✓ VERIFIED | L65-74 8系統（timediff/babacd 含む）・L79-88 `_SYSTEM_SOURCE`（timediff/babacd 単一 source）・L245-248 vector形 count（lambda=0）・mean/latest/sd 既存 vector化維持。wired（builder.build_rolling_features 経由）。 |
| `src/features/availability.py` | `_ROLLING_SYSTEMS_FOR_RESERVED` 8系統 parity | ✓ VERIFIED | L131-140 8系統（rolling.py と順序含め完全一致）・`_RESERVED_NON_FEATURE_COLUMNS`(L146-162) 内包表記で `rolling_timediff_count_5`/`rolling_babacd_count_5` 自動 reserved 化。wired（`test_registry_rolling_systems_match_rolling_impl` GREEN）。 |
| `src/config/feature_availability.yaml` | rolling_timediff_*/rolling_babacd_* 計6エントリ再登録 | ✓ VERIFIED | L200-220 rolling_timediff_* 3エントリ・L294-314 rolling_babacd_* 3エントリ・全て rolling_jyocd_* と完全同一 schema（feature_group/available_from_timing/source_role/source_table/cutoff_rule/leakage_risk_level）。 |
| `src/config/label_spec.yaml` | `timediff_sentinels` に 0000/0000.0 追加 | ✓ VERIFIED | L133 `timediff_sentinels: ["9999", "9999.0", "0000", "0000.0"]`・実データ検証で 0000 は kakuteijyuni='00' と完全一致・builder 側 `_TIMEDIFF_SENTINELS` と整合。 |
| `tests/features/test_builder.py` | parity 6→8・silent-NaN guard fixture 拡張・timediff/babacd 存在 assert 化・WR-01'/WR-02 fail-loud テスト | ✓ VERIFIED | L249 `test_timediff_babacd_present_in_registry_or_rolling`（反転 rename）・L103 `test_no_registered_feature_column_all_nan_end_to_end`（build_feature_matrix 経由に切替）・L317 `test_wr01_prime_raises_on_missing_as_of_datetime`・L377 `test_wr02_raises_on_empty_feature_source`。 |
| `tests/features/test_snapshot_repro.py` | stale 参照整理 + CR-01新 persist→manifest 回帰テスト | ✓ VERIFIED | L123 `test_persist_before_manifest_order`（IO 例外時 manifest 未書込 assert）・L159 `test_persist_then_manifest_order_in_script_source`（AST 解析で行順序機械検査）・`rolling_timediff_mean_5` → `rolling_kakuteijyuni_mean_5` に置換。 |
| `tests/features/test_allowlist.py` | 6エントリ存在 assert 化 | ✓ VERIFIED | L104-106 `rolling_timediff_mean_5`/`rolling_babacd_mean_5`/等6エントリ存在 assert に切替。 |
| `snapshots/feature_matrix_20260619-1a-v3.parquet` | rolling 32 + 静的/PK/label/meta 31 = 63列・§12.4 metadata・byte-reproducible | ✓ VERIFIED | 実ファイル存在（40MB）・PyArrow 実測 rows=554,267 cols=63・on-disk SHA256 = manifest SHA256 = `42865b9a…321516`・6 feature 列 non-null（42.3%-89.0%）。 |
| `snapshots/feature_matrix_20260619-1a-v3.manifest.yaml` | SHA256・feature_count=63・§12.4 metadata | ✓ VERIFIED | feature_count=63・sha256=42865b9a…321516・byte_size=39,958,495・row_count=554,267・category_map_artifact: snapshots/category_map_20260619-1a-v3.json（repo-root 相対）。 |
| `snapshots/category_map_20260619-1a-v3.json` | frozen category map（sort_keys=True・byte-reproducible） | ✓ VERIFIED | 実ファイル存在（1.2MB）・JSON 形式（CR-04 pickle ACE 解消継承）・manifest から参照。 |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `src/etl/normalize.py::_UMA_RACE_SELECT_COLUMNS` | `src/etl/normalize.py::_NORMALIZED_COLUMNS[n_uma_race]` | `_idempotent_load` cols 経由・3者 SELECT/DDL/INSERT のカラム順序一致 | ✓ WIRED | 3者とも末尾 index 43 に `timediff`・DDL 駆動 staging-swap で新カラムが即時本番スキーマへ反映・live-DB で 554,267 行 non-null 実証。 |
| `src/features/builder.py::_HISTORY_DB_SELECT_COLUMNS` | `src/features/builder.py::_construct_derived_columns` | timediff_raw/baba3 SELECT → 派生（timediff real parse・babacd trackcd 分岐） | ✓ WIRED | L114-118 SELECT 拡張・L239-273 派生・alias（timediff_raw/hist_sibababacd/hist_dirtbabacd）で派生名/TARGET_OBS_BANNED 同名衝突回避。 |
| `src/features/builder.py::_construct_derived_columns` | `src/features/rolling.py::build_rolling_features` | timediff(real)/babacd(序数varchar) が rolling source として渡る | ✓ WIRED | L403 `build_rolling_features(feature_matrix, history)` 呼出・rolling.py L86 `_SYSTEM_SOURCE["timediff"]=("timediff",)` / `["babacd"]=("babacd",)` で消費。 |
| `src/features/rolling.py::_ROLLING_SYSTEMS` | `src/features/availability.py::_ROLLING_SYSTEMS_FOR_RESERVED` | tuple 比較で順序含め一致・test が機械保証 | ✓ WIRED | `tuple(_ROLLING_SYSTEMS_FOR_RESERVED) == _ROLLING_SYSTEMS` = True・8系統・`test_registry_rolling_systems_match_rolling_impl` GREEN。 |
| `src/config/feature_availability.yaml` | `src/features/rolling.py::_ROLLING_SYSTEMS` | registry feature_group と rolling.py system 名が一致 | ✓ WIRED | L201/208/215 `feature_group: rolling_timediff`・L295/302/309 `feature_group: rolling_babacd`・rolling.py system 名と整合。 |
| `scripts/run_feature_build.py::persist_category_maps` | `scripts/run_feature_build.py::write_manifest` | persist 成功 → exists assert → manifest 書出（CR-01新・順序化） | ✓ WIRED | L214 persist → L218 assert exists → L225 write_manifest・partial-failure 状態防止・`test_persist_before_manifest_order` + AST test で機械保証。 |
| `scripts/run_feature_build.py` | `snapshots/feature_matrix_20260619-1a-v3.parquet` | persist→exists assert→manifest 順序で stamped snapshot を生成 | ✓ WIRED | live 実行 exit 0・実ファイル SHA256 = manifest SHA256 一致。 |
| `src/etl/normalize.py::run_normalized_etl` | `normalized.n_uma_race.timediff / normalized.n_race.baba3` | staging-swap idempotent で raw から SELECT のみ書込・2回実行で同一 rowcount | ✓ WIRED | live-DB 再実行で 554,267 / 39,593 行 idempotent・timediff/baba3 全行 non-null・raw 881,202/71,972 件不変。 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| feature_matrix v3 Parquet | rolling_timediff_mean_5 | normalized.n_uma_race.timediff (varchar4) → builder parse → rolling | Yes (89.0% non-null) | ✓ FLOWING |
| feature_matrix v3 Parquet | rolling_babacd_mean_5 | normalized.n_race.trackcd/sibababacd/dirtbabacd → builder 派生 → rolling | Yes (57.6% non-null) | ✓ FLOWING |
| feature_matrix v3 Parquet | rolling 32列（8系統×4軸） | normalized.n_uma_race/n_race → rolling per-observation latest-K | Yes | ✓ FLOWING |
| normalized.n_uma_race.timediff | raw(public.n_uma_race.timediff) | staging-swap idempotent ETL（varchar pass-through） | Yes (554,267 件全行 non-null) | ✓ FLOWING |
| normalized.n_race.baba3 | raw(public.n_race) | staging-swap idempotent ETL（varchar pass-through） | Yes (39,593 件全行 non-null) | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| 7 critical named tests GREEN（timediff/babacd 存在・3者 parity・WR-01'/WR-02・CR-01新 persist/AST） | `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/features/test_builder.py::test_timediff_babacd_present_in_registry_or_rolling ... -q` | `7 passed in 0.08s` | ✓ PASS |
| silent-NaN regression guard（build_feature_matrix 経由派生経路確定） | `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/features/test_builder.py::test_no_registered_feature_column_all_nan_end_to_end -q` | `1 passed in 0.06s` | ✓ PASS |
| features 配下テスト全件 | `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/features/ -q` | `62 passed in 0.39s` | ✓ PASS |
| snapshot row/col count | PyArrow read_table | rows=554267 cols=63 | ✓ PASS |
| 6 feature 列 non-null 割合（SUMMARY 主張と一致） | PyArrow `pc.sum(pc.is_valid(col))` | timediff 89.0/89.0/78.7%・babacd 57.6/57.6/42.3% | ✓ PASS |
| on-disk SHA256 == manifest sha256 | `hashlib.sha256(open(...).read())` | `42865b9a…321516` == manifest | ✓ PASS |
| WR-03 vector化（lambda count = 0） | `grep -c "lambda s: int(s.notna().sum())" src/features/rolling.py` | `0` | ✓ PASS |
| 3者 parity（rolling.py == availability.py） | `tuple(_ROLLING_SYSTEMS_FOR_RESERVED) == _ROLLING_SYSTEMS` | `True`・len=8 | ✓ PASS |
| registry↔Parquet parity（SC#4） | `assert_matrix_columns_registered(spec, parquet_cols)` | PASS・未登録=0件 | ✓ PASS |
| live-DB: normalized COUNT idempotent | psycopg SELECT COUNT(*) | n_uma_race=554,267 / n_race=39,593 | ✓ PASS |
| live-DB: timediff/baba3 全行 non-null | psycopg SELECT COUNT(*) WHERE col IS NOT NULL | timediff=554,267 / baba3=39,593 | ✓ PASS |
| live-DB: raw 不変性 | psycopg SELECT COUNT(*) FROM public.n_* | n_uma_race=881,202 / n_race=71,972（前後不変） | ✓ PASS |

### Probe Execution

Step 7c: SKIPPED — 本 phase は `scripts/*/tests/probe-*.sh` 形式の phase-declared probe を持たない（migration/tooling phase でない）。検証は pytest + PyArrow snapshot probe + live-DB COUNT で実施。

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| FEAT-01 | 03.1-01/02/03/04 | 各特徴量に `as_of_datetime`/`feature_cutoff_datetime`/`feature_snapshot_id`/`feature_availability`（`available_from_timing`/`leakage_risk_level` を含む）を付与し、point-in-time 正確性を保証して未来情報リークを防止できる | ✓ SATISFIED | 新6 feature（rolling_timediff_*/rolling_babacd_*）も `available_from_timing: entry_confirmed`/`source_role: history_allowed_post_race`/`cutoff_rule: race_date - 1 day (strict < cutoff)`/`leakage_risk_level: low` で registry 再登録・PIT filter（rolling.py strict `<` per-observation・L206-208）で新規 leak 無し・feature_availability.yaml 宣言と実装が整合。行レベル stamp（as_of_datetime/feature_cutoff_datetime/feature_snapshot_id/feature_availability_version）は snapshot v3 で維持（親 Phase 3 verification で実証済・本 Phase で破壊せず）。 |

REQUIREMENTS.md で Phase 3.1 に紐づく FEAT-01（Phase 3 由来・Phase 3 で Complete 済）は全 plan でカバーされ、本 Phase の拡張（timediff/babacd 復元）でも PIT-correctness とリーク防止が維持されることを実コード + live snapshot v3 で確認。orphaned requirement なし。

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| （該当なし） | - | - | - | Phase 03.1 で変更された全ファイル（normalize.py/builder.py/rolling.py/availability.py/feature_availability.yaml/label_spec.yaml/run_feature_build.py/test_builder.py/test_snapshot_repro.py/test_allowlist.py）に TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER なし。`placeholders = ", ".join(["%s"] * ...)`（normalize.py L455）は SQL placeholder 変数名で stub ではない。 |

Debt-marker gate（TBD/FIXME/XXX を formal follow-up 参照無しに含む）: 該当なし。コメント中の T-03.1-* / CR-* / WR-* / D-* ID は全て本 verification または 03-REVIEW.md / PLAN への formal 参照。

### Human Verification Required

本 phase はローカルデータパイプライン・ETL・snapshot 検証が主で、UI/UX・実タイム動作・外部サービス統合を含まない。PyArrow snapshot probe・pytest・live-DB COUNT・SHA256 再計算で mechanically 検証可能な範囲で全 must_have が確認された。

Plan 04 は `checkpoint:human-verify`（gate=blocking-human）で実施され、人間が live-DB 実行ログと SC#1-#4 の6項目を目視 approved 済み（SUMMARY-04 に resume-signal "approved" 記録）。本 verifier は独立に live-DB に接続して同結果（COUNT / non-null / raw 不変）を再実証し、SUMMARY 主張と一致することを確認した。

human_verification 項目なし。

### Gaps Summary

**gaps なし。** ROADMAP Phase 03.1 の4つの Success Criteria（SC#1 normalized 拡張 / SC#2 rolling 6→8系統化・新規6 feature non-null / SC#3 byte-reproducibility / SC#4 registry↔Parquet parity）は全て実コード + live snapshot v3（554,267 行 × 63 列・SHA256 一致）+ live-DB COUNT（raw 不変）+ critical named tests（7 passed）+ features 全件（62 passed）で達成された。

Phase 3 親 verification が `passed` 判定時に残した advisory 4件（WR-01'/WR-02/CR-01新/WR-03）は本 Phase で全て hardening 完了・実コードで解消を確認:
- **WR-01'**: silent no-filter fallback 削除 → `ValueError` fail-loud（builder.py L430-434）
- **WR-02**: チョークポイント検出追加（builder.py L372-376 RuntimeError）
- **CR-01新**: persist→exists assert→manifest 順序化（run_feature_build.py L214/L218/L225）
- **WR-03**: count 計算 vector化（rolling.py L245-248・lambda=0）

silent-empty breach（Phase 3 gap-closure 03-05 で一時削除した rolling_timediff_*/rolling_babacd_* 6 feature）は完全に復元され、registry↔rolling↔availability↔Parquet の4者 parity が確立・source 存在行で non-null（42.3%-89.0%）を実証。PIT-correctness は rolling.py の strict `< cutoff` + per-observation window（L206-208）で保たれ、新規 leak は生じない（`test_no_registered_feature_column_all_nan_end_to_end` GREEN・`test_registry_rolling_systems_match_rolling_impl` GREEN）。

Phase 4（Model & Prediction）は本 Phase の stamped Parquet snapshot（feature_count=63・rolling 32列 含む）を入力として学習・推論を実施する準備が整った。

---

_Verified: 2026-06-19T10:05:00Z_
_Verifier: Claude (gsd-verifier)_
_Re-verification: initial (no previous VERIFICATION.md)_
