---
phase: 11-race-relative-probability-model
plan: 02
type: tdd
wave: 1
depends_on:
  - 11-01
files_modified:
  - src/model/race_relative.py
autonomous: true
requirements:
  - MODEL-01
  - SAFE-01
tags: [model, race-relative, core-implementation]

must_haves:
  truths:
    - "solve_alpha_for_race が scipy.optimize.brentq で sum_i sigmoid(s_i/θ + α) = k を厳密達成する α を返す（|sum-k| < 1e-9・実測 4.44e-16・D-02/D-10）"
    - "apply_race_relative_correction が D-06 パイプライン step 6-8（p_cal clip → logit → race 毎 α_r → final p）を実装し・race 毎に独立動作する（他 race 不参照・D-10 自己完結）"
    - "apply_race_relative_correction が p_cal 欠損で RuntimeError を raise する（D-09 fail-loud・neutral 補完不採用・silent fallback 禁止）"
    - "compute_overprediction_penalty が segment_eval._odds_band と evaluator の uniform 10 bin 契約を import 再利用し・bit-identical に算出する（D-03/D-05）"
    - "tests/model/test_race_relative.py の12テストが全て GREEN になる（11-01 RED → 11-02 GREEN・TDD GREEN phase）"
  artifacts:
    - "src/model/race_relative.py"
  key_links:
    - "race_relative.py ← orchestrator.train_and_predict（Wave 2・11-03 で統合）"
    - "race_relative.compute_overprediction_penalty ← segment_eval._odds_band / evaluator.CALIBRATION_CURVE_BINS（import 再利用・bit-identical）"
    - "race_relative.solve_alpha_for_race ← scipy.optimize.brentq（xtol=1e-9・maxiter=200）"
---

<objective>
Phase 11 Wave 1: 11-01 で定義した race_relative.py の3公開関数（solve_alpha_for_race / apply_race_relative_correction / compute_overprediction_penalty）を RESEARCH Pattern 1/2/3 の実証済みコード断片に基づいて実装し・11-01 の RED テストを全て GREEN にする（TDD GREEN phase）。

Purpose: 補正層の数学的健全性（α_r 二分探索収束・sum(p)=k 厳密・p∈(0,1)）と安全性（D-09 fail-loud・D-10 自己完結）と bit-identical 性（SC#3・segment_eval binning 再利用）を実コードで保証する。binary 本体（trainer/calibrator/data）は一切触らない（D-01 聖域）。
Output: race_relative.py の3関数が実装され・tests/model/test_race_relative.py が12テスト全て GREEN。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-race-relative-probability-model/11-CONTEXT.md
@.planning/phases/11-race-relative-probability-model/11-RESEARCH.md
@.planning/phases/11-race-relative-probability-model/11-PATTERNS.md
@.planning/phases/11-race-relative-probability-model/11-VALIDATION.md
@.planning/phases/11-race-relative-probability-model/11-01-SUMMARY.md

# analog（必読）
@src/utils/calibrator.py
@src/model/segment_eval.py
@src/model/evaluator.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: solve_alpha_for_race + apply_race_relative_correction 実装（Pattern 1・D-02/D-06/D-09/D-10）</name>
  <files>src/model/race_relative.py</files>
  <read_first>
    - .planning/phases/11-race-relative-probability-model/11-RESEARCH.md（Pattern 1 lines 243-349・実証済みコード・brentq 収束 |sum-k|=4.44e-16・境界挙動 θ→∞/θ→0+）
    - .planning/phases/11-race-relative-probability-model/11-RESEARCH.md（Pitfall 1 lines 481-485 isotonic 0/1 端点・Pitfall 2 lines 487-491 θ 極小発散・Pitfall 5 lines 503-506 cross-race leak・Pitfall 6 lines 508-511 同着）
    - .planning/phases/11-race-relative-probability-model/11-PATTERNS.md（race_relative.py lines 78-137・核心 pure 関数 pattern・race 毎独立ループ）
    - src/model/race_relative.py（11-01 で作成した stub・定数と docstring は維持）
    - src/utils/calibrator.py（pure 関数 + raise guard idiom・L86-94）
  </read_first>
  <behavior>
    - solve_alpha_for_race が brentq(f, ALPHA_SEARCH_BOUNDS[0], ALPHA_SEARCH_BOUNDS[1], xtol=1e-9, rtol=1e-12, maxiter=200) を呼出し・f(α)=Σ sigmoid(s_i/θ+α) − k の根を返す
    - θ=1.0・k=3・18頭の合成データで |sum_p - 3| < 1e-9（実測 4.44e-16）を達成（test_solve_alpha_sum_p_equals_k GREEN）
    - f(α) が単調増加であることを linspace で検証（test_alpha_monotonic_unique_solution GREEN）
    - θ=1e6 で α ≈ logit(k/n) に収束（test_theta_inf_limit GREEN）
    - θ=1e-3 で brentq が失敗（test_theta_zero_divergence GREEN・pytest.raises で捕捉）
    - apply_race_relative_correction が p_cal を clip(ε, 1-ε) → logit → race 毎 solve_alpha_for_race → final p の step 6-8 を実行
    - isotonic 0/1 端点ケースで ε=1e-6 で sum(p)=k 精度 <1e-12（test_clip_epsilon_isotonic_endpoints GREEN）
    - race 毎に独立動作（R1 の logit を変えても R2 の p_final が不変・test_race_independence GREEN）
    - 完全パイプラインで全 race の sum(p)=k が成立（test_pipeline_sum_p_invariant GREEN）
    - p_cal に NaN/inf があると RuntimeError（test_logit_missing_fail_loud GREEN・D-09）
    - base logit が logit(proba) 経路で atol=1e-6 で bit-identical（test_base_logit_bit_identical GREEN・SC#3）
  </behavior>
  <action>
    src/model/race_relative.py の stub（11-01 作成）の NotImplementedError を RESEARCH Pattern 1 lines 262-348 の実証済みコードで置換する。定数・docstring・__all__ は 11-01 のまま維持（変更しない）。

    【solve_alpha_for_race】RESEARCH Pattern 1 lines 301-312 の実装を転記:
    - 内部関数 f(alpha) は `z = s_logits / theta + alpha; return float(np.sum(1.0 / (1.0 + np.exp(-z))) - k)`
    - `return float(brentq(f, ALPHA_SEARCH_BOUNDS[0], ALPHA_SEARCH_BOUNDS[1], xtol=ALPHA_SEARCH_XTOL, rtol=ALPHA_SEARCH_RTOL, maxiter=ALPHA_SEARCH_MAXITER))`
    - brentq が収束しない（θ 極小・α_r 発散）場合は自然に RuntimeError（brentq が raise・Pitfall 2）。docstring の Raises に明記済み。

    【apply_race_relative_correction】RESEARCH Pattern 1 lines 329-348 の実装を転記:
    - D-09 fail-loud guard: `if not np.all(np.isfinite(p_cal)): n_bad = int(np.sum(~np.isfinite(p_cal))); raise RuntimeError(f"apply_race_relative_correction: p_cal に {n_bad} 件の非 finite 値 (NaN/inf)・binary logit 欠損 fail-loud（D-09・silent fallback 禁止）")`（src/utils/calibrator.py L86-94 の raise ValueError idiom と同一・`python -O` で生存）
    - step 6: `p_clipped = np.clip(p_cal, P_CAL_CLIP_EPSILON, 1.0 - P_CAL_CLIP_EPSILON); s_logits = np.log(p_clipped / (1.0 - p_clipped))`
    - step 7-8: `p_final = np.empty_like(p_cal, dtype=float); for rid in np.unique(race_ids): mask = race_ids == rid; s_race = s_logits[mask]; k = int(k_per_race[mask][0]); alpha_r = solve_alpha_for_race(s_race, theta, k); p_final[mask] = 1.0 / (1.0 + np.exp(-(s_race / theta + alpha_r)))`
    - `return p_final`
    - race 毎の独立ループが D-10 自己完結性を構造的に保証（他 race 情報混入不能・Pitfall 5）

    【境界・エッジケース】docstring で明記済みの挙動を実装で保証:
    - θ→0+ で brentq 失敗（Pitfall 2）: THETA_CANDIDATES に極小値を含めないことで予防（11-01 事前登録済み）
    - isotonic 0/1 端点（Pitfall 1）: clip(ε, 1-ε) で logit ±13.8 に抑える
    - race 内で k_per_race が一意でない場合: `k = int(k_per_race[mask][0])` で先頭を採用（呼出側が race 内一意を保証・docstring で明記）

    【変更しないもの】定数（ALPHA_SEARCH_* / P_CAL_CLIP_EPSILON / THETA_CANDIDATES）・docstring・__all__・import 行。11-01 で確定済み。本 task は関数本体の NotImplementedError → 実装 の置換のみ。
  </action>
  <verify>
    <automated>KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/model/test_race_relative.py::test_solve_alpha_sum_p_equals_k tests/model/test_race_relative.py::test_alpha_monotonic_unique_solution tests/model/test_race_relative.py::test_theta_inf_limit tests/model/test_race_relative.py::test_theta_zero_divergence tests/model/test_race_relative.py::test_clip_epsilon_isotonic_endpoints tests/model/test_race_relative.py::test_race_independence tests/model/test_race_relative.py::test_pipeline_sum_p_invariant tests/model/test_race_relative.py::test_logit_missing_fail_loud tests/model/test_race_relative.py::test_base_logit_bit_identical -x</automated>
  </verify>
  <acceptance_criteria>
    - test_solve_alpha_sum_p_equals_k GREEN（|sum-k| < 1e-9・実測 4.44e-16 以下）
    - test_alpha_monotonic_unique_solution GREEN（f(α) 単調増加・IVT）
    - test_theta_inf_limit GREEN（θ=1e6 で α ≈ logit(k/n)・atol=1e-4 程度）
    - test_theta_zero_divergence GREEN（θ=1e-3 で brentq が RuntimeError/ValueError を raise・pytest.raises で捕捉）
    - test_clip_epsilon_isotonic_endpoints GREEN（isotonic 0/1 端点で sum(p)=k 精度 <1e-12）
    - test_race_independence GREEN（R1 logit 変更で R2 p_final 不変）
    - test_pipeline_sum_p_invariant GREEN（全 race sum(p)=k）
    - test_logit_missing_fail_loud GREEN（p_cal NaN で RuntimeError・match="fail-loud"）
    - test_base_logit_bit_identical GREEN（logit(proba) atol=1e-6）
    - race_relative.py の AST に odds/ninki proxy 0件（11-01 の test_no_odds_ninki_proxy が引き続き GREEN）
    - binary 本体（trainer.py / calibrator.py / data.py / evaluator.py / segment_eval.py）は files_modified に含まれない（D-01 聖域・本 task は race_relative.py のみ）
  </acceptance_criteria>
  <done>
    solve_alpha_for_race と apply_race_relative_correction が実装され・9テスト（test_solve_alpha_sum_p_equals_k / test_alpha_monotonic_unique_solution / test_theta_inf_limit / test_theta_zero_divergence / test_clip_epsilon_isotonic_endpoints / test_race_independence / test_pipeline_sum_p_invariant / test_logit_missing_fail_loud / test_base_logit_bit_identical）が GREEN。D-02/D-06/D-09/D-10 達成。
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: compute_overprediction_penalty + 残り3テスト GREEN（Pattern 3・D-03/D-05・SC#3 bit-identical binning）</name>
  <files>src/model/race_relative.py</files>
  <read_first>
    - .planning/phases/11-race-relative-probability-model/11-RESEARCH.md（Pattern 3 lines 380-449・overprediction penalty 厳密式・半波整流 ECE・cell_filter_mask スケール切り替え）
    - .planning/phases/11-race-relative-probability-model/11-PATTERNS.md（race_relative.py lines 112-137・overprediction penalty pattern・binning import 再利用）
    - src/model/segment_eval.py（L142-158 _ninki_band / _odds_band・L69-75 NINKI_BAND_EDGES / ODDS_BAND_EDGES・L166-285 evaluate_segment_axis）
    - src/model/evaluator.py（L89 CALIBRATION_CURVE_BINS=10・L250-256 quantile binning・uniform 10 bin 契約）
    - src/model/race_relative.py（Task 1 で solve/apply を実装済み・本 task は compute_overprediction_penalty のみ）
  </read_first>
  <behavior>
    - compute_overprediction_penalty が segment_eval._odds_band と evaluator の uniform 10 bin 契約（CALIBRATION_CURVE_BINS=10）を import 再利用して bit-identical に算出する（独自 binning 禁止）
    - overall（cell_filter_mask=None）と selected/high-EV 層（cell_filter_mask 指定）で同式が動作する
    - penalty = Σ_cells (count_cell / N_total) × max(0, mean_pred_cell − frac_pos_cell)（半波整流・D-03/D-05）
    - test_overprediction_penalty_binning_parity GREEN（segment_eval と bit-identical）
    - test_k_determination_no_deadheat GREEN（k=sales_start_entry_count ベース・8頭↑=3・5-7頭=2・同着不反映 D-08）
    - test_both_models_bit_identical GREEN（LightGBM/CatBoost 小モデルで race-relative p が np.array_equal・SC#3）
  </behavior>
  <action>
    src/model/race_relative.py の compute_overprediction_penalty の NotImplementedError を RESEARCH Pattern 3 lines 402-449 の実証済みコードで置換する。Task 1 で実装した solve/apply は変更しない。

    【import 追加】ファイル先頭の import ブロックに binning 契約の import を追加:
    - `from src.model.evaluator import CALIBRATION_CURVE_BINS`（uniform 10 bin・L89・再定義禁止）
    - `from src.model.segment_eval import _odds_band`（L151-158・bit-identical banding）

    【_p_bin helper】RESEARCH Pattern 3 lines 392-399 の実装を転記:
    - `P_BIN_EDGES = np.linspace(0.0, 1.0, CALIBRATION_CURVE_BINS + 1)`（uniform 10 bin・evaluator 契約と同一）
    - `def _p_bin(s: pd.Series) -> np.ndarray: arr = s.to_numpy(dtype=float); bin_idx = np.clip(np.digitize(arr, P_BIN_EDGES[1:-1], right=False), 0, CALIBRATION_CURVE_BINS - 1); return np.array([f"p{int(i)}" for i in bin_idx], dtype=object)`（evaluator._compute_calibration_curve_bins と同一契約）

    【compute_overprediction_penalty 本体】RESEARCH Pattern 3 lines 425-449 の実装を転記:
    - cell_filter_mask が None でなければ y_true/y_pred/odds を mask で絞り込み
    - n_total = float(len(y_pred))・0 の場合は float("nan") を返す
    - odds_b = _odds_band(pd.Series(odds))・p_b = _p_bin(pd.Series(y_pred))
    - 二重ループ for ob in np.unique(odds_b): for pb in np.unique(p_b): mask = (odds_b == ob) & (p_b == pb); count = int(mask.sum()); if count == 0: continue; mean_pred = float(np.mean(y_pred[mask])); frac_pos = float(np.mean(y_true[mask])); penalty += (count / n_total) * max(0.0, mean_pred - frac_pos)
    - `return float(penalty)`

    【_p_bin と P_BIN_EDGES の __all__】内部 helper なので __all__ には含めない（compute_overprediction_penalty のみ公開・P_BIN_EDGES はモジュール定数だが非公開）。11-01 の __all__ は変更しない。

    【test_k_determination_no_deadheat・test_both_models_bit_identical の GREEN 化】これらは Task 1 の solve/apply 実装で既に GREEN になる可能性があるが・compute_overprediction_penalty に依存する test_overprediction_penalty_binning_parity は本 task で GREEN になる。test_k_determination_no_deadheat は k_per_race の構築ロジック（sales_start_entry_count ベース・8頭↑=3・5-7頭=2）を test 側で検証するもの・apply_race_relative_correction がそれを正しく消費することを確認（実装は k_per_race をそのまま使う・k 決定ロジック自体は呼出側・orchestrator 11-03 で実装）。test_both_models_bit_identical は LightGBM/CatBoost 小モデルの logit(proba) が一致し race-relative 後も np.array_equal になることを検証（SC#3・RESEARCH Code Examples L515-541）。
  </action>
  <verify>
    <automated>KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/model/test_race_relative.py -x</automated>
  </verify>
  <acceptance_criteria>
    - test_overprediction_penalty_binning_parity GREEN（segment_eval._odds_band と evaluator.CALIBRATION_CURVE_BINS の import 再利用で bit-identical）
    - test_k_determination_no_deadheat GREEN（k=sales_start_entry_count ベース・同着不反映 D-08）
    - test_both_models_bit_identical GREEN（LightGBM/CatBoost 小モデルで np.array_equal・SC#3）
    - tests/model/test_race_relative.py の12テストが全て GREEN（`KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/model/test_race_relative.py -x` exit 0）
    - compute_overprediction_penalty が segment_eval._odds_band / evaluator.CALIBRATION_CURVE_BINS を import 再利用し・独自 binning を導入しない（grep で `np.linspace` が P_BIN_EDGES 構築のみ・他の bin edge 再定義なし）
    - race_relative.py の AST に odds/ninki proxy 0件（11-01 の test_no_odds_ninki_proxy が引き続き GREEN・_odds_band は segment_eval からの import で race_relative 内に odds Name/Attribute は無い）
  </acceptance_criteria>
  <done>
    compute_overprediction_penalty が実装され・tests/model/test_race_relative.py の12テストが全て GREEN。D-03/D-05 達成・binning 契約 bit-identical。SC#3 両モデル bit-identical 実証。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| race_relative.py 内部 | pure 関数・外部状態不参照・市場情報 proxy 不使用（SC#4）|
| α_r 計算の自己完結 | 各 race の logit と k のみから決定・test 窓 outcome 不使用・他 race 不参照（D-10）|

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-03 | Information Disclosure（α_r 計算への test 窓 outcome 混入） | solve_alpha_for_race / apply_race_relative_correction | critical | mitigate | α_r は race logit と k のみから決定（y_true を引数に取らない）・test_alpha_self_contained_outcome_swap が outcome 入れ替えで α_r 不変を検証（D-10・RESEARCH Security Domain L708） |
| T-11-04 | Information Disclosure（α_r 計算への他 race 情報混入） | apply_race_relative_correction | high | mitigate | np.unique(race_ids) で race 毎独立ループ・test_alpha_cross_race_leak_detected が R1 logit 変更で R2 p_final 不変を検証（D-10・RESEARCH Security Domain L709・Pitfall 5） |
| T-11-05 | Tampering（binary logit 欠損の silent fallback） | apply_race_relative_correction | high | mitigate | p_cal の np.isfinite 検査で RuntimeError・neutral 補完不採用・test_logit_missing_fail_loud が NaN で RuntimeError を検証（D-09・RESEARCH Security Domain L711・Phase 10 CR-01〜04 鏡像） |
| T-11-06 | Denial of Service（p_cal clip 忘れで logit(0/1)=±inf・NaN 伝播） | apply_race_relative_correction | high | mitigate | np.clip(p_cal, ε, 1-ε) を logit 前に必須適用・test_clip_epsilon_isotonic_endpoints が isotonic 0/1 端点で精度 <1e-12 を検証（RESEARCH Pitfall 1・Security Domain L713） |
| T-11-07 | Tampering（補正後追加 calib で sum(p)=k 崩壊） | apply_race_relative_correction | high | mitigate | docstring で「補正後追加 calib 禁止」を明記・test_pipeline_sum_p_invariant が完全パイプラインで sum(p)=k を検証（D-06・RESEARCH Security Domain L715・Pitfall 4） |
| T-11-08 | Information Disclosure（target/mean encoding 再導入） | race_relative.py | medium | accept | 補正層は logit 演算のみ・categorical を再処理しない・binary 本体で構造的排除済み（§14.3・RESEARCH Security Domain L714） |
</threat_model>

<verification>
- `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/model/test_race_relative.py -x` が12テスト全て GREEN（exit 0）
- `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/audit/test_audit_race_relative.py::test_no_odds_ninki_proxy tests/audit/test_audit_race_relative.py::test_false_pass_detection_power -x` が GREEN（11-01 から継続）
- `uv run python -c "from src.model.race_relative import solve_alpha_for_race; import numpy as np; rng=np.random.default_rng(42); s=rng.normal(0,1,size=18); a=solve_alpha_for_race(s,1.0,3); print(abs(np.sum(1/(1+np.exp(-(s/1.0+a))))-3))"` が 1e-9 未満（実測 4.44e-16 程度）
</verification>

<success_criteria>
- solve_alpha_for_race が brentq で |sum-k| < 1e-9 を達成
- apply_race_relative_correction が D-06 step 6-8 + D-09 fail-loud + D-10 自己完結を実装
- compute_overprediction_penalty が segment_eval/evaluator binning と bit-identical
- tests/model/test_race_relative.py の12テストが全て GREEN
- binary 本体は一切変更していない（D-01 聖域）
</success_criteria>

<output>
Create `.planning/phases/11-race-relative-probability-model/11-02-SUMMARY.md` when done
</output>
