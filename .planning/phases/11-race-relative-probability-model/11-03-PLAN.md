---
phase: 11-race-relative-probability-model
plan: 03
type: execute
wave: 2
depends_on:
  - 11-02
files_modified:
  - src/model/orchestrator.py
  - src/model/artifact.py
  - src/model/predict.py
autonomous: true
requirements:
  - MODEL-01
  - SAFE-01
tags: [model, orchestrator, integration, artifact]

must_haves:
  truths:
    - "orchestrator.train_and_predict が theta: float | None 引数を取り・theta=None の場合は v1.0 等価（補正層スキップ・A5 後方互換）・theta=float の場合は race_relative 補正層を適用する（D-01/D-06）"
    - "補正層は LightGBM 予測パス（pred_proba 算出後）と CatBoost 予測パス（align_predictions 後）の両方に挿入され・両モデルで同一の apply_race_relative_correction を呼ぶ（SC#3 bit-identical）"
    - "戻り値 dict に race_relative_theta: float | None が追加される（provenance・§19.1）"
    - "artifact.save_native_artifact が metadata.json に race_relative_theta / race_relative_alpha_search_xtol / race_relative_p_cal_clip_epsilon を追加する（§19.1 再現性）"
    - "predict.make_model_version が race-relative 用の short 識別子をサポートする（version_n で v1.0 binary と区別）"
    - "theta=None で既存の Phase 4/5/6/9/10 の全呼出が破壊されない（A5 後方互換・SC#4 bit-identical 回帰防止）"
  artifacts:
    - "src/model/orchestrator.py（拡張）"
    - "src/model/artifact.py（拡張）"
    - "src/model/predict.py（拡張）"
  key_links:
    - "orchestrator.train_and_predict → race_relative.apply_race_relative_correction（Wave 1 11-02 で実装済み）"
    - "orchestrator.train_and_predict(theta=...) ← scripts/run_phase11_evaluation.py（Wave 3 11-04 で統合）"
    - "artifact.save_native_artifact metadata.json ← orchestrator（theta provenance 記録）"
---

<objective>
Phase 11 Wave 2: 11-02 で実装した race_relative.py 補正層を orchestrator.train_and_predict に統合し・theta 引数（calib slice で選んだ事前登録値・None で v1.0 等価）で補正層の on/off を制御する。また artifact.save_native_artifact の metadata.json に theta provenance を追加し・predict.make_model_version が race-relative 用 short 識別子をサポートする。

Purpose: binary 学習→calib→補正層→predict の完全パイプライン（D-06）を orchestrator に実装し・theta=None で既存資産（Phase 4/5/6/9/10）を破壊しない後方互換を保証する。これにより Phase 11 の新モデルが model_version で並列保存可能になる。
Output: orchestrator.py の theta 引数 + 補正層呼出・artifact.py の metadata.json 拡張・predict.py の short 識別子サポート。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-race-relative-probability-model/11-CONTEXT.md
@.planning/phases/11-race-relative-probability-model/11-RESEARCH.md
@.planning/phases/11-race-relative-probability-model/11-PATTERNS.md
@.planning/phases/11-race-relative-probability-model/11-01-SUMMARY.md
@.planning/phases/11-race-relative-probability-model/11-02-SUMMARY.md

# analog（必読・拡張対象）
@src/model/orchestrator.py
@src/model/artifact.py
@src/model/predict.py
@src/model/race_relative.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: orchestrator.train_and_predict に theta 引数 + 補正層呼出を追加（D-01/D-06・A5 後方互換）</name>
  <files>src/model/orchestrator.py</files>
  <read_first>
    - .planning/phases/11-race-relative-probability-model/11-RESEARCH.md（Pattern 2 lines 351-378・base logit 統一経路・System Architecture Diagram lines 156-213・step 6-9）
    - .planning/phases/11-race-relative-probability-model/11-PATTERNS.md（orchestrator.py 拡張セクション lines 141-196・LightGBM/CatBoost 予測パス・theta 引数 idiom・戻り値 provenance）
    - .planning/phases/11-race-relative-probability-model/11-CONTEXT.md（D-06 パイプライン・D-07 is_primary 立てない・§11.2 聖域）
    - src/model/orchestrator.py（L234-610 train_and_predict・L524-533 LightGBM 予測・L534-553 CatBoost 予測・L555-560 index assert・L562-577 predict_p_fukusho 呼出・L599-610 return dict）
    - src/model/race_relative.py（11-02 実装済み・apply_race_relative_correction のシグネチャ）
  </read_first>
  <action>
    src/model/orchestrator.py の train_and_predict を拡張する。PATTERNS lines 141-196 の拡張 idiom と RESEARCH Pattern 2 の base logit 統一経路に従う。

    【1. import 追加】ファイル先頭の import ブロック（L67-79 付近）に `from src.model.race_relative import apply_race_relative_correction` を追加。

    【2. theta 引数追加】train_and_predict のシグネチャ（L234-247）に `theta: float | None = None,` を追加（snapshot_id 引数の後・PATTERNS L173-181 の idiom）。docstring に theta セクションを追加: 「theta : float | None・Phase 11 race-relative logit temperature（D-03 事前登録・calib slice で選んだ値・test 窓選び直し禁止 §11.2）。None の場合は補正層をスキップし v1.0 binary 等価（A5 後方互換・SC#4 bit-identical 回帰防止）。候補 {0.5, 0.75, 1.0, 1.25, 1.5}（THETA_CANDIDATES）。」

    【3. k 決定と race_ids 取得】predict_p_fukusho 呼出の直前（L562 付近）で・theta が None でない場合に備えて race_df_test から k_per_race と race_ids を構築する準備をする。k は D-08 で sales_start_entry_count ベース（8頭↑=3・5-7頭=2）。race_df_test に sales_start_entry_count 列がある場合はそれを使用・無い場合は race_key で groupby した size から推定（但し sales_start_entry_count が正・Phase 2 D-04 で label JOIN 済み）。race_ids は race_df_test["race_key"] を np.array に変換。

    【4. 補正層の挿入】LightGBM 予測パス（L524-533）と CatBoost 予測パス（L534-553）の・それぞれ pred_proba が確定した直後（L533 と L553 の後・L555 の index assert の前）に・theta が None でなければ補正層を適用:
    ```
    if theta is not None:
        # Phase 11 race-relative 補正層（D-06 step 6-8）
        # base logit s_i = logit(clip(p_cal, ε, 1-ε)) は apply_race_relative_correction 内部で処理
        p_final = apply_race_relative_correction(
            pred_proba.to_numpy(),
            theta=theta,
            k_per_race=k_per_race_array,
            race_ids=race_ids_array,
        )
        pred_proba = pd.Series(p_final, index=X_test.index, name="p_fukusho_hit")
    ```
    両モデルで同一の apply_race_relative_correction を呼ぶ（SC#3 bit-identical）。theta=None の場合はこのブロックをスキップし pred_proba はそのまま（v1.0 等価・A5）。

    【5. predict_p_fukusho 呼出】L562-577 の予測パスは変更不要。補正済みの pred_proba が pred_proba=pred_proba で注入される（Cycle 2 NEW HIGH-1 idiom 維持）。

    【6. 戻り値 dict 拡張】L599-610 の return dict に `"race_relative_theta": theta,` を追加（category_map_source の後・PATTERNS L184-191 の idiom）。theta=None（v1.0 等価）or float（事前登録値）を記録（§19.1 再現性）。

    【7. _assert_deterministic への theta 伝播】L773-842 の _assert_deterministic は theta=None のままで良い（v1.0 等価の bit-identical を検証・SC#4）。Wave 4（11-05）で theta=float の bit-identical を別途検証する。本 task では _assert_deterministic の拡張は不要（theta=None で SC#4 回帰防止）。

    【変更しないもの】trainer 呼出（L448-491）・calibrate_model/_calibrate_catboost_manual 呼出（L505-521）・split_3way・make_X_y・category_map 適用・_apply_category_map・予測パスの align_predictions。これらは D-01 聖域・theta=None で完全同一の挙動（A5）。
  </action>
  <verify>
    <automated>KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/model/test_orchestrator.py -x -k "not requires_db"</automated>
  </verify>
  <acceptance_criteria>
    - train_and_predict のシグネチャに `theta: float | None = None` が追加される
    - theta=None で呼出した場合・戻り値の pred_df["p_fukusho_hit"] が theta 引数追加前と bit-identical（`np.array_equal`・A5 後方互換・SC#4 回帰防止・tests/model/test_orchestrator.py の既存テストが全て GREEN）
    - 戻り値 dict に "race_relative_theta" キーが存在し・theta=None の場合は None・theta=float の場合はその値
    - 補正層呼出が LightGBM/CatBoost 両予測パスに挿入される（grep で `apply_race_relative_correction` が2箇所・または共通ヘルパ経由で2パスから呼ばれる）
    - docstring に theta 引数の説明（D-03 事前登録・§11.2 聖域・候補集合・None=v1.0 等価）が追加される
    - binary 本体（trainer.py / calibrator.py / data.py / evaluator.py / segment_eval.py / race_relative.py）は files_modified に含まれない（本 task は orchestrator.py のみ・race_relative.py は 11-02 で確定済み）
  </acceptance_criteria>
  <done>
    orchestrator.train_and_predict が theta 引数を取り・theta=None で v1.0 等価（A5）・theta=float で race_relative 補正層を適用。戻り値に race_relative_theta provenance。既存テスト全て GREEN（回帰なし）。
  </done>
</task>

<task type="auto">
  <name>Task 2: artifact.save_native_artifact metadata.json に theta provenance 追加 + predict.make_model_version short 識別子サポート</name>
  <files>src/model/artifact.py, src/model/predict.py</files>
  <read_first>
    - .planning/phases/11-race-relative-probability-model/11-PATTERNS.md（artifact.py 拡張セクション lines 199-218・metadata_dict 拡張・α_r は保存不要）
    - .planning/phases/11-race-relative-probability-model/11-CONTEXT.md（D-10 自己完結性・α_r は θ + base logit で完全再現）
    - src/model/artifact.py（L98-199 save_native_artifact・L187-197 metadata dict・L72-95 write_metadata_json）
    - src/model/predict.py（L88-93 MODEL_TYPE_TO_SHORT・L105-146 make_model_version）
  </read_first>
  <action>
    artifact.py の metadata と predict.py の MODEL_TYPE_TO_SHORT を拡張する。

    【artifact.py・save_native_artifact の metadata 拡張】L98-199 の save_native_artifact のシグネチャに `race_relative_theta: float | None = None,` キーワード引数を追加（calib_method の後・out_dir の前・後方互換）。L187-197 の metadata dict に以下を追加:
    - `"race_relative_theta": race_relative_theta,`（None=v1.0 等価・or float・事前登録値）
    - `"race_relative_alpha_search_xtol": 1e-9,`（race_relative.ALPHA_SEARCH_XTOL 定数値・再現性・固定値）
    - `"race_relative_p_cal_clip_epsilon": 1e-6,`（race_relative.P_CAL_CLIP_EPSILON 定数値・再現性・固定値）

    α_r 自体は per-race で決定論的（θ + base logit + k から brentq で一意）のため artifact に保存しない（D-10 自己完結性・PATTERNS L218 明記）。θ と base logit（=base estimator + calibrator artifact）があれば α_r は完全再現可能。

    import 追加: ファイル先頭に `from src.model.race_relative import ALPHA_SEARCH_XTOL, P_CAL_CLIP_EPSILON` を追加し・metadata では `race_relative.ALPHA_SEARCH_XTOL` でなく定数値を直接参照（循環 import 回避のため race_relative import は関数内でなく module-level で OK・race_relative は src.model 内で orchestrator 以外に依存しない pure モジュール）。または循環 import リスクを避けるため数値リテラル（1e-9 / 1e-6）を直接 metadata に書き・docstring で race_relative.py の定数と一致することを明記。後者（数値リテラル）を採用・bit-identical 保証のため。

    docstring 拡張: save_native_artifact の Parameters に `race_relative_theta : float | None` を追加（Phase 11 race-relative logit temperature・None=v1.0 等価・D-03 事前登録）。Returns/Raises は変更不要。

    【predict.py・MODEL_TYPE_TO_SHORT 拡張】L88-93 の MODEL_TYPE_TO_SHORT に race-relative 用の short 識別子を追加。race-relative は binary model（lightgbm/catboost）の上に被せるので・既存の "lightgbm": "lgb" / "catboost": "cb" を使いつつ version_n で区別する方針。MODEL_TYPE_TO_SHORT に "lightgbm_rr": "lgbrr" / "catboost_rr": "cbrr" を追加（race-relative の short）。これにより make_model_version("20260626-1a-opponentstrength-v1", "lightgbm_rr", 1) → "20260626-1a-opponentstrength-v1-lgbrr-v1" となる（v1.0 binary の "...-lgb-v1" と区別可能・HIGH#1 model_version-scoped swap で並列保存）。

    ただし・orchestrator.train_and_predict は model_type="lightgbm"/"catboost" を受け取り theta 引数で補正層 on/off を制御する設計（Task 1）。model_version 採番は呼出側（run_phase11_evaluation.py・11-04）が model_type="lightgbm_rr" 等で明示的に race-relative short を指定して make_model_version を呼ぶ。orchestrator 内部の make_model_version 呼出（L413）は model_type をそのまま渡すので・run_phase11_evaluation.py 側で model_type="lightgbm_rr" を指定すると orchestrator が "未知の model_type" で ValueError（L488-491）。

    よって・orchestrator の model_type 分岐（L448 if model_type == "lightgbm": / L466 elif model_type == "catboost":）を拡張し・"lightgbm_rr" は "lightgbm" として扱い、"catboost_rr" は "catboost" として扱う（正規化ヘルパ _normalize_model_type を追加・または startswith で判定）。これにより binary 学習パスは lightgbm/catboost を使いつつ・model_version 採番だけ race-relative short になる。Task 1 の orchestrator 拡張にこの正規化を含めるか・本 Task 2 で predict.py のみ拡張して Task 1 に正規化を追加するか・依存関係上 Task 1 に含めるのが自然（orchestrator が model_type を消費するため）。

    最終方針: Task 1（orchestrator）で model_type 正規化（"lightgbm_rr"→"lightgbm"・"catboost_rr"→"catboost"・学習パス用）を実装し・make_model_version 呼出（L413）には正規化前の元の model_type を渡す（race-relative short を保持）。本 Task 2 は predict.py の MODEL_TYPE_TO_SHORT に "lightgbm_rr": "lgbrr" / "catboost_rr": "cbrr" を追加するのみ。Task 1 の acceptance_criteria に model_type 正規化を追記済みの前提で・本 task は predict.py のみ。

    【predict.py・MODEL_TYPE_TO_SHORT 変更】L89-93 に2エントリ追加:
    ```
    "lightgbm_rr": "lgbrr",
    "catboost_rr": "cbrr",
    ```
    make_model_version の docstring（L121-127）の model_type 説明に "lightgbm_rr" / "catboost_rr"（Phase 11 race-relative）を追記。
  </action>
  <verify>
    <automated>KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/model/test_predict.py tests/model/test_artifact_save_load.py -x -k "not requires_db" 2>&1 | tail -15; uv run python -c "from src.model.predict import make_model_version; assert make_model_version('20260626-1a-opponentstrength-v1', 'lightgbm_rr', 1) == '20260626-1a-opponentstrength-v1-lgbrr-v1'; assert make_model_version('20260626-1a-opponentstrength-v1', 'catboost_rr', 1) == '20260626-1a-opponentstrength-v1-cbrr-v1'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - artifact.save_native_artifact のシグネチャに `race_relative_theta: float | None = None` が追加される（後方互換・デフォルト None）
    - metadata.json に "race_relative_theta" / "race_relative_alpha_search_xtol" (1e-9) / "race_relative_p_cal_clip_epsilon" (1e-6) が追加される
    - α_r は metadata に保存されない（D-10 自己完結性・θ + base logit で完全再現）
    - predict.make_model_version が "lightgbm_rr" / "catboost_rr" をサポートし・それぞれ "lgbrr" / "cbrr" に変換する
    - make_model_version("20260626-1a-opponentstrength-v1", "lightgbm_rr", 1) == "20260626-1a-opponentstrength-v1-lgbrr-v1"
    - 既存の "lightgbm" / "catboost" / "logreg" の short 変更なし（後方互換・tests/model/test_predict.py の既存テスト GREEN）
    - tests/model/test_artifact_save_load.py の既存テストが GREEN（metadata に新キーが追加されても roundtrip 検証は破壊されない）
  </acceptance_criteria>
  <done>
    artifact.save_native_artifact が theta provenance を metadata.json に記録し・predict.make_model_version が race-relative short 識別子をサポート。α_r は不保存（D-10）。既存テスト全て GREEN。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator → race_relative | theta 引数が calib slice で選ばれた事前登録値であること・test 窓で選び直されないこと（§11.2 聖域）|
| artifact metadata | theta provenance が改窜されず §19.1 再現性を保証すること|

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-09 | Elevation of Privilege（過学習・theta 選び直し） | orchestrator.train_and_predict theta 引数 | critical | mitigate | theta は呼出側（run_phase11_evaluation.py）が calib slice で選んだ事前登録値を渡す・orchestrator は theta を再選択しない（受け取った値をそのまま適用）・docstring と provenance stamp で §11.2 聖域を明記（D-03・RESEARCH Security Domain L710） |
| T-11-10 | Tampering（theta=None で v1.0 と非等価・SC#4 回帰） | orchestrator.train_and_predict theta=None パス | high | mitigate | theta=None の場合は補正層をスキップし pred_proba をそのまま predict_p_fukusho に渡す（A5）・tests/model/test_orchestrator.py の既存テストが全て GREEN で回帰なしを保証（SC#4 bit-identical 回帰防止） |
| T-11-11 | Information Disclosure（市場回帰・orchestrator が odds 参照） | orchestrator.py 拡張 | high | mitigate | 補正層呼出は race_relative.apply_race_relative_correction のみ・orchestrator は odds/ninki を直接参照しない・race_relative.py の SC#4 AST 監査（11-01/11-04）が前提保証（SAFE-01） |
| T-11-12 | Repudiation（theta provenance 欠落・再現性崩壊） | artifact.save_native_artifact metadata | high | mitigate | metadata.json に race_relative_theta / alpha_search_xtol / p_cal_clip_epsilon を必須記録・§19.1 再現性・11-05 で idempotent swap と併せて検証（SC#5） |
</threat_model>

<verification>
- `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/model/test_orchestrator.py tests/model/test_predict.py -x -k "not requires_db"` が既存テスト全て GREEN（A5 後方互換・回帰なし）
- `uv run python -c "from src.model.predict import make_model_version; print(make_model_version('20260626-1a-opponentstrength-v1', 'lightgbm_rr', 1))"` が "20260626-1a-opponentstrength-v1-lgbrr-v1"
- `uv run python -c "import inspect; from src.model.orchestrator import train_and_predict; sig=inspect.signature(train_and_predict); assert 'theta' in sig.parameters; assert sig.parameters['theta'].default is None; print('OK')"` が theta 引数デフォルト None
</verification>

<success_criteria>
- orchestrator.train_and_predict が theta 引数を取り・theta=None で v1.0 等価（A5）・theta=float で補正層適用
- 両モデル（LightGBM/CatBoost）で同一の apply_race_relative_correction を呼ぶ（SC#3）
- 戻り値に race_relative_theta provenance
- artifact metadata.json に theta provenance（α_r は不保存・D-10）
- predict.make_model_version が race-relative short をサポート
- 既存テスト全て GREEN（回帰なし・SC#4 bit-identical 回帰防止）
</success_criteria>

<output>
Create `.planning/phases/11-race-relative-probability-model/11-03-SUMMARY.md` when done
</output>
