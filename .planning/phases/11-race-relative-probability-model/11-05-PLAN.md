---
phase: 11-race-relative-probability-model
plan: 05
type: execute
wave: 4
depends_on:
  - 11-04
files_modified:
  - scripts/run_phase11_evaluation.py
  - tests/model/test_race_relative.py
  - src/model/orchestrator.py
  - src/model/predict.py
  - src/db/schema.py
autonomous: false
requirements:
  - MODEL-01
  - SAFE-01
tags: [live-db, sc5-idempotent, sc3-bit-identical, checkpoint]

user_setup:
  - service: postgresql
    why: "live-DB で SC#5 model_version-scoped idempotent swap と SC#3 bit-identical 実データ実証を実行するため（KEIBA_SKIP_DB_TESTS unset）"
    env_vars:
      - name: KEIBA_SKIP_DB_TESTS
        source: "live-DB 実行時は unset（デフォルト・本番相当）・CI/snapshot 無し環境では =1 で skip"

must_haves:
  truths:
    - "scripts/run_phase11_evaluation.py が live-DB（KEIBA_SKIP_DB_TESTS unset）で完走し・reports/11-evaluation/{11-evaluation.md, 11-evaluation.json, theta-selection.json} を生成する"
    - "SC#2 gate（D-04 非劣化 + D-05 改善 3条件）の結果が reports/11-evaluation/11-evaluation.{md,json} に honest に記録される（PASS/FAIL 問わず・§11.2 聖域）"
    - "SC#5 model_version-scoped idempotent swap が prediction.fukusho_prediction に対して実行され・2回実行で checksum bit-identical になる（HIGH#1 踏襲・is_primary 立てない D-07）"
    - "run_phase11_evaluation.py が prediction_load.load_predictions（public wrapper・prediction_df を直接受取り rows 変換を内部委譲）を呼び・private _idempotent_load_prediction に生 DataFrame を渡さない（codex review HIGH#4）"
    - "PREDICTION_COLUMNS に label_version / odds_snapshot_policy / backtest_strategy_version が追加され（schema migration idempotent）・SC#5 §19.1 metadata が prediction 行に永続化される（codex review HIGH#3・未対応だと SC#5 達成不能）"
    - "SC#3 bit-identical が race-relative model で実データ実証される（FIXED_REPRODUCE_TS + 固定 seed/thread + np.array_equal・theta=selected_theta で2回 train_and_predict して p_fukusho_hit が一致・同一モデル同一 seed の再現性で LightGBM≠CatBoost 同一性でない・codex MEDIUM）"
    - "v1.0 binary の予測行が race-relative model 追加後も保持される（model_version scoped swap・is_primary=true は v1.0 のまま・D-07）"
  artifacts:
    - "reports/11-evaluation/11-evaluation.md"
    - "reports/11-evaluation/11-evaluation.json"
    - "reports/11-evaluation/theta-selection.json"
    - "prediction.fukusho_prediction（新 model_version 行追加・v1.0 行保持・label_version/odds_snapshot_policy/backtest_strategy_version 列追加済み）"
  key_links:
    - "run_phase11_evaluation.py → orchestrator.train_and_predict(theta=selected_theta, score_split='test')（live-DB・test 窓一回評価）"
    - "run_phase11_evaluation.py → prediction_load.load_predictions（public wrapper・codex HIGH#4）"
    - "run_phase11_evaluation.py → race_relative.compute_overprediction_penalty（SC#2 gate・evaluator._compute_calibration_curve_bins 再利用 codex HIGH#2）"
---

<objective>
Phase 11 Wave 4: codex review HIGH#3/#4/#5/#8 を解消し（task 順序反転解消・正しい public API 使用・SC#5 metadata schema migration・files_modified 整合）・scripts/run_phase11_evaluation.py を live-DB（KEIBA_SKIP_DB_TESTS unset）で実行し・SC#2 gate（D-04 非劣化 + D-05 改善）の結果を reports/11-evaluation/ に honest 記録する。更に SC#5 model_version-scoped idempotent swap（2回実行で checksum bit-identical）と SC#3 bit-identical（race-relative model で FIXED_REPRODUCE_TS + np.array_equal）を live-DB で実証する。

**タスク順序（codex HIGH#5 反転解消）:** Task 1（schema migration + 永続化・deterministic smoke 実装）→ Task 2（live-DB human checkpoint・永続化と SC#5 を検証）。元の Task1=checkpoint / Task2=実装 の順序は・checkpoint が未実装の永続化を検証する矛盾があったため反転。Task 1 で実装を完了させ・Task 2 checkpoint は完成済みの実装を人手で live-DB 検証する。

Purpose: Phase 11 の全聖域（SC#1-5・D-01〜D-10・§11.2・§19.1）が live-DB で機械保証されることを実データで証明する。core value（リーク防止・再現性）の最終検証。SC#2 gate が PASS でも FAIL でも honest に記録し・Phase 12 で is_primary 切替判断の材料にする（D-07）。
Output: PREDICTION_COLUMNS 拡張 + schema migration・run_phase11_evaluation.py の load_predictions 呼出・orchestrator._assert_deterministic theta 伝播・reports/11-evaluation/*（SC#2 gate 結果・θ 選択経路）・prediction.fukusho_prediction（新 model_version 行・v1.0 行保持・§19.1 metadata 列追加）・SC#3/SC#5 live-DB 実証 GREEN。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-race-relative-probability-model/11-CONTEXT.md
@.planning/phases/11-race-relative-probability-model/11-RESEARCH.md
@.planning/phases/11-race-relative-probability-model/11-PATTERNS.md
@.planning/phases/11-race-relative-probability-model/11-VALIDATION.md
@.planning/phases/11-race-relative-probability-model/11-01-SUMMARY.md
@.planning/phases/11-race-relative-probability-model/11-02-SUMMARY.md
@.planning/phases/11-race-relative-probability-model/11-03-SUMMARY.md
@.planning/phases/11-race-relative-probability-model/11-04-SUMMARY.md

# analog（必読）
@scripts/run_phase10_evaluation.py
@scripts/run_speed_figure_stopgate.py
@src/db/prediction_load.py
@src/model/orchestrator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: SC#5 metadata schema migration + load_predictions 永続化 + orchestrator._assert_deterministic theta 伝播（codex HIGH#3/#4/#5・実装を checkpoint 前に前置）</name>
  <files>src/db/schema.py, src/model/predict.py, src/model/orchestrator.py, scripts/run_phase11_evaluation.py, tests/model/test_race_relative.py</files>
  <read_first>
    - .planning/phases/11-race-relative-probability-model/11-VALIDATION.md（SC#3 test_both_models_bit_identical L57・SC#5 idempotent L59）
    - .planning/phases/11-race-relative-probability-model/11-RESEARCH.md（SC#3 bit-identical 実証 L515-541・Code Examples）
    - .planning/phases/11-race-relative-probability-model/11-REVIEWS.md（HIGH#3 PREDICTION_COLUMNS §19.1 metadata 欠落・HIGH#4 _idempotent_load_prediction rows型・HIGH#5 task 順序反転・HIGH#8 files_modified orchestrator.py 欠落）
    - src/db/schema.py（PREDICTION_TABLE_DDL・PREDICTION_ADD_IS_PRIMARY_SQL・grep で特定）
    - src/model/predict.py（L63-86 PREDICTION_COLUMNS・L88-93 MODEL_TYPE_TO_SHORT・L105-146 make_model_version・L318 列順序 assert）
    - src/db/prediction_load.py（L111 _df_to_prediction_tuples・L191 _idempotent_load_prediction(rows: list[tuple]) private・L373 load_predictions(prediction_df: pd.DataFrame) public wrapper・L447 set_primary_model）
    - src/model/orchestrator.py（_assert_deterministic L773-842・FIXED_REPRODUCE_TS L88・theta/score_split 引数 11-03 拡張）
    - scripts/run_phase11_evaluation.py（11-04 作成・本 task で load_predictions 呼出 + deterministic smoke を追加）
  </read_first>
  <action>
    codex review HIGH#3/#4/#5/#8 を解消し・live-DB checkpoint（Task 2）に先立って永続化と deterministic smoke の実装を完成させる（元 Task 1=checkpoint / Task 2=実装 の順序反転を是正）。

    【1. SC#5 §19.1 metadata schema migration（codex HIGH#3）】src/db/schema.py の PREDICTION_TABLE_DDL に3列を追加し・idempotent migration（ALTER TABLE ... ADD COLUMN IF NOT EXISTS）を定義:
    - `label_version` (TEXT NOT NULL DEFAULT '')
    - `odds_snapshot_policy` (TEXT NOT NULL DEFAULT '')
    - `backtest_strategy_version` (TEXT NOT NULL DEFAULT '')
    DEFAULT '' は既存 v1.0 binary 行との後方互換（NULL 制約違反回避）。migration は既存 PREDICTION_ADD_IS_PRIMARY_SQL と同じ idiom（`ALTER TABLE ... ADD COLUMN IF NOT EXISTS`）で・Phase 6 の is_primary 追加パターンを踏襲。
    src/model/predict.py L63-86 の PREDICTION_COLUMNS に上記3列を追記（is_primary の前・順序は schema.py の DDL 定義順と完全一致・Pitfall 4 3ファイル連鎖）。L318 の `list(df.columns) != list(PREDICTION_COLUMNS)` assert が新列で破壊されないよう・predict_p_fukusho の DataFrame 構築（L266-284）で3列を runtime 引数から設定して付与。runtime 引数（label_version・odds_snapshot_policy・backtest_strategy_version）は predict_p_fukusho のシグネチャに追加（既定値=''・後方互換）・orchestrator から渡す。orchestrator は odds_snapshot_policy を既存の split_periods / as_of_datetime と同様に runtime context から取得して渡す（Phase 2 D-04 の sales_start_entry_count と同等の出所管理）。

    【2. load_predictions public wrapper 使用（codex HIGH#4）】scripts/run_phase11_evaluation.py は prediction_load._idempotent_load_prediction（rows: list[tuple] を要求する private API）に生 DataFrame を渡さない。代わりに prediction_load.load_predictions（public wrapper・prediction_df: pd.DataFrame を受け取り内部で _df_to_prediction_tuples で rows 変換）を呼ぶ:
    - import 追加: `from src.db.prediction_load import load_predictions`（遅延 import・main 内）
    - orchestrator.train_and_predict(theta=selected_theta, score_split="test") の戻り pred_df を load_predictions(write_cur, prediction_df=pred_df, reader_role=...) に渡す
    - model_type="lightgbm_rr"・model_version=rr_model_version（make_model_version で採番・"...-lgbrr-v1"）
    - 2回目の実行で idempotent swap が同一 checksum を生成することをログ出力（HIGH#1 踏襲）
    - set_primary_model は呼ばない（D-07・v1.0 binary 行は保持）

    【3. orchestrator._assert_deterministic theta 伝播（codex HIGH#8 files_modified 整合）】11-03 では theta=None のみ対応だった _assert_deterministic を theta=float にも対応:
    - _assert_deterministic のシグネチャに `theta: float | None = None` を追加・2回の train_and_predict 呼出に theta=theta を伝播
    - theta=None の場合は v1.0 binary の bit-identical（SC#4 回帰防止）・theta=float の場合は race-relative model の bit-identical（SC#3 実データ実証・同一モデル同一 seed の再現性で LightGBM≠CatBoost 同一性でない・codex MEDIUM）
    - src/model/orchestrator.py が files_modified に含まれる（本 task で _assert_deterministic を編集するため・codex HIGH#8）

    【4. run_phase11_evaluation.py・deterministic smoke 呼出】main で・theta=selected_theta で _assert_deterministic を呼出し race-relative model の bit-identical を検証（FIXED_REPRODUCE_TS・固定 seed/thread・np.array_equal）。

    【5. test_race_relative.py・SC#3 docstring 明記】test_both_models_bit_identical の docstring に「同一モデル同一 seed の再現性を検証するのであり・LightGBM と CatBoost の cross-family 同一性を検証するのでない（codex MEDIUM）」を明記。実装は 11-02 で GREEN 済み・docstring 修正のみ。

    【変更しないもの】race_relative.py（11-02 確定）・artifact.py（11-03 確定）・binary 本体 trainer/calibrator/data/evaluator/segment_eval（D-01 聖域）。本 task は schema.py / predict.py / orchestrator.py / run_phase11_evaluation.py / test docstring のみ。
  </action>
  <verify>
    <automated>KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/model/test_race_relative.py tests/audit/test_audit_race_relative.py tests/model/test_predict.py -x; uv run python -c "import ast; ast.parse(open('scripts/run_phase11_evaluation.py').read()); from src.model.predict import PREDICTION_COLUMNS; assert 'label_version' in PREDICTION_COLUMNS; assert 'odds_snapshot_policy' in PREDICTION_COLUMNS; assert 'backtest_strategy_version' in PREDICTION_COLUMNS; print('SC#5 cols OK')"; grep -c "load_predictions\b" scripts/run_phase11_evaluation.py; grep -c "_idempotent_load_prediction" scripts/run_phase11_evaluation.py</automated>
  </verify>
  <acceptance_criteria>
    - src/db/schema.py に label_version / odds_snapshot_policy / backtest_strategy_version の ADD COLUMN IF NOT EXISTS migration が定義される（codex HIGH#3・idempotent・既存行は DEFAULT '' で後方互換）
    - src/model/predict.py PREDICTION_COLUMNS に3列が追記され・順序が schema.py DDL と一致（Pitfall 4 3ファイル連鎖整合）
    - predict_p_fukusho が3つの runtime 引数（label_version・odds_snapshot_policy・backtest_strategy_version・既定値=''）を取り PREDICTION_COLUMNS 順の DataFrame に付与
    - scripts/run_phase11_evaluation.py が prediction_load.load_predictions（public wrapper）を呼び・private _idempotent_load_prediction に生 DataFrame を渡さない（codex HIGH#4・`grep -c "load_predictions\b" scripts/run_phase11_evaluation.py` >= 1・`grep -c "_idempotent_load_prediction" scripts/run_phase11_evaluation.py` == 0）

<!-- planner-discipline-allow: _idempotent_load_prediction -->
    - orchestrator._assert_deterministic が theta 引数を取り・theta=float で race-relative model の bit-identical を検証可能（codex HIGH#8・files_modified に src/model/orchestrator.py が含まれる）
    - test_race_relative.py の test_both_models_bit_identical docstring に「同一モデル同一 seed の再現性・cross-family 同一性でない」が明記される（codex MEDIUM）
    - tests/model/test_race_relative.py の12テスト・tests/audit/test_audit_race_relative.py の4テスト・tests/model/test_predict.py が KEIBA_SKIP_DB_TESTS=1 で GREEN（回帰なし・新列追加で既存 PREDICTION_COLUMNS assert が破壊されない）
    - run_phase11_evaluation.py の Python 構文エラーなし
    - grep で `set_primary_model` が run_phase11_evaluation.py に0件（D-07 is_primary 立てない）
  </acceptance_criteria>
  <done>
    SC#5 §19.1 metadata schema migration（label_version/odds_snapshot_policy/backtest_strategy_version）が定義され・run_phase11_evaluation.py が load_predictions（public）で永続化し・orchestrator._assert_deterministic が theta=float で bit-identical を検証可能。codex HIGH#3/#4/#5/#8 解消。既存テスト GREEN。live-DB checkpoint（Task 2）で SC#2/SC#3/SC#5 を実証する準備完了。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: live-DB で SC#2 gate + SC#5 idempotent swap + SC#3 bit-identical を実証（永続化実装後）</name>
  <action>ユーザーが KEIBA_SKIP_DB_TESTS unset の live-DB 環境で scripts/run_phase11_evaluation.py を直接実行し・reports/11-evaluation/* の SC#2 gate 結果（PASS/FAIL honest 記録）・prediction.fukusho_prediction の model_version scoped idempotent swap（2回実行で checksum bit-identical・v1.0 binary 行保持・is_primary 立てない D-07・label_version/odds_snapshot_policy/backtest_strategy_version 列の永続化）・orchestrator の SC#3 bit-identical（FIXED_REPRODUCE_TS + np.array_equal・theta=selected_theta）を確認する。core value（リーク防止・再現性）の最終実データ実証・Phase 12 引き継ぎ材料の確保。</action>
  <what-built>
    Phase 11 Wave 0-3 で構築した race-relative 補正層（race_relative.py）・orchestrator 統合（theta/score_split 引数・_normalize_model_type）・artifact metadata 拡張・adversarial audit（D-10/SC#4）・評価スクリプト（run_phase11_evaluation.py）と・Wave 4 Task 1 で実装した SC#5 schema migration（label_version/odds_snapshot_policy/backtest_strategy_version）・load_predictions public wrapper 呼出・orchestrator._assert_deterministic theta 伝播が・live-DB で SC#2 gate・SC#5 idempotent swap・SC#3 bit-identical を満たすかを実証する。ユーザーが KEIBA_SKIP_DB_TESTS unset の live-DB 環境で直接実行・結果を確認する。codex review HIGH#3/#4/#5/#8 が実装済みであることを含めて検証。
  </what-built>
  <how-to-verify>
    【前提】PostgreSQL 15.18 が起動し・EveryDB2・feature snapshot `20260626-1a-opponentstrength-v1` が存在すること（Phase 10 完成・SC#3 の入力）。KEIBA_SKIP_DB_TESTS が unset（live-DB 実行）。Task 1 の schema migration が適用済み（label_version/odds_snapshot_policy/backtest_strategy_version 列追加）。

    【手順 1: live-DB SC#2 gate + θ 選択経路（score_split="calib" のみ）】
    ```
    cd /Users/hart/develop/keiba-ai-v3
    unset KEIBA_SKIP_DB_TESTS
    uv run python scripts/run_phase11_evaluation.py \
        --baseline-snapshot-id 20260626-1a-opponentstrength-v1 \
        --bt-split BT-1 \
        --odds-snapshot-policy 30min_before \
        --theta-candidates 0.5 0.75 1.0 1.25 1.5 \
        --out-dir reports
    ```
    - reports/11-evaluation/11-evaluation.md と 11-evaluation.json が生成されることを確認
    - gate verdict（PASS/FAIL）を確認・PASS/FAIL 問わず §11.2 聖域（事前登録許容幅・θ 選び直し禁止・score_split="calib" で θ 選択）が守られていることを確認（codex HIGH#1）
    - reports/11-evaluation/theta-selection.json に候補毎の (Brier, LogLoss, AUC, overprediction_penalty, calib_max_dev, selected_only_calib_max_dev, verdict) と選択経路（足切り→選択→tie-break）が記録されていることを確認・test 窓評価より前に書き出されていることを確認
    - D-05 改善 gate 3条件（overprediction penalty / selected-high-EV / selected-only calib_max_dev）の各判定を確認

    【手順 2: SC#5 model_version-scoped idempotent swap（load_predictions public wrapper・2回実行で checksum bit-identical・codex HIGH#4）】
    - run_phase11_evaluation.py が prediction_load.load_predictions（public wrapper）で race-relative model の行を model_version scoped swap で追加（v1.0 binary 行は保持・codex HIGH#4）
    - 2回実行して prediction.fukusho_prediction の model_version = "...-lgbrr-v1" 行の checksum が bit-identical になることを確認（HIGH#1 idempotent）
    - v1.0 binary（model_version = "...-lgb-v1"・is_primary=true）行が残っていることを確認（D-07 is_primary 立てない）
    - 3つの §19.1 metadata 列（label_version / odds_snapshot_policy / backtest_strategy_version）が race-relative 行に永続化されていることを確認（codex HIGH#3）
    ```
    psql -c "SELECT model_type, model_version, is_primary, label_version, odds_snapshot_policy, backtest_strategy_version, COUNT(*) FROM prediction.fukusho_prediction GROUP BY model_type, model_version, is_primary, label_version, odds_snapshot_policy, backtest_strategy_version ORDER BY model_version;"
    ```
    - 2つの model_version（v1.0 binary・race-relative）が存在し・is_primary=true は v1.0 binary のみ・race-relative 行の3 metadata 列が空でないことを確認

    【手順 3: SC#3 bit-identical（race-relative model・FIXED_REPRODUCE_TS + np.array_equal・codex MEDIUM・同一モデル同一 seed）】
    - orchestrator.train_and_predict を theta=selected_theta・as_of_datetime=FIXED_REPRODUCE_TS・固定 seed/thread で2回呼出し・p_fukusho_hit が np.array_equal で bit-identical になることを確認（同一 LightGBM race-relative model の再現性・LightGBM≠CatBoost 同一性でない）
    - 確認方法: run_phase11_evaluation.py の _assert_deterministic(theta=selected_theta) 呼出（Task 1 で追加）が GREEN で完走・または独立した reproduce smoke で検証
    - VALIDATION.md Test Map L57 test_both_models_bit_identical が live-DB で GREEN になることも確認（同一モデル同一 seed の再現性・LightGBM と CatBoost の cross-family 同一性でない・docstring 明記 codex MEDIUM）

    【手順 4: 全套 audit テスト GREEN（KEIBA_SKIP_DB_TESTS unset・live-DB フルスイート）】
    ```
    unset KEIBA_SKIP_DB_TESTS
    uv run pytest tests/ -x
    ```
    - tests/model/test_race_relative.py（12テスト）・tests/audit/test_audit_race_relative.py（4テスト）・既存の tests/audit/*（Phase 8/9/10）が全て GREEN
    - SC#1/SC#2/SC#3 踏襲（v1.0 対抗的監査パターン）

    【期待結果】
    - SC#2 gate: PASS でも FAIL でも honest 記録（§11.2 聖域・Phase 12 で is_primary 切替判断）
    - SC#5 idempotent: 2回実行で checksum bit-identical・v1.0 binary 行保持・§19.1 metadata 列永続化（codex HIGH#3/#4）
    - SC#3 bit-identical: FIXED_REPRODUCE_TS + np.array_equal GREEN（同一モデル同一 seed・codex MEDIUM）
    - 全套テスト GREEN（live-DB フルスイート）
  </how-to-verify>
  <resume-signal>
    "approved"（全手順 GREEN・SC#2/SC#3/SC#5 実証完了・codex HIGH#3/#4/#5/#8 解消確認）または失敗内容の詳細（どの手順が RED か・reports/11-evaluation/* の内容・psql 出力等）。FAIL の場合は Phase 11 の修正（race_relative.py / orchestrator.py / run_phase11_evaluation.py / schema.py / predict.py）または Phase 12 への引き継ぎ（SC#2 gate FAIL でも honest 記録されていれば Phase 12 で is_primary 切替見送り等の判断材料になる）。
  </resume-signal>
  <files>reports/11-evaluation/11-evaluation.md, reports/11-evaluation/11-evaluation.json, reports/11-evaluation/theta-selection.json, prediction.fukusho_prediction</files>
  <verify>
    <human-check>
      live-DB（KEIBA_SKIP_DB_TESTS unset）で完走し reports/11-evaluation/{11-evaluation.md, 11-evaluation.json, theta-selection.json} を生成・prediction.fukusho_prediction に model_version="...-lgbrr-v1" 行が追加され v1.0 binary 行が保持・label_version/odds_snapshot_policy/backtest_strategy_version が永続化（codex HIGH#3）・2回実行で checksum bit-identical・is_primary=true は v1.0 binary のみ（D-07）・全套 pytest GREEN（how-to-verify の手順1-4 を実施）。
    </human-check>
  </verify>
  <done>
    live-DB で SC#2 gate が honest 記録・SC#5 idempotent swap が load_predictions public wrapper で checksum bit-identical・SC#3 bit-identical が np.array_equal で GREEN（同一モデル同一 seed）・§19.1 metadata 列が永続化・全套 audit テスト GREEN・Phase 12 引き継ぎ材料確保。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live-DB → prediction.fukusho_prediction | model_version scoped swap で race-relative model 行を追加・v1.0 binary 行は保持（D-07・HIGH#1）|
| 実データ → race_relative 補正層 | 実データの base logit が finite であること（D-09 fail-loud）・θ が calib slice で選ばれた事前登録値であること（§11.2 聖域）|

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-18 | Tampering（idempotent swap で silent 履歴破壊・private API 誤用で rows 型不一致） | prediction_load.load_predictions（public wrapper 経由・codex HIGH#4） | high | mitigate | model_version scoped swap（同一 model_type+model_version のみ DELETE→INSERT・他 model_version 行は保持）・load_predictions public wrapper が prediction_df を受け取り内部で _df_to_prediction_tuples で rows 変換（codex HIGH#4・private _idempotent_load_prediction に生 DataFrame を渡さない）・2回実行で checksum bit-identical を実証（HIGH#1・RESEARCH Security Domain L715） |
| T-11-19 | Repudiation（SC#2 gate 結果の隠蔽・改窜・§19.1 metadata 欠落で再現性崩壊） | reports/11-evaluation/* と prediction.fukusho_prediction §19.1 列 | high | mitigate | SC#2 gate は PASS/FAIL 問わず honest に記録・theta-selection.json に候補毎の評価値と選択経路を byte-reproducible に出力（後知恵すり替え禁止・§11.2 聖域・D-05）・更に PREDICTION_COLUMNS に label_version/odds_snapshot_policy/backtest_strategy_version を追加（schema migration idempotent・codex HIGH#3）し SC#5 §19.1 metadata が prediction 行に永続化される |
| T-11-20 | Tampering（SC#3 bit-identical 回帰・非決定論的 thread/seed・cross-family 誤解） | orchestrator._assert_deterministic theta=float | critical | mitigate | FIXED_REPRODUCE_TS + 固定 seed=42 + thread=1 + np.array_equal で2回 train_and_predict を実証・theta=float でも bit-identical（SC#3・同一モデル同一 seed の再現性・LightGBM≠CatBoost の cross-family 同一性でない・codex MEDIUM・review HIGH#7・§19.1 構造的ブロック） |
| T-11-21 | Elevation of Privilege（is_primary 誤設定で Phase 12 前 EV 本番切替） | prediction.fukusho_prediction is_primary | high | mitigate | set_primary_model を呼ばない・is_primary=true は v1.0 binary のみ（D-07・CONTEXT D-07・SC#5・Phase 12 で切替判断） |
| T-11-22 | Information Disclosure（実データ logit 欠損 silent fallback） | live-DB base logit | high | mitigate | apply_race_relative_correction の D-09 fail-loud が実データで発火しないことを確認（feature snapshot の FEATURE_COLUMNS が PIT-correct・base estimator が NaN を native 処理して logit を出す）・発火した場合は RuntimeError で Phase 完了不可（silent fallback 禁止） |
</threat_model>

<verification>
- live-DB（KEIBA_SKIP_DB_TESTS unset）で scripts/run_phase11_evaluation.py が完走し reports/11-evaluation/{11-evaluation.md, 11-evaluation.json, theta-selection.json} を生成（theta-selection.json は test 窓評価より前に書き出し・codex HIGH#1）
- prediction.fukusho_prediction に model_version="...-lgbrr-v1" 行が追加され・v1.0 binary（"...-lgb-v1"・is_primary=true）行が保持される（psql で確認）・label_version/odds_snapshot_policy/backtest_strategy_version 列が永続化される（codex HIGH#3）
- 2回実行で model_version="...-lgbrr-v1" 行の checksum が bit-identical（SC#5 idempotent・load_predictions public wrapper 経由・codex HIGH#4）
- orchestrator._assert_deterministic(theta=selected_theta) が np.array_equal で GREEN（SC#3 bit-identical・実データ・同一モデル同一 seed の再現性・codex MEDIUM）
- `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/` が全套 GREEN（SC#1/SC#2/SC#3 踏襲・live-DB フルスイートは unset で）
</verification>

<success_criteria>
- SC#2 gate 結果が reports/11-evaluation/ に honest 記録される（PASS/FAIL 問わず・§11.2 聖域・theta-selection.json は test 窓評価より前に書き出し codex HIGH#1）
- SC#5 §19.1 metadata（label_version/odds_snapshot_policy/backtest_strategy_version）が schema migration で PREDICTION_COLUMNS に追加され prediction 行に永続化（codex HIGH#3）
- SC#5 idempotent swap が load_predictions（public wrapper）経由で 2回実行 checksum bit-identical・v1.0 binary 行保持（codex HIGH#4・HIGH#1）
- SC#3 bit-identical が race-relative model で実データ実証（FIXED_REPRODUCE_TS + np.array_equal・同一モデル同一 seed の再現性・codex MEDIUM）
- orchestrator._assert_deterministic が theta=float に対応し src/model/orchestrator.py が files_modified に含まれる（codex HIGH#8）
- タスク順序: Task 1（実装）→ Task 2（live-DB checkpoint）の順で永続化が checkpoint に先行（codex HIGH#5 反転解消）
- is_primary 立てない（D-07・v1.0 binary のみ is_primary=true）
- 全套 audit テスト GREEN（live-DB フルスイート・SC#1/#2/#3 踏襲）
- Phase 11 完了・Phase 12（p_lower EV + falsification）への引き継ぎ材料が整う
</success_criteria>

<output>
Create `.planning/phases/11-race-relative-probability-model/11-05-SUMMARY.md` when done
</output>
