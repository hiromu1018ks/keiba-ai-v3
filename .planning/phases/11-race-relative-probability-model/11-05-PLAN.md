---
phase: 11-race-relative-probability-model
plan: 05
type: execute
wave: 4
depends_on:
  - 11-04
files_modified:
  - scripts/run_phase11_evaluation.py
  - tests/model/test_race_relative.py
autonomous: false
requirements:
  - MODEL-01
  - SAFE-01
tags: [live-db, sc5-idempotent, sc3-bit-identical, checkpoint]

user_setup:
  - service: postgresql
    why: "live-DB で SC#5 model_version-scoped idempotent swap と SC#3 bit-identical 実データ実証を実行するため（KEIBA_SKIP_DB_TESTS unset）"
    env_vars:
      - name: KEIBA_SKIP_DB_TESTS
        source: "live-DB 実行時は unset（デフォルト・本番相当）・CI/snapshot 無し環境では =1 で skip"

must_haves:
  truths:
    - "scripts/run_phase11_evaluation.py が live-DB（KEIBA_SKIP_DB_TESTS unset）で完走し・reports/11-evaluation/{11-evaluation.md, 11-evaluation.json, theta-selection.json} を生成する"
    - "SC#2 gate（D-04 非劣化 + D-05 改善 3条件）の結果が reports/11-evaluation/11-evaluation.{md,json} に honest に記録される（PASS/FAIL 問わず・§11.2 聖域）"
    - "SC#5 model_version-scoped idempotent swap が prediction.fukusho_prediction に対して実行され・2回実行で checksum bit-identical になる（HIGH#1 踏襲・is_primary 立てない D-07）"
    - "SC#3 bit-identical が race-relative model で実データ実証される（FIXED_REPRODUCE_TS + 固定 seed/thread + np.array_equal・theta=selected_theta で2回 train_and_predict して p_fukusho_hit が一致）"
    - "v1.0 binary の予測行が race-relative model 追加後も保持される（model_version scoped swap・is_primary=true は v1.0 のまま・D-07）"
  artifacts:
    - "reports/11-evaluation/11-evaluation.md"
    - "reports/11-evaluation/11-evaluation.json"
    - "reports/11-evaluation/theta-selection.json"
    - "prediction.fukusho_prediction（新 model_version 行追加・v1.0 行保持）"
  key_links:
    - "run_phase11_evaluation.py → orchestrator.train_and_predict(theta=selected_theta)（live-DB）"
    - "run_phase11_evaluation.py → prediction_load._idempotent_load_prediction（model_version scoped・is_primary 立てない）"
    - "run_phase11_evaluation.py → race_relative.compute_overprediction_penalty（SC#2 gate）"
---

<objective>
Phase 11 Wave 4: scripts/run_phase11_evaluation.py を live-DB（KEIBA_SKIP_DB_TESTS unset）で実行し・SC#2 gate（D-04 非劣化 + D-05 改善）の結果を reports/11-evaluation/ に honest 記録する。更に SC#5 model_version-scoped idempotent swap（2回実行で checksum bit-identical）と SC#3 bit-identical（race-relative model で FIXED_REPRODUCE_TS + np.array_equal）を live-DB で実証する。

Purpose: Phase 11 の全聖域（SC#1-5・D-01〜D-10・§11.2・§19.1）が live-DB で機械保証されることを実データで証明する。core value（リーク防止・再現性）の最終検証。SC#2 gate が PASS でも FAIL でも honest に記録し・Phase 12 で is_primary 切替判断の材料にする（D-07）。
Output: reports/11-evaluation/*（SC#2 gate 結果・θ 選択経路）・prediction.fukusho_prediction（新 model_version 行・v1.0 行保持）・SC#3/SC#5 live-DB 実証 GREEN。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-race-relative-probability-model/11-CONTEXT.md
@.planning/phases/11-race-relative-probability-model/11-RESEARCH.md
@.planning/phases/11-race-relative-probability-model/11-PATTERNS.md
@.planning/phases/11-race-relative-probability-model/11-VALIDATION.md
@.planning/phases/11-race-relative-probability-model/11-01-SUMMARY.md
@.planning/phases/11-race-relative-probability-model/11-02-SUMMARY.md
@.planning/phases/11-race-relative-probability-model/11-03-SUMMARY.md
@.planning/phases/11-race-relative-probability-model/11-04-SUMMARY.md

# analog（必読）
@scripts/run_phase10_evaluation.py
@scripts/run_speed_figure_stopgate.py
@src/db/prediction_load.py
@src/model/orchestrator.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: live-DB で SC#2 gate + SC#5 idempotent swap + SC#3 bit-identical を実証</name>
  <action>ユーザーが KEIBA_SKIP_DB_TESTS unset の live-DB 環境で scripts/run_phase11_evaluation.py を直接実行し・reports/11-evaluation/* の SC#2 gate 結果（PASS/FAIL honest 記録）・prediction.fukusho_prediction の model_version scoped idempotent swap（2回実行で checksum bit-identical・v1.0 binary 行保持・is_primary 立てない D-07）・orchestrator の SC#3 bit-identical（FIXED_REPRODUCE_TS + np.array_equal・theta=selected_theta）を確認する。core value（リーク防止・再現性）の最終実データ実証・Phase 12 引き継ぎ材料の確保。</action>
  <what-built>
    Phase 11 Wave 0-3 で構築した race-relative 補正層（race_relative.py）・orchestrator 統合（theta 引数）・artifact metadata 拡張・adversarial audit（D-10/SC#4）・評価スクリプト（run_phase11_evaluation.py）が・live-DB で SC#2 gate・SC#5 idempotent swap・SC#3 bit-identical を満たすかを実証する。ユーザーが KEIBA_SKIP_DB_TESTS unset の live-DB 環境で直接実行・結果を確認する。
  </what-built>
  <how-to-verify>
    【前提】PostgreSQL 15.18 が起動し・EveryDB2・feature snapshot `20260626-1a-opponentstrength-v1` が存在すること（Phase 10 完成・SC#3 の入力）。KEIBA_SKIP_DB_TESTS が unset（live-DB 実行）。

    【手順 1: live-DB SC#2 gate + θ 選択経路】
    ```
    cd /Users/hart/develop/keiba-ai-v3
    unset KEIBA_SKIP_DB_TESTS
    uv run python scripts/run_phase11_evaluation.py \
        --baseline-snapshot-id 20260626-1a-opponentstrength-v1 \
        --bt-split BT-1 \
        --odds-snapshot-policy 30min_before \
        --theta-candidates 0.5 0.75 1.0 1.25 1.5 \
        --out-dir reports
    ```
    - reports/11-evaluation/11-evaluation.md と 11-evaluation.json が生成されることを確認
    - gate verdict（PASS/FAIL）を確認・PASS/FAIL 問わず §11.2 聖域（事前登録許容幅・θ 選び直し禁止）が守られていることを確認
    - reports/11-evaluation/theta-selection.json に候補毎の (Brier, LogLoss, AUC, overprediction_penalty, calib_max_dev, selected_only_calib_max_dev, verdict) と選択経路（足切り→選択→tie-break）が記録されていることを確認
    - D-05 改善 gate 3条件（overprediction penalty / selected-high-EV / selected-only calib_max_dev）の各判定を確認

    【手順 2: SC#5 model_version-scoped idempotent swap（2回実行で checksum bit-identical）】
    - run_phase11_evaluation.py が prediction_load._idempotent_load_prediction で race-relative model の行を model_version scoped swap で追加（v1.0 binary 行は保持）
    - 2回実行して prediction.fukusho_prediction の model_version = "...-lgbrr-v1" 行の checksum が bit-identical になることを確認（HIGH#1 idempotent）
    - v1.0 binary（model_version = "...-lgb-v1"・is_primary=true）行が残っていることを確認（D-07 is_primary 立てない）
    ```
    psql -c "SELECT model_type, model_version, is_primary, COUNT(*) FROM prediction.fukusho_prediction GROUP BY model_type, model_version, is_primary ORDER BY model_version;"
    ```
    - 2つの model_version（v1.0 binary・race-relative）が存在し・is_primary=true は v1.0 binary のみであることを確認

    【手順 3: SC#3 bit-identical（race-relative model・FIXED_REPRODUCE_TS + np.array_equal）】
    - orchestrator.train_and_predict を theta=selected_theta・as_of_datetime=FIXED_REPRODUCE_TS・固定 seed/thread で2回呼出し・p_fukusho_hit が np.array_equal で bit-identical になることを確認
    - 確認方法: run_phase11_evaluation.py の実行で model_version scoped swap が2回目も同一 checksum（手順2で確認）・または独立した reproduce smoke スクリプト/テストで検証
    - VALIDATION.md Test Map L57 test_both_models_bit_identical が live-DB で GREEN になることも確認（LightGBM と CatBoost 両方で race-relative p が bit-identical）

    【手順 4: 全套 audit テスト GREEN（KEIBA_SKIP_DB_TESTS unset・live-DB フルスイート）】
    ```
    unset KEIBA_SKIP_DB_TESTS
    uv run pytest tests/ -x
    ```
    - tests/model/test_race_relative.py（12テスト）・tests/audit/test_audit_race_relative.py（4テスト）・既存の tests/audit/*（Phase 8/9/10）が全て GREEN
    - SC#1/SC#2/SC#3 踏襲（v1.0 対抗的監査パターン）

    【期待結果】
    - SC#2 gate: PASS でも FAIL でも honest 記録（§11.2 聖域・Phase 12 で is_primary 切替判断）
    - SC#5 idempotent: 2回実行で checksum bit-identical・v1.0 binary 行保持
    - SC#3 bit-identical: FIXED_REPRODUCE_TS + np.array_equal GREEN
    - 全套テスト GREEN（live-DB フルスイート）
  </how-to-verify>
  <resume-signal>
    "approved"（全手順 GREEN・SC#2/SC#3/SC#5 実証完了）または失敗内容の詳細（どの手順が RED か・reports/11-evaluation/* の内容・psql 出力等）。FAIL の場合は Phase 11 の修正（race_relative.py / orchestrator.py / run_phase11_evaluation.py）または Phase 12 への引き継ぎ（SC#2 gate FAIL でも honest 記録されていれば Phase 12 で is_primary 切替見送り等の判断材料になる）。
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: SC#3 bit-identical reproduce smoke 拡張 + SC#5 idempotent swap 実証ヘルパ（live-DB checkpoint 後の自動化層）</name>
  <files>scripts/run_phase11_evaluation.py, tests/model/test_race_relative.py</files>
  <read_first>
    - .planning/phases/11-race-relative-probability-model/11-VALIDATION.md（SC#3 test_both_models_bit_identical L57・SC#5 idempotent L59）
    - .planning/phases/11-race-relative-probability-model/11-RESEARCH.md（SC#3 bit-identical 実証 L515-541・Code Examples）
    - src/model/orchestrator.py（_assert_deterministic L773-842・FIXED_REPRODUCE_TS L88・theta 引数 11-03 拡張）
    - src/db/prediction_load.py（_idempotent_load_prediction・model_version scoped swap）
    - scripts/run_phase11_evaluation.py（11-04 作成・本 task で idempotent swap 呼出を追加）
  </read_first>
  <action>
    live-DB checkpoint（Task 1）の結果を受けて・SC#3 bit-identical と SC#5 idempotent swap の自動化層を整える。Task 1 で手動実証済みのものを・再実行可能な形で run_phase11_evaluation.py と test に組み込む。

    【run_phase11_evaluation.py・SC#5 idempotent swap 呼出追加】11-04 では比較レポートのみだった run_phase11_evaluation.py に・prediction_load._idempotent_load_prediction の呼出を追加し・race-relative model の行を model_version scoped swap で prediction.fukusho_prediction に永続化する。set_primary_model は呼ばない（D-07・v1.0 binary 行は保持）。

    - import 追加: `from src.db.prediction_load import _idempotent_load_prediction`（遅延 import・main 内）
    - orchestrator.train_and_predict(theta=selected_theta) の戻り pred_df を _idempotent_load_prediction に渡す・model_type="lightgbm_rr"・model_version=rr_model_version（make_model_version で採番・"...-lgbrr-v1"）
    - 2回目の実行で idempotent swap が同一 checksum を生成することをログ出力（HIGH#1 踏襲）
    - §19.1 metadata（model_version / feature_snapshot_id / label_version / odds_snapshot_policy / backtest_strategy_version）が prediction 行に付与されることを確認

    【test_race_relative.py・SC#3 live-DB bit-identical 拡張】test_both_models_bit_identical（11-02 で合成データ GREEN 済み）に・live-DB で実データの race-relative p が LightGBM/CatBoost 両方で bit-identical になることを検証する marker=requires_db 版を追加（オプション・KEIBA_SKIP_DB_TESTS=1 で skip・live-DB でのみ実行）。または・orchestrator._assert_deterministic に theta 引数を伝播し theta=selected_theta で2回 train_and_predict して np.array_equal を検証する拡張（11-03 では theta=None のみ拡張・本 task で theta=float も）。

    - orchestrator._assert_deterministic のシグネチャに theta: float | None = None を追加・2回の train_and_predict 呼出に theta=theta を伝播
    - theta=None の場合は v1.0 binary の bit-identical（11-03 と同一・SC#4 回帰防止）・theta=float の場合は race-relative model の bit-identical（SC#3 実データ実証）
    - run_phase11_evaluation.py の main で・theta=selected_theta で _assert_deterministic を呼出し race-relative model の bit-identical を検証（FIXED_REPRODUCE_TS・固定 seed/thread）

    【変更しないもの】race_relative.py（11-02 確定）・artifact.py / predict.py（11-03 確定）・binary 本体（D-01 聖域）。本 task は orchestrator._assert_deterministic の theta 伝播拡張と run_phase11_evaluation.py の idempotent swap 呼出追加と test 拡張のみ。
  </action>
  <verify>
    <automated>KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/model/test_race_relative.py tests/audit/test_audit_race_relative.py -x; uv run python -c "import ast; ast.parse(open('scripts/run_phase11_evaluation.py').read()); print('syntax OK')"</automated>
  </verify>
  <acceptance_criteria>
    - run_phase11_evaluation.py に prediction_load._idempotent_load_prediction の呼出が追加され・model_version scoped swap で race-relative model 行を永続化する（set_primary_model 呼出なし・D-07）
    - orchestrator._assert_deterministic が theta 引数を取り・theta=float で race-relative model の bit-identical を検証可能（SC#3 実データ実証の自動化）
    - test_race_relative.py の12テストが KEIBA_SKIP_DB_TESTS=1 で引き続き GREEN（合成データ・回帰なし）
    - tests/audit/test_audit_race_relative.py の4テストが KEIBA_SKIP_DB_TESTS=1 で引き続き GREEN
    - run_phase11_evaluation.py の Python 構文エラーなし
    - grep で `set_primary_model` が run_phase11_evaluation.py に0件（D-07 is_primary 立てない）
    - live-DB での実行検証（Task 1 checkpoint で実施）: SC#2 gate honest 記録・SC#5 idempotent checksum bit-identical・SC#3 np.array_equal GREEN
  </acceptance_criteria>
  <done>
    run_phase11_evaluation.py が idempotent swap を呼び・orchestrator._assert_deterministic が theta=float で bit-identical を検証可能。live-DB checkpoint（Task 1）で SC#2/SC#3/SC#5 実証済み。既存テスト GREEN。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live-DB → prediction.fukusho_prediction | model_version scoped swap で race-relative model 行を追加・v1.0 binary 行は保持（D-07・HIGH#1）|
| 実データ → race_relative 補正層 | 実データの base logit が finite であること（D-09 fail-loud）・θ が calib slice で選ばれた事前登録値であること（§11.2 聖域）|

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-18 | Tampering（idempotent swap で silent 履歴破壊） | prediction_load._idempotent_load_prediction | high | mitigate | model_version scoped swap（同一 model_type+model_version のみ DELETE→INSERT・他 model_version 行は保持）・2回実行で checksum bit-identical を実証（HIGH#1・RESEARCH Security Domain L715） |
| T-11-19 | Repudiation（SC#2 gate 結果の隠蔽・改窜） | reports/11-evaluation/* | high | mitigate | SC#2 gate は PASS/FAIL 問わず honest に記録・theta-selection.json に候補毎の評価値と選択経路を byte-reproducible に出力（後知恵すり替え禁止・§11.2 聖域・D-05） |
| T-11-20 | Tampering（SC#3 bit-identical 回帰・非決定論的 thread/seed） | orchestrator._assert_deterministic theta=float | critical | mitigate | FIXED_REPRODUCE_TS + 固定 seed=42 + thread=1 + np.array_equal で2回 train_and_predict を実証・theta=float でも bit-identical（SC#3・review HIGH#7・§19.1 構造的ブロック） |
| T-11-21 | Elevation of Privilege（is_primary 誤設定で Phase 12 前 EV 本番切替） | prediction.fukusho_prediction is_primary | high | mitigate | set_primary_model を呼ばない・is_primary=true は v1.0 binary のみ（D-07・CONTEXT D-07・SC#5・Phase 12 で切替判断） |
| T-11-22 | Information Disclosure（実データ logit 欠損 silent fallback） | live-DB base logit | high | mitigate | apply_race_relative_correction の D-09 fail-loud が実データで発火しないことを確認（feature snapshot の FEATURE_COLUMNS が PIT-correct・base estimator が NaN を native 処理して logit を出す）・発火した場合は RuntimeError で Phase 完了不可（silent fallback 禁止） |
</threat_model>

<verification>
- live-DB（KEIBA_SKIP_DB_TESTS unset）で scripts/run_phase11_evaluation.py が完走し reports/11-evaluation/{11-evaluation.md, 11-evaluation.json, theta-selection.json} を生成
- prediction.fukusho_prediction に model_version="...-lgbrr-v1" 行が追加され・v1.0 binary（"...-lgb-v1"・is_primary=true）行が保持される（psql で確認）
- 2回実行で model_version="...-lgbrr-v1" 行の checksum が bit-identical（SC#5 idempotent）
- orchestrator._assert_deterministic(theta=selected_theta) が np.array_equal で GREEN（SC#3 bit-identical・実データ）
- `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/` が全套 GREEN（SC#1/SC#2/SC#3 踏襲・live-DB フルスイートは unset で）
</verification>

<success_criteria>
- SC#2 gate 結果が reports/11-evaluation/ に honest 記録される（PASS/FAIL 問わず・§11.2 聖域）
- SC#5 idempotent swap が 2回実行で checksum bit-identical・v1.0 binary 行保持（HIGH#1）
- SC#3 bit-identical が race-relative model で実データ実証（FIXED_REPRODUCE_TS + np.array_equal）
- is_primary 立てない（D-07・v1.0 binary のみ is_primary=true）
- 全套 audit テスト GREEN（live-DB フルスイート・SC#1/#2/#3 踏襲）
- Phase 11 完了・Phase 12（p_lower EV + falsification）への引き継ぎ材料が整う
</success_criteria>

<output>
Create `.planning/phases/11-race-relative-probability-model/11-05-SUMMARY.md` when done
</output>
