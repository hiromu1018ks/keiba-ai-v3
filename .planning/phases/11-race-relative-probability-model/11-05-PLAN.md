---
phase: 11-race-relative-probability-model
plan: 05
type: execute
wave: 4
depends_on:
  - 11-04
files_modified:
  - src/db/schema.py
  - src/model/predict.py
  - src/model/orchestrator.py
  - scripts/run_phase11_evaluation.py
  - tests/model/test_race_relative.py
autonomous: false
requirements:
  - MODEL-01
  - SAFE-01
tags: [live-db, sc5-idempotent, sc3-bit-identical, checkpoint, revision]

user_setup:
  - service: postgresql
    why: "live-DB で SC#5 model_version-scoped idempotent swap と SC#3 bit-identical 実データ実証を実行するため（KEIBA_SKIP_DB_TESTS unset）"
    env_vars:
      - name: KEIBA_SKIP_DB_TESTS
        source: "live-DB 実行時は unset（デフォルト・本番相当）・CI/snapshot 無し環境では =1 で skip"

must_haves:
  truths:
    - "scripts/run_phase11_evaluation.py が live-DB（KEIBA_SKIP_DB_TESTS unset）で完走し・reports/11-evaluation/{11-evaluation.md, 11-evaluation.json, theta-selection.json} を生成する"
    - "SC#2 gate（D-04 非劣化 + D-05 改善 3条件）の結果が reports/11-evaluation/11-evaluation.{md,json} に honest に記録される（PASS/FAIL 問わず・§11.2 聖域）"
    - "SC#5 model_version-scoped idempotent swap が prediction.fukusho_prediction に対して実行され・2回実行で checksum bit-identical になる（HIGH#1 踏襲・is_primary 立てない D-07）"
    - "run_phase11_evaluation.py が prediction_load.load_predictions（public wrapper・prediction_df を直接受取り rows 変換を内部委譲）を呼び・private _idempotent_load_prediction に生 DataFrame を渡さない（codex review HIGH#4）"
    - "PREDICTION_COLUMNS に label_version / odds_snapshot_policy / backtest_strategy_version が追加され（schema migration idempotent）・SC#5 §19.1 metadata が prediction 行に永続化される（codex review HIGH#3・未対応だと SC#5 達成不能）"
    - "predict_p_fukusho が runtime 引数 (label_version / odds_snapshot_policy / backtest_strategy_version・既定値='unspecified') を取り PREDICTION_COLUMNS 順の DataFrame に付与・orchestrator.train_and_predict がこれら3引数を末尾に追加して伝播・run_phase11_evaluation.py が orchestrator 呼出で明示的に渡す（codex review HIGH#3・3層ワイヤリング: script → orchestrator → predict_p_fukusho → PREDICTION_COLUMNS）"
    - "orchestrator.train_and_predict が新引数 (label_version: str = 'unspecified' / odds_snapshot_policy: str = 'unspecified' / backtest_strategy_version: str = 'unspecified') を snapshot_id 引数の後に追加し・内部の predict_p_fukusho 呼出に伝播する（codex HIGH#3・HIGH#1 score_split と同じレイヤの拡張・WARNING#2）"
    - "**SC#5 idempotent checksum が bit-identical になるため・orchestrator.train_and_predict の永続化パスが固定 as_of_datetime（FIXED_REPRODUCE_TS or Phase 11 等価固定 timestamp）を明示的に渡す（codex cycle-2 NEW HIGH#2・orchestrator.py:363-364 の datetime.now(UTC) 既定が PK as_of_datetime に入り checksum が不安定になるのを回避・§19.1 再現性）**"
    - "**prediction_model_type_domain CHECK 制約が ('lightgbm','catboost','logreg','lightgbm_rr','catboost_rr') に拡張され（DROP IF EXISTS + ADD の idempotent migration）・race-relative model_type 行が DB に INSERT 可能になる（codex cycle-2 NEW HIGH#1・schema.py:83 既定制約の拡張・設計判断B: 新 model_type 採用）**"
    - "**新 NOT NULL 列（label_version/odds_snapshot_policy/backtest_strategy_version）の既定値が空文字 '' でなく sentinel 'unspecified' になり・_df_to_prediction_tuples の空文字→None 変換（prediction_load.py:175-176）を経由しても NOT NULL 違反にならない（codex cycle-2 NEW HIGH#3・v1.0 binary 呼出 theta=None/3引数省略 でも sentinel が入り安全）**"
    - "SC#3 bit-identical が race-relative model で実データ実証される（FIXED_REPRODUCE_TS + 固定 seed/thread + np.array_equal・theta=selected_theta で2回 train_and_predict して p_fukusho_hit が一致・同一モデル同一 seed の再現性で LightGBM≠CatBoost 同一性でない・codex MEDIUM）"
    - "v1.0 binary の予測行が race-relative model 追加後も保持される（model_version scoped swap・is_primary=true は v1.0 のまま・D-07）"
  prohibitions:
    - "orchestrator.train_and_predict の永続化パス（score_split='test' + load_predictions 呼出）で as_of_datetime=None（datetime.now(UTC) 既定）を使用してはならない（codex cycle-2 NEW HIGH#2・checksum 不安定化）"
    - "prediction.fukusho_prediction の prediction_model_type_domain CHECK 制約を拡張せずに race-relative model_type 行を INSERT してはならない（codex cycle-2 NEW HIGH#1・CHECK 違反で live-DB checkpoint 失敗）"
    - "新 NOT NULL 列の既定値に空文字 '' を使用してはならない（codex cycle-2 NEW HIGH#3・_df_to_prediction_tuples が空文字を None に変換し NOT NULL 違反）・必ず sentinel 'unspecified' を使用"
    - "theta=float かつ model_type が _rr サフィックスでない状態で race-relative 補正済み確率を binary model_version（...-lgb-v1）で刻印してはならない（codex cycle-2 MEDIUM・silent provenance hole・11-03 guard と整合）"
  artifacts:
    - "reports/11-evaluation/11-evaluation.md"
    - "reports/11-evaluation/11-evaluation.json"
    - "reports/11-evaluation/theta-selection.json"
    - "prediction.fukusho_prediction（新 model_version 行追加・v1.0 行保持・label_version/odds_snapshot_policy/backtest_strategy_version 列追加済み・CHECK 制約 lightgbm_rr/catboost_rr 拡張済み）"
    - "src/model/orchestrator.py（train_and_predict 新3引数・_assert_deterministic theta 伝播・永続化パス固定 as_of_datetime）"
    - "src/model/predict.py（PREDICTION_COLUMNS 3列追記・predict_p_fukusho 新3引数・MODEL_TYPE_TO_SHORT 拡張は 11-03 で完了済）"
    - "src/db/schema.py（PREDICTION_TABLE_DDL 3列追加 + idempotent migration + prediction_model_type_domain CHECK 制約 lightgbm_rr/catboost_rr 拡張）"
    - "scripts/run_phase11_evaluation.py（load_predictions 呼出 + deterministic smoke + orchestrator への3引数渡し + 固定 as_of_datetime 渡し）"
  key_links:
    - "run_phase11_evaluation.py → orchestrator.train_and_predict(theta=selected_theta, score_split='test', as_of_datetime=FIXED_REPRODUCE_TS, label_version=..., odds_snapshot_policy=..., backtest_strategy_version=...)（live-DB・test 窓一回評価・codex HIGH#3・WARNING#2 4層 key_link 第1層・codex cycle-2 NEW HIGH#2 固定 as_of_datetime）"
    - "orchestrator.train_and_predict → predict_p_fukusho(label_version=..., odds_snapshot_policy=..., backtest_strategy_version=...)（L566-577 predict_p_fukusho 呼出に3引数伝播・codex HIGH#3・WARNING#2 4層 key_link 第2層）"
    - "predict_p_fukusho → PREDICTION_COLUMNS DataFrame 構築（L266-284 で3列を runtime 引数から設定・codex HIGH#3・WARNING#2 4層 key_link 第3層）"
    - "PREDICTION_COLUMNS ↔ schema.py PREDICTION_TABLE_DDL（列順序 1:1・Pitfall 4 3ファイル連鎖整合・WARNING#2 4層 key_link 第4層）"
    - "schema.py PREDICTION_TABLE_DDL ↔ prediction_model_type_domain CHECK 制約（lightgbm_rr/catboost_rr 拡張・codex cycle-2 NEW HIGH#1・idempotent DROP IF EXISTS + ADD）"
    - "predict_p_fukusho sentinel 既定値 'unspecified' ↔ _df_to_prediction_tuples 空文字→None 変換（prediction_load.py:175-176・codex cycle-2 NEW HIGH#3回避）"
    - "run_phase11_evaluation.py → prediction_load.load_predictions（public wrapper・codex HIGH#4）"
    - "run_phase11_evaluation.py → race_relative.compute_overprediction_penalty（SC#2 gate・evaluator._compute_calibration_curve_bins 再利用 codex HIGH#2）"
---

<objective>
Phase 11 Wave 4: codex review HIGH#3/#4/#5/#8 と checker BLOCKER/WARNING#2 を解消し（task 分割による責務分離・正しい public API 使用・SC#5 metadata schema migration・3層ワイヤリング明示・files_modified 整合）・**codex cycle-2 NEW HIGH#1/#2/#3 の live-DB blocker（CHECK 制約 lightgbm_rr/catboost_rr 拡張・固定 as_of_datetime で checksum bit-identical・sentinel 既定値で loader 空文字→None NOT NULL 違反回避）を解消し**・scripts/run_phase11_evaluation.py を live-DB（KEIBA_SKIP_DB_TESTS unset）で実行し・SC#2 gate（D-04 非劣化 + D-05 改善）の結果を reports/11-evaluation/ に honest 記録する。更に SC#5 model_version-scoped idempotent swap（2回実行で checksum bit-identical）と SC#3 bit-identical（race-relative model で FIXED_REPRODUCE_TS + np.array_equal）を live-DB で実証する。

**タスク構成（checker BLOCKER 解消・責務分割）:**
- Task 1（schema + predict migration）: src/db/schema.py + src/model/predict.py を編集。PREDICTION_TABLE_DDL 3列追加・idempotent migration・**prediction_model_type_domain CHECK 制約を ('lightgbm','catboost','logreg','lightgbm_rr','catboost_rr') に拡張（DROP IF EXISTS + ADD・codex cycle-2 NEW HIGH#1）**・PREDICTION_COLUMNS 3列追記（順序は schema.py と一致・Pitfall 4 3ファイル連鎖）・predict_p_fukusho シグネチャに label_version/odds_snapshot_policy/backtest_strategy_version（**既定値='unspecified' sentinel・codex cycle-2 NEW HIGH#3・空文字 '' でない・_df_to_prediction_tuples 空文字→None 変換回避**）を追加・DataFrame 構築で3列を付与。3ファイル連鎖整合を AST で検証。
- Task 2（orchestrator + script ワイヤリング）: src/model/orchestrator.py + scripts/run_phase11_evaluation.py + tests/model/test_race_relative.py を編集。orchestrator.train_and_predict 新3引数（label_version/odds_snapshot_policy/backtest_strategy_version・**既定値='unspecified' sentinel・codex cycle-2 NEW HIGH#3**）を snapshot_id の後に追加・_assert_deterministic theta 伝播・predict_p_fukusho 呼出に3引数を伝播（codex HIGH#3 3層ワイヤリング・WARNING#2）。run_phase11_evaluation.py が load_predictions（public wrapper・codex HIGH#4）を呼出・orchestrator 呼出で3引数 + **固定 as_of_datetime=FIXED_REPRODUCE_TS（codex cycle-2 NEW HIGH#2・checksum bit-identical のため）**を明示的に渡す・deterministic smoke を追加。test docstring 修正（codex MEDIUM）。
- Task 3（live-DB human checkpoint）: 完成済み実装を live-DB で人手検証（順序維持・HIGH#5 反転解消）。

Purpose: Phase 11 の全聖域（SC#1-5・D-01〜D-10・§11.2・§19.1）が live-DB で機械保証されることを実データで証明する。core value（リーク防止・再現性）の最終検証。SC#2 gate が PASS でも FAIL でも honest に記録し・Phase 12 で is_primary 切替判断の材料にする（D-07）。codex cycle-2 NEW HIGH#1/#2/#3 は SC#5 live-DB 実証の直接的 blocker であり・本 Wave で必ず解消する。
Output: PREDICTION_COLUMNS 拡張 + schema migration（CHECK 制約拡張含む）・orchestrator 新3引数（3層ワイヤリング・sentinel 既定値）・run_phase11_evaluation.py の load_predictions 呼出 + 3引数渡し + 固定 as_of_datetime 渡し + deterministic smoke・reports/11-evaluation/*（SC#2 gate 結果・θ 選択経路）・prediction.fukusho_prediction（新 model_version 行・v1.0 行保持・§19.1 metadata 列追加・CHECK 制約 lightgbm_rr/catboost_rr 拡張）・SC#3/SC#5 live-DB 実証 GREEN。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-race-relative-probability-model/11-CONTEXT.md
@.planning/phases/11-race-relative-probability-model/11-RESEARCH.md
@.planning/phases/11-race-relative-probability-model/11-PATTERNS.md
@.planning/phases/11-race-relative-probability-model/11-VALIDATION.md
@.planning/phases/11-race-relative-probability-model/11-01-SUMMARY.md
@.planning/phases/11-race-relative-probability-model/11-02-SUMMARY.md
@.planning/phases/11-race-relative-probability-model/11-03-SUMMARY.md
@.planning/phases/11-race-relative-probability-model/11-04-SUMMARY.md

# analog（必読）
@scripts/run_phase10_evaluation.py
@scripts/run_speed_figure_stopgate.py
@src/db/prediction_load.py
@src/model/orchestrator.py
@src/model/predict.py
@src/db/schema.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: SC#5 §19.1 metadata schema migration + CHECK 制約 lightgbm_rr/catboost_rr 拡張 + predict.py PREDICTION_COLUMNS 拡張 + predict_p_fukusho シグネチャ拡張（sentinel 既定値） + 3ファイル連鎖整合検証（codex HIGH#3 PART1・codex cycle-2 NEW HIGH#1/#3・Pitfall 4・checker BLOCKER T1）</name>
  <files>src/db/schema.py, src/model/predict.py</files>
  <read_first>
    - .planning/phases/11-race-relative-probability-model/11-RESEARCH.md（SC#5 §19.1 metadata・Pitfall 4 3ファイル連鎖）
    - .planning/phases/11-race-relative-probability-model/11-REVIEWS.md（HIGH#3 PREDICTION_COLUMNS §19.1 metadata 欠落・cycle-2 NEW HIGH#1 CHECK 制約 lightgbm_rr/catboost_rr 拒否・cycle-2 NEW HIGH#3 loader 空文字→None で NOT NULL 違反）
    - src/db/schema.py（L59-94 PREDICTION_TABLE_DDL・L83 prediction_model_type_domain CHECK 制約・L109-123 PREDICTION_ADD_IS_PRIMARY_SQL idiom・DROP IF EXISTS + ADD の idempotent pattern）
    - src/model/predict.py（L57-86 コメント+PREDICTION_COLUMNS・L88-93 MODEL_TYPE_TO_SHORT・L105-146 make_model_version・L154-166 predict_p_fukusho シグネチャ・L266-284 DataFrame 構築・L318 列順序 assert）
    - src/db/prediction_load.py（L111-183 _df_to_prediction_tuples・L175-176 `elif isinstance(v, str): vals.append(v if v != "" else None)` で空文字→None 変換・codex cycle-2 NEW HIGH#3 の根拠）
  </read_first>
  <action>
    codex review HIGH#3 PART1（schema + predict の data layer）と **codex cycle-2 NEW HIGH#1（CHECK 制約 lightgbm_rr/catboost_rr 拡張）** と **codex cycle-2 NEW HIGH#3（loader 空文字→None 変換で NOT NULL 違反回避のため sentinel 既定値採用）** を解消する。orchestrator（Task 2）は触らず・本 task は schema.py と predict.py のみ（checker BLOCKER: 責務を分割）。

    【1. SC#5 §19.1 metadata schema migration（codex HIGH#3）】src/db/schema.py の PREDICTION_TABLE_DDL に3列を追加し・idempotent migration を定義:
    - `label_version` (TEXT NOT NULL DEFAULT 'unspecified')
    - `odds_snapshot_policy` (TEXT NOT NULL DEFAULT 'unspecified')
    - `backtest_strategy_version` (TEXT NOT NULL DEFAULT 'unspecified')
    **DEFAULT 'unspecified'（空文字 '' でない・codex cycle-2 NEW HIGH#3）**: 既存 v1.0 binary 行の ALTER TABLE ADD COLUMN で DEFAULT 'unspecified' が適用され・NULL 制約違反を回避。更に loader 経由（_df_to_prediction_tuples L175-176 の `v if v != "" else None`）でも sentinel 'unspecified' は None 化されず NOT NULL 違反にならない（空文字 '' の場合は None 化され違反）。migration は既存 PREDICTION_ADD_IS_PRIMARY_SQL と同じ idiom（`ALTER TABLE prediction.fukusho_prediction ADD COLUMN IF NOT EXISTS ...`）で・Phase 6 の is_primary 追加パターンを踏襲。新定数 PREDICTION_ADD_PROVENANCE_SQL を追加し run_phase11_evaluation.py の ensure_schema 呼出（Task 2）で実行される想定。

    【2. prediction_model_type_domain CHECK 制約拡張（codex cycle-2 NEW HIGH#1・設計判断B: 新 model_type 採用）】src/db/schema.py に新定数 PREDICTION_EXTEND_MODEL_TYPE_DOMAIN_SQL を追加し・CHECK 制約を ('lightgbm','catboost','logreg') から ('lightgbm','catboost','logreg','lightgbm_rr','catboost_rr') に拡張する idempotent migration を定義（PREDICTION_ADD_IS_PRIMARY_SQL の DROP IF EXISTS + ADD idiom と同一）:
    - `ALTER TABLE prediction.fukusho_prediction DROP CONSTRAINT IF EXISTS prediction_model_type_domain;`
    - `ALTER TABLE prediction.fukusho_prediction ADD CONSTRAINT prediction_model_type_domain CHECK (model_type IN ('lightgbm','catboost','logreg','lightgbm_rr','catboost_rr'));`
    PREDICTION_TABLE_DDL の CHECK 制約定義（L83）も ('lightgbm','catboost','logreg','lightgbm_rr','catboost_rr') に更新（新規 CREATE TABLE の際の整合）。L55 のコメント（"(2) model_type IN ('lightgbm','catboost','logreg')"）も lightgbm_rr/catboost_rr 追記で更新。設計判断B（新 model_type 採用）の根拠: (a) model_type 列単体で binary/race-relative が DB クエリで即座に区別可能（provenance 明確・Phase 12 is_primary 切替判断材料）・(b) revision 済み 11-03 の _normalize_model_type（lightgbm_rr→lightgbm 正規化）と整合・(c) cycle-2 MEDIUM guard（theta is not None 時は _rr サフィックス必須・11-03 Task 1）で silent provenance hole を防止。

    【3. PREDICTION_COLUMNS 拡張（codex HIGH#3・Pitfall 4 3ファイル連鎖）】src/model/predict.py L57-86 の PREDICTION_COLUMNS に3列を追記。**配置位置は is_primary の前・順序は schema.py の DDL 定義順と完全一致**（Pitfall 4: predict.py L318 の `list(df.columns) != list(PREDICTION_COLUMNS)` assert が破壊されないよう・schema.py DDL → PREDICTION_COLUMNS → _df_to_prediction_tuples → INSERT の4経路が同一列順）。L60 の列数コメント（"16 列"）を "19 列" に更新。L57-62 の schema.py 参照コメントも新列に言及。

    【4. predict_p_fukusho シグネチャ拡張（codex HIGH#3・WARNING#2 4層 key_link 第2-3層・sentinel 既定値・codex cycle-2 NEW HIGH#3）】L154-166 の predict_p_fukusho のシグネチャに3つの runtime 引数を追加（**既定値='unspecified' sentinel・空文字 '' でない・codex cycle-2 NEW HIGH#3**・後方互換・pred_proba の後）:
    - `label_version: str = "unspecified",`
    - `odds_snapshot_policy: str = "unspecified",`
    - `backtest_strategy_version: str = "unspecified",`
    L266-284 の DataFrame 構築（`df["model_type"] = model_type` 等）で3列を付与:
    - `df["label_version"] = label_version`
    - `df["odds_snapshot_policy"] = odds_snapshot_policy`
    - `df["backtest_strategy_version"] = backtest_strategy_version`
    docstring（L179-220）の Parameters に3引数の説明を追記（"§19.1 再現性 metadata・既定値 'unspecified' sentinel・race-relative model は事前登録値・v1.0 binary 呼出 theta=None/3引数省略 でも sentinel が入り loader 経由 NOT NULL 違反回避 codex cycle-2 NEW HIGH#3"）。Returns/Raises は変更不要。L318 の列順序 assert は PREDICTION_COLUMNS に3列が追記済みなので・df にも3列が付与されれば通過（破壊されない）。

    【5. Pitfall 4 3ファイル連鎖整合検証（自動化）】本 task の verify で・schema.py PREDICTION_TABLE_DDL の列定義順と predict.py PREDICTION_COLUMNS の順が一致することを AST/text 解析で検証。3層（DDL → PREDICTION_COLUMNS → INSERT順）が bit-identical 列順であることを機械保証。CHECK 制約拡張（lightgbm_rr/catboost_rr）の存在も検証。

    【変更しないもの】orchestrator.py（Task 2）・run_phase11_evaluation.py（Task 2）・race_relative.py（11-02 確定）・artifact.py（11-03 確定）・binary 本体。MODEL_TYPE_TO_SHORT の race-relative short 拡張（lightgbm_rr/catboost_rr）は 11-03 で実装済み・本 task では触らない。loader（_df_to_prediction_tuples）は sentinel 既定値で NOT NULL 違反を回避するため変更不要（codex cycle-2 NEW HIGH#3 の対策 (c) sentinel 採用）。
  </action>
  <verify>
    <automated>KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/model/test_predict.py tests/model/test_artifact_save_load.py -x -k "not requires_db" 2>&1 | tail -15; uv run python -c "
import ast, re
from src.model.predict import PREDICTION_COLUMNS, predict_p_fukusho
import inspect

# (a) PREDICTION_COLUMNS に3列追加・順序確認
assert 'label_version' in PREDICTION_COLUMNS, 'label_version 未追加'
assert 'odds_snapshot_policy' in PREDICTION_COLUMNS, 'odds_snapshot_policy 未追加'
assert 'backtest_strategy_version' in PREDICTION_COLUMNS, 'backtest_strategy_version 未追加'
idx_isp = PREDICTION_COLUMNS.index('is_primary')
idx_lv = PREDICTION_COLUMNS.index('label_version')
assert idx_lv < idx_isp, 'label_version は is_primary の前であること'

# (b) predict_p_fukusho シグネチャに3引数・sentinel 既定値（codex cycle-2 NEW HIGH#3）
sig = inspect.signature(predict_p_fukusho)
assert sig.parameters['label_version'].default == 'unspecified', 'label_version 既定値は sentinel unspecified (codex cycle-2 NEW HIGH#3)'
assert sig.parameters['odds_snapshot_policy'].default == 'unspecified', 'odds_snapshot_policy 既定値は sentinel unspecified'
assert sig.parameters['backtest_strategy_version'].default == 'unspecified', 'backtest_strategy_version 既定値は sentinel unspecified'

# (c) Pitfall 4 3ファイル連鎖: schema.py DDL 列定義順と PREDICTION_COLUMNS の順が一致
import src.db.schema as sch
src = inspect.getsource(sch)
# schema.py に3列の ADD COLUMN IF NOT EXISTS migration がある
assert re.search(r'ADD COLUMN IF NOT EXISTS label_version', src), 'schema.py に label_version migration なし'
assert re.search(r'ADD COLUMN IF NOT EXISTS odds_snapshot_policy', src), 'schema.py に odds_snapshot_policy migration なし'
assert re.search(r'ADD COLUMN IF NOT EXISTS backtest_strategy_version', src), 'schema.py に backtest_strategy_version migration なし'

# (d) codex cycle-2 NEW HIGH#1: prediction_model_type_domain CHECK 制約が lightgbm_rr/catboost_rr を含む
# DDL または migration に拡張後の制約がある
assert re.search(r\"lightgbm_rr.*catboost_rr|catboost_rr.*lightgbm_rr\", src), 'prediction_model_type_domain CHECK 制約に lightgbm_rr/catboost_rr が含まれていない (codex cycle-2 NEW HIGH#1)'
# DROP CONSTRAINT IF EXISTS prediction_model_type_domain がある（idempotent）
assert re.search(r'DROP CONSTRAINT IF EXISTS prediction_model_type_domain', src), 'prediction_model_type_domain DROP IF EXISTS なし (idempotent migration)'

# (e) codex cycle-2 NEW HIGH#3: sentinel 既定値（DEFAULT 'unspecified'・空文字 '' でない）
# DDL または migration に DEFAULT 'unspecified' がある（空文字 '' でない）
unspecified_count = len(re.findall(r\"DEFAULT 'unspecified'\", src))
assert unspecified_count >= 3, f\"DEFAULT 'unspecified' が3列分ない (codex cycle-2 NEW HIGH#3): count={unspecified_count}\"
print('Task1 codex HIGH#3 PART1 + cycle-2 NEW HIGH#1/#3 + Pitfall4 OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - src/db/schema.py に label_version / odds_snapshot_policy / backtest_strategy_version の ADD COLUMN IF NOT EXISTS migration が定義される（codex HIGH#3・idempotent・**DEFAULT 'unspecified' sentinel・codex cycle-2 NEW HIGH#3**）
    - **src/db/schema.py に prediction_model_type_domain CHECK 制約を ('lightgbm','catboost','logreg','lightgbm_rr','catboost_rr') に拡張する idempotent migration（DROP IF EXISTS + ADD）が定義される（codex cycle-2 NEW HIGH#1・PREDICTION_EXTEND_MODEL_TYPE_DOMAIN_SQL 新定数）**
    - **PREDICTION_TABLE_DDL の L83 CHECK 制約と L55 コメントも lightgbm_rr/catboost_rr 追記で更新される（新規 CREATE TABLE 整合）**
    - PREDICTION_TABLE_DDL に3列が追加され・定義順が PREDICTION_COLUMNS と一致（Pitfall 4）
    - src/model/predict.py PREDICTION_COLUMNS に3列が追記され・is_primary の前に配置・順序が schema.py DDL と一致
    - PREDICTION_COLUMNS の列数コメントが "19 列" に更新される
    - predict_p_fukusho が3つの runtime 引数（label_version・odds_snapshot_policy・backtest_strategy_version・**既定値='unspecified' sentinel・codex cycle-2 NEW HIGH#3**）を取り・DataFrame 構築（L266-284）で3列を runtime 引数から設定して付与
    - predict_p_fukusho の docstring に3引数の説明が追記される（sentinel 'unspecified' の意義・codex cycle-2 NEW HIGH#3 に言及）
    - L318 の `list(df.columns) != list(PREDICTION_COLUMNS)` assert が新列で破壊されない（3列付与で一致）
    - tests/model/test_predict.py の既存テストが KEIBA_SKIP_DB_TESTS=1 で GREEN（3引数は sentinel 既定値で後方互換）
    - **loader（_df_to_prediction_tuples）は変更不要**（sentinel 'unspecified' は空文字でないため None 化されず・codex cycle-2 NEW HIGH#3 回避）
    - **本 task は schema.py と predict.py のみ編集**（orchestrator.py / run_phase11_evaluation.py は Task 2・checker BLOCKER 責務分割）
  </acceptance_criteria>
  <done>
    SC#5 §19.1 metadata schema migration（sentinel 既定値）と prediction_model_type_domain CHECK 制約拡張（lightgbm_rr/catboost_rr）が定義され・PREDICTION_COLUMNS に3列が追記され・predict_p_fukusho が3引数（sentinel 既定値）を取り DataFrame に付与する。Pitfall 4 3ファイル連鎖整合が検証済み。codex HIGH#3 PART1 と cycle-2 NEW HIGH#1/#3 解消。既存テスト GREEN。Task 2（orchestrator + script）の準備完了。
  </done>
</task>

<task type="auto">
  <name>Task 2: orchestrator.train_and_predict 新3引数 + _assert_deterministic theta 伝播 + run_phase11_evaluation.py load_predictions ワイヤリング（codex HIGH#3 PART2/#4/#5/#8・WARNING#2 4層 key_link・checker BLOCKER T2）</name>
  <files>src/model/orchestrator.py, scripts/run_phase11_evaluation.py, tests/model/test_race_relative.py</files>
  <read_first>
    - .planning/phases/11-race-relative-probability-model/11-VALIDATION.md（SC#3 test_both_models_bit_identical L57・SC#5 idempotent L59）
    - .planning/phases/11-race-relative-probability-model/11-RESEARCH.md（SC#3 bit-identical 実証 L515-541）
    - .planning/phases/11-race-relative-probability-model/11-REVIEWS.md（HIGH#3 metadata 3層ワイヤリング・HIGH#4 load_predictions public wrapper・HIGH#5 task 順序反転・HIGH#8 files_modified orchestrator.py）
    - src/model/orchestrator.py（L234-247 train_and_predict シグネチャ・L566-577 predict_p_fukusho 呼出・L599-610 return dict・L773-842 _assert_deterministic・theta/score_split 引数は 11-03 で拡張済）
    - src/model/predict.py（Task 1 で PREDICTION_COLUMNS 3列追記 + predict_p_fukusho 新3引数追加済・本 task は触らない）
    - src/db/prediction_load.py（L373-411 load_predictions(write_cur, prediction_df, reader_role=None) public wrapper・L111 _df_to_prediction_tuples・L447 set_primary_model・codex HIGH#4）
    - scripts/run_phase11_evaluation.py（11-04 作成・本 task で load_predictions 呼出 + orchestrator 3引数渡し + deterministic smoke を追加）
  </read_first>
  <action>
    codex review HIGH#3 PART2（orchestrator 3層ワイヤリング）/#4（load_predictions public wrapper）/#5（task 順序）/#8（files_modified orchestrator.py）と WARNING#2（4層 key_link 明示）と **codex cycle-2 NEW HIGH#2（永続化パス固定 as_of_datetime）** を解消する。schema.py / predict.py は Task 1 で確定済（sentinel 既定値・CHECK 制約拡張含む）・本 task は orchestrator + script + test docstring のみ（checker BLOCKER 責務分割）。

    【1. orchestrator.train_and_predict 新3引数（codex HIGH#3 PART2・WARNING#2 4層 key_link 第1層・sentinel 既定値・codex cycle-2 NEW HIGH#3）】L234-247 の train_and_predict シグネチャに・11-03 で追加した theta/score_split の後に（snapshot_id の後）3引数を追加（**既定値='unspecified' sentinel・codex cycle-2 NEW HIGH#3**・後方互換 A5）:
    - `label_version: str = "unspecified",`
    - `odds_snapshot_policy: str = "unspecified",`
    - `backtest_strategy_version: str = "unspecified",`
    docstring に3引数の説明を追記（"§19.1 再現性 metadata・orchestrator は値を選択せず呼出側から受け取った値をそのまま predict_p_fukusho に伝播・既定値 'unspecified' sentinel は v1.0 binary 呼出 theta=None/3引数省略 で NOT NULL 違反回避 codex cycle-2 NEW HIGH#3"）。orchestrator が odds_snapshot_policy を独自に推測しない（聖域：呼出側が明示的に渡す・run_phase11_evaluation.py が事前登録値を渡す）。

    【2. predict_p_fukusho 呼出への3引数伝播（codex HIGH#3・WARNING#2 4層 key_link 第2層）】L566-577 の predict_p_fukusho 呼出に3引数を伝播:
    pred_df = predict_p_fukusho(... calib_result.calibrated ... model_type=original_model_type ... race_df=race_df_score ... split_label=score_split ... as_of_datetime=as_of_dt ... pred_proba=pred_proba ... label_version=label_version ... odds_snapshot_policy=odds_snapshot_policy ... backtest_strategy_version=backtest_strategy_version)
    これにより §19.1 metadata が sentinel/事前登録値で PREDICTION_COLUMNS に流れる（codex HIGH#3 完全解消・WARNING#2）。theta=None かつ3引数='unspecified' sentinel 既定で既存 v1.0 binary 呼出が破壊されない（A5 後方互換・SC#4 回帰防止）。

    【3. return dict 拡張】L599-610 の return dict に provenance として `"label_version": label_version, "odds_snapshot_policy": odds_snapshot_policy, "backtest_strategy_version": backtest_strategy_version,` を追加（codex HIGH#3・呼出側が result 経由で metadata を取得可能・scripts/run_train_predict.py の save_native_artifact 呼出と同じ idiom）。

    【4. orchestrator._assert_deterministic theta 伝播（codex HIGH#8 files_modified 整合）】L773-842 の _assert_deterministic を theta=float にも対応:
    - シグネチャに `theta: float | None = None` を追加
    - 2回の train_and_predict 呼出に theta=theta を伝播（11-03 で theta 引数を取るよう拡張済みの train_and_predict を呼ぶ）
    - theta=None の場合は v1.0 binary の bit-identical（SC#4 回帰防止）・theta=float の場合は race-relative model の bit-identical（SC#3 実データ実証・同一モデル同一 seed の再現性・codex MEDIUM）
    - src/model/orchestrator.py が files_modified に含まれる（本 task で _assert_deterministic + train_and_predict を編集・codex HIGH#8）

    【5. run_phase11_evaluation.py・orchestrator 呼出で3引数 + 固定 as_of_datetime 明示的渡し（codex HIGH#3・WARNING#2 4層 key_link 第1層・script 側・codex cycle-2 NEW HIGH#2）】main の orchestrator.train_and_predict(theta=selected_theta, score_split="test", ...) 呼出に3引数 + **固定 as_of_datetime=FIXED_REPRODUCE_TS** を明示的に渡す:
    - label_version: 事前登録値（例: "v1.0"・run_phase11_evaluation.py の引数/定数から）
    - odds_snapshot_policy: CLI 引数 --odds-snapshot-policy の値（例: "30min_before"）
    - backtest_strategy_version: 事前登録値（例: "BT-1"・--bt-split の値）
    - **as_of_datetime=FIXED_REPRODUCE_TS**（orchestrator.py:88 の既存定数・codex cycle-2 NEW HIGH#2・永続化パスで datetime.now(UTC) 既定を使うと PK as_of_datetime が実行時刻になり checksum が不安定・2回実行で bit-identical にならない）
    import: `from src.model.orchestrator import train_and_predict, FIXED_REPRODUCE_TS`（FIXED_REPRODUCE_TS も明示的 import）。
    これにより metadata が sentinel/事前登録値で永続化され・SC#5 §19.1 が達成され（codex HIGH#3）・2回実行で checksum が bit-identical になる（codex cycle-2 NEW HIGH#2）。

    【6. load_predictions public wrapper 使用（codex HIGH#4）】scripts/run_phase11_evaluation.py は prediction_load._idempotent_load_prediction（private・rows: list[tuple] 要求）に生 DataFrame を渡さない。代わりに prediction_load.load_predictions（public wrapper・prediction_df: pd.DataFrame を受け取り内部で _df_to_prediction_tuples で rows 変換）を呼ぶ:
    - import 追加: `from src.db.prediction_load import load_predictions`（遅延 import・main 内）
    - orchestrator.train_and_predict(theta=selected_theta, score_split="test", as_of_datetime=FIXED_REPRODUCE_TS, label_version=..., odds_snapshot_policy=..., backtest_strategy_version=...) の戻り pred_df を `load_predictions(write_cur, prediction_df=pred_df, reader_role=...)` に渡す（実シグネチャ L373-411: write_cur, prediction_df, reader_role=None）
    - model_type="lightgbm_rr"・model_version=rr_model_version（make_model_version で採番・"...-lgbrr-v1"・11-03 で MODEL_TYPE_TO_SHORT 拡張済）
    - 2回目の実行で idempotent swap が同一 checksum を生成することをログ出力（HIGH#1 踏襲）・**as_of_datetime=FIXED_REPRODUCE_TS 固定で PK が一致し checksum bit-identical（codex cycle-2 NEW HIGH#2）**
    - set_primary_model は呼ばない（D-07・v1.0 binary 行は保持）

    【7. schema migration の適用（Task 1 で定義した PREDICTION_ADD_PROVENANCE_SQL + PREDICTION_EXTEND_MODEL_TYPE_DOMAIN_SQL）】run_phase11_evaluation.py の main 先頭で・Task 1 で定義した両 migration（provenance 3列 + CHECK 制約 lightgbm_rr/catboost_rr 拡張）を etl cursor で実行（idempotent・2回目以降は IF NOT EXISTS / DROP IF EXISTS で no-op）。Phase 6 の is_primary migration 適用 idiom と同一。

    【8. deterministic smoke 呼出】main で・theta=selected_theta で orchestrator._assert_deterministic(theta=selected_theta) を呼出し race-relative model の bit-identical を検証（FIXED_REPRODUCE_TS・固定 seed/thread・np.array_equal・codex MEDIUM）。

    【9. test_race_relative.py・SC#3 docstring 明記】test_both_models_bit_identical の docstring に「同一モデル同一 seed の再現性を検証するのであり・LightGBM と CatBoost の cross-family 同一性を検証するのでない（codex MEDIUM）」を明記。実装は 11-02 で GREEN 済み・docstring 修正のみ。

    【変更しないもの】schema.py（Task 1）・predict.py（Task 1）・race_relative.py（11-02 確定）・artifact.py（11-03 確定）・binary 本体 trainer/calibrator/data/evaluator/segment_eval（D-01 聖域）。
  </action>
  <verify>
    <automated>KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/model/test_race_relative.py tests/audit/test_audit_race_relative.py tests/model/test_orchestrator.py -x -k "not requires_db" 2>&1 | tail -15; uv run python -c "
import ast, inspect, re
# (a) run_phase11_evaluation.py 構文確認 + load_predictions public wrapper 使用 + private 不使用
ast.parse(open('scripts/run_phase11_evaluation.py').read())
src = open('scripts/run_phase11_evaluation.py').read()
assert re.search(r'\bload_predictions\b', src), 'load_predictions public wrapper が呼ばれていない (codex HIGH#4)'
assert '_idempotent_load_prediction' not in src, 'private _idempotent_load_prediction が呼ばれている (codex HIGH#4)'

# (b) orchestrator.train_and_predict 新3引数 (sentinel 既定値 codex cycle-2 NEW HIGH#3) + _assert_deterministic theta
from src.model.orchestrator import train_and_predict, _assert_deterministic
sig = inspect.signature(train_and_predict)
assert sig.parameters['label_version'].default == 'unspecified', 'train_and_predict label_version 既定値は sentinel unspecified (codex cycle-2 NEW HIGH#3)'
assert sig.parameters['odds_snapshot_policy'].default == 'unspecified', 'train_and_predict odds_snapshot_policy 既定値は sentinel unspecified'
assert sig.parameters['backtest_strategy_version'].default == 'unspecified', 'train_and_predict backtest_strategy_version 既定値は sentinel unspecified'
assert 'theta' in sig.parameters, 'theta 引数 (11-03)'
assert 'score_split' in sig.parameters, 'score_split 引数 (11-03)'
dsig = inspect.signature(_assert_deterministic)
assert 'theta' in dsig.parameters, '_assert_deterministic theta 引数 (codex HIGH#8)'

# (c) orchestrator ソースで predict_p_fukusho 呼出に3引数伝播
osrc = inspect.getsource(__import__('src.model.orchestrator', fromlist=['orchestrator']))
assert 'label_version=label_version' in osrc, 'predict_p_fukusho 呼出に label_version 伝播なし (WARNING#2)'
assert 'odds_snapshot_policy=odds_snapshot_policy' in osrc, 'predict_p_fukusho 呼出に odds_snapshot_policy 伝播なし (WARNING#2)'
assert 'backtest_strategy_version=backtest_strategy_version' in osrc, 'predict_p_fukusho 呼出に backtest_strategy_version 伝播なし (WARNING#2)'

# (d) codex cycle-2 NEW HIGH#2: run_phase11_evaluation.py が固定 as_of_datetime を渡す
assert 'FIXED_REPRODUCE_TS' in src, 'FIXED_REPRODUCE_TS が import/参照されていない (codex cycle-2 NEW HIGH#2)'
assert re.search(r'as_of_datetime\s*=\s*FIXED_REPRODUCE_TS', src), 'orchestrator 呼出に as_of_datetime=FIXED_REPRODUCE_TS 固定渡しがない (codex cycle-2 NEW HIGH#2)'

# (e) set_primary_model を呼んでいない (D-07・AST check codex cycle-2 LOW)
tree = ast.parse(src)
for node in ast.walk(tree):
    if isinstance(node, ast.Call):
        f = node.func
        if (isinstance(f, ast.Attribute) and f.attr == 'set_primary_model') or (isinstance(f, ast.Name) and f.id == 'set_primary_model'):
            raise AssertionError('set_primary_model が Call されている (D-07 違反)')

print('Task2 codex HIGH#3 PART2/#4/#5/#8 + WARNING#2 + cycle-2 NEW HIGH#2 + cycle-2 LOW(set_primary AST) OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - orchestrator.train_and_predict のシグネチャに `label_version: str = "unspecified"`, `odds_snapshot_policy: str = "unspecified"`, `backtest_strategy_version: str = "unspecified"` が theta/score_split の後に追加される（codex HIGH#3・WARNING#2 第1層・**sentinel 既定値 codex cycle-2 NEW HIGH#3**・後方互換）
    - L566-577 の predict_p_fukusho 呼出に `label_version=label_version, odds_snapshot_policy=odds_snapshot_policy, backtest_strategy_version=backtest_strategy_version` が伝播される（codex HIGH#3・WARNING#2 第2層）
    - return dict に3 provenance キーが追加される（codex HIGH#3）
    - orchestrator._assert_deterministic が `theta: float | None = None` 引数を取り・2回の train_and_predict 呼出に theta=theta を伝播（codex HIGH#8・files_modified に src/model/orchestrator.py が含まれる）
    - theta=None かつ3引数='unspecified' sentinel 既定で既存テストが全て GREEN（A5 後方互換・SC#4 回帰防止）
    - scripts/run_phase11_evaluation.py が orchestrator.train_and_predict 呼出で3引数を明示的に渡す（codex HIGH#3・WARNING#2 第1層・script 側）
    - **scripts/run_phase11_evaluation.py が orchestrator.train_and_predict 呼出で as_of_datetime=FIXED_REPRODUCE_TS を明示的に渡す（codex cycle-2 NEW HIGH#2・checksum bit-identical のため・datetime.now(UTC) 既定を使わない）**
    - scripts/run_phase11_evaluation.py が prediction_load.load_predictions（public wrapper・実シグネチャ write_cur, prediction_df, reader_role=None）を呼び・private _idempotent_load_prediction に生 DataFrame を渡さない（codex HIGH#4・`_idempotent_load_prediction` が run_phase11_evaluation.py に 0件）
    - run_phase11_evaluation.py が Task 1 で定義した両 schema migration（PREDICTION_ADD_PROVENANCE_SQL + PREDICTION_EXTEND_MODEL_TYPE_DOMAIN_SQL・codex cycle-2 NEW HIGH#1）を etl cursor で適用（idempotent）
    - **AST check で set_primary_model が Call されていない（D-07 is_primary 立てない・codex cycle-2 LOW・docstring の "do not call" 言及で grep gate が false positive にならないよう AST）**
    - run_phase11_evaluation.py が _assert_deterministic(theta=selected_theta) を呼出（deterministic smoke・codex MEDIUM）
    - test_race_relative.py の test_both_models_bit_identical docstring に「同一モデル同一 seed の再現性・cross-family 同一性でない」が明記される（codex MEDIUM）
    - tests/model/test_race_relative.py の12テスト・tests/audit/test_audit_race_relative.py の4テスト・tests/model/test_orchestrator.py が KEIBA_SKIP_DB_TESTS=1 で GREEN（回帰なし）
    - **本 task は orchestrator.py と run_phase11_evaluation.py と test_race_relative.py のみ編集**（schema.py / predict.py は Task 1・checker BLOCKER 責務分割）
  </acceptance_criteria>
  <done>
    orchestrator.train_and_predict が3新引数（sentinel 既定値）を取り predict_p_fukusho に伝播し・run_phase11_evaluation.py が load_predictions（public wrapper）で永続化し orchestrator 呼出で3引数 + 固定 as_of_datetime=FIXED_REPRODUCE_TS（codex cycle-2 NEW HIGH#2）を明示的に渡し・_assert_deterministic が theta=float で bit-identical を検証可能。codex HIGH#3 PART2/#4/#5/#8 + WARNING#2 4層ワイヤリング + cycle-2 NEW HIGH#2 解消。既存テスト GREEN。live-DB checkpoint（Task 3）で SC#2/SC#3/SC#5 を実証する準備完了。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: live-DB で SC#2 gate + SC#5 idempotent swap + SC#3 bit-identical を実証（永続化実装後・順序維持 HIGH#5 反転解消・codex cycle-2 NEW HIGH#1/#2/#3 実証）</name>
  <action>ユーザーが KEIBA_SKIP_DB_TESTS unset の live-DB 環境で scripts/run_phase11_evaluation.py を直接実行し・reports/11-evaluation/* の SC#2 gate 結果（PASS/FAIL honest 記録）・prediction.fukusho_prediction の model_version scoped idempotent swap（2回実行で checksum bit-identical・v1.0 binary 行保持・is_primary 立てない D-07・label_version/odds_snapshot_policy/backtest_strategy_version 列の永続化・**lightgbm_rr model_type が CHECK 制約拡張後 INSERT 可能 codex cycle-2 NEW HIGH#1**・**固定 as_of_datetime=FIXED_REPRODUCE_TS で checksum bit-identical codex cycle-2 NEW HIGH#2**・**sentinel/事前登録値で NOT NULL 違反回避 codex cycle-2 NEW HIGH#3**）・orchestrator の SC#3 bit-identical（FIXED_REPRODUCE_TS + np.array_equal・theta=selected_theta）を確認する。core value（リーク防止・再現性）の最終実データ実証・Phase 12 引き継ぎ材料の確保。</action>
  <what-built>
    Phase 11 Wave 0-3 で構築した race-relative 補正層（race_relative.py）・orchestrator 統合（theta/score_split 引数・_normalize_model_type）・artifact metadata 拡張・adversarial audit（D-10/SC#4）・評価スクリプト（run_phase11_evaluation.py）と・Wave 4 Task 1 で実装した SC#5 schema migration（label_version/odds_snapshot_policy/backtest_strategy_version 列・DEFAULT 'unspecified' sentinel・codex cycle-2 NEW HIGH#3）+ **prediction_model_type_domain CHECK 制約 lightgbm_rr/catboost_rr 拡張（codex cycle-2 NEW HIGH#1）** + PREDICTION_COLUMNS 拡張 + predict_p_fukusho 新3引数（sentinel 既定値）・Task 2 で実装した orchestrator.train_and_predict 新3引数（3層ワイヤリング・sentinel 既定値）+ load_predictions public wrapper 呼出 + **固定 as_of_datetime=FIXED_REPRODUCE_TS 渡し（codex cycle-2 NEW HIGH#2）** + orchestrator._assert_deterministic theta 伝播が・live-DB で SC#2 gate・SC#5 idempotent swap・SC#3 bit-identical を満たすかを実証する。ユーザーが KEIBA_SKIP_DB_TESTS unset の live-DB 環境で直接実行・結果を確認する。codex review HIGH#3/#4/#5/#8 + WARNING#2 + cycle-2 NEW HIGH#1/#2/#3 が実装済みであることを含めて検証。
  </what-built>
  <how-to-verify>
    【前提】PostgreSQL 15.18 が起動し・EveryDB2・feature snapshot `20260626-1a-opponentstrength-v1` が存在すること（Phase 10 完成・SC#3 の入力）。KEIBA_SKIP_DB_TESTS が unset（live-DB 実行）。Task 1 の schema migration（provenance 3列 DEFAULT 'unspecified' + CHECK 制約 lightgbm_rr/catboost_rr 拡張）が適用済み・Task 2 の orchestrator 拡張と load_predictions ワイヤリング（固定 as_of_datetime 含む）が適用済み。

    【手順 0: schema migration 適用確認（codex cycle-2 NEW HIGH#1/#3）】
    - run_phase11_evaluation.py の main 先頭が両 migration（PREDICTION_ADD_PROVENANCE_SQL + PREDICTION_EXTEND_MODEL_TYPE_DOMAIN_SQL）を etl cursor で実行（idempotent）
    - psql で prediction.fukusho_prediction の CHECK 制約が拡張済みか確認:
    ```
    psql -c "SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname = 'prediction_model_type_domain';"
    ```
    - 出力に 'lightgbm_rr' と 'catboost_rr' が含まれることを確認（codex cycle-2 NEW HIGH#1）
    - 3列の DEFAULT が 'unspecified' であることを確認:
    ```
    psql -c "SELECT column_name, column_default FROM information_schema.columns WHERE table_schema='prediction' AND table_name='fukusho_prediction' AND column_name IN ('label_version','odds_snapshot_policy','backtest_strategy_version');"
    ```
    - column_default に 'unspecified' が含まれることを確認（codex cycle-2 NEW HIGH#3）

    【手順 1: live-DB SC#2 gate + θ 選択経路（score_split="calib" のみ）】
    ```
    cd /Users/hart/develop/keiba-ai-v3
    unset KEIBA_SKIP_DB_TESTS
    uv run python scripts/run_phase11_evaluation.py \
        --baseline-snapshot-id 20260626-1a-opponentstrength-v1 \
        --bt-split BT-1 \
        --odds-snapshot-policy 30min_before \
        --theta-candidates 0.5 0.75 1.0 1.25 1.5 \
        --out-dir reports
    ```
    - reports/11-evaluation/11-evaluation.md と 11-evaluation.json が生成されることを確認
    - gate verdict（PASS/FAIL）を確認・PASS/FAIL 問わず §11.2 聖域（事前登録許容幅・θ 選び直し禁止・score_split="calib" で θ 選択）が守られていることを確認（codex HIGH#1）
    - reports/11-evaluation/theta-selection.json に候補毎の (Brier, LogLoss, AUC, overprediction_penalty, calib_max_dev, selected_only_calib_max_dev, verdict) と選択経路（足切り→選択→tie-break）が記録されていることを確認・test 窓評価より前に書き出されていることを確認
    - D-05 改善 gate 3条件（overprediction penalty / selected-high-EV / selected-only calib_max_dev）の各判定を確認

    【手順 2: SC#5 model_version-scoped idempotent swap（load_predictions public wrapper・2回実行で checksum bit-identical・codex HIGH#4・codex cycle-2 NEW HIGH#1/#2/#3）】
    - run_phase11_evaluation.py が prediction_load.load_predictions（public wrapper・実シグネチャ write_cur, prediction_df, reader_role=None）で race-relative model の行を model_version scoped swap で追加（v1.0 binary 行は保持・codex HIGH#4）
    - **model_type="lightgbm_rr" が拡張済み CHECK 制約で受け入れられることを確認（codex cycle-2 NEW HIGH#1・CHECK 違反で INSERT 失敗しない）**
    - **2回実行して prediction.fukusho_prediction の model_version = "...-lgbrr-v1" 行の checksum が bit-identical になることを確認（HIGH#1 idempotent・as_of_datetime=FIXED_REPRODUCE_TS 固定で PK が一致・codex cycle-2 NEW HIGH#2）**
    - v1.0 binary（model_version = "...-lgb-v1"・is_primary=true）行が残っていることを確認（D-07 is_primary 立てない）
    - 3つの §19.1 metadata 列（label_version / odds_snapshot_policy / backtest_strategy_version）が race-relative 行に永続化されていることを確認（codex HIGH#3・WARNING#2 4層ワイヤリングの実証）・**sentinel 'unspecified' でなく事前登録値であること**（run_phase11_evaluation.py が明示的に値を渡したことの確認・codex cycle-2 NEW HIGH#3）
    ```
    psql -c "SELECT model_type, model_version, is_primary, label_version, odds_snapshot_policy, backtest_strategy_version, as_of_datetime, COUNT(*) FROM prediction.fukusho_prediction GROUP BY model_type, model_version, is_primary, label_version, odds_snapshot_policy, backtest_strategy_version, as_of_datetime ORDER BY model_version;"
    ```
    - 2つの model_version（v1.0 binary・race-relative）が存在し・is_primary=true は v1.0 binary のみ・race-relative 行の model_type='lightgbm_rr'（CHECK 制約拡張で許容）・race-relative 行の3 metadata 列が sentinel 'unspecified' でなく事前登録値・**as_of_datetime が両実行で FIXED_REPRODUCE_TS（2026-06-20 00:00:00+00）に一致（codex cycle-2 NEW HIGH#2）**であることを確認

    【手順 3: SC#3 bit-identical（race-relative model・FIXED_REPRODUCE_TS + np.array_equal・codex MEDIUM・同一モデル同一 seed）】
    - orchestrator.train_and_predict を theta=selected_theta・as_of_datetime=FIXED_REPRODUCE_TS・固定 seed/thread で2回呼出し・p_fukusho_hit が np.array_equal で bit-identical になることを確認（同一 LightGBM race-relative model の再現性・LightGBM≠CatBoost 同一性でない）
    - 確認方法: run_phase11_evaluation.py の _assert_deterministic(theta=selected_theta) 呼出（Task 2 で追加）が GREEN で完走・または独立した reproduce smoke で検証
    - VALIDATION.md Test Map L57 test_both_models_bit_identical が live-DB で GREEN になることも確認（同一モデル同一 seed の再現性・LightGBM と CatBoost の cross-family 同一性でない・docstring 明記 codex MEDIUM）

    【手順 4: 全套 audit テスト GREEN（KEIBA_SKIP_DB_TESTS unset・live-DB フルスイート）】
    ```
    unset KEIBA_SKIP_DB_TESTS
    uv run pytest tests/ -x
    ```
    - tests/model/test_race_relative.py（12テスト）・tests/audit/test_audit_race_relative.py（4テスト）・既存の tests/audit/*（Phase 8/9/10）が全て GREEN
    - SC#1/SC#2/SC#3 踏襲（v1.0 対抗的監査パターン）

    【期待結果】
    - SC#2 gate: PASS でも FAIL でも honest 記録（§11.2 聖域・Phase 12 で is_primary 切替判断）
    - SC#5 idempotent: 2回実行で checksum bit-identical・v1.0 binary 行保持・§19.1 metadata 列永続化（sentinel でなく事前登録値・codex HIGH#3/#4・WARNING#2）・model_type='lightgbm_rr' が CHECK 制約で許容（codex cycle-2 NEW HIGH#1）・as_of_datetime=FIXED_REPRODUCE_TS 固定で PK 一致（codex cycle-2 NEW HIGH#2）
    - SC#3 bit-identical: FIXED_REPRODUCE_TS + np.array_equal GREEN（同一モデル同一 seed・codex MEDIUM）
    - 全套テスト GREEN（live-DB フルスイート）
  </how-to-verify>
  <resume-signal>
    "approved"（全手順 GREEN・SC#2/SC#3/SC#5 実証完了・codex HIGH#3/#4/#5/#8 + WARNING#2 + cycle-2 NEW HIGH#1/#2/#3 解消確認・3 Task 分割で実装が checkpoint に先行）または失敗内容の詳細（どの手順が RED か・reports/11-evaluation/* の内容・psql 出力等）。FAIL の場合は Phase 11 の修正（race_relative.py / orchestrator.py / run_phase11_evaluation.py / schema.py / predict.py）または Phase 12 への引き継ぎ（SC#2 gate FAIL でも honest 記録されていれば Phase 12 で is_primary 切替見送り等の判断材料になる）。
  </resume-signal>
  <files>reports/11-evaluation/11-evaluation.md, reports/11-evaluation/11-evaluation.json, reports/11-evaluation/theta-selection.json, prediction.fukusho_prediction</files>
  <verify>
    <human-check>
      live-DB（KEIBA_SKIP_DB_TESTS unset）で完走し reports/11-evaluation/{11-evaluation.md, 11-evaluation.json, theta-selection.json} を生成・prediction.fukusho_prediction に model_type='lightgbm_rr' / model_version="...-lgbrr-v1" 行が追加され（CHECK 制約拡張で許容 codex cycle-2 NEW HIGH#1）v1.0 binary 行が保持・label_version/odds_snapshot_policy/backtest_strategy_version が**sentinel 'unspecified' でなく事前登録値で**永続化（codex HIGH#3・WARNING#2 4層ワイヤリング実証・codex cycle-2 NEW HIGH#3）・**as_of_datetime が両実行で FIXED_REPRODUCE_TS に一致し**（codex cycle-2 NEW HIGH#2）2回実行で checksum bit-identical・is_primary=true は v1.0 binary のみ（D-07）・全套 pytest GREEN（how-to-verify の手順0-4 を実施）。
    </human-check>
  </verify>
  <done>
    live-DB で SC#2 gate が honest 記録・SC#5 idempotent swap が load_predictions public wrapper で checksum bit-identical（as_of_datetime=FIXED_REPRODUCE_TS 固定・codex cycle-2 NEW HIGH#2）・model_type='lightgbm_rr' が CHECK 制約拡張で許容（codex cycle-2 NEW HIGH#1）・SC#3 bit-identical が np.array_equal で GREEN（同一モデル同一 seed）・§19.1 metadata 列が sentinel でなく事前登録値で永続化（codex HIGH#3・WARNING#2・codex cycle-2 NEW HIGH#3）・全套 audit テスト GREEN・Phase 12 引き継ぎ材料確保。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live-DB → prediction.fukusho_prediction | model_version scoped swap で race-relative model 行を追加・v1.0 binary 行は保持（D-07・HIGH#1）・model_type='lightgbm_rr' が CHECK 制約拡張で許容（codex cycle-2 NEW HIGH#1）|
| 実データ → race_relative 補正層 | 実データの base logit が finite であること（D-09 fail-loud）・θ が calib slice で選ばれた事前登録値であること（§11.2 聖域）|
| orchestrator → predict_p_fukusho → PREDICTION_COLUMNS → schema.py | §19.1 metadata 3列が sentinel/事前登録値で伝播されること（codex HIGH#3・WARNING#2 4層 key_link・Pitfall 4 3ファイル連鎖・codex cycle-2 NEW HIGH#3 sentinel で loader 空文字→None 回避）|
| run_phase11_evaluation.py → orchestrator（永続化パス） | as_of_datetime=FIXED_REPRODUCE_TS 固定で PK が一致し checksum bit-identical（codex cycle-2 NEW HIGH#2）|

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-18 | Tampering（idempotent swap で silent 履歴破壊・private API 誤用で rows 型不一致） | prediction_load.load_predictions（public wrapper 経由・codex HIGH#4） | high | mitigate | model_version scoped swap（同一 model_type+model_version のみ DELETE→INSERT・他 model_version 行は保持）・load_predictions public wrapper（実シグネチャ write_cur, prediction_df, reader_role=None）が prediction_df を受け取り内部で _df_to_prediction_tuples で rows 変換（codex HIGH#4・private _idempotent_load_prediction に生 DataFrame を渡さない）・2回実行で checksum bit-identical を実証（HIGH#1・as_of_datetime=FIXED_REPRODUCE_TS 固定で PK 一致・codex cycle-2 NEW HIGH#2・RESEARCH Security Domain L715） |
| T-11-19 | Repudiation（SC#2 gate 結果の隠蔽・改窜・§19.1 metadata 欠落/空文字で再現性崩壊） | reports/11-evaluation/* と prediction.fukusho_prediction §19.1 列 | high | mitigate | SC#2 gate は PASS/FAIL 問わず honest に記録・theta-selection.json に候補毎の評価値と選択経路を byte-reproducible に出力（後知恵すり替え禁止・§11.2 聖域・D-05）・更に PREDICTION_COLUMNS に label_version/odds_snapshot_policy/backtest_strategy_version を追加（schema migration idempotent・DEFAULT 'unspecified' sentinel・codex HIGH#3・codex cycle-2 NEW HIGH#3）し・orchestrator→predict_p_fukusho→PREDICTION_COLUMNS の3層ワイヤリング（WARNING#2）で SC#5 §19.1 metadata が prediction 行に**sentinel 'unspecified' でなく事前登録値で**永続化される（run_phase11_evaluation.py が明示的に値を渡す） |
| T-11-20 | Tampering（SC#3 bit-identical 回帰・非決定論的 thread/seed・cross-family 誤解） | orchestrator._assert_deterministic theta=float | critical | mitigate | FIXED_REPRODUCE_TS + 固定 seed=42 + thread=1 + np.array_equal で2回 train_and_predict を実証・theta=float でも bit-identical（SC#3・同一モデル同一 seed の再現性・LightGBM≠CatBoost の cross-family 同一性でない・codex MEDIUM・review HIGH#7・§19.1 構造的ブロック） |
| T-11-21 | Elevation of Privilege（is_primary 誤設定で Phase 12 前 EV 本番切替） | prediction.fukusho_prediction is_primary | high | mitigate | set_primary_model を呼ばない（AST check で Call 検出・codex cycle-2 LOW・docstring grep false positive 回避）・is_primary=true は v1.0 binary のみ（D-07・CONTEXT D-07・SC#5・Phase 12 で切替判断） |
| T-11-22 | Information Disclosure（実データ logit 欠損 silent fallback） | live-DB base logit | high | mitigate | apply_race_relative_correction の D-09 fail-loud が実データで発火しないことを確認（feature snapshot の FEATURE_COLUMNS が PIT-correct・base estimator が NaN を native 処理して logit を出す）・発火した場合は RuntimeError で Phase 完了不可（silent fallback 禁止） |
| T-11-23 | Tampering（3層ワイヤリングの欠落で metadata が sentinel/事前登録値でなく永続化・SC#5 再現性崩壊） | orchestrator.train_and_predict → predict_p_fukusho → PREDICTION_COLUMNS | high | mitigate | Task 2 で orchestrator.train_and_predict に3新引数（sentinel 既定値）を追加し predict_p_fukusho 呼出に明示的に伝播・run_phase11_evaluation.py が orchestrator 呼出で明示的に値を渡す（WARNING#2 4層 key_link: script → orchestrator → predict_p_fukusho → PREDICTION_COLUMNS）・live-DB psql で3列が sentinel でなく事前登録値であることを検証（codex HIGH#3 完全解消） |
| T-11-24 | Tampering（prediction_model_type_domain CHECK 制約が lightgbm_rr/catboost_rr を拒否し SC#5 live-DB INSERT 失敗） | schema.py prediction_model_type_domain + PREDICTION_EXTEND_MODEL_TYPE_DOMAIN_SQL | high | mitigate | Task 1 で CHECK 制約を ('lightgbm','catboost','logreg','lightgbm_rr','catboost_rr') に拡張する idempotent migration（DROP IF EXISTS + ADD）を定義・PREDICTION_TABLE_DDL の新規 CREATE TABLE も拡張後制約を使用・live-DB で model_type='lightgbm_rr' が INSERT 可能を psql で検証（codex cycle-2 NEW HIGH#1） |
| T-11-25 | Tampering（as_of_datetime=datetime.now(UTC) 既定で PK が実行時刻になり 2回実行で checksum 不一致・SC#5 bit-identical 崩壊） | orchestrator.train_and_predict as_of_datetime + run_phase11_evaluation.py 永続化パス | critical | mitigate | run_phase11_evaluation.py が orchestrator.train_and_predict 呼出で as_of_datetime=FIXED_REPRODUCE_TS（orchestrator.py:88 既存定数）を明示的に渡す・PK as_of_datetime が両実行で一致し checksum bit-identical（codex cycle-2 NEW HIGH#2・§19.1 再現性・SC#5 機械保証） |
| T-11-26 | Denial of Service（新 NOT NULL 列の空文字 '' が loader で None 変換され NOT NULL 違反で INSERT 失敗・v1.0 binary 呼出も破壊） | _df_to_prediction_tuples (prediction_load.py:175-176) + 新3列 DEFAULT | high | mitigate | Task 1 で新3列の DEFAULT を空文字 '' でなく sentinel 'unspecified' に設定・predict_p_fukusho / orchestrator.train_and_predict の既定値も 'unspecified' sentinel・これにより loader の空文字→None 変換（L175-176）を経由しても None 化されず NOT NULL 違反回避・v1.0 binary 呼出（theta=None/3引数省略）でも sentinel が入り安全（codex cycle-2 NEW HIGH#3・対策 (c) sentinel 採用・loader 変更不要で局所的） |
| T-11-27 | Elevation of Privilege（theta=float + model_type が _rr サフィックスでない状態で race-relative 補正済み確率が binary model_version で刻印される silent provenance hole） | orchestrator.train_and_predict theta + model_type | high | mitigate | 11-03 Task 1 で theta is not None 時は model_type が _rr サフィックス必須とする guard（ValueError）を追加（codex cycle-2 MEDIUM）・race-relative 補正済み確率は必ず model_type='lightgbm_rr'/'catboost_rr' + model_version='...-lgbrr-v1'/'...-cbrr-v1' で刻印・binary model_version との混同を構造的ブロック |
</threat_model>

<verification>
- live-DB（KEIBA_SKIP_DB_TESTS unset）で scripts/run_phase11_evaluation.py が完走し reports/11-evaluation/{11-evaluation.md, 11-evaluation.json, theta-selection.json} を生成（theta-selection.json は test 窓評価より前に書き出し・codex HIGH#1）
- prediction.fukusho_prediction に model_type='lightgbm_rr'/model_version="...-lgbrr-v1" 行が追加され（CHECK 制約拡張で許容・codex cycle-2 NEW HIGH#1）・v1.0 binary（"...-lgb-v1"・is_primary=true）行が保持される（psql で確認）・label_version/odds_snapshot_policy/backtest_strategy_version 列が**sentinel 'unspecified' でなく事前登録値で**永続化される（codex HIGH#3・WARNING#2 4層ワイヤリング実証・codex cycle-2 NEW HIGH#3）
- **as_of_datetime が両実行で FIXED_REPRODUCE_TS（2026-06-20 00:00:00+00）に一致し**（codex cycle-2 NEW HIGH#2）・2回実行で model_version="...-lgbrr-v1" 行の checksum が bit-identical（SC#5 idempotent・load_predictions public wrapper 経由・codex HIGH#4）
- orchestrator._assert_deterministic(theta=selected_theta) が np.array_equal で GREEN（SC#3 bit-identical・実データ・同一モデル同一 seed の再現性・codex MEDIUM）
- `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/` が全套 GREEN（SC#1/SC#2/SC#3 踏襲・live-DB フルスイートは unset で）
- 3 Task 構成で Task1（schema+predict・CHECK 制約拡張・sentinel 既定値）→ Task2（orchestrator+script+test・固定 as_of_datetime）→ Task3（live-DB checkpoint）の順で実装が checkpoint に先行（checker BLOCKER 解消・HIGH#5 反転解消維持）
</verification>

<success_criteria>
- SC#2 gate 結果が reports/11-evaluation/ に honest 記録される（PASS/FAIL 問わず・§11.2 聖域・theta-selection.json は test 窓評価より前に書き出し codex HIGH#1）
- SC#5 §19.1 metadata（label_version/odds_snapshot_policy/backtest_strategy_version）が schema migration で PREDICTION_COLUMNS に追加され prediction 行に**sentinel 'unspecified' でなく事前登録値で**永続化（codex HIGH#3・WARNING#2 4層ワイヤリング: script → orchestrator → predict_p_fukusho → PREDICTION_COLUMNS・codex cycle-2 NEW HIGH#3 sentinel で loader 空文字→None 回避）
- **prediction_model_type_domain CHECK 制約が ('lightgbm','catboost','logreg','lightgbm_rr','catboost_rr') に拡張され model_type='lightgbm_rr' が INSERT 可能（codex cycle-2 NEW HIGH#1・idempotent DROP IF EXISTS + ADD migration）**
- **run_phase11_evaluation.py の永続化パスが as_of_datetime=FIXED_REPRODUCE_TS 固定で PK が両実行で一致し checksum bit-identical（codex cycle-2 NEW HIGH#2）**
- SC#5 idempotent swap が load_predictions（public wrapper・実シグネチャ write_cur, prediction_df, reader_role=None）経由で 2回実行 checksum bit-identical・v1.0 binary 行保持（codex HIGH#4・HIGH#1）
- SC#3 bit-identical が race-relative model で実データ実証（FIXED_REPRODUCE_TS + np.array_equal・同一モデル同一 seed の再現性・codex MEDIUM）
- orchestrator._assert_deterministic が theta=float に対応し src/model/orchestrator.py が files_modified に含まれる（codex HIGH#8）
- タスク構成: Task 1（schema+predict migration・codex HIGH#3 PART1・codex cycle-2 NEW HIGH#1/#3）→ Task 2（orchestrator+script ワイヤリング・codex HIGH#3 PART2/#4/#8 + WARNING#2・codex cycle-2 NEW HIGH#2）→ Task 3（live-DB checkpoint）の順で・各 Task の files_modified が 2-3 に収まり実装が checkpoint に先行（checker BLOCKER 解消・HIGH#5 反転解消維持）
- is_primary 立てない（D-07・v1.0 binary のみ is_primary=true・AST check で set_primary_model Call 検出 codex cycle-2 LOW）
- 全套 audit テスト GREEN（live-DB フルスイート・SC#1/#2/#3 踏襲）
- Phase 11 完了・Phase 12（p_lower EV + falsification）への引き継ぎ材料が整う
</success_criteria>

<output>
Create `.planning/phases/11-race-relative-probability-model/11-05-SUMMARY.md` when done
</output>
