---
phase: 11-race-relative-probability-model
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/model/race_relative.py
  - tests/model/test_race_relative.py
  - tests/audit/test_audit_race_relative.py
autonomous: true
requirements:
  - MODEL-01
  - SAFE-01
tags: [model, race-relative, wave0, tdd-stub]

must_haves:
  truths:
    - "src/model/race_relative.py が存在し・VALIDATION.md Test Map の全テストが import できる公開 API（定数 + 関数シグネチャ）を露出する（D-02/D-03/D-05/D-06/D-09/D-10・SC#1/#3/#4）"
    - "tests/model/test_race_relative.py と tests/audit/test_audit_race_relative.py が存在し・KEIBA_SKIP_DB_TESTS=1 で pytest collection が成功する（lazy import・module-level import 失敗なし）"
    - "race_relative.py の関数は stub（NotImplementedError）だが定数 ALPHA_SEARCH_XTOL=1e-9 / P_CAL_CLIP_EPSILON=1e-6 / THETA_CANDIDATES は実値で定義される（事前登録値・§11.2 聖域）"
    - "race_relative.py の AST に odds/ninki/fukuodds/ninkij/tansyouodds の Name/Attribute が0件であることを test_audit_race_relative.py が import 时に検査できる（SAFE-01・SC#4）"
  artifacts:
    - "src/model/race_relative.py"
    - "tests/model/test_race_relative.py"
    - "tests/audit/test_audit_race_relative.py"
  key_links:
    - "race_relative.py __all__ ← test_race_relative.py import"
    - "race_relative.py __all__ ← test_audit_race_relative.py import"
---

<objective>
Phase 11 Wave 0: 新規モジュール `src/model/race_relative.py` の公開 API と事前登録定数を stub で定義し・VALIDATION.md Test Map（MODEL-01 / SAFE-01 / D-09 / D-10 / SC#3 / SC#4 / SC#5 計16テスト）に対応する test stub を RED 状態で作成する（Nyquist 連続性・TDD RED phase）。

Purpose: 後続 Wave 1 の補正層実装が・既に存在する test 契約（シグネチャ + 期待挙動 docstring）を満たす形で進むようにする。stub は「実装なし・契約のみ」を明示し・Wave 1 が埋める。これにより実装と test の乖離（fake-green）を構造的に防ぐ。
Output: race_relative.py（stub・NotImplementedError を raise）・test_race_relative.py（16テスト RED）・test_audit_race_relative.py（adversarial 5段階鋳型 RED）
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-race-relative-probability-model/11-CONTEXT.md
@.planning/phases/11-race-relative-probability-model/11-RESEARCH.md
@.planning/phases/11-race-relative-probability-model/11-PATTERNS.md
@.planning/phases/11-race-relative-probability-model/11-VALIDATION.md

# analog（必読・idiom 踏襲元）
@src/utils/calibrator.py
@tests/model/test_calibrator.py
@tests/audit/test_audit_field_strength.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: race_relative.py 公開 API stub + 事前登録定数</name>
  <files>src/model/race_relative.py</files>
  <read_first>
    - .planning/phases/11-race-relative-probability-model/11-RESEARCH.md（Pattern 1 lines 243-349・Pattern 2 lines 351-378・Pattern 3 lines 380-449・事前登録値 ALPHA_SEARCH_XTOL=1e-9 / P_CAL_CLIP_EPSILON=1e-6）
    - .planning/phases/11-race-relative-probability-model/11-PATTERNS.md（race_relative.py セクション lines 36-137・定数・__all__・import pattern）
    - .planning/phases/11-race-relative-probability-model/11-VALIDATION.md（Test Map・API 契約の正）
    - src/utils/calibrator.py（pure 関数 + `raise ValueError` guard + `from __future__ import annotations` idiom の踏襲元・L30-104）
  </read_first>
  <behavior>
    - 事前登録定数 ALPHA_SEARCH_XTOL = 1e-9 / ALPHA_SEARCH_RTOL = 1e-12 / ALPHA_SEARCH_MAXITER = 200 / ALPHA_SEARCH_BOUNDS = (-100.0, 100.0) が実値で定義される（D-02・researcher 裁量 #1・RESEARCH Pattern 1）
    - 事前登録定数 P_CAL_CLIP_EPSILON = 1e-6 が実値で定義される（researcher 裁量 #2・RESEARCH Pattern 1・Pitfall 1 対策）
    - 事前登録定数 THETA_CANDIDATES = (0.5, 0.75, 1.0, 1.25, 1.5) が実値で定義される（D-03 候補集合・θ=1 baseline 含む・尖鋭化発散で θ<0.5 除外・RESEARCH Open Question #1 推奨）
    - 公開関数 solve_alpha_for_race(s_logits: np.ndarray, theta: float, k: int) -> float が定義される（stub・raise NotImplementedError）
    - 公開関数 apply_race_relative_correction(p_cal, theta, k_per_race, race_ids) -> np.ndarray が定義される（stub・raise NotImplementedError）
    - 公開関数 compute_overprediction_penalty(y_true, y_pred, odds, *, cell_filter_mask=None) -> float が定義される（stub・raise NotImplementedError）
    - __all__ が上記 3 定数 + 3 関数を明示する
    - AST に odds/ninki/fukuodds/ninkij/tansyouodds の Name/Attribute が0件（SAFE-01・SC#4）
  </behavior>
  <action>
    src/utils/calibrator.py の idiom（`from __future__ import annotations` + numpy/scipy import + モジュール先頭定数 + `raise ValueError`/`RuntimeError` guard + docstring）を踏襲し・新規 `src/model/race_relative.py` を作成する。

    【モジュール先頭 docstring】Phase 11 SC#1/#3/#4/D-02〜D-10 の race-relative 補正層（pure 関数）であることを明記。binary 本体不変（D-01）・補正層のみ追加・logit temperature θ + per-race α_r 二分探索（D-02）・test 窓 outcome 不使用（D-10）を docstring で明示。

    【import】RESEARCH Pattern 1 lines 247-249・PATTERNS lines 42-52 準拠: `from __future__ import annotations` / `import numpy as np` / `import pandas as pd` / `from scipy.optimize import brentq`（brentq は Wave 1 実装で使用・stub では import のみ）。binning 契約の import 再利用は Wave 1（11-02）で追加・本 stub では不要（NotImplementedError なので segment_eval import 未使用）。

    【定数定義】PATTERNS lines 56-65 + RESEARCH Pattern 1 lines 252-259 の数値をそのまま実値で定義:
    - ALPHA_SEARCH_XTOL: float = 1e-9（docstring で |sum(p)-k| 理論上界 ≈ 2e-9・実測 4.44e-16 を明記）
    - ALPHA_SEARCH_RTOL: float = 1e-12
    - ALPHA_SEARCH_MAXITER: int = 200
    - ALPHA_SEARCH_BOUNDS: tuple[float, float] = (-100.0, 100.0)
    - P_CAL_CLIP_EPSILON: float = 1e-6（docstring で logit 動的範囲 ±13.8・sum(p)=k 精度 <1e-12 を明記）
    - THETA_CANDIDATES: tuple[float, ...] = (0.5, 0.75, 1.0, 1.25, 1.5)（docstring で D-03 事前登録・θ=1 baseline・尖鋭化発散で θ<0.5 除外・§11.2 聖域 test 窓選び直し禁止を明記）

    【公開関数 stub】3 関数のシグネチャ + docstring（Parameters/Returns/Raises）を定義し・本体は `raise NotImplementedError("Phase 11 Wave 1 (11-02) で実装")` のみ。docstring は RESEARCH Pattern 1/2/3 の docstring をそのまま転記（数学的健全性・境界挙動・D-09 fail-loud・D-10 自己完結を明記）。これにより Wave 1 実装者が契約を逃さない。

    solve_alpha_for_race(s_logits, theta, k): RESEARCH Pattern 1 lines 262-300 の docstring 転記。apply_race_relative_correction(p_cal, theta, k_per_race, race_ids): RESEARCH Pattern 1 lines 315-348 の docstring 転記（step 6-8・D-09 fail-loud）。compute_overprediction_penalty(y_true, y_pred, odds, *, cell_filter_mask=None): RESEARCH Pattern 3 lines 402-449 の docstring 転記（半波整流 ECE・bit-identical binning）。

    【__all__】PATTERNS lines 129-137 準拠: ALPHA_SEARCH_XTOL / P_CAL_CLIP_EPSILON / THETA_CANDIDATES / solve_alpha_for_race / apply_race_relative_correction / compute_overprediction_penalty の 6 要素。

    【SAFE-01・SC#4】モジュール内に odds/ninki/fukuodds/ninkij/tansyouodds の Name/Attribute を一切書かない（docstring の "odds-free" 言及は whitelist 対象だが・本 stub では market 用語を出さない方が安全・「市場情報 proxy 不使用」等の一般化表現を使う）。
  </action>
  <verify>
    <automated>uv run python -c "from src.model import race_relative; assert race_relative.ALPHA_SEARCH_XTOL == 1e-9; assert race_relative.P_CAL_CLIP_EPSILON == 1e-6; assert race_relative.THETA_CANDIDATES == (0.5, 0.75, 1.0, 1.25, 1.5); assert 'solve_alpha_for_race' in race_relative.__all__; assert 'apply_race_relative_correction' in race_relative.__all__; assert 'compute_overprediction_penalty' in race_relative.__all__; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - src/model/race_relative.py が存在し `from src.model import race_relative` で import できる
    - ALPHA_SEARCH_XTOL == 1e-9 / ALPHA_SEARCH_RTOL == 1e-12 / ALPHA_SEARCH_MAXITER == 200 / ALPHA_SEARCH_BOUNDS == (-100.0, 100.0) / P_CAL_CLIP_EPSILON == 1e-6 / THETA_CANDIDATES == (0.5, 0.75, 1.0, 1.25, 1.5) が全て実値で定義される（事前登録・§11.2 聖域）
    - 3 関数が定義され・呼出で NotImplementedError を raise する（stub）
    - __all__ が 3 定数 + 3 関数の 6 要素を含む
    - `uv run python -c "import ast, inspect, src.model.race_relative as m; tree=ast.parse(inspect.getsource(m)); names={n.id for n in ast.walk(tree) if isinstance(n, ast.Name)}; attrs={a.attr for a in ast.walk(tree) if isinstance(a, ast.Attribute)}; forb={'odds','ninki','fukuodds','ninkij','tansyouodds'}; assert not (names & forb), names & forb; assert not (attrs & forb), attrs & forb; print('SAFE-01 OK')"` が exit 0（SC#4 AST odds/ninki proxy 0件・stub 段階で保証）
  </acceptance_criteria>
  <done>
    race_relative.py が事前登録定数（θ 候補・ε・xtol）を実値で定義し・3 公開関数の stub（NotImplementedError）と __all__ を持つ。import 可能。AST に odds/ninki proxy 0件。
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: test_race_relative.py unit test stub（RED・16テスト契約）</name>
  <files>tests/model/test_race_relative.py</files>
  <read_first>
    - .planning/phases/11-race-relative-probability-model/11-VALIDATION.md（Per-Requirement Test Map lines 42-59・16テストの正確なコマンドと期待挙動）
    - .planning/phases/11-race-relative-probability-model/11-PATTERNS.md（test_race_relative.py セクション lines 222-270・ヘッダ docstring・test 関数 pattern・DB 不要）
    - .planning/phases/11-race-relative-probability-model/11-RESEARCH.md（Pattern 1/2/3・Code Examples lines 515-582・実証済み期待値 |sum-k|=4.44e-16 等）
    - tests/model/test_calibrator.py（pure 関数 unit test idiom・`from __future__ import annotations` + pytest + np.random.default_rng・L1-80）
  </read_first>
  <behavior>
    - tests/model/test_race_relative.py が存在し・KEIBA_SKIP_DB_TESTS=1 で `uv run pytest tests/model/test_race_relative.py --co -q` が collection 成功（16テスト全て認識）
    - 各テストが VALIDATION.md Test Map のテスト名（test_solve_alpha_sum_p_equals_k / test_alpha_monotonic_unique_solution / test_theta_inf_limit / test_theta_zero_divergence / test_clip_epsilon_isotonic_endpoints / test_race_independence / test_base_logit_bit_identical / test_pipeline_sum_p_invariant / test_overprediction_penalty_binning_parity / test_k_determination_no_deadheat / test_logit_missing_fail_loud / test_both_models_bit_identical）を正確に定義する
    - 各テストが docstring で検証内容（MODEL-01/D-09/SC#3・期待数値）を明記する
    - 全テストが stub（NotImplementedError）で fail する（RED・TDD）
  </behavior>
  <action>
    tests/model/test_calibrator.py の idiom（`from __future__ import annotations` + `import numpy as np` + `import pytest` + `from src.model.race_relative import ...` + `def test_*():` + docstring + assert）を踏襲し・新規 `tests/model/test_race_relative.py` を作成する。

    【ヘッダ docstring】PATTERNS lines 228-248 の docstring テンプレートを転記（検証内容箇条書き・SC# 番号参照・RESEARCH Pattern 参照・DB 不要・KEIBA_SKIP_DB_TESTS=1 で実行可能を明記）。

    【import】`from src.model.race_relative import (ALPHA_SEARCH_XTOL, P_CAL_CLIP_EPSILON, THETA_CANDIDATES, solve_alpha_for_race, apply_race_relative_correction, compute_overprediction_penalty)`（Task 1 で作成した stub から import・collection 保証）。

    【テスト関数】VALIDATION.md Test Map の16テスト（うち unit 11 + integration 1 = 12テストを本ファイルに・adversarial 2 + AST 1 + live-DB 1 は別ファイル）を定義。各テストは:
    - docstring で VALIDATION.md の Behavior と期待数値（|sum-k| < 1e-9・実測 4.44e-16・atol=1e-6・k=3/2 等）を明記
    - `rng = np.random.default_rng(42)` で決定論的合成データを生成
    - stub 関数を呼出し・結果を assert（stub は NotImplementedError なので pytest.raises(NotImplementedError) で wrap・または実装後に GREEN になる通常 assert を書き・今は RED）

    RED 戦略: 各テストは「実装済みなら GREEN・stub なら RED（NotImplementedError）」になる通常 assert 形式で書く。例: `alpha = solve_alpha_for_race(s, theta=1.0, k=3); assert abs(sum_p - 3) < 1e-9`。stub は NotImplementedError で fail → RED。Wave 1 で実装されると GREEN。これが TDD RED phase。

    テスト一覧（VALIDATION.md Test Map の行番号参照）:
    1. test_solve_alpha_sum_p_equals_k（L44・MODEL-01・|sum-k| < 1e-9）
    2. test_alpha_monotonic_unique_solution（L45・MODEL-01・IVT・単調増加）
    3. test_theta_inf_limit（L46・MODEL-01・θ→∞ で α → logit(k/n)）
    4. test_theta_zero_divergence（L47・MODEL-01・θ→0+ で brentq 失敗）
    5. test_clip_epsilon_isotonic_endpoints（L48・MODEL-01・ε=1e-6 で isotonic 0/1 端点・精度 <1e-12）
    6. test_race_independence（L49・MODEL-01・apply_race_relative_correction が race 毎独立）
    7. test_base_logit_bit_identical（L50・MODEL-01・logit(proba) 経路 atol=1e-6）
    8. test_pipeline_sum_p_invariant（L51・MODEL-01・完全パイプラインで sum(p)=k）
    9. test_overprediction_penalty_binning_parity（L52・MODEL-01・segment_eval binning と bit-identical）
    10. test_k_determination_no_deadheat（L53・MODEL-01・k=sales_start_entry_count ベース・同着不反映 D-08）
    11. test_logit_missing_fail_loud（L54・SAFE-01/D-09・p_cal 欠損で RuntimeError）
    12. test_both_models_bit_identical（L57・SC#3・同一モデル同一 seed で race-relative p が bit-identical・小モデル合成データ・codex review MEDIUM: LightGBM≠CatBoost の cross-family 同一性でない）

    test_both_models_bit_identical は同一 LightGBM 小モデルを同一 seed で2回 fit し race-relative 適用後の p が np.array_equal になることを検証する（SC#3 = 「同一モデル同一 seed の再現性」・codex review MEDIUM・docstring 明記）。LightGBM と CatBoost の cross-family 同一性を主張しない（別モデルなので当然一致しない・誤解を排除）。DB 不要。
  </action>
  <verify>
    <automated>KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/model/test_race_relative.py --co -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/model/test_race_relative.py が存在し `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/model/test_race_relative.py --co -q` が12テストを認識して collection 成功（exit 0・no errors）
    - VALIDATION.md Test Map の12テスト名（test_solve_alpha_sum_p_equals_k 等）が全て定義される
    - 各テストに docstring があり・検証内容と期待数値を明記する
    - `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/model/test_race_relative.py -x` が NotImplementedError で fail する（RED・TDD・stub なので全テスト RED だが collection は成功）
  </acceptance_criteria>
  <done>
    test_race_relative.py が12テストの stub-ready 契約（docstring + 期待数値 + assert）を持ち・KEIBA_SKIP_DB_TESTS=1 で collection 成功・実行は全テスト RED（NotImplementedError）。
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: test_audit_race_relative.py adversarial test stub（RED・D-10/SC#4）</name>
  <files>tests/audit/test_audit_race_relative.py</files>
  <read_first>
    - .planning/phases/11-race-relative-probability-model/11-VALIDATION.md（adversarial/AST テスト L55-58・D-10/SC#4）
    - .planning/phases/11-race-relative-probability-model/11-PATTERNS.md（test_audit_race_relative.py セクション lines 274-334・5段階鋳型・forbidden tokens・D-10 adversarial idiom）
    - .planning/phases/11-race-relative-probability-model/11-RESEARCH.md（Security Domain lines 688-715・8脅威パターン・D-10 自己完結性）
    - tests/audit/test_audit_field_strength.py（5段階鋳型 (a)(b)(c) AST・(d) allowlist・(g) false-pass・(6) monkeypatch lookahead・L1-502）
  </read_first>
  <behavior>
    - tests/audit/test_audit_race_relative.py が存在し・KEIBA_SKIP_DB_TESTS=1 で collection 成功（4テスト認識）
    - test_no_odds_ninki_proxy（SC#4・AST odds/ninki proxy 0件）が定義される
    - test_alpha_self_contained_outcome_swap（D-10・outcome 入れ替えで α_r/p 不変）が定義される
    - test_alpha_cross_race_leak_detected（D-10・他 race logit 混入検出）が定義される
    - test_false_pass_detection_power（(g)・意図的注入を検出することの証明）が定義される
    - test_no_odds_ninki_proxy は stub（NotImplementedError）でなく・即時実装可能な AST 検査（Task 1 の stub が既に SAFE-01 を満たすため GREEN になるべき）
  </behavior>
  <action>
    tests/audit/test_audit_field_strength.py の5段階鋳型 idiom（_FORBIDDEN_TOKENS frozenset・_scan_module_for_forbidden_tokens・_PROXY_PATTERN_BASE・_ODDS_PATTERN・_is_whitelisted_odds_prose・AST Name/Attribute/Constant 走査・monkeypatch）を踏襲し・新規 `tests/audit/test_audit_race_relative.py` を作成する。

    【ヘッダ docstring】PATTERNS lines 280-296 の docstring テンプレートを転記（5段階鋳型 (a)(b)(c)(d)(g)(6)・SC#4/D-10・SAFE-01 横断聖域・DB 不要・KEIBA_SKIP_DB_TESTS=1 を明記）。

    【import】`from src.model import race_relative`（Task 1 stub）+ AST/inspect/textwrap/re/Path/pytest/numpy。

    【forbidden tokens + scanner（codex review MEDIUM: string-constant 走査方針明記）】test_audit_field_strength.py L41-136 の _FORBIDDEN_TOKENS / _FORBIDDEN_PROXY_SUBSTRING_TOKENS_BASE / _ODDS_IN_SQL_WHITELIST / _PROXY_PATTERN_BASE / _ODDS_PATTERN / _is_whitelisted_odds_prose / _scan_module_for_forbidden_tokens を race_relative モジュール向けに再実装（共通 helper に切り出すのでなく・5段階鋳型の独立性維持・PATTERNS L304-308 の方針）。

    **AST scanner 方針（codex MEDIUM・SC#4 の真正性保証）:** test_audit_field_strength.py:91 は Name/Attribute node のみでなく `ast.Constant`（文字列定数）も走査する。race_relative 向け scanner はこれと**完全一致**の挙動にする（Name/Attribute + Constant string の3走査）。これにより docstring やコメント内の "odds" 文字列も検知する。但し・race_relative.py の docstring が SAFE-01 の文脈で「市場情報 proxy 不使用」等の一般化表現を使う場合・それが false-positive で落とされないよう `_is_whitelisted_odds_prose` に相当する allowlist を race_relative 用に調整する（test_audit_field_strength.py L91-136 の allowlist idiom 踏襲）。方針は「文字列定数も走査するが・SAFE-01 文脈の説明文字列は allowlist で許可」・docstring に明記。

    【テスト関数】
    1. test_no_odds_ninki_proxy（SC#4・即時 GREEN 可能）: `_scan_module_for_forbidden_tokens(race_relative)` を呼出し・name_attr も const_str も空であることを assert。Task 1 stub が SAFE-01 を満たすため GREEN になる。
    2. test_alpha_self_contained_outcome_swap（D-10・RED）: solve_alpha_for_race を2回呼出し・outcome を入れ替えても α_r が同一であることを assert。stub は NotImplementedError で RED。docstring で D-10 adversarial 5段階鋳型(6)・test_audit_field_strength.py::test_lookahead_injection_detected idiom 踏襲を明記。
    3. test_alpha_cross_race_leak_detected（D-10・RED）: apply_race_relative_correction で race R1 と R2 が独立に処理されること・R1 の logit を変えても R2 の p_final が不変であることを assert。stub は RED。
    4. test_false_pass_detection_power（(g)・即時 GREEN 可能）: test_audit_field_strength.py L282-372 と同一構造・ダミーソースに odds/ninki を注入し scanner が検出することを証明。stub 依存なしで GREEN。

    RED 戦略: test_no_odds_ninki_proxy と test_false_pass_detection_power は stub に依存しない AST 検査なので即時 GREEN。test_alpha_self_contained_outcome_swap / test_alpha_cross_race_leak_detected は stub 関数を呼ぶので RED（NotImplementedError）。Wave 1 で GREEN。
  </action>
  <verify>
    <automated>KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/audit/test_audit_race_relative.py --co -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - tests/audit/test_audit_race_relative.py が存在し `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/audit/test_audit_race_relative.py --co -q` が4テストを認識して collection 成功（exit 0）
    - test_no_odds_ninki_proxy / test_alpha_self_contained_outcome_swap / test_alpha_cross_race_leak_detected / test_false_pass_detection_power の4テストが定義される
    - `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/audit/test_audit_race_relative.py::test_no_odds_ninki_proxy tests/audit/test_audit_race_relative.py::test_false_pass_detection_power -x` が GREEN（stub 非依存の AST 検査）
    - `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/audit/test_audit_race_relative.py::test_alpha_self_contained_outcome_swap -x` が NotImplementedError で RED（D-10・Wave 1 で GREEN）
  </acceptance_criteria>
  <done>
    test_audit_race_relative.py が4テスト（SC#4 AST・D-10 自己完結2件・false-pass 回避）を持ち・KEIBA_SKIP_DB_TESTS=1 で collection 成功・AST 2テストは即時 GREEN・D-10 2テストは stub で RED。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stub モジュール境界 | race_relative.py が市場情報 proxy を持たないことの静的保証（SC#4・SAFE-01）|

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Information Disclosure（市場回帰） | race_relative.py stub | high | mitigate | stub が odds/ninki/fukuodds/ninkij/tansyouodds の Name/Attribute に加え string-constant（ast.Constant）も持たないことを Task 1 acceptance_criteria の AST 検査で保証（SC#4・codex review MEDIUM・RESEARCH Security Domain L712・test_audit_field_strength.py:91 の Constant 走査と一致） |
| T-11-02 | Information Disclosure（test 窓 outcome 混入） | test stub（D-10） | medium | mitigate | test_alpha_self_contained_outcome_swap が RED で存在し・Wave 1 実装後に outcome 入れ替え不変を検証する契約を固定（D-10・RESEARCH Security Domain L708） |
| T-11-SC | Tampering | npm/pip/cargo installs | high | mitigate | 本 plan は新規依存インストールなし（scipy/sklearn 既存 pin）・slopcheck 不要 |
</threat_model>

<verification>
- `uv run python -c "from src.model import race_relative; print(race_relative.THETA_CANDIDATES)"` が (0.5, 0.75, 1.0, 1.25, 1.5) を出力
- `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/model/test_race_relative.py tests/audit/test_audit_race_relative.py --co -q` が16テスト（12+4）を認識して collection 成功
- `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/audit/test_audit_race_relative.py::test_no_odds_ninki_proxy -x` が GREEN（SC#4 即時保証）
</verification>

<success_criteria>
- race_relative.py が事前登録定数（θ 候補・ε・xtol）と 3 公開関数 stub を持つ
- test_race_relative.py（12テスト）と test_audit_race_relative.py（4テスト）が collection 成功
- AST odds/ninki proxy 0件が即時 GREEN で保証される
- 残りテストは NotImplementedError で RED（TDD RED phase・Wave 1 で GREEN）
</success_criteria>

<output>
Create `.planning/phases/11-race-relative-probability-model/11-01-SUMMARY.md` when done
</output>
