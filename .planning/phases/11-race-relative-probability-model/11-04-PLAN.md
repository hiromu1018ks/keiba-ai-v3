---
phase: 11-race-relative-probability-model
plan: 04
type: execute
wave: 3
depends_on:
  - 11-03
files_modified:
  - tests/audit/test_audit_race_relative.py
  - scripts/run_phase11_evaluation.py
autonomous: true
requirements:
  - MODEL-01
  - SAFE-01
tags: [audit, adversarial, evaluation, safe-01]

must_haves:
  truths:
    - "tests/audit/test_audit_race_relative.py の4テストが全て GREEN（D-10 α_r 自己完結性 outcome swap + cross-race leak 検出・SC#4 AST odds/ninki proxy 0件・false-pass 回避）"
    - "scripts/run_phase11_evaluation.py が v1.0 binary baseline vs race-relative model の 3-way 比較を実行し・D-04 非劣化 gate + D-05 改善 gate 3条件 を判定する"
    - "θ 選択経路が D-03 制約付き選択（足切り → overprediction penalty 最小 → tie-break）に従い・orchestrator.train_and_predict(score_split='calib') のみで候補評価し・test 窓（score_split='test'）への θ の漏れを構造的にブロックする（codex review HIGH#1・§11.2 聖域・D-03）"
    - "θ 選択経路が reports/11-evaluation/theta-selection.json に・test 窓評価に先立ち byte-reproducible に書き出される（候補毎の Brier/LogLoss/AUC/overprediction_penalty/calib_max_dev/selected_only_calib_max_dev/verdict と選択経路・後知恵すり替え禁止・codex HIGH#1）"
    - "run_phase11_evaluation.py が is_primary を立てない（D-07・model_version-scoped swap のみ・v1.0 binary 行は保持）"
    - "run_phase11_evaluation.py が §15.2 binning 定数を evaluator/segment_eval から import 再利用し（再定義禁止・bit-identical）・D-04 許容幅を事前登録固定値（Brier≤0.005/LogLoss≤0.010/AUC≤0.005）で持つ"
  artifacts:
    - "tests/audit/test_audit_race_relative.py（11-01 stub → 11-04 実装）"
    - "scripts/run_phase11_evaluation.py（新規）"
  key_links:
    - "test_audit_race_relative.py ← src.model.race_relative（11-02 実装済み）"
    - "run_phase11_evaluation.py → orchestrator.train_and_predict(theta=...)（11-03 実装済み）"
    - "run_phase11_evaluation.py → race_relative.compute_overprediction_penalty（11-02 実装済み）"
    - "run_phase11_evaluation.py → prediction_load._idempotent_load_prediction（is_primary 立てない・D-07）"
---

<objective>
Phase 11 Wave 3: 11-01 で stub 化した tests/audit/test_audit_race_relative.py の D-10 adversarial テスト（α_r 自己完結性・cross-race leak 検出）を 11-02 の実装に対して実装し GREEN にする。また scripts/run_phase11_evaluation.py を新規作成し・v1.0 binary vs race-relative model の 3-way 比較・D-04 非劣化 gate + D-05 改善 gate 3条件・θ 選択経路（D-03 制約付き選択）を事前登録固定値で実装する。

Purpose: 補正層の自己完結性（D-10）と proxy 排除（SC#4）を adversarial で逆証明し・SC#2 gate（非劣化 + 改善）を test 窓で一回だけ評価する比較レポートを生成する。θ は calib slice のみで選び test 窓で選び直さない（§11.2 聖域）。
Output: test_audit_race_relative.py 4テスト GREEN・run_phase11_evaluation.py（live-DB 実行は 11-05）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-race-relative-probability-model/11-CONTEXT.md
@.planning/phases/11-race-relative-probability-model/11-RESEARCH.md
@.planning/phases/11-race-relative-probability-model/11-PATTERNS.md
@.planning/phases/11-race-relative-probability-model/11-VALIDATION.md
@.planning/phases/11-race-relative-probability-model/11-01-SUMMARY.md
@.planning/phases/11-race-relative-probability-model/11-02-SUMMARY.md
@.planning/phases/11-race-relative-probability-model/11-03-SUMMARY.md

# analog（必読）
@tests/audit/test_audit_field_strength.py
@scripts/run_phase10_evaluation.py
@src/model/race_relative.py
@src/model/orchestrator.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: test_audit_race_relative.py の D-10 adversarial テスト実装（α_r 自己完結性・cross-race leak）</name>
  <files>tests/audit/test_audit_race_relative.py</files>
  <read_first>
    - .planning/phases/11-race-relative-probability-model/11-VALIDATION.md（adversarial テスト L55-58・D-10 outcome swap + cross-race leak）
    - .planning/phases/11-race-relative-probability-model/11-PATTERNS.md（test_audit_race_relative.py lines 274-334・5段階鋳型・forbidden tokens・D-10 adversarial idiom・monkeypatch）
    - .planning/phases/11-race-relative-probability-model/11-RESEARCH.md（Security Domain L688-715・D-10・Pitfall 5 cross-race leak）
    - tests/audit/test_audit_field_strength.py（L413-502 test_lookahead_injection_detected の monkeypatch idiom・L597-663 test_source_asof_value_invariance の値の不変性 idiom）
    - src/model/race_relative.py（11-02 実装済み・solve_alpha_for_race / apply_race_relative_correction の契約）
    - tests/audit/test_audit_race_relative.py（11-01 stub・ヘッダ docstring・forbidden tokens・scanner・test_no_odds_ninki_proxy / test_false_pass_detection_power は既に GREEN）
  </read_first>
  <behavior>
    - test_alpha_self_contained_outcome_swap GREEN: solve_alpha_for_race を outcome に関わらず同じ (s, theta, k) で2回呼出し α_r が bit-identical であることを検証（D-10・outcome は引数に取らないので構造的に不変・決定論的）
    - test_alpha_cross_race_leak_detected GREEN: apply_race_relative_correction で race R1 と R2 が独立処理され・R1 の logit を変えても R2 の p_final が不変であることを検証（D-10・cross-race leak 検出）
    - test_no_odds_ninki_proxy / test_false_pass_detection_power は 11-01 から引き続き GREEN（11-02 実装で odds/ninki proxy が混入していない）
  </behavior>
  <action>
    tests/audit/test_audit_race_relative.py の 11-01 stub のうち・test_alpha_self_contained_outcome_swap と test_alpha_cross_race_leak_detected の NotImplementedError 呼出を 11-02 実装に対する実検証に置換する。ヘッダ docstring・forbidden tokens・scanner・test_no_odds_ninki_proxy・test_false_pass_detection_power は 11-01 のまま維持。

    【test_alpha_self_contained_outcome_swap】PATTERNS L312-324 + test_audit_field_strength.py の monkeypatch idiom に従い:
    - rng = np.random.default_rng(42)・s = rng.normal(0, 1, size=3)（3 頭・k=2）
    - alpha_1 = solve_alpha_for_race(s, theta=1.0, k=2)
    - alpha_2 = solve_alpha_for_race(s, theta=1.0, k=2)（同一引数で再呼出・決定論的）
    - assert alpha_1 == alpha_2（bit-identical・D-10 自己完結性の構造的保証）
    - 更に・adversarial 強化: outcome ラベル [1,0,0] と [0,1,0] を想定し・solve_alpha_for_race が y_true を引数に取らない（シグネチャが (s_logits, theta, k) のみ）ことを AST または sig.inspect で検証し・outcome が α_r 計算に混入する経路が閉じていることを逆証明。inspect.signature(solve_alpha_for_race).parameters に y_true/outcome/label が含まれないことを assert。

    【test_alpha_cross_race_leak_detected】PATTERNS L326-334 + test_audit_field_strength.py::test_source_asof_value_invariance の値の不変性 idiom に従い:
    - race R1（3 頭）と R2（3 頭）の合成データを構築・race_ids = np.array(["R1","R1","R1","R2","R2","R2"])・k_per_race = np.array([2,2,2,2,2,2])
    - p_cal_base = np.array([0.3, 0.5, 0.7, 0.2, 0.4, 0.6])（R1 と R2 で独立）
    - p_final_base = apply_race_relative_correction(p_cal_base, theta=1.0, k_per_race=k_per_race, race_ids=race_ids)
    - p_cal_leak = p_cal_base.copy(); p_cal_leak[0] = 0.95（R1 の先頭の logit を大きく変更・cross-race leak があれば R2 の p_final に影響するはず）
    - p_final_leak = apply_race_relative_correction(p_cal_leak, theta=1.0, k_per_race=k_per_race, race_ids=race_ids)
    - R2 の p_final（index 3-5）が base と leak で bit-identical であることを assert: `assert np.array_equal(p_final_base[3:], p_final_leak[3:])`（R1 の logit 変更が R2 に漏れない・D-10 cross-race leak 検出）
    - R1 の p_final（index 0-2）は base と leak で異なることを assert（検出力証明・silent 変更でない）: `assert not np.array_equal(p_final_base[:3], p_final_leak[:3])`
  </action>
  <verify>
    <automated>KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/audit/test_audit_race_relative.py -x</automated>
  </verify>
  <acceptance_criteria>
    - test_alpha_self_contained_outcome_swap GREEN（outcome に関わらず α_r が bit-identical・D-10・solve_alpha_for_race が y_true を引数に取らないことを inspect で検証）
    - test_alpha_cross_race_leak_detected GREEN（R1 logit 変更で R2 p_final 不変・D-10 cross-race leak 検出）
    - test_no_odds_ninki_proxy GREEN（11-01 から継続・11-02 実装で odds/ninki proxy 混入なし）
    - test_false_pass_detection_power GREEN（11-01 から継続・意図的注入検出）
    - tests/audit/test_audit_race_relative.py の4テストが全て GREEN（`KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/audit/test_audit_race_relative.py -x` exit 0）
  </acceptance_criteria>
  <done>
    test_audit_race_relative.py の4テスト（SC#4 AST・D-10 自己完結2件・false-pass 回避）が全て GREEN。D-10 adversarial で α_r の自己完結性と cross-race leak 検出を逆証明。
  </done>
</task>

<task type="auto">
  <name>Task 2: scripts/run_phase11_evaluation.py 新規（v1.0 vs race-relative 比較・SC#2 gate・θ 選択経路・D-07 is_primary 立てない）</name>
  <files>scripts/run_phase11_evaluation.py</files>
  <read_first>
    - .planning/phases/11-race-relative-probability-model/11-PATTERNS.md（run_phase11_evaluation.py セクション lines 338-429・ヘッダ docstring・sys.path idiom・binning import・事前登録許容幅・θ 選択経路・is_primary 立てない）
    - .planning/phases/11-race-relative-probability-model/11-CONTEXT.md（D-03 θ 選択・D-04 非劣化マージン・D-05 改善 gate 3条件・D-07 is_primary 立てない・D-08 k 決定）
    - .planning/phases/11-race-relative-probability-model/11-RESEARCH.md（System Architecture Diagram L156-213・step 12 θ 選択・overprediction penalty・Open Question #2 θ 選択経路記録形式）
    - scripts/run_phase10_evaluation.py（全 idiom・事前登録許容幅定数化・H6 正しい API chain・H2/H7/H8 生 trainer 直接呼出回避・_attach_label_to_pred・_evaluate_gate・_write_reports・_sanitize_for_json・statement_timeout・L1-839）
    - src/model/race_relative.py（11-02・compute_overprediction_penalty / THETA_CANDIDATES / ALPHA_SEARCH_XTOL / P_CAL_CLIP_EPSILON）
    - src/model/orchestrator.py（11-03・train_and_predict の theta 引数）
    - src/db/prediction_load.py（_idempotent_load_prediction・model_version scoped swap・set_primary_model は呼ばない）
  </read_first>
  <action>
    scripts/run_phase10_evaluation.py の idiom を直接踏襲し・新規 scripts/run_phase11_evaluation.py を作成する。PATTERNS L338-429 のテンプレートに従う。

    【ヘッダ docstring】PATTERNS L344-374 のテンプレートを転記・以下を明記:
    - Purpose: Phase 11 race-relative 補正層が v1.0 binary の確率品質を悪化させず投票層の overprediction を改善することを SC#2 gate で保証
    - D-04 事前登録許容幅（§11.2 聖域・評価後変更禁止）: Brier ≤ 0.005 / LogLoss ≤ 0.010 / AUC ≤ 0.005
    - D-05 SC#2 改善 gate 3必須条件: (1) odds_band×p_bin overprediction penalty < v1.0 (2) selected/high-EV 層の平均予測率−実現率 < v1.0 (3) selected-only calib_max_dev が D-04 マージン内
    - θ 選択経路（D-03 制約付き選択・§11.2 聖域・test 窓選び直し禁止）: 候補 {0.5,0.75,1.0,1.25,1.5} → calib slice で足切り(D-04) → overprediction penalty 最小(D-05-1) → tie-break calib_max_dev → θ=1 に近い候補
    - D-07: is_primary 立てない（並列比較のみ・Phase 12 で切替判断）
    - B-3: baseline と race-relative を同一 trainer 設定で delta
    - §15.2 binning import 再利用（再定義禁止）
    - Usage（live-DB・KEIBA_SKIP_DB_TESTS unset）

    【sys.path・import】run_phase10_evaluation.py L80-97 と同一 idiom:
    - `_REPO_ROOT = Path(__file__).resolve().parent.parent; if str(_REPO_ROOT) not in sys.path: sys.path.insert(0, str(_REPO_ROOT))`
    - `from src.model.evaluator import CALIBRATION_CURVE_BINS, CALIBRATION_CURVE_MIN_BIN_COUNT, compute_metrics`（binning 再利用）
    - `from src.model.segment_eval import NINKI_BAND_EDGES, ODDS_BAND_EDGES, evaluate_all_segments`（binning 再利用）
    - `from src.model.predict import make_model_version`
    - `from src.model.race_relative import apply_race_relative_correction, compute_overprediction_penalty, THETA_CANDIDATES, ALPHA_SEARCH_XTOL, P_CAL_CLIP_EPSILON`（11-02 実装）

    【事前登録許容幅の定数化】run_phase10_evaluation.py L111-113 と同一 idiom・D-04 数値（Phase 10 の 0.002/0.005/0.005 から拡張・race-conditional 構造的変化への根拠再確認）:
    ```
    TOLERANCE_BRIER: float = 0.005
    TOLERANCE_LOGLOSS: float = 0.010
    TOLERANCE_AUC: float = 0.005
    ```
    docstring で「評価後変更禁止（§11.2 聖域）」を明記。

    【BT1_PERIODS】run_phase10_evaluation.py L185-189 と同一（train 2019-06-01..2022-06-30 / calib 2022-07-01..2022-12-31 / test 2023-01-01..2023-12-31）。

    【parse_args】run_phase10_evaluation.py L192-230 に準拠・以下を変更:
    - --baseline-snapshot-id（default "20260626-1a-opponentstrength-v1"・Phase 10 完成 snapshot・v1.1 入力）
    - --bt-split（default BT-1）
    - --odds-snapshot-policy（default 30min_before）
    - --theta-candidates（nargs="+"・default THETA_CANDIDATES = [0.5,0.75,1.0,1.25,1.5]・事前登録・§11.2 聖域）
    - --out-dir（default reports）

    【main()・live-DB orchestration】run_phase10_evaluation.py L334-497 の idiom を踏襲:
    - readonly_pool + statement_timeout configure callback（MEMORY.md subagent-db-query-statement-timeout・30s）
    - load_feature_matrix(snapshot_id=args.baseline_snapshot_id) → load_labels(cur) → build_training_frame → load_frozen_maps（REVIEW H6 正しい API chain）
    - baseline 呼出: train_and_predict(frame, model_type="lightgbm", feature_snapshot_id=..., snapshot_id=..., version_n=1, split_periods=BT1_PERIODS, category_map=cat_map, theta=None, score_split="test")（v1.0 binary・theta=None・test 窓評価）
    - 【θ 選択経路（score_split="calib" のみ・codex review HIGH#1 対応）】race_relative の候補毎に・train_and_predict(theta=θ_i, score_split="calib") を呼出し・calib slice の予測に対して compute_overprediction_penalty と compute_metrics で評価・D-04 足切り → D-05-1 overprediction penalty 最小 → tie-break で selected_theta を決定。**score_split="calib" が X_calib のみを予測対象にするので構造的に test 窓に触れない（§11.2 聖域の機械保証・docstring 紳士協定でない）**。test 窓の y_true は θ 選択に一切使われない。
    - 【theta-selection.json の事前書き出し（codex HIGH#1）】θ 選択経路が決定した直後・test 窓評価（score_split="test"）に先立って・reports/11-evaluation/theta-selection.{md,json} に候補毎の (Brier, LogLoss, AUC, overprediction_penalty, calib_max_dev, selected_only_calib_max_dev, verdict) と選択経路（足切り/選択/tie-break の各段階と残候補）を byte-reproducible に atomic write（_sanitize_for_json・allow_nan=False・sort_keys=True）。これにより後知恵すり替え禁止がアーティファクトで保証される。
    - race-relative test 評価呼出: train_and_predict(frame, model_type="lightgbm_rr", feature_snapshot_id=..., snapshot_id=..., version_n=1, split_periods=BT1_PERIODS, category_map=cat_map, theta=selected_theta, score_split="test")（11-03 拡張・model_type="lightgbm_rr" で race-relative short・theta=selected_theta・test 窓で一回だけ評価・θ の再選択なし）
    - _attach_label_to_pred（run_phase10_evaluation.py L500-560 と同一 idiom）で label JOIN

    【_evaluate_gate】run_phase10_evaluation.py L563-698 の idiom を拡張:
    - baseline_metrics / rr_metrics を compute_metrics で算出
    - delta（rr - baseline）を計算
    - D-04 非劣化 gate: brier_delta <= 0.005 / logloss_delta <= 0.010 / auc_delta >= -0.005（3条件の論理積）
    - D-05 改善 gate 3条件: (1) compute_overprediction_penalty(rr) < compute_overprediction_penalty(baseline) (2) selected/high-EV 層の (mean_pred - frac_pos) が rr < baseline (3) selected-only calib_max_dev が D-04 マージン内
    - gate_pass = D-04 AND D-05（両方満たす必要）
    - D-15 参考記録: segment_eval.evaluate_all_segments（binning import 再利用）
    - result dict に theta_selection / gate_pass / baseline_metrics / rr_metrics / delta / tolerance / d05_improvement_gate / d15_reference_segments / binning_source / safe_01_note を格納

    【is_primary 立てない（D-07）】prediction_load._idempotent_load_prediction で race-relative model の行を model_version-scoped swap で追加するが・set_primary_model は呼ばない（v1.0 binary 行は保持・is_primary=true は v1.0 のまま）。docstring と code comment で明記。run_phase10_evaluation.py は prediction_load を呼ばない（比較レポートのみ）ので・本 script も comparison のみで prediction_load 呼出は 11-05 で実施。本 task は run_phase11_evaluation.py のスクリプト作成（live-DB 実行は 11-05）。

    【_write_reports】run_phase10_evaluation.py L729-831 と同一 idiom・reports/11-evaluation/11-evaluation.{md,json} を atomic write（_sanitize_for_json 適用・allow_nan=False・sort_keys=True）。

    【θ 選択経路記録】RESEARCH Open Question #2・PATTERNS L418-423 に従い・reports/11-evaluation/theta-selection.json に候補毎の評価値と選択経路（足切り/選択/tie-break の各段階）を byte-reproducible に出力（後知恵すり替え禁止・選択ルールは事前登録値で固定）。
  </action>
  <verify>
    <automated>uv run python -c "import ast; ast.parse(open('scripts/run_phase11_evaluation.py').read()); print('syntax OK')"; KEIBA_SKIP_DB_TESTS=1 uv run python scripts/run_phase11_evaluation.py --help 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - scripts/run_phase11_evaluation.py が存在し Python 構文エラーなし（ast.parse 成功）
    - `uv run python scripts/run_phase11_evaluation.py --help` が --baseline-snapshot-id / --bt-split / --odds-snapshot-policy / --theta-candidates / --out-dir を表示
    - TOLERANCE_BRIER == 0.005 / TOLERANCE_LOGLOSS == 0.010 / TOLERANCE_AUC == 0.005 が定数で定義される（D-04 事前登録・§11.2 聖域）
    - THETA_CANDIDATES (0.5, 0.75, 1.0, 1.25, 1.5) がデフォルト theta 候補（D-03 事前登録）
    - binning 定数（CALIBRATION_CURVE_BINS / ODDS_BAND_EDGES / NINKI_BAND_EDGES）が evaluator/segment_eval から import 再利用され・再定義されない（grep で `CALIBRATION_CURVE_BINS\s*=\s*10` 等の再定義が0件）。overprediction penalty 測定も race_relative.compute_overprediction_penalty を経由して evaluator._compute_calibration_curve_bins を利用（codex HIGH#2・二重 binning 禁止）
    - θ 選択経路が score_split="calib" のみで候補評価し・test 窓（score_split="test"）を θ 選択に使わない（codex HIGH#1・§11.2 聖域の機械保証・docstring で明記）
    - reports/11-evaluation/theta-selection.{md,json} が test 窓評価に先立って書き出されるロジック（codex HIGH#1・後知恵すり替え禁止）
    - is_primary を立てない（set_primary_model を呼ばない）ことがコードと docstring で明記（D-07）
    - orchestrator.train_and_predict を theta=None（baseline）と theta=selected_theta（race-relative）で呼び分ける（生 trainer API 直接呼出でない・REVIEW H2/H7/H8）
    - D-05 改善 gate 3条件（overprediction penalty / selected-high-EV 層 / selected-only calib_max_dev）が実装される
    - live-DB 実行は 11-05 で実施（本 task はスクリプト作成・--help と構文チェックのみ）
  </acceptance_criteria>
  <done>
    run_phase11_evaluation.py が作成され・D-04 非劣化 gate + D-05 改善 gate 3条件 + θ 選択経路（D-03 calib slice のみ）+ is_primary 立てない（D-07）を実装。§15.2 binning import 再利用。live-DB 実行は 11-05。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adversarial test 境界 | D-10 α_r 自己完結性が outcome/cross-race 情報混入を構造的にブロックすることの逆証明 |
| θ 選択 → test 窓評価 | θ が calib slice で選ばれ test 窓で選び直されないこと（§11.2 聖域）・run_phase11_evaluation.py の実行プロセス契約 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-13 | Information Disclosure（α_r 計算への test 窓 outcome 混入） | test_alpha_self_contained_outcome_swap | critical | mitigate | solve_alpha_for_race が y_true を引数に取らないことを inspect.signature で検証・outcome 入れ替えで α_r が不変であることを adversarial で逆証明（D-10・RESEARCH Security Domain L708） |
| T-11-14 | Information Disclosure（α_r 計算への他 race 情報混入） | test_alpha_cross_race_leak_detected | high | mitigate | apply_race_relative_correction が race 毎独立ループであることを・R1 logit 変更で R2 p_final 不変で逆証明（D-10・Pitfall 5・Security Domain L709） |
| T-11-15 | Elevation of Privilege（過学習・θ 選び直し・test 窓への θ 漏れ） | run_phase11_evaluation.py θ 選択経路 | critical | mitigate | θ は score_split="calib" でのみ候補評価し・test 窓（score_split="test"）では事前登録 gate で一回だけ評価（codex HIGH#1・§11.2 聖域の機械保証・D-03）・reports/11-evaluation/theta-selection.json に候補毎の評価値と選択経路を test 窓評価に先立ち byte-reproducible に記録（後知恵すり替え禁止・RESEARCH Security Domain L710） |
| T-11-16 | Information Disclosure（市場回帰・run script が odds を feature に混入） | run_phase11_evaluation.py | high | mitigate | market データ（odds）は diagnostic 層（compute_overprediction_penalty / segment_eval）でのみ使用し FEATURE_COLUMNS には混入しない・orchestrator.train_and_predict が FEATURE_COLUMNS allowlist で保証（SAFE-01・run_phase10_evaluation.py safe_01_note idiom 踏襲） |
| T-11-17 | Tampering（is_primary を誤って立て Phase 12 前に EV 本番切替） | run_phase11_evaluation.py prediction_load 呼出 | high | mitigate | set_primary_model を呼ばない・model_version-scoped swap のみ・v1.0 binary 行は保持（D-07・RESEARCH Security Domain L715・CONTEXT D-07） |
</threat_model>

<verification>
- `KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/audit/test_audit_race_relative.py -x` が4テスト全て GREEN
- `uv run python scripts/run_phase11_evaluation.py --help` が CLI 引数を表示
- `grep -c "CALIBRATION_CURVE_BINS\s*=\s*10\|ODDS_BAND_EDGES\s*=\s*np.array" scripts/run_phase11_evaluation.py` が 0（binning 再定義なし・import 再利用）
- `grep -c "set_primary_model" scripts/run_phase11_evaluation.py` が 0（D-07 is_primary 立てない）
</verification>

<success_criteria>
- test_audit_race_relative.py 4テスト全て GREEN（D-10 + SC#4 + false-pass 回避）
- run_phase11_evaluation.py が D-04 + D-05 gate・θ 選択経路（calib slice のみ）・is_primary 立てない を実装
- §15.2 binning import 再利用（bit-identical）
- 事前登録値（θ 候補・許容幅）が固定（§11.2 聖域）
</success_criteria>

<output>
Create `.planning/phases/11-race-relative-probability-model/11-04-SUMMARY.md` when done
</output>
