---
phase: 10-opponent-strength-race-relative-features
plan: 07
type: execute
wave: 5
depends_on: [05]
files_modified:
  - src/audit/report.py
  - tests/audit/test_audit_field_strength.py
  - tests/audit/test_audit_speed_figure.py
autonomous: true
requirements: [SAFE-01]
granularity: fine

must_haves:
  truths:
    # SC#4（SAFE-01 proxy 排除証明・adversarial audit）
    - "src/features/field_strength.py と src/features/race_relative.py のソース AST に odds/ninki/fukuodds/ninkij/tansyouodds の Name/Attribute ノードが0件である（SAFE-01・SC#4）"
    - "src/features/field_strength.py と src/features/race_relative.py の SQL 文字列リテラルに odds/ninki/fukuodds/ninkij/tansyouodds の word-boundary 部分一致が0件である（REVIEW H5・SQL proxy 排除）"
    - "lookahead 注入テスト（adversarial）: _pit_cutoff_prefilter を monkeypatch で <= 版に差し替えると T+1 opponent データが検出され test が RED になる（guard が有効なことを逆証明）"
    # false-pass 回避
    - "guard が意図的注入（odds/ninki proxy の Name/Attribute/SQL 文字列）を検出できる（5段階鋳型(5)・false-pass 回避・test_audit_speed_figure.py L309-384 と対称）"
    - "FEATURE_COLUMNS allowlist（src/model/data.py 経由・106 feature）に forbidden prefix が0件である"
    # SAFE-01 検査対象拡張
    - "test_audit_speed_figure.py の expected_features hardcode list が Phase 10 の 27 新 feature で拡張される（SAFE-01 検査対象拡張）"
    # 性能検証
    - "compute_field_strength_profile の vectorized 実装が cProfile で秒単位（6.7M ペア・純粋ループ禁止・Pitfall 2 回避）"
  artifacts:
    - "tests/audit/test_audit_field_strength.py が存在し全テスト GREEN（AST odds/ninki proxy 排除・lookahead 注入・false-pass 検出）"
    - "tests/audit/test_audit_speed_figure.py の expected_features hardcode list が Phase 10 の 27 feature で拡張される"
    - "src/audit/report.py の SURFACE_ROWS に Phase 10 feature 行が追加される（任意・SC#4 supplement）"
  key_links:
    - "test_audit_field_strength.py は tests/audit/test_audit_speed_figure.py の5段階鋳型（_FORBIDDEN_TOKENS + _scan_module_for_forbidden_tokens）を直接踏襲する"
    - "_FORBIDDEN_TOKENS = frozenset({'odds', 'ninki', 'fukuodds', 'ninkij', 'tansyouodds'}) を共通定数として再定義または import"
    - "AST 検査は docstring/comment の 'odds-free' 等の文字列を false positive に出さない（Name/Attribute ノード厳密判定・REVIEW H5）"
---

<objective>
Phase 10 PLAN 07: SC#4 SAFE-01 proxy 排除証明を adversarial audit で完了する。新規モジュール field_strength.py / race_relative.py の AST に odds/ninki proxy が混入しないことを静的証明し・lookahead 注入テスト（guard 無効化で違反検出）で PIT 保証を逆証明する。更に compute_field_strength_profile の vectorized 実装の性能を cProfile で検証する（Pitfall 2 回避）。

Purpose: core value「リーク防止最優先」の最終保証層。新 feature が市場回帰（odds/ninki proxy）と未来情報リーク（lookahead）のいずれも引き起こさないことを機械的に証明する。Phase 9/9.1 の adversarial audit idiom を直接拡張する。

Output: tests/audit/test_audit_field_strength.py（5段階鋳型・AST odds/ninki proxy 排除・lookahead 注入・false-pass 検出）・tests/audit/test_audit_speed_figure.py 拡張（hardcode list 更新）・src/audit/report.py 拡張（任意）・cProfile 性能検証レポート
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-opponent-strength-race-relative-features/10-CONTEXT.md
@.planning/phases/10-opponent-strength-race-relative-features/10-RESEARCH.md
@.planning/phases/10-opponent-strength-race-relative-features/10-PATTERNS.md

# PLAN 01/03 出力（依存・検査対象モジュール）
@.planning/phases/10-opponent-strength-race-relative-features/10-01-PLAN.md
@.planning/phases/10-opponent-strength-race-relative-features/10-03-PLAN.md

# analog（必読・5段階鋳型）
@tests/audit/test_audit_speed_figure.py
@src/audit/report.py
@src/features/field_strength.py
@src/features/race_relative.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tests/audit/test_audit_field_strength.py を作成し SAFE-01 adversarial audit（AST + lookahead 注入）を実装</name>
  <files>tests/audit/test_audit_field_strength.py, tests/audit/test_audit_speed_figure.py</files>

  <read_first>
    - tests/audit/test_audit_speed_figure.py L1-48（imports・_FORBIDDEN_TOKENS frozenset 定義・共通定数）
    - tests/audit/test_audit_speed_figure.py L63-108（_scan_module_for_forbidden_tokens: AST Name/Attribute ノード検査・SQL 文字列リテラル検査・5段階鋳型(1)(2)(3)）
    - tests/audit/test_audit_speed_figure.py L123-172（test_no_odds_ninki_proxy_in_*_source: AST audit・対称に field_strength/race_relative を検査）
    - tests/audit/test_audit_speed_figure.py L178-256（test_feature_columns_contains_*_features_no_proxy: FEATURE_COLUMNS allowlist 検査・expected_features hardcode list）
    - tests/audit/test_audit_speed_figure.py L309-384（test_false_pass_detection_power: 5段階鋳型(5)・意図的注入を検出することを証明）
    - src/features/field_strength.py（PLAN 01 出力・検査対象）
    - src/features/race_relative.py（PLAN 03 出力・検査対象）
    - src/features/speed_figure.py L98-118（_pit_cutoff_prefilter・lookahead 注入テストの monkeypatch 対象）
    - tests/features/test_speed_figure_pit.py（PIT adversarial テストの analog・_pit_cutoff_prefilter monkeypatch idiom）
    - .planning/phases/10-opponent-strength-race-relative-features/10-RESEARCH.md L673-686（FEAT-02/03 SAFE-01 test map）・L435-441（Pitfall 3 D-01 PIT 違反・adversarial lookahead 注入）
    - .planning/phases/10-opponent-strength-race-relative-features/10-PATTERNS.md L577-621（test_audit_field_strength.py 5段階鋳型 idiom）
  </read_first>

  <behavior>
    - Test 1 (AST Name/Attribute・SC#4): src.features.field_strength モジュールの AST に odds/ninki/fukuodds/ninkij/tansyouodds の Name ノード(id)・Attribute ノード(attr) が0件
    - Test 2 (AST Name/Attribute・SC#4): src.features.race_relative モジュールも同様に0件
    - Test 3 (SQL 文字列リテラル・REVIEW H5): 両モジュールの SQL 文字列リテラル内に odds/ninki/fukuodds/ninkij/tansyouodds の word-boundary 部分一致が0件
    - Test 4 (false-pass 回避・5段階鋳型(5)): 意図的に odds/ninki proxy の Name/Attribute/SQL 文字列を注入した偽モジュールで guard が検出することを証明
    - Test 5 (lookahead 注入・D-01 PIT 保証・adversarial): _pit_cutoff_prefilter を monkeypatch で <= 版に差し替えると T+1 opponent データが profile に混入し値が変化する・guard が有効なことを逆証明
    - Test 6 (FEATURE_COLUMNS allowlist): src/model/data.py の FEATURE_COLUMNS（106 feature）に forbidden prefix が0件
  </behavior>

  <action>
    tests/audit/test_audit_field_strength.py を新規作成する（tests/audit/test_audit_speed_figure.py の5段階鋳型を直接踏襲・PAT§ L577-621 idiom）:

    (1) imports: `from src.features import field_strength, race_relative`・`import ast`・必要に応じて `from src.features.availability import ...`。`_FORBIDDEN_TOKENS` は test_audit_speed_figure.py から import するか共通化して再定義（frozenset({'odds', 'ninki', 'fukuodds', 'ninkij', 'tansyouodds'})）。

    (2) _scan_module_for_forbidden_tokens(module) helper: test_audit_speed_figure.py L63-108 の idiom を再利用（または共通ユーティリティ化して import）。AST Name ノード(id)・Attribute ノード(attr)・SQL 文字列リテラル（word-boundary 部分一致・REVIEW H5）の3層検査。

    (3) (a)(b)(c) AST audit テスト（test_audit_speed_figure.py L123-172 と対称）:
        - test_no_odds_ninki_proxy_in_field_strength_source: src.features.field_strength の AST が forbidden Name/Attribute 0件・SQL proxy 0件を assert。
        - test_no_odds_ninki_proxy_in_race_relative_source: src.features.race_relative も同様。

    (4) (d) FEATURE_COLUMNS allowlist テスト（test_audit_speed_figure.py L178-256 と対称）:
        - _PHASE10_SNAPSHOT_ID = "20260626-1a-opponentstrength-v1" を定義。
        - test_feature_columns_contains_phase10_features_no_proxy: _derive_feature_columns(snapshot_id=_PHASE10_SNAPSHOT_ID) が返す 106 feature が rolling_field_strength_mean_mean_5 / speed_index_rank_mean5 / gap_to_top / gap_to_3rd / field_strength_adjusted_rank 等 の新 feature を含み・かつ forbidden prefix が0件であることを assert。

    (5) (g) false-pass 回避テスト（test_audit_speed_figure.py L309-384 と対称・そのまま再利用可能）:
        - test_false_pass_detection_power: 意図的に forbidden token を注入した偽ソース（インメモリ文字列または一時ファイル）で guard が検出することを証明。5段階鋳型(5)。

    (6) (新規) lookahead 注入テスト・D-01 PIT 保証 adversarial:
        - test_lookahead_injection_detected: monkeypatch で src.features.field_strength._pit_cutoff_prefilter を `<=` 版（opponent.available_at <= self.available_at）に差し替え・source race と同日の opponent speed_figure を含む合成 history で compute_field_strength_profile を呼ぶ。guard 有効時（本番の strict `<`）は同日 opponent が除外され profile が変化する・guard 無効時（`<=` 版）は同日 opponent が混入し profile が異なる値になることを assert（guard が有効なことを逆証明）。

    (7) tests/audit/test_audit_speed_figure.py 拡張:
        - L219-238 等の expected_speed_features hardcode list を Phase 10 の 27 新 feature で拡張（09.1 で 17 feature 追加した idiom と対称）。これにより SAFE-01 検査対象が Phase 10 全 feature に拡張される。

    src/audit/report.py の SURFACE_ROWS 拡張は任意（SC#1 supplement 的位置づけ）・本 task では必須でない。追加する場合は AUDIT_SURFACE_COLUMNS 7項目スキーマに従う。

    docstring/comment に _FORBIDDEN_TOKENS の文字列リテラル（"odds" 等）をそのまま書かないこと（自前のテストが自身を fail させないよう・概念で書くか allowlist marker を使う）。
  </action>

  <verify>
    <automated>uv run pytest tests/audit/test_audit_field_strength.py tests/audit/test_audit_speed_figure.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - tests/audit/test_audit_field_strength.py が存在する
    - tests/audit/test_audit_field_strength.py に `from src.features import field_strength, race_relative` を含む
    - tests/audit/test_audit_field_strength.py に `test_no_odds_ninki_proxy_in_field_strength_source` `test_no_odds_ninki_proxy_in_race_relative_source` が存在する
    - tests/audit/test_audit_field_strength.py に `test_lookahead_injection_detected`（_pit_cutoff_prefilter monkeypatch による adversarial lookahead テスト）が存在する
    - tests/audit/test_audit_field_strength.py に `test_false_pass_detection_power`（5段階鋳型(5)・意図的注入検出）が存在する
    - tests/audit/test_audit_field_strength.py に `_PHASE10_SNAPSHOT_ID = "20260626-1a-opponentstrength-v1"` が定義される
    - tests/audit/test_audit_speed_figure.py の expected_features リストが Phase 10 の 27 新 feature を含む（rolling_field_strength_mean_mean_5・speed_index_rank_mean5・gap_to_top・gap_to_3rd・field_strength_adjusted_rank 等）
    - src/features/field_strength.py と src/features/race_relative.py の AST に odds/ninki/fukuodds/ninkij/tansyouodds の Name/Attribute/SQL 文字列 proxy が0件（テストが GREEN で証明）
    - `uv run pytest tests/audit/ -x -q` GREEN（新規 test_audit_field_strength + 既存 test_audit_speed_figure 回帰）
    - ruff lint GREEN: `uv run ruff check tests/audit/test_audit_field_strength.py tests/audit/test_audit_speed_figure.py`
  </acceptance_criteria>

  <done>
    test_audit_field_strength.py が5段階鋳型（AST Name/Attribute・SQL proxy・FEATURE_COLUMNS allowlist・false-pass 回避・lookahead 注入）で SAFE-01 と PIT 保証を証明する。新 feature モジュール（field_strength.py・race_relative.py）に odds/ninki proxy と lookahead が混入しないことが静的・動的に保証される。test_audit_speed_figure.py の検査対象が Phase 10 全 feature に拡張される。
  </done>
</task>

<task type="auto">
  <name>Task 2: compute_field_strength_profile の vectorized 実装の性能を cProfile で検証（Pitfall 2 回避）</name>
  <files>tests/features/test_field_strength.py</files>

  <read_first>
    - src/features/field_strength.py（PLAN 01 出力・性能検証対象）
    - .planning/phases/10-opponent-strength-race-relative-features/10-RESEARCH.md L412-434（Pitfall 2 計算量爆発・6.7M ペア・純粋ループ致命的低速・cProfile hot spot 警告）・L622-630（A3 計算量見積もり・MEDIUM confidence・PLAN で cProfile 必要）
    - .planning/phases/10-opponent-strength-race-relative-features/10-PATTERNS.md（field_strength.py idiom・per-source-race batch + vectorized groupby）
  </read_first>

  <behavior>
    - Test 1 (性能検証・Pitfall 2 回避): history ~48万行 × 平均14 opponent 規模の合成データ（または縮小版）で compute_field_strength_profile を呼び・実行時間が秒単位（純粋ループの数十分と対比）であることを確認
    - Test 2 (cProfile hot spot): cProfile で Python ループ（iterrows・for race in races 等）が hot spot に現れないことを確認・vectorized groupby/nlargest/sort_values が主流であることを確認
  </behavior>

  <action>
    tests/features/test_field_strength.py に性能検証テストを追加する（必須・Pitfall 2 回避の実証）:

    (1) test_compute_field_strength_profile_performance テストを追加:
        - 合成 history を規模実データに近い形で生成（例: 1000 race × 14 opponent = 14000 行・縮小版・本番は48万行だが unit test では縮小）。
        - time.perf_counter() で compute_field_strength_profile の実行時間を計測。
        - 実行時間が閾値（例: 縮小版で5秒以内・本番規模への外挿で秒〜十数秒単位）以下であることを assert。閾値は「純粋ループの致命的低速（数十分）」と対比して現実的な秒単位であることを示す値に設定。
        - ※厳密な閾値は実装後に cProfile 実測で調整・ただし PLAN 01 の vectorized 実装が意図通り機能すれば秒単位で収まるはず（A3・MEDIUM confidence の実証）。

    (2) test_no_python_loop_hot_spot テストを追加（cProfile・任意だが推奨）:
        - cProfile.run で compute_field_strength_profile を実行し・プロファイル統計を取得。
        - hot spot 上位が pandas/numpy の C 実装（groupby・nlargest・sort_values 等）であることを確認・純粋 Python ループ（iterrows・for loop・apply で Python関数）が上位に現れないことを assert。
        - 万が一 Python ループが hot spot にある場合は PLAN 01 の vectorized 実装を見直す必要がある（Pitfall 2 回避）。

    この検証は SC#5（機能ノイズ化でない）の前提・および本番運用可能性（snapshot 生成が現実的時間で完了する）の保証。
  </action>

  <verify>
    <automated>uv run pytest tests/features/test_field_strength.py -x -q -k "performance or hot_spot"</automated>
  </verify>

  <acceptance_criteria>
    - tests/features/test_field_strength.py に `test_compute_field_strength_profile_performance` が存在する
    - tests/features/test_field_strength.py に `test_no_python_loop_hot_spot`（cProfile・任意）が存在する
    - 性能テストが GREEN（実行時間が閾値以内・cProfile で Python ループ hot spot 無し）
    - PLAN 01 の vectorized 実装が6.7M ペア規模で秒単位で完了することが実証される
  </acceptance_criteria>

  <done>
    compute_field_strength_profile の vectorized 実装が Pitfall 2（計算量爆発）を回避し・6.7M ペア規模で秒単位で実行されることが cProfile で実証される。本番 snapshot 生成が現実的時間で完了する保証される。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 新 feature モジュール（field_strength.py・race_relative.py）→ adversarial audit | 静的 AST 検査と動的 lookahead 注入で SAFE-01/PIT 保証を証明する最終層 |
| FEATURE_COLUMNS allowlist → SAFE-01 | 106 feature 全てに forbidden prefix が0件であることを静的保証 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-28 | Information disclosure | odds/ninki proxy の feature 混入（SAFE-01 違反） | high | mitigate | AST Name/Attribute/SQL proxy 3層検査・lookahead 注入テスト・false-pass 回避テスト（5段階鋳型）で機械証明 |
| T-10-29 | Tampering | lookahead leak（未来情報の特徴量混入） | critical | mitigate | _pit_cutoff_prefilter monkeypatch で guard を `<=` に差替・T+1 opponent 混入を検出する adversarial test（guard 有効の逆証明） |
| T-10-30 | Tampering | false-pass（guard が実は何も検出していない状態） | high | mitigate | 5段階鋳型(5) test_false_pass_detection_power・意図的注入を検出することを証明 |
| T-10-31 | Denial of service | compute_field_strength_profile の計算量爆発 | medium | mitigate | cProfile 性能検証・Python ループ hot spot 無し・vectorized groupby/nlargest で秒単位（Pitfall 2 回避） |
</threat_model>

<verification>
- 単体テスト: `uv run pytest tests/audit/ tests/features/test_field_strength.py -x -q` GREEN
- lint: `uv run ruff check tests/audit/ src/features/field_strength.py src/features/race_relative.py` GREEN
- SC#4 SAFE-01 証明: 新 feature モジュール AST に odds/ninki proxy 0件・FEATURE_COLUMNS allowlist に forbidden prefix 0件
- 性能: compute_field_strength_profile が秒単位（cProfile 検証済み・Pitfall 2 回避）
</verification>

<success_criteria>
- SC#4 SAFE-01 proxy 排除証明が adversarial audit で完了する（AST odds/ninki proxy 0件・lookahead 注入で guard 有効性証明・false-pass 回避）
- 新 feature モジュール（field_strength.py・race_relative.py）に市場回帰と未来情報リークが混入しないことが機械保証される
- test_audit_speed_figure.py の SAFE-01 検査対象が Phase 10 全 feature に拡張される
- compute_field_strength_profile の vectorized 実装が Pitfall 2 を回避し本番運用可能であることが実証される
</success_criteria>

<output>
Create `.planning/phases/10-opponent-strength-race-relative-features/10-07-SUMMARY.md` when done
</output>
