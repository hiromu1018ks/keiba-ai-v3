---
phase: 10-opponent-strength-race-relative-features
plan: 07
type: execute
wave: 5
depends_on: [05]
files_modified:
  - src/audit/report.py
  - tests/audit/test_audit_field_strength.py
  - tests/audit/test_audit_speed_figure.py
autonomous: true
requirements: [SAFE-01]
granularity: fine

must_haves:
  truths:
    # SC#4（SAFE-01 proxy 排除証明・adversarial audit）
    - "src/features/field_strength.py と src/features/race_relative.py のソース AST に odds/ninki/fukuodds/ninkij/tansyouodds の Name/Attribute ノードが0件である（SAFE-01・SC#4）"
    - **REVIEW H3 (10-REVIEWS.md L158, L197, L229)**: "src/features/field_strength.py と src/features/race_relative.py の SQL 文字列リテラル（ast.Constant value=str・word-boundary）に ninki/fukuodds/ninkij/tansyouodds が0件である（既存 scanner がカバー）ことに加え・**odds も SQL 文字列リテラル検査で検出される** ことを機械保証する。既存 scanner は odds を Name/Attribute 完全一致のみ扱い SQL リテラルから意図的に除外している（test_audit_speed_figure.py L39-48）ため・本 PLAN は odds を SQL 文字列リテラル検査にも含める拡張（または専用 odds-in-SQL gate テスト）を追加し・SQL text に odds が埋込まれないことを保証する。whitelist（'odds-free' 等の無害な docstring/prose）は明示的に除外。"
    - "lookahead 注入テスト（adversarial・opponent-vs-source）: field_strength._pit_cutoff_prefilter を monkeypatch で <= 版に差し替えると T+1 opponent データが検出され test が RED になる（guard が有効なことを逆証明）"
    - **CYCLE-2 MEDIUM-C2-4 (10-REVIEWS.md L230-235, L301)**: "lookahead 注入テストは行包含でなく **値の不変性（value-invariance）** を検証する・同じ pre-source opponent race が異なる2つの target observation（feature_cutoff_datetime = T1 < T2・両方とも source race available_at=S より後）のどちらを消費しても・source-as-of full-pipeline 再計算された speed_figure が bit-identical になることを assert・これにより obs_id 展開済み target-cutoff-contaminated speed_figure の source-race 流用リーク（CYCLE-2 HIGH-C2-1・行包含テストでは GREEN だが値レベルでリークが残る）を SAFE-01 audit ファイルが検出できることを証明する。行包含（(S,T] 区間の opponent レース除外）に加えて値の不変性を独立に assert しないと監査が GREEN でもリークが残る（10-REVIEWS.md L230-235）。"
    - **REVIEW H4 (10-REVIEWS.md L160, L198, L229)**: "lookahead 注入テスト（adversarial・source-vs-target-cutoff・より重大なリークパス）: field_strength._compute_source_asof_opponent_speed_figures を monkeypatch で target observation の feature_cutoff_datetime を使う版に差し替えると・(source_race.available_at, target.feature_cutoff_datetime] 区間に走った opponent レースが par/variant/speed_figure 値に混入し値が変化する（guard 有効の逆証明・PLAN 01 の T-10-01b に対応）。本テストは本 PLAN でも独立に実行し・SAFE-01 audit ファイルがこのリークパスを検出できることを証明する。"
    # false-pass 回避
    - "guard が意図的注入（odds/ninki proxy の Name/Attribute/SQL 文字列）を検出できる（5段階鋳型(5)・false-pass 回避・test_audit_speed_figure.py L309-384 と対称）"
    - "FEATURE_COLUMNS allowlist（src/model/data.py 経由・106 feature）に forbidden prefix が0件である"
    # SAFE-01 検査対象拡張
    - "test_audit_speed_figure.py の expected_features hardcode list が Phase 10 の 27 新 feature で拡張される（SAFE-01 検査対象拡張）"
    # 性能検証（W-3 事前登録閾値）
    - "compute_field_strength_profile の vectorized 実装が W-3 事前登録閾値（縮小版 1000 race × 14 opponent = 14000 行 で wall time ≤ 5.0 秒・cProfile 上位3位以内に Python ループ無し）を満たす（Pitfall 2 回避の機械保証・実測で下回れなかった場合は PLAN 更新してから再実行・後追い緩和禁止）"
  artifacts:
    - "tests/audit/test_audit_field_strength.py が存在し全テスト GREEN（AST odds/ninki proxy 排除・lookahead 注入・false-pass 検出）"
    - "tests/audit/test_audit_speed_figure.py の expected_features hardcode list が Phase 10 の 27 feature で拡張される"
    - "src/audit/report.py の SURFACE_ROWS に Phase 10 feature 行が追加される（任意・SC#4 supplement）"
  key_links:
    - "test_audit_field_strength.py は tests/audit/test_audit_speed_figure.py の5段階鋳型（_FORBIDDEN_TOKENS + _scan_module_for_forbidden_tokens）を直接踏襲する"
    - "_FORBIDDEN_TOKENS = frozenset({'odds', 'ninki', 'fukuodds', 'ninkij', 'tansyouodds'}) を共通定数として再定義または import"
    - "AST 検査は docstring/comment の 'odds-free' 等の文字列を false positive に出さない（Name/Attribute ノード厳密判定・REVIEW H5）"
---

<objective>
Phase 10 PLAN 07: SC#4 SAFE-01 proxy 排除証明を adversarial audit で完了する。新規モジュール field_strength.py / race_relative.py の AST に odds/ninki proxy が混入しないことを静的証明し・lookahead 注入テスト（guard 無効化で違反検出）で PIT 保証を逆証明する。更に compute_field_strength_profile の vectorized 実装の性能を cProfile で検証する（Pitfall 2 回避・**W-3 事前登録閾値**: 縮小版 14000 行 ≤ 5.0 秒・cProfile 上位3位以内に Python ループ無し・後追い緩和禁止）。

Purpose: core value「リーク防止最優先」の最終保証層。新 feature が市場回帰（odds/ninki proxy）と未来情報リーク（lookahead）のいずれも引き起こさないことを機械的に証明する。Phase 9/9.1 の adversarial audit idiom を直接拡張する。

Output: tests/audit/test_audit_field_strength.py（5段階鋳型・AST odds/ninki proxy 排除・lookahead 注入・false-pass 検出）・tests/audit/test_audit_speed_figure.py 拡張（hardcode list 更新）・src/audit/report.py 拡張（任意）・cProfile 性能検証レポート
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-opponent-strength-race-relative-features/10-CONTEXT.md
@.planning/phases/10-opponent-strength-race-relative-features/10-RESEARCH.md
@.planning/phases/10-opponent-strength-race-relative-features/10-PATTERNS.md

# PLAN 01/03 出力（依存・検査対象モジュール）
@.planning/phases/10-opponent-strength-race-relative-features/10-01-PLAN.md
@.planning/phases/10-opponent-strength-race-relative-features/10-03-PLAN.md

# analog（必読・5段階鋳型）
@tests/audit/test_audit_speed_figure.py
@src/audit/report.py
@src/features/field_strength.py
@src/features/race_relative.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tests/audit/test_audit_field_strength.py を作成し SAFE-01 adversarial audit（AST + lookahead 注入）を実装</name>
  <files>tests/audit/test_audit_field_strength.py, tests/audit/test_audit_speed_figure.py</files>

  <read_first>
    - tests/audit/test_audit_speed_figure.py L1-48（imports・_FORBIDDEN_TOKENS frozenset 定義・共通定数）
    - tests/audit/test_audit_speed_figure.py L63-108（_scan_module_for_forbidden_tokens: AST Name/Attribute ノード検査・SQL 文字列リテラル検査・5段階鋳型(1)(2)(3)）
    - tests/audit/test_audit_speed_figure.py L123-172（test_no_odds_ninki_proxy_in_*_source: AST audit・対称に field_strength/race_relative を検査）
    - tests/audit/test_audit_speed_figure.py L178-256（test_feature_columns_contains_*_features_no_proxy: FEATURE_COLUMNS allowlist 検査・expected_features hardcode list）
    - tests/audit/test_audit_speed_figure.py L309-384（test_false_pass_detection_power: 5段階鋳型(5)・意図的注入を検出することを証明）
    - src/features/field_strength.py（PLAN 01 出力・検査対象）
    - src/features/race_relative.py（PLAN 03 出力・検査対象）
    - src/features/speed_figure.py L98-118（_pit_cutoff_prefilter・lookahead 注入テストの monkeypatch 対象）
    - **CYCLE-2 MEDIUM-C2-4 / HIGH-C2-1 必読**: src/features/speed_figure.py L343-378・L401-402・L509-510・L633-658・L683-695（obs_id 展開 = target-cutoff-contamination の証拠・行包含でなく値の不変性で監査しなければならない根拠・10-REVIEWS.md L181-186, L230-235）・.planning/phases/10-opponent-strength-race-relative-features/10-REVIEWS.md L57-92・L171-209・L230-235・L295・L301（CYCLE-2 HIGH-C2-1 root cause + MEDIUM-C2-4 監査 blind spot の完全説明）
    - tests/features/test_speed_figure_pit.py（PIT adversarial テストの analog・_pit_cutoff_prefilter monkeypatch idiom）
    - .planning/phases/10-opponent-strength-race-relative-features/10-RESEARCH.md L673-686（FEAT-02/03 SAFE-01 test map）・L435-441（Pitfall 3 D-01 PIT 違反・adversarial lookahead 注入）
    - .planning/phases/10-opponent-strength-race-relative-features/10-PATTERNS.md L577-621（test_audit_field_strength.py 5段階鋳型 idiom）
  </read_first>

  <behavior>
    - Test 1 (AST Name/Attribute・SC#4): src.features.field_strength モジュールの AST に odds/ninki/fukuodds/ninkij/tansyouodds の Name ノード(id)・Attribute ノード(attr) が0件
    - Test 2 (AST Name/Attribute・SC#4): src.features.race_relative モジュールも同様に0件
    - Test 3 (SQL 文字列リテラル ninki系・既存 scanner): 両モジュールの SQL 文字列リテラル内に ninki/fukuodds/ninkij/tansyouodds の word-boundary 部分一致が0件（既存 _scan_module_for_forbidden_tokens がカバー）
    - Test 4 (REVIEW H3・SQL 文字列リテラル odds 拡張・10-REVIEWS.md L158, L197, L229): odds も SQL 文字列リテラル検査で検出されることを証明。既存 scanner は odds を SQL リテラル検査から意図的に除外している（test_audit_speed_figure.py L39-48 が "odds" 単独を _FORBIDDEN_PROXY_SUBSTRING_TOKENS から除外）ため・本テストは odds を SQL リテラル検査に含める拡張（_FORBIDDEN_PROXY_SUBSTRING_TOKENS に 'odds' を追加 または 専用 odds-in-SQL gate）を追加し・SQL 文字列中の odds が検出されることを検証。whitelist で 'odds-free' 'odds_snapshot_policy' 等の無害 prose は明示的に除外し false-positive を防ぐ。
    - Test 5 (false-pass 回避・5段階鋳型(5)): 意図的に odds/ninki proxy の Name/Attribute/SQL 文字列を注入した偽モジュールで guard が検出することを証明
    - Test 6 (lookahead 注入 opponent-vs-source・D-01 PIT 保証・adversarial): field_strength._pit_cutoff_prefilter を monkeypatch で <= 版に差し替えると T+1 opponent データが profile に混入し値が変化する・guard が有効なことを逆証明
    - Test 7 (**CYCLE-2 MEDIUM-C2-4 value-invariance + REVIEW H4 source-vs-target-cutoff lookahead 注入・adversarial・10-REVIEWS.md L160, L198, L229, L230-235, L301**): (a) **行包含**(REVIEW H4): field_strength._compute_source_asof_opponent_speed_figures を monkeypatch で target observation の feature_cutoff_datetime を使う版に差し替えると・(source.available_at, target.feature_cutoff_datetime] 区間に走った opponent レースが profile に混入し値が変化する（PLAN 01 Test 2 と対・本 PLAN で独立実行で SAFE-01 audit ファイルがこの経路のリークを検出できることを証明）。(b) **値の不変性**(CYCLE-2 MEDIUM-C2-4・行包含テストでは不十分): 同じ pre-source opponent race を異なる2つの target observation（feature_cutoff_datetime=T1<T2・両方とも source available_at=S より後）で消費した時・source-as-of full-pipeline 再計算された speed_figure が bit-identical になることを assert（行包含でなく値レベルのリーク検出・10-REVIEWS.md L230-235）。source-vs-opponent（`<`→`<=`）でなく source-vs-target-cutoff を直接 poke する・より重大なリークパス。行包含のみ（Cycle-1 の H4 テスト）では値レベルのリークが残るため・値の不変性を独立に assert することが本 Test の核心。
    - Test 8 (FEATURE_COLUMNS allowlist): src/model/data.py の FEATURE_COLUMNS（106 feature）に forbidden prefix が0件
  </behavior>

  <action>
    tests/audit/test_audit_field_strength.py を新規作成する（tests/audit/test_audit_speed_figure.py の5段階鋳型を直接踏襲・PAT§ L577-621 idiom）:

    (1) imports: `from src.features import field_strength, race_relative`・`import ast`・必要に応じて `from src.features.availability import ...`。`_FORBIDDEN_TOKENS` は test_audit_speed_figure.py から import するか共通化して再定義（frozenset({'odds', 'ninki', 'fukuodds', 'ninkij', 'tansyouodds'})）。

    (2) _scan_module_for_forbidden_tokens(module) helper: test_audit_speed_figure.py L63-108 の idiom を再利用（または共通ユーティリティ化して import）。AST Name ノード(id)・Attribute ノード(attr)・SQL 文字列リテラル（word-boundary 部分一致）の3層検査。**REVIEW H3 (10-REVIEWS.md L158, L197, L229)**: 既存 scanner は SQL リテラル検査で odds を意図的に除外している（test_audit_speed_figure.py L39-48・_FORBIDDEN_PROXY_SUBSTRING_TOKENS が ninki/fukuodds/ninkij/tansyouodds のみ）。本 PLAN は field_strength/race_relative 向けに odds を SQL リテラル検査に含める拡張を行う（専用 helper _scan_module_for_forbidden_tokens_with_odds_in_sql・または _FORBIDDEN_PROXY_SUBSTRING_TOKENS に 'odds' を追加した上新たな false-positive を避ける whitelist ['odds-free', 'odds_snapshot_policy', 'odds_snapshot_policy_doc'] を設ける）。これにより SQL text に odds が埋込まれないことを機械保証。test_audit_speed_figure.py の既存の（odds を SQL 除外する）振る舞いは既存モジュール向けに保持し・新規 field_strength/race_relative に対してのみ odds-in-SQL 拡張を適用（regression 防止）。

    (3) (a)(b)(c) AST audit テスト（test_audit_speed_figure.py L123-172 と対称）:
        - test_no_odds_ninki_proxy_in_field_strength_source: src.features.field_strength の AST が forbidden Name/Attribute 0件・SQL proxy 0件を assert。
        - test_no_odds_ninki_proxy_in_race_relative_source: src.features.race_relative も同様。

    (4) (d) FEATURE_COLUMNS allowlist テスト（test_audit_speed_figure.py L178-256 と対称）:
        - _PHASE10_SNAPSHOT_ID = "20260626-1a-opponentstrength-v1" を定義。
        - test_feature_columns_contains_phase10_features_no_proxy: _derive_feature_columns(snapshot_id=_PHASE10_SNAPSHOT_ID) が返す 106 feature が rolling_field_strength_mean_mean_5 / speed_index_rank_mean5 / gap_to_top / gap_to_3rd / field_strength_adjusted_rank 等 の新 feature を含み・かつ forbidden prefix が0件であることを assert。

    (5) (g) false-pass 回避テスト（test_audit_speed_figure.py L309-384 と対称・そのまま再利用可能）:
        - test_false_pass_detection_power: 意図的に forbidden token を注入した偽ソース（インメモリ文字列または一時ファイル）で guard が検出することを証明。5段階鋳型(5)。

    (6) (新規) lookahead 注入テスト・D-01 PIT 保証 adversarial（opponent-vs-source・既存指摘）:
        - test_lookahead_injection_detected: monkeypatch で src.features.field_strength._pit_cutoff_prefilter を `<=` 版（opponent.available_at <= self.available_at）に差し替え・source race と同日の opponent speed_figure を含む合成 history で compute_field_strength_profile を呼ぶ。guard 有効時（本番の strict `<`）は同日 opponent が除外され profile が変化する・guard 無効時（`<=` 版）は同日 opponent が混入し profile が異なる値になることを assert（guard が有効なことを逆証明）。

    (6b) **CYCLE-2 MEDIUM-C2-4 + REVIEW H4 (10-REVIEWS.md L160, L198, L229, L230-235, L301)** (新規) source-vs-target-cutoff lookahead 注入テスト・より重大なリークパス adversarial・**行包含と値の不変性の両方** を検証:
        - test_source_vs_target_cutoff_lookahead_injection_detected: 合成 raw_history で・source race available_at=S・target observation feature_cutoff_datetime=T (S < T) の状況を作る。opponent の speed_figure 履歴のうち・(S, T] 区間に走った強いレースを含める。
        - **(a) 行包含（REVIEW H4）**: guard 有効時（本番の _compute_source_asof_opponent_speed_figures が source.available_at=S を cutoff として full-pipeline 再計算）は (S,T] の opponent レースが除外される・guard 無効時（monkeypatch で再計算 cutoff を target cutoff=T に差替）は (S,T] の opponent レースが混入し profile が異なる値になることを assert（guard が有効なことを逆証明）。
        - **(b) 値の不変性（CYCLE-2 MEDIUM-C2-4・行包含テストでは不十分・10-REVIEWS.md L230-235）**: 同じ pre-source opponent race（available_at < S）を異なる2つの target observation（feature_cutoff_datetime=T1<T2・両方とも S より後・(S,T2] に当該 opponent の強い追加レースあり）で消費した時・source-as-of full-pipeline 再計算された speed_figure 値が bit-identical になることを assert。Cycle-1 の行包含テスト（(S,T] opponent レース除外）は GREEN だが・obs_id 展開済み target-cutoff-contaminated speed_figure を再利用していると値レベルでリークが残る（10-REVIEWS.md L230-235）・値の不変性を独立に assert することでこのリークを検出する。
        - このテストは opponent-vs-source（`<`→`<=`）でなく・CYCLE-2 HIGH-C2-1 が指摘したより重大なリークパス（obs_id 展開済み target-cutoff-contaminated speed_figure を source-race opponent 流用）を直接 poke する。

    (7) tests/audit/test_audit_speed_figure.py 拡張:
        - L219-238 等の expected_speed_features hardcode list を Phase 10 の 27 新 feature で拡張（09.1 で 17 feature 追加した idiom と対称）。これにより SAFE-01 検査対象が Phase 10 全 feature に拡張される。

    src/audit/report.py の SURFACE_ROWS 拡張は任意（SC#1 supplement 的位置づけ）・本 task では必須でない。追加する場合は AUDIT_SURFACE_COLUMNS 7項目スキーマに従う。

    docstring/comment に _FORBIDDEN_TOKENS の文字列リテラル（"odds" 等）をそのまま書かないこと（自前のテストが自身を fail させないよう・概念で書くか allowlist marker を使う）。
  </action>

  <verify>
    <automated>uv run pytest tests/audit/test_audit_field_strength.py tests/audit/test_audit_speed_figure.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - tests/audit/test_audit_field_strength.py が存在する
    - tests/audit/test_audit_field_strength.py に `from src.features import field_strength, race_relative` を含む
    - tests/audit/test_audit_field_strength.py に `test_no_odds_ninki_proxy_in_field_strength_source` `test_no_odds_ninki_proxy_in_race_relative_source` が存在する
    - tests/audit/test_audit_field_strength.py に `test_lookahead_injection_detected`（_pit_cutoff_prefilter monkeypatch による opponent-vs-source adversarial lookahead テスト）が存在する
    - **REVIEW H4 + CYCLE-2 MEDIUM-C2-4 (10-REVIEWS.md L160, L198, L229, L230-235, L301)**: tests/audit/test_audit_field_strength.py に `test_source_vs_target_cutoff_lookahead_injection_detected`（_compute_source_asof_opponent_speed_figures monkeypatch による source-vs-target-cutoff adversarial lookahead テスト・(S,T] 区間の opponent レース混入を行包含で検出）が存在する・**加えて値の不変性 assert**（同じ pre-source opponent race を異なる target cutoff T1<T2 で消費しても source-as-of full-pipeline 再計算 speed_figure が bit-identical）が同じテスト内または別テストで存在する・行包含のみでなく値レベルのリーク検出を機械保証（10-REVIEWS.md L230-235・Cycle-1 行包含テストでは不十分と判明）
    - tests/audit/test_audit_field_strength.py に `test_false_pass_detection_power`（5段階鋳型(5)・意図的注入検出）が存在する
    - **REVIEW H3 (10-REVIEWS.md L158, L197, L229)**: tests/audit/test_audit_field_strength.py に odds を SQL 文字列リテラル検査に含めた拡張テスト（または専用 odds-in-SQL gate テスト）が存在する・whitelist で 'odds-free' 等の無害 prose を除外し false-positive を防ぐ・SQL text 中の odds を検出できることを検証
    - tests/audit/test_audit_field_strength.py に `_PHASE10_SNAPSHOT_ID = "20260626-1a-opponentstrength-v1"` が定義される
    - tests/audit/test_audit_speed_figure.py の expected_features リストが Phase 10 の 27 新 feature を含む（rolling_field_strength_mean_mean_5・speed_index_rank_mean5・gap_to_top・gap_to_3rd・field_strength_adjusted_rank 等）
    - src/features/field_strength.py と src/features/race_relative.py の AST に odds/ninki/fukuodds/ninkij/tansyouodds の Name/Attribute/SQL 文字列 proxy（odds 含む・REVIEW H3 拡張）が0件（テストが GREEN で証明）
    - `uv run pytest tests/audit/ -x -q` GREEN（新規 test_audit_field_strength + 既存 test_audit_speed_figure 回帰）
    - ruff lint GREEN: `uv run ruff check tests/audit/test_audit_field_strength.py tests/audit/test_audit_speed_figure.py`
  </acceptance_criteria>

  <done>
    test_audit_field_strength.py が5段階鋳型（AST Name/Attribute・SQL proxy・FEATURE_COLUMNS allowlist・false-pass 回避・lookahead 注入）で SAFE-01 と PIT 保証を証明する。新 feature モジュール（field_strength.py・race_relative.py）に odds/ninki proxy と lookahead が混入しないことが静的・動的に保証される。test_audit_speed_figure.py の検査対象が Phase 10 全 feature に拡張される。
  </done>
</task>

<task type="auto">
  <name>Task 2: compute_field_strength_profile の vectorized 実装の性能を cProfile で検証（Pitfall 2 回避）</name>
  <files>tests/features/test_field_strength.py</files>

  <read_first>
    - src/features/field_strength.py（PLAN 01 出力・性能検証対象）
    - .planning/phases/10-opponent-strength-race-relative-features/10-RESEARCH.md L412-434（Pitfall 2 計算量爆発・6.7M ペア・純粋ループ致命的低速・cProfile hot spot 警告）・L622-630（A3 計算量見積もり・MEDIUM confidence・PLAN で cProfile 必要）
    - .planning/phases/10-opponent-strength-race-relative-features/10-PATTERNS.md（field_strength.py idiom・per-source-race batch + vectorized groupby）
  </read_first>

  <behavior>
    - Test 1 (性能検証・Pitfall 2 回避): history ~48万行 × 平均14 opponent 規模の合成データ（または縮小版）で compute_field_strength_profile を呼び・実行時間が秒単位（純粋ループの数十分と対比）であることを確認
    - Test 2 (cProfile hot spot): cProfile で Python ループ（iterrows・for race in races 等）が hot spot に現れないことを確認・vectorized groupby/nlargest/sort_values が主流であることを確認
    - **W-3 事前登録閾値**: 縮小版 1000 race × 14 opponent = 14000 行 で compute_field_strength_profile を実行した時の wall time が **5.0 秒以内** であること（事前登録値・実測で値を下回れなかった場合は PLAN を更新してから再実行・後追いで閾値を緩めない）
    - **W-3 cProfile hot spot 事前登録**: cProfile 統計の上位3位以内に純粋 Python ループ（iterrows / `for ... in ...` で Python 側の行iterate / DataFrame.apply での Python 関数適用）が現れないこと（pandas/numpy の C 実装 groupby/nlargest/sort_values が主流であること）
  </behavior>

  <action>
    tests/features/test_field_strength.py に性能検証テストを追加する（必須・Pitfall 2 回避の実証・W-3 事前登録閾値で機械保証）:

    (1) test_compute_field_strength_profile_performance テストを追加:
        - 合成 history を規模実データに近い形で生成（**縮小版 1000 race × 14 opponent = 14000 行**・本番は48万行だが unit test では縮小・W-3 事前登録規模）。
        - time.perf_counter() で compute_field_strength_profile の実行時間を計測。
        - **W-3 事前登録閾値**: 実行時間が **5.0 秒以内** であることを assert（モジュール定数 PERF_BUDGET_SEC: float = 5.0 として事前登録・本 PLAN への事前登録値）。閾値は「純粋ループの致命的低速（数十分）」と対比して現実的な秒単位であることを示す値。
        - **W-3 聖域**: 実測で 5.0 秒を超過した場合はテストを RED にして PLAN を更新（閾値の根拠を再確認）してから再実行すること。実測に合わせて閾値を後追いで緩める（5.0 → 10.0 等）のは禁止（Pitfall 2・本番で数十分の低速化を回避する機械保証なので）。

    (2) test_no_python_loop_hot_spot テストを追加（cProfile・**W-3 事前登録**）:
        - cProfile.run で compute_field_strength_profile を実行し・プロファイル統計を取得。
        - **W-3 事前登録**: hot spot 上位 **3位以内** が pandas/numpy の C 実装（groupby・nlargest・sort_values 等）であることを確認・純粋 Python ループ（iterrows・`for ... in ...` の行iterate・apply での Python 関数）が上位3位以内に現れないことを assert。
        - 万が一 Python ループが上位3位以内にある場合は PLAN 01 の vectorized 実装を見直す必要がある（Pitfall 2 回避・テストを RED にして PLAN 01 修正後に再実行）。

    この検証は SC#5（機能ノイズ化でない）の前提・および本番運用可能性（snapshot 生成が現実的時間で完了する）の保証。W-3 事前登録閾値で「実測で調整」の曖昧さを排除し Pitfall 2 回避を機械保証する。
  </action>

  <verify>
    <automated>uv run pytest tests/features/test_field_strength.py -x -q -k "performance or hot_spot"</automated>
  </verify>

  <acceptance_criteria>
    - tests/features/test_field_strength.py に `test_compute_field_strength_profile_performance` が存在する
    - tests/features/test_field_strength.py に `test_no_python_loop_hot_spot`（cProfile・**W-3 事前登録**）が存在する
    - **W-3 事前登録閾値**: 性能テストが GREEN（縮小版 14000 行で wall time ≤ 5.0 秒・モジュール定数 PERF_BUDGET_SEC=5.0 で事前登録）。実測で 5.0 秒を超過した場合はテスト RED → PLAN 更新（閾値根拠再確認）→ 再実行（後追い緩和禁止）。
    - **W-3 cProfile 事前登録**: cProfile 統計の上位3位以内に純粋 Python ループ（iterrows / for-in 行iterate / apply Python 関数）が現れない
    - PLAN 01 の vectorized 実装が縮小版 14000 行で 5 秒以内（事前登録）・6.7M ペア規模への外挿で秒〜十数秒単位で完了することが実証される
  </acceptance_criteria>

  <done>
    compute_field_strength_profile の vectorized 実装が Pitfall 2（計算量爆発）を回避し・W-3 事前登録閾値（縮小版 14000 行 ≤ 5.0 秒・cProfile 上位3位以内に Python ループ無し）で機械保証される。本番 snapshot 生成が現実的時間で完了する保証される。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 新 feature モジュール（field_strength.py・race_relative.py）→ adversarial audit | 静的 AST 検査と動的 lookahead 注入で SAFE-01/PIT 保証を証明する最終層 |
| FEATURE_COLUMNS allowlist → SAFE-01 | 106 feature 全てに forbidden prefix が0件であることを静的保証 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-28 | Information disclosure | odds/ninki proxy の feature 混入（SAFE-01 違反） | high | mitigate | AST Name/Attribute/SQL proxy 3層検査・lookahead 注入テスト・false-pass 回避テスト（5段階鋳型）で機械証明 |
| T-10-28b | Information disclosure | **REVIEW H3 (10-REVIEWS.md L158, L197, L229)** 既存 scanner は odds を SQL リテラル検査から意図的に除外（test_audit_speed_figure.py L39-48）・SQL text に odds が埋込まれても検出されない false-pass | high | mitigate | field_strength/race_relative 向けに odds を SQL リテラル検査に含める拡張（_FORBIDDEN_PROXY_SUBSTRING_TOKENS に 'odds' 追加 または 専用 odds-in-SQL gate）・whitelist ['odds-free', 'odds_snapshot_policy'] で無害 prose を除外・Test 4 で SQL text 中 odds を検出できることを証明 |
| T-10-29 | Tampering | lookahead leak（未来情報の特徴量混入・opponent-vs-source） | critical | mitigate | _pit_cutoff_prefilter monkeypatch で guard を `<=` に差替・T+1 opponent 混入を検出する adversarial test（guard 有効の逆証明） |
| T-10-29b | Tampering | **REVIEW H4 + CYCLE-2 MEDIUM-C2-4 (10-REVIEWS.md L160, L198, L229, L230-235, L301)** source-vs-target-cutoff lookahead（obs_id 展開済み target-cutoff-contaminated speed_figure を source-race opponent 流用で再利用すると (source, target] 区間の opponent レースが値レベルで混入・より重大なリークパス・行包含テストでは捉えられない） | critical | mitigate | _compute_source_asof_opponent_speed_figures monkeypatch で再計算 cutoff を target cutoff に差替・(S,T] 区間の opponent レース混入で profile が変化することを行包含で検出（Test 7a）・**加えて値の不変性 assert**（同じ pre-source opponent race を異なる target cutoff で消費しても source-as-of full-pipeline 再計算 speed_figure が bit-identical）で値レベル検出（Test 7b・Cycle-1 行包含テストでは不十分・10-REVIEWS.md L230-235）・PLAN 01 T-10-01b に対応・本 PLAN で独立実行 |
| T-10-30 | Tampering | false-pass（guard が実は何も検出していない状態） | high | mitigate | 5段階鋳型(5) test_false_pass_detection_power・意図的注入を検出することを証明 |
| T-10-31 | Denial of service | compute_field_strength_profile の計算量爆発 | medium | mitigate | cProfile 性能検証・**W-3 事前登録閾値（縮小版 14000 行 ≤ 5.0 秒・cProfile 上位3位以内に Python ループ無し）**・vectorized groupby/nlargest で秒単位（Pitfall 2 回避・後追い閾値緩和禁止） |
</threat_model>

<verification>
- 単体テスト: `uv run pytest tests/audit/ tests/features/test_field_strength.py -x -q` GREEN
- lint: `uv run ruff check tests/audit/ src/features/field_strength.py src/features/race_relative.py` GREEN
- SC#4 SAFE-01 証明: 新 feature モジュール AST に odds/ninki proxy 0件・FEATURE_COLUMNS allowlist に forbidden prefix 0件
- 性能: compute_field_strength_profile が W-3 事前登録閾値（縮小版 14000 行 ≤ 5.0 秒・cProfile 上位3位以内に Python ループ無し）を満たす（cProfile 検証済み・Pitfall 2 回避）
</verification>

<success_criteria>
- SC#4 SAFE-01 proxy 排除証明が adversarial audit で完了する（AST odds/ninki proxy 0件・lookahead 注入で guard 有効性証明・false-pass 回避）
- 新 feature モジュール（field_strength.py・race_relative.py）に市場回帰と未来情報リークが混入しないことが機械保証される
- test_audit_speed_figure.py の SAFE-01 検査対象が Phase 10 全 feature に拡張される
- compute_field_strength_profile の vectorized 実装が W-3 事前登録閾値（縮小版 14000 行 ≤ 5.0 秒・cProfile 上位3位以内に Python ループ無し）で Pitfall 2 を回避し本番運用可能であることが実証される
</success_criteria>

<output>
Create `.planning/phases/10-opponent-strength-race-relative-features/10-07-SUMMARY.md` when done
</output>

<artifacts_produced>
## Artifacts this phase produces

新規・拡張シンボル（plan-review-convergence の source-grounding pass が新規作成として除外すべき対象）:

- **新規テストファイル**: `tests/audit/test_audit_field_strength.py`（5段階鋳型 + REVIEW H3 odds-in-SQL 拡張 + REVIEW H4 + CYCLE-2 MEDIUM-C2-4 source-vs-target-cutoff lookahead テスト【行包含 + 値の不変性】・8 テスト）
- **拡張テストファイル**: `tests/audit/test_audit_speed_figure.py`（expected_features hardcode list に Phase 10 の 27 feature 追加）
- **新規 helper（任意）**: `_scan_module_for_forbidden_tokens_with_odds_in_sql(module)` または `_FORBIDDEN_PROXY_SUBSTRING_TOKENS_WITH_ODDS` と whitelist `['odds-free', 'odds_snapshot_policy', ...]`（REVIEW H3）
- **新規定数**: `_PHASE10_SNAPSHOT_ID = "20260626-1a-opponentstrength-v1"` / `PERF_BUDGET_SEC: float = 5.0`（W-3 事前登録）
- **拡張モジュール（任意）**: `src/audit/report.py`（SURFACE_ROWS に Phase 10 feature 行追加・SC#4 supplement）
- **新規テスト（性能）**: `test_compute_field_strength_profile_performance` / `test_no_python_loop_hot_spot`（tests/features/test_field_strength.py に追加）
</artifacts_produced>
