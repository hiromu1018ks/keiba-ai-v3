---
phase: 10-opponent-strength-race-relative-features
plan: 08
type: execute
gap_closure: true
wave: 1
depends_on: ["10-01", "10-02", "10-03", "10-04", "10-05", "10-06", "10-07"]
files_modified:
  - src/features/field_strength.py
  - src/features/race_relative.py
  - src/features/builder.py
  - src/features/snapshot.py
  - scripts/run_phase10_evaluation.py
  - tests/features/test_field_strength.py
  - tests/audit/test_audit_field_strength.py
autonomous: false
requirements:
  - FEAT-02
  - FEAT-03
  - SAFE-01
requirements_note: "gap-closure（regression-guard / hardening）。FEAT-02/FEAT-03/SAFE-01 の新規要件追加でなく・10-REVIEW.md 4 Critical + 6 Warning の fail-loud 化・堅牢化・silent fallback 経路封印・live-DB 再生成検証が目的。core value「リーク防止」の鏡像「silent fallback 禁止」を機械保証。"
tags: ["gap-closure", "fail-loud", "silent-fallback", "live-db-regen", "phase-11-input"]

must_haves:
  truths:
    # CR-01 silent data loss 経路の封印（core value の鏡像）
    - "src/features/field_strength.py の _compute_source_asof_opponent_speed_figures に・source_available_at_by_race が非空かつ全 source race が starter 不在（batches が空）の場合に RuntimeError を raise する fail-loud 経路が存在する（silent empty DataFrame 返却を封印・CYCLE-2 HIGH-C2-1）"
    - "src/features/field_strength.py の compute_field_strength_profile に・starters が存在する source race 数 vs source_available_at_by_race の件数を比較する fail-loud 検査が存在する（silent data quality 低下の検知）"
    - "src/features/field_strength.py の各バッチで空 synth_obs を追跡し・空バッチが混在する場合は logger.warning で記録する（silent pd.concat による source race 欠落の可視化）"
    # CR-02 silent NaN merge の封印
    - "src/features/builder.py Step5c の profile merge 実行前に・history['race_date'] と _profile_merge['race_date'] の両者が pd.to_datetime で正規化される（dtype mismatch 由来の silent NaN merge を構造的排除）"
    - "src/features/builder.py Step5c の merge 後に・starter_mask（kakuteijyuni > 0）上の field_strength_mean の notna 率が 0.5 未満の場合に RuntimeError を raise する fail-loud 検査が存在する（FEAT-02 21 feature 全行 sentinel 化の silent data loss を検知）"
    # CR-03 race_relative 境界防御
    - "src/features/race_relative.py の _gap_to_3rd_within_race に・len(mean5) < 3 の場合の早返し（NaN series を index=mean5.index で返す）が存在する（pandas バージョン依存の transform 境界を明示防御）"
    # CR-04 W-2 証跡の fail-loud 化（§11.2 聖域強化）
    - "scripts/run_phase10_evaluation.py の _compute_w2_diagnostics が・必須列（rolling_speed_figure_mean_5 / rolling_field_strength_mean_mean_5 等）欠損時に WARNING skip でなく RuntimeError を raise する（W-2 acceptance criteria 未達・§11.2 聖域履行証跡欠損を防ぐ）"
    - "scripts/run_phase10_evaluation.py の _compute_w2_diagnostics 呼出し前に・phase10_frame が rolling 系 feature を含むことを assert する"
    # WR-02 statement_timeout の pool 全体適用
    - "scripts/run_phase10_evaluation.py が・make_pool 後に psycopg_pool の configure callback で SET statement_timeout='30s' を pool 全体に適用する（cursor 単位でなく・connection checkout 毎に SET・memory subagent-db-query-statement-timeout）"
    # WR-03 snapshot 動的導出（更新忘れ排除）
    - "src/features/snapshot.py の _FEAT03_NUMERIC_COLUMNS が・race_relative.py の _AXIS_TO_RANK_SUFFIX から動的導出される（frozenset ハードコードでなく・新 feature 追加時の snapshot.py 更新忘れを排除）"
    # WR-04 本番規模性能 smoke（W-3 聖域遵守・緩和でなく検出力向上）
    - "tests/features/test_field_strength.py に PROD_SMOKE_N_RACES_LARGE=1000 を用いた test_production_scale_smoke_large が存在し・peak_rss ≤ PROD_PEAK_MEM_BUDGET_GB(8.0) で PASS する（KEIBA_RUN_PERF_TESTS=1・W-3 per-race 予算 ≤0.30s/race + 準二次スケーリングガード・cProfile 上位3位に Python ループ無し・全て不変）"
    # WR-05 rss 主指標化
    - "tests/features/test_field_strength.py::test_production_scale_smoke_no_h_squared_blowup の peak memory 主指標が rss_gb(getrusage) に変更され・tracemalloc(peak_py) は参考記録扱い・docstring に『tracemalloc は Python ヒープのみ・numpy/pandas native buffer 含まず過小評価』が明記される"
    # WR-06 audit テストの silent fallback mask 排除
    - "tests/audit/test_audit_field_strength.py::test_feature_columns_contains_phase10_features_no_proxy の else 経路（snapshot 未生成時）が pytest.raises(FileNotFoundError) でなく pytest.skip(...) に変更される（silent fallback で mask しない・実 snapshot 検証は test_data.py::test_phase10_* に一本化）"
    # WR-01 obs_id parseの堅牢化（docstring + テスト整合）
    - "src/features/field_strength.py の _opponent_ability_latest_mean5 の obs_id parse に関する docstring に・make_race_nkey が _ を含まないこと・rsplit('_', n=1) が kettonum 境界として安全であることが明記される。tests/features/test_field_strength.py の _fs_history_row ヘルパが本番 make_race_nkey 形式（_ 含まない・YYYYJJJKKNN 形式）に整合する"
    # live-DB snapshot 再生成（CR-02/WR-03 で値が変わる可能性の証明）
    - "live-DB（KEIBA_SKIP_DB_TESTS unset・readonly role）で feature snapshot 20260626-1a-opponentstrength-v1 を再生成し・CR-02 dtype 正規化と WR-03 snapshot coerce の修正前後で SHA256 が一致する（silent NaN merge が存在しなかったことの証明）・または値が変わる場合は SC#5 非劣化 gate を再実行して D-16 許容幅内であることを確認する"
    - "再生成 snapshot で tests/features/test_snapshot_repro.py（SC#3 byte-reproducibility・sha256・§12.4 metadata）が GREEN・registry↔Parquet parity（feature_count=106・schema_version 0.6.0）が GREEN"
    - "Phase 11 入力 snapshot 整合: 再生成 snapshot が Phase 11 MODEL-01 の入力として使用可能であること（feature_count=106・27 Phase 10 feature 含有・SAFE-01 odds/ninki proxy 0 件）"
    # 聖域回帰（SAFE-01 + §11.2 + §19.1）
    - "tests/audit/test_audit_field_strength.py（SC#4 SAFE-01・AST odds/ninki proxy 0 件・lookahead 注入）が GREEN を維持する（修正後も聖域回帰なし）"
    - "tests/features/test_field_strength.py + test_race_relative.py + test_rolling.py + test_snapshot_repro.py が全て GREEN（fail-loud の新テスト・境界テスト含む）"
    - "scripts/run_phase10_evaluation.py の W-2 diagnostic が status=ok を返す（rolling 系 feature 伝播済み・§11.2 聖域）"
  artifacts:
    - "src/features/field_strength.py（_compute_source_asof_opponent_speed_figures fail-loud・compute_field_strength_profile fail-loud・バッチ空追跡 warning・_opponent_ability_latest_mean5 docstring）"
    - "src/features/race_relative.py（_gap_to_3rd_within_race 早返し防御）"
    - "src/features/builder.py（Step5c race_date dtype 正規化 + JOIN 率 fail-loud）"
    - "src/features/snapshot.py（_FEAT03_NUMERIC_COLUMNS 動的導出）"
    - "scripts/run_phase10_evaluation.py（_compute_w2_diagnostics RuntimeError 格上げ + pool configure callback で statement_timeout）"
    - "tests/features/test_field_strength.py（fail-loud 新テスト・_fs_history_row 本番形式整合・rss 主指標化・test_production_scale_smoke_large 追加）"
    - "tests/audit/test_audit_field_strength.py（else 経路 pytest.skip 化）"
    - "snapshots/feature_matrix_20260626-1a-opponentstrength-v1.parquet（live-DB 再生成・SHA256 一致または再生成後 SHA256 記録）"
  key_links:
    - "field_strength.py _compute_source_asof_opponent_speed_figures の fail-loud → CYCLE-2 HIGH-C2-1 値レベル PIT 保証の silent fallback 経路封印"
    - "builder.py Step5c dtype 正規化 → FEAT-02 21 feature の silent sentinel 化封印（live-DB でしか発覚しない・memory feature-snapshot-regen-required）"
    - "run_phase10_evaluation.py W-2 RuntimeError → §11.2 聖域（test 窓 rank すり替え禁止）履行証跡の欠損を構造的ブロック"
    - "snapshot.py 動的導出 → 新 feature 追加時の Parquet 直列化 bug（実データでしか発覚しない）の更新忘れ排除"
    - "live-DB snapshot 再生 → Phase 11 入力 snapshot 整合（CR-02 修正で merge 値が変わる可能性の証明・値が変わらない場合は silent NaN merge が存在しなかったことの証明）"
---

# Phase 10 Gap-Closure Plan 08: REVIEW Findings の fail-loud 化・堅牢化・live-DB 再生成検証

<objective>
10-REVIEW.md の 4 Critical（CR-01〜04）+ 6 Warning（WR-01〜06）のうち・ユーザー決定で採用された全対象を fail-loud 化・堅牢化し・live-DB で snapshot 再生成検証を実施する。Info（IN-01〜05）は対象外（deferred 明記）。W-3 性能聖域（絶対 5.0s 閾値）と D-15 segment_eval は既に別途対応済みのため本 plan では扱わない。

核心は core value「リーク防止」の鏡像である「silent fallback 禁止」の機械保証。CR-01/CR-02 の本体は「silent data loss を検知せず後段に進む」ことであり・CYCLE-2 HIGH-C2-1 値レベル PIT 保証の根幹（全 source race の opponent ability を full-pipeline 再計算）を silent に破る経路を封印する。CR-04 は §11.2 聖域（test 窓 rank すり替え禁止・W-2 候補 score diagnostic）の履行証跡欠損を防ぐ。WR-03/WR-04 は live-DB でしか発覚しない Parquet 直列化 bug・本番規模 OOM リスクの検出力を向上させる（W-3 聖域の緩和でなく検出力向上・memory perf-threshold-sanctuary-rationale-rebase）。

Phase 11（MODEL-01 レース内相対確率モデル）は Phase 10 snapshot に依存するため・CR-02 修正で merge 値が変わる可能性（silent NaN → 実データ）を live-DB で証明し・値が変わる場合は SC#5 非劣化 gate を再実行して Phase 11 入力 snapshot の整合を担保する。

Purpose: 10-REVIEW.md の Critical/Warning を fail-loud 化し・core value「リーク防止」と再現性 §19.1 を更に機械保証する。Phase 11 入力の健全性を live-DB で証明する。
Output: 修正された 6 ソースファイル + 2 テストファイル + live-DB 再生成 snapshot + 新規 fail-loud/境界/性能テスト
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-opponent-strength-race-relative-features/10-CONTEXT.md
@.planning/phases/10-opponent-strength-race-relative-features/10-RESEARCH.md
@.planning/phases/10-opponent-strength-race-relative-features/10-REVIEW.md
@.planning/phases/10-opponent-strength-race-relative-features/10-VERIFICATION.md
@CLAUDE.md

# 現状把握（修正対象コード・必読）
@src/features/field_strength.py
@src/features/race_relative.py
@src/features/builder.py
@src/features/snapshot.py
@scripts/run_phase10_evaluation.py
@tests/features/test_field_strength.py
@tests/audit/test_audit_field_strength.py

# 既存 plan（現状把握・10-08 はこれらを破壊しない）
@.planning/phases/10-opponent-strength-race-relative-features/10-01-PLAN.md
@.planning/phases/10-opponent-strength-race-relative-features/10-05-PLAN.md
@.planning/phases/10-opponent-strength-race-relative-features/10-06-PLAN.md
@.planning/phases/10-opponent-strength-race-relative-features/10-07-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/features 配下の silent fallback 封印（CR-01 field_strength fail-loud + CR-03 race_relative 境界防御 + CR-02 builder dtype 正規化/JOIN率 fail-loud）</name>
  <files>src/features/field_strength.py, src/features/race_relative.py, src/features/builder.py, tests/features/test_field_strength.py, tests/features/test_race_relative.py</files>
  <read_first>
    - CLAUDE.md（leakage-prevention 聖域・silent fallback 禁止は core value の鏡像）
    - .planning/phases/10-opponent-strength-race-relative-features/10-REVIEW.md（CR-01 L49-93・CR-02 L95-140・CR-03 L142-175・WR-01 L221-241）
    - src/features/field_strength.py L240-290（_compute_source_asof_opponent_speed_figures の batch loop と空 DataFrame 返却経路）・L289-385（_opponent_ability_latest_mean5・obs_id parse の rsplit）・L387-460（compute_field_strength_profile の len(raw_history)==0 fail-loud）
    - src/features/race_relative.py L140-192（_gap_to_top_within_race / _gap_to_3rd_within_race・rank==3 不在時の NaN Series）
    - src/features/builder.py L530-590（Step5c merge・race_date dtype・_fs_profile_cols ガード）・L589-610（Step5 rolling 呼出）
    - 10-01-PLAN.md（現状の CYCLE-2 HIGH-C2-1 設計・fail-loud の既存 idiom・WR-01 fail-loud 踏襲）
    - tests/features/test_field_strength.py L783-810（PROD_PEAK_MEM_BUDGET_GB / PROD_SMOKE_N_RACES 定数）・_fs_history_row ヘルパの race_nkey 形式
  </read_first>
  <behavior>
    - Test 1 (CR-01 fail-loud): _compute_source_asof_opponent_speed_figures に source_available_at_by_race が非空の raw_history を渡し・starters が全 source race に存在しない（kakuteijyuni > 0 の行が source race に無い）場合・RuntimeError が raise される（空 DataFrame 返却でない）
    - Test 2 (CR-01 compute_field_strength_profile 側 fail-loud): compute_field_strength_profile が・starters 存在 source race 数 vs source_available_at_by_race 件数の不整合を検知して RuntimeError を raise する経路が存在する
    - Test 3 (CR-01 空 バッチ warning): 一部バッチが空（synth_obs 空・compute_speed_figure_for_history が空フレーム返却）の場合・logger.warning で空バッチ数が記録され・non_empty_batches のみが concat される（silent な source race 欠落が可視化される）
    - Test 4 (CR-03 境界防御): _gap_to_3rd_within_race に len < 3 の Series を渡した場合・index=mean5.index を持つ NaN Series が返る（早返しが存在し・pandas バージョン依存の transform 境界を明示防御）
    - Test 5 (CR-02 dtype 正規化): Step5c merge 前に history['race_date'] と _profile_merge['race_date'] が pd.to_datetime で正規化されることを・merge 実行時の dtype が一致することで検証（合成データで datetime.date object と datetime64 が混在するケースで NaN merge が起きない）
    - Test 6 (CR-02 JOIN 率 fail-loud): Step5c merge 後・starter_mask 上の field_strength_mean の notna 率が 0.5 未満の場合（合成データで意図的に merge 失敗を起こす）・RuntimeError が raise される
    - Test 7 (WR-01 docstring + helper 整合): _fs_history_row ヘルパが本番 make_race_nkey 形式（_ を含まない・YYYYJJJKKNN 形式）の race_nkey を生成する・_opponent_ability_latest_mean5 の docstring に obs_id parse の安全性（make_race_nkey は _ を含まない・rsplit('_', n=1) は kettonum 境界として安全）が明記される
  </behavior>
  <action>
    【CR-01 field_strength.py silent fallback 封印・CYCLE-2 HIGH-C2-1 の鏡像】
    src/features/field_strength.py の _compute_source_asof_opponent_speed_figures（L262-286 付近・batch loop 後）を修正する。具体的目標状態のみ記述（実装は executor に委ねる）:
    - batch loop 後・`batches` が空（`if not batches:`）の分岐で・`source_available_at_by_race` が非空（`len(source_available_at_by_race) > 0`）の場合は RuntimeError を raise する。エラーメッセージに「source race N 件中・全 source race が starter 不存在・silent data loss を検知（CYCLE-2 HIGH-C2-1）」を含める。`source_available_at_by_race` が空の場合のみ空 DataFrame を返す（正当な空入力）。
    - 各バッチの空 synth_obs を追跡し・`batches` に空 DataFrame が含まれる場合は `logger.warning` で空バッチ数 / 全バッチ数を記録する。`pd.concat` は non_empty_batches（`len(b) > 0`）のみで行い・non_empty_batches が空の場合のみ空 DataFrame を返す。
    - 併せて compute_field_strength_profile（L387-460 付近）側に・starters が存在する source race 数（starter_mask & source_race_keys のユニーク race_nkey 数）vs `source_available_at_by_race` の件数を比較する fail-loud 検査を追加する。差分がある場合は RuntimeError（silent data quality 低下の検知）。

    【CR-03 race_relative.py 境界防御】
    src/features/race_relative.py の _gap_to_3rd_within_race（L159-192）に・関数冒頭で `if len(mean5) < 3:` の早返しを追加する（`pd.Series([np.nan] * len(mean5), index=mean5.index)` を返す）。race size 0/1/2 で rank==3 が存在し得ないことを明示的に処理し・pandas バージョン依存の transform 境界を防御する。既存の rank==3 不在時の早返し（L188-190）はそのまま残す。docstring に「race size < 3 は rank==3 が存在し得ない（competition ranking でも）・早返しで NaN series を返す」を追記する。

    【CR-02 builder.py dtype 正規化 + JOIN 率 fail-loud】
    src/features/builder.py の Step5c（L556-577 付近・profile merge）を修正する:
    - merge 実行直前（`history = history.merge(...)` の前）に・`history['race_date'] = pd.to_datetime(history['race_date'])` と `_profile_merge['race_date'] = pd.to_datetime(_profile_merge['race_date'])` の双方向 dtype 正規化を挿入する。これにより history 側（nr.race_date AS race_date・psycopg3 で datetime.date object の可能性）と profile 側（raw_history.assign(available_at=...)・同じ object のはずだが Step5b 後の history は実装依存）の dtype 不一致による silent NaN merge を構造的排除する。
    - merge 後・`field_strength_mean` 列が history に存在し `len(history) > 0` の場合に・`starter_mask = history['kakuteijyuni'].fillna(0) > 0` 上の `field_strength_mean` の notna 率を計算し・`joined_ratio < 0.5` の場合に RuntimeError を raise する（FEAT-02 21 feature 全行 sentinel 化の silent data loss 検知・エラーメッセージに「silent data loss・dtype mismatch の疑い・core value 違反」を含める）。既存の suffix 衝突解決（L578-582）の後に挿入する。

    【WR-01 docstring + test helper 整合】
    - src/features/field_strength.py の _opponent_ability_latest_mean5（L336-344 付近・obs_id parse）の docstring に・make_race_nkey（builder.py・YYYYJJJKKNN 形式・_ を含まない）が本番の race_nkey 形式であること・`rsplit('_', n=1)` が `<source_race_nkey>` と `<opponent_kettonum>` の境界として安全であること（race_nkey に _ が含まれない前提）を明記する。
    - tests/features/test_field_strength.py の _fs_history_row ヘルパを・本番 make_race_nkey 形式（_ を含まない・例: "2023061001" のような YYYYJJJKKNN 連結）に整合させる。テスト識別性を保つため必要なら race_date を別列で持たせる（_ 区切りでなく）。既存のテスト（adversarial lookahead・value-invariance・same-day opponent 混入検出等）が全て GREEN を維持すること。

    【聖域回帰確認】
    修正後・既存の adversarial テスト（test_opponent_vs_source_pit_strict_less_adversarial / test_cycle2_high_c2_1_value_invariance / test_cycle2_high_c2_1_value_invariance_across_targets / test_obs_id_expanded_reuse_forbidden_and_available_at_derivation）が GREEN を維持すること。fail-loud の新テスト（Test 1-6）は合成データで意図的に silent fallback / NaN merge を起こして RuntimeError を検証する。
  </action>
  <verify>
    <automated>uv run pytest tests/features/test_field_strength.py tests/features/test_race_relative.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/features/field_strength.py の _compute_source_asof_opponent_speed_figures に・`source_available_at_by_race` 非空かつ batches 空で RuntimeError を raise する経路が存在する（grep で RuntimeError と CYCLE-2 HIGH-C2-1 を含む raise 文が確認できる）
    - src/features/field_strength.py の compute_field_strength_profile に・starters 存在 source race 数 vs source_available_at_by_race 件数の fail-loud 検査が存在する
    - src/features/field_strength.py の batch loop に・空バッチ追跡の logger.warning と non_empty_batches のみを concat する経路が存在する
    - src/features/race_relative.py の _gap_to_3rd_within_race に `if len(mean5) < 3:` の早返しが存在する
    - src/features/builder.py の Step5c merge 直前に pd.to_datetime による race_date 双方向正規化が存在する
    - src/features/builder.py の Step5c merge 後に field_strength_mean の notna 率 < 0.5 で RuntimeError を raise する fail-loud 検査が存在する
    - src/features/field_strength.py の _opponent_ability_latest_mean5 docstring に make_race_nkey 形式（_ 無し）と rsplit の安全性が明記される
    - tests/features/test_field_strength.py の _fs_history_row が _ を含まない race_nkey 形式を生成する
    - tests/features/test_field_strength.py + test_race_relative.py が全て GREEN（fail-loud の新テスト Test 1-6・境界テスト・既存 adversarial テスト含む）
    - tests/audit/test_audit_field_strength.py（SAFE-01 odds/ninki proxy 0 件・AST audit）が GREEN を維持する（聖域回帰なし）
  </acceptance_criteria>
  <done>CR-01/CR-02/CR-03/WR-01 の実装が完了し・silent fallback 経路が RuntimeError で封印され・race_relative 境界が早返しで防御され・Step5c の dtype 正規化と JOIN 率 fail-loud が機能する。fail-loud の新テスト（Test 1-6）と既存 adversarial テストが全て GREEN。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: snapshot.py 動的導出（WR-03）+ run_phase10_evaluation.py W-2 RuntimeError 格上げ・pool statement_timeout（CR-04/WR-02）+ テスト系（WR-04/WR-05/WR-06）</name>
  <files>src/features/snapshot.py, scripts/run_phase10_evaluation.py, tests/features/test_field_strength.py, tests/audit/test_audit_field_strength.py</files>
  <read_first>
    - CLAUDE.md（§19.1 再現性・§11.2 聖域・live-DB検証必須）
    - .planning/phases/10-opponent-strength-race-relative-features/10-REVIEW.md（CR-04 L177-217・WR-02 L243-273・WR-03 L275-299・WR-04 L301-323・WR-05 L325-354・WR-06 L356-389）
    - src/features/snapshot.py L75-137（_FEAT03_NUMERIC_COLUMNS frozenset・_coerce_rolling_columns_for_parquet・_is_categorical_rolling_col）
    - src/features/race_relative.py L59-81（_AXIS_TO_RANK_SUFFIX・ADJUSTED_RANK_COEF_CANDIDATES・ADJUSTED_RANK_COEF_CANONICAL）
    - scripts/run_phase10_evaluation.py L240-323（_compute_w2_diagnostics・必須列 missing で WARNING skip・race_date mask）・L329-410（main・make_pool・readonly_cursor・SET statement_timeout='30s'）
    - src/db/connection.py L21-90（make_pool・readonly_cursor・psycopg_pool ConnectionPool）
    - tests/features/test_field_strength.py L783-810（PROD_PEAK_MEM_BUDGET_GB / PROD_SMOKE_N_RACES）・L1012-1080（test_production_scale_smoke_no_h_squared_blowup・tracemalloc + getrusage・macOS/Linux ru_maxrss 単位分岐）
    - tests/audit/test_audit_field_strength.py L210-270（test_feature_columns_contains_phase10_features_no_proxy・snapshot_path.exists 分岐・else 経路 pytest.raises(FileNotFoundError)）
    - memory: subagent-db-query-statement-timeout（pool 再利用で statement_timeout 残らないリスク）
  </read_first>
  <behavior>
    - Test 1 (WR-03 動的導出): src/features/snapshot.py の _FEAT03_NUMERIC_COLUMNS が race_relative.py の _AXIS_TO_RANK_SUFFIX から動的導出される（frozenset のハードコード要素が import された定数から構築される）。race_relative に新たな axis を追加すると snapshot.py 側も自動追従する（テストで race_relative の定数を拡張した想定で snapshot 側の frozen set が拡張されることを検証・monkeypatch または定数参照のテスト）
    - Test 2 (CR-04 W-2 RuntimeError): scripts/run_phase10_evaluation.py の _compute_w2_diagnostics に・必須列（rolling_speed_figure_mean_5 / rolling_field_strength_mean_mean_5 等）が欠損した phase10_frame を渡した場合・RuntimeError が raise される（WARNING skip でない・W-2 acceptance criteria 未達・§11.2 聖域履行証跡欠損を明示）
    - Test 3 (CR-04 W-2 assert): _compute_w2_diagnostics 呼出し前に phase10_frame が rolling 系 feature を含むことが assert される（_compute_w2_diagnostics 内・または caller 側）
    - Test 4 (WR-02 pool statement_timeout): run_phase10_evaluation.py の main が・make_pool 後に psycopg_pool の configure callback で SET statement_timeout='30s' を pool 全体に適用する（cursor 単位の SET でない・connection checkout 毎に SET が走る）。テストで pool の configure callback が呼ばれることを検証（mock pool・または configure メソッド呼出しの検証）
    - Test 5 (WR-04 1000 race smoke): KEIBA_RUN_PERF_TESTS=1 で test_production_scale_smoke_large（PROD_SMOKE_N_RACES_LARGE=1000）が実行され・peak_rss ≤ PROD_PEAK_MEM_BUDGET_GB(8.0) で PASS する。W-3 聖域（per-race 予算 ≤0.30s/race + 準二次スケーリングガード・cProfile 上位3位に Python ループ無し）を破らない。default skip（KEIBA_RUN_PERF_TESTS 未設定時は skip）
    - Test 6 (WR-05 rss 主指標): test_production_scale_smoke_no_h_squared_blowup の peak_gb が rss_gb を主指標とし（`peak_gb = rss_gb`・tracemalloc は `peak_py_gb` 参考記録）・docstring に「tracemalloc は Python ヒープのみ・numpy/pandas native buffer 含まず過小評価・rss_gb を主指標にする」が明記される
    - Test 7 (WR-06 skip 化): tests/audit/test_audit_field_strength.py::test_feature_columns_contains_phase10_features_no_proxy の else 経路が pytest.raises(FileNotFoundError) でなく pytest.skip(...) に変更される。skip メッセージに「Phase 10 snapshot 未生成・実 snapshot 検証は test_data.py::test_phase10_* に一本化」を含む
  </behavior>
  <action>
    【WR-03 snapshot.py 動的導出】
    src/features/snapshot.py L84-91 の _FEAT03_NUMERIC_COLUMNS を・race_relative.py の _AXIS_TO_RANK_SUFFIX から動的導出する。目標状態:
    - snapshot.py 先頭の import に `from src.features.race_relative import _AXIS_TO_RANK_SUFFIX` を追加する（循環 import に注意・race_relative.py は snapshot.py を import しないため安全）。
    - _FEAT03_NUMERIC_COLUMNS を `frozenset({f"speed_index_rank_{suffix}" for suffix in _AXIS_TO_RANK_SUFFIX.values()}) | {"gap_to_top", "gap_to_3rd", "field_strength_adjusted_rank"}` で構築する。frozenset であることは維持（_coerce_rolling_columns_for_parquet の `col in _FEAT03_NUMERIC_COLUMNS` lookup 不変）。
    - docstring に「race_relative の _AXIS_TO_RANK_SUFFIX から動的導出・新 feature 追加時の snapshot.py 更新忘れを排除（WR-03）」を明記する。

    【CR-04 run_phase10_evaluation.py W-2 RuntimeError 格上げ・§11.2 聖域強化】
    scripts/run_phase10_evaluation.py の _compute_w2_diagnostics（L262-280 付近）を修正:
    - 必須列 `required = ("race_nkey", "rolling_speed_figure_mean_5", "rolling_field_strength_mean_mean_5")` の missing 分岐で・`logger.warning` + `return {"status": "skipped", ...}` を・`raise RuntimeError(...)` に格上げする。エラーメッセージに「W-2 必須列が phase10_frame に無い・FEAT-02/03 未伝播・Phase 10 acceptance_criteria W-2 が履行不能（§11.2 聖域）」を含める。
    - _compute_w2_diagnostics の冒頭（import 失敗分岐以外）に・phase10_frame が rolling 系 feature を含むことを assert する検査を追加する（例: `assert "rolling_speed_figure_mean_5" in phase10_frame.columns, "..."`・より早期の fail-loud）。既存の import 失敗分岐（L255-259）は WARN skip のままでよい（import error は別問題・W-2 本体でない）。
    - 既存の race_date 列欠損分岐（L262-264）も RuntimeError 格上げを検討するが・これは mask 構築不能であって W-2 証跡欠損と同質のため RuntimeError に格上げする（§11.2 聖域）。

    【WR-02 pool 全体 statement_timeout】
    scripts/run_phase10_evaluation.py の main（L360-366 付近）を修正:
    - `readonly_pool = make_pool(settings, role="readonly")` の後に・psycopg_pool の `configure` callback で SET statement_timeout='30s' を pool 全体に適用する。psycopg_pool.ConnectionPool には configure 引数（新規 connection checkout 毎に呼ばれる callback）がある。`readonly_pool.configure(lambda conn: (conn.cursor().execute("SET statement_timeout = '30s'"), conn.commit()))` の形式・または `readonly_pool.open()` 待ちの構造に応じて configure を設定する（psycopg_pool の API を確認のこと）。既存の cursor 内 `cur.execute("SET statement_timeout = '30s'")`（L365）は残してもよいが・pool 全体適用が主・cursor 内は二重防衛。
    - memory subagent-db-query-statement-timeout の指摘（cursor 単位の SET は pool 再利用で残らない）に対応する。
    - 併せて orchestrator.train_and_predict 呼出し（main 内の別経路）でも statement_timeout が効いていることを保証する（pool 経由で connection を取得するため・configure callback が適用される）。

    【WR-04 1000 race smoke 拡張（W-3 聖域遵守・緩和でなく検出力向上）】
    tests/features/test_field_strength.py に新規テスト test_production_scale_smoke_large を追加する:
    - 定数 `PROD_SMOKE_N_RACES_LARGE: int = 1000` を追加（PROD_SMOKE_N_RACES=200 の別系統・既存テストは温存）。
    - test_production_scale_smoke_large は test_production_scale_smoke_no_h_squared_blowup と同構造（合成 history 1000 race で compute_field_strength_profile を実行）し・peak_rss ≤ PROD_PEAK_MEM_BUDGET_GB(8.0) で PASS する。
    - KEIBA_RUN_PERF_TESTS=1 でのみ実行（`pytest.mark.skipif(not KEIBA_RUN_PERF_TESTS, ...)`）。
    - W-3 聖域（per-race 予算 ≤0.30s/race + 準二次スケーリングガード・cProfile 上位3位に Python ループ無し・memory perf-threshold-sanctuary-rationale-rebase）を破らない。本テストは「閾値緩和」でなく「検出力向上」（200 race では発覚しない OOM リスクを 1000 race で検出）であることを docstring に明記する。W-3 縮小版 5.0s 閾値（PLAN 01 設計と構造的矛盾・default skip・10-07 で option-a 根拠再設定済み）とは別物・本テストは memory smoke であって時間閾値でない。

    【WR-05 rss 主指標化】
    tests/features/test_field_strength.py::test_production_scale_smoke_no_h_squared_blowup（L1041-1069 付近）を修正:
    - `peak_gb = max(peak_py_gb, rss_gb)` を `peak_gb = rss_gb` に変更する（rss_gb を主指標・tracemalloc は Python ヒープのみで numpy/pandas native buffer を含まず過小評価）。
    - logger.info に `peak_rss=%.2fGB (py_heap=%.2fGB・参考)` の形式を維持（tracemalloc は参考記録）。
    - docstring に「tracemalloc は Python ヒープのみ・numpy/pandas native buffer 含まず過小評価・主指標は rss_gb(getrusage)」を明記する。macOS（ru_maxrss は bytes）/ Linux（ru_maxrss は KiB）の単位分岐は既存のまま。

    【WR-06 audit テスト skip 化】
    tests/audit/test_audit_field_strength.py::test_feature_columns_contains_phase10_features_no_proxy の else 経路（L266-269）を `pytest.raises(FileNotFoundError)` から `pytest.skip(...)` に変更する。skip メッセージ: `f"Phase 10 snapshot ({_PHASE10_SNAPSHOT_ID}) 未生成・実 snapshot 検証は test_data.py::test_phase10_* に一本化"`。実 snapshot 検証（27 feature 含有・forbidden prefix 0 件）は test_data.py::test_phase10_derive_feature_columns_new_and_baseline_regression に一本化される（本テストの if 経路は実 snapshot 環境でのみ走り・CI/nil snapshot 環境では skip する・silent fallback mask を排除）。

    【聖域回帰確認】
    修正後・既存の test_audit_field_strength.py（SC#4 SAFE-01・5段階鋳型・AST odds/ninki proxy 0 件）が GREEN を維持すること。W-2 diagnostic は rolling 系 feature が伝播済みの phase10_frame で status=ok を返す。
  </action>
  <verify>
    <automated>uv run pytest tests/features/test_snapshot_repro.py tests/features/test_field_strength.py tests/audit/test_audit_field_strength.py -x -q &amp;&amp; uv run pytest tests/features/test_field_strength.py -k "w2 or statement_timeout or snapshot or production_scale" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/features/snapshot.py の _FEAT03_NUMERIC_COLUMNS が race_relative._AXIS_TO_RANK_SUFFIX から動的導出される（grep で `from src.features.race_relative import _AXIS_TO_RANK_SUFFIX` と `_AXIS_TO_RANK_SUFFIX.values()` を含む構築式が確認できる・frozenset リテラルのハードコードでない）
    - scripts/run_phase10_evaluation.py の _compute_w2_diagnostics が必須列 missing で RuntimeError を raise する（logger.warning + return skip でない）
    - scripts/run_phase10_evaluation.py の _compute_w2_diagnostics に phase10_frame の rolling 系 feature 存在 assert が存在する
    - scripts/run_phase10_evaluation.py の main が make_pool 後に psycopg_pool の configure callback で SET statement_timeout='30s' を pool 全体に適用する（cursor 単位でない・grep で readonly_pool.configure または同等の pool 全体適用が確認できる）
    - tests/features/test_field_strength.py に PROD_SMOKE_N_RACES_LARGE=1000 と test_production_scale_smoke_large が存在し・KEIBA_RUN_PERF_TESTS=1 でのみ実行される（default skip）・W-3 聖域（per-race 予算 ≤0.30s/race・cProfile 上位3位 Python ループ無し）を破らないことが docstring に明記される
    - tests/features/test_field_strength.py::test_production_scale_smoke_no_h_squared_blowup の peak_gb が rss_gb を主指標とし（max(peak_py_gb, rss_gb) でない）・docstring に tracemalloc 過小評価が明記される
    - tests/audit/test_audit_field_strength.py::test_feature_columns_contains_phase10_features_no_proxy の else 経路が pytest.skip になり（pytest.raises(FileNotFoundError) でない）・skip メッセージに test_data.py 一本化が明記される
    - 全テスト（tests/features/test_snapshot_repro.py + test_field_strength.py + test_audit_field_strength.py）が GREEN。W-2 diagnostic の新テスト（Test 2/3）が GREEN。
    - SAFE-01 聖域（AST odds/ninki proxy 0 件・5段階鋳型）が GREEN を維持する（聖域回帰なし）
  </acceptance_criteria>
  <done>WR-03 動的導出・CR-04 W-2 RuntimeError 格上げ・WR-02 pool statement_timeout・WR-04 1000 race smoke・WR-05 rss 主指標化・WR-06 skip 化が完了し・全テストが GREEN。§11.2 聖域（W-2 証跡）が強化され・silent fallback mask 経路が排除される。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: live-DB で snapshot 再生成検証（CR-02/WR-03 修正で値が変わる可能性の証明・Phase 11 入力整合・SC#3 byte-reproducibility・§19.1 聖域）</name>
  <files>snapshots/feature_matrix_20260626-1a-opponentstrength-v1.parquet, snapshots/feature_matrix_20260626-1a-opponentstrength-v1.manifest.yaml, reports/10-gap-closure/regen-verification.json</files>
  <read_first>
    - CLAUDE.md（§19.1 再現性・live-DB検証必須・memory feature-snapshot-regen-required）
    - .planning/phases/10-opponent-strength-race-relative-features/10-05-PLAN.md（snapshot 生成手順・feature_count=106・SHA256・§12.4 metadata）
    - .planning/phases/10-opponent-strength-race-relative-features/10-06-PLAN.md（SC#5 非劣化 gate・baseline snapshot 20260620-1a-postreview-v2・D-16 許容幅）
    - src/features/builder.py（build_feature_matrix CLI・Step5c 修正後）・src/features/snapshot.py（write_snapshot・_coerce_rolling_columns_for_parquet 修正後）
    - .planning/STATE.md（Phase 11 は Phase 10 snapshot に依存・MEMORY.md feature-snapshot-regen-required / phase7-ui-live-db-bugs）
    - .planning/phases/10-opponent-strength-race-relative-features/10-VERIFICATION.md（SC#3 byte-reproducibility・SC#5 gate_pass=True）
  </read_first>
  <what-built>
    Task 1/2 で CR-02（builder Step5c dtype 正規化 + JOIN 率 fail-loud）と WR-03（snapshot.py 動的導出）を修正した。CR-02 の dtype 正規化は・歴史的に silent NaN merge が存在した場合に merge 値を変える可能性がある（silent NaN → 実データ）。WR-03 は snapshot coerce 対象列の動的導出であり・直列化結果は不変のはずだが・live-DB で Parquet schema が変わらないことを証明する必要がある（memory feature-snapshot-regen-required: 実データでしか発覚しない Parquet 直列化 bug あり）。
    この task は live-DB（KEIBA_SKIP_DB_TESTS unset・readonly role）で feature snapshot 20260626-1a-opponentstrength-v1 を再生成し・(a) SHA256 が修正前と一致すること（silent NaN merge が存在しなかったことの証明）・または (b) SHA256 が変わる場合は SC#5 非劣化 gate を再実行して D-16 許容幅内であることを確認し・Phase 11 入力 snapshot の整合を担保する。
  </what-built>
  <action>
    checkpoint:human-verify（blocking）。Task 1/2 で修正した builder.py・snapshot.py を使って live-DB で snapshot を再生成し・修正前後の SHA256 を比較する。SHA256 一致なら silent NaN merge が存在しなかったことの証明・SHA256 変更なら SC#5 非劣化 gate を再実行して feature ノイズ化でないことを確認する。Phase 11 入力 snapshot 整合を担保する。executor 側で実行可能な CLI（run_feature_build.py / run_phase10_evaluation.py / pytest）は自動化し・結果記録と SHA256 変更時の判断は人間が確認する。詳細手順は下記 how-to-verify の 1-9 に従う。
  </action>
  <how-to-verify>
    1. live-DB 接続確認（KEIBA_SKIP_DB_TESTS unset・readonly role・psql で normalized.n_uma_race が見える）:
       `psql "$KEIBA_READONLY_DSN" -c "SELECT COUNT(*) FROM normalized.n_uma_race WHERE race_date >= '2015-01-01';"`

    2. **修正前 SHA256 記憶**（既存 snapshot・10-VERIFICATION.md 記載の sha256=c25ae556...）:
       `shasum -a 256 snapshots/feature_matrix_20260626-1a-opponentstrength-v1.parquet`
       → 既存 sha256 を記録（c25ae556... の先頭）。

    3. **snapshot 再生成**（Task 1/2 修正後の builder + snapshot を使う）:
       subagent 経由の重いDBクエリは statement_timeout 必須（memory subagent-db-query-statement-timeout）。
       `uv run python scripts/run_feature_build.py --snapshot-id 20260626-1a-opponentstrength-v1 --output snapshots/feature_matrix_20260626-1a-opponentstrength-v1.parquet`
       （または 10-05-PLAN.md に記載の実装 CLI 引数・build_feature_matrix の正規呼出）

    4. **SHA256 比較**（修正前後で一致するか）:
       `shasum -a 256 snapshots/feature_matrix_20260626-1a-opponentstrength-v1.parquet`
       → 修正前 SHA256 と一致するか確認。

    5. **SHA256 一致の場合**（silent NaN merge が存在しなかったことの証明）:
       - reports/10-gap-closure/regen-verification.json に `{"sha256_unchanged": true, "silent_nan_merge_absent": true}` を記録。
       - tests/features/test_snapshot_repro.py（SC#3 byte-reproducibility・sha256・§12.4 metadata）が GREEN。
       - registry↔Parquet parity（feature_count=106・schema_version 0.6.0）が GREEN:
         `uv run pytest tests/features/test_snapshot_repro.py tests/features/test_allowlist.py -x -q`
       - Phase 11 入力 snapshot 整合確認: feature_count=106・27 Phase 10 feature 含有（pyarrow で schema 確認）。

    6. **SHA256 が変わる場合**（silent NaN merge が存在したことの発覚・SC#5 再実行が必要）:
       - reports/10-gap-closure/regen-verification.json に `{"sha256_changed": true, "old_sha256": "...", "new_sha256": "...", "silent_nan_merge_present": true}` を記録。
       - **SC#5 非劣化 gate 再実行**（Phase 11 入力 snapshot が変わったため・D-16 許容幅内であることを再確認）:
         `uv run python scripts/run_phase10_evaluation.py --baseline-snapshot-id 20260620-1a-postreview-v2 --phase10-snapshot-id 20260626-1a-opponentstrength-v1 --bt-split BT-1 --odds-snapshot-policy 30min_before --out-dir reports`
         → reports/10-evaluation/10-evaluation.json の gate_pass が True のまま（3/3 D-16 許容幅内）であることを確認。
       - gate_pass=False の場合は・Phase 11 開始前にユーザー判断（feature ノイズ化の可能性・PLAN 再検討）。

    7. **§12.4 metadata 確認**:
       `uv run python -c "import yaml; m=yaml.safe_load(open('snapshots/feature_matrix_20260626-1a-opponentstrength-v1.manifest.yaml')); print(m['feature_count'], m['schema_version'], m['feature_cutoff_rule'], m['dataset_version'])"`
       → feature_count=106・schema_version=0.6.0・feature_cutoff_rule='race_date - 1 day'・dataset_version v1.0.0。

    8. **SAFE-01 聖域回帰確認**（再生成 snapshot で odds/ninki proxy 0 件）:
       `uv run pytest tests/audit/test_audit_field_strength.py -x -q`
       → SC#4 SAFE-01（AST odds/ninki proxy 0 件・5段階鋳型）が GREEN。

    9. **Phase 11 入力整合サマリ**: 再生成 snapshot が Phase 11 MODEL-01 の入力として使用可能であること（feature_count=106・27 Phase 10 feature 含有・SAFE-01 odds/ninki proxy 0 件・byte-reproducible）を reports/10-gap-closure/regen-verification.json に記録。
  </how-to-verify>
  <verify>
    <automated>uv run pytest tests/features/test_snapshot_repro.py tests/features/test_allowlist.py tests/audit/test_audit_field_strength.py -x -q</automated>
    <human-check>live-DB で snapshot 再生成後・SHA256 修正前後比較（一致 or 変更時 SC#5 gate_pass=True）・§12.4 metadata（feature_count=106・schema_version=0.6.0）・registry↔Parquet parity・SAFE-01 odds/ninki proxy 0 件・Phase 11 入力整合を確認</human-check>
  </verify>
  <resume-signal>Type "approved"（SHA256 一致または SHA256 変更時に SC#5 gate_pass=True の場合・Phase 11 入力 snapshot 整合確認の場合）または問題を記述（SHA256 不一致 + gate_pass=False・feature ノイズ化疑い・ArrowTypeError・feature count 不一致・誤 metadata 等）</resume-signal>
  <done>live-DB で snapshot 20260626-1a-opponentstrength-v1 が再生成され・(a) SHA256 が修正前と一致（silent NaN merge が存在しなかったことの証明）または (b) SHA256 変更時に SC#5 gate_pass=True（feature ノイズ化でないことの証明）のいずれかが確認される。SC#3 byte-reproducibility・registry↔Parquet parity・§12.4 metadata・SAFE-01 odds/ninki proxy 0 件が GREEN。Phase 11 入力 snapshot 整合が reports/10-gap-closure/regen-verification.json に記録される。</done>
</task>

</tasks>

## Artifacts this phase produces

### 変更する関数・定数・テスト（10-08 完了後）
- **src/features/field_strength.py**:
  - `_compute_source_asof_opponent_speed_figures`: batch loop 後の fail-loud（source_available_at_by_race 非空かつ batches 空 → RuntimeError）・空バッチ warning ・ non_empty_batches のみ concat
  - `compute_field_strength_profile`: starters 存在 source race 数 vs source_available_at_by_race 件数の fail-loud 検査を追加
  - `_opponent_ability_latest_mean5` docstring: make_race_nkey 形式（_ 無し）と rsplit の安全性を明記
- **src/features/race_relative.py**:
  - `_gap_to_3rd_within_race`: `if len(mean5) < 3:` の早返し（NaN series・index=mean5.index）を追加・docstring に race size < 3 仕様を追記
- **src/features/builder.py**:
  - Step5c merge 直前: `history['race_date'] = pd.to_datetime(...)` と `_profile_merge['race_date'] = pd.to_datetime(...)` の双方向 dtype 正規化
  - Step5c merge 後: `field_strength_mean` の notna 率 < 0.5 で RuntimeError（starter_mask 上・silent sentinel 化検知）
- **src/features/snapshot.py**:
  - `_FEAT03_NUMERIC_COLUMNS`: race_relative._AXIS_TO_RANK_SUFFIX から動的導出（frozenset・import 追加）
- **scripts/run_phase10_evaluation.py**:
  - `_compute_w2_diagnostics`: 必須列 missing で RuntimeError（WARNING skip でない）・race_date 列欠損も RuntimeError・phase10_frame rolling 系 feature 存在 assert
  - `main`: make_pool 後に psycopg_pool configure callback で SET statement_timeout='30s' を pool 全体に適用
- **tests/features/test_field_strength.py**:
  - `PROD_SMOKE_N_RACES_LARGE=1000` 定数追加
  - `test_production_scale_smoke_large` 新規（KEIBA_RUN_PERF_TESTS=1・W-3 聖域遵守 docstring）
  - `test_production_scale_smoke_no_h_squared_blowup`: peak_gb=rss_gb 主指標化・docstring に tracemalloc 過小評価明記
  - `_fs_history_row` ヘルパ: _ 無し race_nkey 形式に整合
  - fail-loud 新テスト（Test 1-6・CR-01/CR-02/CR-03/WR-01）
- **tests/audit/test_audit_field_strength.py**:
  - `test_feature_columns_contains_phase10_features_no_proxy`: else 経路 pytest.skip 化（pytest.raises でない）

### 再生成する snapshot
- **snapshots/feature_matrix_20260626-1a-opponentstrength-v1.parquet**: live-DB 再生成（SHA256 一致または変更時 SC#5 再実行）
- **snapshots/feature_matrix_20260626-1a-opponentstrength-v1.manifest.yaml**: §12.4 metadata（feature_count=106・schema_version 0.6.0）
- **reports/10-gap-closure/regen-verification.json**: SHA256 比較結果・silent NaN merge 有無・Phase 11 入力整合サマリ

### 含めないもの（Info 除外・W-3/D-15 非対象）
- IN-01（observations 引数 docstring）・IN-02（score.std 冗長条件）・IN-03（関数内 import）・IN-04（_ROLLING_SYSTEMS 二重定義）・IN-05（W-2 test 窓暗黙）→ backlog/issue 化のみ（本 plan で扱わない・deferred）
- W-3 縮小版 5.0s 閾値（絶対）→ PLAN 01/07 で option-a 根拠再設定済み（per-race 予算 ≤0.30s/race + 準二次スケーリングガード・緩和でなく根拠再設定・10-VERIFICATION.md deferred L14-17・NOT a gap）
- D-15 segment_eval column-name mismatch → Phase 12 EVAL-01 に先送り済み（10-VERIFICATION.md deferred L21-23・参照用 only・gate 判定に使わず）

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live-DB → snapshot builder | readonly role で normalized 層 SELECT・feature snapshot 再生成（KEIBA_SKIP_DB_TESTS unset）|
| snapshot → model trainer | Phase 11 MODEL-01 入力・feature_count=106・FEATURE_COLUMNS allowlist（make_X_y 完全一致 assert）|
| builder Step5c → rolling | field_strength profile が history に merge される境界（CR-02 silent NaN merge リスク）|

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-08-01 | Tampering/Spoofing | field_strength._compute_source_asof_opponent_speed_figures（CR-01）| high | mitigate | silent empty DataFrame 返却を RuntimeError で fail-loud 化・CYCLE-2 HIGH-C2-1 値レベル PIT 保証の silent fallback 経路を封印（core value「リーク防止」の鏡像「silent fallback 禁止」）|
| T-10-08-02 | Tampering | builder.Step5c merge（CR-02）| high | mitigate | race_date dtype 双方向正規化 + field_strength_mean notna 率 < 0.5 で RuntimeError・FEAT-02 21 feature 全行 sentinel 化の silent data loss を検知（live-DB でしか発覚しない・memory feature-snapshot-regen-required）|
| T-10-08-03 | Denial of service | race_relative._gap_to_3rd_within_race（CR-03）| medium | mitigate | len < 3 の早返しで pandas バージョン依存の transform 境界を明示防御・index 不整合の NaN Series 伝播を防止|
| T-10-08-04 | Elevation of privilege | run_phase10_evaluation._compute_w2_diagnostics（CR-04）| high | mitigate | 必須列 missing で WARNING skip でなく RuntimeError に格上げ・§11.2 聖域（test 窓 rank すり替え禁止・W-2 候補 score diagnostic）履行証跡欠損を構造的ブロック|
| T-10-08-05 | Denial of service | run_phase10_evaluation readonly_pool（WR-02）| medium | mitigate | psycopg_pool configure callback で SET statement_timeout='30s' を pool 全体に適用・cursor 単位でなく connection checkout 毎に SET・orphan CPU 張り付き防止（memory subagent-db-query-statement-timeout）|
| T-10-08-06 | Tampering | snapshot._FEAT03_NUMERIC_COLUMNS（WR-03）| medium | mitigate | race_relative._AXIS_TO_RANK_SUFFIX から動的導出・新 feature 追加時の snapshot.py 更新忘れ（実データ Parquet 直列化 bug）を排除|
| T-10-08-07 | Denial of service | field_strength.compute_field_strength_profile（WR-04）| medium | mitigate | PROD_SMOKE_N_RACES_LARGE=1000 で本番規模 smoke を拡張・200 race では発覚しない OOM リスクを検出（W-3 聖域遵守・緩和でなく検出力向上）|
| T-10-08-08 | Information disclosure | test_field_strength.py tracemalloc（WR-05）| low | mitigate | rss_gb(getrusage) を主指標に・tracemalloc は参考記録・numpy/pandas native buffer 過小評価を docstring 明記で誤判定防止|
| T-10-08-09 | Information disclosure | test_audit_field_strength.py else 経路（WR-06）| low | mitigate | snapshot 未生成時の pytest.raises(FileNotFoundError) を pytest.skip に変更・silent fallback で mask しない・実 snapshot 検証は test_data.py に一本化|
| T-10-08-10 | Tampering | live-DB snapshot 再生成（Task 3）| high | mitigate | SHA256 修正前後比較で silent NaN merge の有無を証明・SHA256 変更時は SC#5 非劣化 gate 再実行で feature ノイズ化を鑑別・Phase 11 入力 snapshot 整合を担保|
| T-10-08-SC | Tampering | (新規パッケージインストール無し) | low | accept | 本 plan は既存依存関係のみで完結・Package Legitimacy Gate N/A（10-RESEARCH.md Package Legitimacy Audit 参照）|

</threat_model>

<verification>
## Phase 10 gap-closure 検証

### 自動検証（ユニットテスト・KEIBA_SKIP_DB_TESTS=1 で DB 不要）
- `uv run pytest tests/features/test_field_strength.py tests/features/test_race_relative.py tests/features/test_rolling.py tests/features/test_snapshot_repro.py tests/audit/test_audit_field_strength.py -x -q` → GREEN（fail-loud 新テスト・境界テスト・既存 adversarial テスト含む）
- `uv run pytest tests/model/test_data.py -x -q` → GREEN（FEATURE_COLUMNS 106 回帰・H1-b・Phase 10 gap-closure で回帰なし）
- `uv run ruff check src/features/field_strength.py src/features/race_relative.py src/features/builder.py src/features/snapshot.py scripts/run_phase10_evaluation.py tests/features/test_field_strength.py tests/audit/test_audit_field_strength.py` → GREEN（lint）

### 自動検証（性能・KEIBA_RUN_PERF_TESTS=1）
- `KEIBA_RUN_PERF_TESTS=1 uv run pytest tests/features/test_field_strength.py::test_production_scale_smoke_large -x -q` → GREEN（1000 race・peak_rss ≤ 8GB・W-3 聖域遵守）
- `KEIBA_RUN_PERF_TESTS=1 uv run pytest tests/features/test_field_strength.py::test_no_python_loop_hot_spot -x -q` → GREEN（cProfile 上位3位 Python ループ無し・W-3 核心・10-07 で実証済み・回帰監視）

### live-DB 検証（Task 3 checkpoint）
- snapshot 再生成: SHA256 一致（silent NaN merge 無し）または SHA256 変更 + SC#5 gate_pass=True（feature ノイズ化でない）
- SC#3 byte-reproducibility: `uv run pytest tests/features/test_snapshot_repro.py -x -q` GREEN（KEIBA_SKIP_DB_TESTS unset）
- registry↔Parquet parity: feature_count=106・schema_version 0.6.0
- SAFE-01 聖域: `uv run pytest tests/audit/test_audit_field_strength.py -x -q` GREEN（odds/ninki proxy 0 件・AST audit）

### 聖域回帰（全て GREEN を維持）
- SC#1 PIT-correct: adversarial lookahead テスト（test_opponent_vs_source_pit_strict_less_adversarial 等）GREEN
- SC#4 SAFE-01: AST odds/ninki proxy 0 件・5段階鋳型 GREEN
- §11.2 聖域: W-2 diagnostic status=ok（rolling 系 feature 伝播・test 窓 rank すり替え禁止）
- §19.1 再現性: SHA256 一致（silent NaN merge 無し）または SC#5 再実行で D-16 許容幅内
- W-3 性能聖域: cProfile 上位3位 Python ループ無し・per-race 予算 ≤0.30s/race・準二次スケーリングガード（緩和でなく根拠再設定・memory perf-threshold-sanctuary-rationale-rebase）
</verification>

<success_criteria>
## Measurable completion

1. CR-01/CR-02/CR-03/CR-04/WR-01〜06 の全対象 finding が実装され・fail-loud 化・境界防御・動的導出・テスト拡張が完了する
2. 全ユニットテスト（KEIBA_SKIP_DB_TESTS=1）が GREEN（fail-loud 新テスト・境界テスト・既存 adversarial テスト含む）
3. live-DB snapshot 再生成で SHA256 一致（silent NaN merge 無しの証明）または SHA256 変更 + SC#5 gate_pass=True（feature ノイズ化でないの証明）が確認される
4. Phase 11 入力 snapshot 整合が reports/10-gap-closure/regen-verification.json に記録される（feature_count=106・27 Phase 10 feature 含有・SAFE-01 odds/ninki proxy 0 件・byte-reproducible）
5. 聖域回帰なし: SC#1 PIT-correct / SC#4 SAFE-01 / §11.2 W-2 / §19.1 byte-reproducibility / W-3 性能 が全て GREEN を維持する
6. Info（IN-01〜05）と W-3 縮小版 5.0s 閾値と D-15 segment_eval が deferred として明記される（本 plan 対象外）
</success_criteria>

<output>
Create `.planning/phases/10-opponent-strength-race-relative-features/10-08-SUMMARY.md` when done.
要録: 各 finding（CR-01〜04/WR-01〜06）の実装内容・新テストリスト・live-DB 再生成結果（SHA256 一致 or 変更 + SC#5 gate_pass）・Phase 11 入力整合サマリ・聖域回帰確認結果・deferred（IN-01〜05/W-3/D-15）明記。
</output>
