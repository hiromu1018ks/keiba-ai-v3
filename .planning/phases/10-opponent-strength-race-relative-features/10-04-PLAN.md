---
phase: 10-opponent-strength-race-relative-features
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - src/features/builder.py
  - src/features/availability.py
  - src/config/feature_availability.yaml
  - tests/features/test_speed_figure_builder_integration.py
  - tests/features/test_allowlist.py
autonomous: true
requirements: [FEAT-02, FEAT-03, SAFE-01]
granularity: fine

must_haves:
  truths:
    # SC#3（registry↔Parquet parity）・builder 統合
    - "builder.build_feature_matrix が Step 5c（compute_field_strength_profile・PLAN 01）→ Step 5d（build_rolling_features で rolling_field_strength_* 21 feature・PLAN 02 拡張）→ Step 7（compute_race_relative_features・PLAN 03）→ Step 7b（中間列 drop）の順で実行する"
    - "Step 5（build_rolling_features）の呼出位置を Step 5c の後に移動する（field_strength profile が history に揃った後で rolling する・A6 構成変更）"
    - "Step 7b で中間値（field_strength_mean/median/top3_mean/top5_mean/max/sd/valid_count/coverage・rolling_ prefix 無し）を明示 drop し feature_matrix に出力しない（rolling_field_strength_* 21 feature と FEAT-03 6 feature のみ残す）"
    # registry parity
    - "feature_availability.yaml の schema_version が 0.5.0 → 0.6.0 に bump される"
    - "feature_availability.yaml に rolling_field_strength_* 21 feature と FEAT-03 6 feature（speed_index_rank_{mean5,best2_mean5,median5}・gap_to_top・gap_to_3rd・field_strength_adjusted_rank）が7項目スキーマで登録される"
    - "availability._ROLLING_SYSTEMS_FOR_RESERVED に 'field_strength' が追加される・ただし rolling_field_strength_valid_count_mean_5 / coverage_mean_5 は feature 扱いで reserved 自動展開から除外（speed_figure count_5 と同様）"
    - "availability.assert_matrix_columns_registered が新 feature 27 個を含む feature_matrix で GREEN（registry↔Parquet parity）"
    # SC#4 SAFE-01 下地（allowlist）
    - "FEATURE_COLUMNS allowlist（src/model/data.py 経由）が registry から動的導出され・27 新 feature を含む 106 feature になる（feature_count 79 → 106）"
  artifacts:
    - "src/features/builder.py の build_feature_matrix に Step 5c / Step 5d（位置移動）/ Step 7 / Step 7b が挿入される"
    - "src/features/availability.py の _ROLLING_SYSTEMS_FOR_RESERVED に 'field_strength' が追加される"
    - "src/config/feature_availability.yaml の schema_version が '0.6.0' になる"
    - "src/config/feature_availability.yaml に rolling_field_strength_mean_latest_1 等 21 feature と speed_index_rank_mean5 等 6 feature のエントリが追加される"
    - "tests/features/test_speed_figure_builder_integration.py の hardcode feature list が 79 → 106 に更新される"
    - "tests/features/test_allowlist.py が新 feature 27 個で GREEN"
  key_links:
    - "builder Step 5c は src.features.field_strength.compute_field_strength_profile を import して呼ぶ（PLAN 01 の API）"
    - "builder Step 7 は src.features.race_relative.compute_race_relative_features を import して呼ぶ（PLAN 03 の API）"
    - "feature_availability.yaml の rolling_field_strength_mean_mean_5 エントリが src.features.race_relative.field_strength_adjusted_rank の入力（D-11）になる"
    - "availability.assert_matrix_columns_registered が builder Step 9 で呼ばれ・新 feature 27 個が registry に登録されていれば自動的に GREEN（drop した中間値は違反にならない）"
---

<objective>
Phase 10 PLAN 04: PLAN 01/02/03 で実装した 3 モジュール（field_strength.py・rolling.py 拡張・race_relative.py）を builder.py パイプラインに統合し・feature_availability.yaml に 27 新 feature を registry 登録して schema_version を 0.6.0 に bump する。

Purpose: feature snapshot 生成パイプラインが FEAT-02（21 feature）+ FEAT-03（6 feature）= 27 新 feature を正しく生成するようにする。registry↔Parquet parity（SC#3）と FEATURE_COLUMNS allowlist（SAFE-01 下地）を保証する。A6 構成変更（Step 5 rolling 呼出位置の移動）を安全に適用する。

Output: src/features/builder.py 拡張（Step 5c/5d/7/7b・obs_id idiom・FEATURE_COLUMNS allowlist）・src/features/availability.py 拡張（_ROLLING_SYSTEMS_FOR_RESERVED）・src/config/feature_availability.yaml 拡張（schema_version 0.6.0 + 27 feature エントリ）・テスト拡張
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-opponent-strength-race-relative-features/10-CONTEXT.md
@.planning/phases/10-opponent-strength-race-relative-features/10-RESEARCH.md
@.planning/phases/10-opponent-strength-race-relative-features/10-PATTERNS.md

# PLAN 01/02/03 出力（依存）
@.planning/phases/10-opponent-strength-race-relative-features/10-01-PLAN.md
@.planning/phases/10-opponent-strength-race-relative-features/10-02-PLAN.md
@.planning/phases/10-opponent-strength-race-relative-features/10-03-PLAN.md

# analog（必読）
@src/features/builder.py
@src/features/availability.py
@src/config/feature_availability.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: builder.py に Step 5c/5d/7/7b を挿入し feature_availability.yaml に 27 feature を registry 登録</name>
  <files>src/features/builder.py, src/features/availability.py, src/config/feature_availability.yaml, tests/features/test_speed_figure_builder_integration.py, tests/features/test_allowlist.py</files>

  <read_first>
    - src/features/builder.py L461-472（Step 4b copy-not-rename idiom・HIGH#5）
    - src/features/builder.py L516-627（Step 5b speed_figure + Step 6b 中間列 drop idiom・Step 5c/7 挿入位置の直接の analog）
    - src/features/builder.py L625-647（Step 6b drop + Step 9 assert_matrix_columns_registered）
    - src/features/availability.py L143-159（_ROLLING_SYSTEMS_FOR_RESERVED idiom）
    - src/features/availability.py L188-192（reserved 自動展開の speed_figure count_5 除外 idiom・field_strength も同様に除外）
    - src/features/availability.py L314-348（assert_matrix_columns_registered）
    - src/config/feature_availability.yaml L1-30（ヘッダ・schema_version・7項目スキーマの冒頭）
    - src/config/feature_availability.yaml L336-468（rolling_speed_figure 17 feature エントリの直接の analog）
    - tests/features/test_speed_figure_builder_integration.py（既存 hardcode feature list・79 → 106 更新位置）
    - tests/features/test_allowlist.py（既存 registry parity テスト）
    - .planning/phases/10-opponent-strength-race-relative-features/10-RESEARCH.md L571-605（builder.py Step 5c/5d/7 挿入 skeleton）・L620-630（A6 構成変更：Step 5 rolling 呼出位置の移動）
    - .planning/phases/10-opponent-strength-race-relative-features/10-PATTERNS.md L287-397（builder.py / availability.py / feature_availability.yaml の idiom・skeleton）
    - .planning/phases/09.1-speed-ability-profile-expansion/09.1-01-PLAN.md（Phase 9.1 17 feature 統合の直接の先例・hardcode feature list 更新 idiom）
  </read_first>

  <behavior>
    - Test 1 (Step 5c 挿入): build_feature_matrix が Step 5b の後に compute_field_strength_profile を呼び・history に field_strength profile 8値が付与される
    - Test 2 (Step 5 呼出位置移動・A6): 既存 Step 5 build_rolling_features の呼出位置が Step 5c の後に移動し・rolling_field_strength_* 21 feature が生成される（profile が揃った後で rolling）
    - Test 3 (Step 7 挿入): Step 6b の obs_id drop の前（race_nkey が使える段階）で compute_race_relative_features を呼び・FEAT-03 6 feature が生成される
    - Test 4 (Step 7b 中間列 drop): field_strength profile 生値（rolling_ prefix 無し）が feature_matrix から drop される・rolling_field_strength_* 21 と FEAT-03 6 feature のみ残る
    - Test 5 (feature_count): feature_matrix の feature 数が 79 → 106 になる（27 新 feature 追加）
    - Test 6 (registry parity・SC#3): assert_matrix_columns_registered が新 feature 27 個で GREEN・schema_version 0.6.0
    - Test 7 (FEATURE_COLUMNS allowlist): src/model/data.py の _derive_feature_columns が新 feature 27 個を含む（registry から動的導出・REVIEW H1）
    - Test 8 (回帰): 既存テスト（speed_figure 17 feature・Phase 9.1 same_surface 等）が破壊されない
  </behavior>

  <action>
    src/features/builder.py・availability.py・feature_availability.yaml・テスト2件を拡張する（PAT§ L287-445 idiom）:

    (1) src/features/builder.py 拡張:
        - Step 5b（L516-529）の直後に Step 5c を挿入: `from src.features.field_strength import compute_field_strength_profile`・`history = compute_field_strength_profile(history, observations=feature_matrix)`。copy-not-rename（HIGH#5）。
        - A6 構成変更: 既存 Step 5 build_rolling_features 呼出（L531-551 付近）を Step 5c の後に移動。これにより rolling_field_strength_* 21 feature が profile 揮った後の history から生成される。
        - Step 6b（L625-627 drop）の前に Step 7 を挿入: `from src.features.race_relative import compute_race_relative_features`・`feature_matrix = compute_race_relative_features(feature_matrix)`。race_nkey が使える段階（obs_id drop 前）で計算。
        - Step 7b を Step 7 の直後に挿入: 中間値（`[c for c in feature_matrix.columns if c.startswith("field_strength_") and not c.startswith("rolling_field_strength_")]`）を `drop(columns=..., errors="ignore")` で明示的に除去。既存 Step 6b の obs_id/trackcd/kyori drop は維持。
        - Step 9 assert_matrix_columns_registered（L644）は新 feature が registry 登録されていれば自動追従・手動変更不要。

    (2) src/features/availability.py 拡張:
        - _ROLLING_SYSTEMS_FOR_RESERVED（L143-159）の末尾（"speed_figure" の後）に "field_strength" を追加。
        - reserved 自動展開の count_5 除外（L188-192 付近）に field_strength を追加: `if sys != "speed_figure" and sys != "field_strength"`。これにより rolling_field_strength_valid_count_mean_5 / coverage_mean_5 は feature 扱いで reserved 自動展開から除外され・7項目スキーマで明示登録される。

    (3) src/config/feature_availability.yaml 拡張:
        - schema_version を `0.5.0` → `0.6.0` に bump（L19 idiom）。
        - rolling_field_strength 21 feature エントリを追加（L336-468 rolling_speed_figure エントリと同一の7項目スキーマ・feature_name/feature_group/available_from_timing: entry_confirmed/source_role: history_allowed_post_race/source_table: "normalized.n_uma_race (history) + derived field_strength profile"/cutoff_rule: "race_date - 1 day (strict < cutoff, JST midnight)"/leakage_risk_level: low）。21 feature 全て: rolling_field_strength_{mean,median,top3_mean,top5_mean,max,sd}_latest_1・{mean,median,top3_mean,top5_mean,max}_mean_3・{mean,median,top3_mean,top5_mean,max,sd}_mean_5・mean_trend_last_minus_mean5・mean_trend_mean3_minus_mean5・valid_count_mean_5・coverage_mean_5。
        - FEAT-03 6 feature エントリを追加（race_relative group・source_role: both_allowed・target + history の両方を参照するため 09.1 same_surface と同様）: speed_index_rank_mean5/best2_mean5/median5・gap_to_top・gap_to_3rd・field_strength_adjusted_rank。cutoff_rule: "race_date - 1 day (strict < cutoff, JST midnight)"・leakage_risk_level: low。

    (4) tests/features/test_speed_figure_builder_integration.py 拡張:
        - hardcode feature list（feature_count 79 を主張する箇所）を 79 → 106 に更新（27 新 feature 追加）。09.1-01-PLAN.md で 17 feature 追加した idiom と対称。

    (5) tests/features/test_allowlist.py 拡張:
        - schema_version 0.6.0 を主張するテストを更新。
        - 新 feature 27 個が registered であることを検証するテストを追加（任意・assert_matrix_columns_registered が自動 cover するが明示テスト推奨）。

    copy-not-rename・FEATURE_COLUMNS allowlist・obs_id idiom は既存 Phase 9/9.1 の idiom を完全に踏襲（CYCLE-2 HIGH#5/HIGH#9）。
  </action>

  <verify>
    <automated>uv run pytest tests/features/test_speed_figure_builder_integration.py tests/features/test_allowlist.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - src/features/builder.py に `from src.features.field_strength import compute_field_strength_profile` と `compute_field_strength_profile(history` の呼出が存在する（Step 5c）
    - src/features/builder.py に `from src.features.race_relative import compute_race_relative_features` と `compute_race_relative_features(feature_matrix)` の呼出が存在する（Step 7）
    - src/features/builder.py の build_rolling_features 呼出が compute_field_strength_profile 呼出の後に位置する（A6 構成変更・grep または行番号で確認）
    - src/features/builder.py の Step 7b で `field_strength_` prefix かつ `rolling_field_strength_` prefix 無しの列を drop する処理が存在する
    - src/features/availability.py の _ROLLING_SYSTEMS_FOR_RESERVED に "field_strength" が含まれる
    - src/config/feature_availability.yaml の schema_version が "0.6.0"
    - src/config/feature_availability.yaml に `rolling_field_strength_mean_latest_1` `rolling_field_strength_mean_mean_5` `rolling_field_strength_valid_count_mean_5` `rolling_field_strength_coverage_mean_5` `speed_index_rank_mean5` `gap_to_top` `gap_to_3rd` `field_strength_adjusted_rank` の各 feature_name エントリが存在する（27 feature の代表点）
    - tests/features/test_speed_figure_builder_integration.py の hardcode feature list が 106 を主張する
    - `uv run pytest tests/features/test_speed_figure_builder_integration.py tests/features/test_allowlist.py -x -q` GREEN
    - `uv run pytest tests/features/ -x -q`（feature 系全体回帰）GREEN
    - ruff lint GREEN: `uv run ruff check src/features/builder.py src/features/availability.py src/config/feature_availability.yaml`
  </acceptance_criteria>

  <done>
    builder.py に Step 5c/5d（位置移動）/7/7b が挿入され・feature_availability.yaml に 27 新 feature が registry 登録され schema_version 0.6.0 になる。registry↔Parquet parity（SC#3）が GREEN・feature_count 79 → 106・A6 構成変更が安全に適用される。単体テスト8項目 GREEN・既存テスト回帰 GREEN。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PLAN 01/02/03 出力（3モジュール）→ builder 統合 | compute_field_strength_profile / build_rolling_features 拡張 / compute_race_relative_features が正しい API を持つ前提 |
| feature_matrix → registry parity 検査 | 出力 feature_matrix 全列が registry または reserved に登録されている必要（HIGH#3 fake-green 防止）|

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-15 | Tampering | Step 5 rolling 呼出位置の移動（A6）による regression | high | mitigate | copy-not-rename・hardcode feature list 更新・既存テスト（test_speed_figure_builder_integration.py）で回帰検出 |
| T-10-16 | Tampering | 中間値の feature_matrix 残存（registry parity 違反） | medium | mitigate | Step 7b で field_strength_* prefix（rolling_ 無し）を明示 drop・assert_matrix_columns_registered が未登録列を fail-loud |
| T-10-17 | Tampering | registry ↔ Parquet parity 違反（SC#3） | high | mitigate | feature_availability.yaml に 27 feature 登録・assert_matrix_columns_registered GREEN・test_allowlist.py で機械保証 |
| T-10-18 | Tampering | banned source カラムの allowlist sneak-in（SAFE-01） | high | mitigate | assert_matrix_columns_registered が TARGET_OBS_BANNED_COLUMNS ∩ allowed を fail-loud（availability.py L333-348 既存 idiom） |
</threat_model>

<verification>
- 単体テスト: `uv run pytest tests/features/test_speed_figure_builder_integration.py tests/features/test_allowlist.py -x -q` GREEN
- feature 系全体回帰: `uv run pytest tests/features/ -x -q` GREEN
- lint: `uv run ruff check src/features/builder.py src/features/availability.py` GREEN
- feature_count が 79 → 106（27 新 feature）
- schema_version 0.6.0
</verification>

<success_criteria>
- builder.py パイプラインが 27 新 feature を正しく生成する（FEAT-02 21 + FEAT-03 6）
- SC#3 registry↔Parquet parity が GREEN・schema_version 0.6.0
- FEATURE_COLUMNS allowlist（src/model/data.py 経由）が 27 新 feature を含む 106 feature になる（PLAN 06 で v1.0 LightGBM 再学習に使用）
- A6 構成変更（Step 5 rolling 呼出位置移動）が安全に適用される
</success_criteria>

<output>
Create `.planning/phases/10-opponent-strength-race-relative-features/10-04-SUMMARY.md` when done
</output>
