---
phase: 10-opponent-strength-race-relative-features
plan: 05
type: execute
wave: 4
depends_on: [04]
files_modified:
  - src/features/snapshot.py
  - snapshots/feature_matrix_20260626-1a-opponentstrength-v1.parquet
  - tests/features/test_snapshot_repro.py
autonomous: false
requirements: [FEAT-02, FEAT-03, SAFE-01]
granularity: fine
user_setup:
  - service: PostgreSQL (EveryDB2 normalized)
    why: "live-DB で feature snapshot を再生成し byte-reproducibility を実データで検証（memory feature-snapshot-regen-required・unit test では発覚しない Parquet 直列化 bug あり）"
    env_vars:
      - name: KEIBA_SKIP_DB_TESTS
        source: "unset（live-DB 必須テストを有効化）"

must_haves:
  truths:
    # SC#3（byte-reproducible・§19.1 聖域）
    - "feature snapshot 20260626-1a-opponentstrength-v1 が live-DB から生成され・同一入力で SHA256 が一致する（byte-reproducible・FIXED_REPRODUCE_TS・metadata 除外）"
    - "snapshot に §12.4 metadata 9 keys（feature_snapshot_id・feature_cutoff_datetime・label_version・prediction_timing・dataset_version・feature_availability_schema_version=0.6.0 等）が埋め込まれる"
    # Parquet 直列化（Pitfall 5）
    - "rolling_field_strength_* 21 feature は rolling_ prefix で snapshot.py::_coerce_rolling_columns_for_parquet が自動的に nullable Float64 に変換する"
    - "FEAT-03 の speed_index_rank_*・gap_to_top・gap_to_3rd・field_strength_adjusted_rank 列（rolling_ prefix 無し）は nullable Float64 扱いを builder 側で保証し PyArrow ArrowTypeError を起こさない（Pitfall 5・_is_categorical_rolling_col 対象外なので別途対応）"
    # feature_count
    - "live-DB 生成 snapshot の feature_count が 106（79 既存 + 27 新 feature）"
    # 実データ検証（memory feature-snapshot-regen-required）
    - "live-DB で snapshot 再生成検証が PASS（unit test では発覚しない Parquet 直列化 bug を実データで検出）"
  artifacts:
    - "snapshots/feature_matrix_20260626-1a-opponentstrength-v1.parquet が存在する"
    - "src/features/snapshot.py の _coerce_rolling_columns_for_parquet または _is_categorical_rolling_col が新 feature（特に FEAT-03 の rolling_ prefix 無し列）を正しく扱う"
    - "tests/features/test_snapshot_repro.py が新 snapshot id で SHA256 一致を検証する"
  key_links:
    - "snapshot_id = '20260626-1a-opponentstrength-v1'（Open Question #3 事前登録・make_model_version 形式・09/9.1 系統継承）"
    - "snapshot 生成は builder.build_feature_matrix（PLAN 04 統合済み）を呼び・ Parquet 書込は snapshot.py::write_snapshot 既存 idiom を踏襲"
    - "metadata の feature_availability_schema_version が '0.6.0'（PLAN 04 で bump）"
---

<objective>
Phase 10 PLAN 05: PLAN 04 で統合した builder パイプラインを使って live-DB から feature snapshot 20260626-1a-opponentstrength-v1（feature_count=106）を生成し・byte-reproducibility（§19.1 聖域）を実データで検証する。

Purpose: unit test では発覚しない Parquet 直列化 bug（Pitfall 5・memory feature-snapshot-regen-required）を実データで検出する。FEAT-02/03 の 27 新 feature が正しく Parquet に直列化され・2回 build で SHA256 が一致することを保証する。SC#3 byte-reproducible + §12.4 metadata を最終保証する。

Output: snapshots/feature_matrix_20260626-1a-opponentstrength-v1.parquet・src/features/snapshot.py 拡張確認（必要なら _coerce_rolling_columns_for_parquet または _is_categorical_rolling_col の対象拡張）・tests/features/test_snapshot_repro.py 拡張
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-opponent-strength-race-relative-features/10-CONTEXT.md
@.planning/phases/10-opponent-strength-race-relative-features/10-RESEARCH.md
@.planning/phases/10-opponent-strength-race-relative-features/10-PATTERNS.md

# PLAN 04 出力（依存・builder 統合済み snapshot 生成可能状態）
@.planning/phases/10-opponent-strength-race-relative-features/10-04-PLAN.md

# analog（必読）
@src/features/snapshot.py
@src/features/builder.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: snapshot.py の FEAT-03 列の nullable Float64 扱いを確認し byte-reproducibility テストを拡張</name>
  <files>src/features/snapshot.py, tests/features/test_snapshot_repro.py</files>

  <read_first>
    - src/features/snapshot.py L81-113（_coerce_rolling_columns_for_parquet: rolling_ prefix 列を nullable Float64 に変換・L100-101 `if not col.startswith("rolling_"): continue`）
    - src/features/snapshot.py L116-154（_is_categorical_rolling_col: rolling_ prefix 列の categorical/numeric 判定・numeric axis リスト L150）
    - src/features/snapshot.py L157-314（write_snapshot: §12.4 metadata 9 keys・SHA256・FIXED_REPRODUCE_TS・決定論的 PyArrow 書込 use_dictionary=False compression="zstd" row_group_size=100_000）
    - src/features/snapshot.py L240-251（SHA256 は metadata 無し schema bytes のみ）
    - tests/features/test_snapshot_repro.py（既存 byte-reproducible テスト・09.1 で 17 feature 拡張時に更新済み）
    - .planning/phases/10-opponent-strength-race-relative-features/10-RESEARCH.md L453-461（Pitfall 5: Parquet 直列化失敗・object dtype + sentinel・rolling_field_strength_* は自動対象・FEAT-03 は別途 nullable Float64 扱い）
    - .planning/phases/10-opponent-strength-race-relative-features/10-PATTERNS.md L340-358（snapshot.py 拡張確認 idiom）
  </read_first>

  <behavior>
    - Test 1 (SC#3 byte-reproducible): 同一 DataFrame で write_snapshot を2回呼ぶと SHA256 が完全一致する
    - Test 2 (Pitfall 5・rolling_field_strength_*): rolling_field_strength_* 21 feature が nullable Float64 で Parquet に直列化される（_coerce_rolling_columns_for_parquet が rolling_ prefix で自動対象）
    - Test 3 (Pitfall 5・FEAT-03 列): speed_index_rank_mean5/best2_mean5/median5・gap_to_top・gap_to_3rd・field_strength_adjusted_rank が nullable Float64 で Parquet に直列化される（rolling_ prefix 無し・ArrowTypeError 起きない）
    - Test 4 (§12.4 metadata): snapshot metadata に feature_snapshot_id='20260626-1a-opponentstrength-v1'・feature_availability_schema_version='0.6.0' が埋め込まれる
    - Test 5 (FIXED_REPRODUCE_TS): timestamp 等の非決定論的要素が metadata 除外で SHA256 に影響しない
  </behavior>

  <action>
    src/features/snapshot.py の動作確認と（必要なら）拡張を行う:

    (1) _coerce_rolling_columns_for_parquet（L81-113）の動作確認: rolling_field_strength_* 21 feature は rolling_ prefix のため自動的に nullable Float64 変換対象（L100-101 の `if not col.startswith("rolling_"): continue` で skip されない）。変更不要を確認。

    (2) _is_categorical_rolling_col（L116-154）の動作確認: rolling_field_strength_valid_count_mean_5・coverage_mean_5・top3_mean・top5_mean 等 の axis が numeric として正しく扱われるか確認。必要なら L150 の numeric axis リストに top3_mean/top5_mean/valid_count/coverage を追加（mean/sd/count/last/max/median 等は既出）。

    (3) FEAT-03 列（speed_index_rank_*・gap_to_top・gap_to_3rd・field_strength_adjusted_rank・rolling_ prefix 無し）の nullable Float64 扱い:
        - これらは rolling_ prefix でないため _coerce_rolling_columns_for_parquet の自動対象外。
        - builder.py 側（PLAN 04）でこれらの列を nullable Float64（pandas Float64 dtype）または sentinel 無しの通常 float64 で出力することを保証する。snapshot.py 側では対応不要なように builder 側で dtype を整えるのが理想。
        - もし builder 側だけでは不十分な場合・snapshot.py に FEAT-03 列名の明示的 nullable Float64 変換を追加する（例: _coerce_rolling_columns_for_parquet の rolling_ prefix チェックに加え・speed_index_rank_・gap_to_・field_strength_adjusted_rank prefix も対象に拡張）。

    (4) tests/features/test_snapshot_repro.py 拡張:
        - test_snapshot_id を '20260626-1a-opponentstrength-v1' に更新（Open Question #3 事前登録）。
        - 27 新 feature を含む合成 DataFrame で byte-reproducible テスト（2回 write で SHA256 一致）を拡張。
        - FEAT-03 列の nullable Float64 直列化テストを追加（Pitfall 5 回避）。
        - metadata 9 keys の feature_availability_schema_version='0.6.0' を主張するテストを追加。

    §19.1 聖域: write_snapshot の FIXED_REPRODUCE_TS・SHA256（metadata 除外）・決定論的 PyArrow 書込（use_dictionary=False・compression="zstd"・write_statistics=True・row_group_size=100_000）は既存 idiom を完全に踏襲・新規アルゴリズムなし。
  </action>

  <verify>
    <automated>uv run pytest tests/features/test_snapshot_repro.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - src/features/snapshot.py の _coerce_rolling_columns_for_parquet が rolling_field_strength_* 列を nullable Float64 に変換する（rolling_ prefix で自動対象）
    - src/features/snapshot.py の _is_categorical_rolling_col が rolling_field_strength_valid_count_mean_5・coverage_mean_5・top3_mean・top5_mean 等 を numeric として扱う（必要なら L150 の numeric axis リスト拡張）
    - FEAT-03 列（speed_index_rank_mean5・gap_to_top・gap_to_3rd・field_strength_adjusted_rank 等）が PyArrow ArrowTypeError を起こさず nullable Float64 で直列化される（builder 側または snapshot.py 側で保証）
    - tests/features/test_snapshot_repro.py の snapshot_id が '20260626-1a-opponentstrength-v1'
    - tests/features/test_snapshot_repro.py で SHA256 一致テスト・§12.4 metadata テスト（schema_version='0.6.0'）が GREEN
    - `uv run pytest tests/features/test_snapshot_repro.py -x -q` GREEN
  </acceptance_criteria>

  <done>
    snapshot.py が 27 新 feature（rolling_field_strength_* 21 + FEAT-03 6）を正しく nullable Float64 で Parquet 直列化する。byte-reproducibility テストと §12.4 metadata テストが GREEN。Open Question #3（snapshot_id='20260626-1a-opponentstrength-v1'）が事前登録される。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: live-DB で snapshot 20260626-1a-opponentstrength-v1 を生成し byte-reproducibility を検証</name>
  <files>snapshots/feature_matrix_20260626-1a-opponentstrength-v1.parquet</files>
  <action>live-DB（KEIBA_SKIP_DB_TESTS unset・readonly role）から feature snapshot 20260626-1a-opponentstrength-v1 を生成し・feature_count=106・SHA256 2回一致・ArrowTypeError 無し・§12.4 metadata（feature_availability_schema_version=0.6.0）を検証する。subagent 経由の重いDBクエリは statement_timeout 必須（memory subagent-db-query-statement-timeout）。詳細手順は how-to-verify 参照。</action>
  <verify><automated>shasum -a 256 snapshots/feature_matrix_20260626-1a-opponentstrength-v1.parquet（2回 build で一致を確認）</automated><human-check>feature_count=106・metadata 9 keys・ArrowTypeError 無し</human-check></verify>
  <done>snapshot 20260626-1a-opponentstrength-v1（feature_count=106）が live-DB で生成され・byte-reproducible（SHA256 一致）・§12.4 metadata が埋め込まれる。Pitfall 5（Parquet 直列化失敗）が実データで回避される。</done>
  <what-built>
    PLAN 05 Task 1 で snapshot.py の新 feature 対応とテスト拡張が完了した。次に live-DB で実際に snapshot を生成し・実データでの Parquet 直列化と byte-reproducibility を検証する必要がある（memory feature-snapshot-regen-required: unit test では発覚しない Parquet 直列化 bug あり）。
  </what-built>

  <how-to-verify>
    1. PostgreSQL (EveryDB2 normalized) に接続できることを確認（host Homebrew 15.x・readonly role）。
    2. `KEIBA_SKIP_DB_TESTS` 環境変数を unset する（live-DB 必須テストを有効化）。
    3. live-DB から feature snapshot を生成:
       ```
       uv run python scripts/run_feature_build.py --snapshot-id 20260626-1a-opponentstrength-v1 --bt-split BT-1
       ```
       （scripts/run_feature_build.py の実際の CLI 引数は既存 Phase 9.1 の idiom を踏襲・--snapshot-id で '20260626-1a-opponentstrength-v1' を明示）
    4. snapshots/feature_matrix_20260626-1a-opponentstrength-v1.parquet が生成されることを確認。
    5. 生成された snapshot の feature_count が 106（79 既存 + 27 新 feature）であることを確認:
       ```
       uv run python -c "import pyarrow.parquet as pq; t = pq.read_table('snapshots/feature_matrix_20260626-1a-opponentstrength-v1.parquet'); print('cols:', len(t.column_names)); print('rows:', t.num_rows)"
       ```
    6. 2回目の生成で SHA256 が一致することを確認（byte-reproducible・§19.1 聖域）:
       ```
       shasum -a 256 snapshots/feature_matrix_20260626-1a-opponentstrength-v1.parquet
       # もう1回 build して再度 shasum・同じ値であることを確認
       ```
    7. PyArrow ArrowTypeError が発生しないこと（Pitfall 5・FEAT-03 列の nullable Float64 扱いが実データで成功）。
    8. snapshot metadata に feature_availability_schema_version='0.6.0'・feature_snapshot_id='20260626-1a-opponentstrength-v1' が埋め込まれていることを確認。
    9. subagent 経由の重いDBクエリは statement_timeout を必ず設定（memory subagent-db-query-statement-timeout・孤立実行がCPU張り付き）。
  </how-to-verify>

  <resume-signal>Type "approved"（snapshot 生成成功・feature_count=106・SHA256 一致の場合）または問題を記述（ArrowTypeError・feature_count 不一致・SHA256 不一致 等）</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live-DB → snapshot 生成 | EveryDB2 normalized 層の実データ（~48万行 history・~4.7万件/年 target）が信頼できる前提・readonly role（V4 Access Control）|
| snapshot → 下流（data.py/trainer）| byte-reproducible であることが §19.1 聖域・SHA256 で保証|

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-19 | Tampering | Parquet 直列化失敗（Pitfall 5・object dtype + sentinel） | high | mitigate | rolling_field_strength_* は rolling_ prefix で自動 nullable Float64・FEAT-03 列は builder または snapshot.py で明示的に nullable Float64 扱い・live-DB で実データ検証 |
| T-10-20 | Tampering | reproducibility drift（run 毎に SHA256 変動） | high | mitigate | FIXED_REPRODUCE_TS・決定論的 PyArrow 書込・SHA256 は metadata 除外 schema bytes のみ・2回 build で一致を live-DB 検証 |
| T-10-21 | Denial of service | live-DB 重クエリの孤立実行・CPU 張り付き | medium | mitigate | subagent 経由の重いDBクエリは statement_timeout 必須（memory subagent-db-query-statement-timeout）|
| T-10-22 | Tampering | §12.4 metadata 欠落 | medium | mitigate | write_snapshot が 9 keys を必ず埋め込む・feature_availability_schema_version='0.6.0' を主張するテスト |
</threat_model>

<verification>
- 単体テスト: `uv run pytest tests/features/test_snapshot_repro.py -x -q` GREEN
- live-DB 検証（checkpoint）: snapshot 生成成功・feature_count=106・SHA256 2回一致・ArrowTypeError 無し
- metadata: feature_snapshot_id='20260626-1a-opponentstrength-v1'・feature_availability_schema_version='0.6.0'
</verification>

<success_criteria>
- SC#3 byte-reproducible + §12.4 metadata が live-DB 実データで検証される（memory feature-snapshot-regen-required）
- snapshot 20260626-1a-opponentstrength-v1（feature_count=106）が生成され PLAN 06 の v1.0 LightGBM 再学習の入力になる
- Open Question #3（snapshot_id='20260626-1a-opponentstrength-v1'）が事前登録され使用される
- Pitfall 5（Parquet 直列化失敗）が実データで回避される
</success_criteria>

<output>
Create `.planning/phases/10-opponent-strength-race-relative-features/10-05-SUMMARY.md` when done
</output>
