---
phase: 10-opponent-strength-race-relative-features
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - src/features/race_relative.py
  - tests/features/test_race_relative.py
autonomous: true
requirements: [FEAT-03, SAFE-01]
granularity: fine

must_haves:
  truths:
    # SC#2（race_id 単位・future-information なし・D-07 target-only）
    - "FEAT-03 レース内相対特徴量（speed_index_rank 3軸・gap_to_top・gap_to_3rd・field_strength_adjusted_rank）が race_id（race_nkey）group-by で算出される"
    - "FEAT-03 は target observation のみで計算される・過去走には適用しない（D-07・feature_matrix 上でのみ動作）"
    # D-10 同着・D-09 欠損
    - "同着は competition ranking（min rank・'1224' 方式・D-10・pandas Series.rank method='min' ascending=False na_option='keep'）で同順位・次は同着数だけ飛ぶ"
    - "speed_index 欠損馬は NaN 保持・母集団から除外される（D-09・na_option='keep'・最下位固定・sentinel 数値は使わない）"
    # D-08 gap（Open Question #2 事前登録）
    - "gap_to_top = top(1位).mean_5 - self.mean_5・gap_to_3rd = 3位.mean_5 - self.mean_5・mean_5 主軸（D-08・Open Question #2 解決）"
    - "出走馬 < 3 の場合は gap_to_3rd は NaN と明文化する（Open Question #2・複勝払戻対象2頭でも gap_to_3rd は意味を持つが母数不足時は NaN）"
    # D-11/D-12 additive score（事前登録）
    - "field_strength_adjusted_score = rolling_speed_figure_mean_5 + 0.25 * rolling_field_strength_mean_mean_5・coef=0.25 事前登録 canonical 初期値（D-11/D-12）"
    - "**CYCLE-3 MEDIUM #2 (10-REVIEWS.md L222・par-scale 兼容性・非リーク)**: rolling_speed_figure_mean_5（target 経路・horse-level par 由来）と rolling_field_strength_mean_mean_5（PLAN 01 source-asof 経路）は **同一 horse-level par の normalization** 上にある（PLAN 01 の obs_id を `SOURCE_ASOF_<race_nkey>_<kettonum>` として target 経路と揃えたため）・よって additive score の加算項は scale 整合し・係数 0.25 の事前登録 canonical が意味的に妥当。仮に PLAN 01 が race 単位 obs_id（field-level par）を採用していた場合は scale 不一致で加算が不正確化するため・PLAN 01 の CYCLE-3 MEDIUM #2 設計選択（horse-level par）が本特徴量の前提・test で両項の scale が一致することを検証"
    - "係数候補 {0.0, 0.1, 0.25, 0.5} は定数として公開し・0.0 は baseline（raw mean_5 rank と同値）・train/calib 窓内のみ感度分析（test 窓選び直し禁止・§11.2 聖域）"
    - "feature_matrix に出力するのは coef=0.25 canonical の rank のみ・候補別 rank は内部計算用（test 窓すり替え防止）・W-5: 候補別 score の一時列は関数末尾で確実に drop され戻り値 columns に残存しない（機械保証テスト付）"
    # W-2 diagnostic（係数妥当性証跡・§11.2 聖域は保護・test 窓すり替えではない）
    - "W-2: compute_candidate_score_diagnostics(feature_matrix, split_mask) が公開され・train/calib 窓それぞれで候補 {0.0,0.1,0.25,0.5} の adjusted_score 分布（mean/std/min/max/p10/p50/p90・race_id group の field_strength_adjusted_rank の分布）を dict で返す。PLAN 06 run_phase10_evaluation.py がこれを消費し train/calib 窓の分布を log 出力する（係数妥当性の証跡・REVIEWS.md L86 actionable MEDIUM/LOW・test 窓の rank すり替えではない・候補選定は train/calib 窓内のみ）"
    # SAFE-01
    - "race_relative.py のソース AST に odds/ninki/fukuodds/ninkij/tansyouodds の Name/Attribute/Constant が0件である（SAFE-01・SC#4 下地）"
  artifacts:
    - "src/features/race_relative.py に compute_race_relative_features(feature_matrix) -> pd.DataFrame が存在"
    - "src/features/race_relative.py に _competition_rank_desc_within_race(series) helper が存在（D-10・pandas.Series.rank）"
    - "src/features/race_relative.py に ADJUSTED_RANK_COEF_CANDIDATES = (0.0, 0.1, 0.25, 0.5) と ADJUSTED_RANK_COEF_CANONICAL = 0.25 定数が定義される（D-12 事前登録）"
    - "src/features/race_relative.py に _SPEED_INDEX_AXES_FOR_RANK = ('rolling_speed_figure_mean_5', 'rolling_speed_figure_best2_mean_5', 'rolling_speed_figure_median_5') 定数が定義される（D-07 3軸）"
    - "src/features/race_relative.py に compute_candidate_score_diagnostics(feature_matrix, split_mask) が存在し・候補 {0.0,0.1,0.25,0.5} の adjusted_score 分布統計を返す（W-2・PLAN 06 が消費）"
    - "tests/features/test_race_relative.py が存在し全テスト GREEN"
  key_links:
    - "compute_race_relative_features は race_nkey で group-by し transform で各馬に rank/gap/adjusted_rank を付与する（target feature_matrix のみ）"
    - "出力 feature_matrix は入力を copy に 6 列（speed_index_rank_mean5/best2_mean5/median5・gap_to_top・gap_to_3rd・field_strength_adjusted_rank）を追加（copy-not-rename・HIGH#5）"
    - "field_strength_adjusted_rank の計算には rolling_field_strength_mean_mean_5（PLAN 02 出力）と rolling_speed_figure_mean_5（Phase 9.1 出力）の両方が必要・PLAN 02 とはファイル衝突なし（race_relative.py は新規・rolling.py とは別）"
---

<objective>
Phase 10 PLAN 03: FEAT-03 レース内相対特徴量（speed_index_rank 3軸 / gap_to_top / gap_to_3rd / field_strength_adjusted_rank）を新規 src/features/race_relative.py に実装する。target observation のみ・race_id group-by・competition ranking で算出する。

Purpose: 複勝の「相対競争・各馬独立事象でない」性質を特徴量層で表現する。target race の出馬表確定時点 feature_cutoff_datetime 基準で・出走馬内の順序付けを future-information なしで確定する（D-07 target-only）。D-12 additive score 係数は 0.25 を事前登録 canonical とし候補集合 {0.0, 0.1, 0.25, 0.5} を公開定数化する（§11.2 聖域）。

Output: src/features/race_relative.py（compute_race_relative_features + _competition_rank_desc_within_race + 定数）・tests/features/test_race_relative.py（rank 3軸 / gap / competition ranking / adjusted_rank / 欠損馬除外 単体テスト）
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-opponent-strength-race-relative-features/10-CONTEXT.md
@.planning/phases/10-opponent-strength-race-relative-features/10-RESEARCH.md
@.planning/phases/10-opponent-strength-race-relative-features/10-PATTERNS.md

# analog（必読）
@src/features/rolling.py
@src/features/builder.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: compute_race_relative_features（FEAT-03・target-only・race_id group-by）を実装</name>
  <files>src/features/race_relative.py, tests/features/test_race_relative.py</files>

  <read_first>
    - src/features/speed_figure.py L1-46（モジュール docstring 3聖域・CUTOFF_SEMANTICS 実行時 assert の idiom・対称に書く）
    - src/features/rolling.py L74-104（_ROLLING_SYSTEMS + _SYSTEM_SOURCE idiom・target-only だが race_id group の idiom 参照）
    - src/features/rolling.py L375-383（groupby + transform idiom）
    - src/features/builder.py L461-472（Step 4b copy-not-rename idiom・HIGH#5）
    - src/features/builder.py L516-627（Step 5b speed_figure + Step 6b 中間列 drop idiom・Step 7 race_relative 挿入位置の参考）
    - .planning/phases/10-opponent-strength-race-relative-features/10-RESEARCH.md L360-410（Pattern 3 competition ranking + Pattern 4 中間列 drop skeleton）・L520-568（competition ranking + adjusted score skeleton）・L443-461（Pitfall 4 過去走誤適用警告）・L463-468（Pitfall 6 係数 test 窓すり替え聖域）
    - .planning/phases/10-opponent-strength-race-relative-features/10-PATTERNS.md L127-202（race_relative.py の docstring/helper/skeleton idiom）
  </read_first>

  <behavior>
    - Test 1 (D-10 competition ranking・同着): values [100, 95, 95, 90] desc → ranks [1, 2, 2, 4]（dense でない・min rank・"1224" 方式）
    - Test 2 (D-07 rank 3軸): speed_index_rank が mean_5 / best2_mean_5 / median_5 の3軸それぞれで race_id 内 rank として算出される・列名は speed_index_rank_mean5 / speed_index_rank_best2_mean5 / speed_index_rank_median5
    - Test 3 (D-09 欠損馬): speed_index 欠損馬（rolling_speed_figure_mean_5 が NaN）は rank も NaN・母集団から除外される（na_option="keep"・最下位固定しない）
    - Test 4 (D-08 gap_to_top・Open Question #2): gap_to_top = top(1位).mean_5 - self.mean_5・mean_5 主軸・top は competition ranking で1位確定後の馬の mean_5
    - Test 5 (D-08 gap_to_3rd・Open Question #2): gap_to_3rd = 3位.mean_5 - self.mean_5・出走馬 < 3 の場合は gap_to_3rd は NaN
    - Test 5b (REVIEW MEDIUM-7 gap_to_3rd tie 仕様・10-REVIEWS.md L201, L226): values=[100, 95, 95, 90] の race（competition rank [1,2,2,4]・rank==3 は空位）の場合・gap_to_3rd は race 内の全馬で NaN となる（rank==3 の馬が存在しない・3番目のソート値でない）。tie が無い [100, 95, 90, 85] → rank [1,2,3,4] の場合は rank==3 の馬（95）が存在し gap_to_3rd が算出される。両ケースを検証。
    - Test 6 (D-11 additive score・D-12 coef): field_strength_adjusted_rank は (mean_5 + 0.25 * field_strength_mean_mean_5) の race_id 内 rank・coef=0.25 canonical
    - Test 6b (**CYCLE-3 MEDIUM #2 scale 兼容性・10-REVIEWS.md L222・非リーク**): rolling_speed_figure_mean_5 と rolling_field_strength_mean_mean_5 が同 horse-level par normalization 上にあることを前提として加算が成立することを検証（両者の units/scale が揃っていること・PLAN 01 の obs_id `SOURCE_ASOF_<race_nkey>_<kettonum>` 設計選択に依存）。合成 feature_matrix で両列が同 scale（例: 両者とも speed_figure 単位）のとき adjusted_score が race_id 内で意味のある順序付けを与えることを assert。
    - Test 7 (D-12 候補集合・§11.2 聖域): ADJUSTED_RANK_COEF_CANDIDATES = (0.0, 0.1, 0.25, 0.5) が公開定数・0.0 は baseline・feature_matrix に出力するのは coef=0.25 の rank のみ（候補別は内部計算用）
    - Test 8 (D-07 target-only・過去走誤適用防止): feature_matrix の行数が target observations 数と一致・過去走 history 行には rank/gap が付与されない（Pitfall 4 回避）
    - Test 9 (copy-not-rename・HIGH#5): 入力 feature_matrix の既存列は破壊されず・6 列が copy 追加される
    - Test 10 (fail-loud): 必須列（race_nkey/rolling_speed_figure_mean_5/rolling_speed_figure_best2_mean_5/rolling_speed_figure_median_5/rolling_field_strength_mean_mean_5）欠落時に raise ValueError
    - Test 11 (W-5 候補別一時列の非残存・§11.2 聖域保護): compute_race_relative_features の戻り値 DataFrame の columns に・候補別 score 計算用の一時列（coef_0.0 / coef_0.1 / coef_0.5 / adjusted_score_internal 等・候補 {0.0,0.1,0.25,0.5} の非 canonical 値で作った score 列）が **含まれない** ことを assert。うっかり残ると候補別 rank が feature として漏出し §11.2 聖域（test 窓すり替え）を破るため・関数末尾の drop が確実に効いていることを機械保証する。
    - Test 12 (W-2 係数妥当性証跡・diagnostic helper): compute_candidate_score_diagnostics(feature_matrix, split_mask=train_mask) が dict を返し・キー 0.0/0.1/0.25/0.5 が全て存在し・各キーに mean/std/min/max/p10/p50/p90 + adjusted_rank_mean/adjusted_rank_std が含まれることを assert。0.0 の adjusted_rank_mean は raw mean_5 rank の分布統計と一致（baseline・係数0は raw rank と同値）。feature_matrix の列が破壊されないこと（copy 使用・列追加なし）を assert。split_mask=None は全行対象。
  </behavior>

  <action>
    src/features/race_relative.py を新規作成する（PAT§ L127-202 idiom）:

    (1) モジュール docstring 3聖域: SAFE-01 odds-free / D-07 target-only（feature_cutoff_datetime 時点で確定した rolling_speed_figure_mean_5 等のみ・当日結果不使用）/ byte-reproducible（決定論的 competition ranking）。speed_figure.py L1-32 と対称な形式。

    (2) imports: `from __future__ import annotations`・`import pandas as pd`・`import numpy as np`。必要に応じて `from src.features.availability import CUTOFF_SEMANTICS` と assert（target-only で PIT は更に厳格・feature_cutoff_datetime 基準を明示）。

    (3) D-07 定数（3軸）: `_SPEED_INDEX_AXES_FOR_RANK: tuple[str, ...] = ("rolling_speed_figure_mean_5", "rolling_speed_figure_best2_mean_5", "rolling_speed_figure_median_5")` を定義。各 axis が1つの rank 列を生成。

    (4) D-12 事前登録定数（§11.2 聖域・Open Question 解決）:
        - `ADJUSTED_RANK_COEF_CANDIDATES: tuple[float, ...] = (0.0, 0.1, 0.25, 0.5)` を公開定数（0.0 は baseline・raw mean_5 rank と同値）。
        - `ADJUSTED_RANK_COEF_CANONICAL: float = 0.25` を canonical 初期値。
        - docstring で「test 窓で係数を選び直すのは禁止（§11.2 聖域）」を明記。

    (5) D-10 `_competition_rank_desc_within_race(s: pd.Series) -> pd.Series` helper: `return s.rank(method="min", ascending=False, na_option="keep")`。docstring で [100, 95, 95, 90] → [1, 2, 2, 4] の例を明示（PAT§ L144-153 skeleton）。D-09 欠損馬は NaN 保持・母集団除外（na_option="keep"）。

    (6) D-11/D-12 `_adjusted_score(mean5: pd.Series, fs_mean5: pd.Series, coef: float) -> pd.Series` helper: `return mean5 + coef * fs_mean5`。docstring で「差/比は不採用（強い相手と走ってきた馬を不当に下げるため）」を明記（PAT§ L188-201 skeleton）。**CYCLE-3 MEDIUM #2 (10-REVIEWS.md L222)**: 加算の前提として mean5（rolling_speed_figure_mean_5）と fs_mean5（rolling_field_strength_mean_mean_5）が同一 horse-level par normalization 上にあることを docstring に明記（PLAN 01 が obs_id を `SOURCE_ASOF_<race_nkey>_<kettonum>` として target 経路と同 horse-level par に揊えたため・race 単位 obs_id なら scale 不一致で加算が不正確化）。

    (7) D-08 gap 計算 helper（Open Question #2 解決・**REVIEW MEDIUM-7 (10-REVIEWS.md L201, L226) gap_to_3rd tie 仕様明文化**）:
        - gap_to_top: race_id 内の mean_5 降順1位（top・max）の mean_5 - self.mean_5。competition ranking で順位確定後の1位馬の mean_5 を使う（同着1位の場合は同値）。
        - **gap_to_3rd tie 仕様（REVIEW MEDIUM-7 明文化）**: gap_to_3rd は「competition ranking で rank==3 である馬の mean_5 − self.mean_5」と定義する（rank 値が 3 の馬・3番目のソート済み non-null 値でない）。[100, 95, 95, 90] の場合 competition rank は [1, 2, 2, 4] となり rank==3 の馬は存在しないため・gap_to_3rd は race 内の全馬で NaN となる（同着2位がいる場合 rank 3 は空位・これが competition ranking の min rank 方式の挙動）。docstring に [100, 95, 95, 90] → rank [1,2,2,4] → gap_to_3rd は NaN の例を明示。**tie テスト（追加）**: values=[100, 95, 95, 90] の race で gap_to_3rd が NaN になることを検証。
        - 出走馬 < 3 の場合も NaN（rank==3 が存在しないため上記と同値）。
        - 3位未満の出走馬（5-7頭）でも算出（複勝払戻対象2頭でも相手強度評価として意味）・ただし rank==3 の馬が存在する場合のみ（上記 tie 仕様）。
        - 欠損馬（D-09）は母集団除外・自身の gap も NaN。

    (8) `compute_race_relative_features(feature_matrix: pd.DataFrame) -> pd.DataFrame` メイン関数:
        - 入力検証: 必須列（race_nkey・_SPEED_INDEX_AXES_FOR_RANK の3列・rolling_field_strength_mean_mean_5）を assert（fail-loud）。
        - copy-not-rename: `result = feature_matrix.copy()`。
        - D-07 rank 3軸: for axis_col in _SPEED_INDEX_AXES_FOR_RANK: rank_col = f"speed_index_rank_{axis_suffix}"（axis_suffix = mean5/best2_mean5/median5・_SPEED_INDEX_AXES_FOR_RANK の要素から機械導出）。`result[rank_col] = result.groupby("race_nkey")[axis_col].transform(_competition_rank_desc_within_race)`。
        - D-08 gap: race_id group で mean_5 降順1位・3位を特定（competition ranking 後）し gap_to_top / gap_to_3rd を算出・欠損馬は NaN・出走馬<3は gap_to_3rd NaN。
        - D-11 adjusted_rank: `score = _adjusted_score(result["rolling_speed_figure_mean_5"], result["rolling_field_strength_mean_mean_5"], ADJUSTED_RANK_COEF_CANONICAL)`・`result["field_strength_adjusted_rank"] = result.groupby("race_nkey")[score_internal_col].transform(_competition_rank_desc_within_race)`。候補別 score は内部計算用の一時列（coef=0.0/0.1/0.5）・feature_matrix に出力しない（test 窓すり替え防止・D-12）。
        - **W-5 一時列の確実な drop**: 一時列（候補別 score 計算用・coef=0.0/0.1/0.5 の score や "adjusted_score_internal" 等の名前）は関数末尾で確実に drop する（Step 7b idiom・中間値は残さない）。うっかり残ると候補別 rank が feature として漏出し §11.2 聖域（test 窓すり替え）を破るため・Test 11 で戻り値 columns にこれら一時列名が存在しないことを機械保証する。
        - return result（6 列追加: speed_index_rank_mean5/best2_mean5/median5・gap_to_top・gap_to_3rd・field_strength_adjusted_rank）。

    (8b) **W-2 係数妥当性証跡（REVIEWS.md L86 actionable MEDIUM/LOW・§11.2 聖域保護）**: `compute_candidate_score_diagnostics(feature_matrix: pd.DataFrame, split_mask: pd.Series | np.ndarray | None = None) -> dict` を公開関数として実装。目的は additive score 係数（0.25 canonical）の妥当性を train/calib 窓内で事前登録候補集合 {0.0, 0.1, 0.25, 0.5} に対して感度証跡を残すこと（test 窓の rank すり替えではない・候補選定は train/calib 窓内のみ・§11.2 聖域遵守）:
        - 必須列（rolling_speed_figure_mean_5・rolling_field_strength_mean_mean_5）を assert（fail-loud）。feature_matrix に rank が未付与の場合は内部で _adjusted_score + _competition_rank_desc_within_race を再計算して使う（feature_matrix の rank 列を破壊しない・copy 使用）。
        - split_mask 指定時はそのマスク（train 窓=True/calib 窓=True 等の boolean Series・race_id 単位）で行を抽出し分布を計算。None の場合は全行を対象。
        - 戻り値 dict の構造: `{coef: {mean, std, min, max, p10, p50, p90, "adjusted_rank_mean", "adjusted_rank_std"} }`（各 coef 0.0/0.1/0.25/0.5 について・race_id group の rank 分布統計を含む）。0.0 は baseline（raw mean_5 rank と同値）。
        - 戻り値 dict にはいかなる test 窓の rank も含めない（呼出側が split_mask で制御・本関数は与えられた行集合の統計のみ返す）。
        - docstring で「test 窓の rank すり替え目的ではない・係数妥当性の train/calib 窓内証跡（§11.2 聖域）・PLAN 06 run_phase10_evaluation.py が消費」を明記。
        - 本関数は読み取り専用（feature_matrix に列を追加しない・copy のみ）・戻り値は純粋な dict（DataFrame 列を汚さない）。

    (9) `__all__ = ["compute_race_relative_features", "compute_candidate_score_diagnostics"]`。

    docstring またはコメントで「FEAT-03 は target observation のみ・過去走には適用しない（D-07）」と「test 窓で係数を選び直すのは禁止（§11.2 聖域）」を明記（Pitfall 4/6 回避）。ただし _FORBIDDEN_TOKENS の "odds" "ninki" 等の文字列リテラルを docstring に書かないこと（adversarial AST audit false positive 回避）。
  </action>

  <verify>
    <automated>uv run pytest tests/features/test_race_relative.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - src/features/race_relative.py に `def compute_race_relative_features(feature_matrix: pd.DataFrame) -> pd.DataFrame:` が存在
    - src/features/race_relative.py に `def _competition_rank_desc_within_race(s: pd.Series) -> pd.Series:` が存在し `method="min"` `ascending=False` `na_option="keep"` を含む（D-10/D-09）
    - src/features/race_relative.py に `ADJUSTED_RANK_COEF_CANDIDATES = (0.0, 0.1, 0.25, 0.5)` と `ADJUSTED_RANK_COEF_CANONICAL = 0.25` が事前登録される（D-12・§11.2 聖域）
    - src/features/race_relative.py に `_SPEED_INDEX_AXES_FOR_RANK = (...)` が3軸（mean_5/best2_mean_5/median_5）を含む（D-07）
    - src/features/race_relative.py の出力列が speed_index_rank_mean5 / speed_index_rank_best2_mean5 / speed_index_rank_median5 / gap_to_top / gap_to_3rd / field_strength_adjusted_rank の6列
    - tests/features/test_race_relative.py の全テストが GREEN（competition ranking 同着・rank 3軸・gap top/3rd・欠損馬除外・adjusted_rank・候補集合聖域・target-only・copy-not-rename・fail-loud・**W-5 候補別一時列の非残存**・**W-2 diagnostic helper**）
    - **W-5**: tests/features/test_race_relative.py に・compute_race_relative_features 戻り値の columns に候補別 score の一時列名（coef_0.0 / coef_0.1 / coef_0.5 / adjusted_score_internal 等）が含まれないことを assert するテストが存在する
    - **W-2**: src/features/race_relative.py に `def compute_candidate_score_diagnostics(feature_matrix: pd.DataFrame, split_mask=None) -> dict:` が存在し・候補 {0.0,0.1,0.25,0.5} の adjusted_score 分布統計（mean/std/min/max/p10/p50/p90 + rank 統計）を dict で返す・test_feature_matrix の列を破壊しない（copy 使用）。PLAN 06 run_phase10_evaluation.py がこの関数を消費し train/calib 窓の分布を log 出力する（係数妥当性証跡・REVIEWS.md L86 actionable・§11.2 聖域遵守）
    - ruff lint GREEN: `uv run ruff check src/features/race_relative.py tests/features/test_race_relative.py`
  </acceptance_criteria>

  <done>
    compute_race_relative_features が feature_matrix（target observation のみ）に 6 列（speed_index_rank 3軸・gap_to_top・gap_to_3rd・field_strength_adjusted_rank）を付与する。D-10 competition ranking・D-09 欠損馬除外・D-08 gap（top/3rd・Open Question #2 解決）・D-11/D-12 additive score（0.25 canonical 事前登録・候補 {0.0,0.1,0.25,0.5} 公開定数・§11.2 聖域）が実装され・W-5 候補別一時列の非残存（戻り値 columns に候補別 score 列が含まれない）が機械保証され・W-2 diagnostic helper（compute_candidate_score_diagnostics）が係数妥当性証跡を PLAN 06 に提供し・単体テスト12項目 GREEN。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| feature_matrix（target obs）→ race_relative 出力 | feature_matrix の rolling_speed_figure_mean_5 / rolling_field_strength_mean_mean_5 等が PLAN 02 / Phase 9.1 で正しく付与済みである前提 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-10 | Elevation of privilege | 係数 test 窓すり替え（§11.2 聖域）・**候補別 score 一時列の漏出** | high | mitigate | ADJUSTED_RANK_COEF_CANDIDATES/CANONICAL を事前登録公開定数化・test 窓は canonical(0.25) のみ評価・候補別は内部計算用の一時列で関数末尾に drop・**W-5: 戻り値 columns に候補別一時列が残存しないことを機械保証テスト（Test 11）で保護** |
| T-10-11 | Tampering | FEAT-03 の過去走への誤適用（Pitfall 4） | high | mitigate | compute_race_relative_features は feature_matrix（target obs のみ）に動作・history には適用しない・target-only を docstring と単体テストで保証 |
| T-10-12 | Tampering | rank 母集団への future-information 混入 | high | mitigate | rank 母集団は feature_cutoff_datetime 時点で確定した rolling_speed_figure_mean_5 等（Phase 9.1 PLAN 02 で strict `<cutoff` 済み）のみ・当日結果不使用 |
| T-10-13 | Information disclosure | odds/ninki proxy の feature 混入（SAFE-01 違反） | high | mitigate | 入力は speed_figure float 系のみ・adversarial AST audit（PLAN 07）が odds/ninki/fukuodds/ninkij/tansyouodds Name/Attribute/Constant 0件を証明 |
| T-10-14 | Tampering | 同着判定の誤り（D-10） | medium | mitigate | pandas.Series.rank(method="min") で competition ranking を機械保証・[100, 95, 95, 90] → [1, 2, 2, 4] の単体テスト |
</threat_model>

<verification>
- 単体テスト: `uv run pytest tests/features/test_race_relative.py -x -q` GREEN
- lint: `uv run ruff check src/features/race_relative.py tests/features/test_race_relative.py` GREEN
- SAFE-01 下地: src/features/race_relative.py に odds/ninki/fukuodds/ninkij/tansyouodds の token が現れない（adversarial AST audit 本体は PLAN 07）
</verification>

<success_criteria>
- FEAT-03 レース内相対特徴量 6 列が target observation のみ・race_id group-by・competition ranking で算出される
- SC#2 race_id 単位・同着（D-10）・欠損（D-09）の境界処理が明文化され機械保証される
- D-11/D-12 additive score 係数が 0.25 事前登録 canonical で・候補集合 {0.0, 0.1, 0.25, 0.5} が公開定数（§11.2 聖域）
- Open Question #2（gap top/3rd 定義・top=1位・3rd=3位・欠損除外・出走馬<3は NaN）が解決され PLAN に明文化される
- W-5: 候補別 score の一時列が戻り値 columns に残存しないことが機械保証される（§11.2 聖域保護・test 窓すり替え防止）
- W-2: compute_candidate_score_diagnostics が候補 {0.0,0.1,0.25,0.5} の adjusted_score 分布統計を dict で返し・PLAN 06 run_phase10_evaluation.py が train/calib 窓の分布を log 出力して係数妥当性の証跡を残す（§11.2 聖域遵守・test 窓すり替えではない）
</success_criteria>

<output>
Create `.planning/phases/10-opponent-strength-race-relative-features/10-03-SUMMARY.md` when done
</output>

<artifacts_produced>
## Artifacts this phase produces

新規シンボル（plan-review-convergence の source-grounding pass が新規作成として除外すべき対象）:

- **モジュール**: `src/features/race_relative.py`（新規）
- **公開関数**: `compute_race_relative_features(feature_matrix: pd.DataFrame) -> pd.DataFrame` / `compute_candidate_score_diagnostics(feature_matrix: pd.DataFrame, split_mask=None) -> dict`（W-2 係数妥当性証跡・PLAN 06 が消費）
- **内部 helper**: `_competition_rank_desc_within_race(s)` / `_adjusted_score(mean5, fs_mean5, coef)`
- **定数**: `ADJUSTED_RANK_COEF_CANDIDATES = (0.0, 0.1, 0.25, 0.5)` / `ADJUSTED_RANK_COEF_CANONICAL = 0.25` / `_SPEED_INDEX_AXES_FOR_RANK`（3軸 tuple）
- **出力列（6 feature・target-only・rolling_ prefix 無し）**: `speed_index_rank_mean5` / `speed_index_rank_best2_mean5` / `speed_index_rank_median5` / `gap_to_top` / `gap_to_3rd` / `field_strength_adjusted_rank`
- **テストファイル**: `tests/features/test_race_relative.py`（新規・12 テスト・tie 仕様テスト含む）
</artifacts_produced>
