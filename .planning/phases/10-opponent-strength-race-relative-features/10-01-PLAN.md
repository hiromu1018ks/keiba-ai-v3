---
phase: 10-opponent-strength-race-relative-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/field_strength.py
  - tests/features/test_field_strength.py
autonomous: true
requirements: [FEAT-02, SAFE-01]
granularity: fine

must_haves:
  truths:
    # SC#1（as-of 厳格版・D-01）・REVIEW H1/H2 (10-REVIEWS.md L48-49, L195-196)・CYCLE-2 HIGH-C2-1 (10-REVIEWS.md L57-92, L171-209, L295)
    - "history の各行に field_strength profile 8値（mean/median/top3_mean/top5_mean/max/sd/valid_count/coverage）が付与される（D-06 第1段階・FEAT-02）"
    - "相手 rolling 能力は opponent.available_at < source_race.available_at (strict `<`) を満たす opponent 過去走のみで算出される（D-01 厳格版・opponent-vs-source 保証）"
    # CYCLE-2 HIGH-C2-1 (value-invariance・root cause): source-vs-target-cutoff gate は行包含でなく「値の不変性」で保証する
    - "**CYCLE-2 HIGH-C2-1 (10-REVIEWS.md L57-92, L171-209, L295)** 相手の ability 値は target observation の feature_cutoff_datetime でなく source race 自身の available_at を cutoff として **FULL par + variant + speed_figure pipeline を通して再計算** される（`compute_speed_figure_for_history` を raw history に `obs_id='SOURCE_ASOF_<race_nkey>_<kettonum>'`・`feature_cutoff_datetime=source_race.available_at` の合成 observation で呼出）・同じ pre-source opponent race の speed_figure はどの target observation が消費しても同一（値の不変性）・source race < target cutoff の間に走った opponent レースが値・行ともに混入しない（adversarial value-invariance test が機械保証・行包含のみの Cycle-1 H2 gate は不十分と判明）"
    # CYCLE-2 HIGH-C2-1 前提: obs_id 展開済み speed_figure は target-cutoff-contaminated なので再利用禁止
    - "**CYCLE-2 HIGH-C2-1 前提**: `compute_speed_figure_for_history(history, observations=feature_matrix)` の戻り値は `obs_id` 展開済みで各行の par_sec/variant_sec/speed_figure が target cutoff に依存する（`speed_figure.py` L350-362/L402/L509-510/L655-657/L691・10-REVIEWS.md L181-186）・これを source-race opponent 流用すると (source, target] 区間の opponent レースが値レベルで混入する・よって汚染されていない **raw history（Step 5b 前・obs_id 未展開）** から full-pipeline 再計算する"
    # CYCLE-3 MEDIUM #2 (par-scale 設計選択・horse-level par 採用)
    - "**CYCLE-3 MEDIUM #2 (10-REVIEWS.md L222 対応・par-scale 設計選択・非リーク)** synth_obs の obs_id は **race×horse 単位** `SOURCE_ASOF_<race_nkey>_<kettonum>` とする（target 経路の obs_id `(race_nkey, kettonum)`・`builder.py` L502-514 と対称）。これにより `compute_speed_figure_for_history` の par groupby `['obs_id','_jyocd','_trackcd','_kyori']`（`speed_figure.py` L401-402）が各馬毎に独立し・par は **その馬自身の pre-cutoff history の median（horse-level par）** となる。target 経路と同じ normalization なので `field_strength_adjusted_rank = rank(rolling_speed_figure_mean_5 + 0.25 * rolling_field_strength_mean_mean_5)`（PLAN 03）の加算項が同一 scale 上で成立する。race 単位 obs_id（field-level par）は採用しない（相手能力の集約は profile 8値計算ステージで行い・par 計算の粒度と混同しない）。value-invariance は obs_id が source race × opponent horse 毎に独立なことで構造的に保持される（10-REVIEWS.md L181-186 の par/variant groupby 先頭キー obs_id の性質は horse 単位でも成立）。"
    # D-02 事前登録（Open Question #1 解決・mean_5 1軸）
    - "相手個人の rolling 能力は rolling_speed_figure_mean_5（Phase 9 D-09 安定能力代表値・source race as-of で再計算）1軸のみで測る（D-02 具象・17倍計算量抑制）"
    # D-04 発走馬特定
    - "発走馬は kakuteijyuni > 0 のみ（未発走 kakuteijyuni=0 は除外・競走中止馬 kakuteijyuni=11-16 は含む）・tozaicd は使わない（D-04・live-DB 実証済）"
    # D-03/D-05 profile
    - "source race 内 opponent profile 8値の top3_mean/top5_mean は k = min(k, valid_opponents) でクランプし valid_count/coverage を必ず保持する（D-03/D-05）"
    # SAFE-01
    - "field_strength.py のソース AST に odds/ninki/fukuodds/ninkij/tansyouodds の Name/Attribute/Constant が0件である（SAFE-01・SC#4 下地）"
    # §19.1
    - "compute_field_strength_profile は決定論的（同一入力で bit-identical・vectorized groupby + nlargest + 固定 sort 順序）"
  artifacts:
    - "src/features/field_strength.py に compute_field_strength_profile(raw_history, observations=None) -> pd.DataFrame が存在（第1引数は raw_history・obs_id 展開済みでない Step 5b 前の生 history・CYCLE-2 HIGH-C2-1）"
    - "src/features/field_strength.py に _pit_cutoff_prefilter(expanded) helper が存在（adversarial test が monkeypatch で <= に差替可能・opponent-vs-source gate）"
    - "src/features/field_strength.py に _compute_source_asof_opponent_speed_figures(raw_history, source_available_at_by_race) helper が存在（**CYCLE-2 HIGH-C2-1 root-cause fix**・raw history に対し source race の available_at を cutoff として full par+variant+speed_figure pipeline を再実行する `compute_speed_figure_for_history` の wrapper・target cutoff で展開された obs_id-expanded speed_figure は再利用しない）"
    - "src/features/field_strength.py に _build_source_asof_observation(source_race_rows) helper が存在（CYCLE-2 HIGH-C2-1 + CYCLE-3 MEDIUM #2・obs_id='SOURCE_ASOF_<race_nkey>_<kettonum>'（race×horse 単位・horse-level par）・feature_cutoff_datetime=source_race.available_at の synthetic observation を構築し full-pipeline 再計算の入力にする・各行に available_at 列を RaceLevel ではなく horse 毎に付与）"
    - "src/features/field_strength.py に _opponent_ability_latest_mean5(source_asof_speed_figures, source_available_at) helper が存在（CYCLE-2 HIGH-C2-1・full-pipeline 再計算済み speed_figure 値から opponent 毎に latest-K=5 の mean を算出・source cutoff を厳格適用）"
    - "src/features/field_strength.py に _topk_mean_clamped(values, k) helper が存在（D-05 クランプ）"
    - "tests/features/test_field_strength.py が存在し全テスト GREEN（下記 adversarial 3種・value-invariance を含む）"
  key_links:
    - "compute_field_strength_profile は src.features.availability.CUTOFF_SEMANTICS を import し comparison_operator == 'strict_less_than' を実行時 assert"
    - "_pit_cutoff_prefilter は speed_figure.py L98-118 / rolling.py L114-126 と対称（同一不変量・guard 無効化可能・opponent-vs-source）"
    - "**CYCLE-2 HIGH-C2-1**: _compute_source_asof_opponent_speed_figures は obs_id 展開済みの target-cutoff-contaminated な speed_figure（compute_speed_figure_for_history の observations= 渡し戻り値）を一切再利用せず・raw history（Step 5b 前）に対し source race の available_at を cutoff とする合成 observation で compute_speed_figure_for_history を再実行し par+variant+speed_figure を source-as-of で再計算する（10-REVIEWS.md L89-92, L199-205）"
    - "**CYCLE-2 HIGH-C2-1 value-invariance**: 同じ pre-source opponent race の再計算済み speed_figure は・どの target observation がその source race を消費しても同一（値の不変性）・これが行包含でなく値レベルの source-vs-target-cutoff 保証（10-REVIEWS.md L207-209）"
    - "出力 history は入力 history の copy に 8 列を追加（copy-not-rename・HIGH#5 踏襲）・入力列は破壊しない"
---

<objective>
Phase 10 PLAN 01: FEAT-02 相手強度 `field_strength` の D-06 第1段階（source race 内 opponent profile 8値）を新規 `src/features/field_strength.py` に実装する。

Purpose: 相手強度特徴量の基盤となる中間値（profile 8値）を history 全行に付与する。第2段階（PLAN 02 で rolling.py 拡張）がこの profile 列を入力に 21 feature を生成する。D-01 厳格版 as-of（strict `<`）と D-04 発走馬特定（kakuteijyuni > 0）を機械保証し・core value「リーク防止最優先」を最も厳格に問う。

Output: src/features/field_strength.py（compute_field_strength_profile + _pit_cutoff_prefilter + _topk_mean_clamped）・tests/features/test_field_strength.py（PIT strict < / 発走馬特定 / profile 8値 / top-k クランプ 単体テスト）
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-opponent-strength-race-relative-features/10-CONTEXT.md
@.planning/phases/10-opponent-strength-race-relative-features/10-RESEARCH.md
@.planning/phases/10-opponent-strength-race-relative-features/10-PATTERNS.md

# analog（必読・PATTERNS.md が line 指示）
@src/features/speed_figure.py
@src/features/rolling.py
@src/features/availability.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: compute_field_strength_profile（D-06 第1段階）を vectorized で実装</name>
  <files>src/features/field_strength.py</files>

  <read_first>
    - src/features/speed_figure.py L1-46（モジュール docstring 3聖域・CUTOFF_SEMANTICS 実行時 assert の idiom）
    - src/features/speed_figure.py L98-118（_pit_cutoff_prefilter helper・adversarial monkeypatch 対象）
    - **CYCLE-2 HIGH-C2-1 必読（obs_id 展開 = target-cutoff-contamination の証拠）**: src/features/speed_figure.py L343-378（_compute_pit_par docstring・par groupby キーが obs_id 先頭・observation 毎に独立 par）・L401-402（par Stage1 groupby key = ["obs_id","_jyocd","_trackcd","_kyori"]）・L509-510（variant Stage1 groupby key = ["obs_id","_source_race_date","_jyocd","_surface"]）・L633-658（obs_id 展開: out.merge(obs_keys, on="kettonum")・result = with_variant で展開フレームを保持・L655-657 コメント「obs_id 毎に par/variant が異なるため」）・L683-695（speed_figure = (par_sec - time_sec + variant_sec) * pps・obs_id 毎の par/variant を継承）・これらが「入力 history の obs_id 展開済み speed_figure は target cutoff 依存・source-race opponent 流用禁止（CYCLE-2 HIGH-C2-1）」の根拠
    - src/features/speed_figure.py L566-580・L624-658（compute_speed_figure_for_history 公開 API・observations= 引数で obs_id 展開・本 PLAN が raw history に synth_obs を渡して full-pipeline を source-as-of で再実行するために再利用）
    - src/features/rolling.py L74-104（_ROLLING_SYSTEMS + _SYSTEM_SOURCE idiom）
    - src/features/rolling.py L114-126（rolling 側の _pit_cutoff_prefilter・対称 idiom）
    - src/features/rolling.py L183-192（_best2_mean_of_group・nlargest + 決定論的 idiom）
    - src/features/rolling.py L375-383（per-observation latest-K: sort_values + groupby.head(5) idiom）
    - src/features/availability.py（CUTOFF_SEMANTICS 定数）
    - .planning/phases/10-opponent-strength-race-relative-features/10-RESEARCH.md L268-327（Pattern 1 per-source-race batch + vectorized groupby skeleton）・L473-518（compute_field_strength_profile skeleton）・L412-461（Pitfall 1/2/3・tozaicd 警告）
    - .planning/phases/10-opponent-strength-race-relative-features/10-PATTERNS.md L35-123（field_strength.py の docstring/helper/集約 idiom）
    - .planning/phases/10-opponent-strength-race-relative-features/10-REVIEWS.md L57-92・L171-209・L295（**CYCLE-2 HIGH-C2-1 root cause・なぜ行包含 gate が不十分で値レベル再計算が必要かの完全な説明・実装前に必読**）
  </read_first>

  <behavior>
    - Test 1 (PIT strict `<` opponent-vs-source・D-01): source race と同日の opponent speed_figure（available_at == self.available_at）は profile に混入しない・monkeypatch で _pit_cutoff_prefilter を `<=` 版に差し替えると T+1 opponent が混入して値が変化する（adversarial）
    - Test 2 (**CYCLE-2 HIGH-C2-1 value-invariance・adversarial・最重大・10-REVIEWS.md L57-92, L181-209, L295**): 同じ pre-source opponent race が・異なる2つの target observation（feature_cutoff_datetime = T1 < T2・両方とも source race available_at=S より後・かつ (S, T2] に当該 opponent の強い追加レースが存在）のどちらを消費しても・再計算された speed_figure が **値レベルで bit-identical（値の不変性）** になることを assert。これが行包含でなく値レベルの source-vs-target-cutoff 保証の核心（Cycle-1 の行包含テストは不十分と判明・10-REVIEWS.md L83-88）。本テストは _compute_source_asof_opponent_speed_figures が raw history に source_available_at=S で full par+variant+speed_figure pipeline を再実行することで値不変性が達成されることを直接証明する。monkeypatch で再計算 cutoff を target cutoff(T2) に差し替えると・(S, T2] の opponent 追加レースが par/variant 計算に混入し値が変化することを assert（guard 有効の逆証明）。
    - Test 3 (**CYCLE-2 HIGH-C2-1 obs_id-expanded 再利用禁止 + CYCLE-3 MEDIUM #1・10-REVIEWS.md L181-186, L221**): compute_field_strength_profile は入力 history の既存 speed_figure 列（compute_speed_figure_for_history が observations= 渡しで付与した obs_id 展開済み target-cutoff-contaminated 値）および available_at 列（同 L698 で付与）を一切再利用せず・raw history（Step 5b 前・obs_id 未展開・speed_figure/available_at は非存在）から _compute_source_asof_opponent_speed_figures で full-pipeline 再計算し・available_at は関数内で `pd.to_datetime(raw_history["race_date"])` から導出する。検証: (a) 入力 history の speed_figure 列を人為的に汚染（全行 +1000 等）しても出力 profile が変化しないこと・(b) 入力 history に available_at 列が存在しない状態で呼出しても正常に動作し出力が一致すること・を assert（obs_id-expanded 値および available_at 列への非依存の機械保証・10-REVIEWS.md L207-209 の value-invariance の補強 + MEDIUM #1）。
    - Test 4 (発走馬特定・D-04): kakuteijyuni=0（未発走）は相手から除外・kakuteijyuni=11-16（競走中止馬）は相手に含む・tozaicd は使わない
    - Test 5 (D-02 相手 rolling mean_5 1軸): 相手個人の rolling 能力は rolling_speed_figure_mean_5 のみで算出される（他 axis は使わない・Open Question #1 事前登録・source as-of 再計算）
    - Test 6 (D-03 profile 8値): source race 内 opponent の mean/median/top3_mean/top5_mean/max/sd/valid_count/coverage が正しく算出される
    - Test 7 (D-05 top-k クランプ): opponent が2頭の場合 top3_mean は上位2件の平均（min(3, 2)=2 でクランプ）・top5_mean も同様
    - Test 8 (coverage・D-05): coverage = valid_count / race_size[race_nkey]・race_size は starters（kakuteijyuni > 0）の race_nkey group size
    - Test 9 (copy-not-rename・HIGH#5): 入力 history の既存列は破壊されず・8 列が copy 追加される
    - Test 10 (byte-reproducible・§19.1): 同一入力で2回呼出すと bit-identical（np.array_equal 等価）・sort 順序は固定（kind="mergesort" 等の決定論的順序）
    - Test 11 (**CYCLE-3 MEDIUM #1 対応・fail-loud・10-REVIEWS.md L221**): 必須列（kakuteijyuni/race_nkey/kettonum/race_date/time/trackcd/jyocd/kyori/as_of_datetime）欠落時に raise ValueError/RuntimeError。**raw_history（Step 5b 前）には speed_figure も available_at も存在しない**（`builder.py` L480 `_fetch_history` → `_construct_derived_columns` L206-323 の派生列は `race_nkey`/`as_of_datetime`/`days_since_prev`/`timediff`/`babacd` のみ・`available_at` は `compute_speed_figure_for_history` が L698 で付与・`speed_figure` も同 L691 で付与）ため・speed_figure/available_at は必須列に含めない（これらを含めると実装者が「Step 5b 後の汚染 history から取る」で解消する脆弱な修正経路を開く）。compute_field_strength_profile が内部で `available_at = pd.to_datetime(raw_history["race_date"])` を導出することで raw_history の列としての available_at 非存在を補い・関数内完結で source cutoff を race_date 由来に固定する（MEDIUM #1 の本質的解決）。
  </behavior>

  <action>
    src/features/field_strength.py を新規作成する。構成要素（PATTERNS.md L35-123 の idiom と対称）。**REVIEW H1/H2 (10-REVIEWS.md L48-49, L195-196) 対応**: 相手の rolling_speed_figure_mean_5 は history の既存列に依存せず・各 source race の available_at を as-of cutoff として再計算する（target cutoff でなく source cutoff・source-vs-target-cutoff lookahead の構造的排除）。

    (1) モジュール docstring 3聖域 + CYCLE-2 HIGH-C2-1 対応明記（SAFE-01 odds-free / PIT 厳格版 as-of D-01（opponent-vs-source AND source-vs-target-cutoff の2層・後者は値の不変性で保証）/ byte-reproducible §19.1）・speed_figure.py L1-32 と対称な形式。**CYCLE-2**: source-vs-target-cutoff 保証は行包含でなく値レベル（同じ pre-source opponent race がどの target に消費されても同一 speed_figure）で保証することを明記。

    (2) imports: `from __future__ import annotations`・`import numpy as np`・`import pandas as pd`・`from src.features.availability import CUTOFF_SEMANTICS`・`from src.features.speed_figure import compute_speed_figure_for_history`（**CYCLE-2 HIGH-C2-1**: full-pipeline 再計算のために import・target-cutoff で展開済みの戻り値でなく raw history を source-as-of cutoff で再実行するために使用）。実行時 assert `CUTOFF_SEMANTICS["comparison_operator"] == "strict_less_than"`（speed_figure.py L46 idiom）。

    (3) D-02 事前登録定数（Open Question #1 解決）: `OPPONENT_ROLLING_AXIS = "rolling_speed_figure_mean_5"`・`OPPONENT_ROLLING_K = 5`（latest-K 窓・Phase 9 D-09 と同値）を定義。相手個人の rolling 能力はこの1軸のみ（17倍計算量抑制・Phase 9 D-09 安定能力代表値）。**CYCLE-2 HIGH-C2-1**: この axis は入力 history の既存 speed_figure 列（obs_id 展開済み・target-cutoff-contaminated）から読まず・_compute_source_asof_opponent_speed_figures で raw history に full-pipeline を再実行して得られた source-as-of 再計算 speed_figure 値に対して latest-K=5 を適用する対象であることを docstring で明記。

    (4) `_pit_cutoff_prefilter(expanded: pd.DataFrame) -> pd.DataFrame` helper（speed_figure.py L98-118 と対称・opponent-vs-source gate・adversarial test が monkeypatch で `<=` に差替可能）: 列 `_opp_available_at < available_at` の strict `<` で filter（self.available_at = source race の available_at・opponent.available_at < self.available_at が PIT 不変量・D-01）。

    (5) **CYCLE-2 HIGH-C2-1 root-cause fix + CYCLE-3 MEDIUM #3 pre-merge cutoff pre-filter** `_compute_source_asof_opponent_speed_figures(raw_history: pd.DataFrame, source_available_at_by_race: pd.Series) -> pd.DataFrame` helper:
        - 入力: raw_history（Step 5b 前・obs_id 未展開・`compute_speed_figure_for_history` 未適用の生 history・必須列: kettonum/race_nkey/race_date/time/trackcd/jyocd/kyori/as_of_datetime・**CYCLE-3 MEDIUM #1: speed_figure も available_at も存在しない・available_at は本関数が `pd.to_datetime(race_date)` で導出**）と・各 source race（race_nkey）の available_at（= race_date・target cutoff でない）。
        - **核心**: 入力の obs_id 展開済み speed_figure（target-cutoff-contaminated・`compute_speed_figure_for_history(history, observations=feature_matrix)` の戻り値・各行が target obs の feature_cutoff_datetime に依存する par/variant/speed_figure を持つ・10-REVIEWS.md L181-186）を一切再利用しない。代わりに _build_source_asof_observation で構築した合成 observation（obs_id='SOURCE_ASOF_<race_nkey>_<kettonum>'・feature_cutoff_datetime=source_race.available_at・**CYCLE-3 MEDIUM #2 horse-level par**）を用い・raw_history に対し `compute_speed_figure_for_history(raw_history, observations=synth_obs)` を呼出すことで par+variant+speed_figure の **FULL pipeline** を source-as-of cutoff で再実行する。これが値レベルの source-vs-target-cutoff 保証（値の不変性）の唯一の達成方法（10-REVIEWS.md L89-92, L199-205）。
        - **CYCLE-3 MEDIUM #3 pre-merge cutoff pre-filter (10-REVIEWS.md L223・cardinality 爆発回避)**: 全 source race の synth_obs を1連結して compute_speed_figure_for_history を1回呼ぶと・`speed_figure.py` L635 `out.merge(obs_keys, on="kettonum")` が cutoff filter（L641）の **前** に全馬 H について「H の履歴行 × H を含む source race 数」の積を materialize し・馬平均50走 × ~1万頭で2500万行超に達する。これを回避するため・synth_obs を **per-source-race（または source race の小バッチ）に分割** し・各バッチ毎に compute_speed_figure_for_history を呼出す構造（バッチ内の merge は当該 source race 群の starter に限定され H² 積を回避）。バッチ化が値の不変性を損なわないことは obs_id が source×horse 毎に独立（CYCLE-3 MEDIUM #2）なことで構造的に保証（par/variant groupby 先頭キー obs_id・10-REVIEWS.md L181-186）。バッチサイズ（例: source race 100 件/バッチ）は定数 SOURCE_RACE_BATCH_SIZE として事前登録し・W-3 性能検証（PLAN 07）で本番規模の拡大性を実証する。
        - **CYCLE-3 MEDIUM #1 対応**: raw_history に available_at 列が存在しないため・本 helper の冒頭で `raw_history = raw_history.assign(available_at=pd.to_datetime(raw_history["race_date"]))` を導出する（`speed_figure.py` L698 と対称・target cutoff でない race_date 由来で固定）。これにより source_available_at_by_race も race_date 由来の available_at を持つ。実装者が「Step 5b 後の汚染 history から available_at を取る」脆弱な修正経路を構造的に閉じる。
        - 出力: source_race × opponent_kettonum × opponent_history_row の展開フレームで・各 opponent 過去走行の speed_figure 値が source-as-of（当該 source race の available_at cutoff）で再計算されたもの。これが値の不変性を保証された唯一の ability 値ソース。

    (6) **CYCLE-2 HIGH-C2-1 + CYCLE-3 MEDIUM #2** `_build_source_asof_observation(source_race_rows: pd.DataFrame) -> pd.DataFrame` helper:
        - 入力: 各 source race の代表行（race_nkey・kettonum・available_at を持つ starters・self を含む）。
        - 出力: 合成 observation DataFrame・列 = (obs_id, kettonum, feature_cutoff_datetime)。obs_id = `'SOURCE_ASOF_' + race_nkey.astype(str) + '_' + kettonum.astype(str)`（**race×horse 単位・CYCLE-3 MEDIUM #2 horse-level par**・target observation の obs_id と名前空間衝突しない接頭辞）・feature_cutoff_datetime = source_race.available_at（target cutoff でない・これが source-vs-target-cutoff gate の本体・race_date 由来で固定・CYCLE-3 MEDIUM #1）・kettonum = 当該 source race の各 starter（horse と race の組毎に1行）。
        - この synth_obs を compute_speed_figure_for_history に渡すことで・par groupby キー(obs_id)・variant groupby キー(obs_id) が source race × opponent horse 毎に独立になり・各 (source race, horse) の par/variant/speed_figure がその source race の available_at cutoff と horse 自身の pre-cutoff history のみで決定される（target cutoff 非依存・値の不変性・**CYCLE-3 MEDIUM #2: target 経路の horse-level par と同 normalization**）。

    (7) `_opponent_ability_latest_mean5(source_asof_speed_figures: pd.DataFrame, source_available_at_by_race: pd.Series) -> pd.DataFrame` helper:
        - 入力: _compute_source_asof_opponent_speed_figures の出力（source-as-of 再計算済み speed_figure 値を持つ展開フレーム）と source race の available_at。
        - 各 opponent kettonum × source_race について・`opponent_hist.available_at < source_available_at`（strict `<`・opponent-vs-source・D-01）を満たす行から再計算済み speed_figure を取り出し latest-K=5（OPPONENT_ROLLING_K）の平均を計算。
        - vectorized 実装: (source_race, opponent_kettonum, opponent_available_at) をキーに sort_values DESC + groupby.head(K) で最新5件を取得し mean（rolling.py L375-383 idiom 転用）・純粋 Python ループ禁止（Pitfall 2）。
        - **値レベル lookahead 排除**: speed_figure 値自体が source-as-of で再計算済み（step 5）・かつ opponent_vs_source filter で source 以後の opponent レースを行レベルで除外する2層構造。target cutoff は step 5/7 のいずれにも現れない。
        - 出力: (source_race_nkey, opponent_kettonum) → opponent_ability 値（= 当該 source race 時点での opponent の rolling_speed_figure_mean_5・source as-of）。

    (8) `_topk_mean_clamped(values: pd.Series, k: int) -> float` helper（rolling.py L183-192 _best2_mean_of_group idiom・nlargest で vectorized）: `valid = values.dropna()`・`actual_k = min(k, len(valid))`（D-05 クランプ）・`return float(valid.nlargest(actual_k).mean())`・`len(valid)==0` は NaN。

    (9) `compute_field_strength_profile(raw_history, observations=None) -> pd.DataFrame` メイン関数:
        - 入力検証: raw_history の必須列（kakuteijyuni/race_nkey/kettonum/race_date/time/trackcd/jyocd/kyori/as_of_datetime・**CYCLE-2 HIGH-C2-1: speed_figure は必須でない・target cutoff で展開された汚染値でなく本関数が raw history に full-pipeline を source-as-of で再実行して算出する・speed_figure 列が存在しても読み込まない**・**CYCLE-3 MEDIUM #1: available_at も必須でない・Step 5b 前 raw_history には存在せず本関数が `pd.to_datetime(race_date)` で導出**）を assert（fail-loud・Pitfall 6 silent data loss 回避・10-REVIEWS.md L221）。
        - **CYCLE-3 MEDIUM #1 対応**: raw_history に available_at 列が存在しないため・関数冒頭で `raw_history = raw_history.assign(available_at=pd.to_datetime(raw_history["race_date"]))` を導出（`speed_figure.py` L698 と対称・race_date 由来で固定・Step 5b 後汚染 history への依存経路を閉じる）。以降の step はこの race_date 由来 available_at を使用する。
        - copy-not-rename: `out = raw_history.copy()`（available_at 導出後）。
        - D-04 発走馬特定: `starters_mask = raw_history["kakuteijyuni"].fillna(0) > 0`・`starters = raw_history[starters_mask].copy()`・`source_races = starters[["race_nkey","kettonum","available_at"]].drop_duplicates()`（**CYCLE-3 MEDIUM #2: kettonum 含む・race×horse 単位**・CYCLE-3 MEDIUM #1: available_at は race_date 由来導出列）・`race_size = starters.groupby("race_nkey").size().to_dict()`（coverage 計算用）。
        - **CYCLE-2 HIGH-C2-1 pipeline 実行**:
          * (a) source_available_at_by_race = source_races.groupby("race_nkey")["available_at"].first() を構築（race 内で available_at は一意・race_date 由来）。
          * (b) source_asof_sf = _compute_source_asof_opponent_speed_figures(raw_history=raw_history, source_available_at_by_race=source_available_at_by_race) を呼出し・全 source race について source-as-of 再計算済み opponent speed_figure 値を一括取得（full par+variant+speed_figure pipeline・obs_id='SOURCE_ASOF_<race_nkey>_<kettonum>'・**CYCLE-3 MEDIUM #3 per-source-race バッチ化で H² 積 materialize 回避**）。
          * (c) opponent_ability = _opponent_ability_latest_mean5(source_asof_sf, source_available_at_by_race) で (source_race_nkey, opponent_kettonum) → opponent_ability（rolling_speed_figure_mean_5・source as-of）を算出。
        - per-source-race batch + vectorized groupby（RESEARCH Pattern 1 L268-327・Pitfall 2 計算量爆発回避・6.7M ペアを秒単位）:
          * starter × starter join で「同じ source race の他の starter」を展開・self（kettonum 同一）は除外。opponent は各 source race 内の starters のみ（D-04）。
          * 各 (source_race, opponent) について・opponent_ability[source_race, opponent] をルックアップして _opp_rolling_ability 列に設定（target cutoff 由来でなく source-as-of 再計算由来）。
          * _pit_cutoff_prefilter で `_opp_available_at < available_at` (strict `<`・opponent-vs-source) を追加適用（D-01・2層 gate）。
        - D-03 profile 8値 vectorized 集約: `expanded.groupby(["race_nkey", "kettonum"]).agg(...)` で mean/median/top3_mean（_topk_mean_clamped(s, 3)）/top5_mean（_topk_mean_clamped(s, 5)）/max/sd/valid_count を一括算出・coverage = valid_count / race_size[race_nkey]。
        - 出力: out に 8 列（field_strength_mean/median/top3_mean/top5_mean/max/sd/valid_count/coverage）を merge（race_nkey + kettonum キー）で copy 追加・該当なし行は NaN。
        - 決定論的順序: groupby/sort_values で `kind="mergesort"` または `sort=False` + 明示 reset_index で byte-reproducible を保証（§19.1）。

    (10) `__all__ = ["compute_field_strength_profile"]` を定義。

    D-04 tozaicd 警告: docstring またはコメントで「tozaicd は EveryDB2 独自拡張で意味不明（live-DB 実証: 1/2=完走馬大多数・3=競走中止190件・4=失格16件）・kakuteijyuni > 0 を使う」を明記（PAT§ Pitfall 1）。ただし docstring 内に _FORBIDDEN_TOKENS の文字列リテラルを書かないこと（adversarial AST audit で false positive を出すため・SAFE-01 は「市場情報不使用」等の別表現で書く）。

    **CYCLE-2 HIGH-C2-1 要点（実装者が必ず理解すべき点）**: `compute_speed_figure_for_history(history, observations=feature_matrix)` の戻り値（obs_id 展開済み・target-cutoff-contaminated・各行の par/variant/speed_figure が target obs の feature_cutoff_datetime に依存）を source-race opponent 流用すると (source, target] 区間の opponent レースが値レベルで混入する（10-REVIEWS.md L181-186）。本 PLAN は obs_id 展開済み speed_figure を一切再利用せず・raw history（Step 5b 前）に `obs_id='SOURCE_ASOF_<race_nkey>_<kettonum>'`・`feature_cutoff_datetime=source_race.available_at` の合成 observation で compute_speed_figure_for_history を再実行することで par+variant+speed_figure の FULL pipeline を source-as-of で再計算する。これが値の不変性（同じ pre-source opponent race がどの target に消費されても同一 speed_figure）を達成する唯一の方法（10-REVIEWS.md L89-92, L199-209）。
  </action>

  <verify>
    <automated>uv run pytest tests/features/test_field_strength.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - src/features/field_strength.py に `def compute_field_strength_profile(raw_history: pd.DataFrame, observations: pd.DataFrame | None = None) -> pd.DataFrame:` が存在（第1引数は raw_history・CYCLE-2 HIGH-C2-1）
    - src/features/field_strength.py に `def _pit_cutoff_prefilter(expanded: pd.DataFrame) -> pd.DataFrame:` が存在（opponent-vs-source gate）
    - src/features/field_strength.py に `def _compute_source_asof_opponent_speed_figures(raw_history, source_available_at_by_race)` helper が存在（**CYCLE-2 HIGH-C2-1 root-cause fix + CYCLE-3 MEDIUM #1/#3**・obs_id 展開済み speed_figure 再利用禁止・raw history に full par+variant+speed_figure pipeline を source-as-of cutoff で再実行・available_at は `pd.to_datetime(race_date)` で導出・synth_obs は per-source-race バッチ化で H² 積 materialize 回避）
    - src/features/field_strength.py に `def _build_source_asof_observation(source_race_rows)` helper が存在（**CYCLE-2 HIGH-C2-1 + CYCLE-3 MEDIUM #2**・obs_id='SOURCE_ASOF_<race_nkey>_<kettonum>'（race×horse 単位・horse-level par）・feature_cutoff_datetime=source_race.available_at の合成 observation 構築）
    - src/features/field_strength.py に `def _opponent_ability_latest_mean5(source_asof_speed_figures, source_available_at_by_race)` helper が存在（CYCLE-2 HIGH-C2-1・full-pipeline 再計算済み speed_figure から opponent 毎 latest-K=5 mean 算出）
    - src/features/field_strength.py に `def _topk_mean_clamped(values: pd.Series, k: int) -> float:` が存在
    - src/features/field_strength.py に `OPPONENT_ROLLING_AXIS = "rolling_speed_figure_mean_5"`・`OPPONENT_ROLLING_K = 5` 定数が事前登録される（Open Question #1 解決・D-02 具象）
    - src/features/field_strength.py が `from src.features.availability import CUTOFF_SEMANTICS` を import し `assert CUTOFF_SEMANTICS["comparison_operator"] == "strict_less_than"` を含む
    - src/features/field_strength.py が `from src.features.speed_figure import compute_speed_figure_for_history` を import する（CYCLE-2 HIGH-C2-1・full-pipeline 再計算のため）
    - src/features/field_strength.py に `kakuteijyuni` を含む（発走馬特定）が `tozaicd` を含まない（grep -c tozaicd == 0）
    <!-- planner-discipline-allow: tozaicd -->
    - src/features/field_strength.py に `nlargest` を含む（D-05 vectorized top-k クランプ）
    - **CYCLE-2 HIGH-C2-1 value-invariance 機械保証（10-REVIEWS.md L57-92, L181-209, L295）**: tests/features/test_field_strength.py に value-invariance adversarial テスト（同じ pre-source opponent race が異なる target observation に消費されても再計算 speed_figure が bit-identical・monkeypatch で再計算 cutoff を target cutoff に差替えると値が変化し (S,T] opponent レース混入を検出）が存在し GREEN・これが行包含でなく値レベルの source-vs-target-cutoff 保証
    - **CYCLE-2 HIGH-C2-1 obs_id-expanded 再利用禁止機械保証 + CYCLE-3 MEDIUM #1**: tests/features/test_field_strength.py に・入力 raw_history の speed_figure 列を人為的汚染（全行 +1000）しても出力 profile が変化しない（obs_id 展開済み汚染値への非依存）テスト・**および available_at 列を欠落させた raw_history で呼出しても race_date から available_at を導出し正常動作する**テストが存在し GREEN（10-REVIEWS.md L221）
    - **CYCLE-3 MEDIUM #2 (10-REVIEWS.md L222・par-scale 設計選択)**: src/features/field_strength.py の obs_id が `SOURCE_ASOF_<race_nkey>_<kettonum>`（race×horse 単位）であり・`compute_speed_figure_for_history` の par groupby キー `['obs_id','_jyocd','_trackcd','_kyori']` が各馬毎に独立して horse-level par（target 経路と同 normalization）となることを単体テストで検証（例: source race 内の opponent A と opponent B で par_sec がそれぞれの馬自身の pre-cutoff history 由来で異なり得ることを assert）
    - **CYCLE-3 MEDIUM #3 (10-REVIEWS.md L223・cardinality 回避)**: src/features/field_strength.py に `SOURCE_RACE_BATCH_SIZE` 定数（per-source-race バッチ化）が事前登録され・全 source race を1連結せずバッチ毎に compute_speed_figure_for_history を呼ぶことで `out.merge(obs_keys, on="kettonum")` の H² 積 materialize を回避することを単体テストで検証（バッチ境界で値が変わらないことも value-invariance で確認）
    - tests/features/test_field_strength.py の全テストが GREEN（PIT strict < opponent-vs-source / CYCLE-2 HIGH-C2-1 value-invariance / CYCLE-2 HIGH-C2-1 obs_id-expanded 再利用禁止 / 発走馬特定 / D-02 mean_5 1軸 / profile 8値 / top-k クランプ / copy-not-rename / byte-reproducible / fail-loud・11項目）
    - ruff lint GREEN: `uv run ruff check src/features/field_strength.py tests/features/test_field_strength.py`
  </acceptance_criteria>

  <done>
    compute_field_strength_profile が raw_history 全行に field_strength profile 8値を付与する。**CYCLE-2 HIGH-C2-1 対応（root-cause fix）**: 相手 ability 値は入力 history の obs_id 展開済み target-cutoff-contaminated speed_figure を一切再利用せず・raw history に `obs_id='SOURCE_ASOF_<race_nkey>_<kettonum>'`（**CYCLE-3 MEDIUM #2 race×horse 単位・horse-level par**）・`feature_cutoff_datetime=source_race.available_at` の合成 observation で compute_speed_figure_for_history を再実行することで full par+variant+speed_figure pipeline を source-as-of で再計算する。これにより値レベルの source-vs-target-cutoff 保証（同じ pre-source opponent race がどの target に消費しても同一 speed_figure・値の不変性）が達成される（行包含のみの Cycle-1 H2 gate は不十分と判明）。**CYCLE-3 MEDIUM #1**: available_at は raw_history の race_date から関数内導出し（Step 5b 後汚染 history への依存経路を閉じる）・**CYCLE-3 MEDIUM #3**: per-source-race バッチ化で `out.merge` の H² 積 materialize を回避する。opponent-vs-source gate（D-01 strict `<`）の 2 層 PIT 保証。D-04 発走馬特定（kakuteijyuni > 0）。adversarial value-invariance test が monkeypatch で値レベル PIT 違反を検出できる。D-02 相手 rolling は mean_5 1軸に事前登録（Open Question #1 解決）。単体テスト11項目 GREEN。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| history 入力 → field_strength 出力 | history の kakuteijyuni/race_nkey/kettonum/race_date/time/trackcd/jyocd/kyori/as_of_datetime が信頼できる正規化済データである前提（V5）・**CYCLE-3 MEDIUM #1**: available_at/speed_figure は入力に存在せず本関数が導出/再計算する |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | compute_field_strength_profile の opponent-vs-source PIT filter | critical | mitigate | _pit_cutoff_prefilter で opponent.available_at < self.available_at (strict `<`)・adversarial test が monkeypatch で guard を `<=` に差替 T+1 opponent 混入を検出 |
| T-10-01b | Tampering | **CYCLE-2 HIGH-C2-1 (10-REVIEWS.md L57-92, L171-209, L295)** target-cutoff-contaminated `obs_id`-expanded speed_figure を source-race opponent 流用で再利用すると (source, target] 区間の opponent レースが par/variant/speed_figure の **値レベル** で混入・より重大なリークパス・Cycle-1 H2 の行包含 gate は不十分（汚染値を行 filter しても un-contaminate できない） | critical | mitigate | **full-pipeline source-as-of 再計算**: _compute_source_asof_opponent_speed_figures が obs_id 展開済み speed_figure を一切再利用せず・raw history に obs_id='SOURCE_ASOF_<race_nkey>_<kettonum>'・feature_cutoff_datetime=source_race.available_at の合成 observation で compute_speed_figure_for_history を再実行し par+variant+speed_figure 全体を source-as-of で再計算・値レベルの source-vs-target-cutoff 保証（値の不変性）を達成・adversarial value-invariance test（同じ pre-source opponent race が異なる target に消費されても再計算 speed_figure が bit-identical・monkeypatch で再計算 cutoff を target に差替えると値変化検出）で機械保証（Test 2） |
| T-10-01c | Information disclosure | **CYCLE-2 HIGH-C2-1 前提 + CYCLE-3 MEDIUM #1** obs_id 展開済み speed_figure の非依存（history の speed_figure 列が target-cutoff-contaminated・これを source-race opponent 流用は禁止）・および available_at 列の非依存（Step 5b 前の raw_history には存在せず・Step 5b 後汚染 history から読むと汚染フレームへの依存経路を開く） | high | mitigate | compute_field_strength_profile は入力 raw_history の既存 speed_figure 列および available_at 列を読み込まない（どちらも必須列から除外）・available_at は関数内で `pd.to_datetime(race_date)` を導出（race_date 由来で固定）・_compute_source_asof_opponent_speed_figures が raw history に full-pipeline を再実行・入力 speed_figure 列を人為的汚染（全行 +1000）しても出力 profile が変化しないテスト・および available_at 列を欠落させた raw_history で呼出しても正常動作するテスト（Test 3・10-REVIEWS.md L221）で機械保証 |
| T-10-02 | Tampering | 発走馬特定（D-04）の誤判定 | high | mitigate | kakuteijyuni > 0 単一条件（live-DB 実証: time=0 と kakuteijyuni=0 が1516件完全一致）・tozaicd は使わない（EveryDB2 独自拡張で意味不明） |
| T-10-03 | Information disclosure | odds/ninki proxy の feature 混入（SAFE-01 違反） | high | mitigate | field_strength.py の入力は speed_figure float（source-as-of 再計算）のみ・adversarial AST audit（PLAN 07）が odds/ninki/fukuodds/ninkij/tansyouodds Name/Attribute/Constant 0件を証明 |
| T-10-04 | Tampering | 計算量爆発（6.7M ペア + source-as-of full-pipeline 再計算で数十分の低速化） | medium | mitigate | per-source-race batch + vectorized groupby（純粋ループ禁止・Pitfall 2）・**CYCLE-3 MEDIUM #3 (10-REVIEWS.md L223)**: pre-merge cutoff pre-filter として synth_obs を per-source-race（または小バッチ・SOURCE_RACE_BATCH_SIZE）に分割し compute_speed_figure_for_history を呼出すことで `out.merge(obs_keys, on="kettonum")` の H² 積 materialize（cutoff filter 前・2500万行超リスク）を回避・cProfile 検証は PLAN 07（W-3 縮小版閾値は温存しつつ production-scale smoke を追加） |
| T-10-06 | Tampering | **CYCLE-3 MEDIUM #2 (10-REVIEWS.md L222)** par-scale 不一致（race 単位 obs_id で field-level par になると target 経路 horse-level par と異なる scale になり `field_strength_adjusted_rank` の加算項が不正確化・非リーク） | medium | mitigate | obs_id を `SOURCE_ASOF_<race_nkey>_<kettonum>`（race×horse 単位）とし・target 経路（`builder.py` L502-514 の `(race_nkey, kettonum)`）と同 horse-level par に統一・value-invariance は obs_id が source×horse 毎に独立なことで構造的に保持・Test 2 で機械保証 |
| T-10-05 | Tampering | byte-reproducibility drift | medium | mitigate | 決定論的 groupby/sort（kind=mergesort 等の固定順序）・同一入力で bit-identical・test_byte_reproducible で機械保証 |
</threat_model>

<verification>
- 単体テスト: `uv run pytest tests/features/test_field_strength.py -x -q` GREEN
- lint: `uv run ruff check src/features/field_strength.py tests/features/test_field_strength.py` GREEN
- SAFE-01 下地: src/features/field_strength.py に tozaicd/odds/ninki/fukuodds/ninkij/tansyouodds の token が現れない（adversarial AST audit 本体は PLAN 07）
</verification>

<success_criteria>
- FEAT-02 第1段階（source race 内 opponent profile 8値）が vectorized で実装され・SC#1 PIT-correct 厳格版 as-of が機械保証される
- D-01/D-02（mean_5 1軸事前登録・Open Question #1 解決）/D-03/D-04/D-05 が実装され・単体テストで検証される
- PLAN 02（rolling.py 拡張 第2段階）が history の field_strength_* profile 列を前提にできる
</success_criteria>

<output>
Create `.planning/phases/10-opponent-strength-race-relative-features/10-01-SUMMARY.md` when done
</output>

<artifacts_produced>
## Artifacts this phase produces

新規シンボル（plan-review-convergence の source-grounding pass が新規作成として除外すべき対象）:

- **モジュール**: `src/features/field_strength.py`（新規）
- **公開関数**: `compute_field_strength_profile(raw_history: pd.DataFrame, observations: pd.DataFrame | None = None) -> pd.DataFrame`（第1引数は raw_history・obs_id 展開済みでない Step 5b 前の生 history・CYCLE-2 HIGH-C2-1）
- **内部 helper（CYCLE-2 HIGH-C2-1 root-cause fix + CYCLE-3 MEDIUM #1/#2/#3）**: `_pit_cutoff_prefilter(expanded)` / `_compute_source_asof_opponent_speed_figures(raw_history, source_available_at_by_race)`（full par+variant+speed_figure pipeline 再計算・available_at は race_date から導出・per-source-race バッチ化で H² 積 materialize 回避） / `_build_source_asof_observation(source_race_rows)`（obs_id='SOURCE_ASOF_<race_nkey>_<kettonum>'・race×horse 単位・horse-level par・feature_cutoff_datetime=source_race.available_at 合成 observation 構築） / `_opponent_ability_latest_mean5(source_asof_speed_figures, source_available_at_by_race)` / `_topk_mean_clamped(values, k)`
- **定数**: `OPPONENT_ROLLING_AXIS = "rolling_speed_figure_mean_5"` / `OPPONENT_ROLLING_K = 5` / `SOURCE_RACE_BATCH_SIZE`（CYCLE-3 MEDIUM #3・per-source-race バッチサイズ・事前登録）
- **import 新規**: `from src.features.speed_figure import compute_speed_figure_for_history`（CYCLE-2 HIGH-C2-1・full-pipeline 再計算のため）
- **obs_id 名前空間**: `SOURCE_ASOF_<race_nkey>_<kettonum>`（target observation の obs_id と衝突しない接頭辞・**CYCLE-3 MEDIUM #2: race×horse 単位・horse-level par**・source-as-of full-pipeline 再計算の group key）
- **CYCLE-3 MEDIUM #1 導出列**: available_at（`pd.to_datetime(race_date)`・関数内完結・Step 5b 後汚染 history への非依存・10-REVIEWS.md L221）
- **出力列（8値・raw_history の copy に追加）**: `field_strength_mean` / `field_strength_median` / `field_strength_top3_mean` / `field_strength_top5_mean` / `field_strength_max` / `field_strength_sd` / `field_strength_valid_count` / `field_strength_coverage`
- **テストファイル**: `tests/features/test_field_strength.py`（新規・11 テスト・CYCLE-2 HIGH-C2-1 value-invariance adversarial テストを含む）
</artifacts_produced>
