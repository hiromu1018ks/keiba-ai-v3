---
phase: 10-opponent-strength-race-relative-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/field_strength.py
  - tests/features/test_field_strength.py
autonomous: true
requirements: [FEAT-02, SAFE-01]
granularity: fine

must_haves:
  truths:
    # SC#1（as-of 厳格版・D-01）
    - "history の各行に field_strength profile 8値（mean/median/top3_mean/top5_mean/max/sd/valid_count/coverage）が付与される（D-06 第1段階・FEAT-02）"
    - "相手 rolling 能力は opponent.available_at < source_race.available_at (strict `<`) を満たす opponent 過去走のみで算出される（D-01 厳格版・未来情報の遡及注入なし）"
    # D-02 事前登録（Open Question #1 解決・mean_5 1軸）
    - "相手個人の rolling 能力は rolling_speed_figure_mean_5（Phase 9 D-09 安定能力代表値）1軸のみで測る（D-02 具象・17倍計算量抑制）"
    # D-04 発走馬特定
    - "発走馬は kakuteijyuni > 0 のみ（未発走 kakuteijyuni=0 は除外・競走中止馬 kakuteijyuni=11-16 は含む）・tozaicd は使わない（D-04・live-DB 実証済）"
    # D-03/D-05 profile
    - "source race 内 opponent profile 8値の top3_mean/top5_mean は k = min(k, valid_opponents) でクランプし valid_count/coverage を必ず保持する（D-03/D-05）"
    # SAFE-01
    - "field_strength.py のソース AST に odds/ninki/fukuodds/ninkij/tansyouodds の Name/Attribute/Constant が0件である（SAFE-01・SC#4 下地）"
    # §19.1
    - "compute_field_strength_profile は決定論的（同一入力で bit-identical・vectorized groupby + nlargest + 固定 sort 順序）"
  artifacts:
    - "src/features/field_strength.py に compute_field_strength_profile(history, observations=None) -> pd.DataFrame が存在"
    - "src/features/field_strength.py に _pit_cutoff_prefilter(expanded) helper が存在（adversarial test が monkeypatch で <= に差替可能）"
    - "src/features/field_strength.py に _topk_mean_clamped(values, k) helper が存在（D-05 クランプ）"
    - "tests/features/test_field_strength.py が存在し全テスト GREEN"
  key_links:
    - "compute_field_strength_profile は src.features.availability.CUTOFF_SEMANTICS を import し comparison_operator == 'strict_less_than' を実行時 assert"
    - "_pit_cutoff_prefilter は speed_figure.py L98-118 / rolling.py L114-126 と対称（同一不変量・guard 無効化可能）"
    - "出力 history は入力 history の copy に 8 列を追加（copy-not-rename・HIGH#5 踏襲）・入力列は破壊しない"
---

<objective>
Phase 10 PLAN 01: FEAT-02 相手強度 `field_strength` の D-06 第1段階（source race 内 opponent profile 8値）を新規 `src/features/field_strength.py` に実装する。

Purpose: 相手強度特徴量の基盤となる中間値（profile 8値）を history 全行に付与する。第2段階（PLAN 02 で rolling.py 拡張）がこの profile 列を入力に 21 feature を生成する。D-01 厳格版 as-of（strict `<`）と D-04 発走馬特定（kakuteijyuni > 0）を機械保証し・core value「リーク防止最優先」を最も厳格に問う。

Output: src/features/field_strength.py（compute_field_strength_profile + _pit_cutoff_prefilter + _topk_mean_clamped）・tests/features/test_field_strength.py（PIT strict < / 発走馬特定 / profile 8値 / top-k クランプ 単体テスト）
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-opponent-strength-race-relative-features/10-CONTEXT.md
@.planning/phases/10-opponent-strength-race-relative-features/10-RESEARCH.md
@.planning/phases/10-opponent-strength-race-relative-features/10-PATTERNS.md

# analog（必読・PATTERNS.md が line 指示）
@src/features/speed_figure.py
@src/features/rolling.py
@src/features/availability.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: compute_field_strength_profile（D-06 第1段階）を vectorized で実装</name>
  <files>src/features/field_strength.py</files>

  <read_first>
    - src/features/speed_figure.py L1-46（モジュール docstring 3聖域・CUTOFF_SEMANTICS 実行時 assert の idiom）
    - src/features/speed_figure.py L98-118（_pit_cutoff_prefilter helper・adversarial monkeypatch 対象）
    - src/features/rolling.py L74-104（_ROLLING_SYSTEMS + _SYSTEM_SOURCE idiom）
    - src/features/rolling.py L114-126（rolling 側の _pit_cutoff_prefilter・対称 idiom）
    - src/features/rolling.py L183-192（_best2_mean_of_group・nlargest + 決定論的 idiom）
    - src/features/rolling.py L375-383（per-observation latest-K: sort_values + groupby.head(5) idiom）
    - src/features/availability.py（CUTOFF_SEMANTICS 定数）
    - .planning/phases/10-opponent-strength-race-relative-features/10-RESEARCH.md L268-327（Pattern 1 per-source-race batch + vectorized groupby skeleton）・L473-518（compute_field_strength_profile skeleton）・L412-461（Pitfall 1/2/3・tozaicd 警告）
    - .planning/phases/10-opponent-strength-race-relative-features/10-PATTERNS.md L35-123（field_strength.py の docstring/helper/集約 idiom）
  </read_first>

  <behavior>
    - Test 1 (PIT strict `<`・D-01): source race と同日の opponent speed_figure（available_at == self.available_at）は profile に混入しない・monkeypatch で _pit_cutoff_prefilter を `<=` 版に差し替えると T+1 opponent が混入して値が変化する（adversarial）
    - Test 2 (発走馬特定・D-04): kakuteijyuni=0（未発走）は相手から除外・kakuteijyuni=11-16（競走中止馬）は相手に含む・tozaicd は使わない
    - Test 3 (D-02 相手 rolling mean_5 1軸): 相手個人の rolling 能力は rolling_speed_figure_mean_5 のみで算出される（他 axis は使わない・Open Question #1 事前登録）
    - Test 4 (D-03 profile 8値): source race 内 opponent の mean/median/top3_mean/top5_mean/max/sd/valid_count/coverage が正しく算出される
    - Test 5 (D-05 top-k クランプ): opponent が2頭の場合 top3_mean は上位2件の平均（min(3, 2)=2 でクランプ）・top5_mean も同様
    - Test 6 (coverage・D-05): coverage = valid_count / race_size[race_nkey]・race_size は starters（kakuteijyuni > 0）の race_nkey group size
    - Test 7 (copy-not-rename・HIGH#5): 入力 history の既存列は破壊されず・8 列が copy 追加される
    - Test 8 (byte-reproducible・§19.1): 同一入力で2回呼出すと bit-identical（np.array_equal 等価）・sort 順序は固定（kind="mergesort" 等の決定論的順序）
    - Test 9 (fail-loud): 必須列（kakuteijyuni/race_nkey/kettonum/speed_figure/available_at）欠落時に raise ValueError/RuntimeError
  </behavior>

  <action>
    src/features/field_strength.py を新規作成する。構成要素（PATTERNS.md L35-123 の idiom と対称）:

    (1) モジュール docstring 3聖域（SAFE-01 odds-free / PIT 厳格版 as-of D-01 / byte-reproducible §19.1）・speed_figure.py L1-32 と対称な形式。

    (2) imports: `from __future__ import annotations`・`import numpy as np`・`import pandas as pd`・`from src.features.availability import CUTOFF_SEMANTICS`。実行時 assert `CUTOFF_SEMANTICS["comparison_operator"] == "strict_less_than"`（speed_figure.py L46 idiom）。

    (3) D-02 事前登録定数（Open Question #1 解決）: `OPPONENT_ROLLING_AXIS = "rolling_speed_figure_mean_5"` を定義。相手個人の rolling 能力はこの1軸のみ（17倍計算量抑制・Phase 9 D-09 安定能力代表値）。

    (4) `_pit_cutoff_prefilter(expanded: pd.DataFrame) -> pd.DataFrame` helper（speed_figure.py L98-118 と対称・adversarial test が monkeypatch で `<=` に差替可能）: `expanded["_opp_available_at"] < expanded["available_at"]` の strict `<` で filter（注: self.available_at = source race の available_at・opponent.available_at < self.available_at が PIT 不変量）。

    (5) `_topk_mean_clamped(values: pd.Series, k: int) -> float` helper（rolling.py L183-192 _best2_mean_of_group idiom・nlargest で vectorized）: `valid = values.dropna()`・`actual_k = min(k, len(valid))`（D-05 クランプ）・`return float(valid.nlargest(actual_k).mean())`・`len(valid)==0` は NaN。

    (6) `compute_field_strength_profile(history, observations=None) -> pd.DataFrame` メイン関数:
        - 入力検証: history の必須列（kakuteijyuni/race_nkey/kettonum/speed_figure/available_at・加えて OPPONENT_ROLLING_AXIS = rolling_speed_figure_mean_5 列・これは Phase 9.1 で history に付与済み）を assert（fail-loud・Pitfall 6 silent data loss 回避）。
        - copy-not-rename: `out = history.copy()`。
        - D-04 発走馬特定: `starters_mask = history["kakuteijyuni"].fillna(0) > 0`・`starters = history[starters_mask].copy()`・`race_size = starters.groupby("race_nkey").size().to_dict()`（coverage 計算用）。
        - per-source-race batch + vectorized groupby（RESEARCH Pattern 1 L268-327・Pitfall 2 計算量爆発回避・6.7M ペアを秒単位）:
          * starter × starter join で「同じ source race の他の starter」を展開・self（kettonum 同一）は除外。
          * 各 opponent について source race 時点（self.available_at）以前の opponent 過去走から OPPONENT_ROLLING_AXIS（rolling_speed_figure_mean_5）を取得。ここが計算量最大・per-opponent latest-K rolling を sort_values + groupby.head(K) で vectorized（rolling.py L375-383 idiom 転用）・純粋 Python ループ禁止（Pitfall 2）。
          * _pit_cutoff_prefilter で `_opp_available_at < available_at` (strict `<`) を適用（D-01・adversarial test が monkeypatch で無効化）。
        - D-03 profile 8値 vectorized 集約: `expanded.groupby(["race_nkey", "kettonum"]).agg(...)` で mean/median/top3_mean（_topk_mean_clamped(s, 3)）/top5_mean（_topk_mean_clamped(s, 5)）/max/sd/valid_count を一括算出・coverage = valid_count / race_size[race_nkey]。
        - 出力: out に 8 列（field_strength_mean/median/top3_mean/top5_mean/max/sd/valid_count/coverage）を merge（race_nkey + kettonum キー）で copy 追加・該当なし行は NaN。
        - 決定論的順序: groupby/sort_values で `kind="mergesort"` または `sort=False` + 明示 reset_index で byte-reproducible を保証（§19.1）。

    (7) `__all__ = ["compute_field_strength_profile"]` を定義。

    D-04 tozaicd 警告: docstring またはコメントで「tozaicd は EveryDB2 独自拡張で意味不明（live-DB 実証: 1/2=完走馬大多数・3=競走中止190件・4=失格16件）・kakuteijyuni > 0 を使う」を明記（PAT§ Pitfall 1）。ただし docstring 内に "odds" "ninki" 等 _FORBIDDEN_TOKENS の文字列リテラルを書かないこと（adversarial AST audit で false positive を出すため・SAFE-01 は「市場情報不使用」等の別表現で書く）。
  </action>

  <verify>
    <automated>uv run pytest tests/features/test_field_strength.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - src/features/field_strength.py に `def compute_field_strength_profile(history: pd.DataFrame, observations: pd.DataFrame | None = None) -> pd.DataFrame:` が存在
    - src/features/field_strength.py に `def _pit_cutoff_prefilter(expanded: pd.DataFrame) -> pd.DataFrame:` が存在
    - src/features/field_strength.py に `def _topk_mean_clamped(values: pd.Series, k: int) -> float:` が存在
    - src/features/field_strength.py に `OPPONENT_ROLLING_AXIS = "rolling_speed_figure_mean_5"` 定数が事前登録される（Open Question #1 解決・D-02 具象）
    - src/features/field_strength.py が `from src.features.availability import CUTOFF_SEMANTICS` を import し `assert CUTOFF_SEMANTICS["comparison_operator"] == "strict_less_than"` を含む
    - src/features/field_strength.py に `kakuteijyuni` を含む（発走馬特定）が `tozaicd` を含まない（grep -c tozaicd == 0）
    <!-- planner-discipline-allow: tozaicd -->
    - src/features/field_strength.py に `nlargest` を含む（D-05 vectorized top-k クランプ）
    - tests/features/test_field_strength.py の全テストが GREEN（PIT strict < / 発走馬特定 / D-02 mean_5 1軸 / profile 8値 / top-k クランプ / copy-not-rename / byte-reproducible / fail-loud）
    - ruff lint GREEN: `uv run ruff check src/features/field_strength.py tests/features/test_field_strength.py`
  </acceptance_criteria>

  <done>
    compute_field_strength_profile が history 全行に field_strength profile 8値を付与する。D-01 厳格版 as-of（strict `<`）と D-04 発走馬特定（kakuteijyuni > 0）が機械保証され・adversarial test が monkeypatch で PIT 違反を検出できる。D-02 相手 rolling は mean_5 1軸に事前登録（Open Question #1 解決）。単体テスト9項目 GREEN。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| history 入力 → field_strength 出力 | history の kakuteijyuni/race_nkey/kettonum/speed_figure/available_at が信頼できる正規化済データである前提（V5）|

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | compute_field_strength_profile の PIT filter | critical | mitigate | _pit_cutoff_prefilter で opponent.available_at < self.available_at (strict `<`)・adversarial test が monkeypatch で guard を `<=` に差替 T+1 opponent 混入を検出 |
| T-10-02 | Tampering | 発走馬特定（D-04）の誤判定 | high | mitigate | kakuteijyuni > 0 単一条件（live-DB 実証: time=0 と kakuteijyuni=0 が1516件完全一致）・tozaicd は使わない（EveryDB2 独自拡張で意味不明） |
| T-10-03 | Information disclosure | odds/ninki proxy の feature 混入（SAFE-01 違反） | high | mitigate | field_strength.py の入力は speed_figure float + rolling_speed_figure_mean_5 のみ・adversarial AST audit（PLAN 07）が odds/ninki/fukuodds/ninkij/tansyouodds Name/Attribute/Constant 0件を証明 |
| T-10-04 | Tampering | 計算量爆発（6.7M ペアで数十分の低速化） | medium | mitigate | per-source-race batch + vectorized groupby（純粋ループ禁止・Pitfall 2）・cProfile 検証は PLAN 07 |
| T-10-05 | Tampering | byte-reproducibility drift | medium | mitigate | 決定論的 groupby/sort（kind=mergesort 等の固定順序）・同一入力で bit-identical・test_byte_reproducible で機械保証 |
</threat_model>

<verification>
- 単体テスト: `uv run pytest tests/features/test_field_strength.py -x -q` GREEN
- lint: `uv run ruff check src/features/field_strength.py tests/features/test_field_strength.py` GREEN
- SAFE-01 下地: src/features/field_strength.py に tozaicd/odds/ninki/fukuodds/ninkij/tansyouodds の token が現れない（adversarial AST audit 本体は PLAN 07）
</verification>

<success_criteria>
- FEAT-02 第1段階（source race 内 opponent profile 8値）が vectorized で実装され・SC#1 PIT-correct 厳格版 as-of が機械保証される
- D-01/D-02（mean_5 1軸事前登録・Open Question #1 解決）/D-03/D-04/D-05 が実装され・単体テストで検証される
- PLAN 02（rolling.py 拡張 第2段階）が history の field_strength_* profile 列を前提にできる
</success_criteria>

<output>
Create `.planning/phases/10-opponent-strength-race-relative-features/10-01-SUMMARY.md` when done
</output>
