---
phase: 10-opponent-strength-race-relative-features
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/features/rolling.py
  - tests/features/test_rolling.py
autonomous: true
requirements: [FEAT-02]
granularity: fine

must_haves:
  truths:
    # SC#1（第2段階 latest-K rolling も PIT-correct）
    - "history に付与された field_strength profile 8値（PLAN 01 出力）が obs_id group + strict `<cutoff` で latest-K rolling されて 21 feature（D-13）が生成される"
    - "rolling_field_strength_* 21 feature は既存 speed_figure 17 feature と対称な idiom で生成される（obs_id = (race_nkey, kettonum) group・LOOKBACK=5・sentinel ルール）"
    # D-13 21 feature 完全性
    - "21 feature が全て生成される: latest_1 系6・mean_3 系5・mean_5 系6・trend 系2・count/coverage 系2（D-13）"
    # sentinel・count 軸
    - "rolling_field_strength_valid_count_mean_5 / coverage_mean_5 は信頼度 feature として常に実際数値（0〜window）を出力し sentinel 化しない（D-11 信頼度軸・Phase 9.1 idiom 踏襲）"
    - "mean/median/top3_mean/top5_mean/max/sd/trend_* は count>=window で算出・未満は sentinel（NaN）・best2_mean 系は使わない（D-09/D-09.1-01 と対称）"
    # §19.1
    - "rolling_field_strength_* 21 feature は決定論的（同一入力で bit-identical）"
  artifacts:
    - "src/features/rolling.py の _ROLLING_SYSTEMS に 'field_strength' が追加される"
    - "src/features/rolling.py の _SYSTEM_SOURCE に field_strength 系統の source 8列が定義される"
    - "src/features/rolling.py に _FIELD_STRENGTH_AXES: tuple[tuple[str, int], ...] = (...) が定義される（D-13 21 feature・_SPEED_FIGURE_AXES と対称）"
    - "src/features/rolling.py の build_rolling_features に if system == 'field_strength': 分岐が追加される"
    - "tests/features/test_rolling.py に rolling_field_strength_* 21 feature の単体テストが追加され GREEN"
  key_links:
    - "_FIELD_STRENGTH_AXES の axis 命名は rolling_field_strength_{axis}_{window} 形式（rolling_speed_figure_{axis}_{window} と対称・D-13 命名）"
    - "_FIELD_STRENGTH_AXES の trend 系（trend_last_minus_mean5 / trend_mean3_minus_mean5）は _speed_figure_col_name idiom を再利用し _{window} を付けない"
    - "field_strength 系統の source 列（field_strength_mean/median/top3_mean/top5_mean/max/sd/valid_count/coverage の8値）は PLAN 01 が history に付与済みであることを前提とする"
---

<objective>
Phase 10 PLAN 02: FEAT-02 相手強度の D-06 第2段階（target 馬の過去走 field_strength profile を latest-K rolling で集約）を src/features/rolling.py を拡張して実装する。D-13 の 21 feature を生成する。

Purpose: PLAN 01 が history に付与した field_strength profile 8値（中間値）を入力に・target 馬の過去走にわたり latest-K rolling（obs_id group・strict `<cutoff`・LOOKBACK=5）で集約し 21 feature を生成する。既存 speed_figure 17 feature と完全に対称な idiom で拡張する（CYCLE-2 HIGH#1 cross-obs leak 回避）。

Output: src/features/rolling.py 拡張（_ROLLING_SYSTEMS + _SYSTEM_SOURCE + _FIELD_STRENGTH_AXES + 集約ブロック分岐）・tests/features/test_rolling.py 拡張（21 feature 単体テスト）
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-opponent-strength-race-relative-features/10-CONTEXT.md
@.planning/phases/10-opponent-strength-race-relative-features/10-RESEARCH.md
@.planning/phases/10-opponent-strength-race-relative-features/10-PATTERNS.md

# PLAN 01 出力（依存）
@.planning/phases/10-opponent-strength-race-relative-features/10-01-PLAN.md

# analog（必読）
@src/features/rolling.py
@src/features/speed_figure.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: rolling.py に _FIELD_STRENGTH_AXES + field_strength 集約ブロックを追加（D-06 第2段階・21 feature）</name>
  <files>src/features/rolling.py, tests/features/test_rolling.py</files>

  <read_first>
    - src/features/rolling.py L74-104（_ROLLING_SYSTEMS + _SYSTEM_SOURCE idiom・field_strength 追加位置）
    - src/features/rolling.py L114-126（_pit_cutoff_prefilter・既存・本 task では変更しない）
    - src/features/rolling.py L136-180（_SPEED_FIGURE_AXES + _SPEED_FIGURE_CONDITIONAL_AXES idiom・_FIELD_STRENGTH_AXES の直接の analog）
    - src/features/rolling.py L155-164（_speed_figure_col_name: trend_ プレフィックスの特別扱い・再利用）
    - src/features/rolling.py L183-192（_best2_mean_of_group・nlargest idiom）
    - src/features/rolling.py L375-383（obs_id group・sort_values DESC + groupby.head(5) idiom）
    - src/features/rolling.py L403-535（speed_figure 集約ブロック・axis 毎の sentinel threshold・count_series・val_series の idiom・field_strength 集約ブロックはこの直後に挿入）
    - src/features/rolling.py L525-535（sentinel threshold ルール: count 軸は常に実際 count・mean/median/max/sd/trend_* は count>=window）
    - src/features/speed_figure.py L98-118（_pit_cutoff_prefilter helper・対称 idiom）
    - .planning/phases/10-opponent-strength-race-relative-features/10-RESEARCH.md L329-358（Pattern 2 latest-K rolling extension skeleton）・L620-630（A6 構成変更：Step 5 rolling 呼出位置の移動）
    - .planning/phases/10-opponent-strength-race-relative-features/10-PATTERNS.md L206-284（rolling.py 拡張の idiom・_FIELD_STRENGTH_AXES skeleton・集約ブロック分岐）
    - .planning/phases/09.1-speed-ability-profile-expansion/09.1-01-PLAN.md（Phase 9.1 17 feature 拡張の直接の先例）
  </read_first>

  <behavior>
    - Test 1 (D-13 21 feature 完全性): history に field_strength profile 8値が揃った状態で build_rolling_features を呼ぶと・21 feature が全て生成される（列名は rolling_field_strength_{axis}_{window} 形式・trend 系は _{window} 無し）
    - Test 2 (PIT strict `<`・obs_id group): LOOKBACK=5 窓で cutoff 以前の最新5件のみで集約・未来行は混入しない・同一 horse の複数 observation は独立 window（CYCLE-2 HIGH#1）
    - Test 3 (sentinel ルール): count>=window で算出・未満は sentinel（NaN）・best2_mean 系は使わない
    - Test 4 (count/coverage 軸): rolling_field_strength_valid_count_mean_5 / coverage_mean_5 は常に実際数値（0〜5）を出力・sentinel 化しない
    - Test 5 (trend 系): rolling_field_strength_mean_trend_last_minus_mean5 = latest_1 の mean - mean_5 の mean・rolling_field_strength_mean_trend_mean3_minus_mean5 = mean_3 の mean - mean_5 の mean
    - Test 6 (top-k source): source 列の top3_mean/top5_mean（PLAN 01 出力）を rolling で更に mean 化する場合も正常動作（profile → rolling の2段階構造）
    - Test 7 (byte-reproducible): 同一 history で2回呼出すと21 feature 全て bit-identical
    - Test 8 (copy-not-rename・HIGH#5): 入力 history の既存列は破壊されず・21 列が copy 追加される
  </behavior>

  <action>
    src/features/rolling.py を拡張する（同ファイル内拡張・新規アルゴリズムなし・_SPEED_FIGURE_AXES L136-180 と対称）:

    (1) _ROLLING_SYSTEMS（L74-87）の末尾（"speed_figure" の後）に "field_strength" を追加。コメントで「Phase 10 D-06 第2段階・source 列 = field_strength.py が history に付与した profile 8値・D-13 21 feature 生成」と明記。

    (2) _SYSTEM_SOURCE（L92-104）に "field_strength": ("field_strength_mean", "field_strength_median", "field_strength_top3_mean", "field_strength_top5_mean", "field_strength_max", "field_strength_sd", "field_strength_valid_count", "field_strength_coverage") を追加（PLAN 01 が history に付与する8列）。

    (3) _FIELD_STRENGTH_AXES: tuple[tuple[str, int], ...] = (...) を _SPEED_FIGURE_AXES の直後に定義（D-13・21 feature・PAT§ L248-264 skeleton）:
        - latest_1 系6: ("mean", 1) ("median", 1) ("top3_mean", 1) ("top5_mean", 1) ("max", 1) ("sd", 1)
        - mean_3 系5: ("mean", 3) ("median", 3) ("top3_mean", 3) ("top5_mean", 3) ("max", 3)（sd は定義不能なので除く・_SPEED_FIGURE_AXES の best2_mean 系と同様の方針）
        - mean_5 系6: ("mean", 5) ("median", 5) ("top3_mean", 5) ("top5_mean", 5) ("max", 5) ("sd", 5)
        - trend 系2: ("trend_last_minus_mean5", 5) ("trend_mean3_minus_mean5", 5)（axis 名に window 埋込・_speed_figure_col_name idiom で特別扱い）
        - count/coverage 系2: ("valid_count_mean", 5) ("coverage_mean", 5)（信頼度軸・列名生成で特別扱い: rolling_field_strength_valid_count_mean_5 / coverage_mean_5）
        - 合計 6+5+6+2+2 = 21 feature。

    (4) _field_strength_col_name(axis, window) helper を _speed_figure_col_name と対称に追加（trend_ プレフィックスと valid_count_mean/coverage_mean の特別扱い）。または既存 _speed_figure_col_name を一般化して field_strength にも使えるようにする（関数名を _rolling_col_name に変更可・ただし既存呼出箇所の更新が必要）。

    (5) build_rolling_features の集約ブロック（L403-609 の if system == "speed_figure": の直後）に `if system == "field_strength":` 分岐を追加:
        - recent_fs = history_filtered から _FIELD_STRENGTH_AXES の各 (axis, window) で windowed = groupby("obs_id").head(window)（rolling.py L375-383 idiom・PIT filter 済み LOOKBACK=5 窓）。
        - 各 axis について _SPEED_FIGURE_AXES の axis 分岐（mean/median/max/sd/trend_*）と対称な処理を field_strength profile 8値に適用。
        - sentinel threshold ルール（rolling.py L525-535 idiom 転用）: mean/median/top3_mean/top5_mean/max/sd/trend_* は count>=window で算出（未満 NaN）・valid_count_mean/coverage_mean は常に実際数値（0〜window）。
        - 命名は _field_strength_col_name で生成: rolling_field_strength_{axis}_{window}（trend 系と valid_count_mean/coverage_mean は _{window} の付け方が異なるので helper で吸収）。

    (6) tests/features/test_rolling.py に rolling_field_strength_* 21 feature の単体テストを追加（合成データ・test_rolling.py 既存の speed_figure 17 feature テストの鋳型）:
        - 21 feature が全て生成される（D-13 完全性）
        - PIT strict `<`・obs_id group・sentinel ルール・count/coverage 軸・trend 系・byte-reproducible・copy-not-rename の各テスト。
        - 合成 history には field_strength profile 8値（PLAN 01 出力形式）を手動で設定して入力にする（PLAN 01 の compute_field_strength_profile を呼ばず・直接 profile 列を設定・unit test として独立）。

    sentinel 実装の注意: `MISSING` sentinel（src.utils.category_map.MISSING）を NaN に変換する仕組みは snapshot.py の _coerce_rolling_columns_for_parquet（L81-113）で自動対応（rolling_ prefix のため）・本 task では sentinel → NaN 変換は扱わない（PLAN 05 で snapshot.py の nullable Float64 扱いを確認）。
  </action>

  <verify>
    <automated>uv run pytest tests/features/test_rolling.py -x -q -k "field_strength"</automated>
  </verify>

  <acceptance_criteria>
    - src/features/rolling.py の _ROLLING_SYSTEMS に "field_strength" が含まれる（grep -c '"field_strength"' src/features/rolling.py >= 1）
    - src/features/rolling.py の _SYSTEM_SOURCE["field_strength"] が 8 source 列（field_strength_mean/median/top3_mean/top5_mean/max/sd/valid_count/coverage）を含む
    - src/features/rolling.py に `_FIELD_STRENGTH_AXES` が定義され・要素数が 21 である（len(_FIELD_STRENGTH_AXES) == 21）
    - src/features/rolling.py の build_rolling_features に `if system == "field_strength":` 分岐が存在する
    - tests/features/test_rolling.py に `rolling_field_strength_mean_latest_1` `rolling_field_strength_mean_mean_5` `rolling_field_strength_mean_trend_last_minus_mean5` `rolling_field_strength_valid_count_mean_5` `rolling_field_strength_coverage_mean_5` の各生成を検証するテストが存在する
    - 既存 speed_figure 17 feature のテストが破壊されない（回帰 GREEN）
    - `uv run pytest tests/features/test_rolling.py -x -q` が全体 GREEN
    - ruff lint GREEN: `uv run ruff check src/features/rolling.py tests/features/test_rolling.py`
  </acceptance_criteria>

  <done>
    rolling.py に field_strength 系統が追加され・D-13 の 21 feature が既存 speed_figure 17 feature と対称な idiom で生成される。PLAN 01 の history 出力（profile 8値）を入力に latest-K rolling（obs_id group・strict `<cutoff`）で集約する。単体テスト8項目 GREEN・既存テスト回帰 GREEN。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| history profile 列（PLAN 01 出力）→ rolling 出力 | PLAN 01 が正しく PIT-correct に付与した field_strength profile 8値を入力にする前提|

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-06 | Tampering | cross-observation leak（CYCLE-2 HIGH#1） | critical | mitigate | obs_id = (race_nkey, kettonum) group・既存 _SPEED_FIGURE_AXES idiom と対称・per-observation independent window |
| T-10-07 | Tampering | PIT 違反（未来行の混入） | critical | mitigate | 既存 _pit_cutoff_prefilter（rolling.py L114-126・strict `<`）を再利用・新規に filter を書かない |
| T-10-08 | Tampering | sentinel/count 軸の誤実装 | medium | mitigate | count/coverage は常に実際数値・mean/median/top3_mean/top5_mean/max/sd/trend_* は count>=window で sentinel・L525-535 idiom 転用 |
| T-10-09 | Tampering | byte-reproducibility drift | medium | mitigate | 決定論的 groupby/sort・同一入力で bit-identical・test で機械保証 |
</threat_model>

<verification>
- 単体テスト: `uv run pytest tests/features/test_rolling.py -x -q` GREEN（新規 21 feature テスト + 既存回帰）
- lint: `uv run ruff check src/features/rolling.py tests/features/test_rolling.py` GREEN
- _FIELD_STRENGTH_AXES の要素数が 21（D-13 完全性）
</verification>

<success_criteria>
- FEAT-02 第2段階（target 馬の過去走 field_strength profile を latest-K rolling で集約）が既存 idiom と対称に実装される
- D-13 の 21 feature が全て生成され・SC#1 PIT-correct・byte-reproducible が機械保証される
- PLAN 04（builder.py 統合）が build_rolling_features の呼出で 21 feature を自動取得できる
</success_criteria>

<output>
Create `.planning/phases/10-opponent-strength-race-relative-features/10-02-SUMMARY.md` when done
</output>
