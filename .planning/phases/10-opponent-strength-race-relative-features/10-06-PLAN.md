---
phase: 10-opponent-strength-race-relative-features
plan: 06
type: execute
wave: 5
depends_on: [05]
files_modified:
  - scripts/run_phase10_evaluation.py
  - src/model/data.py
  - tests/model/test_data.py
  - reports/10-evaluation
# 補足: trainer.py / evaluator.py / segment_eval.py は **自動追従確認のみ**（コード編集しない）。
# PATTERNS.md が「コード変更不要」を明記。実行エージェントが binning 固定（§15.2 聖域）を破壊しないよう
# these are read-only confirmation targets — FEATURE_COLUMNS 106 自動追従（registry 動的導出）と
# binning 定数の import 再利用（CALIBRATION_CURVE_BINS 等・再定義禁止）のみを検証する。
autonomous: false
requirements: [FEAT-02, FEAT-03, SAFE-01]
granularity: fine
user_setup:
  - service: PostgreSQL (EveryDB2 normalized)
    why: "SC#5 非劣化 gate で v1.0 baseline vs Phase 10 snapshot の 3-way 比較を live-DB で実行"
    env_vars:
      - name: KEIBA_SKIP_DB_TESTS
        source: "unset（live-DB 必須）"

must_haves:
  truths:
    # SC#5（非劣化 gate・D-15/D-16 事前登録・§11.2 聖域・B-3 両 snapshot 同一 trainer 設定で delta）
    - "Phase 10 snapshot 20260626-1a-opponentstrength-v1（feature_count=106）と baseline snapshot 20260620-1a-postreview-v2 の **両方を同一 trainer 設定**（LightGBM params・seed・category_map・split_periods が bit-identical）で v1.0 LightGBM を再学習し・両 **実測値** の delta が D-16 許容幅内に収まる（B-3・§11.2 聖域の核心）"
    - "D-16 事前登録許容幅（Open Question #4 解決・§11.2 聖域）: Brier 悪化 ≤0.002 / LogLoss 悪化 ≤0.005 / AUC 悪化 ≤0.005（Phase 11 SC#2 事前登録マージンと整合）。delta 基準は baseline snapshot の **実測値**（BASELINE_* 定数=Phase 6 当時参考値ではない）"
    - "許容幅は評価結果を見た後に変更しない（§11.2 聖域・D-16）。BASELINE_* 定数（0.15222/0.47488/0.73230）は参考値としてログ表示のみ・gate 判定の delta 基準には使わない（feature ノイズ化とベースライン設定ドリフトを鑑別するため）"
    # FEATURE_COLUMNS allowlist
    - "src/model/data.py の FEATURE_COLUMNS（_derive_feature_columns 経由・registry から動的導出・REVIEW H1）が 27 新 feature を含む 106 feature になる・make_X_y の完全一致 assert が GREEN"
    - "snapshot_id を明示伝播（H1-b・省略すると v1.0 FEATURE_COLUMNS が選択され stop gate が無意味化）"
    # D-15 参考記録
    - "selected-only calibration / odds_band×p_bin は参考記録として見える化される（Phase 12 EVAL-01 先行指標・D-15）"
    - "binning 契約（evaluator/segment_eval の定数）は固定 bit-identical・§15.2 事前登録指標不変"
  artifacts:
    - "scripts/run_phase10_evaluation.py が存在し baseline snapshot と Phase 10 snapshot を **同一 trainer 設定**（B-3）で 3-way 比較（再評価→delta）を実行する"
    - "scripts/run_phase10_evaluation.py に TOLERANCE_BRIER=0.002 / TOLERANCE_LOGLOSS=0.005 / TOLERANCE_AUC=0.005 が事前登録される（D-16・Open Question #4 解決）"
    - "scripts/run_phase10_evaluation.py に BASELINE_BRIER/LOGLOSS/AUC = 0.15222/0.47488/0.73230 が **参考値** として定義される（gate 判定の delta 基準ではなくログ表示用・B-3）"
    - "reports/10-evaluation に baseline 実測値・Phase 10 実測値・delta・BASELINE_* 参考値との乖離 WARNING を含む LightGBM baseline/scenario 評価結果が出力される"
  key_links:
    - "run_phase10_evaluation.py は orchestrator.train_and_predict(snapshot_id='20260626-1a-opponentstrength-v1') と orchestrator.train_and_predict(snapshot_id='20260620-1a-postreview-v2') の **両方を同一 trainer 設定** で呼ぶ（生 trainer API は直接呼ばない・REVIEW H2/H7/H8・B-3）"
    - "evaluator.compute_metrics で算出した baseline 実測値と Phase 10 実測値の delta を D-16 許容幅で判定する（BASELINE_* 定数=参考値は delta 基準でない・B-3）"
    - "binning 定数（CALIBRATION_CURVE_BINS/ODDS_BAND_EDGES/NINKI_BAND_EDGES）は evaluator/segment_eval から import 再利用・再定義禁止"
---

<objective>
Phase 10 PLAN 06: PLAN 05 で生成した snapshot 20260626-1a-opponentstrength-v1（feature_count=106）と baseline snapshot 20260620-1a-postreview-v2 を **同一 trainer 設定**（B-3）で v1.0 LightGBM により再学習して delta を取り・SC#5 非劣化 gate（Brier/LogLoss/AUC）を検証する。

Purpose: 特徴量ノイズ化の回帰検知（SC#5）。27 新 feature がモデル性能を悪化させないことを定量的に保証する。D-16 許容幅を事前登録（Brier 悪化 ≤0.002 / LogLoss 悪化 ≤0.005 / AUC 悪化 ≤0.005・§11.2 聖域・Open Question #4 解決）し・評価結果を見た後に変更しない。B-3: delta 基準は baseline snapshot の **実測値**（BASELINE_* 定数=Phase 6 当時参考値ではない）とし・feature ノイズ化とベースライン設定ドリフト（LightGBM version・hyperparams・seed・category_map・split_periods の揺らぎ）を鑑別可能にする（§11.2 聖域の核心）。

Output: scripts/run_phase10_evaluation.py（run_speed_figure_stopgate.py 鋳型・D-16 事前登録許容幅・両 snapshot 同一 trainer 設定で delta・B-3）・reports/10-evaluation・tests/model/test_data.py 拡張（FEATURE_COLUMNS 106/79 回帰・make_X_y snapshot_id 明示伝播・H1-b 無言失敗 catch・B-2）。trainer.py/evaluator.py/segment_eval.py は **自動追従確認のみ**（コード編集しない・binning 固定 §15.2 聖域・W-2）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-opponent-strength-race-relative-features/10-CONTEXT.md
@.planning/phases/10-opponent-strength-race-relative-features/10-RESEARCH.md
@.planning/phases/10-opponent-strength-race-relative-features/10-PATTERNS.md

# PLAN 05 出力（依存・snapshot 生成済み）
@.planning/phases/10-opponent-strength-race-relative-features/10-05-PLAN.md

# analog（必読）
@scripts/run_speed_figure_stopgate.py
@src/model/data.py
@src/model/trainer.py
@src/model/evaluator.py
@src/model/segment_eval.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: src/model/data.py が 106 feature で FEATURE_COLUMNS 完全一致 assert GREEN になることを確認</name>
  <files>src/model/data.py</files>

  <read_first>
    - src/model/data.py L179-211（_derive_feature_columns: snapshot_id 指定で registry から FEATURE_COLUMNS を動的導出・REVIEW H1）
    - src/model/data.py L455-527（make_X_y: FEATURE_COLUMNS 完全一致 assert・L489-493）
    - src/model/data.py L487-493（X.columns == FEATURE_COLUMNS 完全一致 assert・review HIGH#9）
    - src/model/data.py 全体（FEATURE_COLUMNS 定数・load_feature_matrix）
    - .planning/phases/10-opponent-strength-race-relative-features/10-PATTERNS.md L449-467（data.py 拡張確認 idiom・手動変更不要想定・registry から動的導出）
  </read_first>

  <behavior>
    - _derive_feature_columns(snapshot_id='20260626-1a-opponentstrength-v1') が 106 feature を返す（27 新 feature 含む・registry から動的導出）
    - **回帰検知（B-2・H1-b 無言失敗 catch）**: _derive_feature_columns(snapshot_id='20260620-1a-postreview-v2')（baseline・v1.0 postreview）が 79 feature を返す。新 snapshot_id で 106・旧 snapshot_id で 79 が両方成立することで registry 伝播の健全性を直接保証。
    - **既存 make_X_y snapshot_id 伝播テストの直接実行**: tests/model/test_data.py にある既存の `make_X_y` 完全一致 assert テストが・新 snapshot_id='20260626-1a-opponentstrength-v1' で GREEN になることを確認（独自テストのみだと make_X_y 本体の snapshot_id 未伝播の無言失敗を catch しきれないため・既存テストの実値確認が必須）。
    - snapshot_id 省略時は v1.0 FEATURE_COLUMNS が選択される（H1-b・stop gate 無意味化を避けるため明示伝播必須）
  </behavior>

  <action>
    src/model/data.py は PLAN 04 で feature_availability.yaml に 27 feature を registry 登録すれば・_derive_feature_columns が自動的に 106 feature を返すように設計されている（REVIEW H1・registry から動的導出）。本 task では手動変更が不要であることを確認する:

    (1) _derive_feature_columns(snapshot_id='20260626-1a-opponentstrength-v1') を呼び出し・返される FEATURE_COLUMNS の len が 106 であることを検証する単体テストを tests/model/test_data.py（既存）に追加。

    (2) **回帰テスト（B-2・単一テストで 106/79 を両立）**: 同テスト内（または対のテスト）で _derive_feature_columns(snapshot_id='20260620-1a-postreview-v2')（baseline・v1.0 postreview）の len が 79 であることも assert。新旧 snapshot_id を続けざまに叩き・106 と 79 の両方が成立することで・registry 動的導出と snapshot_id 明示伝播（H1-b）の両方を単一回帰テストで保証する。snapshot_id 解決が壊れて新 feature が見えない無言失敗（H1-b）を直接 catch できる形。

    (3) **既存 make_X_y 完全一致 assert テストの新 snapshot_id での GREEN 確認**: tests/model/test_data.py の既存 `make_X_y` テスト（FEATURE_COLUMNS と X.columns の完全一致を assert するもの・review HIGH#9 由来）を parametrize または新ケース追加で snapshot_id='20260626-1a-opponentstrength-v1' でも GREEN になることを確認。新規テストだけでなく・既存テストが新 snapshot_id で実行されることが H1-b の無言失敗 catch の鍵。

    (4) もし _derive_feature_columns が何らかの理由で新 feature を含まない場合（hardcode list や snapshot_id 解決の不具合）は・src/model/data.py を修正して registry から動的導出されるようにする（REVIEW H1 の方針遵守）。修正が必要な場合は別コミット化し・理由（どの行が hardcode だったか）を SUMMARY に記録。

    (5) FEATURE_COLUMNS allowlist 契約: SAFE-01 の下地・banned source カラムが sneak-in しないことを確認（assert_matrix_columns_registered と二重保証）。
  </action>

  <verify>
    <automated>uv run pytest tests/model/test_data.py -x -q -k "feature_columns or make_X_y"</automated>
  </verify>

  <acceptance_criteria>
    - src/model/data.py の _derive_feature_columns が snapshot_id='20260626-1a-opponentstrength-v1' で 106 feature を返す（len == 106）
    - **回帰（B-2）**: _derive_feature_columns(snapshot_id='20260620-1a-postreview-v2') が 79 feature を返す（len == 79・baseline）・同一テスト内で 106/79 両者が assert される
    - src/model/data.py の make_X_y が snapshot_id='20260626-1a-opponentstrength-v1' で X.columns と FEATURE_COLUMNS が完全一致し assert GREEN
    - **既存テスト直接確認（B-2・H1-b 無言失敗 catch）**: tests/model/test_data.py の既存 `make_X_y` snapshot_id 伝播テストが・新 snapshot_id='20260626-1a-opponentstrength-v1' で GREEN になることが確認される（独自新規テストのみでなく既存テストの新 snapshot_id 実行が必須）
    - tests/model/test_data.py に 106 feature と 79 baseline 回帰・snapshot_id 明示伝播を検証するテストが存在する
    - FEATURE_COLUMNS に rolling_field_strength_mean_mean_5 / speed_index_rank_mean5 / field_strength_adjusted_rank 等の新 feature が含まれる
    - `uv run pytest tests/model/test_data.py -x -q` GREEN
  </acceptance_criteria>

  <done>
    data.py が registry から 27 新 feature を含む 106 feature を動的導出し・make_X_y の完全一致 assert が新 snapshot_id で GREEN。106（新）/ 79（baseline）回帰テストと既存 make_X_y テストの新 snapshot_id 実行で・H1-b の snapshot_id 無言失敗が直接 catch される。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: scripts/run_phase10_evaluation.py を作成し SC#5 非劣化 gate（D-16 事前登録許容幅）を実装</name>
  <files>scripts/run_phase10_evaluation.py</files>

  <read_first>
    - scripts/run_speed_figure_stopgate.py 全体（直接の analog・D-14 4指標 + residual proxy idiom・許容幅定数 L96-98・orchestrator.train_and_predict snapshot_id 明示伝播・make_model_version idiom・3-way 比較表示）
    - src/model/evaluator.py L89 等の binning 定数（CALIBRATION_CURVE_BINS・§15.2 事前登録指標不変）
    - src/model/evaluator.py L172-（compute_metrics・Brier/LogLoss/AUC/calibration_max_dev/sum_p 分布）
    - src/model/segment_eval.py（evaluate_all_segments・binning 契約固定・odds_band×p_bin・selected-only calibration・Plotly HTML+JSON）
    - src/model/orchestrator.py（train_and_predict・snapshot_id パラメタ・REVIEW H2/H7/H8: 生 trainer API は直接呼ばない）
    - src/model/predict.py L105-118（make_model_version 形式・feature_snapshot_id 全体を prefix に）
    - .planning/phases/10-opponent-strength-race-relative-features/10-RESEARCH.md L686（SC#5 非劣化 gate test map）・L620-630（A5 係数事前登録）
    - .planning/phases/10-opponent-strength-race-relative-features/10-PATTERNS.md L624-664（run_phase10_evaluation.py idiom・skeleton・make_model_version 形式）
  </read_first>

  <behavior>
    - Test 1 (D-16 事前登録・Open Question #4 解決): TOLERANCE_BRIER=0.002・TOLERANCE_LOGLOSS=0.005・TOLERANCE_AUC=0.005 がモジュール定数として事前登録される
    - Test 2 (非劣化 gate): brier_delta <= TOLERANCE_BRIER・logloss_delta <= TOLERANCE_LOGLOSS・auc_delta >= -TOLERANCE_AUC の3条件が全て成立すると PASS・1つでも違反すると FAIL（exit code 非0）
    - Test 3 (§15.2 事前登録指標不変): evaluator/segment_eval の binning 定数を import 再利用・再定義禁止
    - Test 4 (snapshot_id 明示伝播・H1-b): orchestrator.train_and_predict に snapshot_id='20260626-1a-opponentstrength-v1' を明示・省略時 v1.0 FEATURE_COLUMNS 選択を防ぐ
    - Test 5 (D-15 参考記録): selected-only calibration・odds_band×p_bin は参考記録として計算・見える化・ただし gate 判定には使わない
    - Test 6 (3-way 比較表示・任意): v1.0 baseline vs Phase 10 snapshot の Brier/LogLoss/AUC delta を表示・D-16 後知恵すり替えは禁止（候補選定は train/calib 窓内のみ）
  </behavior>

  <action>
    scripts/run_phase10_evaluation.py を新規作成する（run_speed_figure_stopgate.py の直接鋳型・PAT§ L624-664 skeleton）:

    (1) モジュール docstring: SC#5 非劣化 gate の目的（feature ノイズ化の回帰検知）・D-15 中間方針（必須 gate + 参考記録）・D-16 許容幅事前登録（§11.2 聖域）・H1-b snapshot_id 明示伝播・§15.2 binning 再利用を明記。

    (2) D-16 事前登録定数（Open Question #4 解決・§11.2 聖域）:
        - TOLERANCE_BRIER: float = 0.002   # Brier 悪化許容幅・Phase 6 D-07 水準(0.15222) との比較
        - TOLERANCE_LOGLOSS: float = 0.005  # LogLoss 悪化許容幅・Phase 6 D-07 水準(0.47488)
        - TOLERANCE_AUC: float = 0.005      # AUC 悪化許容幅・Phase 6 D-07 水準(0.73230)
        docstring またはコメントで「評価結果を見た後に変更しない（§11.2 聖域・D-16）」を明記。Phase 11 SC#2 事前登録マージンと整合する値。

    (3) D-07 水準定数（Phase 6 実績値・**参考値**）:
        - BASELINE_BRIER = 0.15222
        - BASELINE_LOGLOSS = 0.47488
        - BASELINE_AUC = 0.73230
        **※B-3 重要**: これらはあくまで「Phase 6 当時の参考値」。SC#5 gate 判定は**後述 (5) の通り Phase 10 実行時に baseline snapshot (20260620-1a-postreview-v2) を同一 trainer 設定で再評価した数値**を用いる。固定ハードコード値を gate 判定に使うと「feature ノイズ化かベースライン設定ドリフト（LightGBM version・hyperparams・seed・category_map・split_periods の揺らぎ）か」の鑑別ができない（§11.2 聖域の核心）。BASELINE_* 定数は docstring で「参考値・gate 判定の delta 基準には使わない」を明示。

    (4) CLI 引数（run_speed_figure_stopgate.py idiom・argparse）:
        - --baseline-snapshot-id（default: '20260620-1a-postreview-v2'）
        - --phase10-snapshot-id（default: '20260626-1a-opponentstrength-v1'・Open Question #3 事前登録）
        - --bt-split（default: 'BT-1'）
        - --odds-snapshot-policy（default: '30min_before'）
        - --out-dir（default: 'reports'）

    (5) **B-3（両 snapshot 同一 trainer 設定で delta・§11.2 聖域の核心）**: main() で baseline と Phase 10 の **両 snapshot を全く同一の trainer 設定**（LightGBM version・hyperparams・seed・category_map・split_periods・odds_snapshot_policy・bt_split）で orchestrator.train_and_predict により再評価し・両者の delta を取る（run_speed_figure_stopgate.py が両 snapshot を train_and_predict で比較する idiom と整合・既存 analog はこの形なので実質 action の補強のみ）。手順:
        - orchestrator.train_and_predict(snapshot_id=args.baseline_snapshot_id, bt_split=args.bt_split, odds_snapshot_policy=args.odds_snapshot_policy, **共通 trainer 設定) → baseline_brier / baseline_logloss / baseline_auc を **実測** 取得。
        - orchestrator.train_and_predict(snapshot_id=args.phase10_snapshot_id, bt_split=args.bt_split, odds_snapshot_policy=args.odds_snapshot_policy, **同一 trainer 設定) → phase10_brier / phase10_logloss / phase10_auc を **実測** 取得。
        - REVIEW H2/H7/H8: 生 trainer API は直接呼ばない。両呼び出しで trainer 設定（LightGBM params・seed・category_map・split_periods）が bit-identical になることを保証（設定を dict 等で一箇所定義し両呼び出しに渡す）。

    (6) evaluator.compute_metrics で Brier/LogLoss/AUC/calibration_max_dev/sum_p 分布を算出・binning 定数（CALIBRATION_CURVE_BINS 等）は evaluator から import 再利用・再定義禁止（§15.2 事前登録指標不変）。

    (7) segment_eval.evaluate_all_segments で selected-only calibration・odds_band×p_bin を参考記録として算出・D-15 の通り gate 判定には使わない。

    (8) **B-3 非劣化 gate 判定**: delta は両 **実測値** の差（BASELINE_* 定数ではない）。
        - brier_delta = phase10_brier - baseline_brier（**実測 baseline・BASELINE_BRIER 定数ではない**）
        - logloss_delta = phase10_logloss - baseline_logloss（**実測 baseline**）
        - auc_delta = phase10_auc - baseline_auc（**実測 baseline**）
        - gate_pass = (brier_delta <= TOLERANCE_BRIER) and (logloss_delta <= TOLERANCE_LOGLOSS) and (auc_delta >= -TOLERANCE_AUC)
        - gate_pass が True なら exit 0・False なら exit 非0（fail-loud）。
        - **鑑別ログ**: レポートに baseline 実測値 3つ・Phase 10 実測値 3つ・delta 3つ・参考として BASELINE_* 定数（Phase 6 当時値）と baseline 実測値の乖離（あれば WARNING）を全て記録。これにより「feature ノイズ化」と「ベースライン設定ドリフト（LightGBM version・hyperparams・seed 等の揺らぎ）」を鑑別できる（§11.2 聖域の核心）。

    (9) reports/10-evaluation に LightGBM baseline/scenario 評価結果（Brier/LogLoss/AUC/calibration/segment・baseline 実測値・delta・BASELINE_* 参考値との乖離 WARNING）を JSON + Plotly HTML で出力（run_speed_figure_stopgate.py idiom）。

    (10) make_model_version 形式（predict.py L105-118 idiom）: model_version = make_model_version('20260626-1a-opponentstrength-v1', 'lightgbm', 1) → '20260626-1a-opponentstrength-v1-lgb-v1'。
  </action>

  <verify>
    <automated>uv run python scripts/run_phase10_evaluation.py --phase10-snapshot-id 20260626-1a-opponentstrength-v1 --baseline-snapshot-id 20260620-1a-postreview-v2 --bt-split BT-1 --out-dir reports; echo "exit=$?"</automated>
  </verify>

  <acceptance_criteria>
    - scripts/run_phase10_evaluation.py が存在する
    - scripts/run_phase10_evaluation.py に `TOLERANCE_BRIER: float = 0.002` `TOLERANCE_LOGLOSS: float = 0.005` `TOLERANCE_AUC: float = 0.005` が事前登録される（D-16・Open Question #4 解決・§11.2 聖域）
    - scripts/run_phase10_evaluation.py に `BASELINE_BRIER = 0.15222` `BASELINE_LOGLOSS = 0.47488` `BASELINE_AUC = 0.73230`（Phase 6 D-07 水準・**参考値**）が定義される
    - **B-3（両 snapshot 再評価）**: scripts/run_phase10_evaluation.py が baseline snapshot (20260620-1a-postreview-v2) と Phase 10 snapshot (20260626-1a-opponentstrength-v1) の **両方を同一 trainer 設定**（LightGBM params・seed・category_map・split_periods が bit-identical）で orchestrator.train_and_predict により再評価し・両 **実測値** の delta を取る。BASELINE_* 定数（参考値）は gate 判定の delta 基準には使わない。
    - scripts/run_phase10_evaluation.py が orchestrator.train_and_predict を呼び（生 trainer API 直接呼出し無し・REVIEW H2/H7/H8）・snapshot_id を明示伝播する（H1-b）
    - scripts/run_phase10_evaluation.py が evaluator/segment_eval から binning 定数を import 再利用する（CALIBRATION_CURVE_BINS 等・再定義禁止・§15.2）
    - scripts/run_phase10_evaluation.py の gate 判定が3条件（brier_delta・logloss_delta・auc_delta・**両実測値の差**）の論理積。delta 基準は baseline snapshot 実測値（BASELINE_* 定数=参考値ではない）
    - reports/10-evaluation に baseline 実測値・Phase 10 実測値・delta・BASELINE_* 参考値との乖離 WARNING を含む JSON + Plotly HTML で評価結果が出力される
    - ruff lint GREEN: `uv run ruff check scripts/run_phase10_evaluation.py`
  </acceptance_criteria>

  <done>
    run_phase10_evaluation.py が v1.0 baseline snapshot と Phase 10 snapshot の **両方を同一 trainer 設定で再評価** して delta を取り・D-16 事前登録許容幅（Brier ≤0.002 / LogLoss ≤0.005 / AUC ≤0.005・§11.2 聖域）で SC#5 非劣化 gate を判定する。BASELINE_* 定数（Phase 6 当時値=0.15222/0.47488/0.73230）は参考値として表示のみ。D-15 参考記録（selected-only calibration・odds_band×p_bin）も見える化。Open Question #4 が解決される。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: live-DB で SC#5 非劣化 gate（D-16 事前登録許容幅・両 snapshot 同一 trainer 設定で delta）を実行</name>
  <files>reports/10-evaluation</files>
  <action>live-DB（KEIBA_SKIP_DB_TESTS unset・statement_timeout 必須）で run_phase10_evaluation.py を実行し・baseline snapshot 20260620-1a-postreview-v2 と Phase 10 snapshot 20260626-1a-opponentstrength-v1 の **両方を同一 trainer 設定**（LightGBM params・seed・category_map・split_periods が bit-identical）で v1.0 LightGBM を再学習し・両 **実測値** の delta を取る（B-3・§11.2 聖域の核心）。D-16 事前登録許容幅（Brier 悪化 ≤0.002 / LogLoss 悪化 ≤0.005 / AUC 悪化 ≤0.005・§11.2 聖域）で3条件が全て成立すると PASS・1つでも超過すると FAIL。BASELINE_* 定数（0.15222/0.47488/0.73230）は参考値としてログ表示のみ・gate 判定の delta 基準には使わない。超過時は許容幅を緩めず feature 見直しを協議。詳細手順は how-to-verify 参照。</action>
  <verify><automated>uv run python scripts/run_phase10_evaluation.py --phase10-snapshot-id 20260626-1a-opponentstrength-v1 --baseline-snapshot-id 20260620-1a-postreview-v2 --bt-split BT-1 --odds-snapshot-policy 30min_before --out-dir reports; echo "exit=$?"</automated><human-check>reports/10-evaluation の baseline 実測値・Phase 10 実測値・delta・BASELINE_* 参考値との乖離 WARNING を確認・3条件が D-16 許容幅内か（delta 基準は baseline snapshot 実測値）</human-check></verify>
  <done>SC#5 非劣化 gate が D-16 事前登録許容幅で PASS（3条件全て成立・delta 基準は baseline snapshot 実測値）する。§11.2 聖域遵守・許容幅は評価後変更しない。</done>
  <what-built>
    PLAN 06 で data.py が 106 feature で動作することを確認し・run_phase10_evaluation.py で SC#5 非劣化 gate（両 snapshot 同一 trainer 設定で delta・B-3）を実装した。次に live-DB で実際に 3-way 比較を実行し・D-16 事前登録許容幅で PASS/FAIL を判定する必要がある。
  </what-built>

  <how-to-verify>
    1. KEIBA_SKIP_DB_TESTS unset・PostgreSQL 接続確認。
    2. subagent 経由の重いDBクエリは statement_timeout を必ず設定（memory subagent-db-query-statement-timeout）。
    3. live-DB で SC#5 評価を実行（両 snapshot を同一 trainer 設定で再評価）:
       ```
       uv run python scripts/run_phase10_evaluation.py --phase10-snapshot-id 20260626-1a-opponentstrength-v1 --baseline-snapshot-id 20260620-1a-postreview-v2 --bt-split BT-1 --odds-snapshot-policy 30min_before --out-dir reports
       ```
    4. **B-3 出力される baseline 実測値・Phase 10 実測値・delta** を確認（BASELINE_* 定数 0.15222/0.47488/0.73230 は参考値としてログ表示のみ）:
       - brier_delta = phase10_brier - **baseline_brier（実測）** が ≤0.002 か（BASELINE_BRIER 定数との差ではない）
       - logloss_delta = phase10_logloss - **baseline_logloss（実測）** が ≤0.005 か
       - auc_delta = phase10_auc - **baseline_auc（実測）** が ≥-0.005 か（AUC は低下NG・悪化 = マイナス方向）
       - 参考: baseline 実測値 3つ が BASELINE_* 定数（Phase 6 当時値）と大きく乖離している場合は trainer 設定ドリフト（LightGBM version・hyperparams・seed・category_map・split_periods の揺らぎ）を疑う。スクリプトが出す WARNING を確認（聖域の鑑別資料）。
    5. gate_pass が True（3条件全て成立）なら exit 0・False なら exit 非0。
    6. D-16 §11.2 聖域: もし許容幅を超過した場合・許容幅を緩めて再実行してはならない。超過した場合は Phase 10 feature（相手強度・レース内相対）がノイズ化を引き起こしたことを示す・ユーザーと協議して feature の見直し（roll back・軸の削減・係数調整）を判断する。ただし baseline 実測値と BASELINE_* 参考値が大きく乖離している場合は・まず trainer 設定ドリフトの確認を優先（feature ノイズ化と設定ドリフトの鑑別）。
    7. reports/10-evaluation の JSON + Plotly HTML を確認・selected-only calibration / odds_band×p_bin（参考記録・Phase 12 EVAL-01 先行指標）の傾向を確認。
    8. binning 契約が固定 bit-identical であることを確認（evaluator/segment_eval の定数 import・再定義無し）。
  </how-to-verify>

  <resume-signal>Type "approved"（gate PASS・3条件全て成立の場合）または問題を記述（gate FAIL・どの指標が許容幅を超過したか・baseline 実測値と BASELINE_* 参考値の乖離状況・聖域に従い許容幅は変更せず feature 見直しを協議）</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| snapshot → trainer → evaluator | snapshot 20260626-1a-opponentstrength-v1 が byte-reproducible である前提（PLAN 05 で保証）|
| evaluator → gate 判定 | binning 定数が固定 bit-identical である前提（§15.2 事前登録指標不変）|

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-23 | Elevation of privilege | 許容幅の評価後すり替え（§11.2 聖域・D-16）・**BASELINE_* 固定値で gate 判定し feature ノイズ化と設定ドリフトを混同**（B-3・§11.2 聖域の核心） | critical | mitigate | TOLERANCE_* を事前登録定数化・docstring で「評価結果を見た後に変更しない」を明記・checkpoint で聖域遵守を明示・**B-3: baseline snapshot を同一 trainer 設定で再評価した実測値を delta 基準に使い BASELINE_* 定数（参考値）と gate 判定を分離** |
| T-10-24 | Elevation of privilege | additive score 係数の test 窓すり替え（D-12・§11.2 聖域） | high | mitigate | 候補 {0.0,0.1,0.25,0.5} は train/calib 窓内のみ・test 窓は canonical(0.25) のみ評価・PLAN 03 で事前登録 |
| T-10-25 | Tampering | binning 契約の再定義（§15.2 違反） | high | mitigate | evaluator/segment_eval の定数を import 再利用・run_phase10_evaluation.py での再定義禁止・単体テストで同一性保証 |
| T-10-26 | Tampering | snapshot_id 省略で v1.0 FEATURE_COLUMNS 選択（H1-b・stop gate 無意味化） | high | mitigate | orchestrator.train_and_predict に snapshot_id='20260626-1a-opponentstrength-v1' を明示伝播・単体テストで検証 |
| T-10-27 | Information disclosure | feature ノイズ化の回帰検知漏れ（SC#5 違反）・**BASELINE_* 固定値で delta を取ると設定ドリフト隠蔽**（B-3） | high | mitigate | 3-way 比較（baseline vs Phase 10・**両 snapshot 同一 trainer 設定で再評価し delta を取る**）・D-16 許容幅で fail-loud・超過時は feature 見直し（聖域遵守で許容幅緩和しない）・baseline 実測値と BASELINE_* 参考値の乖離 WARNING で設定ドリフトを鑑別 |
</threat_model>

<verification>
- 単体テスト: `uv run pytest tests/model/test_data.py -x -q` GREEN（FEATURE_COLUMNS 106/79 回帰・B-2・make_X_y snapshot_id 明示伝播・H1-b 無言失敗 catch）
- live-DB SC#5 gate（checkpoint）: gate PASS（3条件全て成立・delta 基準は baseline snapshot 実測値）・reports/10-evaluation 生成
- §11.2 聖域: TOLERANCE_* が評価後変更されていないこと・binning が再定義されていないこと・**B-3: BASELINE_* 定数（参考値）が gate 判定の delta 基準に使われていないこと**（baseline snapshot 実測値が delta 基準）
</verification>

<success_criteria>
- SC#5 非劣化 gate が D-16 事前登録許容幅（Brier ≤0.002 / LogLoss ≤0.005 / AUC ≤0.005・§11.2 聖域・Open Question #4 解決）で PASS する。delta 基準は baseline snapshot の **実測値**（B-3・両 snapshot 同一 trainer 設定で再評価）。
- v1.0 LightGBM が 106 feature で再学習され・baseline snapshot（実測）水準を悪化させないことが定量証明される。BASELINE_* 定数（Phase 6 当時参考値 0.15222/0.47488/0.73230）はログ参考値。
- D-15 参考記録（selected-only calibration・odds_band×p_bin）が見える化され Phase 12 EVAL-01 先行指標として残る
- §15.2 binning 契約が固定 bit-identical・snapshot_id 明示伝播（H1-b）が保証される
- B-2: data.py の _derive_feature_columns が新 snapshot_id で 106・旧 baseline snapshot_id で 79 を両方返す回帰テストが GREEN・既存 make_X_y テストが新 snapshot_id で GREEN（H1-b 無言失敗 catch）
</success_criteria>

<output>
Create `.planning/phases/10-opponent-strength-race-relative-features/10-06-SUMMARY.md` when done
</output>
