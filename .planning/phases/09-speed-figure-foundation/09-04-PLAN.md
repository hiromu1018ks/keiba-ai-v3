---
phase: 09-speed-figure-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 09-01
  - 09-02
  - 09-03
files_modified:
  - tests/audit/test_audit_speed_figure.py
  - scripts/verify_speed_figure_domain.py
autonomous: true
requirements:
  - SAFE-01
  - FEAT-01
user_setup:
  - service: PostgreSQL (everydb2)
    why: "SC#5 ドメイン整合性確認は live-DB で生成した snapshot が必要(KEIBA_SKIP_DB_TESTS unset)"
    env_vars: []
    dashboard_config: []

must_haves:
  truths:
    - "SC#4: src/features/speed_figure.py・src/features/rolling.py・src/features/builder.py のソースが odds/ninki/fukuodds/ninkij/tansyouodds proxy を含まないことを AST 静的解析で証明する(SAFE-01)"
    - "SC#4: FEATURE_COLUMNS allowlist に rolling_speed_figure_* 6 feature が含まれ・banned feature(odds/ninki/過去オッズ proxy) が含まれないことを grep で証明する"
    - "SC#4: audit テストが tests/audit/ パッケージの5段階鋳型(false-pass 回避・docstring cross-reference)を踏襲する"
    - "SC#5: 同一馬の連続走で speed_figure が大きく安定し・クラス昇降で有意に変動し・極端な外れ値がないことを Plotly HTML で可視化確認する(D-08・live-DB)"
  artifacts:
    - "tests/audit/test_audit_speed_figure.py(新規): SC#4 SAFE-01 proxy 排除証明(AST read-only + allowlist grep)"
    - "scripts/verify_speed_figure_domain.py(新規): SC#5 ドメイン整合性可視化(Plotly HTML・include_plotlyjs='directory')"
  key_links:
    - "tests/audit/test_audit_speed_figure.py が tests/audit/test_audit_features.py の5段階鋳型構造を踏襲し・SC#4 として SAFE-01 proxy 排除を静的証明"
    - "scripts/verify_speed_figure_domain.py が src/model/segment_eval.py の Plotly HTML idiom(include_plotlyjs='directory')と scripts/run_evaluation.py の CLI 起動 idiom を再利用"
---

<objective>
SC#4(SAFE-01 proxy 排除の adversarial audit)と SC#5(ドメイン整合性の可視化)を実装する。SC#4 は AST 静的解析と allowlist grep で odds/ninki/fukuodds/ninkij/tansyouodds proxy が新特徴量に混入しないことを構造的に証明する。SC#5 は live-DB で生成した snapshot の speed_figure 分布を Plotly HTML で可視化し・同一馬連続走の安定性・クラス昇降での変動・外れ値なしを目視確認する。

Purpose: SAFE-01 は横断聖域・新特徴量追加時に必ず proxy 排除を証明する。SC#5 は points_per_second [ASSUMED] テーブルの妥当性をドメイン知識で検証し・必要あれば後続 plan で微調整する。
Output: tests/audit/test_audit_speed_figure.py(新規) + scripts/verify_speed_figure_domain.py(新規)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-speed-figure-foundation/09-CONTEXT.md
@.planning/phases/09-speed-figure-foundation/09-RESEARCH.md
@.planning/phases/09-speed-figure-foundation/09-PATTERNS.md
@src/features/speed_figure.py
@src/features/rolling.py
@src/features/builder.py
@src/model/data.py
@src/model/segment_eval.py
@tests/audit/test_audit_features.py
@.planning/phases/09-speed-figure-foundation/09-01-SUMMARY.md
@.planning/phases/09-speed-figure-foundation/09-02-SUMMARY.md
@.planning/phases/09-speed-figure-foundation/09-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SC#4 SAFE-01 proxy 排除 adversarial audit 新規作成</name>
  <files>tests/audit/test_audit_speed_figure.py</files>
  <read_first>
    - tests/audit/test_audit_speed_figure.py(未作成・新規)
    - tests/audit/test_audit_features.py(L1-146 5段階鋳型・guard monkeypatch・docstring cross-reference・SC#2 adversarial 構造)
    - tests/audit/test_audit_label.py(AST read-only audit の analog・参考)
    - src/features/speed_figure.py(P01 作成・odds-free コメント末尾にある想定)
    - src/features/rolling.py(P02 拡張・_ROLLING_SYSTEMS に speed_figure)
    - src/features/builder.py(P03 拡張・Step 5b compute_speed_figure_for_history 挿入)
    - src/features/availability.py(TARGET_OBS_BANNED_COLUMNS に odds/ninki 含む)
    - src/model/data.py(FEATURE_COLUMNS・_derive_feature_columns・make_X_y)
    - .planning/phases/09-speed-figure-foundation/09-RESEARCH.md(Security Domain・SC#4・SAFE-01)
    - .planning/phases/09-speed-figure-foundation/09-VALIDATION.md(SC#4 verify command)
  </read_first>
  <behavior>
    - AST 解析で src/features/speed_figure.py・src/features/rolling.py・src/features/builder.py のソースから odds/ninki/fukuodds/ninkij/tansyouodds の Name ノード/Attribute 参照が検出されないこと
    - REVIEW H5: ast.Constant の str リテラルについて・ninki/fukuodds/ninkij/tansyouodds の word-boundary 部分一致(odds は完全一致のみ)で検査し・SQL 文字列定数内の proxy トークン埋込み("ur.ninki AS prior_ninki" 形式)を検出すること
    - REVIEW H1: FEATURE_COLUMNS が data.py parameterization 後も rolling_speed_figure_* 6 feature を含み・forbidden prefix カラムが0件であること(動的 _derive_feature_columns 経由)
    - docstring に "SC#4 adversarial" と "cross-reference: tests/features/test_speed_figure.py" を含む(T-08-04 踏襲)
  </behavior>
  <action>
    `tests/audit/test_audit_speed_figure.py` を新規作成する。モジュール docstring の冒頭に「SC#4 adversarial(SAFE-01 proxy 排除証明)・cross-reference: tests/features/test_speed_figure.py・cross-reference: tests/audit/test_audit_features.py」を明記し・機能テスト(test_speed_figure.py)と棲み分け(機能は正しく動くこと・audit は proxy が混入しないことの静的証明)を T-08-04 と同様に文書化。

    [定数]
    `_FORBIDDEN_TOKENS: frozenset[str] = frozenset({"odds", "ninki", "fukuodds", "ninkij", "tansyouodds"})` を定義。ただし "odds" は部分一致になりやすいため・厳密な Name ノードマッチで判定する(文字列 contains でなく AST で Name.id == 'odds' または Attribute.attr == 'odds')。コメントで「文字列 contains ではコメント中の "odds-free" 言及が false positive になるため AST Name ノードで判定」と明記。

    [テスト]
    (a) `test_no_odds_ninki_proxy_in_speed_figure_source`(SC#4 AST): `inspect.getsource(src.features.speed_figure)` でソースを取得し `ast.parse` で AST 構築。`ast.walk(tree)` で全 Name ノードと Attribute ノードを走査し・`node.id in _FORBIDDEN_TOKENS`(Name)または `node.attr in _FORBIDDEN_TOKENS`(Attribute)のものが0件であることを assert。ただし docstring・コメントは AST walk で Name ノードとして現れないため・"odds-free" のような docstring 言及は自動的に除外される(false positive 回避)。**REVIEW H5 対応**: 文字列リテラル(ast.Constant value が str)については完全一致でなく **word-boundary 部分一致**(例: `re.search(r'\\b(ninki|fukuodds|ninkij|tansyouodds)\\b', constant_str)` または `_FORBIDDEN_TOKENS - {'odds'}` の部分文字列チェック)を追加し・SQL 文字列定数内の `"ur.ninki AS prior_ninki"` のような proxy トークン埋込みを検出する。`odds` は部分一致が多すぎるため(half_odds 等)完全一致のみとし・ninki/fukuodds/ninkij/tansyouodds は word-boundary 部分一致で検査する。docstring で「REVIEW H5: SQL 文字列リテラル内の禁止トークン(proxy)を word-boundary 部分一致で検出・ast.Constant の str を再帰走査」と明記。

    (b) `test_no_odds_ninki_proxy_in_rolling_source`(SC#4 AST・rolling): 同様に src.features.rolling に対して AST 解析(H5 word-boundary 拡張含む)。rolling.py は odds/ninki に触れないはず(speed_figure 列のみ集約)。

    (c) `test_no_odds_ninki_proxy_in_builder_source`(SC#4 AST・builder・H5 強化): 同様に src.features.builder に対して AST 解析(H5 word-boundary 拡張含む)。builder は _HISTORY_DB_SELECT_COLUMNS で odds/ninki を SELECT していないはず(TARGET_OBS_BANNED と disjoint・L320-322 assert)。特に `_HISTORY_DB_SELECT_COLUMNS`/`_OBS_DB_SELECT_COLUMNS` の SQL 文字列定数(`"nr.sibababacd AS hist_sibababacd"` 形式・src/features/builder.py L116-120 実読)内に禁止トークンが埋込まれていないことを H5 word-boundary check で検証。将来の `"ur.ninki AS prior_ninki"` のような proxy が SQL 文字列で追加された場合に AST Constant ノードの str 走査で検出する。

    (d) `test_feature_columns_contains_speed_figure_no_proxy`(SC#4 allowlist・REVIEW H1 対応): REVIEW H1 で data.py が parameterization されたため・`from src.model.data import _derive_feature_columns` で動的取得する。テストでは v1.0 デフォルト `_derive_feature_columns(snapshot_id=None)` と・speed_figure snapshot が生成された後の `_derive_feature_columns(snapshot_id="20260625-1a-speedfigure-v1")`(テスト用・存在しない場合は v1.0 で代用し docstring 明記) の両方を検査する。検査内容: rolling_speed_figure_last_1/mean_3/mean_5/max_5/sd_5/count_5 の6 feature が(生成済 snapshot では)FEATURE_COLUMNS に含まれること・かつ FEATURE_COLUMNS の各要素について・forbidden prefix(odds/ninki/fukuodds/ninkij/tansyouodds で始まる)が0件であること。banned alias sneak-in の構造的防止(RESEARCH・HIGH #9)。

    (e) `test_target_obs_banned_columns_disjoint_from_history_select`(SC#4 構造的保証): `from src.features.availability import TARGET_OBS_BANNED_COLUMNS` と `from src.features.builder import _HISTORY_SELECT_COLUMNS` を import し・両者が disjoint であることを assert(builder L320-322 の既存 assert と対称・P04 で再確認)。"odds"/"ninki" が TARGET_OBS_BANNED_COLUMNS に含まれることをも併せて assert(聖域の定義変更なし)。

    (f) `test_docstring_cross_reference`: モジュール docstring と audit 関数の docstring に "SC#4 adversarial" と "cross-reference: tests/features/test_speed_figure.py" が含まれることを assert(test_audit_features.py L128-146 踏襲・T-08-04)。

    (g) `test_false_pass_detection_power`(SC#4 false-pass 回避・H5 拡張): 注入型メタ検証。意図的に禁止トークンを含むダミーソース文字列を構築し AST parse して・本 audit の walk が forbidden Name ノードを検出することを確認。さらに **REVIEW H5**: ダミーソースに SQL 文字列定数(例: dummy select 文字列に proxy トークンを埋込)を含め・ast.Constant の str に対する word-boundary 部分一致 check がこれを検出することを確認する。これにより「guard が正しく働くこと」と「SQL 文字列内 proxy も検出すること」を証明(test_audit_features.py の5段階鋳型(5)に相当・false-pass 回避)。
  </action>
  <verify>
    <automated>uv run pytest tests/audit/test_audit_speed_figure.py -x -v</automated>
  </verify>
  <done>
    - tests/audit/test_audit_speed_figure.py が新規作成され・7テストが GREEN
    - speed_figure.py・rolling.py・builder.py の AST から forbidden トークン(odds/ninki/fukuodds/ninkij/tansyouodds)の Name/Attribute ノードが0件で検出される
    - REVIEW H5: ast.Constant str リテラルの word-boundary 部分一致で SQL 文字列内 proxy も0件(ninki/fukuodds/ninkij/tansyouodds・odds は完全一致)
    - REVIEW H1: FEATURE_COLUMNS(_derive_feature_columns 経由)に rolling_speed_figure_* 6 feature が含まれ・forbidden prefix カラムが0件
    - TARGET_OBS_BANNED_COLUMNS と _HISTORY_SELECT_COLUMNS が disjoint(odds/ninki 構造的排除)
    - docstring に "SC#4 adversarial" と cross-reference が含まれる
    - false-pass 回避テスト(意図的注入を検出・SQL 文字列 proxy 注入含む)が GREEN
  </done>
</task>

<task type="auto">
  <name>Task 2: SC#5 ドメイン整合性可視化スクリプト新規作成(Plotly HTML・live-DB)</name>
  <files>scripts/verify_speed_figure_domain.py</files>
  <read_first>
    - scripts/verify_speed_figure_domain.py(未作成・新規)
    - scripts/run_evaluation.py(L65-68 CLI 起動 idiom・_REPO_ROOT sys.path 挿入・L1316-1441 masked DSN・try/finally pool close)
    - src/model/segment_eval.py(L444-449 include_plotlyjs='directory'・Plotly HTML 出力 idiom)
    - src/features/builder.py(build_feature_matrix・P03 拡張済み)
    - src/features/snapshot.py(write_snapshot・snapshot 読込方法参考)
    - src/db/connection.py(make_pool role='readonly')
    - .planning/phases/09-speed-figure-foundation/09-RESEARCH.md(Code Examples・points_per_second [ASSUMED]・D-08 クラス単調性)
    - .planning/phases/09-speed-figure-foundation/09-VALIDATION.md(SC#5 verify command・manual-only)
  </read_first>
  <acceptance_criteria>
    - `uv run python scripts/verify_speed_figure_domain.py` が exit 0 で reports/09-speed-figure-domain.html を生成する(KEIBA_SKIP_DB_TESTS unset・live-DB 必須)
    - HTML に3つのプロットが含まれる: (1) 同一馬の連続走 speed_figure推移(安定性確認) (2) クラス別(class_code_normalized) speed_figure 分布ボックスプロット(単調性確認・D-08) (3) speed_figure ヒストグラム(外れ値確認)
    - Plotly HTML は include_plotlyjs='directory' + div_id="speed-figure-domain" 固定で byte-reproducible(plotly.min.js 共有1ファイル・random HTML ID 回避・REVIEW C13 + M3 踏襲・src/model/segment_eval.py L445-452 idiom)
    - DSN は生で出力されず settings.dsn_masked のみログ出力(T-06-15 踏襲)
    - readonly pool が try/finally で close される(scripts/run_evaluation.py L1316-1441 idiom)
    - statement_timeout='30s' がセッション先頭で設定される(MEMORY.md subagent-db-query-statement-timeout)
    - 生成された HTML は reports/ に tracked(gitignore 対象外・REVIEW C13 policy 維持)
  </acceptance_criteria>
  <action>
    `scripts/verify_speed_figure_domain.py` を新規作成する。モジュール docstring で「Phase 9 SC#5 ドメイン整合性可視化・live-DB で生成した snapshot の speed_figure 分布を Plotly HTML で出す・同一馬連続走安定・クラス昇降変動・外れ値なしを目視確認(D-08)・cross-reference: 09-VALIDATION.md」を明記。

    [CLI 起動 idiom]
    (a) ファイル先頭に `_REPO_ROOT = Path(__file__).resolve().parent.parent` と `if str(_REPO_ROOT) not in sys.path: sys.path.insert(0, str(_REPO_ROOT))` を置き `src.*` import を可能にする(scripts/run_evaluation.py L65-68 と同一)。
    (b) argparse で `--snapshot-id`(default: P03 SUMMARY で決定した候補 例 "20260625-1a-speedfigure-v1")・`--out-dir`(default: reports)・`--sample-horses`(default: 20・同一馬推移プロット用のサンプル馬数)を受け取る。

    [DB 読込 idiom]
    (c) `from src.db.connection import make_pool`・`from src.config.settings import Settings`。`settings = Settings()`・`logger.info("readonly DSN: %s", settings.dsn_masked)`(T-06-15 踏襲・生 DSN 絶対禁止)。
    (d) `readonly_pool = make_pool(settings, role="readonly")` を try ブロックで生成し finally で close(scripts/run_evaluation.py L1316-1441 と同一)。
    (e) セッション先頭で `SET statement_timeout = '30s'` を発行(MEMORY.md subagent-db-query-statement-timeout・重クエリの孤立実行防止)。
    (f) build_feature_matrix(readonly_pool, snapshot_id=..., label_version=..., fa_version="0.4.0") を呼出し feature_matrix を取得(P03 の Step 5b 拡張済み・rolling_speed_figure_* 6 feature 含む)。**REVIEW M2 対応**: `build_feature_matrix` は DataFrame でなく **dict** を返す(src/features/builder.py L590-596・`result["feature_matrix"]`・`result["snapshot_id"]` 等)。したがって `result = build_feature_matrix(...); feature_matrix = result["feature_matrix"]` で DataFrame を取り出してから後続処理に渡す。または既存 snapshot から Parquet を読込(DuckDB read_parquet・RESEARCH interop)・その場合は dict 抽出不要。docstring で「M2: build_feature_matrix は dict 戻り値・result["feature_matrix"] で DataFrame を取得」と明記。

    [Plotly HTML 出力 idiom]
    (g) `import plotly.graph_objects as go`・`from plotly.subplots import make_subplots`。3プロットを構築:
        - プロット1: 同一馬(_sample_horses 件の kettonum)の連続走(race_date 昇順)speed_figure 推移ラインプロット。各馬1ライン・安定性(連続走で大きくブレしない)を目視。
        - プロット2: class_code_normalized 毎の speed_figure ボックスプロット。クラス昇格で指数上昇・降格で下降の単調性(D-08)を目視。
        - プロット3: speed_figure 全体ヒストグラム。極端な外れ値(±1000 等・Pitfall 4)がないこと・分布がドメイン整合的(0-100 程度に収まる)ことを目視。
    (h) `fig.write_html(out_path, include_plotlyjs="directory", full_html=True, auto_open=False, div_id="speed-figure-domain")` で出力(segment_eval.py L444-452 と同一・byte-reproducible・plotly.min.js 共有1ファイル・REVIEW C13・**M3 対応**: `div_id` を固定文字列で指定し Plotly が生成する random HTML ID を回避・byte-reproducible HTML を保証・src/model/segment_eval.py L445-452 の idiom 踏襲)。out_path = reports/09-speed-figure-domain.html。
    (i) メタデータ(feature_snapshot_id・生成時刻・row_count・プロット毎の統計量 min/max/mean/std)を HTML 内 annotation または別途 JSON(reports/09-speed-figure-domain.json)で付加。byte-reproducible を壊わさないよう・生成時刻は別 JSON のみ(HTML 本体には入れない)。

    [安全性]
    (j) odds/ninki は本スクリプトでも SELECT/特徴量化しない(診断層のみ・SAFE-01)。万が一 parity 確認で market データを参照する場合は prediction テーブル経由のみ(speed_figure 特徴量には混入しない)。スクリプトのコメントで「SAFE-01: speed_figure は odds-free・本スクリプトも odds-free を維持」と明記。

    (k) main 関数の戻りは 0(成功)または非0(失敗・RuntimeError伝搬)。レポート生成後に「SC#5 ドメイン整合性: reports/09-speed-figure-domain.html を開いて目視確認(同一馬安定・クラス単調・外れ値なし)」を stdout に表示(manual-only verification・VALIDATION.md 参照)。
  </action>
  <verify>
    <automated>uv run python -c "import ast; ast.parse(open('scripts/verify_speed_figure_domain.py').read()); print('syntax OK')"</automated>
  </verify>
  <done>
    - scripts/verify_speed_figure_domain.py が新規作成され・構文エラーがない
    - reports/09-speed-figure-domain.html と .json が生成される(KEIBA_SKIP_DB_TESTS unset・live-DB)
    - REVIEW M2: build_feature_matrix の dict 戻り値から result["feature_matrix"] で DataFrame を取り出している
    - 3プロット(同一馬推移・クラス別ボックス・ヒストグラム)が含まれる
    - REVIEW M3: include_plotlyjs='directory' + div_id="speed-figure-domain" 固定で plotly.min.js 共有1ファイル参照・random HTML ID 回避で byte-reproducible(REVIEW C13 + M3)
    - 生 DSN はログ出力されない(settings.dsn_masked のみ)
    - readonly pool が finally で close される
    - statement_timeout='30s' がセッション先頭で設定される
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ソースコード → AST audit | src/features/{speed_figure,rolling,builder}.py のソースを AST 解析し・禁止トークン(odds/ninki proxy)の Name/Attribute ノードが0件を静的証明(SC#4) |
| FEATURE_COLUMNS allowlist → モデル入力 | rolling_speed_figure_* が allowlist に含まれ・banned alias が sneak-in しない構造的保証(HIGH #9) |
| live-DB → 可視化スクリプト | readonly SELECT で snapshot を読込・Plotly HTML を生成。DSN は masked のみログ出力(T-06-15) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04 | Tampering | speed_figure/rolling/builder への odds/ninki proxy 混入(SAFE-01 違反) | high | mitigate | AST 静的解析で Name/Attribute ノードを検査・ast.Constant str リテラルの word-boundary 部分一致で SQL 文字列内 proxy も検出(H5)・false-pass 回避テストで検出力証明(SC#4) |
| T-09-14 | Tampering | FEATURE_COLUMNS に banned alias が sneak-in | high | mitigate | FEATURE_COLUMNS(_derive_feature_columns 経由・REVIEW H1 parameterization) の各要素が forbidden prefix で始まらないことを grep assert・HIGH #9 完全一致 assert と二重防御 |
| T-09-15 | Information Disclosure | 可視化スクリプトでの生 DSN ログ出力 | medium | mitigate | settings.dsn_masked のみログ出力・scripts/run_evaluation.py L1316-1441 idiom 踏襲(T-06-15) |
| T-09-16 | Denial of Service | 可視化スクリプトの重クエリが orphan で CPU 張り付き | medium | mitigate | SET statement_timeout='30s' をセッション先頭で発行(MEMORY.md subagent-db-query-statement-timeout) |
</threat_model>

<verification>
- `uv run pytest tests/audit/test_audit_speed_figure.py -x -v` が GREEN(7テスト・SC#4 完全証明)
- `uv run python scripts/verify_speed_figure_domain.py` が exit 0・reports/09-speed-figure-domain.html 生成(KEIBA_SKIP_DB_TESTS unset)
- `grep -c 'dsn_masked' scripts/verify_speed_figure_domain.py` >= 1(生 DSN ログ禁止)
- `grep -c "statement_timeout" scripts/verify_speed_figure_domain.py` >= 1(タイムアウト設定)
- `uv run pytest tests/audit/ -x` で tests/audit/ パッケージ全体が GREEN(false-pass 回避構造維持)
</verification>

<success_criteria>
- SC#4 SAFE-01 proxy 排除: speed_figure/rolling/builder の AST から odds/ninki/fukuodds/ninkij/tansyouodds が0件・FEATURE_COLUMNS にも0件・false-pass 回避テスト GREEN
- SC#5 ドメイン整合性: live-DB で生成した snapshot の speed_figure 分布が可視化され・同一馬安定・クラス単調・外れ値なしが目視確認可能
- tests/audit/ パッケージの5段階鋳型(false-pass 回避・docstring cross-reference)を踏襲
- byte-reproducible な Plotly HTML(include_plotlyjs='directory')
- 生 DSN ログ禁止・statement_timeout 設定で DoS 防止
</success_criteria>

<output>
`.planning/phases/09-speed-figure-foundation/09-04-SUMMARY.md` を作成し・以下を記録する:
- SC#4 audit 7テストの GREEN 実績・AST 解析の検出力(false-pass 回避テスト含む)
- SC#5 ドメイン整合性の目視結果(同一馬安定性・クラス単調性・外れ値なし・points_per_second [ASSUMED] テーブルの妥当性)・必要あれば後続 plan で points_per_second 微調整を推奨
- reports/09-speed-figure-domain.html の統計量(min/max/mean/std・外れ値の有無)
- P05 stop gate が依存する情報: SAFE-01 proxy 排除が構造的証明済み・SC#4 GREEN
</output>

## Artifacts this phase produces

本 PLAN(P04)が新規作成するシンボル:
- `tests/audit/test_audit_speed_figure.py`:
  - `_FORBIDDEN_TOKENS`(frozenset)
  - REVIEW H5: SQL 文字列リテラル検査ヘルパー(word-boundary 部分一致・ninki/fukuodds/ninkij/tansyouodds・odds は完全一致)
  - `test_no_odds_ninki_proxy_in_speed_figure_source`(SC#4 AST・speed_figure・H5 拡張)
  - `test_no_odds_ninki_proxy_in_rolling_source`(SC#4 AST・rolling・H5 拡張)
  - `test_no_odds_ninki_proxy_in_builder_source`(SC#4 AST・builder・H5 強化・SQL 文字列定数検査含む)
  - `test_feature_columns_contains_speed_figure_no_proxy`(SC#4 allowlist・H1 _derive_feature_columns 経由)
  - `test_target_obs_banned_columns_disjoint_from_history_select`(SC#4 構造的保証)
  - `test_docstring_cross_reference`(T-08-04)
  - `test_false_pass_detection_power`(SC#4 false-pass 回避・H5 SQL proxy 注入検出含む)
- `scripts/verify_speed_figure_domain.py`:
  - `main()`(argparse・CLI 起動)
  - `_REPO_ROOT`(sys.path 挿入 idiom)
  - Plotly HTML 出力(reports/09-speed-figure-domain.html・include_plotlyjs='directory'・div_id="speed-figure-domain" 固定・M3)

後続 PLAN が参照する新シンボル:
- P05 stop gate は SC#4 GREEN(SAFE-01 証明済み)を前提として・モデル評価に進む
- P05 は reports/09-speed-figure-domain.html のドメイン整合性確認(SC#5 GREEN)を前提として stop gate を評価
- Phase 10/11/12 でも SAFE-01 横断聖域として本 audit パターンを再利用
