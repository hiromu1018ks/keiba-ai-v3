---
phase: 09-speed-figure-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
files_modified:
  - src/features/builder.py
  - src/model/data.py
  - src/model/orchestrator.py
  - tests/features/test_speed_figure.py
  - tests/features/test_speed_figure_builder_integration.py
  - tests/model/test_data.py
autonomous: true
requirements:
  - FEAT-01
user_setup: []

must_haves:
  truths:
    - "builder.py が Step 5(rolling) の直前に speed_figure 計算を history に挿入し・rolling が speed_figure 列を集約できるようになる"
    - "history に time/trackcd/jyocd/kyori 列が SELECT され・speed_figure.py が消費できる(copy-not-rename・既存列は破壊しない)"
    - "feature_matrix に rolling_speed_figure_* 6 feature が含まれ・HIGH #3 assert_matrix_columns_registered が GREEN を返す"
    - "byte-reproducible snapshot が SC#1 で再生成可能(同一 metadata で bit-identical)・P02 で course_kubun 削除・speed_figure 6 feature 追加が反映される"
    - "新 feature_snapshot_id が v1.0 '20260620-1a-postreview-v2' 系統を継承し make_model_version の prefix 整合を保つ"
    - "REVIEW H1-a 対応(acceptance 厳格化・横断的・最優先): src/model/data.py の load_feature_matrix が arity-0 でなく snapshot_id を必須パラメータ(デフォルト None で v1.0 に明示 routing)として受け取る。acceptance_criteria の 'or list(sig.parameters)==[]' 逃げ道を削除し・古い arity-0 関数が GREEN に通るのを構造的に拒否する"
    - "REVIEW H1-b 対応(orchestrator 伝播・新規・最優先): orchestrator.train_and_predict(src/model/orchestrator.py:372-375) 内部の3箇所の make_X_y(train_df/calib_df/test_df) 呼出が bare call(snapshot_id 無し)でなく・make_X_y(..., snapshot_id=snapshot_id_param) に明示伝播する。新たに train_and_predict のシグネチャに snapshot_id 引数を追加(feature_snapshot_id とは別・FEATURE_COLUMNS 選択用)し・予測経路で v1.0 FEATURE_COLUMNS が静かに使われる失敗を閉塞する(予測経路に bare 'make_X_y(' が残らないことを grep/AST で保証)"
    - "REVIEW H1-c 対応(obs_id 構築順序整合・新規・最優先): builder.py Step 5b 挿入位置では feature_matrix に obs_id が未構築(現状 obs_id は Step 6 L505-517 で初出)のため・Step 5b の直前で既存 idiom(race_nkey+kettonum があればそれ・無ければ index+kettonum)で feature_matrix['obs_id'] を早期構築する。これにより P01 compute_speed_figure_for_history(history, observations=feature_matrix) が observations[['obs_id','kettonum','feature_cutoff_datetime']] API 契約を満たし・rolling.py L207-215 と対称な obs_id 展開が可能になる。obs_id は引き続き Step 6b で drop(PyArrow 直列化不能)"
    - "REVIEW H1 対応(横断的): src/model/data.py が SNAPSHOT_PATH/SNAPSHOT_MANIFEST_PATH/FEATURE_COLUMNS を import 時固定(v1.0 硬結合・src/model/data.py L77-79/L160-176)するのでなく・load_feature_matrix(snapshot_id) が引数で v1.0/speed_figure 両 snapshot をロード可能・FEATURE_COLUMNS が選択 snapshot から動的導出される"
    - "REVIEW H1 対応(継続): scripts/run_train_predict.py の --snapshot-id 検証(L174-193)が data.py.SNAPSHOT_PATH と異なると reject する硬結合を緩和し・新 snapshot での学習を許可する"
  artifacts:
    - "src/features/builder.py(拡張): Step 5b speed_figure 挿入・_HISTORY_DB_SELECT_COLUMNS に time 追加(必要なら)"
    - "src/model/data.py(拡張 REVIEW H1): load_feature_matrix(snapshot_id) parameterization・FEATURE_COLUMNS 動的導出(_derive_feature_columns(snapshot_id))・prepare_model_matrix/make_X_y の snapshot-aware 化"
    - "tests/features/test_speed_figure.py(拡張): SC#1 byte-reproducible + SC#3 registry↔Parquet parity 統合テスト追記"
    - "tests/features/test_speed_figure_builder_integration.py(新規): builder Step 5b と rolling の統合 GREEN"
  key_links:
    - "builder.py の history に付与された speed_figure 列を rolling.py::build_rolling_features が集約し rolling_speed_figure_* 6 feature を生成"
    - "REVIEW H1: data.py が _derive_feature_columns(snapshot_id) で registry(P02 更新済み)から rolling_speed_figure_* を動的 pick up し・load_feature_matrix(snapshot_id) が v1.0/speed_figure 両 Parquet をロード可能・make_X_y 完全一致 assert で FEATURE_COLUMNS と X.columns が一致"
    - "snapshot.py::write_snapshot は speed_figure が float のため numeric path で自動 Float64 化(P02 H3a で可変 window suffix coercion 拡張済み・RESEARCH Pattern 4)"
---

<objective>
P01 の `compute_speed_figure_for_history` と P02 の rolling/registry 拡張を・builder.py の feature 構築 pipeline に統合する。history に speed_figure 列を付与し・rolling がそれを集約して rolling_speed_figure_* 6 feature を出力するようにする。SC#1(byte-reproducible 再生)と SC#3(registry↔Parquet parity)の統合テストで契約を証明する。

Purpose: この PLAN 完了後・実際の feature snapshot 生成スクリプトが rolling_speed_figure_* を含む byte-reproducible な Parquet を生成できるようになる。P04(audit/可視化)と P05(stop gate)は完成した snapshot が前提。
Output: builder.py 拡張(Step 5b 挿入) + 統合テスト2種
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-speed-figure-foundation/09-CONTEXT.md
@.planning/phases/09-speed-figure-foundation/09-RESEARCH.md
@.planning/phases/09-speed-figure-foundation/09-PATTERNS.md
@src/features/builder.py
@src/features/snapshot.py
@src/features/rolling.py
@src/features/speed_figure.py
@src/model/data.py
@.planning/phases/09-speed-figure-foundation/09-01-SUMMARY.md
@.planning/phases/09-speed-figure-foundation/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: builder.py Step 5b 挿入(history に speed_figure 計算を統合・copy-not-rename)</name>
  <files>src/features/builder.py</files>
  <read_first>
    - src/features/builder.py(全文・L101-124 _HISTORY_DB_SELECT_COLUMNS・L195-312 _construct_derived_columns・L457-494 Step 5 rolling 統合・L502-558 Step 6 推定脚質・L560-564 Step 6b obs_id drop・L580-582 Step 9 assert_matrix_columns_registered)
    - src/features/speed_figure.py(P01・compute_speed_figure_for_history のシグネチャ確認)
    - src/features/rolling.py(P02・_ROLLING_SYSTEMS に speed_figure 追加済み・build_rolling_features が rolling_speed_figure_* 6 feature を出力)
    - .planning/phases/09-speed-figure-foundation/09-PATTERNS.md(L136-156 builder.py 拡張 pattern・Step 5b 挿入位置)
    - src/features/availability.py(P02・_ROLLING_SYSTEMS_FOR_RESERVED に speed_figure・assert_matrix_columns_registered が rolling_speed_figure_* を許可)
  </read_first>
  <acceptance_criteria>
    - `grep -c 'compute_speed_figure_for_history' src/features/builder.py` >= 1(Step 5b 挿入)
    - `grep -c 'from src.features.speed_figure' src/features/builder.py` >= 1(import)
    - _HISTORY_DB_SELECT_COLUMNS に "ur.time AS time" が含まれる(speed_figure.py が time 列を消費・必要に応じて追加)
    - feature_matrix に rolling_speed_figure_last_1/mean_3/mean_5/max_5/sd_5/count_5 の6列が含まれる
    - assert_matrix_columns_registered が rolling_speed_figure_* に対して GREEN(registry 登録済み・P02)
    - 既存テスト `uv run pytest tests/features/test_builder.py -x` が GREEN(回帰なし・speed_figure 列が history に無いテストケースは sentinel のまま)
    - copy-not-rename: 既存 history 列(time/trackcd/jyocd/kyori 等)は破壊されず speed_figure 列が追加される
    - obs_id 中間処理列は Step 6b で引き続き drop される(PyArrow 直列化不能・speed_figure も obs_id 経由なら同一 idiom)
    - REVIEW H1-c: Step 5b の直前に feature_matrix["obs_id"] を早期構築する既存 idiom が存在する(`grep -A2 -B2 'Step 5b' src/features/builder.py` 範囲で obs_id 構築を確認・L505-517 と同一ロジック)。P01 API 契約(observations が obs_id を持つ)を充足し・Step 6 L505-517 は再利用("obs_id" in columns で skip)・Step 6b で drop は不変
  </acceptance_criteria>
  <action>
    src/features/builder.py を以下の通り拡張する。

    (a) **_HISTORY_DB_SELECT_COLUMNS 拡張(L101-124)**: speed_figure.py が history で `time` 列(real・0.1秒単位)を消費する必要がある。現状 _HISTORY_DB_SELECT_COLUMNS に `time` は含まれない。`"ur.time AS time"` を追加する。`_HISTORY_SELECT_COLUMN_NAMES`(L122-124)と `_HISTORY_SELECT_COLUMNS`(L155-157・derived 除外)にも反映される。`time` は derived でないので _DERIVED_HISTORY_COLUMN_NAMES(L152-154)には追加しない。TARGET_OBS_BANNED_COLUMNS(availability L91-100)との disjoint assert(L320-322)は `time` が banned 集合に含まれないため維持される。

    (b) **Step 5b 挿入(L494 の直前・Step 5 rolling 統合の直前)**: history が _construct_derived_columns で time/trackcd/jyocd/kyori/race_date/as_of_datetime を持った状態で・Step 5 rolling の直前に speed_figure 計算を挿入する。copy-not-rename で history に speed_figure 列を付与。

    **REVIEW H1-c 対応(obs_id 早期構築・rolling.py L207-215 と対称)**: 現状 builder は obs_id を Step 6(L505-517 推定脚質)で初出させるため・Step 5b 挿入時点では feature_matrix に obs_id 列が無い。P01 compute_speed_figure_for_history は observations[["obs_id","kettonum","feature_cutoff_datetime"]] API 契約(P01 PLAN L114)を期待するため・Step 5b の**直前**で既存 idiom(Step 6 L505-517 と完全同一ロジック)により feature_matrix["obs_id"] を早期構築する: `feature_matrix["race_nkey"]` が存在すれば `list(zip(feature_matrix["race_nkey"], feature_matrix["kettonum"]))`・無ければ `list(zip(feature_matrix.index, feature_matrix["kettonum"]))`。これにより P01 が observations から obs_id を取り expanded frame を構築できる(rolling.py L207-215・L237-243 と対称)。obs_id は Step 6 の推定脚質で既存 idiom が再利用(再生成でなく再利用・"obs_id" in columns で skip)し・Step 6b(L560-564)で引き続き drop される(PyArrow 直列化不能・契約不変)。docstring で「H1-c: Step 5b 前に obs_id を早期構築・P01 API 契約充足・Step 6b で drop は不変」と明記。
        ```
        # --- Step 5b: speed_figure 計算(Phase 9・Beyer 型・history に speed_figure 列を付与) ---
        # history に time/trackcd/jyocd/kyori/race_date/as_of_datetime が揃った段階で
        # src.features.speed_figure を呼出。PIT 保証は speed_figure.py 内の _pit_cutoff_prefilter
        # で strict < で適用(rolling と対称)。copy-not-rename: history に "speed_figure" 列を追加
        # (既存列は破壊しない・HIGH #5 踏襲)。available_at = race_date を付与。
        from src.features.speed_figure import compute_speed_figure_for_history
        history = compute_speed_figure_for_history(history, observations=feature_matrix)
        ```
        位置は CR-01 merge 直後・build_rolling_features(feature_matrix, history) 呼出の前。history はStep 5 rolling に渡る前に speed_figure 列を持つ。

    (c) **Step 5 rolling の変更不要**: rolling.py(P02)が _ROLLING_SYSTEMS に speed_figure を追加済みのため・build_rolling_features が history["speed_figure"] を numeric 系統として自動集約する。builder.py L475-493 の rolling_df 構築と merge は変更不要(speed_figure は rolling_cols に自動的に含まれる)。

    (d) **obs_id 取り扱い(L505-517・L560-564)**: speed_figure.py は observations の obs_id を利用する(P01・rolling と対称)。REVIEW H1-c で Step 5b の直前に obs_id を早期構築した(b)ため・既存 Step 6 の obs_id 構築(L505-517)は "obs_id" in columns で skip して再利用される(回帰なし・再生成でない)。Step 6b(L560-564) で obs_id は引き続き drop される(PyArrow 直列化不能・契約不変)。docstring で「H1-c: obs_id は Step 5b 前に早期構築→Step 6 で再利用→Step 6b で drop」を明記。

    (e) **Step 9 assert_matrix_columns_registered(L580-582)**: 拡張不要。feature_matrix の rolling_speed_figure_* 6列は P02 で registry に追加済みのため・検査が GREEN になる。もし GREEN にならない場合は P02 の yaml/availability 拡張に不備があるため FAIL で検出される(高 stakes・即修正)。

    (f) **docstring 更新(L349-407 build_feature_matrix docstring)**: Pipeline ステップに「Step 5b: speed_figure 計算(compute_speed_figure_for_history・PIT保護は speed_figure.py 側・strict <)」を追記。
  </action>
  <verify>
    <automated>uv run pytest tests/features/test_builder.py tests/features/test_speed_figure_builder_integration.py -x</automated>
  </verify>
  <done>
    - builder.py に Step 5b として compute_speed_figure_for_history 呼出が挿入されている
    - _HISTORY_DB_SELECT_COLUMNS に "ur.time AS time" が含まれる
    - feature_matrix に rolling_speed_figure_* 6 feature が含まれる(統合テスト GREEN)
    - assert_matrix_columns_registered が GREEN(rolling_speed_figure_* が registry 登録済み)
    - 既存 test_builder.py が全て GREEN(回帰なし)
    - copy-not-rename: 既存 history/feature_matrix 列は保持されたまま speed_figure/rolling_speed_figure_* が追加
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SC#1 byte-reproducible + SC#3 registry↔Parquet parity 統合テスト</name>
  <files>tests/features/test_speed_figure_builder_integration.py, tests/features/test_speed_figure.py</files>
  <read_first>
    - tests/features/test_speed_figure.py(P01 作成・9 テスト・このタスクで SC#1/SC#3 統合を追記)
    - tests/features/test_speed_figure_builder_integration.py(未作成・新規)
    - tests/features/test_snapshot_repro.py(byte-reproducible snapshot の analog・test_byte_reproducible_by_hash idiom)
    - src/features/snapshot.py(write_snapshot・FIXED_REPRODUCE_TS・SHA256 metadata 除外)
    - src/features/builder.py(Task 1 で Step 5b 拡張)
    - src/features/availability.py(assert_matrix_columns_registered)
    - src/model/data.py(_derive_feature_columns・FEATURE_COLUMNS・make_X_y)
    - .planning/phases/09-speed-figure-foundation/09-VALIDATION.md(SC#1/SC#3 verify command)
  </read_first>
  <behavior>
    - SC#1: 同一 DataFrame で write_snapshot を2回(snapshot_id と created_at を変える)呼出し・SHA256 が bit-identical になる(metadata 除外・CR-04 踏襲)
    - SC#3: rolling_speed_figure_* 6 feature が registry と Parquet 実カラムの両方に存在する parity・FEATURE_COLUMNS に含まれる・make_X_y が完全一致 assert で GREEN
    - builder Step 5b 挿入後の統合: 合成 history で build_rolling_features を呼び rolling_speed_figure_* 6 列が出力される
  </behavior>
  <action>
    [test_speed_figure.py 拡張]
    `tests/features/test_speed_figure.py` に以下の2テストを追記する:

    (a) `test_byte_reproducible_snapshot_with_speed_figure`(SC#1): 合成 history+observations で compute_speed_figure_for_history → build_rolling_features で feature_matrix 相当を構築。snapshot.write_snapshot を2回(snapshot_id="test-v1" と "test-v2"・created_at_fixed=FIXED_REPRODUCE_TS で同一)呼出し・戻り SHA256 が完全一致することを assert。FIXED_REPRODUCE_TS は snapshot.py から import。metadata 除外 schema bytes のみ依存することを実証(CR-04・RESEARCH Pattern 4)。

    (b) `test_registry_parquet_parity_speed_figure`(SC#3): load_feature_availability() で registry をロードし・rolling_speed_figure_last_1/mean_3/mean_5/max_5/sd_5/count_5 の6 feature が registered_feature_columns(spec) に全て含まれることを assert。更に data.py から `from src.model.data import _derive_feature_columns` し・REVIEW H1 で parameterization された `_derive_feature_columns(snapshot_id=None)`(v1.0 デフォルト) と `_derive_feature_columns(snapshot_id="20260625-1a-speedfigure-v1")`(speed_figure・テスト用合成 snapshot_id または実際の P03 生成 snapshot) の両方で・`any(c.startswith("rolling_speed_figure_") for c in cols)` が評価できることを確認。v1.0 デフォルト(speed_figure snapshot 未生成時)は rolling_speed_figure_* が FEATURE_COLUMNS に含まれない前提で・speed_figure snapshot 生成後は含まれる前提を docstring で明記(動的導出の契約)。このテストは KEIBA_SKIP_DB_TESTS によらず registry と derived list の静的整合性を検証する。

    [test_speed_figure_builder_integration.py 新規作成]
    新規作成。モジュール docstring に「Phase 9 builder Step 5b と rolling の統合テスト・cross-reference: tests/features/test_speed_figure.py」を明記。以下を実装:
    (c) `test_builder_step5b_adds_speed_figure_to_history`: 合成 history+observations を構築し・compute_speed_figure_for_history を呼出し・history に speed_figure 列が付与されることを assert。更に build_rolling_features に渡し・rolling_speed_figure_last_1/mean_3/mean_5/max_5/sd_5/count_5 の6列が出力されることを assert。
    (d) `test_builder_step5b_copy_not_rename`: history の既存列(time/trackcd/jyocd/kyori/kakuteijyuni/harontimel3 等)が compute_speed_figure_for_history 呼出後も保持されることを assert(HIGH #5 copy-not-rename・破壊的 rename でない)。
    (e) `test_builder_step5b_pit_correct`: 5段階 adversarial history(target/same_day_prior/same_day_later/previous_day/future + eligible)で compute_speed_figure_for_history を呼出し・speed_figure が eligible 3行のみで算出された値になることを assert(SC#2 機能側・P01 の adversarial と対称・ここでは builder 経由での契約)。
  </action>
  <verify>
    <automated>uv run pytest tests/features/test_speed_figure.py tests/features/test_speed_figure_builder_integration.py -x -v</automated>
  </verify>
  <done>
    - test_speed_figure.py に test_byte_reproducible_snapshot_with_speed_figure(SC#1)と test_registry_parquet_parity_speed_figure(SC#3)が追加され GREEN
    - test_speed_figure_builder_integration.py が新規作成され・3テスト(step5b 追加・copy-not-rename・PIT correct)が GREEN
    - SHA256 bit-identical が実証される(snapshot_id/created_at が異なっても同一 DataFrame で同一 SHA256)
    - rolling_speed_figure_* 6 feature が registry・FEATURE_COLUMNS の両方に含まれる
    - builder 経由で rolling_speed_figure_* 6 列が出力される(統合 GREEN)
  </done>
</task>

<task type="auto">
  <name>Task 3: src/model/data.py の snapshot parameterization(REVIEW H1 横断的・最優先・v1.0 硬結合解除)</name>
  <files>src/model/data.py</files>
  <read_first>
    - src/model/data.py(全文・L77-79 SNAPSHOT_PATH/SNAPSHOT_MANIFEST_PATH/EXPECTED_FEATURE_COUNT hard-code・L149-176 _derive_feature_columns・L176 FEATURE_COLUMNS import 時固定・L211-229 load_feature_matrix 引数 arity 0・L232-292 verify_snapshot_sha256・L405-465 make_X_y・L471-499 prepare_model_matrix・L642-652 load_frozen_maps)
    - src/model/orchestrator.py(L234-246 train_and_predict シグネチャ・feature_snapshot_id 引数・L251-257 feature_df は label-joined frame 前提)
    - scripts/run_train_predict.py(L174-193 --snapshot-id 検証が data.py.SNAPSHOT_PATH と異なると reject・H1 継続)
    - src/features/snapshot.py(write_snapshot・snapshot_id から parquet path を構築・manifest path 命名規則)
    - src/features/availability.py(registered_feature_columns・_RESERVED_NON_FEATURE_COLUMNS・P02 H3b で rolling_speed_figure_count_5 が reserved から除外済み)
    - tests/model/test_data.py(data.py 既存テスト・FEATURE_COLUMNS 完全一致 assert idiom)
  </read_first>
  <acceptance_criteria>
    - `grep -c 'def load_feature_matrix' src/model/data.py` >= 1(拡張)
    - `grep -c 'def _derive_feature_columns' src/model/data.py` >= 1(拡張・snapshot_id 引数追加)
    - REVIEW H1-a(厳格化・escape hatch 削除): `uv run python -c "import inspect, src.model.data as d; sig = inspect.signature(d.load_feature_matrix); params = list(sig.parameters.keys()); assert 'snapshot_id' in params, f'load_feature_matrix must accept snapshot_id (got params={params})・arity-0 escape は許可しない(H1-a)'; print('OK')"` が OK を出力(`or list(sig.parameters)==[]` の逃げ道を削除・古い arity-0 関数を構造的に拒否)
    - v1.0 既存テスト `uv run pytest tests/model/test_data.py -x` が GREEN(snapshot_id 引数省略時は v1.0 デフォルトで後方互換・A5 idiom)
    - REVIEW H1 検証: `uv run python -c "import src.model.data as d; import src.features.availability as a; reg = a.registered_feature_columns(a.load_feature_availability()); excluded = d.META_KEY_COLUMNS | d.RAW_ID_COLUMNS | d.LABEL_COLUMNS; speed_cols_in_feature = [c for c in reg if c.startswith('rolling_speed_figure_') and c not in excluded]; print(speed_cols_in_feature)"` が rolling_speed_figure_last_1/mean_3/mean_5/max_5/sd_5/count_5 を含む(H3b で count_5 が reserved から除外済みのため FEATURE_COLUMNS に含まれる)
    - load_frozen_maps が snapshot_id に対応する category_map path を解決可能(v1.0 は既存 path・speed_figure は P03 で生成予定の category_map path・存在しない場合は明示的エラー)
    - REVIEW H1-b(orchestrator 伝播・予測経路に bare make_X_y 残存禁止): `grep -nE 'make_X_y\(' src/model/orchestrator.py | grep -v 'snapshot_id=' | grep -v '^\s*#' | wc -l` == 0(orchestrator 内部で snapshot_id 無しの make_X_y 呼出が予測経路に残らない)。train_and_predict シグネチャに snapshot_id 引数(デフォルト None・feature_snapshot_id とは別)が追加され・内部3箇所の make_X_y が snapshot_id=snapshot_id で明示伝播する
    - REVIEW H1-b 機能証明(新 snapshot の FEATURE_COLUMNS が実際消費される): `uv run python -c "import inspect, src.model.orchestrator as o; sig = inspect.signature(o.train_and_predict); assert 'snapshot_id' in sig.parameters, 'train_and_predict must accept snapshot_id (H1-b)'; print('OK')"` が OK
    - REVIEW H1-b テスト(新規・tests/model/test_data.py 拡張): test_make_X_y_uses_snapshot_feature_columns を追加。合成 speed_figure snapshot(snapshot_id="test-speed-figure"・rolling_speed_figure_* 6 列を含む Parquet)と v1.0 snapshot を切替・make_X_y(frame, snapshot_id="test-speed-figure") が返す X.columns が rolling_speed_figure_* を含み・snapshot_id=None が v1.0(rolling_speed_figure_* 非含)を返すことを assert。orchestrator.train_and_predict(snapshot_id=...) が内部 make_X_y に snapshot_id を伝播し・両 snapshot で異なる FEATURE_COLUMNS が選択されることを monkeypatch または合成 Parquet で実証(H1-b 静かな失敗の閉塞証明)
  </acceptance_criteria>
  <action>
    REVIEW H1(横断的・P03/P04/P05 で反復した最優先事項): src/model/data.py は現在 SNAPSHOT_PATH/SNAPSHOT_MANIFEST_PATH(L77-79) が v1.0 hard-code で・FEATURE_COLUMNS(L176) が import 時に v1.0 Parquet から導出され・load_feature_matrix()(L211-229) が引数 arity 0 で SNAPSHOT_PATH のみを読む。このままでは Phase 9 の rolling_speed_figure_* 6 feature が registry に追加されても model input に入らず・P05 stop gate が両 snapshot を比較できない(「Parquet は正しく生成されたが学習は v1.0 のまま」の静かな失敗)。以下の parameterization を実装する。

    **後方互換 A5 idiom(Phase 5 D-03 と同一パターン)**: 全ての新規引数はデフォルト値を持ち・既存呼出元は変更なしで動作する。

    (a) **モジュール定数の default 化**: `SNAPSHOT_PATH`/`SNAPSHOT_MANIFEST_PATH`/`CATEGORY_MAP_PATH`(L77-79) を `DEFAULT_*` に rename し・v1.0 の値(`snapshots/feature_matrix_20260620-1a-postreview-v2.parquet` 等)をデフォルト値として保持する。`EXPECTED_FEATURE_COUNT` は manifest から読み込むため廃止検討(または動的取得)。

    (b) **snapshot_id から path を解決する helper**: `def _snapshot_paths(snapshot_id: str | None = None) -> tuple[str, str, str]:` を追加。snapshot_id=None の場合は DEFAULT_* を返す(v1.0・後方互換)。snapshot_id 指定時は `f"snapshots/feature_matrix_{snapshot_id}.parquet"` / `f"snapshots/feature_matrix_{snapshot_id}.manifest.yaml"` / `f"snapshots/category_map_{snapshot_id}.json"` を返す。

    (c) **_derive_feature_columns の parameterization(L149-176)**: シグネチャを `def _derive_feature_columns(snapshot_id: str | None = None) -> list[str]:` に拡張。L162 `pq.read_table(SNAPSHOT_PATH)` を `pq.read_table(_snapshot_paths(snapshot_id)[0])` に変更。これにより指定 snapshot の実カラム集合から FEATURE_COLUMNS を動的導出する。モジュールレベル `FEATURE_COLUMNS: list[str] = _derive_feature_columns()`(L176) は v1.0 デフォルトのまま残し(後方互換・既存 import が壊れない)・新しいコードは `_derive_feature_columns(snapshot_id)` を明示的に呼ぶ。

    (d) **load_feature_matrix の parameterization(L211-229)**: シグネチャを `def load_feature_matrix(snapshot_id: str | None = None) -> pd.DataFrame:` に拡張。L225 `pq.read_table(SNAPSHOT_PATH)` を `pq.read_table(_snapshot_paths(snapshot_id)[0])` に変更。verify_snapshot_sha256(snapshot_id)(後述) を呼ぶ。docstring で「snapshot_id=None は v1.0 デフォルト(後方互換・A5)・Phase 9 stop gate は明示的に両 snapshot_id を渡す」と明記。

    (e) **verify_snapshot_sha256 の parameterization(L232-292)**: シグネチャに `snapshot_id: str | None = None` を追加。L256/275 の SNAPSHOT_MANIFEST_PATH/SNAPSHOT_PATH を _snapshot_paths 経由に変更。

    (f) **make_X_y の parameterization(L405-465)**: feature 選択に使う FEATURE_COLUMNS を snapshot_id に基づき動的取得する。シグネチャ `def make_X_y(frame: pd.DataFrame, *, snapshot_id: str | None = None) -> tuple[pd.DataFrame, pd.Series]:` に拡張。L420 `assert_matrix_columns_registered(spec, FEATURE_COLUMNS)` を `_derive_feature_columns(snapshot_id)` で取得したリストに変更。L425 `frame[FEATURE_COLUMNS]` も同様。完全一致 assert(L427-431)は動的取得したリストで行う。

    (g) **prepare_model_matrix の parameterization(L471-499)**: `snapshot_id: str | None = None` 引数を追加し・make_X_y に伝播。

    (h) **load_frozen_maps の parameterization(L642-652)**: シグネチャに `snapshot_id: str | None = None` を追加。snapshot_id=None は既存 v1.0 path・指定時は `_snapshot_paths(snapshot_id)[2]`(category_map path)。存在しない category_map は明示的 FileNotFoundError( Silent fallback 禁止・D-13 fail-loud)。

    (i) **後方互換性の保証**: 既存の `from src.model.data import FEATURE_COLUMNS` (P04 audit 等)は引き続き v1.0 デフォルトで動作する。既存の `load_feature_matrix()`(arity 0 呼出・test_data.py 等)は snapshot_id=None で v1.0 を返す。既存の `make_X_y(frame)` も v1.0 デフォルト。新コード(P05 stop gate)のみが明示的に snapshot_id を渡す。

    (j) **docstring 更新**: data.py モジュール docstring と各関数 docstring に「REVIEW H1: snapshot_id parameterization・v1.0 硬結合解除・後方互換 A5」を追記。

    (k) **REVIEW H1-b(orchestrator.train_and_predict への snapshot_id 伝播・新規・最優先)**: src/model/orchestrator.py は現状 `make_X_y(train_df)` / `make_X_y(calib_df)` / `make_X_y(test_df)`(L373-375) を bare call で起動する。本 Task の (f) で make_X_y が snapshot_id を受け取るようにしても・orchestrator が伝播しないと「speed_figure snapshot で学習しても v1.0 FEATURE_COLUMNS が静かに使われる」失敗が残る(P05 stop gate がこの失敗を検出できない)。以下を実装する:
        - **train_and_predict シグネチャ拡張(L234-246)**: 新引数 `snapshot_id: str | None = None` を追加する。既存 `feature_snapshot_id`(L238・provenance/model_version 用・FEATURE_COLUMNS 選択には無関係)とは別引数である。docstring で「snapshot_id: FEATURE_COLUMNS 選択用(feature_snapshot_id とは別・H1-b)・None は v1.0 デフォルト(後方互換 A5)」と明記。
        - **内部 make_X_y 呼出の書換(L373-375)**: 3箇所を `make_X_y(train_df, snapshot_id=snapshot_id)` / `make_X_y(calib_df, snapshot_id=snapshot_id)` / `make_X_y(test_df, snapshot_id=snapshot_id)` に書換える。bare `make_X_y(` が予測経路に残らないこと。
        - **_assert_deterministic(SC#4 reproduce smoke・L758-820)**: シグネチャに `snapshot_id` を追加し・train_and_predict の2回呼出に `snapshot_id=snapshot_id` を伝播する(reproduce smoke でも両 snapshot で同一契約)。
        - **test 追加(tests/model/test_data.py 拡張)**: `test_make_X_y_uses_snapshot_feature_columns`(H1-b) を追加。合成 speed_figure snapshot Parquet(snapshot_id="test-speed-figure"・rolling_speed_figure_* 6 列を含む)を一時生成し・make_X_y(frame, snapshot_id="test-speed-figure") が返す X.columns が rolling_speed_figure_* を含み・snapshot_id=None(v1.0)は含まないことを assert。更に orchestrator.train_and_predict を合成 Parquet 経由で呼出し・内部 make_X_y が伝播された snapshot_id で FEATURE_COLUMNS を選択することを mock/合成 frame で実証(H1-b 静かな失敗の閉塞)。
        - **後方互換**: snapshot_id=None は v1.0 デフォルトで既存 run_train_predict.py・test_data.py が変更不要で動作する(A5)。run_train_predict.py の --snapshot-id 検証(L174-193)緩和は本 Task のスコープ外・別途 P05 stop gate スクリプトが直接 orchestrator.train_and_predict(snapshot_id=...) を呼ぶ形で迂回(P05 で対応)。

    **注意**: scripts/run_train_predict.py の --snapshot-id 検証(L174-193)は別途対応が必要だが・本 Task のスコープは data.py parameterization と orchestrator.train_and_predict の snapshot_id 伝播(H1-b・(k)) のみ。run_train_predict.py は data.py の新 API を呼ぶよう別 task/plan で更新するか・P05 stop gate スクリプトが直接 orchestrator.train_and_predict(snapshot_id=...) を呼ぶことで迂回する(P05 で対応)。
  </action>
  <verify>
    <automated>uv run pytest tests/model/test_data.py -x -k "data or make_X_y or snapshot_id or feature_columns"</automated>
    <automated_extra>uv run python -c "import inspect, src.model.data as d, src.model.orchestrator as o; sig_d = inspect.signature(d.load_feature_matrix); sig_o = inspect.signature(o.train_and_predict); assert 'snapshot_id' in sig_d.parameters, 'H1-a: load_feature_matrix must accept snapshot_id (no arity-0 escape)'; assert 'snapshot_id' in sig_o.parameters, 'H1-b: train_and_predict must accept snapshot_id'; import subprocess, pathlib; orch = pathlib.Path('src/model/orchestrator.py').read_text(); bare = [ln for ln in orch.splitlines() if 'make_X_y(' in ln and 'snapshot_id=' not in ln and not ln.strip().startswith('#')]; assert not bare, f'H1-b: bare make_X_y( without snapshot_id remains in orchestrator prediction path: {bare}'; print('H1-a/H1-b OK')"</automated_extra>
  </verify>
  <done>
    - load_feature_matrix(snapshot_id) が v1.0 と speed_figure 両 snapshot をロード可能(後方互換: snapshot_id=None は v1.0)
    - _derive_feature_columns(snapshot_id) が選択 snapshot から FEATURE_COLUMNS を動的導出
    - make_X_y/prepare_model_matrix/verify_snapshot_sha256/load_frozen_maps が snapshot_id 引数を受け取る(後方互換デフォルト付き)
    - 既存 tests/model/test_data.py が全て GREEN(回帰なし・A5 後方互換)
    - FEATURE_COLUMNS(v1.0 デフォルト・モジュールレベル)は既存 import を壊さず維持
    - REVIEW H1-a: load_feature_matrix の acceptance から 'or list(sig.parameters)==[]' 逃げ道を削除し snapshot_id を必須パラメータ化・古い arity-0 関数を構造的に拒否
    - REVIEW H1-b: orchestrator.train_and_predict が snapshot_id 引数を受け取り・内部3箇所の make_X_y 呼出(L373-375)が snapshot_id=snapshot_id で明示伝播・予測経路に bare 'make_X_y(' が残らない(grep/AST verify GREEN)・新 snapshot の rolling_speed_figure_* が FEATURE_COLUMNS として実際消費されるテスト GREEN
    - REVIEW H1 横断的: P04 audit・P05 stop gate が新 snapshot で学習可能になる基盤が完成
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| builder → rolling → snapshot | builder.py が history に speed_figure 列を付与し・rolling が集約し・snapshot.py が byte-reproducible Parquet に書出す。各段階で列欠損・順序不整合・metadata 混入が起きうる |
| registry ↔ 実カラム | feature_availability.yaml(P02)の rolling_speed_figure_* と Parquet 実カラムが一致する必要がある(HIGH #3・silent parity 違反は course_kubun bug の再発) |
| data.py ↔ snapshot(P04/P05 横断) | REVIEW H1: data.py が v1.0 snapshot に硬結合していると・新 snapshot の rolling_speed_figure_* が model input に入らない(静かな失敗)。load_feature_matrix(snapshot_id) と _derive_feature_columns(snapshot_id) で両 snapshot を切替可能にする |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-10 | Tampering | builder Step 5b での PIT 違反(speed_figure 計算が target 当日を含む) | high | mitigate | compute_speed_figure_for_history は observations を受け取り _pit_cutoff_prefilter で strict < を適用・builder は feature_matrix を observations として渡す(SC#2・P01 と対称) |
| T-09-11 | Repudiation | snapshot 再生成で SHA256 が変わる(byte-reproducibility 破壊・§19.1) | high | mitigate | speed_figure は float で決定論的アルゴリズム(median/trim_mean・固定順序)・snapshot.py numeric path で自動 Float64 化・同一入力で bit-identical を SC#1 テストで実証 |
| T-09-12 | Tampering | _HISTORY_DB_SELECT_COLUMNS に time を追加したことによる TARGET_OBS_BANNED 衝突 | medium | mitigate | `time` は TARGET_OBS_BANNED_COLUMNS(availability L91-100)に含まれない・disjoint assert(builder L320-322)が維持される・テストで確認 |
| T-09-13 | Tampering | rolling_speed_figure_* が registry と Parquet で不一致(silent parity) | high | mitigate | SC#3 テストで registered_feature_columns と FEATURE_COLUMNS と実出力の3者整合を assert・HIGH #3 assert_matrix_columns_registered が GREEN |
| T-09-25 | Tampering/Spoofing | REVIEW H1: data.py SNAPSHOT_PATH 硬結合で新 snapshot の rolling_speed_figure_* が model input に入らない(静かな失敗・D-13 公平性違反の下地) | high | mitigate | load_feature_matrix(snapshot_id)・_derive_feature_columns(snapshot_id) parameterization・後方互換 A5 デフォルト・make_X_y/prepare_model_matrix/load_frozen_maps に snapshot_id 伝播・acceptance から arity-0 escape を削除し snapshot_id を必須化(H1-a)・既存 test_data.py GREEN で回帰保証 |
| T-09-26 | Tampering/Spoofing | REVIEW H1-b(新規): orchestrator.train_and_predict 内部の make_X_y(train_df)(L373-375) が bare call で snapshot_id 未伝播の結果・speed_figure snapshot で学習しても v1.0 FEATURE_COLUMNS が静かに使われる(P05 stop gate も検出不能) | high | mitigate | train_and_predict シグネチャに snapshot_id 引数を追加し内部3箇所の make_X_y を snapshot_id=snapshot_id で明示伝播・予測経路に bare make_X_y( が残らないことを grep/AST で verify・test_make_X_y_uses_snapshot_feature_columns で新 snapshot の FEATURE_COLUMNS が実際消費されることを機能証明 |
| T-09-27 | Tampering | REVIEW H1-c(新規): builder Step 5b 挿入時点で feature_matrix に obs_id 未構築(Step 6 L505-517 で初出)のため P01 API 契約(observations は obs_id を持つ)違反・compute_speed_figure_for_history が obs_id 展開できず cross-observation leak または silent fall-back | high | mitigate | Step 5b の直前に既存 idiom(race_nkey+kettonum or index+kettonum・L505-517 と同一)で feature_matrix["obs_id"] を早期構築・P01 API 契約を充足・obs_id は Step 6 で再利用され Step 6b で drop(契約不変) |
</threat_model>

<verification>
- `uv run pytest tests/features/test_speed_figure.py tests/features/test_speed_figure_builder_integration.py tests/features/test_builder.py tests/model/test_data.py -x` が GREEN
- `grep -c 'compute_speed_figure_for_history' src/features/builder.py` >= 1
- `grep -c 'ur.time AS time' src/features/builder.py` >= 1
- SHA256 bit-identical が SC#1 テストで実証
- rolling_speed_figure_* 6 feature が FEATURE_COLUMNS に含まれる(SC#3)
- REVIEW H1-a: `grep -c 'def load_feature_matrix' src/model/data.py` >= 1・シグネチャに snapshot_id 引数(デフォルト None)追加・既存 tests/model/test_data.py が後方互換で GREEN・acceptance から arity-0 escape('or list(sig.parameters)==[]')を削除
- REVIEW H1-b: `grep -nE 'make_X_y\(' src/model/orchestrator.py | grep -v 'snapshot_id=' | grep -v '^\s*#' | wc -l` == 0(予測経路に bare call 残存なし・train_and_predict が snapshot_id を内部 make_X_y に伝播)
- REVIEW H1-c: `grep -n 'obs_id' src/features/builder.py | head -20` で Step 5b 直前の obs_id 早期構築が L494 周辺に現れる・Step 6 と Step 6b の obs_id 取り扱いは従来通り
</verification>

<success_criteria>
- SC#1 byte-reproducible: 同一 DataFrame で snapshot 再生成すると SHA256 が bit-identical
- SC#3 registry↔Parquet parity: rolling_speed_figure_* 6 feature が registry と FEATURE_COLUMNS と実カラムで一致・assert_matrix_columns_registered GREEN
- builder Step 5b が history に speed_figure を付与し・rolling が rolling_speed_figure_* 6 feature を出力
- copy-not-rename: 既存 history/feature_matrix 列は保持
- 既存 test_builder.py が回帰なく GREEN
</success_criteria>

<output>
`.planning/phases/09-speed-figure-foundation/09-03-SUMMARY.md` を作成し・以下を記録する:
- builder.py Step 5b の最終挿入位置と CR-01 merge との順序
- _HISTORY_DB_SELECT_COLUMNS への time 追加
- 新 feature_snapshot_id 候補(例: 20260625-1a-speedfigure-v1・v1.0 系統継承・make_model_version prefix 整合)
- SC#1/SC#3 統合テスト GREEN 実績
- P04(audit/可視化)が依存する情報: feature_matrix に rolling_speed_figure_* 6 feature が含まれる・registry と Parquet parity が GREEN
</output>

## Artifacts this phase produces

本 PLAN(P03)が拡張・新規作成するシンボル:
- `src/features/builder.py`(拡張):
  - `_HISTORY_DB_SELECT_COLUMNS`(に `"ur.time AS time"` 追加)
  - `build_feature_matrix` の Pipeline に「Step 5b: speed_figure 計算」挿入(compute_speed_figure_for_history 呼出)
- `tests/features/test_speed_figure.py`(拡張):
  - `test_byte_reproducible_snapshot_with_speed_figure`(SC#1)
  - `test_registry_parquet_parity_speed_figure`(SC#3)
- `tests/features/test_speed_figure_builder_integration.py`(新規):
  - `test_builder_step5b_adds_speed_figure_to_history`
  - `test_builder_step5b_copy_not_rename`
  - `test_builder_step5b_pit_correct`
- `src/model/data.py`(REVIEW H1 拡張):
  - `DEFAULT_SNAPSHOT_PATH` / `DEFAULT_SNAPSHOT_MANIFEST_PATH` / `DEFAULT_CATEGORY_MAP_PATH`(L77-79 rename・v1.0 デフォルト値保持)
  - `_snapshot_paths(snapshot_id)` helper(新設・snapshot_id から parquet/manifest/category_map path を解決)
  - `load_feature_matrix(snapshot_id=None)`(L211 拡張・両 snapshot ロード可能・H1-a: snapshot_id を必須パラメータ化・arity-0 escape 削除)
  - `_derive_feature_columns(snapshot_id=None)`(L149 拡張・選択 snapshot から FEATURE_COLUMNS 動的導出)
  - `verify_snapshot_sha256(snapshot_id=None)`(L232 拡張)
  - `make_X_y(frame, *, snapshot_id=None)`(L405 拡張)
  - `prepare_model_matrix(feature_df_or_frame, *, readonly_cur=None, snapshot_id=None)`(L471 拡張)
  - `load_frozen_maps(snapshot_id=None)`(L642 拡張)
  - 注: `FEATURE_COLUMNS`(モジュールレベル・L176)は v1.0 デフォルトのまま残存(後方互換・既存 import 保護)
- `src/model/orchestrator.py`(REVIEW H1-b 拡張・新規):
  - `train_and_predict(feature_df, *, model_type, feature_snapshot_id, ..., snapshot_id=None)`(L234 拡張・新 snapshot_id 引数追加・FEATURE_COLUMNS 選択用・feature_snapshot_id とは別)
  - L373-375 の3箇所の `make_X_y(...)` を `make_X_y(..., snapshot_id=snapshot_id)` に書換(予測経路の bare call を排除)
  - `_assert_deterministic`(L758-820)も snapshot_id 引数を追加し reproduce smoke で両 snapshot 同一契約を保証
- `tests/model/test_data.py`(REVIEW H1-b 拡張):
  - `test_make_X_y_uses_snapshot_feature_columns`(新規・合成 speed_figure snapshot Parquet で X.columns が rolling_speed_figure_* を含むこと・snapshot_id=None は含まないことを assert・orchestrator.train_and_predict も合成 frame で検証)

後続 PLAN が参照する新シンボル:
- P04 は builder.py が Step 5b で compute_speed_figure_for_history を呼ぶことを AST audit で確認
- P04 の SC#5 ドメイン整合性は完成した feature_matrix が必要(本 PLAN で生成可能に)
- P05 stop gate は feature_snapshot_id(本 PLAN で候補決定)と rolling_speed_figure_* を含む FEATURE_COLUMNS を使用
