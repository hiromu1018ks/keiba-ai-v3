---
phase: 09-speed-figure-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/speed_figure.py
  - tests/features/test_speed_figure.py
  - tests/features/test_speed_figure_pit.py
  - tests/features/conftest.py
autonomous: true
requirements:
  - FEAT-01
  - SAFE-01
user_setup: []

must_haves:
  truths:
    - "src/features/speed_figure.py が par time・variant・speed_figure を PIT-correct（available_at < feature_cutoff_datetime・strict <）に算出する（D-03 PIT 集約 invariant・SC#2）"
    - "D-03: 各 speed figure に available_at を持ち・feature row では available_at < feature_cutoff_datetime(= race_date - 1day・JST midnight・strict <)の指数のみ集計する・full-period par/variant と target day results の混入は禁止（merge_asof(direction='backward') 相当・5段階 adversarial lookahead テスト GREEN・SC#2）"
    - "REVIEW H4 対応(厳格化・集約キーに obs_id 明示): par/variant は展開済みフレーム上で groupby する際・group キーに obs_id を含め observation 毎に独立した par/variant を算出する(par fallback group = obs_id + jyocd×trackcd×kyori・variant group = obs_id + source_race_date×jyocd×surface)。これにより cutoff の異なる複数 observation が同一 jyocd×trackcd×kyori group に混入し earlier observation の par/variant median に later observation の eligible 行が漏れる cross-observation leak を構造的に回避する(rolling.py L236-265 obs_id expand idiom と対称)"
    - "time は 0.1秒単位(decisecond)で解釈し time/10.0 で秒換算・time<=0(取消/競走中止4882件)は NaN 化する"
    - "D-04: par の層別キーは jyocd×trackcd×kyori 粒度（expanding/as-of robust median・サンプル不足時 trackcd×kyori へ fallback・fallback_level 監査列 D-01/D-07）とし・クラスは層別キーに入れない（強い馬が速い能力差を消さないため・クラスはスケール調整/variant 補助で扱う）・course_kubun にも依存しない"
    - "variant は source_race_date×jyocd×surface 単位の leave-one-race-out robust median(D-02)・自レースは除外する"
    - "REVIEW H4 adversarial truth: ある observation O_i の par/variant は available_at < O_i.feature_cutoff_datetime を満たす行**のみ**で算出される。別 observation O_j(j≠i) に属する未来行(available_at >= O_i.feature_cutoff_datetime だが available_at < O_j.feature_cutoff_datetime)を O_j の expand frame に注入しても・O_i の par/variant は変化しない(集約キーが obs_id 区分のため・cross-observation leak が構造的不能・SC#2 拡張)"
    - "canonical feature は丸めない float(D-05)・D-06: speed_figure_int = round(speed_figure) は監査/可視化用のみ(モデル入力でない)"
    - "オッズ/人気/過去人気/過去オッズ proxy は speed_figure.py のソースに一切現れない(SC#4 下地・SAFE-01)"
  artifacts:
    - "src/features/speed_figure.py(新規): compute_speed_figure_for_history・_pit_cutoff_prefilter・_points_per_second・POINTS_PER_SECOND_BY_DISTANCE_M・_compute_pit_par・_compute_leave_one_out_variant"
    - "tests/features/test_speed_figure.py(新規): par/variant/byte-reproducible/parity 単体テスト"
    - "tests/features/test_speed_figure_pit.py(新規): SC#2 5段階 adversarial lookahead テスト"
    - "tests/features/conftest.py(拡張): speed_figure 用合成 history builder 追加"
  key_links:
    - "speed_figure.py::_pit_cutoff_prefilter は availability.CUTOFF_SEMANTICS['comparison_operator']=='strict_less_than' を assert で固定(rolling.py と対称)"
    - "speed_figure.py は pandas/scipy.stats.trim_mean のみを使用し新規外部依存を追加しない"
    - "compute_speed_figure_for_history は history DataFrame を受け取り time/trackcd/jyocd/kyori/surface/babacd を消費して speed_figure 列(available_at=race_date)を付与して返す"
---

<objective>
Phase 9 の核となる `src/features/speed_figure.py` を新規実装し・par time(PIT expanding/as-of robust)・variant(leave-one-race-out)・speed_figure(Beyer 型 float) を odds-free・PIT-correct に算出する。同時に SC#1(byte-reproducible 単体)・SC#2(5段階 adversarial lookahead) のテストをWave 0 として作成する。

Purpose: 後続 P02(rolling 拡張)・P03(builder 統合)・P04(audit/可視化)・P05(stop gate) が依存する能力特徴量の新主軸を確立する。target race 当日結果の par/variant 混入は決定的リーク(high severity・BLOCK)なので・adversarial テストで構造的証明する。
Output: src/features/speed_figure.py(新規) + tests/features/test_speed_figure.py(新規) + tests/features/test_speed_figure_pit.py(新規) + conftest.py 拡張
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-speed-figure-foundation/09-CONTEXT.md
@.planning/phases/09-speed-figure-foundation/09-RESEARCH.md
@.planning/phases/09-speed-figure-foundation/09-PATTERNS.md
@src/features/rolling.py
@src/features/availability.py
@src/utils/pit_join.py
@tests/features/conftest.py
@tests/audit/test_audit_features.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: speed_figure.py 新規実装(par/variant/PIT/float・D-01〜D-07・D-09 素材算出)</name>
  <files>src/features/speed_figure.py</files>
  <read_first>
    - src/features/speed_figure.py(未作成・新規)
    - src/features/rolling.py(L104-116 _pit_cutoff_prefilter・L45-55 CUTOFF_SEMANTICS assert・L236-265 obs_id expand idiom・完全踏襲)
    - src/features/availability.py(L45-56 CUTOFF_SEMANTICS・L91-111 TARGET_OBS_BANNED/HISTORY_ALLOWED)
    - src/utils/pit_join.py(merge_asof primitive 参考)
    - .planning/phases/09-speed-figure-foundation/09-RESEARCH.md(Code Examples 全般・Pitfall 1-5・Pattern 1/2)
    - .planning/phases/09-speed-figure-foundation/09-PATTERNS.md(src/features/speed_figure.py pattern・L42-109)
  </read_first>
  <behavior>
    - time=1108.0(1200mダート1着) → time_sec=110.8(物理的妥当・50秒未満は NaN)
    - time<=0 は NaN 化(完走馬フィルタ・4882件除外)
    - trackcd 10-22→turf / 23-25→dirt / 51-59→obstacle(builder.py::_construct_derived_columns と同一閾値)
    - par は available_at < cutoff の過去走のみで算出(strict <・JST midnight)
    - target race 当日(as_of >= cutoff)行は par/variant に混入しない
    - variant leave-one-race-out: source race 自身を除く same-day residual の robust median
    - speed_figure = (par_sec - time_sec + variant_sec) × points_per_second(kyori) で速いほど大きい float
    - fallback_level は "jyocd_trackcd_kyori" / "trackcd_kyori" / "all_day" の3値のいずれか(監査列・NULL 禁止)
    - 空 DataFrame 入力は RuntimeError(fail-loud・WR-01 踏襲)
    - odds/ninki/fukuodds/ninkij/tansyouodds はソース上の識別子に一切現れない
  </behavior>
  <action>
    `src/features/speed_figure.py` を新規作成する。モジュール docstring の冒頭に「Phase 9・Beyer 型スピード指数(odds-free・PIT-correct・SC#1/SC#2・D-01〜D-07/D-09)」を明記し・odds-free 原則・PIT strict <・byte-reproducible の3聖域を文書化する。

    [定数]
    (a) `from src.features.availability import CUTOFF_SEMANTICS` を import し・モジュール top-level で `assert CUTOFF_SEMANTICS["comparison_operator"] == "strict_less_than"` を実行(rolling.py L55 と対称・単一不変量)。
    (b) `POINTS_PER_SECOND_BY_DISTANCE_M: dict[int, float]` を RESEARCH.md Code Examples の値(1000:16.5, 1200:13.2, 1400:11.0, 1600:10.0, 1800:8.8, 2000:8.0, 2400:6.6, 3000:5.3, 3200:5.0)で定義する(D-05・Beyer 文献 [ASSUMED]・SC#5 で微調整可)。コメントで "[ASSUMED] Beyer 文献ベース概算値" を明記。
    (c) `_MIN_SAMPLES_PAR_JYO_TRACK_KYORI = 30`・`_MIN_SAMPLES_PAR_TRACK_KYORI = 30`・`_MIN_SAMPLES_VARIANT_GROUP = 10`・`_TRIM_PROPORTION = 0.1`(scipy.stats.trim_mean 用)を定義(planner discretion・live-DB 精査値)。

    [helper 関数]
    (d) `def _pit_cutoff_prefilter(expanded: pd.DataFrame) -> pd.DataFrame:` を rolling.py L104-116 と対称に切出す。`expanded[expanded["as_of_datetime"] < expanded["feature_cutoff_datetime"]].copy()` を返す。docstring で「adversarial test が monkeypatch で本関数を <= 版に差替可能にするため helper 化(SC#2・rolling.py と対称)」を明記。
    (e) `def _time_to_seconds(time_real) -> float:` RESEARCH Code Examples 通り・None/<=0 は NaN・それ以外は /10.0。vectorized 版の `def _time_to_seconds_series(time_series: pd.Series) -> pd.Series:` も用意(`pd.to_numeric(errors="coerce").where(>0)/10.0`)。
    (f) `def _derive_surface(trackcd) -> pd.Series:` RESEARCH Code Examples 通り・trackcd 数値範囲で turf/dirt/obstacle を返す。unknown は "unknown"(sentinel)。
    (g) `def get_points_per_second(kyori: int) -> float:` RESEARCH Code Examples 通り・線形補間・端点クランプ。

    [par/variant 算出 — REVIEW H4: per-observation PIT を集約キー仕様で明示化(obs_id 必須)]
    (h) `def _compute_pit_par(expanded_filtered: pd.DataFrame) -> pd.DataFrame:` を実装。入力は rolling.py L236-265 と対称な **obs_id 展開済みかつ _pit_cutoff_prefilter(strict <) 済み**のフレーム(obs_id, kettonum, feature_cutoff_datetime, race_date, as_of_datetime 列を含む)。**REVIEW H4(集約キー仕様)**: par 算出の groupby キーは **obs_id + jyocd + trackcd + kyori** とする(obs_id が先頭・observation 毎に独立した par)。各 group で time_sec の robust median(pandas .median()・決定論的)を算出。group size < _MIN_SAMPLES_PAR_JYO_TRACK_KYORI の group は obs_id + trackcd + kyori で再計算・それでも不足は obs_id 単位の all-history median。各 row に par_sec / sample_count / fallback_level 列を付与(D-07)。`fallback_level` は文字列で "jyocd_trackcd_kyori" / "trackcd_kyori" / "all_day" の3値。**重要(REVIEW H4・集約キーに obs_id が必須)**: par は各 observation の cutoff 以前の行のみで算出される**だけでなく**・groupby キーが obs_id 区分なため cutoff の異なる observation が同一 par median に混入しない(rolling.py L236-265 obs_id expand + groupby と対称)。もし obs_id を groupby キーから外すと・jyocd×trackcd×kyori が同一で cutoff の異なる2 observation が同一 group に混入し earlier obs の eligible 行が later obs の par に漏れる。docstring で「per-observation PIT invariant・集約キー = obs_id + jyocd×trackcd×kyori(obs_id 必須)・展開後に as_of < cutoff を assert」と明記。
    (i) `def _compute_leave_one_out_variant(expanded_with_par: pd.DataFrame) -> pd.DataFrame:` を実装(D-02)。入力は (h) の出力(展開済み・par_sec 付与済み)。residual = time_sec - par_sec を算出後・**REVIEW H4(集約キー仕様)**: group キーは **obs_id + source_race_date + jyocd + surface** とする(obs_id が先頭・par と対称に observation 毎に独立)。vectorized 近似(RESEARCH Pattern 2): group 内で「自レースを除く residual の median」を算出。pandas で `group.transform(lambda s: s.drop(s.index).median())` は精度担保だが計算重いので・実装では `group_median - (self_residual - group_median)/(n-1)` の一次近似を使い docstring で「A3 に基づく近似(avg 215完走馬/group で自レース寄与<1%)」を明記(MEDIUM 懸念・精度上限を docstring/test で明示済み)。group size < _MIN_SAMPLES_VARIANT_GROUP は obs_id 単位の all-history robust variant へ fallback。variant_sec 列を付与(D-07・監査列)。source_race_date 列は pd.to_datetime(race_date).dt.date で導出。**variant も par と同一の展開済みフレーム上で obs_id 区分で算出**(グローバル未展開 history でなく・集約キーに obs_id を含む・REVIEW H4)。
    (j) `def compute_speed_figure(time_sec, par_sec, variant_sec, kyori) -> float:` RESEARCH Code Examples 通り。pd.isna(time_sec) or pd.isna(par_sec) は NaN。pps = get_points_per_second(kyori)・adjusted_diff = (par_sec - time_sec) + variant_sec・戻り値 = adjusted_diff * pps(float・丸めない・D-05)。

    [公開 API]
    (k) `def compute_speed_figure_for_history(history: pd.DataFrame, observations: pd.DataFrame | None = None) -> pd.DataFrame:` を実装。builder.py L502-556(推定脚質) と rolling.py L189-265 の idiom を統合:
        1. history に time/trackcd/jyocd/kyori/race_date/as_of_datetime 列が必須・欠損は ValueError(必須列明示)。
        2. history が空(新馬のみ)は RuntimeError(fail-loud・WR-01 踏襲)。
        3. time_sec = _time_to_seconds_series(history["time"])・surface = _derive_surface(history["trackcd"])・source_race_date = pd.to_datetime(history["race_date"]).dt.date を history に追加(copy-not-rename・既存列は破壊しない)。
        4. **REVIEW H4 (per-observation PIT・rolling.py L236-265 と対称)**: observations が渡された場合は rolling.py L237-243 と同様に `obs_keys = observations[["obs_id","kettonum","feature_cutoff_datetime"]].copy()` で obs_id×kettonum×feature_cutoff_datetime を取り・`expanded = hist.merge(obs_keys, on="kettonum", how="inner")` で各 observation に過去走を紐付け(obs_id 単位の展開)。その後 `expanded_filtered = _pit_cutoff_prefilter(expanded)` で strict < を適用し・**展開済みフレーム上で** (h)/(i) を呼出し par/variant を算出(グローバル history でなく)。observations が None(テスト用)の場合は history 自身の全行で par/variant を算出(この場合 cutoff は history.max(as_of_datetime)+1day を仮・docstring 明記・本パスは unit test 専用)。実運用は builder が observations を渡す(production で observations=None は禁じる・docstring 明記)。
        5. speed_figure 列を compute_speed_figure で付与(丸めない float)。
        6. 監査列(par_sec/variant_sec/speed_residual_sec=time_sec-par_sec/sample_count/fallback_level)を付与(D-07)。
        7. available_at = pd.to_datetime(history["race_date"]) 列を付与(rolling の PIT 集約で使用)。
        戻り値: history に speed_figure/available_at/監査列を copy 追加した DataFrame(copy-not-rename)。

    [静的保証(SC#4 下地)]
    (l) モジュール末尾に `# SAFE-01 odds-free: 本モジュールは odds/ninki/fukuodds/ninkij/tansyouodds proxy を一切使用しない(SC#4・AST audit 対象)` コメントを置く(P04 audit が grep で確認)。これら禁止トークンは識別子・文字列リテラル・コメントのいずれにも現れないこと。
  </action>
  <verify>
    <automated>uv run pytest tests/features/test_speed_figure.py tests/features/test_speed_figure_pit.py -x</automated>
  </verify>
  <done>
    - src/features/speed_figure.py に compute_speed_figure_for_history と _pit_cutoff_prefilter と _compute_pit_par と _compute_leave_one_out_variant と get_points_per_second が存在する
    - CUTOFF_SEMANTICS["comparison_operator"] == "strict_less_than" の assert がモジュール top-level にある
    - time<=0 は NaN に変換される(単体テスト GREEN)
    - trackcd 10-22→turf/23-25→dirt/51-59→obstacle が派生する(単体テスト GREEN)
    - fallback_level は "jyocd_trackcd_kyori"/"trackcd_kyori"/"all_day" の3値のみ(NULL 禁止・単体テスト GREEN)
    - odds/ninki/fukuodds/ninkij/tansyouodds がソース上の識別子に現れない(grep 検証・P04 で完全 AST audit)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test_speed_figure.py + test_speed_figure_pit.py 新規作成(SC#1 unit + SC#2 adversarial)</name>
  <files>tests/features/test_speed_figure.py, tests/features/test_speed_figure_pit.py, tests/features/conftest.py</files>
  <read_first>
    - tests/features/test_speed_figure.py(未作成・新規)
    - tests/features/test_speed_figure_pit.py(未作成・新規)
    - tests/features/conftest.py(_build_se_history_row・_build_adversarial_rolling_rows・_build_race_obs_row を再利用/拡張)
    - tests/audit/test_audit_features.py(L35-125 5段階鋳型・guard monkeypatch・docstring cross-reference)
    - src/features/speed_figure.py(Task 1 で作成)
    - src/features/rolling.py L104-116(_pit_cutoff_prefilter の analog)
  </read_first>
  <behavior>
    - test_speed_figure.py: par 計算(time/kyori/jyocd/trackcd 合成値で par_sec が既知の central tendency に一致)・time<=0 の NaN 化・fallback 階層(サンプル不足 group が上位階層へ fallback・fallback_level 文字列確認)・variant leave-one-race-out(自レース除外で residual median 変化)・speed_figure 速いほど大きい・byte-reproducible(同一入力で同一出力・決定論的)
    - test_speed_figure_pit.py: 5段階鋳型(target/same_day_prior/same_day_later/previous_day/future が全て par/variant から除外される)・guard monkeypatch で < を <= に緩めると previous_day が混入して speed_figure が変化する(false-pass 回避)
    - REVIEW H4 (test_cross_observation_pit_no_leak): cutoff の異なる2 observation で・集約キーに obs_id が含まれるため・later observation 専用の介入行が earlier observation の par/variant に漏れないこと。guard monkeypatch + obs_id groupby 外しの hack で逆に混入が生じることで false-pass 回避を機械実証
    - docstring に "SC#2 adversarial" + "cross-reference: tests/features/test_speed_figure.py" を必ず含む(T-08-04 踏襲)
  </behavior>
  <action>
    [conftest.py 拡張]
    `tests/features/conftest.py` に `_build_speed_figure_history_rows(kettonum, obs_race_date)` を追加する。既存 `_build_se_history_row` を拡張し・time/trackcd/jyocd/kyori/race_date 列を持つ合成 history を生成する。5段階 adversarial(target/same_day_prior/same_day_later/previous_day/future) + eligible 3行の計8行・各区別 time 値(例: eligible は time=1100/1110/1120・adversarial は time=9990 など識別値)を持つ。rolling 用 `_build_adversarial_rolling_rows` と同様の idiom だが kakuteijyuni でなく time を識別値にする(speed_figure が time 由来のため)。docstring で「SC#2 adversarial・speed_figure は time 識別値で混入を機械検出」と明記。

    [test_speed_figure.py]
    新規作成。モジュール docstring に「SC#1/SC#3 単体テスト・speed_figure 算出・byte-reproducible・fallback・parity 下地」を明記。以下のテストを実装:
    (a) `test_time_decisecond_conversion`: time=1108.0 → 110.8 秒・time=0 は NaN・time<0 は NaN(time_to_seconds_series)。
    (b) `test_surface_derivation`: trackcd 10/17/20 は turf・23/24 は dirt・52/54 は obstacle・それ以外は unknown。
    (c) `test_points_per_second_interpolation`: kyori=1200→13.2・kyori=1600→10.0・kyori=1400(中間)は 11.0 と 10.0 の線形補間・端点(1000/3200)クランプ。
    (d) `test_par_pit_expanding`: 合成 history(eligible 3行 + target 当日 1行)で・target 当日行は par に混入しない(strict <)。par_sec は eligible 3行の median。
    (e) `test_par_fallback_hierarchy`: jyocd×trackcd×kyori group が _MIN_SAMPLES 未満の場合・trackcd×kyori へ fallback し fallback_level=="trackcd_kyori"。更に不足で "all_day"。NULL 禁止。
    (f) `test_variant_leave_one_race_out`: source race 自身を除く same-day residual median・自レースを含めると変化(D-02・近似精度は docstring 明記の範囲内)。
    (g) `test_speed_figure_float_monotone`: par より速い(time<par)ほど speed_figure が大きい(float・丸めない・D-05)。
    (h) `test_byte_reproducible_speed_figure`: 同一 history+observations で compute_speed_figure_for_history を2回呼出し・speed_figure 列が bit-identical(np.array_equal)。seed 固定不要・決定論的アルゴリズムのため。
    (i) `test_empty_history_raises`: history が空 DataFrame の場合は RuntimeError(fail-loud・WR-01)。
    (j) `test_compute_speed_figure_for_history_adds_columns`: 戻り値に speed_figure/available_at/par_sec/variant_sec/speed_residual_sec/sample_count/fallback_level 列が追加される(copy-not-rename・入力列は保持)。

    [test_speed_figure_pit.py]
    新規作成。モジュール docstring の冒頭に「SC#2 adversarial(注入型メタ検証)・cross-reference: tests/features/test_speed_figure.py」を必ず含める(test_audit_features.py L128-146 と同一 T-08-04 義務)。以下を実装:
    (a) `test_lookahead_injection_detected_and_fails`: test_audit_features.py L35-125 の5段階鋳型を speed_figure に適用:
       (1) _build_speed_figure_history_rows で8行合成 history 構築(obs_race_date="2023-06-04"・kettonum=1001)。
       (2) guard 有効な通常経路で baseline の speed_figure を取得(eligible 3行のみで算出された値)。
       (3) `import src.features.speed_figure as sf_mod`・`sf_mod._pit_cutoff_prefilter` を <= 版に monkeypatch。
       (4) guard 有効経路を再実行し・baseline と一致することを assert(混入検出)。
       (5) guard 無効経路で previous_day(time 識別値)が混入し・speed_figure が baseline から変化することを assert(false-pass 回避・T-08-01 mitigate)。
       finally ブロックで `sf_mod._pit_cutoff_prefilter = original` を確実に戻す(test_audit_features.py L83-87 と同一 idiom)。
    (b) `test_docstring_cross_reference`: モジュール docstring と test 関数 docstring に "SC#2 adversarial" + "cross-reference: tests/features/test_speed_figure.py" が含まれることを assert(T-08-04・test_audit_features.py L128-146 踏襲)。

    (c) `test_cross_observation_pit_no_leak`(REVIEW H4・2-observation adversarial・新規): cutoff の異なる2つの observation O_early(cutoff=2023-06-03)と O_late(cutoff=2023-06-10)を構築する。同一 jyocd×trackcd×kyori(例: jyocd=05・trackcd=24・kyori=1600)で race_date=2023-06-04 の介入行(O_early では available_at >= cutoff で ineligible・O_late では available_at < cutoff で eligible)を含む合成 history を渡す。集約キーに obs_id が含まれるため・O_early の par/variant が O_late 専用の介入行の影響を受けないことを assert(注入前後で O_early の par_sec/variant_sec が一致)。更に guard monkeypatch で _pit_cutoff_prefilter を無効化(`<=` 版または完全 skip)した上で obs_id を groupby キーから外すような hack を注入すると・O_early の par が変化することを assert(false-pass 回避・H4 per-obs invariant が guard でなく集約キー仕様に依拠することを実証)。docstring で「H4: 集約キー = obs_id + jyocd×trackcd×kyori・cross-observation leak 構造的不能・guard 無効化でも invariant が groupby 仕様由来であることを実証」と明記。
  </action>
  <verify>
    <automated>uv run pytest tests/features/test_speed_figure.py tests/features/test_speed_figure_pit.py -x -v</automated>
  </verify>
  <done>
    - tests/features/test_speed_figure.py の9テストが全て GREEN
    - tests/features/test_speed_figure_pit.py の3テスト(5段階鋳型 + docstring cross-reference + REVIEW H4 cross_observation_pit_no_leak)が GREEN
    - test_speed_figure_pit.py のモジュール docstring に "SC#2 adversarial" と "cross-reference: tests/features/test_speed_figure.py" が含まれる
    - guard monkeypatch で < を <= に緩めると previous_day の time 識別値が speed_figure に混入して値が変化する(false-pass 回避の実証)
    - REVIEW H4: cutoff の異なる2 observation で later obs 専用介入行が earlier obs の par/variant に漏れないこと・guard monkeypatch + obs_id groupby 外しで逆に混入が生じることを実証(false-pass 回避・H4 invariant が集約キー仕様由来)
    - conftest.py に _build_speed_figure_history_rows が追加され・8行の合成 history を生成する
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| normalized 層 → speed_figure.py | readonly SELECT された time/kyori/trackcd/jyocd/babacd/race_date が境界を越える。素材は raw 素材のまま・derived は本モジュール内で計算 |
| history → observations PIT 境界 | target race 当日(as_of >= feature_cutoff_datetime)の走破タイム/馬場結果が par/variant に混入すると決定的リーク(SC#2・high severity・BLOCK) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering/Elevation | _compute_pit_par での target 当日結果混入 | high | mitigate | `available_at < feature_cutoff_datetime`(strict <)・5段階 adversarial テスト GREEN・rolling.py と対称な _pit_cutoff_prefilter helper(monkeypatch 可能) |
| T-09-02 | Tampering | _compute_leave_one_out_variant での source race 自身混入 | high | mitigate | leave-one-race-out(group から自レースを除外)・same-day residual の robust median・D-02 |
| T-09-03 | Tampering | par 計算での full-period 固定値混入(未来情報) | high | mitigate | expanding/as-of robust(各 observation の cutoff 以前の過去走のみ)・full-period 固定値は D-01 で明示禁止 |
| T-09-28 | Tampering | REVIEW H4: par/variant groupby キーが jyocd×trackcd×kyori(無 obs_id)のため・cutoff の異なる複数 observation が同一 group に混入し earlier obs の eligible 行が later obs の par/variant median に漏れる cross-observation leak | high | mitigate | par fallback group = obs_id + jyocd×trackcd×kyori・variant group = obs_id + source_race_date×jyocd×surface と集約キー仕様で obs_id を必須化・2-observation adversarial テスト(test_cross_observation_pit_no_leak)で later obs 専用介入行が earlier obs に影響しないことを実証・guard monkeypatch + obs_id 外し hack で false-pass 回避を機械証明 |
| T-09-04 | Tampering | speed_figure.py への odds/ninki/過去オッズ proxy 混入 | high | mitigate | odds-free 原則・ソース上の識別子に禁止トークンを一切現さない・P04 で AST audit で完全証明(SC#4・SAFE-01) |
| T-09-05 | Repudiation | byte-reproducibility 破壊(再現性聖域 §19.1) | medium | mitigate | 決定論的アルゴリズム(median/trim_mean・固定順序)・同一入力で bit-identical を単体テストで実証 |
| T-09-06 | Denial | silent data loss(空 history・必須列欠損の黙握り) | medium | mitigate | RuntimeError/ValueError で fail-loud(WR-01 踏襲)・空 history は即 fail |
| T-09-SC | Tampering | 新規外部パッケージ依存(npm/pip/cargo 等価) | low | accept | 新規依存なし(pandas/scipy は v1.0 既存・RESEARCH Package Legitimacy Audit で skip 明示) |
</threat_model>

<verification>
- `uv run pytest tests/features/test_speed_figure.py tests/features/test_speed_figure_pit.py -x` が GREEN
- `grep -c 'def compute_speed_figure_for_history' src/features/speed_figure.py` == 1
- `grep -c 'def _pit_cutoff_prefilter' src/features/speed_figure.py` == 1
- `grep -c 'strict_less_than' src/features/speed_figure.py` >= 1(CUTOFF_SEMANTICS assert)
- `grep -v '^#' src/features/speed_figure.py | grep -cE 'odds|ninki|fukuodds|ninkij|tansyouodds'` == 0(SC#4 下地・禁止トークンが実行コードに現れない)
- time<=0 の NaN 変換が単体テストで実証される
</verification>

<success_criteria>
- SC#1 byte-reproducible: 同一入力で compute_speed_figure_for_history が bit-identical な speed_figure 列を返す(単体テスト GREEN)
- SC#2 PIT-correct: target/same_day_prior/same_day_later/previous_day/future が全て par/variant から除外される(5段階 adversarial GREEN・guard 無効化で混入を実証)
- REVIEW H4 per-observation PIT: 集約キーが obs_id + jyocd×trackcd×kyori(par)/obs_id + source_race_date×jyocd×surface(variant)のため・cutoff の異なる2 observation 間で cross-observation leak が構造的不能(2-observation adversarial GREEN・guard 無効化でも invariant が集約キー仕様由来であることを実証)
- D-01 par 粒度: jyocd×trackcd×kyori(最細)→ trackcd×kyori fallback・course_kubun 非依存
- D-02 variant: leave-one-race-out で source race 自身を除外
- D-05 float: speed_figure は丸めない float
- D-07 監査列: par_sec/variant_sec/speed_residual_sec/sample_count/fallback_level が付与される
- SAFE-01 odds-free 下地: odds/ninki/fukuodds/ninkij/tansyouodds がソース識別子に現れない
</success_criteria>

<output>
`.planning/phases/09-speed-figure-foundation/09-01-SUMMARY.md` を作成し・以下を記録する:
- compute_speed_figure_for_history の最終シグネチャ
- POINTS_PER_SECOND_BY_DISTANCE_M の採用値(Beyer [ASSUMED])
- fallback 階層の実測サンプル数(live-DB 確認後に更新)
- SC#1/SC#2 テストの GREEN 実績
- P02(rolling 拡張) が依存する公開 API(compute_speed_figure_for_history が history に speed_figure 列を付与すること)
</output>

## Artifacts this phase produces

本 PLAN(P01)が新規作成するシンボル:
- `src/features/speed_figure.py`:
  - 定数: `POINTS_PER_SECOND_BY_DISTANCE_M`・`_MIN_SAMPLES_PAR_JYO_TRACK_KYORI`・`_MIN_SAMPLES_PAR_TRACK_KYORI`・`_MIN_SAMPLES_VARIANT_GROUP`・`_TRIM_PROPORTION`
  - helper: `_pit_cutoff_prefilter`・`_time_to_seconds`・`_time_to_seconds_series`・`_derive_surface`・`get_points_per_second`
  - 算出: `_compute_pit_par`・`_compute_leave_one_out_variant`・`compute_speed_figure`
  - 公開 API: `compute_speed_figure_for_history(history, observations=None) -> pd.DataFrame`
- `tests/features/test_speed_figure.py`: 9 テスト(time/surface/pps/par/variant/fallback/float/byte-repro/empty)
- `tests/features/test_speed_figure_pit.py`: 2 テスト(5段階 adversarial・docstring cross-reference)
- `tests/features/conftest.py`: `_build_speed_figure_history_rows` 追加

後続 PLAN が参照する新シンボル:
- P02 は `compute_speed_figure_for_history` が付与する history["speed_figure"] 列を前提とする
- P04 は `src/features/speed_figure.py` を AST audit 対象とする
