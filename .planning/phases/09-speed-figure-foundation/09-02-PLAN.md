---
phase: 09-speed-figure-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/features/rolling.py
  - src/features/availability.py
  - src/config/feature_availability.yaml
autonomous: true
requirements:
  - FEAT-01
user_setup: []

must_haves:
  truths:
    - "rolling.py の _ROLLING_SYSTEMS に 'speed_figure' が追加され・availability.py の _ROLLING_SYSTEMS_FOR_RESERVED にも同一順序で追加される(二重定義回避)"
    - "_axes_for('speed_figure') が D-09 の6 feature(mean/latest/sd/count/max・window 3 と 5)を出力するよう拡張される"
    - "feature_availability.yaml に rolling_speed_figure_* 6 feature エントリが7項目スキーマ完全踏襲で追加される"
    - "feature_availability.yaml の course_kubun エントリ(L129-135)が削除される(silent parity bug 修正・normalized 層にカラム不存在)"
    - "rolling_speed_figure_count_5 が _ROLLING_SYSTEMS_FOR_RESERVED 経由で reserved 自動登録される"
  artifacts:
    - "src/features/rolling.py(拡張): _ROLLING_SYSTEMS・_SYSTEM_SOURCE・_axes_for・build_rolling_features の speed_figure/mean_3/max_5 対応"
    - "src/features/availability.py(拡張): _ROLLING_SYSTEMS_FOR_RESERVED に 'speed_figure' 追加"
    - "src/config/feature_availability.yaml(拡張): rolling_speed_figure_* 6エントリ追加・course_kubun エントリ削除"
  key_links:
    - "rolling.py::_ROLLING_SYSTEMS と availability.py::_ROLLING_SYSTEMS_FOR_RESERVED が同一順序・同一要素(循環依存回避のため再定義・CONTEXT comment 踏襲)"
    - "feature_availability.yaml 追加エントリが data.py::_derive_feature_columns で自動 pick up され FEATURE_COLUMNS に rolling_speed_figure_* が含まれる(P03 で make_X_y 完全一致 assert で確認)"
---

<objective>
P01 で作成した `compute_speed_figure_for_history` が付与する history["speed_figure"] 列を・既存 rolling.py per-observation latest-K algorithm で集約可能にする。同時に registry(feature_availability.yaml + availability.py) に6 feature を追加し・v1.0 から潜在していた course_kubun silent parity bug(normalized 層にカラム不存在)を修正する。

Purpose: 後続 P03(builder 統合) が `build_rolling_features` を呼んだ際に rolling_speed_figure_* 6 feature が出力されるための前提。registry 登録がないと `make_X_y` の FEATURE_COLUMNS 完全一致 assert が fail する。
Output: rolling.py・availability.py・feature_availability.yaml の3ファイル拡張(各1-2行追加が中心)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-speed-figure-foundation/09-CONTEXT.md
@.planning/phases/09-speed-figure-foundation/09-RESEARCH.md
@.planning/phases/09-speed-figure-foundation/09-PATTERNS.md
@src/features/rolling.py
@src/features/availability.py
@src/config/feature_availability.yaml
@.planning/phases/09-speed-figure-foundation/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: rolling.py 拡張(speed_figure 系統追加 + D-09 6 axis 対応・window 3/5)</name>
  <files>src/features/rolling.py</files>
  <read_first>
    - src/features/rolling.py(全文・L60 LOOKBACK=5・L71-80 _ROLLING_SYSTEMS・L85-94 _SYSTEM_SOURCE・L102 _CATEGORICAL_SYSTEMS・L120-124 _axes_for・L220-225 rolling_cols 構築・L272-378 集約ループ)
    - src/features/speed_figure.py(P01 作成・compute_speed_figure_for_history が history["speed_figure"] float 列を付与)
    - .planning/phases/09-speed-figure-foundation/09-PATTERNS.md(L112-133 rolling.py 拡張 pattern)
    - .planning/phases/09-speed-figure-foundation/09-CONTEXT.md(D-09 6 feature・D-10 window 3 と 5 のみ)
  </read_first>
  <acceptance_criteria>
    - `grep -c '"speed_figure"' src/features/rolling.py` >= 1(_ROLLING_SYSTEMS 追加)
    - `grep -c '"speed_figure": ("speed_figure",)' src/features/rolling.py` >= 1(_SYSTEM_SOURCE 追加)
    - `uv run pytest tests/features/test_rolling.py -x` が既存 GREEN を維持(回帰なし)
    - _axes_for("speed_figure") が ("mean","latest","sd","count","max") を返す(numeric 拡張)
    - build_rolling_features が rolling_speed_figure_mean_5/latest_5/sd_5/count_5/max_5 を出力
    - 加えて rolling_speed_figure_mean_3 と rolling_speed_figure_last_1 も出力(D-09 6 feature)
    - 5走未満は __MISSING__ sentinel 既存ロジックが維持される(D-11)
  </acceptance_criteria>
  <action>
    src/features/rolling.py を以下の通り拡張する。

    (a) `_ROLLING_SYSTEMS` タプル(L71-80)の末尾に `"speed_figure"` を追加する。コメントで「# NEW Phase 9・Beyer 型スピード指数(history 側で builder が compute_speed_figure_for_history で付与・float)」を明記。

    (b) `_SYSTEM_SOURCE` dict(L85-94)に `"speed_figure": ("speed_figure",)` を追加する。history の "speed_figure" 列をそのまま source とする(builder が付与済み・P03)。

    (c) `_axes_for` 関数(L120-124)を拡張し・speed_figure 系統のみ特別に `("mean","latest","sd","count","max")` を返すようにする。既存 numeric(mean/latest/sd/count)に max を追加するのは speed_figure のみ(D-09 の max_5 は潜在能力/ピーク軸)。実装は `if system == "speed_figure": return ("mean","latest","sd","count","max")` を先頭に置き・その後既存の categorical/numeric 分岐を維持。max 軸は rolling_<system>_max_5 列として出力。

    (d) **D-09 6 feature のうち mean_3 と last_1 は window=5 でない別窓で算出**する。これは rolling.py の既存 LOOKBACK=5 固定構造を拡張する必要がある。最小侵入アプローチ: build_rolling_features の出力段(L354-378・result.at[...] ループ)で・speed_figure 系統の場合のみ追加で:
        - `rolling_speed_figure_mean_3`: window=3(recent の上位3行)の _sys_value.mean()。recent が sort 済み groupby("obs_id").head(5) なので head(3) で切直し・groupby mean を算出。
        - `rolling_speed_figure_last_1`: recent の最新1件(race_start_datetime DESC で first)・実質的に既存 latest_5 と同値だが・D-09 命名の last_1 エイリアスとして別途出力。
        これらを rolling_cols 初期化(L220-225)に追加・speed_figure 系統のループ内で算出・result に書込。docstring L133-183 の出力列説明に rolling_speed_figure_* 6 feature(mean_3/mean_5/latest_5/max_5/sd_5/count_5)を追記。last_1 は D-09 仕様だが latest_5 と実質同一なので・CONFIG.md との整合を取り rolling_speed_figure_last_1 を追加(latest_5 は既存命名だが D-09 は last_1 を指定・両方は出力せず last_1 を正とする・latest_5 は出力しない)。

        **命名確定**(D-09 準拠・6 feature):
        - rolling_speed_figure_last_1(直近状態・window=1)
        - rolling_speed_figure_mean_3(安定能力・window=3)
        - rolling_speed_figure_mean_5(安定能力・window=5)
        - rolling_speed_figure_max_5(潜在能力/ピーク・window=5)
        - rolling_speed_figure_sd_5(不安定性・window=5)
        - rolling_speed_figure_count_5(信頼度・window=5)
        ※ 既存命名の latest/sd と D-09 の last/sd で揺れがあるため・D-09 の `last_1` を正とし latest_5 は speed_figure 系統では出力しない(sd_5/count_5 は D-09 と既存で一致・mean_5/max_5 も一致・mean_3 と last_1 を追加)。ただし既存7系統の latest_5 は維持(speed_figure のみ last_1 に切替)。

    (e) rolling_cols 初期化(L220-225)を拡張し・speed_figure 系統の6列を object dtype で初期化する。既存ループの `for axis in _axes_for(system)` は維持し・speed_figure の max 軸を追加で処理可能にする。mean_3 と last_1 は window が異なるため・ループ外で個別算出するヘルパを追加。

    (f) docstring(L133-183)の出力列説明を更新し・rolling_speed_figure_* 6 feature を追記。「Phase 9 で追加: speed_figure 系統のみ last_1(window=1)・mean_3(window=3)・mean_5/max_5/sd_5/count_5(window=5)の6軸(D-09)」を明記。

    (g) CUTOFF_SEMANTICS assert(L55)・_pit_cutoff_prefilter(L104-116)・obs_id group idiom(L236-265)は一切変更しない(SC#2 不変・CYCLE-2 HIGH #1 不変)。
  </action>
  <verify>
    <automated>uv run pytest tests/features/test_rolling.py tests/features/test_speed_figure.py -x</automated>
  </verify>
  <done>
    - rolling.py の _ROLLING_SYSTEMS に "speed_figure" が追加されている
    - _axes_for("speed_figure") が mean/latest/sd/count/max を返す(speed_figure のみ latest でなく last_1 命名・max 追加)
    - build_rolling_features が rolling_speed_figure_last_1/mean_3/mean_5/max_5/sd_5/count_5 の6列を出力する
    - 既存 test_rolling.py が全て GREEN(回帰なし・speed_figure 系統は history に列が無い場合は sentinel 初期値のまま)
    - CUTOFF_SEMANTICS assert と _pit_cutoff_prefilter は不変
  </done>
</task>

<task type="auto">
  <name>Task 2: availability.py + feature_availability.yaml 拡張(6エントリ追加 + course_kubun bug 修正)</name>
  <files>src/features/availability.py, src/config/feature_availability.yaml</files>
  <read_first>
    - src/features/availability.py(L143-179 _ROLLING_SYSTEMS_FOR_RESERVED・L158-179 _RESERVED_NON_FEATURE_COLUMNS・L296-336 出力カラム検査)
    - src/config/feature_availability.yaml(L129-135 course_kubun エントリ・L295-315 rolling_babacd ブロック・L317-335 rolling_jyocd ブロック・7項目スキーマ)
    - .planning/phases/09-speed-figure-foundation/09-PATTERNS.md(L178-208 availability/yaml 拡張 pattern・course_kubun bug 修正 pattern)
    - .planning/phases/09-speed-figure-foundation/09-RESEARCH.md(Pitfall 2 course_kubun silent parity 違反・live-DB で不存在確定)
  </read_first>
  <acceptance_criteria>
    - `grep -c '"speed_figure"' src/features/availability.py` >= 1(_ROLLING_SYSTEMS_FOR_RESERVED 追加)
    - feature_availability.yaml に rolling_speed_figure_last_1/mean_3/mean_5/max_5/sd_5/count_5 の6エントリが追加される
    - `grep -c 'course_kubun' src/config/feature_availability.yaml` == 0(エントリ削除・silent parity bug 修正)
    - 各 rolling_speed_figure_* エントリが7項目スキーマ(feature_name/feature_group/available_from_timing/source_role/source_table/cutoff_rule/leakage_risk_level)を持つ
    - 全 rolling_speed_figure_* の available_from_timing が "entry_confirmed"(1-A 許可・D-07)
    - 全 rolling_speed_figure_* の leakage_risk_level が "low"(odds-free・PIT-correct)
    - feature_availability.yaml の当該 silent-parity エントリ(正常化層に実カラム不存在・live-DB 確定済)が削除されている(grep -c 0)
<!-- planner-discipline-allow: course_kubun -->
    - `uv run python -c "from src.features.availability import load_feature_availability, assert_all_entries_allowed; spec=load_feature_availability(); assert_all_entries_allowed(spec); print('OK')"` が OK を出力
    - `uv run python -c "from src.features.availability import banned_features, load_feature_availability; print(banned_features(load_feature_availability()))"` が [] を出力
  </acceptance_criteria>
  <action>
    [availability.py 拡張]
    `src/features/availability.py` の `_ROLLING_SYSTEMS_FOR_RESERVED` タプル(L143-152)の末尾に `"speed_figure"` を追加する。コメントで「# NEW Phase 9(rolling.py::_ROLLING_SYSTEMS と同一順序・二重定義の危険を下げるため再定義・循環依存回避)」を明記。これにより `rolling_speed_figure_count_5` が `{f"rolling_{sys}_count_5" for sys in _ROLLING_SYSTEMS_FOR_RESERVED}`(L179) で自動的に reserved に追加される。**ただし D-09 命名は last_1/mean_3 で window が異なるため・count_5 は window=5 のみ生成し・last_1/mean_3 用の count は別途生成しない**(sentinel 扱い・D-11)。_RESERVED_NON_FEATURE_COLUMNS の rolling_{sys}_count_5 展開は speed_figure にも適用され rolling_speed_figure_count_5 が reserved 化される。

    [feature_availability.yaml 拡張]
    `src/config/feature_availability.yaml` を以下の通り編集:

    (a) **course_kubun エントリ削除**: L129-135 の `- feature_name: course_kubun` ブロック(7項目・feature_group: race_context)を完全に削除する。コメントで「# Phase 9: course_kubun は normalized 層にカラム不存在(live-DB 確定)・silent parity bug 修正・trackcd が代替」を1行残す形で削除跡を明示(RESEARCH Pitfall 2・A4)。

    (b) **rolling_speed_figure_* 6 エントリ追加**: L317-335(rolling_jyocd ブロック)の直後・L337(rolling_days_since_prev ブロック)の前に・以下の6エントリを7項目スキーマ完全踏襲で挿入する。コメント冒頭に「# --- rolling_speed_figure (Phase 9・Beyer 型スピード指数・D-09 6 feature) ---」を置く:
        - rolling_speed_figure_last_1(feature_group: rolling_speed_figure・available_from_timing: entry_confirmed・source_role: history_allowed_post_race・source_table: "normalized.n_uma_race (history) + derived speed_figure"・cutoff_rule: "race_date - 1 day (strict < cutoff, JST midnight)"・leakage_risk_level: low)
        - rolling_speed_figure_mean_3(同スキーマ)
        - rolling_speed_figure_mean_5(同スキーマ)
        - rolling_speed_figure_max_5(同スキーマ)
        - rolling_speed_figure_sd_5(同スキーマ)
        - rolling_speed_figure_count_5(同スキーマ)

    (c) schema_version(L15)を現行 "0.3.0" から "0.4.0" に bumpする(Phase 9 で feature 定義が変わった印・§19.1 再現性)。コメント(L4-6)に「# Phase 9: schema_version 0.3.0 → 0.4.0(rolling_speed_figure 6 feature 追加 + course_kubun 削除)」を追記。

    (d) feature_schema ブロック(L31-52)は変更不要(7項目スキーマは不変)。

    (e) cutoff_semantics ブロック(L21-26)は一切変更しない(HIGH #2 不変・strict_less_than / Asia/Tokyo)。
  </action>
  <verify>
    <automated>uv run python -c "from src.features.availability import load_feature_availability, assert_all_entries_allowed, banned_features, registered_feature_columns; spec=load_feature_availability(); assert_all_entries_allowed(spec); assert banned_features(spec)==[]; cols=registered_feature_columns(spec); assert 'rolling_speed_figure_mean_5' in cols; assert 'rolling_speed_figure_last_1' in cols; assert 'rolling_speed_figure_count_5' in cols; assert 'course_kubun' not in cols; assert spec['schema_version']=='0.4.0'; print('OK')"</automated>
  </verify>
  <done>
    - availability.py の _ROLLING_SYSTEMS_FOR_RESERVED に "speed_figure" が rolling.py と同一順序で追加されている
    - feature_availability.yaml に rolling_speed_figure_* 6 エントリ(last_1/mean_3/mean_5/max_5/sd_5/count_5)が7項目スキーマで追加されている
    - feature_availability.yaml の course_kubun エントリが削除されている(silent parity bug 修正)
    - schema_version が 0.4.0 に bump されている
    - banned_features(spec) == [](許可 timing のみ)
    - registered_feature_columns に rolling_speed_figure_* 6 feature が全て含まれる
    - load_feature_availability が fail-fast せず正常にロードされる
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry(yaml) ↔ 実カラム | feature_availability.yaml の feature_name と Parquet 実カラムが一致(registry↔Parquet parity・HIGH #3)。course_kubun は実カラム不存在のため silent parity 違反だった(本 PLAN で修正) |
| rolling 出力 ↔ FEATURE_COLUMNS allowlist | rolling_speed_figure_* が registry に追加されることで data.py::_derive_feature_columns が自動 pick up・make_X_y の完全一致 assert で banned alias sneak-in を構造的防止 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-07 | Tampering | course_kubun silent parity 違反(既存 bug) | medium | mitigate | feature_availability.yaml から course_kubun エントリを削除・normalized 層にカラム不存在(live-DB 確定)・trackcd が代替 |
| T-09-08 | Tampering | rolling_speed_figure_* が FEATURE_COLUMNS allowlist に含まれない silent parity | high | mitigate | feature_availability.yaml + availability.py::_ROLLING_SYSTEMS_FOR_RESERVED の両方に追加・二重定義で順序含め一致 |
| T-09-04 | Tampering | rolling_speed_feature への odds/ninki proxy 混入 | high | mitigate | rolling.py は history["speed_figure"] 列(float)のみを集約・odds/ninki は SOURCE に現れない・P04 で AST audit で完全証明 |
| T-09-09 | Repudiation | _ROLLING_SYSTEMS と _ROLLING_SYSTEMS_FOR_RESERVED の順序不整合 | medium | mitigate | 両タプルを完全同一順序で定義・循環依存回避のため再定義(CONTEXT 既存 comment 踏襲) |
</threat_model>

<verification>
- `uv run pytest tests/features/test_rolling.py tests/features/test_speed_figure.py -x` が GREEN(既存回帰なし + speed_figure 系統追加)
- `grep -c 'course_kubun' src/config/feature_availability.yaml` == 0
- `grep -c 'rolling_speed_figure' src/config/feature_availability.yaml` >= 6(6エントリ)
- `uv run python -c "from src.features.availability import load_feature_availability, assert_all_entries_allowed; spec=load_feature_availability(); assert_all_entries_allowed(spec); print(spec['schema_version'])"` が 0.4.0 を出力
</verification>

<success_criteria>
- rolling.py の _ROLLING_SYSTEMS に speed_figure が追加され・build_rolling_features が rolling_speed_figure_* 6 feature を出力する
- availability.py と feature_availability.yaml の両方に speed_figure 系統が登録される(二重定義・順序一致)
- course_kubun silent parity bug が修正される(yaml エントリ削除)
- schema_version が 0.4.0 に bump される
- load_feature_availability と assert_all_entries_allowed が GREEN
</success_criteria>

<output>
`.planning/phases/09-speed-figure-foundation/09-02-SUMMARY.md` を作成し・以下を記録する:
- rolling.py の _axes_for 拡張内容(speed_figure のみ max 追加・last_1/mean_3 別窓対応)
- feature_availability.yaml の schema_version(0.4.0)と追加6エントリ
- course_kubun 削除跡(yaml コメントで明示)
- P03(builder 統合) が依存する情報: rolling_speed_figure_* 6 feature が registry 登録済み・build_rolling_features が出力可能
</output>

## Artifacts this phase produces

本 PLAN(P02)が拡張するシンボル:
- `src/features/rolling.py`:
  - `_ROLLING_SYSTEMS`(末尾に "speed_figure" 追加)
  - `_SYSTEM_SOURCE`(`"speed_figure": ("speed_figure",)`)
  - `_axes_for`(speed_figure のみ mean/latest/sd/count/max・ただし命名は last_1/mean_3/mean_5/max_5/sd_5/count_5)
  - `build_rolling_features`(speed_figure 系統の6軸出力に対応)
- `src/features/availability.py`:
  - `_ROLLING_SYSTEMS_FOR_RESERVED`(末尾に "speed_figure" 追加・rolling.py と同一順序)
  - 結果的に `rolling_speed_figure_count_5` が _RESERVED_NON_FEATURE_COLUMNS に自動追加
- `src/config/feature_availability.yaml`:
  - 新 feature_name: `rolling_speed_figure_last_1` / `rolling_speed_figure_mean_3` / `rolling_speed_figure_mean_5` / `rolling_speed_figure_max_5` / `rolling_speed_figure_sd_5` / `rolling_speed_figure_count_5`
  - 削除: `course_kubun` エントリ(silent parity bug 修正)
  - `schema_version: "0.4.0"`(0.3.0 から bump)

後続 PLAN が参照する新シンボル:
- P03 builder は rolling.py が出力する rolling_speed_figure_* 6 feature を registry に基づき parity 検証
- P04 audit は feature_availability.yaml と availability.py の両方に rolling_speed_figure_* が整合登録されていることを検証
- data.py::_derive_feature_columns が registry から rolling_speed_figure_* を自動 pick up(FEATURE_COLUMNS 拡張・P05 stop gate で使用)
