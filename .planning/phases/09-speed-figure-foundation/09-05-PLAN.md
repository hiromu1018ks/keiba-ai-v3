---
phase: 09-speed-figure-foundation
plan: 05
type: execute
wave: 5
depends_on:
  - 09-01
  - 09-02
  - 09-03
  - 09-04
files_modified:
  - scripts/run_speed_figure_stopgate.py
  - tests/model/test_speed_figure_stopgate.py
autonomous: false
requirements:
  - FEAT-01
user_setup:
  - service: PostgreSQL (everydb2)
    why: "stop gate は live-DB で生成した snapshot で v1.0 baseline と +speed_figure を比較(KEIBA_SKIP_DB_TESTS unset)"
    env_vars: []
    dashboard_config: []

must_haves:
  truths:
    - "SC#6 stop gate が v1.0 baseline と(v1.0+speed_figure) 単体モデルを同一 BT split / 同一 odds snapshot policy / 同一選択ルール で比較する(D-13・公平性)"
    - "REVIEW H2/H7/H8 対応(横断的・最優先): stop gate は生 trainer(train_lightgbm.predict_proba / train_catboost + 手動 align)を直接呼ぶのでなく・src/model/orchestrator.py::train_and_predict(L234-562・train→calibrate→predict の統合パイプライン) を両 snapshot で呼び・calibration 済み確率で比較する。これにより (a) calibration pipeline skip による D-13 公平性違反(H2)・(b) CatBoost sorted_index を test 予測に誤用(H7・training Pool 由来でなく test Pool の sorted_test_idx が必要・orchestrator L519-538)・(c) LightGBM categorical dtype mismatch(H8・orchestrator L490-517 が明示的に再準備) の3バグを構造的に回避する"
    - "REVIEW H1 対応: load_feature_matrix(snapshot_id) と _derive_feature_columns(snapshot_id) が P03 で parameterization 済みのため・本 stop gate は両 snapshot(baseline v1.0 + speed_figure)を明示的な snapshot_id でロードし比較可能"
    - "REVIEW H6 対応: 診断用 odds 列名は fukuodds でなく fuku_odds_lower / fuku_odds_upper(src/ev/odds_snapshot.py L210-215・EV コード src/ev/ev_rank.py L80-112 もこれらを消費)を使用する"
    - "D-14 必須4指標(odds_band×p_bin calibration 改善 / selected-only ROI 改善 / Brier/LogLoss/AUC 非劣化 or 許容幅内 / model-market disagreement bucket ROI 改善)が算出される"
    - "D-15 軽量 residual proxy(market_implied×model_p 分位 bucket・同一 market bucket 内で model_p 高低による実的中率/ROI 差)が算出される・p値/回帰係数の正式結論は出さない"
    - "§15.2 事前登録指標(calibration_max_dev/Brier/LogLoss/sum(p) 分布)は一切変更せず・evaluator.py/segment_eval.py の binning 契約を固定再利用する(bit-identical)"
    - "D-16: 両方(selected/high-EV calibration 改善 と residual proxy)が全く改善しない場合・reports に「構造的限界寄り・Phase 10-12 進行前にユーザー確認」と honest 記録される"
  artifacts:
    - "scripts/run_speed_figure_stopgate.py(新規): SC#6 stop gate・v1.0 vs +speed_figure 比較・4指標+residual proxy・reports/09-stopgate.{md,json} 出力"
    - "tests/model/test_speed_figure_stopgate.py(新規): 合成データでの E2E smoke・binning 契約固定再利用確認"
  key_links:
    - "scripts/run_speed_figure_stopgate.py が src/model/evaluator.py の CALIBRATION_CURVE_BINS=10/MIN_BIN_COUNT=30 と src/model/segment_eval.py の ODDS_BAND_EDGES を import 再利用(bit-identical・§15.2 不変)"
    - "src/model/predict.py::make_model_version で Phase 9 単体モデルの model_version を採番(feature_snapshot_id-{short}-v{N} 形式・review HIGH#4)"
    - "checkpoint:human-verify でユーザーが 4指標+residual proxy 結果を見て Phase 10-12 進行可否を判断(D-16)"
---

<!-- planner-discipline-allow: fukuodds -->
<!-- planner-discipline-allow: fuku_odds_lower -->
<!-- planner-discipline-allow: train_and_predict -->
<!-- planner-discipline-allow: train_lightgbm -->
<!-- planner-discipline-allow: train_catboost -->
<!-- planner-discipline-allow: align_predictions -->
<!-- planner-discipline-allow: _prepare_catboost_pool -->

<objective>
SC#6 stop gate を実装し・Phase 9 のスピード指数追加が v1.0 baseline に対して「投票層の過大予測是正」または「market residual 残存」のいずれのシグナルを示すかを定量評価する。D-14 必須4指標と D-15 軽量 residual proxy を・§15.2 事前登録指標を一切変更せず binning 契約固定で算出する。D-16 に基づき「両方改善しない場合は構造的限界寄り」として Phase 10-12 進行前にユーザー確認を求める。

Purpose: マイルストーン目的「市場残差能力の定量測定・鑑別」に合致し・早期撤退判断を可能にする。黒字化でなく「Phase 10-12 に進む価値があるか」を判定する gate(D-12/D-16)。
Output: scripts/run_speed_figure_stopgate.py(新規) + reports/09-stopgate.{md,json} + tests/model/test_speed_figure_stopgate.py + checkpoint:human-verify
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-speed-figure-foundation/09-CONTEXT.md
@.planning/phases/09-speed-figure-foundation/09-RESEARCH.md
@.planning/phases/09-speed-figure-foundation/09-PATTERNS.md
@.planning/phases/09-speed-figure-foundation/09-REVIEWS.md
@src/model/evaluator.py
@src/model/segment_eval.py
@src/model/orchestrator.py
@src/model/trainer.py
@src/model/data.py
@src/model/predict.py
@src/model/artifact.py
@src/ev/odds_snapshot.py
@scripts/run_evaluation.py
@.planning/phases/09-speed-figure-foundation/09-01-SUMMARY.md
@.planning/phases/09-speed-figure-foundation/09-02-SUMMARY.md
@.planning/phases/09-speed-figure-foundation/09-03-SUMMARY.md
@.planning/phases/09-speed-figure-foundation/09-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/run_speed_figure_stopgate.py 新規作成(SC#6・D-14 4指標 + D-15 residual proxy・binning 契約固定)</name>
  <files>scripts/run_speed_figure_stopgate.py</files>
  <read_first>
    - scripts/run_speed_figure_stopgate.py(未作成・新規)
    - scripts/run_evaluation.py(L65-68 CLI 起動 idiom・L1316-1441 masked DSN/try/finally pool close・L1048-1306 atomic write/BLOCK 発火)
    - src/model/evaluator.py(L89-91 CALIBRATION_CURVE_BINS=10/CALIBRATION_CURVE_MIN_BIN_COUNT=30・L172 compute_metrics・check_acceptance_gate・L210-214 single-class AUC NaN に注意 M4)
    - src/model/segment_eval.py(L69-74 NINKI_BAND_EDGES/ODDS_BAND_EDGES・L94-112 sanitizer pattern M4・L142-158 _ninki_band/_odds_band・evaluate_all_segments・L445-452 Plotly div_id idiom M3)
    - REVIEW H2/H7/H8(最優先): src/model/orchestrator.py(L234-246 train_and_predict シグネチャ・L251-257 feature_df は label-joined frame 前提・L478-507 calibrate_model・L508-562 calibrated 予測生成・L490-517 _prepare_lightgbm_train_eval H8・L519-538 _prepare_catboost_pool/sorted_test_idx/align_predictions H7)
    - src/model/trainer.py(L54-75 LOW_CARD_CAT_COLS/HIGH_CARD_CODE_COLS・参考・直接呼ばない H2/H7/H8)
    - REVIEW H1: src/model/data.py(P03 で parameterization 済み・load_feature_matrix(snapshot_id)・_derive_feature_columns(snapshot_id)・build_training_frame・load_labels・load_frozen_maps(snapshot_id))
    - src/model/predict.py(L105-146 make_model_version・{feature_snapshot_id}-{short}-v{N} 形式)
    - src/model/artifact.py(_atomic_write_text)
    - REVIEW H6: src/ev/odds_snapshot.py(L210-215 fuku_odds_lower/fuku_odds_upper 列・L320-324)・src/ev/ev_rank.py(L80-112 fuku_odds_lower を消費)
    - src/ev/(baseline.py・metrics.py・selector.py の D-14 指標算出 analog)
    - .planning/phases/09-speed-figure-foundation/09-CONTEXT.md(D-12〜D-16 stop gate 仕様)
    - .planning/phases/09-speed-figure-foundation/09-RESEARCH.md(Pattern・SC#6・§15.2 事前登録指標不変・Open Questions (RESOLVED) #2 許容幅根拠)
    - .planning/phases/09-speed-figure-foundation/09-VALIDATION.md(SC#6 verify command)
    - .planning/phases/09-speed-figure-foundation/09-REVIEWS.md(H2/H6/H7/H8/H1/M4 の横断的 HIGH・本 PLAN で対処)
  </read_first>
  <acceptance_criteria>
    - `grep -c 'CALIBRATION_CURVE_BINS' scripts/run_speed_figure_stopgate.py` >= 1(import 再利用)
    - `grep -c 'ODDS_BAND_EDGES' scripts/run_speed_figure_stopgate.py` >= 1(import 再利用)
    - `grep -c 'make_model_version' scripts/run_speed_figure_stopgate.py` >= 1(model_version 採番)
    - REVIEW H2/H7/H8: `grep -c 'train_and_predict' scripts/run_speed_figure_stopgate.py` >= 1(orchestrator 経由・生 trainer 直接呼出でなく)
    - REVIEW H2/H7/H8: `grep -v '^#' scripts/run_speed_figure_stopgate.py | grep -c 'train_lightgbm\\|train_catboost\\|align_predictions'` == 0(生 trainer 公開 API を直接呼ばない・orchestrator.train_and_predict のみ使用・H2 calibration skip / H7 sorted_index 誤用 / H8 categorical dtype mismatch の3バグ構造的回避)
    - REVIEW H6: `grep -c 'fuku_odds_lower' scripts/run_speed_figure_stopgate.py` >= 1(正しい odds 列名・fukuodds でない)
    - REVIEW H6: `grep -v '^#' scripts/run_speed_figure_stopgate.py | grep -c 'fukuodds'` == 0(誤列名 fukuodds を使用しない・word-boundary)
    - `grep -c 'dsn_masked' scripts/run_speed_figure_stopgate.py` >= 1(生 DSN ログ禁止・T-06-15)
    - `grep -c 'statement_timeout' scripts/run_speed_figure_stopgate.py` >= 1(MEMORY.md・重クエリ防止)
    - `grep -c 'readonly' scripts/run_speed_figure_stopgate.py` >= 1(role='readonly')
    - `uv run python -c "import ast; ast.parse(open('scripts/run_speed_figure_stopgate.py').read()); print('syntax OK')"` が OK
    - **REVIEW H2/H7/H8 trainer API 戻り値契約(orchestrator 経由)**: 本 stop gate は `orchestrator.train_and_predict(feature_df, model_type="lightgbm", feature_snapshot_id=..., version_n=1, ...)` を呼び・戻り値 dict の予測 DataFrame(pred_df・p_fukusho_hit 列・calibration 済み・行整列保証済み・orchestrator L234-562)を使用する。生 trainer(train_lightgbm/train_catboost/align_predictions)は直接呼ばない。CatBoost の整列も orchestrator 内部の `_prepare_catboost_pool(X_test_cb, sort=True)` → `sorted_test_idx`(L525・test Pool 由来・H7 修正) → `align_predictions`(L534-538)で処理済み。LightGBM の categorical dtype も orchestrator 内部の `_prepare_lightgbm_train_eval(X_train_core, X_test)`(L516・H8 修正)で処理済み。両 snapshot で同一 BT split・同一 odds_snapshot_policy・同一 selector(D-13 公平性)。
    - reports/09-stopgate.{md,json} が生成され・json に4指標と residual proxy の数値が含まれる(live-DB)
    - json が sort_keys=True・ensure_ascii=False・allow_nan=False(RFC 8259 strict・scripts/run_evaluation.py L1048-1306 と同一・M4 NaN sanitization)
    - D-16: 両方(calibration 改善・residual proxy)が改善しない場合・json/md に "構造的限界寄り・Phase 10-12 進行前にユーザー確認" が含まれる
  </acceptance_criteria>
  <action>
    `scripts/run_speed_figure_stopgate.py` を新規作成する。モジュール docstring で「Phase 9 SC#6 stop gate・v1.0 baseline vs (v1.0+speed_figure) 単体モデル比較・D-14 4指標 + D-15 residual proxy・§15.2 事前登録指標不変・binning 契約固定再利用・cross-reference: 09-VALIDATION.md SC#6」を明記。

    [CLI 起動 idiom]
    (a) `_REPO_ROOT = Path(__file__).resolve().parent.parent` と sys.path 挿入(scripts/run_evaluation.py L65-68 踏襲)。
    (b) argparse: `--baseline-snapshot-id`(default "20260620-1a-postreview-v2"・v1.0)・`--speed-figure-snapshot-id`(default P03 候補 "20260625-1a-speedfigure-v1")・`--bt-split`(default BT-1 "2019-06-01..2022-12-31 train / 2023 test"・§15.5)・`--odds-snapshot-policy`(default "30min_before"・v1.0 と同一)・`--out-dir`(default reports)・`--non-interactive`(CI 用・デフォルト False)。

    [DB・pool idiom]
    (c) `from src.db.connection import make_pool`・`from src.config.settings import Settings`。`settings = Settings()`・`logger.info("readonly DSN: %s", settings.dsn_masked)`(T-06-15 踏襲)。
    (d) `readonly_pool = make_pool(settings, role="readonly")` を try・finally で close(scripts/run_evaluation.py L1316-1441 と同一)。
    (e) セッション先頭で `SET statement_timeout = '30s'` を発行(MEMORY.md subagent-db-query-statement-timeout・重クエリの孤立 CPU 張り付き防止)。

    [モデル学習・予測 idiom — REVIEW H2/H7/H8: orchestrator.train_and_predict 経由]
    (f) REVIEW H1 で P03 が parameterization した `from src.model.data import load_feature_matrix, build_training_frame, load_labels, prepare_model_matrix` を使い・baseline と +speed_figure の両 snapshot を明示的な snapshot_id でロード。`load_feature_matrix(snapshot_id="20260620-1a-postreview-v2")`(v1.0 baseline)と `load_feature_matrix(snapshot_id="20260625-1a-speedfigure-v1")`(speed_figure・P03 で生成)を個別に呼ぶ。それぞれ `load_labels(readonly_cur)` → `build_training_frame` で label-joined frame を構築(orchestrator は label join を再実行しない・feature_df は label-joined 前提・orchestrator.py L251-257)。
    (g) **REVIEW H2/H7/H8(最優先・生 trainer 直接呼出禁止)**: `from src.model.orchestrator import train_and_predict`。両 snapshot で `result = train_and_predict(feature_df=label_joined_frame, model_type="lightgbm", feature_snapshot_id=<snapshot_id>, version_n=1, split_periods=BT1_PERIODS, category_map=load_frozen_maps(snapshot_id=<snapshot_id>))` を呼ぶ。これにより:
        - **H2 (calibration pipeline)**: orchestrator L478-507 が `calibrate_model(estimator, X_calib, y_calib, ...)` で later-disjoint calibration を実行し・L508-562 で calibrated 予測を生成する。生 trainer の `predict_proba` は未 calibration になるため・v1.0 baseline(calibration 済み)との比較は D-13 公平性違反になる(本 stop gate は必ず orchestrator 経由で calibration 済み予測を比較する)。
        - **H7 (CatBoost sorted_index)**: orchestrator L519-538 が test 予測用に `_prepare_catboost_pool(X_test_cb, sort=True)` で `pool_test, sorted_test_idx = ...` を生成し(training Pool の sorted_index でなく test Pool の sorted_test_idx)・`align_predictions(raw_pred_sorted, sorted_test_idx, X_test.index)` で元の行順序に復元する。生 trainer の戻り値 sorted_index は training Pool 由来で test 予測に使うと誤整列。
        - **H8 (LightGBM categorical dtype)**: orchestrator L490-517 が `_prepare_lightgbm_train_eval(X_train_core, X_test)` で train/test の categorical categories を統一し・"train and valid dataset categorical_feature do not match" エラーを回避する。生 `model.predict_proba(X_test)` はこの準備を飛ばす。
      生 trainer(train_lightgbm/train_catboost/align_predictions/_prepare_catboost_pool)は直接呼ばない。orchestrator.train_and_predict の戻り値は dict で・`pred_df`(p_fukusho_hit 列・calibration 済み・行整列保証済み)を含む。両 snapshot で同一 BT split・同一 odds_snapshot_policy・同一 selector(D-13 公平性)。
    (h) model_version 採番: `from src.model.predict import make_model_version`。baseline は既存 v1.0 model_version・+speed_figure は `make_model_version("20260625-1a-speedfigure-v1", "lightgbm", 1)` → "20260625-1a-speedfigure-v1-lgb-v1"(review HIGH#4・orchestrator が内部で呼ぶが・stop gate レポートの model_version 記録用に取得)。予測は orchestrator 戻り値の pred_df を使用し・src/ev/ の既存純粋関数(metrics・selector・purchase_simulator)で D-14 指標を算出する。

    [D-14 必須4指標・binning 契約固定]
    (i) `from src.model.evaluator import CALIBRATION_CURVE_BINS, CALIBRATION_CURVE_MIN_BIN_COUNT, compute_metrics, check_acceptance_gate`(import・再定義禁止・bit-identical・T-06-07)。`from src.model.segment_eval import ODDS_BAND_EDGES, NINKI_BAND_EDGES, _odds_band, _ninki_band, evaluate_all_segments`(import)。**これらの定数は再定義・上書きしない**(§15.2 事前登録指標不変)。
    (j) 指標1: odds_band×p_bin の selected/high-EV 層 calibration 改善。v1.0 と +speed_figure で・evaluate_all_segments を同一 ODDS_BAND_EDGES で呼出・各 bin の |mean_pred - frac_pos| を比較。投票層(odds>=4.9 かつ p∈[0.15,0.20]・v1.0 で4倍過大だった領域)の改善量を算出。
    (k) 指標2: selected-only 実現 EV/ROI 改善。selector で選ばれた馬の回収率(payout/stake・src/ev/metrics.py の profit_loss・refund_accounting)を v1.0 と +speed_figure で比較。
    (l) 指標3: Brier/LogLoss/AUC の非劣化 or 許容幅内。`compute_metrics` を両モデルで呼出・差分を算出。**許容幅(Claude's Discretion・planner 決定・RESEARCH.md Open Questions (RESOLVED) #2 採用)**: **Brier +0.005 以内・LogLoss +0.02 以内・AUC -0.005 以内**を「非劣化」とする。

        **許容幅の根拠**(SUMMARY にも明記):
        - v1.0 LightGBM Brier=0.15222・LogLoss=0.47488・AUC=0.73230 を基準。
        - 特徴量6個追加で起きうる微小な性能変動(過学習リスク・特徴量間相互作用のノイズ)を許容する現実的なマージン。Brier +0.005 は v1.0 Brier の ~3.3%・LogLoss +0.02 は ~4.2%・AUC -0.005 は ~0.7% に相当(いずれも「モデル実用性を毀損しない変動」範囲)。
        - **Phase 11 SC#2 事前登録マージンとの整合**: Phase 11 plan 作成時に SC#2 で事前登録する非劣化マージンと**同一スケール**であることを確認すること(事前登録の聖域 §15.2 を事後的に緩めない)。**整合確認事項**: もし Phase 11 が本 P05 より厳しい値（例: Brier +0.003）を事前登録した場合、本 P05 stop gate の「非劣化」判定は事後的に見て緩すぎた可能性があり・P05 SUMMARY の判定を Phase 11 視点で再解釈する必要がある（stop gate 通過≠Phase 11 SC#2 通過）。この依存関係を P05 SUMMARY と Phase 11 plan の cross-reference に明記する。
    (m) 指標4: model-market disagreement bucket の ROI/hit-rate 改善。D-15 residual proxy と連動(後述)。

    [D-15 軽量 residual proxy — REVIEW H6: odds 列名修正]
    (n) REVIEW H6 対応: market_implied は `1.0 / fuku_odds_lower`(複勝オッズ下限の逆数・src/ev/odds_snapshot.py L210-215 が出力する `fuku_odds_lower`/`fuku_odds_upper` 列・EV コード src/ev/ev_rank.py L80-112 もこれらを消費)・診断層のみ使用(speed_figure 特徴量には入れない・SAFE-01)。旧 PLAN の `fukuodds` は誤り(実コードに存在しない列名)・必ず `fuku_odds_lower` を使用する。model_p は +speed_figure モデル(orchestrator.train_and_predict 経由・calibration 済み)の p_fukusho_hit。
    (o) bucket 数(Claude's Discretion): 10分位(CALIBRATION_CURVE_BINS=10 と整合・MIN_BIN_COUNT=30 で極小 bucket skip)。market_implied 10分位×model_p 10分位の2軸グリッドで・各 cell の的中率/ROI を算出。同一 market bucket 内で model_p 高低による実的中率/ROI 差が残るかを確認(market residual の暫定シグナル)。**p値判定・回帰係数の正式結論は出さない**(Phase 12 EVAL-02 に委譲・D-12)。

    [レポート出力・atomic write・D-16・REVIEW M4 NaN sanitization]
    (p) `from src.model.artifact import _atomic_write_text`。reports/09-stopgate.{md,json} を `json.dumps(sort_keys=True, ensure_ascii=False, allow_nan=False)` で atomic write(scripts/run_evaluation.py L1048-1306 と同一・RFC 8259 strict)。**REVIEW M4 対応**: single-class AUC や空 bucket で NaN/Inf が混入する可能性があるため(src/model/evaluator.py L210-214 で single-class AUC が NaN を出しうる・src/model/segment_eval.py L94-112 が sanitizer pattern を持つ)・json.dumps 前に NaN/Inf を明示的に sanitization する helper(例: `_sanitize_for_json(obj)`・NaN→None または "NaN" 文字列・Inf→"Infinity"・src/model/segment_eval.py L94-112 の sanitizer pattern を再利用)を通す。これにより `allow_nan=False` で ValueError になるのを防ぐ。sanitizer は再利用可能な形でスクリプト内に定義し・test_speed_figure_stopgate.py の test_json_output_strict で検証する。
    (q) D-16: 両方(指標1 selected calibration 改善・指標4 residual proxy シグナル)が全く改善しない場合・json と md の両方に "構造的限界寄り・Phase 10-12 進行前にユーザー確認 (D-16)" を honest 記録(D-05 踏襲・KNOWN_LIMITATIONS 定数で強制)。片方でも改善があれば「特徴量追加の有効性シグナルあり・Phase 10 進行候補」と記録。両方とも BLOCK でなく exit 0(継続可否は checkpoint:human-verify で判断)。

    [SAFE-01 保証]
    (r) 本スクリプトは market_implied を診断層のみで使用し・speed_figure 特徴量や model_p モデル入力には絶対に混入しない。コメントで「SAFE-01: market_implied は診断層のみ・p モデル特徴量には入れない(EVAL-02/SAFE-01)」を明記(P04 audit 拡張対象外だが・Phase 12 EVAL-02 での audit のために文書化)。
  </action>
  <verify>
    <automated>uv run python -c "import ast; ast.parse(open('scripts/run_speed_figure_stopgate.py').read()); print('syntax OK')"</automated>
  </verify>
  <done>
    - scripts/run_speed_figure_stopgate.py が新規作成され・構文エラーがない
    - evaluator.py/segment_eval.py の binning 定数を import 再利用(再定義なし・bit-identical)
    - REVIEW H2/H7/H8: orchestrator.train_and_predict を両 snapshot で呼び(calibration 済み予測で比較)・生 trainer(train_lightgbm/train_catboost/align_predictions)は直接呼ばない
    - REVIEW H6: market_implied が fuku_odds_lower から算出される(fukuodds でない)
    - REVIEW M4: NaN/Inf sanitization helper が json.dumps 前に適用される(allow_nan=False で ValueError 回避)
    - make_model_version で +speed_figure モデルの model_version を採番
    - masked DSN・statement_timeout='30s'・readonly pool try/finally が実装
    - reports/09-stopgate.{md,json} に D-14 4指標と D-15 residual proxy の数値が含まれる(live-DB)
    - D-16: 両方改善しない場合「構造的限界寄り」が honest 記録される
    - json が RFC 8259 strict(allow_nan=False・sort_keys=True・M4 sanitizer 適用済)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 合成データ E2E smoke テスト + binning 契約固定再利用確認</name>
  <files>tests/model/test_speed_figure_stopgate.py</files>
  <read_first>
    - tests/model/test_speed_figure_stopgate.py(未作成・新規)
    - tests/model/test_trainer.py(trainer/evaluator テスト analog)
    - tests/model/test_evaluator.py(compute_metrics・check_acceptance_gate テスト analog・binning 契約)
    - tests/features/test_speed_figure.py(P01・P03 で SC#1/SC#3 拡張済み)
    - scripts/run_speed_figure_stopgate.py(Task 1 で作成)
    - src/model/evaluator.py(CALIBRATION_CURVE_BINS・compute_metrics)
    - src/model/segment_eval.py(ODDS_BAND_EDGES・evaluate_all_segments)
  </read_first>
  <behavior>
    - 合成データ(少数の予測 p と outcome と odds)で stop gate スクリプトのコアロジック(指標算出・residual proxy・D-16 判定)が動作する
    - evaluator/segment_eval の binning 定数を import で再利用し・再定義していないこと
    - REVIEW H2/H7/H8: スクリプトが orchestrator.train_and_predict を使用し・生 trainer(train_lightgbm/train_catboost/align_predictions)を直接呼ばないこと(AST で検証)
    - REVIEW H6: market_implied 算出が fuku_odds_lower から行われること(fukuodds でない・AST/grep で検証)
    - D-16 の両方改善しない場合に「構造的限界寄り」フラグが立つ
  </behavior>
  <action>
    `tests/model/test_speed_figure_stopgate.py` を新規作成する。モジュール docstring で「Phase 9 SC#6 stop gate・合成データ E2E smoke・binning 契約固定再利用確認・DB 不要(KEIBA_SKIP_DB_TESTS=1 で実行可)」を明記。

    [テスト]
    (a) `test_binning_constants_imported_not_redefined`(契約固定): scripts/run_speed_figure_stopgate.py のソースを `inspect.getsource` + `ast.parse` で解析し・`from src.model.evaluator import` / `from src.model.segment_eval import` の import 文が存在し・かつモジュール内で CALIBRATION_CURVE_BINS/ODDS_BAND_EDGES に「代入」(ast.Assign で targets にこれらの名前を含む)がないことを assert(再定義禁止・§15.2 不変・bit-identical)。

    (b) `test_d16_structural_limit_flag`(D-16): 合成データで「両方とも改善しない」シナリオを構築(baseline も +speed_figure も同程度の calibration/ROI・residual proxy に差なし)・D-16 判定関数(スクリプトから import 可能なヘルパー)を呼出し・"構造的限界寄り" フラグが立つことを assert。

    (c) `test_d16_effective_signal_flag`(D-16 対称): 合成データで「selected calibration 改善」または「residual proxy シグナル残存」のいずれかがあるシナリオで・「特徴量追加の有効性シグナルあり・Phase 10 進行候補」フラグが立つことを assert。

    (d) `test_make_model_version_phase9_format`(model_version 採番): `make_model_version("20260625-1a-speedfigure-v1", "lightgbm", 1)` が "20260625-1a-speedfigure-v1-lgb-v1" を返すことを assert(review HIGH#4・prefix 全体使用・二重 postfix 回避)。

    (e) `test_market_implied_diagnostic_only_and_correct_column`(SAFE-01 + REVIEW H6): スクリプトのソースを AST 解析し・market_implied が診断用ローカル変数としてのみ使用され・FEATURE_COLUMNS やモデル入力の X に混入しないことを確認(簡易チェック・FEATURE_COLUMNS に market 系カラムが無いことは P04 audit で完全証明済み)。**REVIEW H6**: あわせて market_implied の算出が `fuku_odds_lower`(src/ev/odds_snapshot.py L210-215 の正しい列名)から行われていることを grep/AST で assert し・誤った `fukuodds`(実コードに存在しない)を使用していないことを確認する(`grep -c 'fuku_odds_lower' scripts/run_speed_figure_stopgate.py` >= 1 かつ `fukuodds` が出現しない)。

    (f) `test_json_output_strict_with_sanitizer`(RFC 8259 + REVIEW M4): 合成結果 dict に NaN/Inf を混ぜた場合・スクリプトの `_sanitize_for_json` helper(M4) が NaN→None/"NaN"・Inf→"Infinity" に変換し・その後 `json.dumps(sort_keys=True, ensure_ascii=False, allow_nan=False)` が成功することを確認する。sanitizer を通さずに NaN を含む dict を allow_nan=False で dumps すると ValueError になることも assert し・sanitizer の必要性を証明する。

    (g) `test_orchestrator_path_not_raw_trainer`(REVIEW H2/H7/H8): スクリプトのソースを AST 解析し・`from src.model.orchestrator import train_and_predict` の import と `train_and_predict(...)` の呼出が存在すること・かつ `train_lightgbm` / `train_catboost` / `align_predictions` / `_prepare_catboost_pool` の生 trainer API が Call ノードとして出現しないこと(直接呼出禁止・H2 calibration skip / H7 sorted_index 誤用 / H8 categorical dtype mismatch 構造的回避)を assert する。

    ※ E2E smoke は live-DB 不要・スクリプトのコアヘルパー関数(指標算出・D-16 判定・_sanitize_for_json)を直接 import して合成データで検証する。スクリプトは `if __name__ == "__main__":` の他に・ヘルパー関数を module-level で定義しテストから import 可能にする(main 関数とロジック層を分離)。
  </action>
  <verify>
    <automated>uv run pytest tests/model/test_speed_figure_stopgate.py -x -v</automated>
  </verify>
  <done>
    - tests/model/test_speed_figure_stopgate.py が新規作成され・7テストが GREEN(H2/H6/H7/H8/M4 拡張含む)
    - binning 定数が import 再利用され・再定義されていないことが AST で証明
    - D-16 両シナリオ(構造的限界寄り / 有効性シグナルあり)のフラグが正しく立つ
    - make_model_version の Phase 9 形式が確認
    - REVIEW H2/H7/H8: orchestrator.train_and_predict 使用・生 trainer 直接呼出禁止が AST で証明
    - REVIEW H6: market_implied が fuku_odds_lower から算出(fukuodds でない)ことが検証
    - REVIEW M4: _sanitize_for_json が NaN/Inf を処理し json が RFC 8259 strict(sanitizer 適用済)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: SC#6 stop gate 実行結果の確認と Phase 10 進行可否判断(D-16)</name>
  <files>reports/09-stopgate.{md,json}</files>
  <action>SC#6 stop gate スクリプトと合成データ E2E smoke テストを作成した。live-DB で v1.0 baseline と +speed_figure 単体モデルを同一 BT split/policy で比較し・D-14 4指標(odds_band×p_bin calibration 改善 / selected ROI 改善 / Brier等の非劣化 / model-market disagreement ROI 改善)と D-15 軽量 residual proxy を reports/09-stopgate.{md,json} に出力する。ユーザーが結果を確認し Phase 10-12 進行可否を判断する。</action>
  <verify>checkpoint:human-verify・resume-signal "approved"/"structural-limit"/課題記述</verify>
  <done>ユーザーが reports/09-stopgate.{md,json} を確認し Phase 10 進行(approved)または構造的限界判定(structural-limit)または課題記述を行う</done>
  <what-built>
    SC#6 stop gate スクリプトと合成データ E2E smoke テストを作成した。live-DB で v1.0 baseline と +speed_figure 単体モデルを同一 BT split/policy で比較し・D-14 4指標(odds_band×p_bin calibration 改善 / selected ROI 改善 / Brier等の非劣化 / model-market disagreement ROI 改善)と D-15 軽量 residual proxy を reports/09-stopgate.{md,json} に出力する。
  </what-built>
  <how-to-verify>
    1. KEIBA_SKIP_DB_TESTS を unset し live-DB に接続した状態で `uv run python scripts/run_speed_figure_stopgate.py` を実行。
    2. reports/09-stopgate.json と reports/09-stopgate.md が生成されることを確認。
    3. md レポートを開き・D-14 4指標の数値を確認:
       - 指標1: 投票層(odds>=4.9 かつ p∈[0.15,0.20])の |mean_pred - frac_pos| が v1.0 から改善したか(D-08 の過大予測4倍是正のシグナル)
       - 指標2: selected-only 実現 ROI が v1.0(回収率~0.65-0.70)から改善したか
       - 指標3: Brier(v1.0=0.15222)/LogLoss(0.47488)/AUC(0.73230)が許容幅(Brier +0.005・LogLoss +0.02・AUC -0.005)以内か
       - 指標4: model-market disagreement bucket で同一 market 内の model_p 高低による実 ROI 差が残るか(residual proxy シグナル)
    4. D-16 判定フラグを確認:
       - 両方(指標1 selected calibration 改善・指標4 residual proxy シグナル)が全く改善しない場合 → "構造的限界寄り・Phase 10-12 進行前にユーザー確認" が記録される。この場合・マイルストーン継続可否を判断する(早期撤退 or Phase 10 進行)。
       - 片方でも改善 → 「Phase 10 進行候補」が記録される。
    5. 並行して reports/09-speed-figure-domain.html(P04・SC#5)を開き・ドメイン整合性(同一馬安定・クラス単調・外れ値なし)を目視確認する。points_per_second [ASSUMED] テーブルが不妥当なら後続 plan での微調整を検討する。
  </how-to-verify>
  <resume-signal>Type "approved" (Phase 10 進行可) または "structural-limit" (マイルストーン見直し) または課題記述(points_per_second 微調整等)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live-DB → stop gate スクリプト | readonly SELECT で snapshot と prediction/market データを読込。DSN は masked のみログ(T-06-15) |
| §15.2 事前登録指標 → stop gate 評価 | evaluator/segment_eval の binning 定数を import 再利用し・再定義しない(bit-identical・§15.2 不変) |
| 診断層(market_implied) → 特徴量層 | market_implied は D-15 residual proxy の診断層のみで使用し・speed_figure 特徴量や model_p 入力には絶対混入しない(SAFE-01/EVAL-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-17 | Tampering | binning 定数の再定義・上書き(§15.2 事前登録指標不変違反) | high | mitigate | evaluator/segment_eval から import で再利用・スクリプト内での代入を AST テストで禁止・bit-identical保証 |
| T-09-18 | Tampering | market_implied の speed_figure 特徴量への混入(SAFE-01 違反・市場回帰) | high | mitigate | 診断層ローカル変数のみ・FEATURE_COLUMNS には含まない(P04 audit で完全証明済み)・スクリプト docstring で明示 |
| T-09-19 | Repudiation | BT split/policy/選択ルールの v1.0 との不一致(公平性違反・D-13) | high | mitigate | 同一 BT split(§15.5 BT-1)・同一 odds_snapshot_policy・同一 selector 関数を両モデルで使用・スクリプト引数で固定 |
| T-09-26 | Tampering/Spoofing | REVIEW H2: 生 trainer 直接呼出で calibration pipeline を skip(D-13 公平性違反・未 calibration と calibration 済み baseline の非比較) | high | mitigate | orchestrator.train_and_predict(L234-562・train→calibrate→predict 統合)を両 snapshot で使用・生 trainer(train_lightgbm/train_catboost/align_predictions)直接呼出を AST テストで禁止 |
| T-09-27 | Tampering | REVIEW H7: CatBoost sorted_index(training Pool 由来)を test 予測整列に誤用・誤った馬の予測を生成 | high | mitigate | orchestrator L519-538 が test 予ーカー用 sorted_test_idx を生成し align_predictions で元順序復元・生 trainer 戻り値の sorted_index は test に使わない(orchestrator 経由で回避) |
| T-09-28 | Tampering | REVIEW H8: LightGBM categorical dtype mismatch で予測失敗または silent 誤予測 | high | mitigate | orchestrator L490-517 が _prepare_lightgbm_train_eval で train/test の categorical categories を統一・生 model.predict_proba(X_test) はこの準備を skip(orchestrator 経由で回避) |
| T-09-29 | Information Disclosure/Spoofing | REVIEW H6: 誤 odds 列名 fukuodds で KeyError または silent NaN(実コードは fuku_odds_lower/fuku_odds_upper) | high | mitigate | src/ev/odds_snapshot.py L210-215 の正しい列名 fuku_odds_lower を使用・AST/grep テストで fukuodds を禁止 |
| T-09-30 | Repudiation | REVIEW M4: single-class AUC 等の NaN/Inf が json 出力で ValueError または silent 欠損 | medium | mitigate | _sanitize_for_json helper が NaN/Inf を None/"NaN"/"Infinity" に変換・allow_nan=False で安全に json.dumps・segment_eval.py L94-112 sanitizer pattern 踏襲 |
| T-09-20 | Information Disclosure | 生 DSN のログ出力 | medium | mitigate | settings.dsn_masked のみログ出力(scripts/run_evaluation.py L1316-1441 idiom・T-06-15) |
| T-09-21 | Denial of Service | stop gate 重クエリの orphan CPU 張り付き | medium | mitigate | SET statement_timeout='30s' をセッション先頭で発行(MEMORY.md subagent-db-query-statement-timeout) |
| T-09-22 | Repudiation | 後知恵すり替え(stop gate 結果を見てから閾値・BT窓・policy を再選択) | high | mitigate | §11.2 聖域・BT-1/30min_before/selector はスクリプト引数で固定し結果後の再選択禁止・D-16 判定ロジックは事前登録 |
</threat_model>

<verification>
- `uv run pytest tests/model/test_speed_figure_stopgate.py -x -v` が GREEN(7テスト・H2/H6/H7/H8/M4 拡張含む)
- `uv run python -c "import ast; ast.parse(open('scripts/run_speed_figure_stopgate.py').read()); print('syntax OK')"` が OK
- `grep -c 'CALIBRATION_CURVE_BINS\|ODDS_BAND_EDGES\|make_model_version' scripts/run_speed_figure_stopgate.py` >= 3(import 再利用)
- REVIEW H2/H7/H8: `grep -c 'train_and_predict' scripts/run_speed_figure_stopgate.py` >= 1・かつ `grep -v '^#' scripts/run_speed_figure_stopgate.py | grep -c 'train_lightgbm\|train_catboost\|align_predictions'` == 0(生 trainer 直接呼出禁止)
- REVIEW H6: `grep -c 'fuku_odds_lower' scripts/run_speed_figure_stopgate.py` >= 1・かつ `grep -v '^#' scripts/run_speed_figure_stopgate.py | grep -cw 'fukuodds'` == 0(誤列名禁止)
- REVIEW M4: `grep -c '_sanitize_for_json' scripts/run_speed_figure_stopgate.py` >= 1(NaN sanitization helper)
- `grep -c 'dsn_masked' scripts/run_speed_figure_stopgate.py` >= 1
- `grep -c 'statement_timeout' scripts/run_speed_figure_stopgate.py` >= 1
- KEIBA_SKIP_DB_TESTS unset で `uv run python scripts/run_speed_figure_stopgate.py` が exit 0・reports/09-stopgate.{md,json} 生成
- json が RFC 8259 strict(allow_nan=False・M4 sanitizer 適用済)
- checkpoint:human-verify で D-16 判定をユーザー確認
</verification>

<success_criteria>
- SC#6 stop gate: v1.0 baseline vs +speed_figure を同一 BT split/policy/selector で比較し D-14 4指標と D-15 residual proxy を算出
- §15.2 事前登録指標不変: evaluator/segment_eval の binning 定数を import 再利用・再定義なし・bit-identical
- D-16: 両方改善しない場合「構造的限界寄り」を honest 記録・片方でも改善なら「Phase 10 進行候補」
- SAFE-01: market_implied は診断層のみ・特徴量への混入なし
- 生 DSN ログ禁止・statement_timeout 設定・readonly pool try/finally
- checkpoint:human-verify で Phase 10-12 進行可否をユーザー判断(D-16)
</success_criteria>

<output>
`.planning/phases/09-speed-figure-foundation/09-05-SUMMARY.md` を作成し・以下を記録する:
- SC#6 stop gate の実行結果(D-14 4指標の数値・D-15 residual proxy シグナルの有無)
- D-16 判定フラグ(構造的限界寄り or Phase 10 進行候補)
- checkpoint:human-verify のユーザー判断(approved / structural-limit / 課題)
- v1.0 baseline との Brier/LogLoss/AUC/selected ROI 比較表
- **許容幅(Brier +0.005/LogLoss +0.02/AUC -0.005)の根拠と採用経緯**(RESEARCH.md Open Questions (RESOLVED) #2・CONTEXT Claude's Discretion・v1.0 Brier=0.15222 基準の ~3.3% 変動許容)
- **Phase 11 SC#2 事前登録マージンとの整合確認事項**(cross-reference): 本 P05 stop gate の許容幅は Phase 11 SC#2 で事前登録される非劣化マージンと同一スケールであることを前提とする。Phase 11 plan 作成時に SC#2 事前登録値が本 P05 より厳しい場合（例: Brier +0.003 等）・P05 の stop gate 判定を Phase 11 視点で再解釈する必要がある（stop gate 通過≠Phase 11 SC#2 通過）。この依存関係を Phase 11 plan の context に引用すること。
- points_per_second [ASSUMED] テーブルの SC#5 ドメイン整合性確認結果・必要あれば後続 plan での微調整提案
- Phase 10(FEAT-02/03)に進むかどうかのユーザー決定記録
</output>

## Artifacts this phase produces

本 PLAN(P05)が新規作成するシンボル:
- `scripts/run_speed_figure_stopgate.py`:
  - `main()`(argparse CLI・live-DB・reports/09-stopgate.{md,json} 出力)
  - D-14 指標算出ヘルパー(`_compute_selected_calibration`・`_compute_selected_roi`・`_compute_global_metric_delta`・`_compute_residual_proxy`)
  - D-16 判定ヘルパー(`_decide_stopgate_verdict`)
  - REVIEW M4: `_sanitize_for_json(obj)`(NaN/Inf → None/"NaN"/"Infinity"・segment_eval.py L94-112 sanitizer pattern 再利用)
  - REVIEW H2/H7/H8: orchestrator.train_and_predict 経由で両 snapshot の calibration 済み予測を取得(生 trainer 直接呼出禁止)
  - REVIEW H6: market_implied は fuku_odds_lower から算出(fukuodds でない)
  - model_version: `20260625-1a-speedfigure-v1-lgb-v1`(make_model_version 経由・review HIGH#4)
- `tests/model/test_speed_figure_stopgate.py`:
  - `test_binning_constants_imported_not_redefined`
  - `test_d16_structural_limit_flag`
  - `test_d16_effective_signal_flag`
  - `test_make_model_version_phase9_format`
  - `test_market_implied_diagnostic_only_and_correct_column`(SAFE-01 + REVIEW H6)
  - `test_json_output_strict_with_sanitizer`(RFC 8259 + REVIEW M4)
  - `test_orchestrator_path_not_raw_trainer`(REVIEW H2/H7/H8)
- `reports/09-stopgate.{md,json}`(live-DB 生成・git tracked)

後続 PLAN が参照する新シンボル:
- Phase 10(FEAT-02/03)は SC#6 stop gate の結果(approved)を前提として進行可否判断
- Phase 11(MODEL-01)は §15.2 事前登録指標不変の慣行を本 PLAN で確立した binning 契約固定 idiom から踏襲
- Phase 12(EVAL-02)は D-15 軽量 residual proxy を正式 falsification test に拡張(clustered SE・market 再校正・本 PLAN で意図的に省略した部分)
