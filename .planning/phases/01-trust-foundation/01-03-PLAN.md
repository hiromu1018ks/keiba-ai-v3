---
phase: 01-trust-foundation
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-04]
files_modified:
  - src/etl/normalize.py
  - src/etl/class_normalize.py
  - scripts/run_normalized_etl.py
  - tests/test_normalized_etl.py
  - tests/test_class_normalization.py
  - tests/test_raw_immutability.py
autonomous: true
requirements: [DATA-02, DATA-03]
must_haves:
  truths:
    - "normalized スキーマの n_race / n_uma_race テーブルが全 varchar から明示的キャストされた型付きカラムを持つ（kyori=integer, futan=real, hassotime=timestamp 等）"
    - "raw（public.n_*）は ETL 前後で1バイトも変わらないことが pytest で証明される（成功基準#2 / D-06 二重保護）"
    - "クラス正規化が class_normalization.yaml の jyokencd5×gradecd×year 機械導出で行われ class_code_normalized / class_name_normalized / class_level_numeric / post_2019_class_system_flag / is_grade_race / is_listed / is_open_class / grade_numeric / class_normalization_status を生成する"
    - "jyokencd5='005' が 2018年（500万下）と2019年後半（1勝クラス）の両方で同じ class_level_numeric=1 を返す（Pitfall 7 核心・制度改革を跨ぐコード連続性）"
    - "post_2019_class_system_flag が race_date >= '2019-06-08' で True、未満で False となる（D-11 基準日）"
    - "未知の jyokencd5 / gradecd を検出した場合 class_normalization_status='unresolved' となり silent fallback しない（D-13）"
    - "ETL 実行後に scripts/run_quality_report.py が依然 verdict=pass を出力する（raw 不変の直接確認）"
  artifacts:
    - path: "src/etl/normalize.py"
      provides: "raw(n_*)→normalized への型/コード変換 ETL（psycopg3 + Python 明示実装、D-08）"
      contains: "def run_normalized_etl"
    - path: "src/etl/class_normalize.py"
      provides: "class_normalization.yaml 適用・jyokencd5×gradecd×year 機械導出・unresolved 隔離"
      contains: "def normalize_class"
    - path: "scripts/run_normalized_etl.py"
      provides: "ETL 実行エントリポイント・raw 不変ログ出力"
      contains: "run_normalized_etl"
    - path: "tests/test_normalized_etl.py"
      provides: "型キャスト・code 変換・raw 不変の integration test"
      contains: "test_type_cast"
    - path: "tests/test_class_normalization.py"
      provides: "DATA-03 の unit test・Pitfall 7 連続性・D-11 基準日・D-13 unresolved"
      contains: "test_code_005_spans_reform"
    - path: "tests/test_raw_immutability.py"
      provides: "成功基準#2 の直接証明・ETL 前後の raw 行ハッシュ不変 + pg_stat_user_tables"
      contains: "test_raw_unchanged_after_etl"
  key_links:
    - from: "src/etl/normalize.py"
      to: "everydb2.public.n_race / n_uma_race"
      via: "psycopg3 readonly SELECT（Pitfall 2 jyocd BETWEEN '01' AND '10'）"
      pattern: "jyocd BETWEEN '01' AND '10'"
    - from: "src/etl/normalize.py"
      to: "everydb2.normalized.n_race / n_uma_race"
      via: "psycopg3 INSERT（明示的キャスト付き）"
      pattern: "INSERT INTO normalized"
    - from: "src/etl/class_normalize.py"
      to: "src/config/class_normalization.yaml"
      via: "YAML 読込 → jyokencd5×gradecd×year 機械導出（hondai regex 不使用）"
      pattern: "class_normalization\\.yaml"
    - from: "tests/test_raw_immutability.py"
      to: "everydb2.public.n_race"
      via: "ETL 前後の md5 指紋 + pg_stat_user_tables.n_tup_upd/n_tup_del==0"
      pattern: "pg_stat_user_tables"
---

<objective>
DATA-02（normalized ETL）と DATA-03（クラス正規化）を統合実装し、成功基準#2（raw read-only 証明）と Pitfall 7（クラス正規化のコード基準連続性）を pytest で証明する。raw（public.n_*）の全 varchar カラムを明示的キャストして normalized スキーマへ書込み、class_normalization.yaml（plan 01）を適用して class_* / is_grade_race / post_2019_class_system_flag 等を機械導出する。ETL 前後で raw の行ハッシュが不変であることをテストし、D-06 二重保護を実証する。

Purpose: Build-Order DAG step 2。品質ゲート（plan 02）が通った raw を typed/code-converted/class-normalized な normalized 層へ変換し、Phase 2 labels / Phase 3 features が読む準備を整える。Pitfall 1（全 varchar）・Pitfall 2（NAR 混入）・Pitfall 7（クラス正規化2019改革）を統合的に検証する最も load-bearing な plan。
Output: src/etl/{normalize,class_normalize}.py / scripts/run_normalized_etl.py / tests/test_{normalized_etl,class_normalization,raw_immutability}.py
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-trust-foundation/01-CONTEXT.md
@.planning/phases/01-trust-foundation/01-RESEARCH.md
@.planning/phases/01-trust-foundation/01-PATTERNS.md
@.planning/phases/01-trust-foundation/01-VALIDATION.md
@.planning/phases/01-trust-foundation/01-01-SUMMARY.md
@.planning/phases/01-trust-foundation/01-02-SUMMARY.md
@.planning/phases/01-trust-foundation/01-04-SUMMARY.md
@docs/keiba_ai_requirements_v1.3.md
@docs/everydb2/03-RACE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: class_normalize.py（class_normalization.yaml 適用・jyokencd5×gradecd×year 機械導出・unresolved 隔離）+ DATA-03 unit test</name>
  <files>src/etl/class_normalize.py, tests/test_class_normalization.py</files>
  <read_first>
    - 01-RESEARCH.md（§クラス正規化完全対応表（実データ検証済み）/ §Anti-Patterns to Avoid / §Common Pitfalls Pitfall 7）
    - 01-PATTERNS.md（`src/etl/class_normalize.py` / `tests/test_class_normalization.py` 推奨イディオム）
    - 01-CONTEXT.md（D-09/D-10/D-11/D-12/D-13 / DATA-03）
    - docs/keiba_ai_requirements_v1.3.md §12.3（クラス正規化骨格）/ §7.3（障害・新馬戦モデル除外）
    - src/config/class_normalization.yaml（plan 01 で作成・正データ）
  </read_first>
  <behavior>
    - normalize_class(jyokencd5, gradecd, race_date, *, config) の場合分け:
      - race_date >= "2019-06-08" → post_2019_class_system_flag=True、未満 → False（D-11）
      - jyokencd5 が config.jyokencd5_map の6値（701/703/005/010/016/999）に存在 → class_code_normalized/class_name_normalized/class_level_numeric を返す
      - gradecd が config.gradecd_map のキーに存在 → is_grade_race/is_listed/is_open_class/grade_numeric を返す
      - jyokencd5 or gradecd が対応表に無い → class_normalization_status="unresolved" を返し他フィールドは null（D-13 silent fallback 禁止）
      - **hondai（レース名）の regex マッチは一切使わない**（Pitfall 7・Anti-Pattern）
    - jyokencd5='005' は 2018年でも2019年後半でも class_level_numeric=1（制度改革を跨ぐコード連続性、Pitfall 7 核心）
    - gradecd='A'（G1）→ is_grade_race=True, grade_numeric=1。gradecd='L'（Listed）→ is_listed=True。gradecd=''（一般条件戦）→ 全 flag False
- audit_gradecd_d_by_syubetucd(read_cur) の場合分け:
  - `SELECT gradecd, syubetucd, count(*) FROM public.n_race WHERE jyocd BETWEEN '01' AND '10' AND gradecd IN ('C','D') GROUP BY 1,2` を実行
  - 戻り値の `rows` を返す（`syubetucd='18'/'19'` の割合が高ければ D の大部分が障害G3、低ければ平地G3/legacy、01-03 SUMMARY で Phase 4 調整の判断材料）
  </behavior>
  <action>
`src/etl/class_normalize.py` を作成:
- `import yaml`, `from pathlib import Path`, `from datetime import date`
- `def load_class_config(path: str | Path = "src/config/class_normalization.yaml") -> dict`: YAML を読込 dict で返す。post_2019_class_system_reform_date を `date.fromisoconfig(...)` で parse。
- `def normalize_class(jyokencd5: str, gradecd: str, race_date: date, *, config: dict | None = None) -> dict`: 引数 config が None なら `load_class_config()` を呼出。以下を機械導出（**hondai 等の名称を使わない、Pitfall 7**）:
  - `post_2019_class_system_flag = race_date >= config["post_2019_class_system_reform_date"]`（date vs date 比較、文字列比較ではない）
  - `jyokencd5` を str に正規化（"005" 等）。`j5 = config["jyokencd5_map"].get(jyokencd5)`
  - `gradecd` を str に正規化。`g = config["gradecd_map"].get(gradecd)`
  - 両方存在 → `class_code_normalized=jyokencd5`, `class_name_normalized=j5["class_name_normalized"]`, `class_level_numeric=j5["class_level_numeric"]`, `is_grade_race=g.get("is_grade_race", False)`, `is_listed=g.get("is_listed", False)`, `is_open_class=g.get("is_open_class", False)`, `grade_numeric=g.get("grade_numeric")`, `class_normalization_status="resolved"` を返す
  - いずれか不在 → `class_normalization_status="unresolved"`・他フィールドは None・WARNING ログを出力（D-13 silent fallback 禁止）。**未知コードを勝手に '999' 等にマップしない**。
- `def normalize_race_classes(df, *, config=None) -> pd.DataFrame`: pandas DataFrame（jyokencd5/gradecd/year/monthday 列を持つ）に上記を row wise 適用し class_* 列を追加。race_date は `(year + monthday)` から `datetime.strptime(..., "%Y%m%d").date()` で構築（Pitfall 1: 文字列ソートではなく date オブジェクトで比較）。WARNING 件数も集計して返す。
- docstring に「Pitfall 7 対応: hondai 名称マッチ不使用・jyokencd5+gradecd+year の機械導出・2019-06-08 基準日」を明記。

**RESEARCH Open Question #1（A1 gradecd='D'）解決用 cross-check（必須・本 Task に含める）:**
- `def audit_gradecd_d_by_syubetucd(read_cur, *, config=None) -> dict` を追加実装: `SELECT gradecd, syubetucd, count(*) FROM public.n_race WHERE jyocd BETWEEN '01' AND '10' AND gradecd IN ('C','D') GROUP BY 1,2 ORDER BY 1,2` を実行し結果（`{"rows": [{"gradecd": "C", "syubetucd": "00", "count": N}, ...]}`）を返す。`syubetucd IN ('18','19')` は障害（§7.3 でモデル除外）。
- 関数 docstring に「plan 01-01 で `gradecd='D'` を `grade_numeric=3` として暫定扱いしたが、本 cross-check の実測結果（平地 vs 障害の混在比率）を 01-03 SUMMARY で報告し、必要に応じて Phase 4 特徴量設計で再調整する」を明記（RESEARCH Open Questions RESOLVED の直接実装）。

`tests/test_class_normalization.py` を作成（01-VALIDATION.md §Phase Requirements → Test Map 準拠）:
- `test_code_005_spans_reform`: 同じ jyokencd5='005' を race_date=2018-06-01 と race_date=2019-07-01 で呼出し、両方とも `class_level_numeric == 1` を assert（Pitfall 7 核心・制度改革を跨ぐ連続性）。class_name_normalized は時代で異なっても良いため class_level_numeric の連続性を検証する
- `test_reform_date`: race_date=2019-06-07 で `post_2019_class_system_flag is False`、race_date=2019-06-08 で `True`、race_date=2019-06-09 で `True`（D-11 境界日）
- `test_grade_derivation_A_G1`: gradecd='A' → `is_grade_race=True`, `grade_numeric=1`
- `test_grade_derivation_L_listed`: gradecd='L' → `is_listed=True`, `is_grade_race=False`
- `test_grade_derivation_general_empty`: gradecd='' → 全 flag False
- `test_unresolved_unknown_jyokencd5`: jyokencd5='ZZZ'（未定義）→ `class_normalization_status == "unresolved"`・他 class フィールドは None（D-13）
- `test_unresolved_unknown_gradecd`: gradecd='Z' → 同様 unresolved
- `test_normalize_race_classes_df`: サンプル DataFrame（複数行）に適用し全行に class_* 列が追加される
- `test_audit_gradecd_d_by_syubetucd` (`@pytest.mark.requires_db`): 実 DB で `audit_gradecd_d_by_syubetucd(cur)` を呼出し、戻り値 `rows` が空でないこと・`gradecd ∈ {'C','D'}` のみであること・`syubetucd` 列が含まれることを assert。件数・分布は INFO ログ出力（RESEARCH Open Question #1 RESOLVED の検証）。`.env` 未設定かつ `KEIBA_SKIP_DB_TESTS=1` 以外では fail（plan 01-01 Task 2 の conftest marker ポリシーに従う）。
  </action>
  <verify>
    <automated>uv run pytest tests/test_class_normalization.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `src/etl/class_normalize.py` が `def normalize_class` と `def load_class_config` を含む
    - `src/etl/class_normalize.py` が `post_2019_class_system_reform_date` を参照し date 比較で `post_2019_class_system_flag` を算出する
    - `src/etl/class_normalize.py` が `class_normalization_status` フィールドを返し unresolved 戻り値を含む（D-13）
    - `src/etl/class_normalize.py` が `hondai` を含まない（Pitfall 7・`grep -c 'hondai' src/etl/class_normalize.py` が 0）
    - `tests/test_class_normalization.py` が `test_code_005_spans_reform` と `test_reform_date` と `test_unresolved_unknown_jyokencd5` を含む
    - `test_code_005_spans_reform` が 2018年と2019年後半の両方で `class_level_numeric == 1` を assert
    - `test_reform_date` が 2019-06-07 で False、2019-06-08 で True を assert（D-11 境界）
    - `src/etl/class_normalize.py` が `def audit_gradecd_d_by_syubetucd` を含む（RESEARCH Open Question #1 RESOLVED の実装・`grep -c 'def audit_gradecd_d_by_syubetucd' src/etl/class_normalize.py` が 1）
    - `tests/test_class_normalization.py` が `test_audit_gradecd_d_by_syubetucd` を含み `@pytest.mark.requires_db` マークを持つ
    - `uv run pytest tests/test_class_normalization.py -v` が green
  </acceptance_criteria>
  <done>クラス正規化が jyokencd5×gradecd×year 機械導出で行われ・2019-06-08 基準日・005 の制度改革跨ぎ連続性・D-13 unresolved 隔離が unit test で証明される（DATA-03 / Pitfall 7 / D-11/D-13）</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: normalize.py（raw→normalized 型/コード変換 ETL・psycopg3 明示実装）+ integration test</name>
  <files>src/etl/normalize.py, scripts/run_normalized_etl.py, tests/test_normalized_etl.py</files>
  <read_first>
    - src/etl/class_normalize.py（Task 1 で作成）
    - src/config/class_normalization.yaml・src/config/code_tables.yaml（plan 01）
    - src/db/connection.py・src/db/schema.py（plan 01）
    - 01-RESEARCH.md（§Pitfall 1 全 varchar / §Pitfall 2 NAR 混入 / §Pitfall 4 s_*/n_* / §Don't Hand-Roll）
    - 01-PATTERNS.md（`src/etl/normalize.py` / `tests/test_normalized_etl.py` / `scripts/run_normalized_etl.py` 推奨イディオム）
    - 01-CONTEXT.md（D-06 二重保護 / D-08 psycopg3 Python 明示実装・DuckDB 補助のみ）
    - docs/keiba_ai_requirements_v1.3.md §12.1-12.2（DB/ETL/5層スキーマ）
    - docs/everydb2/03-RACE.md（n_race 110フィールドの varchar 型・kyori/futan/hassotime 等の数値項目）
  </read_first>
  <behavior>
    - run_normalized_etl(read_pool, write_conn, *, tables=["n_race","n_uma_race"]) の場合分け:
      - raw（public.n_*）から psycopg3 readonly SELECT で読込（全クエリで `WHERE jyocd BETWEEN '01' AND '10'`、Pitfall 2）
      - n_race の主要数値カラムを明示キャスト（Pitfall 1）: `kyori` → integer, `futan` → CAST(CAST(futan AS numeric)/10.0 AS real) 単位0.1kg, `hassotime` → to_timestamp, `year||monthday` → date
      - code_tables.yaml で jyocd→競馬場名・syubetucd→レース種別等のコード変換
      - `hassotime` が EveryDB2 初期値 `'0'` や長さ4数値でない不正値の場合は `race_start_datetime=pd.NaT` にフォールバック + WARNING（ETL 停止回避）・正常値のみ timestamp 構築（Warning #5）
      - class_normalize.normalize_race_classes(df) で class_* 列を追加
      - normalized スキーマのテーブル（CREATE TABLE IF NOT EXISTS normalized.n_race (...) with typed columns）に INSERT
      - **raw には一切 UPDATE/DELETE/INSERT を発行しない**（成功基準#2、tests/test_raw_immutability.py で保証）
  </behavior>
  <action>
`src/etl/normalize.py` を作成（D-08: psycopg3 + Python 明示実装、DuckDB は補助のみ）:

1. `import pandas as pd`, `from psycopg import Cursor`, `from psycopg_pool import ConnectionPool`, `from src.etl.class_normalize import normalize_race_classes`
2. `def _create_normalized_tables(write_cur)`: `CREATE TABLE IF NOT EXISTS normalized.n_race (...)` で型付きカラム定義を作成。主要カラム（docs/everydb2/03-RACE.md 参照）:
   - PK: `year int, jyocd varchar(2), kaiji int, nichiji varchar(2), racenum int`
   - 数値（明示キャスト・Pitfall 1）: `kyori integer, futan real, hassotonen integer`（futan は0.1kg単位→real）
   - 日付: `race_date date`（`year||monthday` から構築）
   - timestamp: `race_start_datetime timestamp`（`hassotime` HHMM を結合）
   - コード（varchar のまま保持）: `jyokencd5 varchar(3), gradecd varchar(1), syubetucd varchar(2), jyocd varchar(2)`
   - クラス正規化結果: `class_code_normalized varchar(3), class_name_normalized text, class_level_numeric smallint, post_2019_class_system_flag boolean, is_grade_race boolean, is_listed boolean, is_open_class boolean, grade_numeric smallint, class_normalization_status varchar(16)`
   - 同様に `normalized.n_uma_race` を作成（PK: 上記+umaban/kettonum、kakuteijyuni integer 等の数値キャスト）
3. `def _select_raw_race(read_cur, chunksize=10000)`: `SELECT year, jyocd, kaiji, nichiji, racenum, monthday, kyori, futan, hassotime, hassotonen, jyokencd5, gradecd, syubetucd, hondai FROM public.n_race WHERE jyocd BETWEEN '01' AND '10'`（Pitfall 2 JRA 限定・Pitfall 4 n_* 確定のみ）。psycopg3 の server-side cursor または chunked fetch で大量データを扱う。
4. `def _transform_race_df(df)`: pandas で `kyori=pd.to_numeric(df["kyori"], errors="coerce").astype("Int64")`、`futan=pd.to_numeric(df["futan"], errors="coerce")/10.0`、`race_date=pd.to_datetime(df["year"].astype(str)+df["monthday"], format="%Y%m%d", errors="coerce").dt.date` を実施。`race_start_datetime` 構築時は `hassotime` が EveryDB2 の初期値 `'0'`（RESEARCH.md Sources で確認: `HassoTime varchar(4) 初期値 0`）や長さ4の数値でない不正値を **事前チェック** し、不正値（`'0'`, 長さ≠4, `int()` 失敗, HH>23 or MM>59）は `race_start_datetime=pd.NaT` にフォールバック + WARNING ログ（件数集計）。`datetime.strptime('0', '%H%M')` が `ValueError` で ETL 全体が停止するのを防ぐ。正常値のみ `race_date + time(hour=HH, minute=MM)` で timestamp 構築。その後 `normalize_race_classes(df)` で class_* 列を追加。
5. `def run_normalized_etl(read_pool: ConnectionPool, write_conn, *, tables=["n_race","n_uma_race"]) -> dict`: 各テーブルについて `_create_normalized_tables` → SELECT raw → transform → INSERT into normalized の順で実行。戻り値は `{"rows_inserted": {table: count}, "class_unresolved_count": int, "raw_touched": False}`。`raw_touched` は常に False（read-only）。
6. **DuckDB は使わない**（§12.1 禁止・DuckDB は補助のみで今回は不要）。全て psycopg3 + pandas。

`scripts/run_normalized_etl.py` を作成。`Settings` から2つの接続（raw 読取 readonly ロール・normalized 書込ロール）を作成し `run_normalized_etl` を呼出。実行前後で `src.etl.raw_fingerprint` 相当の関数（下記 Task 3 で test_raw_immutability.py と共有）を呼出し raw の指紋をログ出力。

`tests/test_normalized_etl.py` を作成（integration・実 DB 接続必須・`.env` 未設定時 skip）:
- `test_type_cast_kyori_int`: 実 DB で ETL 後、`SELECT kyori FROM normalized.n_race LIMIT 1` が integer で返る（Pitfall 1）
- `test_type_cast_futan_real`: `futan` が real で返る（0.1kg 単位）
- `test_race_date_constructed`: `race_date` 列が date 型で 2015年以降の値を含む
- `test_jra_only_filter`: normalized.n_race の全行が `jyocd BETWEEN '01' AND '10'`（NAR 混入なし、Pitfall 2）
- `test_class_columns_populated`: class_level_numeric / post_2019_class_system_flag / class_normalization_status の3列が全行に存在
- `test_rowcount_jra_matches_raw`: normalized.n_race 件数 ≈ `SELECT count(*) FROM public.n_race WHERE jyocd BETWEEN '01' AND '10'`（誤差0）
- `test_hassotime_zero_falls_back_to_nat` (unit): `hassotime='0'`（EveryDB2 初期値）を含むサンプル DataFrame で `_transform_race_df` を呼出し、該当行の `race_start_datetime` が `pd.NaT` になること・ETL が `ValueError` で停止しないことを assert（Warning #5）
  </action>
  <verify>
    <automated>uv run pytest tests/test_normalized_etl.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `src/etl/normalize.py` が `def run_normalized_etl` と `def _create_normalized_tables` を含む
    - `src/etl/normalize.py` が `kyori` と `futan` と `race_date` の明示キャストロジックを含む（Pitfall 1）
    - `src/etl/normalize.py` が全 SELECT で `jyocd BETWEEN '01' AND '10'` を含む（Pitfall 2、`grep -c "jyocd BETWEEN '01' AND '10'" src/etl/normalize.py` が 1 以上）
    - `src/etl/normalize.py` が `INSERT INTO normalized` を含む
    - `src/etl/normalize.py` が `UPDATE public.n_` または `DELETE FROM public.n_` を含まない（成功基準#2、`grep -cE 'UPDATE public\\.n_|DELETE FROM public\\.n_' src/etl/normalize.py` が 0）
    - `src/etl/normalize.py` が `normalize_race_classes` を import する
    - `src/etl/normalize.py` が DuckDB を import しない（`grep -c 'import duckdb' src/etl/normalize.py` が 0、§12.1 禁止）
    - `scripts/run_normalized_etl.py` が `run_normalized_etl` を呼出し raw fingerprint ログ出力を含む
    - `tests/test_normalized_etl.py` が `test_type_cast_kyori_int` と `test_jra_only_filter` と `test_rowcount_jra_matches_raw` を含む
    - `src/etl/normalize.py` の `_transform_race_df` が `hassotime` の長さ4・数値妥当性を事前チェックし不正値を NaT にフォールバックするロジックを含む（Warning #5）
    - `tests/test_normalized_etl.py` が `test_hassotime_zero_falls_back_to_nat` を含む
    - `uv run pytest tests/test_normalized_etl.py -v` が green（実 DB 接続環境）
  </acceptance_criteria>
  <done>normalized ETL が psycopg3 で明示実装され raw(n_*) の全 varchar を typed/code-converted/class-normalized な normalized 層へ書込む・DuckDB 永続化なし（DATA-02 / D-08 / Pitfall 1/2/4）</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: raw 不変性 pytest（成功基準#2 直接証明）+ raw_fingerprint helper</name>
  <files>src/etl/raw_fingerprint.py, tests/test_raw_immutability.py</files>
  <read_first>
    - src/etl/normalize.py（Task 2 で作成）
    - 01-RESEARCH.md（§Pattern 2: raw read-only 二重保護）
    - 01-PATTERNS.md（`tests/test_raw_immutability.py` 推奨イディオム）
    - 01-CONTEXT.md（D-06 二重保護 / 成功基準#2）
    - docs/keiba_ai_requirements_v1.3.md §12.2（5層スキーマ・raw read-only）
  </read_first>
  <behavior>
    - compute_raw_fingerprint(read_cur, *, sample_sql) の場合分け:
      - 指定サンプル（例: 特定日の n_race 全行、または LIMIT 100）の md5 指紋を計算
      - pg_stat_user_tables.n_tup_upd と n_tup_del を取得（public.n_race / n_uma_race / n_harai 等の主要テーブル）
      - 戻り値: `{"row_hash": md5hex, "row_count": int, "n_tup_upd": dict[str,int], "n_tup_del": dict[str,int]}`
    - test_raw_unchanged_after_etl(pg_pool) の場合分け:
      - ETL 前 fingerprint = compute_raw_fingerprint(cur)
      - run_normalized_etl(read_pool, write_conn) を実行
      - ETL 後 fingerprint = compute_raw_fingerprint(cur)
      - assert before == after（row_hash・row_count が完全一致）
      - assert 全テーブルの n_tup_upd == 0 and n_tup_del == 0（raw に UPDATE/DELETE が発行されていない）
  </behavior>
  <action>
`src/etl/raw_fingerprint.py` を作成。01-RESEARCH.md §Pattern 2 の注意書き通り、全表 `string_agg` は重いので組み合わせアプローチを採用:
- `def compute_raw_fingerprint(read_cur, *, tables=["n_race","n_uma_race","n_harai","n_hyosu","n_odds_tanpuku"], sample_date: str | None = "20240101") -> dict`:
  - row_hash: 各テーブルについて代表サンプル（sample_date 指定時は `WHERE (year||monthday)=sample_date`、無指定時は `LIMIT 100`）を取得し `md5(string_agg(t::text, ',' ORDER BY ...))` を計算。各テーブルの md5 を dict で返す。
  - row_count: 各テーブルの全体件数 と JRA限定件数（`WHERE jyocd BETWEEN '01' AND '10'`）を dict で返す
  - `n_tup_upd` / `n_tup_del`: `SELECT relname, n_tup_upd, n_tup_del FROM pg_stat_user_tables WHERE schemaname='public' AND relname IN (...)` で取得（VACUUM でリセットされる場合があるため `pg_stat_reset_single_table_counters` を呼ばない前提。ETL 直前と直後の差分で判定する方が安全）。
- `def assert_raw_unchanged(before: dict, after: dict) -> None`: `before["row_hash"] == after["row_hash"]`・`before["row_count"] == after["row_count"]`・`after["n_tup_upd"]` の各値が `before["n_tup_upd"]` と等しい（差分0）・同様に n_tup_del。違反時 AssertionError に詳細メッセージを含む。

`tests/test_raw_immutability.py` を作成（01-VALIDATION.md §Phase Requirements → Test Map / 成功基準#2 直接証明）:
- `test_raw_unchanged_after_etl(pg_pool)`: session/pg_pool fixture（plan 01）を使用
  1. `before = compute_raw_fingerprint(cur)` を取得
  2. write 接続（normalized ロール、別途 Settings から作成 or 同 pool を使い分け）で `run_normalized_etl(pg_pool, write_conn)` を実行
  3. `after = compute_raw_fingerprint(cur)` を取得
  4. `assert_raw_unchanged(before, after)` を呼出
  - 実 DB 依存の integration test・`.env` 未設定時 skip
- `test_raw_role_has_no_update_grant` (`@pytest.mark.requires_db`): plan 01-01 Task 3 が `KEIBA_ADMIN_DSN` で `REVOKE UPDATE, DELETE, TRUNCATE ON ALL TABLES IN SCHEMA raw_everydb2 FROM <KEIBA_DB_USER>` を実行した前提を検証。**対象スキーマ・対象ロールを一意に固定して plan 01-01 と完全一致させる（Blocker #3 解決）:**
  - 対象スキーマ: `raw_everydb2`（plan 01-01 が REVOKE を発行したスキーマと同一・`public.n_*` を VIEW でラップした層）
  - 対象ロール: `Settings.db_user`（= `KEIBA_DB_USER` env var・plan 01-01 が REVOKE を発行した被付与者）
  - クエリ: `SELECT privilege_type FROM information_schema.role_table_grants WHERE table_schema='raw_everydb2' AND grantee = current_user AND privilege_type IN ('UPDATE','DELETE','TRUNCATE','INSERT')`（`KEIBA_DB_USER` で everydb2 に接続した上で自身の権限を問合せ）
  - assert: 戻り値が0行（UPDATE/DELETE/TRUNCATE/INSERT のいずれも GRANT されていない）・GRANT されていた場合は `fail`（plan 01-01 の REVOKE が未実行か KEIBA_ADMIN_DSN 未設定で Task 3 が失敗しているはず）
  - skip 条件: plan 01-01 Task 2 で定義した `KEIBA_SKIP_DB_TESTS=1` 環境変数時のみ（デフォルトは fail）。**WARNING skip は廃止**（D-06 二重保護のロール側は必須検証）
- `test_raw_is_view_in_raw_everydb2_schema` (`@pytest.mark.requires_db`): `SELECT table_type FROM information_schema.views WHERE table_schema='raw_everydb2' AND table_name='n_race'` が VIEW を返すことを確認（plan 01-01 schema.py で作成した raw_everydb2 VIEW の検証）。
  </action>
  <verify>
    <automated>uv run pytest tests/test_raw_immutability.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `src/etl/raw_fingerprint.py` が `def compute_raw_fingerprint` と `def assert_raw_unchanged` を含む
    - `src/etl/raw_fingerprint.py` が `pg_stat_user_tables` と `md5` を参照する
    - `tests/test_raw_immutability.py` が `test_raw_unchanged_after_etl` と `test_raw_role_has_no_update_grant` を含む
    - `test_raw_unchanged_after_etl` が ETL 前後の row_hash と n_tup_upd/n_tup_del を比較し assert する
    - `uv run pytest tests/test_raw_immutability.py -v` が green（実 DB + REVOKE 適用済み環境）
    - `src/etl/raw_fingerprint.py` が `UPDATE` または `DELETE` SQL を含まない（read-only helper、`grep -cE "'UPDATE |'DELETE " src/etl/raw_fingerprint.py` が 0）
  </acceptance_criteria>
  <done>ETL 前後で raw(public.n_*) の行ハッシュが不変であることが pytest で証明され、pg_stat_user_tables の n_tup_upd/n_tup_del==0 も検証される（成功基準#2 / D-06 二重保護）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| readonly ロール → public.n_*（raw） | 読取専用。UPDATE/DELETE は plan 01 で REVOKE 済み。本 plan は SELECT のみ発行（成功基準#2・ASVS V4） |
| write ロール → normalized.n_* | 新規スキーマへの INSERT のみ。raw には触れない（接続使い分け・D-06） |
| class_normalization.yaml → normalized.n_race.class_* | YAML 読込 → 機械導出。hondai 名称マッチ不使用（Pitfall 7）・未知コードは unresolved 隔離（D-13・ASVS V7） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | ETL バグで raw（public.n_*）を誤更新/削除（成功基準#2 違反） | mitigate | (1) plan 01-01 Task 3 で `KEIBA_ADMIN_DSN` 接続により `REVOKE UPDATE, DELETE, TRUNCATE ON ALL TABLES IN SCHEMA raw_everydb2 FROM <KEIBA_DB_USER>` を**必須実行**（skip廃止・未設定時は Task 3 自体が fail）、(2) `src/etl/normalize.py` は raw に SELECT のみ発行（grep gate: `UPDATE public.n_`/`DELETE FROM public.n_` が0件）、(3) `tests/test_raw_immutability.py` が ETL 前後の row_hash と pg_stat_user_tables.n_tup_upd/n_tup_del==0 を assert（データ側不変）、(4) `test_raw_role_has_no_update_grant` が `raw_everydb2` スキーマ上の `KEIBA_DB_USER` に UPDATE/DELETE/TRUNCATE/INSERT 権限が無いことを DB カタログから検証（権限側・plan 01-01 REVOKE 対象と完全一致・Blocker #3 解決）（D-06 二重保護・ASVS V4/V8） |
| T-03-02 | Tampering | クラス正規化で hondai（レース名）regex を使い 2019 改革で分類破綻（Pitfall 7） | mitigate | `class_normalize.py` は jyokencd5×gradecd×year のみで機械導出・`hondai` を参照しない（grep gate）。`test_code_005_spans_reform` が 2018/2019 で同じ class_level_numeric=1 を assert（DATA-03・ASVS V5） |
| T-03-03 | Tampering | silent fallback で未知 jyokencd5/gradecd を既存クラスに誤分類（D-13 違反） | mitigate | 未知コードは `class_normalization_status="unresolved"` として記録・隔離。WARNING ログ + 件数レポート。`test_unresolved_unknown_jyokencd5` が検証（D-13・ASVS V7） |
| T-03-04 | Tampering | 全 varchar raw の暗黙キャストで文字列ソート事故（Pitfall 1: `'900' > '1000'` が真） | mitigate | normalize.py が最初のステップで `pd.to_numeric(errors="coerce")` で明示キャスト。`futan` は0.1kg単位 → real。`test_type_cast_kyori_int` / `test_type_cast_futan_real` が検証（ASVS V5） |
| T-03-05 | Tampering | NAR（jyocd>=30）が normalized に混入し JRA 限定前提を破る（Pitfall 2） | mitigate | normalize.py の全 SELECT で `WHERE jyocd BETWEEN '01' AND '10'`。`test_jra_only_filter` が normalized.n_race 全行で JRA のみを assert（§2.1・ASVS V5） |
| T-03-06 | Tampering | DuckDB で永続化して §12.1 違反 | mitigate | `src/etl/normalize.py` は `import duckdb` しない（grep gate）。PostgreSQL のみ永続化（D-08・§12.1） |
</threat_model>

<verification>
- `uv run pytest tests/test_class_normalization.py tests/test_normalized_etl.py tests/test_raw_immutability.py -v` が green
- 実 DB 環境で `scripts/run_normalized_etl.py` 実行後 `scripts/run_quality_report.py` が依然 verdict=pass を出力（raw 不変の間接確認）
- normalized.n_race に class_level_numeric / post_2019_class_system_flag / class_normalization_status 列が存在
- normalized.n_race の件数が raw(public.n_race WHERE jyocd BETWEEN '01' AND '10') と一致
</verification>

<success_criteria>
- DATA-02: normalized ETL が psycopg3 で明示実装され raw(n_*) を typed/code-converted/class-normalized に変換（DuckDB 永続化なし）
- DATA-03: クラス正規化が jyokencd5×gradecd×year 機械導出で行われ・005 が制度改革を跨いで class_level_numeric=1・post_2019_class_system_flag が2019-06-08基準・未知コードは unresolved 隔離
- 成功基準#2: raw（public.n_*）が ETL 前後で1バイトも変わらないことが pytest で証明（D-06 二重保護）
- 成功基準#3: class_code_normalized / class_name_normalized / class_level_numeric / post_2019_class_system_flag が生成され code 005 が両時代で class_level_numeric=1（Pitfall 7 解決）
- Pitfall 1/2/4/7 全てが本 plan で統合検証される
</success_criteria>

<output>
Create `.planning/phases/01-trust-foundation/01-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

### Functions / Classes
- `src.etl.class_normalize.load_class_config(path="src/config/class_normalization.yaml") -> dict`
- `src.etl.class_normalize.normalize_class(jyokencd5, gradecd, race_date, *, config=None) -> dict`
- `src.etl.class_normalize.normalize_race_classes(df, *, config=None) -> pd.DataFrame`
- `src.etl.normalize._create_normalized_tables(write_cur)` (internal)
- `src.etl.normalize._select_raw_race(read_cur, chunksize=10000)` (internal)
- `src.etl.normalize._transform_race_df(df)` (internal)
- `src.etl.normalize.run_normalized_etl(read_pool, write_conn, *, tables=["n_race","n_uma_race"]) -> dict`
- `src.etl.raw_fingerprint.compute_raw_fingerprint(read_cur, *, tables=[...], sample_date="20240101") -> dict`
- `src.etl.raw_fingerprint.assert_raw_unchanged(before: dict, after: dict) -> None`

### Database objects created (normalized schema)
- `normalized.n_race` (typed columns: `year int, jyocd varchar(2), kaiji int, nichiji varchar(2), racenum int, kyori integer, futan real, race_date date, race_start_datetime timestamp, jyokencd5 varchar(3), gradecd varchar(1), syubetucd varchar(2), class_code_normalized varchar(3), class_name_normalized text, class_level_numeric smallint, post_2019_class_system_flag boolean, is_grade_race boolean, is_listed boolean, is_open_class boolean, grade_numeric smallint, class_normalization_status varchar(16)`)
- `normalized.n_uma_race` (typed columns incl. `kakuteijyuni integer`, etc.)

### Generated columns (DATA-03 outputs)
- `class_code_normalized` (= jyokencd5 string)
- `class_name_normalized` (e.g. "1勝クラス", "新馬", "OP・重賞")
- `class_level_numeric` (0 for 新馬/未勝利, 1 for 005/1勝クラス, 2 for 010/2勝クラス, 3 for 016/3勝クラス, 4 for 999/OP・重賞)
- `post_2019_class_system_flag` (boolean, race_date >= 2019-06-08)
- `is_grade_race` (gradecd IN A/B/C/D)
- `is_listed` (gradecd = L)
- `is_open_class` (jyokencd5='999' AND gradecd NOT IN A/B/C/D)
- `grade_numeric` (A→1, B→2, C/D→3, else null)
- `class_normalization_status` ("resolved" | "unresolved")

### Test names (DATA-02 / DATA-03 / 成功基準#2 coverage)
- `tests/test_class_normalization.py::test_code_005_spans_reform` (Pitfall 7 核心)
- `tests/test_class_normalization.py::test_reform_date` (D-11 境界)
- `tests/test_class_normalization.py::test_unresolved_unknown_jyokencd5` (D-13)
- `tests/test_normalized_etl.py::test_type_cast_kyori_int` (Pitfall 1)
- `tests/test_normalized_etl.py::test_jra_only_filter` (Pitfall 2)
- `tests/test_normalized_etl.py::test_rowcount_jra_matches_raw`
- `tests/test_raw_immutability.py::test_raw_unchanged_after_etl` (成功基準#2)
- `tests/test_raw_immutability.py::test_raw_role_has_no_update_grant` (D-06)
- `tests/test_raw_immutability.py::test_raw_is_view_in_raw_everydb2_schema`

### New file paths
- `src/etl/normalize.py`, `src/etl/class_normalize.py`, `src/etl/raw_fingerprint.py`
- `scripts/run_normalized_etl.py`
- `tests/test_normalized_etl.py`, `tests/test_class_normalization.py`, `tests/test_raw_immutability.py`

### CLI entry point
- `scripts/run_normalized_etl.py` — reads `Settings`, opens raw-readonly pool + normalized-write connection, calls `run_normalized_etl`, logs raw fingerprint before/after
