---
phase: 01-trust-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - .gitignore
  - .env.example
  - README.md
  - src/__init__.py
  - src/config/__init__.py
  - src/config/settings.py
  - src/config/class_normalization.yaml
  - src/config/code_tables.yaml
  - src/config/feature_availability.yaml
  - src/db/__init__.py
  - src/db/connection.py
  - src/db/schema.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_bootstrap.py
  - scripts/apply_schema.sql
  - scripts/run_apply_schema.py
autonomous: true
requirements: [DATA-03]
user_setup:
  - service: PostgreSQL everydb2
    why: "Phase 1 全成果物がローカル PostgreSQL の everydb2 DB に依存。プロジェクト前提（§3.1）で完了想定。"
    env_vars:
      - name: KEIBA_DB_NAME
        source: "ローカル PostgreSQL 上の everydb2 DB 名"
      - name: KEIBA_DB_USER
        source: "raw 読取専用ロール名（後続 plan 01-03 の test_raw_role_has_no_update_grant がこのロールを public.n_* と raw_everydb2 の両方で検証・REVIEWS HIGH #4）"
      - name: KEIBA_DB_PASSWORD
        source: "当該 raw 読取ロールのパスワード（.env のみ、planning 文書に書かない）"
      - name: KEIBA_DB_HOST
        source: "通常 localhost"
      - name: KEIBA_DB_PORT
        source: "通常 5432"
      - name: KEIBA_ETL_DB_USER
        source: "**normalized 書込ロール名（REVIEWS HIGH #6・plan 01-03 の ETL が使用）**。plan 01-01 Task 3 が KEIBA_ADMIN_DSN で CREATE ROLE し normalized スキーマにのみ INSERT/UPDATE/DELETE/TRUNCATE を GRANT、raw (public.n_*) には SELECT のみ GRANT"
      - name: KEIBA_ETL_DB_PASSWORD
        source: "当該 ETL ロールのパスワード（.env のみ）"
      - name: KEIBA_ADMIN_DSN
        source: "REVOKE/CREATE SCHEMA/CREATE ROLE/GRANT 実行用の管理者接続 DSN（superuser または everydb2 DB の所有者ロール）。Task 3 で必須。未設定の場合は Task 3 が fail する（skip+WARNING は廃止、D-06 二重保護を必須化）"
    dashboard_config:
      - task: "raw 読取専用ロール（例: keiba_reader）を作成し everydb2.public.n_* へ SELECT 権限のみ付与"
        location: "psql で everydb2 DB に接続して CREATE ROLE / GRANT SELECT（plan 01-01 Task 3 が KEIBA_ADMIN_DSN から自動実行する SQL に含まれる）"
      - task: "**物理 public.n_* テーブルと raw_everydb2 VIEW の両方**に対し当該 raw 読取ロールへ UPDATE/DELETE/TRUNCATE を REVOKE する管理者権限を用意（REVIEWS HIGH #4：物理テーブルも保護）"
        location: "KEIBA_ADMIN_DSN で示す管理者ロールが everydb2 DB の objects を所有、または PostgreSQL superuser。plan 01-01 Task 3 が両スキーマ分の REVOKE を含む"

must_haves:
  truths:
    - "uv プロジェクトが Python 3.12 で初期化され uv.lock が commit されている（再現性の前提）"
    - "`uv run python -c 'import psycopg; import pandas; import sklearn; import mlxtend'` が成功する"
    - "`uv run pytest tests/` が psycopg3 readonly fixture で everydb2.public.n_race に接続・SELECT を成功させる"
    - "`src/config/class_normalization.yaml` が実データ検証済みのクラス正規化対応表（jyokencd5×gradecd×2019-06-08 基準日）を verbatim で保持する"
    - "`src/db/schema.py` が5層スキーマ（raw_everydb2/normalized/label/prediction/backtest）作成 DDL と **public.n_* と raw_everydb2 の両方に対する raw REVOKE 文** を保持する（REVIEWS HIGH #4）"
    - "**normalized 書込ロール（KEIBA_ETL_DB_USER）が定義され、normalized スキーマにのみ INSERT/UPDATE 権限を持ち raw(public.n_*) には SELECT のみを持つ**（REVIEWS HIGH #6・plan 01-03 がこのロールで ETL を実行）"
    - "**`Settings.dsn` プロパティは生パスワードを含む DSN を返すがログ出力禁止であり、別途 `Settings.dsn_masked` がパスワードを `***` でマスクした DSN を返す**（REVIEWS MEDIUM: dsn password exposure）"
  artifacts:
    - path: "pyproject.toml"
      provides: "uv プロジェクト設定・requires-python>=3.12,<3.13・Phase 1 dependency pin・pytest/ruff 設定"
      contains: "requires-python"
    - path: "uv.lock"
      provides: "byte 再現性のある依存 lockfile（§19.1）"
      contains: "psycopg"
    - path: ".gitignore"
      provides: ".env/data/models/__pycache__ 除外。reports/ は除外しない（成果物）"
      contains: ".env"
    - path: ".env.example"
      provides: "DB 接続設定雛形（機密値プレースホルダ）"
      contains: "KEIBA_DB_PASSWORD"
    - path: "src/config/settings.py"
      provides: "pydantic-settings BaseSettings・SecretStr で DSN マスク・dsn_masked プロパティ"
      contains: "class Settings"
    - path: "src/config/class_normalization.yaml"
      provides: "DATA-03 の正・実データ検証済みクラス対応表"
      contains: "post_2019_class_system_reform_date"
    - path: "src/db/connection.py"
      provides: "psycopg3 ConnectionPool + readonly_cursor context manager + make_write_pool（normalized 書込用・REVIEWS HIGH #6）"
      contains: "make_pool"
    - path: "src/db/schema.py"
      provides: "5層スキーマ作成 DDL + raw REVOKE UPDATE,DELETE（**public.n_* と raw_everydb2 の両方**・REVIEWS HIGH #4）+ ETL ロール CREATE/GRANT（REVIEWS HIGH #6）"
      contains: "raw_everydb2"
    - path: "tests/conftest.py"
      provides: "settings/pg_pool/readonly_cur session/func fixtures + write_pool fixture（REVIEWS HIGH #6）+ KEIBA_SKIP_DB_TESTS=1 のみ skip の fail-by-default policy"
      contains: "readonly_cur"
  key_links:
    - from: "tests/conftest.py"
      to: "src/db/connection.py"
      via: "make_pool(settings) 呼出"
      pattern: "make_pool\\("
    - from: "src/db/connection.py"
      to: "src/config/settings.py"
      via: "Settings.dsn / Settings.etl_dsn プロパティ参照"
      pattern: "settings\\.(dsn|etl_dsn)"
    - from: "src/config/class_normalization.yaml"
      to: "src/etl/class_normalize.py（後続 plan）"
      via: "YAML 読込 → jyokencd5×gradecd 機械導出"
      pattern: "post_2019_class_system_reform_date"
---

<objective>
Phase 1 の Wave 1 基盤を構築する。uv プロジェクト初期化（Python 3.12 固定）・Phase 1 サブセット依存追加・`.env` ベースの DB 接続設定（pydantic-settings SecretStr）・psycopg3 ConnectionPool・5層 PostgreSQL スキーマ DDL（raw_everydb2 を VIEW として public.n_* を参照、**REVOKE UPDATE,DELETE を public.n_* と raw_everydb2 の両方に発行** で raw read-only）・pytest fixture（readonly_cur / write_pool）・そして **DATA-03 の正である `src/config/class_normalization.yaml`（実データ検証済み対応表 verbatim）** を作成する。

**REVIEWS Cycle-1 修正（本 plan の主要改訂）:**
- **HIGH #4:** raw 不変性証明の対象を `raw_everydb2` VIEW だけでなく **物理 `public.n_*` テーブル**にまで拡張する。後続 plan（01-02/01-03）は `public.n_*` を直接読むため、VIEW の REVOKE だけでは不十分。Task 3 は **両スキーマに対する REVOKE を発行**し、plan 01-03 の権限テストも両方を検証する。
- **HIGH #6:** **normalized 書込ロール**（`KEIBA_ETL_DB_USER` / `KEIBA_ETL_DB_PASSWORD`）とその DSN（`Settings.etl_dsn`）を本 plan で定義する。plan 01-03 の ETL はこのロールで `normalized.*` に書込む。Task 3 が `CREATE ROLE` + `GRANT INSERT,UPDATE,DELETE ON SCHEMA normalized` + raw には SELECT-only を発行する。
- **HIGH #8:** DB-test skip ポリシーを **fail-by-default unless `KEIBA_SKIP_DB_TESTS=1`** に統一（plan 01-02/01-03 もこれに従う）。`.env` 未設定で skip する記述は廃止。
- **MEDIUM (#1 DSN password exposure):** `Settings.dsn` プロパティに加え `Settings.dsn_masked`（パスワードを `***` でマスク）を定義する。ログには `dsn_masked` のみ使用可能。
- **MEDIUM (#2 gradecd F/G/H):** `gradecd_map` で F/G/H を明示的に unresolved 扱いで定義する（silent skip ではなく D-13 unresolved）。
- **MEDIUM (#3 identifier quoting):** `scripts/apply_schema.sql` のロール名は実行時に `psycopg.sql.Identifier` で安全に quote する（SQL injection / 文法エラー回避）。

Purpose: 下流の Wave 2/3 で作る品質ゲート・normalized ETL・utils プリミティブが依存する接続・設定・テスト infra・クラス対応表を先に用意する。Wave 0 Gaps（01-VALIDATION.md）をすべて解消する。D-01 ハイブリッドゲート・D-05 5層スキーマ・D-06 raw read-only 二重保護（**物理テーブル含む**）・D-07 YAML で Git 管理するコード辞書・D-11 2019-06-08 基準日定数・D-13 silent fallback 禁止の土台を置く。
Output: pyproject.toml / uv.lock / .env.example / .gitignore / src/config/{settings,class_normalization,code_tables,feature_availability}.yaml|py / src/db/{connection,schema}.py / tests/conftest.py / scripts/apply_schema.sql / scripts/run_apply_schema.py
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-trust-foundation/01-CONTEXT.md
@.planning/phases/01-trust-foundation/01-RESEARCH.md
@.planning/phases/01-trust-foundation/01-PATTERNS.md
@.planning/phases/01-trust-foundation/01-VALIDATION.md
@.planning/phases/01-trust-foundation/01-REVIEWS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: uv プロジェクト初期化 + Phase 1 依存追加 + pytest/ruff 設定</name>
  <files>pyproject.toml, uv.lock, .gitignore, .env.example, README.md, src/__init__.py, src/config/__init__.py, src/db/__init__.py, tests/__init__.py</files>
  <read_first>
    - CLAUDE.md（Recommended Stack + Leakage-prevention configuration + What NOT to Use）
    - .planning/phases/01-trust-foundation/01-RESEARCH.md（§Standard Stack + §Installation + §Package Legitimacy Audit）
    - .planning/phases/01-trust-foundation/01-PATTERNS.md（`pyproject.toml` / `.gitignore` / `.env.example` の推奨イディオム）
    - .planning/phases/01-trust-foundation/01-VALIDATION.md（§Test Infrastructure / §Wave 0 Requirements）
    - .planning/phases/01-trust-foundation/01-REVIEWS.md（§Cross-Plan Consistency: role mismatch HIGH #6 / DB-test skip policy HIGH #8）
  </read_first>
  <action>
`uv init --package --python 3.12` を実行してプロジェクトを初期化する。次に 01-RESEARCH.md §Installation に厳密に従い、以下のパッケージを `uv add` する:
データ層: `psycopg[binary]==3.3.4` `psycopg-pool==3.3.1` `pandas==3.0.3` `pyarrow==24.0.0` `duckdb==1.5.3`
リーク防止プリミティブ用: `scikit-learn==1.9.0` `mlxtend==0.25.0`（※CLAUDE.md の「mlxtend 4.x」は誤り、PyPI 0.25.0 が正）
設定管理: `pydantic` `pydantic-settings==2.14.1`
Dev: `uv add --dev ruff==0.15.17 pytest==9.1.0`

`pyproject.toml` の `[project]` に `requires-python = ">=3.12,<3.13"` を必ず含める（§17.1 + 01-RESEARCH.md Standard Stack。3.13 は CatBoost wheel ラグで回避）。`[tool.pytest.ini_options]` に `testpaths = ["tests"]` と `addopts = "-ra"` を設定する。`[tool.ruff]` には `line-length = 100`（推奨）と基本 lint 設定を置く。LightGBM/CatBoost/Streamlit/plotly は Phase 1 では追加しない（01-RESEARCH.md Standard Stack 注記）。

`uv.lock` が生成されたら commit 対象とする（§19.1 再現性）。`.gitignore` には `.env`, `data/`, `models/`, `__pycache__/`, `.venv/`, `*.parquet`, `.pytest_cache/`, `.ruff_cache/` を含める。**`reports/` は除外しない**（品質レポートは成果物、01-RESEARCH.md Recommended Project Structure）。

`.env.example` を作成（01-PATTERNS.md `.env.example` 推奨イディオム）。実パスワードは一切書かない（anti-pattern #20）。**REVIEWS HIGH #6 で追加された ETL ロール env vars を含める**。キー: `KEIBA_DB_NAME=everydb2` `KEIBA_DB_USER=<raw readonly role>` `KEIBA_DB_PASSWORD=<your-password-here>` `KEIBA_DB_HOST=localhost` `KEIBA_DB_PORT=5432` `KEIBA_DB_SCHEMA_RAW=public` `KEIBA_DB_SCHEMA_NORMALIZED=normalized` `KEIBA_ETL_DB_USER=<normalized write role>` `KEIBA_ETL_DB_PASSWORD=<your-etl-password-here>` `KEIBA_ADMIN_DSN=<postgresql://admin:pw@localhost/everydb2>`（Task 3 の REVOKE/CREATE ROLE/GRANT 実行用・D-06 二重保護必須化・REVIEWS HIGH #4/#6 により未設定時は Task 3 が fail）。

`src/__init__.py` `src/config/__init__.py` `src/db/__init__.py` `tests/__init__.py` を空（or docstring のみ）で作成してパッケージ化。`README.md` はプロジェクト概要1段落 + `uv sync` / `uv run pytest` の実行方法だけを記載。
  </action>
  <verify>
    <automated>uv sync --frozen &amp;&amp; uv run python -c "import psycopg, pandas, sklearn, mlxtend, pyarrow, duckdb, pydantic_settings; print('deps OK')" &amp;&amp; uv run pytest --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` の `[project]` セクションに `requires-python = ">=3.12,<3.13"` が存在する
    - `pyproject.toml` の `[tool.pytest.ini_options]` に `testpaths = ["tests"]` が存在する
    - `uv.lock` が存在し `psycopg==3.3.4` と `mlxtend==0.25.0` を含む（`grep -E 'name = "(psycopg|mlxtend|pandas|pyarrow|duckdb|scikit-learn|pydantic-settings|ruff|pytest)"' uv.lock` で確認）
    - `.gitignore` が `.env` を含み `reports/` を含まない（`grep -c '^\\.env$' .gitignore` が 1 以上、`grep -c '^reports/' .gitignore` が 0）
    - **`.env.example` が `KEIBA_DB_PASSWORD`, `KEIBA_DB_NAME=everydb2`, `KEIBA_ADMIN_DSN`, および `KEIBA_ETL_DB_USER` と `KEIBA_ETL_DB_PASSWORD` を含む**（REVIEWS HIGH #6・`grep -c 'KEIBA_ETL_DB_USER' .env.example` が 1 以上、`grep -c 'KEIBA_ETL_DB_PASSWORD' .env.example` が 1 以上）
    - `uv run python -c "import psycopg, pandas, sklearn, mlxtend"` が exit 0
    - `uv run pytest --collect-only -q` が `no tests ran` でもエラーなく collector を起動する
  </acceptance_criteria>
  <done>Phase 1 サブセット依存が Python 3.12 venv に install 済み・uv.lock commit 対象化・pytest/ruff 設定配置・.gitignore と .env.example 配置（ETL ロール env vars 含む・REVIEWS HIGH #6）</done>
</task>

<task type="auto">
  <name>Task 2: pydantic-settings による DB 接続設定（raw 読取 + ETL 書込の2ロール・dsn_masked）+ psycopg3 ConnectionPool + tests/conftest.py fixtures</name>
  <files>src/config/settings.py, src/db/connection.py, tests/conftest.py, tests/test_bootstrap.py</files>
  <read_first>
    - 01-RESEARCH.md（§Architecture Patterns / §Don't Hand-Roll / §Security Domain）
    - 01-PATTERNS.md（`src/config/settings.py` / `src/db/connection.py` / `tests/conftest.py` の推奨イディオム）
    - 01-CONTEXT.md（`<decisions>` Claude's Discretion「接続設定管理」）
    - 01-REVIEWS.md（§Cross-Plan Consistency: HIGH #6 role mismatch / HIGH #8 DB-test skip policy / MEDIUM Settings.dsn password exposure）
    - docs/keiba_ai_requirements_v1.3.md §12.1（PostgreSQL が system of record）
  </read_first>
  <action>
`src/config/settings.py` を作成（01-PATTERNS.md `src/config/settings.py` 推奨イディオムをほぼそのまま採用 + REVIEWS HIGH #6/MEDIUM 対応）。pydantic-settings 2.14.1 の `BaseSettings` を継承し `SettingsConfigDict(env_prefix="KEIBA_", env_file=".env", extra="ignore")` を設定。フィールド:
- `db_name: str`, `db_user: str`, `db_password: SecretStr`（pydantic.SecretStr、anti-pattern #20 + ASVS V8）, `db_host: str = "localhost"`, `db_port: int = 5432`, `db_schema_raw: str = "public"`（実 everydb2 配置は public.n_*）, `db_schema_normalized: str = "normalized"`
- **REVIEWS HIGH #6:** `etl_db_user: str`, `etl_db_password: SecretStr`（normalized 書込ロール・plan 01-03 が使用）

プロパティ:
- `@property def dsn(self) -> str:` で `postgresql://{db_user}:{db_password.get_secret_value()}@{db_host}:{db_port}/{db_name}` を返す（**生パスワードを含むためログ出力禁止・docstring に明記**）。raw 読取ロールの DSN。
- `@property def etl_dsn(self) -> str:` で `postgresql://{etl_db_user}:{etl_db_password.get_secret_value()}@{db_host}:{db_port}/{db_name}` を返す。normalized 書込ロールの DSN（REVIEWS HIGH #6）。
- **REVIEWS MEDIUM (#1 DSN password exposure):** `@property def dsn_masked(self) -> str:` でパスワードを `***` に置換した DSN を返す（`postgresql://{db_user}:***@{db_host}:{db_port}/{db_name}`）。同様に `@property def etl_dsn_masked(self) -> str:`。**ログ出力可能なのは masked 版のみ**。docstring に「`dsn` / `etl_dsn` は psycopg 接続専用・ログ出力厳禁。ログには必ず `dsn_masked` / `etl_dsn_masked` を使用」と明記。

`src/db/connection.py` を作成（01-PATTERNS.md `src/db/connection.py` 推奨イディオム + REVIEWS HIGH #6）。以下を実装:
- `from psycopg_pool import ConnectionPool`, `from contextlib import contextmanager`, `from src.config.settings import Settings`
- `def make_pool(settings: Settings, *, role: str = "readonly", min_size: int = 1, max_size: int = 8) -> ConnectionPool` — `role="readonly"` のときは `settings.dsn` で raw 読取ロール、`role="etl"` のときは `settings.etl_dsn` で normalized 書込ロール。`kwargs` で `options=f"-c search_path={settings.db_schema_raw},public"`（readonly）または `-c search_path={settings.db_schema_normalized},public`（etl）を渡す。**legacy psycopg2 は import しない**（CLAUDE.md What NOT to Use）。
- `@contextmanager def readonly_cursor(pool)` — pool.connection() の cursor を yield（読取専用）
- `@contextmanager def write_cursor(pool)` — 同様だが書込用（normalized スキーマ）。plan 01-03 が使用。

`tests/conftest.py` を作成。session scope の `settings()` fixture（`Settings()` を返し `.env` から読込）、session scope の `pg_pool(settings)` fixture（`make_pool(settings, role="readonly")` で raw 読取 pool を作成し teardown で `pool.close()`）、function scope の `readonly_cur(pg_pool)` fixture（cursor を yield）、**`write_pool(settings)` fixture（`make_pool(settings, role="etl")` で normalized 書込 pool・REVIEWS HIGH #6・plan 01-03 が使用）**、`write_cur(write_pool)` fixture。

**`.env` 未設定時の挙動を全 plan で統一（REVIEWS HIGH #8・plan 01-02/01-03 もこれに従う）:**
- `pyproject.toml` の `[tool.pytest.ini_options]` に `markers = ["requires_db: marks tests that need a live PostgreSQL everydb2 connection"]` を登録
- `tests/conftest.py` に以下の autouse フックを置く:
  - `KEIBA_SKIP_DB_TESTS=1` 環境変数が設定されている時のみ `@pytest.mark.requires_db` マーク付きテストを skip（開発時の一時回避用）
  - **それ以外（デフォルト・CI 含む）は `.env` 未設定でも skip ではなく `Settings()` の validation error で fail させる**（テスト環境の設定ミスを明示・REVIEWS HIGH #8 の stricter policy に統一）
- これにより「実 DB 無しで verdict=pass が未検証のまま green になる」quality-degrade リスクを排除
- 実 DB 接続が必要なテスト（`test_readonly_cur_can_select_n_race` や plan 01-02/01-03 の integration テスト）には `@pytest.mark.requires_db` を付ける
- **plan 01-02/01-03 で「`.env` 未設定時 skip 可」という記述は廃止し、本 policy に完全一致させる**

`tests/test_bootstrap.py` を作成し、最小 smoke テストを置く:
- `test_dsn_masks_password`: `settings.dsn_masked` がパスワードを含まない（`***` に置換されている）ことを assert。加えて `repr(settings.dsn)` と `repr(settings.dsn_masked)` をキャプチャログで出力しても**元パスワード文字列は `dsn_masked` 側に出現しない**こと、および `dsn`（生パスワード含む）は絶対にログに書かないこと（明示的に `dsn_masked` のみを logging.info の引数に使うサンプルコードも同テスト内に示す）を検証。実パスワードをテストコードに埋め込まない。
- `test_etl_dsn_uses_etl_role`: `settings.etl_dsn` が `etl_db_user` を user 部に含むこと（`KEIBA_ETL_DB_USER` env var と一致）を assert（REVIEWS HIGH #6）
- `test_readonly_cur_can_select_n_race`: `readonly_cur` で `SELECT count(*) FROM n_race WHERE jyocd BETWEEN '01' AND '10'` を実行し JRA レース件数（>0）が返ることを確認（Pitfall 2: jyocd で JRA 限定）。`@pytest.mark.requires_db`。
  </action>
  <verify>
    <automated>uv run pytest tests/test_bootstrap.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `src/config/settings.py` が `class Settings(BaseSettings):` と `db_password: SecretStr` を含む（`grep -c 'SecretStr' src/config/settings.py` が 2 以上：db + etl）
    - `src/config/settings.py` に `def dsn`, `def etl_dsn`, `def dsn_masked`, `def etl_dsn_masked` の4プロパティが存在する（`grep -cE 'def (dsn|etl_dsn|dsn_masked|etl_dsn_masked)' src/config/settings.py` が 4）
    - `src/config/settings.py` が `etl_db_user` と `etl_db_password` フィールドを含む（REVIEWS HIGH #6）
    - `src/db/connection.py` が `from psycopg_pool import ConnectionPool` と `def make_pool` と `def readonly_cursor` と `def write_cursor` を含む（`grep -cE 'def (make_pool|readonly_cursor|write_cursor)' src/db/connection.py` が 3）
    - `src/db/connection.py` が `role` パラメータを含み `"readonly"` と `"etl"` の分岐を持つ（`grep -c 'role' src/db/connection.py` が 1 以上）
    - `src/db/connection.py` が `psycopg2` を含まない（`grep -c 'psycopg2' src/db/connection.py` が 0）
    - `tests/conftest.py` が `pg_pool` と `readonly_cur` と `write_pool` と `write_cur` の4 fixture を含む
    - `tests/conftest.py` が `KEIBA_SKIP_DB_TESTS` を含み `.env` 未設定時は skip せず fail する policy を持つ（REVIEWS HIGH #8）
    - `uv run pytest tests/test_bootstrap.py -v` が `.env` 設定済み環境で `test_readonly_cur_can_select_n_race` を含め green
    - `test_dsn_masks_password` が `dsn_masked` から生パスワードが除去されていることを assert する
    - `test_etl_dsn_uses_etl_role` が `settings.etl_dsn` に `KEIBA_ETL_DB_USER` が含まれることを assert する
  </acceptance_criteria>
  <done>pydantic-settings が .env を読込・SecretStr でマスク・dsn_masked でログ安全・psycopg3 pool 経由で everydb2.public.n_race に readonly 接続 + ETL 書込ロール接続が可能（REVIEWS HIGH #6 + MEDIUM DSN password exposure 解消）</done>
</task>

<task type="auto">
  <name>Task 3: 5層 PostgreSQL スキーマ DDL + **物理 public.n_* と raw_everydb2 の両方に REVOKE** + **ETL ロール CREATE/GRANT**（REVIEWS HIGH #4/#6）+ 適用スクリプト（psycopg.sql.Identifier で安全 quoting・MEDIUM #3）</name>
  <files>src/db/schema.py, scripts/apply_schema.sql, scripts/run_apply_schema.py</files>
  <read_first>
    - 01-RESEARCH.md（§Architecture Patterns / §Pattern 2: raw read-only 二重保護）
    - 01-PATTERNS.md（`src/db/schema.py` 推奨イディオム）
    - 01-CONTEXT.md（D-05 5層スキーマ、D-06 二重保護）
    - 01-REVIEWS.md（§Plan 01-01: HIGH #4 raw access mismatch / HIGH #6 role mismatch / §Cross-Plan Consistency MEDIUM identifier quoting）
    - docs/keiba_ai_requirements_v1.3.md §12.2（論理層5層）
  </read_first>
  <action>
`src/db/schema.py` を作成（01-PATTERNS.md `src/db/schema.py` 推奨イディオム + REVIEWS HIGH #4/#6 拡張）。モジュール定数として `SCHEMAS = ["raw_everydb2", "normalized", "label", "prediction", "backtest"]` を定義（D-05 / §12.2 論理層に1:1）。

`raw_everydb2` は物理的には `public.n_*`（確定テーブル、01-RESEARCH.md Sources で実測: スキーマは public のみ・n_* が system of record）への **VIEW** として作成（UPDATE/DELETE 不可）。主要5系統（01-CONTEXT.md D-02）の VIEW 文字列を含める: `CREATE OR REPLACE VIEW raw_everydb2.n_race AS SELECT * FROM public.n_race;` および `n_uma_race`, `n_harai`, `n_hyosu`, `n_odds_tanpuku`, `n_odds_fukusho`。

**REVIEWS HIGH #4 修正（必須）:** D-06 二重保護のため、`KEIBA_DB_USER`（raw 読取ロール）に対して **物理 `public.n_*` と `raw_everydb2` VIEW の両方**に REVOKE を発行する。両方を保護しないと物理テーブルに直接書込める経路が残る（後続 plan は `public.n_*` を直接読むため）。
- `REVOKE UPDATE, DELETE, TRUNCATE ON ALL TABLES IN SCHEMA public FROM <KEIBA_DB_USER>;`（**物理層・HIGH #4 で追加**）
- `REVOKE UPDATE, DELETE, TRUNCATE ON ALL TABLES IN SCHEMA raw_everydb2 FROM <KEIBA_DB_USER>;`（VIEW 層）
- `ALTER DEFAULT PRIVILEGES IN SCHEMA public REVOKE UPDATE, DELETE, TRUNCATE FROM <KEIBA_DB_USER>;`
- `ALTER DEFAULT PRIVILEGES IN SCHEMA raw_everydb2 REVOKE UPDATE, DELETE, TRUNCATE FROM <KEIBA_DB_USER>;`
- これらは plan 01-03 Task 3 の `test_raw_role_has_no_update_grant` が `public` と `raw_everydb2` の両方のスキーマで UPDATE/DELETE/TRUNCATE 権限が無いことを検証する対象と完全一致（HIGH #4 解消）。

**REVIEWS HIGH #6 修正（必須）:** normalized 書込ロール `KEIBA_ETL_DB_USER` を CREATE し、`normalized` スキーマにのみ INSERT/UPDATE/DELETE/TRUNCATE を GRANT、`public.n_*` と `raw_everydb2` には SELECT-only を GRANT。
- `CREATE ROLE <KEIBA_ETL_DB_USER> WITH LOGIN PASSWORD '<from env>';`（パスワードは `KEIBA_ADMIN_DSN` で実行時に `Settings.etl_db_password` を注入・SQL に埋め込まないため `psycopg.sql.Identifier` + `psycopg.sql.SQL` で組み立て・MEDIUM #3）
- `GRANT USAGE ON SCHEMA normalized TO <KEIBA_ETL_DB_USER>;`
- `GRANT SELECT, INSERT, UPDATE, DELETE, TRUNCATE ON ALL TABLES IN SCHEMA normalized TO <KEIBA_ETL_DB_USER>;`
- `GRANT SELECT ON ALL TABLES IN SCHEMA public TO <KEIBA_ETL_DB_USER>;`（raw は読取のみ）
- `GRANT SELECT ON ALL TABLES IN SCHEMA raw_everydb2 TO <KEIBA_ETL_DB_USER>;`
- `REVOKE UPDATE, DELETE, TRUNCATE ON ALL TABLES IN SCHEMA public FROM <KEIBA_ETL_DB_USER>;`（念のため ETL ロールにも raw への書込を禁止）
- `REVOKE UPDATE, DELETE, TRUNCATE ON ALL TABLES IN SCHEMA raw_everydb2 FROM <KEIBA_ETL_DB_USER>;`

normalized/label/prediction/backtest は空 SCHEMA のみ作成（テーブル定義は後続 plan で normalized のみ作成、他は後続 Phase）。

`scripts/apply_schema.sql` に idempotent な SQL（`CREATE SCHEMA IF NOT EXISTS`, `CREATE OR REPLACE VIEW`, `REVOKE`）をまとめる。**ロール名 `<KEIBA_DB_USER>` と `<KEIBA_ETL_DB_USER>` はプレースホルダとし、実行時に `scripts/run_apply_schema.py` が `psycopg.sql.Identifier` で安全に置換する**（MEDIUM #3・SQL injection / identifier 文法エラー回避）。パスワードは SQL ファイルに書かず `run_apply_schema.py` が `ALTER ROLE <etl_role> WITH PASSWORD ...` をパラメータ化実行（`cursor.execute("ALTER ROLE %s WITH PASSWORD %s", ...)` ではなく psycopg3 では `Identifier` と `Literal` を組み立てて実行）。

`scripts/run_apply_schema.py` を `argparse` で `--sql-file`（デフォルト `scripts/apply_schema.sql`）・`--dry-run` をサポートするエントリポイントとして作成。**管理者接続の取り扱い（D-06 必須化）:** CREATE ROLE / GRANT / REVOKE は所有者/superuser 権限が必要なため、`scripts/run_apply_schema.py` は `KEIBA_ADMIN_DSN` 環境変数から接続する。`KEIBA_ADMIN_DSN` 未設定時は **skip + WARNING ではなく即座に `RuntimeError("KEIBA_ADMIN_DSN is required to apply REVOKE + CREATE ROLE + GRANT; D-06 raw read-only protection is mandatory (REVIEWS HIGH #4/#6)")` で fail する**。また `KEIBA_DB_USER` または `KEIBA_ETL_DB_USER` が未設定の場合も同様に `RuntimeError` で fail。`psycopg.sql.Identifier` を用いてロール名を安全に SQL に埋め込む。正常時は CREATE SCHEMA → CREATE ROLE → CREATE VIEW → GRANT → REVOKE の順に SQL を順次実行し、各ステップの結果を INFO ログ出力する（DSN は必ず `dsn_masked` 形式で出力）。
  </action>
  <verify>
    <automated>uv run python scripts/run_apply_schema.py --dry-run 2>&amp;1 | grep -E '(CREATE SCHEMA|CREATE OR REPLACE VIEW raw_everydb2|REVOKE UPDATE|CREATE ROLE|GRANT INSERT)' &amp;&amp; grep -c 'raw_everydb2' src/db/schema.py | awk '$1 &gt;= 5 {exit 0} {exit 1}' &amp;&amp; grep -cE 'SCHEMA public FROM|SCHEMA public REVOKE' src/db/schema.py | awk '$1 &gt;= 2 {exit 0} {exit 1}' &amp;&amp; test -n "$KEIBA_ADMIN_DSN" &amp;&amp; uv run python scripts/run_apply_schema.py &amp;&amp; psql "$KEIBA_ADMIN_DSN" -Atc "SELECT count(*) FROM information_schema.role_table_grants WHERE table_schema IN ('public','raw_everydb2') AND grantee = current_setting('role') AND privilege_type IN ('UPDATE','DELETE','TRUNCATE')" | awk '$1==0{exit 0}{exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/schema.py` が `SCHEMAS = ["raw_everydb2", "normalized", "label", "prediction", "backtest"]` を含む（`grep -c 'raw_everydb2' src/db/schema.py` が 5 以上）
    - `src/db/schema.py` が `CREATE OR REPLACE VIEW raw_everydb2.n_race AS SELECT * FROM public.n_race` 文を含む
    - **HIGH #4 直接検証:** `src/db/schema.py` が `REVOKE UPDATE, DELETE, TRUNCATE ON ALL TABLES IN SCHEMA public` と `REVOKE UPDATE, DELETE, TRUNCATE ON ALL TABLES IN SCHEMA raw_everydb2` の **両方** を含む（`grep -c 'SCHEMA public' src/db/schema.py` が 2 以上 かつ `grep -c 'SCHEMA raw_everydb2' src/db/schema.py` が 2 以上）
    - **HIGH #6 直接検証:** `src/db/schema.py` が `KEIBA_ETL_DB_USER` または ETL ロール用 `CREATE ROLE` と `GRANT INSERT`（normalized スキーマ）を含む（`grep -c 'CREATE ROLE' src/db/schema.py` が 1 以上、`grep -c 'GRANT.*INSERT.*normalized' src/db/schema.py` が 1 以上）
    - `scripts/apply_schema.sql` が `CREATE SCHEMA IF NOT EXISTS` を5層分（raw_everydb2/normalized/label/prediction/backtest）含む（`grep -c 'CREATE SCHEMA IF NOT EXISTS' scripts/apply_schema.sql` が 5）
    - `scripts/run_apply_schema.py` が `argparse` と `KEIBA_ADMIN_DSN` と `KEIBA_DB_USER` と `KEIBA_ETL_DB_USER` 参照を含み、未設定時に `RuntimeError` で fail する（skip+WARNING を返さない、D-06 必須化）
    - **MEDIUM #3 直接検証:** `scripts/run_apply_schema.py` が `psycopg.sql.Identifier` または `from psycopg import sql` を含む（ロール名 quoting）
    - `KEIBA_ADMIN_DSN` + `KEIBA_DB_USER` + `KEIBA_ETL_DB_USER` 設定環境で `uv run python scripts/run_apply_schema.py` が実行され、`information_schema.role_table_grants` で `public` と `raw_everydb2` の**両方**のスキーマ上の `KEIBA_DB_USER` に UPDATE/DELETE/TRUNCATE 権限が無いことが確認できる（HIGH #4・plan 01-03 Task 3 の `test_raw_role_has_no_update_grant` と同一対象）
  </acceptance_criteria>
  <done>5層スキーマ DDL と raw_everydb2 VIEW + **物理 public.n_* と VIEW の両方への REVOKE**（HIGH #4）+ **ETL ロール CREATE/GRANT**（HIGH #6）+ psycopg.sql.Identifier による安全 quoting（MEDIUM #3）が idempotent SQL として配置・管理者接続で実際に適用され KEIBA_DB_USER に UPDATE/DELETE/TRUNCATE 権限が無いことが DB カタログで証明される</done>
</task>

<task type="auto">
  <name>Task 4: DATA-03 の正となる class_normalization.yaml verbatim 転記（gradecd F/G/H も明示 unresolved で追加・MEDIUM #2）+ code_tables.yaml + feature_availability.yaml 枠</name>
  <files>src/config/class_normalization.yaml, src/config/code_tables.yaml, src/config/feature_availability.yaml</files>
  <read_first>
    - 01-RESEARCH.md（§クラス正規化完全対応表（実データ検証済み）— 1. jyokencd5 コード値表 / 2. 2019-06-08 基準日 / 3. gradecd 導出 / 4. 未知コード D-13）
    - 01-CONTEXT.md（D-07 Git 管理コード辞書 / D-09 / D-10 / D-11 / D-12 / D-13）
    - 01-PATTERNS.md（`src/config/class_normalization.yaml` / `code_tables.yaml` / `feature_availability.yaml` 推奨イディオム）
    - 01-REVIEWS.md（§Cross-Plan Consistency MEDIUM: gradecd F/G/H inconsistency）
    - docs/keiba_ai_requirements_v1.3.md §12.3（クラス正規化骨格）§13.3（feature_availability 項目）
  </read_first>
  <action>
`src/config/class_normalization.yaml` を作成。**01-RESEARCH.md §クラス正規化完全対応表を実データ検証値のまま1行も省略せず verbatim で転記する（これが DATA-03 の正、D-09/D-11/D-12/D-13 の解決）。** 内容:
1. `post_2019_class_system_reform_date: "2019-06-08"`（D-11 実測確認・夏季競馬開催初日・JRA-VAN 通知 2018-12-05 は発表日で適用日ではない）
2. `jyokencd5_map`（JRA 限定6値、01-RESEARCH.md 表の通り）:
   - `"701": {class_level_numeric: 0, class_name_normalized: "新馬", note: "§7.3 モデル対象外だが保存"}`
   - `"703": {class_level_numeric: 0, class_name_normalized: "未勝利", note: "§7.3 モデル対象外だが保存"}`
   - `"005": {class_level_numeric: 1, class_name_normalized: "1勝クラス", note: "2019年前: 500万下"}`（**Pitfall 7 核心: 制度改革を跨いで class_level_numeric=1 連続**）
   - `"010": {class_level_numeric: 2, class_name_normalized: "2勝クラス", note: "2019年前: 1000万下"}`
   - `"016": {class_level_numeric: 3, class_name_normalized: "3勝クラス", note: "2019年前: 1600万下"}`
   - `"999": {class_level_numeric: 4, class_name_normalized: "OP・重賞", note: "gradecd で細分"}`
3. `gradecd_map`（実測10値、01-RESEARCH.md 表の通り、`is_grade_race`/`is_listed`/`is_open_class` を gradecd 単独から機械導出・名称マッチ不使用 D-12）:
   - `"A": {grade_numeric: 1, is_grade_race: true,  is_listed: false}`
   - `"B": {grade_numeric: 2, is_grade_race: true,  is_listed: false}`
   - `"C": {grade_numeric: 3, is_grade_race: true,  is_listed: false}`
   - `"D": {grade_numeric: 3, is_grade_race: true,  is_listed: false, note: "A1: 障害G3可能性。ETLでsyubetucd交差確認推奨"}`
   - `"L": {grade_numeric: null, is_grade_race: false, is_listed: true}`
   - `"E": {grade_numeric: null, is_grade_race: false, is_listed: false, is_open_class: true}`（jyokencd5='999' のみ）
   - `"":  {grade_numeric: null, is_grade_race: false, is_listed: false, is_open_class: false}`（一般条件戦）
   - **REVIEWS MEDIUM #2 修正:** `"F": {grade_numeric: null, is_grade_race: false, is_listed: false, is_open_class: false, class_normalization_status: "unresolved", note: "障害等の希少コード・01-RESEARCH.md で未検証・D-13 unresolved で明示格納"}`, `"G": {...同様に unresolved...}`, `"H": {...同様に unresolved...}`。silent skip ではなく明示的にキーを置き、plan 01-03 の class_normalize が `class_normalization_status="unresolved"` を返すよう value を記述する。これにより未知の F/G/H が「未記載」ではなく「unresolved 扱いで定義済み」となり YAML の一貫性が保たれる。
4. `jra_jyocd_range: ["01", "10"]`（Pitfall 2: JRA 10場限定。NAR jyocd>=30 は品質チェック/クラス正規化の対象外）
5. `unresolved_strategy: "error_and_isolate"`（D-13 silent fallback 禁止・該当レースは `class_normalization_status='unresolved'` で記録・隔離）
6. `jra_only_filter: "jyocd BETWEEN '01' AND '10'"`（全クエリで共有する JRA 限定 WHERE 句）

`src/config/code_tables.yaml` を作成（Pitfall 3: CODE.md は不完全、実データ SELECT DISTINCT のみ正）。track code（jyocd 01-10: 01札幌/02函館/03福島/04新潟/05東京/06中山/07中京/08阪神/09京都/10小倉）・`syubetucd`（障害=18/19 は §7.3 除外対象）の補助コード表を置く。planner が 01-RESEARCH.md の検証済み値のみを記載する（CODE.md からは引用しない）。

`src/config/feature_availability.yaml` を作成（D-15: Phase 1 は枠+項目定義のみ・本格運用と allowlist test は Phase 3）。§13.3 の6項目スキーマ定義を必ず含める: `feature_name` / `feature_group` / `available_from_timing` / `source_table` / `cutoff_rule` / `leakage_risk_level`。`features: []`（空リスト）+ `schema_version: "0.1.0"` + `feature_schema:` ブロック（上記6キーをキーとして値は型/説明文字列、Phase 3 で各 feature 行が従う contract）+ Phase 3 用のコメントアウト例1つ（`jockey_win_rate_1y` 等、6キー全てを埋めた例）。これにより成功基準#4 が要求する registry bootstrap が「枠のみ」ではなく「6項目スキーマ定義付き」で達成される。
  </action>
  <verify>
    <automated>uv run python -c "import yaml; d=yaml.safe_load(open('src/config/class_normalization.yaml')); assert d['post_2019_class_system_reform_date']=='2019-06-08'; assert set(d['jyokencd5_map'])=={'701','703','005','010','016','999'}; assert d['jyokencd5_map']['005']['class_level_numeric']==1; assert d['gradecd_map']['A']['is_grade_race'] is True; assert d['gradecd_map']['F']['class_normalization_status']=='unresolved'; assert d['gradecd_map']['G']['class_normalization_status']=='unresolved'; assert d['gradecd_map']['H']['class_normalization_status']=='unresolved'; assert d['unresolved_strategy']=='error_and_isolate'; print('class_normalization.yaml OK (incl. F/G/H unresolved)')" &amp;&amp; uv run python -c "import yaml; d=yaml.safe_load(open('src/config/feature_availability.yaml')); assert d['features']==[]; assert 'schema_version' in d; required={'feature_name','feature_group','available_from_timing','source_table','cutoff_rule','leakage_risk_level'}; assert required <= set(d['feature_schema']), f'missing keys: {required - set(d[\"feature_schema\"])}'; print('feature_availability.yaml OK (6-key schema present)')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/config/class_normalization.yaml` が `post_2019_class_system_reform_date: "2019-06-08"` を含む
    - `src/config/class_normalization.yaml` の `jyokencd5_map` が 701/703/005/010/016/999 の6キーを全て含む（`grep -cE '"?(701|703|005|010|016|999)"?:' src/config/class_normalization.yaml` が 6）
    - `src/config/class_normalization.yaml` の `jyokencd5_map` で `"005"` の `class_level_numeric` が 1（**Pitfall 7 核心**）
    - `src/config/class_normalization.yaml` の `gradecd_map` が A/B/C/D/L/E と空文字キーを含む
    - **MEDIUM #2 直接検証:** `src/config/class_normalization.yaml` の `gradecd_map` が F/G/H を明示的に含み、それぞれ `class_normalization_status: "unresolved"` を持つ（`grep -cE '"[FGH]".*unresolved|class_normalization_status.*unresolved' src/config/class_normalization.yaml` が 3 以上）
    - `src/config/class_normalization.yaml` が `unresolved_strategy: "error_and_isolate"` を含む（D-13）
    - `src/config/class_normalization.yaml` が `jra_only_filter` または `jra_jyocd_range` を含む（Pitfall 2）
    - `src/config/feature_availability.yaml` が `features: []` と `schema_version` を含む
    - `src/config/feature_availability.yaml` が `feature_schema:` ブロックを含み、§13.3 の6キー（feature_name/feature_group/available_from_timing/source_table/cutoff_rule/leakage_risk_level）が全て存在する（成功基準#4 + D-15 registry bootstrap）
    - `src/config/code_tables.yaml` が `jyocd` と `syubetucd` の両方を含む
    - 上記 `<verify>` の python yaml assert が全て exit 0
  </acceptance_criteria>
  <done>DATA-03 の正である class_normalization.yaml が実データ検証済み対応表を verbatim 保持（F/G/H も unresolved で明示・MEDIUM #2）・code_tables.yaml と feature_availability.yaml 枠が配置</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env` ファイル → プロセス環境変数 | DB 認証情報（パスワード）がプロセスに渡る境界。`.env` は gitignore、コード内にハードコードしない（anti-pattern #20、ASVS V8） |
| readonly ロール → everydb2.public.n_*（物理）および raw_everydb2.*（VIEW） | raw データへの読取専用アクセス境界。**両スキーマで** UPDATE/DELETE/TRUNCATE が REVOKE される（REVIEWS HIGH #4・D-06・ASVS V4） |
| ETL ロール → normalized.* | normalized スキーマへの書込境界。raw には SELECT-only（REVIEWS HIGH #6・ASVS V4） |
| プロセス → PostgreSQL | psycopg3 接続。DSN に生パスワードを含むため `dsn_masked` のみログ出力可能（REVIEWS MEDIUM #1・ASVS V8 Information Disclosure） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `src/config/settings.py` の DSN 文字列・ログ出力（REVIEWS MEDIUM #1） | mitigate | pydantic `SecretStr` で `db_password` と `etl_db_password` を保持し `repr()`/`str()` でマスク。**`Settings.dsn` / `Settings.etl_dsn` は生パスワードを含むためログ出力厳禁・`Settings.dsn_masked` / `Settings.etl_dsn_masked`（パスワードを `***` でマスク）のみログ可能**。`test_dsn_masks_password` で `dsn_masked` から生パスワードが出現しないことを assert（anti-pattern #20、ASVS V8） |
| T-01-02 | Tampering | ETL ロール経由の物理 raw テーブル（public.n_*）誤更新（REVIEWS HIGH #4） | mitigate | `scripts/apply_schema.sql` で **物理 `public.n_*` と `raw_everydb2` VIEW の両方** に `REVOKE UPDATE, DELETE, TRUNCATE` を `KEIBA_DB_USER` と `KEIBA_ETL_DB_USER` の両方に対して発行。さらに raw_everydb2 を物理テーブルではなく VIEW として定義（構造的に UPDATE 不可、ASVS V4）。plan 01-03 Task 3 の `test_raw_role_has_no_update_grant` が両スキーマ×両ロールを検証 |
| T-01-03 | Tampering | コード辞書（class_normalization.yaml）の silent fallback で未知コードを隠蔽 | mitigate | `unresolved_strategy: "error_and_isolate"` を定数として YAML に明示。後続 plan 03 で class_normalize が未知コードを `class_normalization_status='unresolved'` として隔離し WARNING ログを出力（D-13、ASVS V7）。F/G/H も明示 unresolved（MEDIUM #2） |
| T-01-04 | Information Disclosure | notebook/コード内に DB 認証情報埋め込み | accept | local-only 単一ユーザ環境（§19.3）。`.env` + `SecretStr` + `.gitignore` + `dsn_masked` で十分。CI secret injection は Phase 8 で評価（low risk） |
| T-01-05 | Tampering | ETL ロール未定義で normalized 書込が raw 読取ロールの権限で実行される（REVIEWS HIGH #6） | mitigate | `KEIBA_ETL_DB_USER` / `KEIBA_ETL_DB_PASSWORD` を定義し、normalized スキーマにのみ INSERT/UPDATE/DELETE を GRANT、raw には SELECT-only を GRANT。`Settings.etl_dsn` と `make_pool(role="etl")` で書込 pool を分離。`test_etl_dsn_uses_etl_role` が検証 |
| T-01-06 | Tampering | `apply_schema.sql` にロール名を直接 string-substitute して SQL injection / 文法エラー（MEDIUM #3） | mitigate | `scripts/run_apply_schema.py` が `psycopg.sql.Identifier` でロール名を安全に quote。SQL ファイルにロール名を埋め込まず実行時に置換。acceptance_criteria で `psycopg.sql.Identifier` 使用を検証 |
| T-01-SC | Tampering | uv 依存インストール時の供給チェーン | mitigate | 01-RESEARCH.md §Package Legitimacy Audit で全 package を PyPI version + GitHub repo で検証済み。全 `[SUS]` は seam の `unknown-downloads` 誤陽性で実リスクなし・`[SLOP]` 0件。`uv.lock` commit で version 固定（§19.1） |
</threat_model>

<verification>
- `uv sync --frozen` が成功（lockfile 整合）
- `uv run python -c "import psycopg, pandas, sklearn, mlxtend"` が成功
- `uv run pytest tests/test_bootstrap.py -v` が green（`.env` 設定済み環境）
- `uv run python -c "import yaml; ..."` の class_normalization.yaml assert が全て exit 0（F/G/H unresolved 含む）
- `scripts/run_apply_schema.py --dry-run` が raw_everydb2 と REVOKE（public と raw_everydb2 の両方）と CREATE ROLE と GRANT を含む SQL を出力
- `KEIBA_ADMIN_DSN` + `KEIBA_DB_USER` + `KEIBA_ETL_DB_USER` 設定環境で `scripts/run_apply_schema.py` が実行され、`public` と `raw_everydb2` の両方で `KEIBA_DB_USER` に UPDATE/DELETE/TRUNCATE 権限が無いことが DB カタログで確認できる
</verification>

<success_criteria>
- Python 3.12 venv + Phase 1 dependency pin が再現可能（uv.lock commit）
- pydantic-settings SecretStr で DB 認証情報がマスクされ、`dsn_masked` のみログ出力可能（MEDIUM #1）
- psycopg3 readonly fixture で everydb2.public.n_race に接続・SELECT 成功
- 5層スキーマ DDL + raw_everydb2 VIEW + **物理 public.n_* と VIEW の両方への REVOKE**（HIGH #4）+ **ETL ロール CREATE/GRANT**（HIGH #6）が idempotent SQL で配置
- psycopg.sql.Identifier でロール名が安全に quote される（MEDIUM #3）
- class_normalization.yaml が DATA-03 の正として実データ検証済み対応表を verbatim 保持（F/G/H も unresolved で明示・MEDIUM #2）
- DB-test skip policy が fail-by-default unless `KEIBA_SKIP_DB_TESTS=1` に統一（HIGH #8・plan 01-02/01-03 も従う）
- Wave 0 Gaps（01-VALIDATION.md）が全て解消
</success_criteria>

<output>
Create `.planning/phases/01-trust-foundation/01-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

### Functions / Classes
- `src.config.settings.Settings` (pydantic BaseSettings) — `db_password: SecretStr`, `etl_db_user: str`, `etl_db_password: SecretStr`, `@property def dsn() -> str`, `@property def etl_dsn() -> str`, `@property def dsn_masked() -> str`, `@property def etl_dsn_masked() -> str`
- `src.db.connection.make_pool(settings: Settings, *, role: str = "readonly", min_size: int = 1, max_size: int = 8) -> psycopg_pool.ConnectionPool` — `role="readonly"` は raw 読取、`role="etl"` は normalized 書込（HIGH #6）
- `src.db.connection.readonly_cursor(pool)` — contextmanager yielding `psycopg.Cursor`（raw 読取専用）
- `src.db.connection.write_cursor(pool)` — contextmanager yielding `psycopg.Cursor`（normalized 書込用・HIGH #6）

### Module-level constants
- `src.db.schema.SCHEMAS = ["raw_everydb2", "normalized", "label", "prediction", "backtest"]`
- `src.db.schema.CREATE_RAW_VIEW` (SQL str)
- `src.db.schema.REVOKE_RAW_WRITES_PUBLIC` (SQL str — **物理 public.n_* 向け・HIGH #4**)
- `src.db.schema.REVOKE_RAW_WRITES_VIEW` (SQL str — raw_everydb2 VIEW 向け)
- `src.db.schema.CREATE_ETL_ROLE` (SQL str — HIGH #6)
- `src.db.schema.GRANT_ETL_NORMALIZED` (SQL str — HIGH #6)

### Config YAML keys
- `src/config/class_normalization.yaml`: `post_2019_class_system_reform_date` (= "2019-06-08"), `jyokencd5_map` (6 keys: 701/703/005/010/016/999), `gradecd_map` (A/B/C/D/L/E/"" および **F/G/H unresolved・MEDIUM #2**), `unresolved_strategy` (= "error_and_isolate"), `jra_only_filter`, `jra_jyocd_range`
- `src/config/feature_availability.yaml`: `features: []`, `schema_version: "0.1.0"`
- `src/config/code_tables.yaml`: `jyocd`, `syubetucd` mappings

### New file paths
- `pyproject.toml`, `uv.lock`, `.gitignore`, `.env.example`, `README.md`
- `src/__init__.py`, `src/config/__init__.py`, `src/db/__init__.py`
- `src/config/settings.py`, `src/db/connection.py`, `src/db/schema.py`
- `src/config/class_normalization.yaml`, `src/config/code_tables.yaml`, `src/config/feature_availability.yaml`
- `tests/__init__.py`, `tests/conftest.py`, `tests/test_bootstrap.py`
- `scripts/apply_schema.sql`, `scripts/run_apply_schema.py`

### Env vars (consumed by Settings)
- `KEIBA_DB_NAME`, `KEIBA_DB_USER`, `KEIBA_DB_PASSWORD`, `KEIBA_DB_HOST`, `KEIBA_DB_PORT`, `KEIBA_DB_SCHEMA_RAW`, `KEIBA_DB_SCHEMA_NORMALIZED`
- **`KEIBA_ETL_DB_USER`, `KEIBA_ETL_DB_PASSWORD`** (HIGH #6)
- `KEIBA_ADMIN_DSN`
- `KEIBA_SKIP_DB_TESTS` (= "1" のみ skip・HIGH #8)

### PostgreSQL schema names (DDL constants, applied later)
- `raw_everydb2` (VIEW layer over public.n_*, read-only)
- `normalized` (ETL ロールが INSERT/UPDATE/DELETE 可能・HIGH #6)
- `label`, `prediction`, `backtest` (empty schemas)

### PostgreSQL roles (created by Task 3)
- `<KEIBA_DB_USER>` — raw 読取専用（public.n_* と raw_everydb2 の両方で SELECT-only・HIGH #4）
- `<KEIBA_ETL_DB_USER>` — normalized 書込 + raw 読取（HIGH #6）

### pytest fixtures (tests/conftest.py)
- `settings()` (session), `pg_pool()` (session, role="readonly"), `readonly_cur()` (function)
- **`write_pool()` (session, role="etl"), `write_cur()` (function)** — HIGH #6・plan 01-03 が使用
