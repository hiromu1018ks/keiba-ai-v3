---
phase: 08-adversarial-audit-suite
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - scripts/run_reproducibility_smoke.py
  - src/audit/__init__.py
  - src/audit/report.py
  - reports/08-audit.md
  - reports/08-audit.json
autonomous: true
requirements:
  - TEST-01
user_setup: []

must_haves:
  truths:
    - "SC#3: scripts/run_reproducibility_smoke.py が合成的 bit-identical pytest 群（SC#4 calibrator / tests/audit/）を束ねて固定 seed 再現を確認し GREEN を返す（live-DB 必須 CLI は 08-03 checkpoint で証明・08-02 は DB 不要 pytest のみで SC#3 の合成層を担う）"
    - "D-03: run_reproducibility_smoke.py は新規フルパイプライン runner を作らず既存 pytest（test_reproduce_bit_identical / tests/audit/）を subprocess で束ねる薄い orchestrator（live-DB 必須の run_train_predict --check-reproduce / run_backtest --check-reproduce は 08-03 が実行・08-02 frontmatter は autonomous: true / user_setup: [] を維持）"
    - "D-01: reports/08-audit.{md,json} が生成され・サーフェス別カバレッジマップ（SC#1 #1-#8 + 評価指標計算）・SC#1/#2/#3 対応表・Known Limitations を含む"
    - "D-05: reports/08-audit の Known Limitations セクションに回収率~0.65 天井・Phase 5 odds JODDS再検証 subject・Phase 4 SC#2 Calibration BL 劣位が隠さず明示される"
    - "reports/08-audit.json は sort_keys=True・ensure_ascii=False で byte-reproducible（src/ev/report.py DRY パターン再利用）"
    - "reports/08-audit.md の列ヘッダと reports/08-audit.json のキーが AUDIT_SURFACE_COLUMNS 定数で 1:1（presence assert・LOW-05 analog）"
  artifacts:
    - path: "scripts/run_reproducibility_smoke.py"
      provides: "SC#3 再現性スモーク orchestrator（main(argv) -> int・既存 CLI/pytest を subprocess で束ねる）"
      contains: "run_reproducibility_smoke"
      min_lines: 80
    - path: "src/audit/__init__.py"
      provides: "src.audit パッケージマーカー"
      min_lines: 1
    - path: "src/audit/report.py"
      provides: "reports/08-audit.{md,json} 生成ロジック（AUDIT_SURFACE_COLUMNS 定数・generate_audit_report・RECOVERY_CEILING_NOTE/CALIBRATION_BL_INFERIOR_NOTE/ODDS_JODDS_REVERIFICATION_NOTE 定数）"
      contains: "generate_audit_report"
      min_lines: 80
    - path: "reports/08-audit.md"
      provides: "Phase 8 監査レポート人間確認版（サーフェス別カバレッジマップ・SC対応表・Known Limitations）"
      contains: "Known Limitations"
    - path: "reports/08-audit.json"
      provides: "Phase 8 監査レポート機械消費版（byte-reproducible・sort_keys=True・surface_map/known_limitations/constants 階層）"
      contains: "surface_map"
  key_links:
    - from: "scripts/run_reproducibility_smoke.py"
      to: "tests/model/test_calibrator.py::test_reproduce_bit_identical"
      via: "subprocess で pytest で SC#4 calibrator bit-identical を確認（live-DB 不要）"
      pattern: "test_reproduce_bit_identical"
    - from: "scripts/run_reproducibility_smoke.py"
      to: "tests/audit/"
      via: "subprocess で pytest tests/audit/ を呼び SC#2 adversarial 全 GREEN を確認"
      pattern: "pytest.*tests/audit"
    - from: "scripts/run_reproducibility_smoke.py"
      to: "scripts/run_train_predict.py / scripts/run_backtest.py"
      via: "SC#3 の live-DB 必須 CLI（--check-reproduce）は 08-03 checkpoint が実行（08-02 は呼ばない・--synthetic flag は run_train_predict に非存在・run_backtest --synthetic --check-reproduce は readonly_pool=None でラベル未結合→ValueError で非零 exit のため 08-02 では使用しない）"
      pattern: "08-03"
    - from: "src/audit/report.py"
      to: "src/model/artifact.py::_atomic_write_text"
      via: "md+json 原子的書込（src/ev/report.py DRY 再利用）"
      pattern: "_atomic_write_text"
    - from: "src/audit/report.py"
      to: "src/ev/report.py"
      via: "REPORT_COLUMNS 定数外部化 + md+json 分離 + sort_keys=True presence assert の DRY パターン再利用"
      pattern: "AUDIT_SURFACE_COLUMNS"
---

<objective>
SC#3（固定 seed 再現）のうち合成データ層を `scripts/run_reproducibility_smoke.py` で証明し（per D-03）・D-01/D-05 の `reports/08-audit.{md,json}`（サーフェス別カバレッジマップ・SC#1/#2/#3 対応表・Known Limitations）を `src/audit/report.py` で生成する。両者は既存パターンの組み合わせと集約で完結する（RESEARCH "Key insight"・新規の仕組みを発明しない）。live-DB 必須の再現 CLI（run_train_predict --check-reproduce・run_backtest --check-reproduce）は 08-03 checkpoint が実行する。

Purpose: TEST-01 SC#3「再現性スモークテストパスに接続」+ D-01「監査レポート集約可視化」+ D-05「Known Limitations honest 開示」を満たす。SC#3 の合成層は Phase 4 SC#4 の bit-identical インフラ（seed=42 + num_threads=1 + FIXED_REPRODUCE_TS）を DB 不要 pytest（calibrator bit-identical・tests/audit/）で orchestrate する薄いスクリプト（新規 runner 不要）。live-DB 必須の再現 CLI は scripts/run_train_predict.py に --synthetic flag が存在せず（argparse L107-163）Settings()+make_pool+load_labels で live-DB 必須・scripts/run_backtest.py --synthetic --check-reproduce は readonly_pool=None（L1031）→ _prepare_feature_df がラベル未結合（L1308-1317）→ orchestrator L341 で raise ValueError で非零 exit するため・08-02（autonomous・user_setup なし）では呼ばず 08-03 に委譲する。reports は src/ev/report.py の DRY パターン（REPORT_COLUMNS 定数外部化・md+json 分離・_atomic_write_text・sort_keys=True・presence assert）を再利用。
Output: `scripts/run_reproducibility_smoke.py` / `src/audit/report.py` / `reports/08-audit.{md,json}`。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-adversarial-audit-suite/08-CONTEXT.md
@.planning/phases/08-adversarial-audit-suite/08-RESEARCH.md
@.planning/phases/08-adversarial-audit-suite/08-PATTERNS.md
@.planning/phases/08-adversarial-audit-suite/08-01-SUMMARY.md

# DRY 再利用元（READ・変更しない）
@src/ev/report.py
@src/model/artifact.py
@scripts/run_train_predict.py
@scripts/run_backtest.py
@src/model/orchestrator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/run_reproducibility_smoke.py 新設 — SC#3 既存 CLI orchestrate（D-03）</name>
  <files>scripts/run_reproducibility_smoke.py</files>
  <read_first>
    - scripts/run_train_predict.py（L46-61 sys.path ガード・L218 def main(argv: list[str] | None = None) -> int 構造・**argparse L107-163 に --synthetic flag は存在しない**・L225 Settings() で .env KEIBA_DATABASE_URL 必須・L253 make_pool(role="readonly") で live-DB 必須・L257-261 --check-reproduce が _prepare_feature_df(readonly_pool) → load_labels(cur) で live-DB SELECT を呼ぶ。つまり --check-reproduce は live-DB 必須・08-02 からは呼ばない）
    - scripts/run_backtest.py（L135 --synthetic flag・L148 --check-reproduce flag・L1031 readonly_pool = None if args.synthetic・L1071-1093 --check-reproduce 経路が _prepare_feature_df(args, readonly_pool) を呼ぶ・L1308-1317 _prepare_feature_df が readonly_pool is None で load_feature_matrix() 生戻り値（ラベル未結合）を返す・src/model/orchestrator.py L341 で fukusho_hit_validated 無しで raise ValueError → 非零 exit。つまり --synthetic --check-reproduce は現状必ず非零 exit するため 08-02 からは呼ばない）
    - src/model/orchestrator.py（L88 FIXED_REPRODUCE_TS 定数・L758 def _assert_deterministic は内部で train_and_predict を呼ぶため live-DB / ラベル結合が必要・L341 で fukusho_hit_validated 欠落で raise ValueError）
    - tests/model/test_calibrator.py（test_reproduce_bit_identical: calibrator bit-identical pytest・**live-DB 不要・合成データ**・subprocess で呼ぶ対象）
    - tests/model/test_trainer.py（合成データ系 bit-identical / deterministic テスト群の有無を grep で確認・存在する場合のみ step (2) に含める）
    - .planning/phases/08-adversarial-audit-suite/08-PATTERNS.md（run_reproducibility_smoke.py の analog 割当・subprocess orchestrate パターン・sys.path ガード・main()->int 構造）
    - .planning/phases/08-adversarial-audit-suite/08-RESEARCH.md（Architecture Patterns Pattern 3: 既存 pytest orchestrate・Code Examples SC#3 smoke 核心）
  </read_first>
  <action>
    scripts/run_reproducibility_smoke.py を新設。モジュール docstring に「SC#3 合成層: 固定 seed で合成データの bit-identical 再現性を確認する薄い orchestrator（per D-03）。Phase 4 SC#4 の bit-identical インフラ（seed=42 + num_threads=1 + FIXED_REPRODUCE_TS）を DB 不要 pytest で orchestrate する。新規フルパイプライン runner は作らない（keep it simple・重複回避）。live-DB 必須の run_train_predict --check-reproduce / run_backtest --check-reproduce は本スクリプトでは呼ばず Plan 08-03 checkpoint が実行する（run_train_predict は --synthetic flag 非存在で live-DB 必須・run_backtest --synthetic --check-reproduce は readonly_pool=None でラベル未結合→ValueError で非零 exit するため）。」を明記。

    imports: from __future__ import annotations / import argparse / import logging / import subprocess / import sys / from pathlib import Path。sys.path ガード（analog scripts/run_train_predict.py L46-61）: _REPO_ROOT = Path(__file__).resolve().parent.parent・if str(_REPO_ROOT) not in sys.path: sys.path.insert(0, str(_REPO_ROOT))。logging.basicConfig(level=logging.INFO, format="%(asctime)s [%(levelname)s] %(name)s: %(message)s")・logger = logging.getLogger("run_reproducibility_smoke")。

    def main(argv: list[str] | None = None) -> int: を実装（analog run_train_predict.py L218 構造）。docstring に「SC#3 合成層: 合成データの固定 seed 再現を確認。2 step を subprocess で順次実行し・いずれかが非零 exit なら即座に return 1。live-DB 必須 CLI は 08-03 が実行。」。steps リストを定義（RESEARCH L381-410 / PATTERNS.md subprocess orchestrate パターン準拠・live-DB 不要 pytest のみ・NC-03 対応で trainer bit-identical step は当初から外す）:
      (1) ["uv", "run", "pytest", "tests/model/test_calibrator.py::test_reproduce_bit_identical", "-q"] — SC#4 calibrator bit-identical（live-DB 不要・合成データ・決定論的）
      (2) ["uv", "run", "pytest", "tests/audit/", "-q"] — SC#2 adversarial tests（Plan 08-01 成果物・全 GREEN を確認・KEIBA_SKIP_DB_TESTS=1 でも実行される DB 不要テスト）

    ※ trainer bit-identical 群（tests/model/test_trainer.py の -k "reproduce or bit_identical or deterministic"）は現状（commit eff76c6 時点）該当テスト0件・collect-only で確認済み（test_lightgbm_nonneg_codes / test_catboost_has_time / test_catboost_predict_preserves_row_order / test_no_target_encoding_leak / test_eval_set_disjoint_from_calib_test / test_no_target_encoding_imports_in_trainer_module のいずれもキーワード非含・NC-03）。将来 trainer に bit-identical テストが追加された場合は steps に戻すこと。

    for cmd, desc in steps: で順次 subprocess.run(cmd) を実行・result.returncode != 0 なら logger.error("FAIL: %s", desc) して return 1・成功なら logger.info("PASS: %s", desc)。全 PASS で logger.info("SC#3 reproducibility smoke (synthetic layer): ALL PASS")・return 0。

    argparse で --step（単一 step 実行・デバッグ用・省略時は全 step）を追加可能（最小限）。if __name__ == "__main__": sys.exit(main())。

    本スクリプトは live-DB を使用しない（autonomous・user_setup なし）。run_train_predict.py / run_backtest.py の --check-reproduce 経路は呼ばない（F-02/F-03: 両経路とも live-DB 必須または非零 exit が確定するため・08-03 に委譲）。action 内に fenced code block を含めないこと（識別子・CLI flag・step 構造は名前で言及）。
  </action>
  <verify>
    <automated>uv run python scripts/run_reproducibility_smoke.py; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/run_reproducibility_smoke.py が存在し def main(argv: list[str] | None = None) -> int を持つ
    - sys.path ガード（_REPO_ROOT = Path(__file__).resolve().parent.parent）が先頭にある
    - 2 step（pytest test_reproduce_bit_identical / pytest tests/audit/）が順次 subprocess.run で実行される（NC-03: trainer bit-identical 群は現状該当テスト0件のため当初から除外・将来追加時に戻すこと）
    - **run_train_predict.py --check-reproduce および run_backtest.py --synthetic --check-reproduce は steps に含まれない**（`grep -n "subprocess.run.*run_train_predict\|subprocess.run.*run_backtest" scripts/run_reproducibility_smoke.py` のマッチ行数 == 0・NC-02: subprocess.run 呼出に限定することで docstring の委譲説明由来のマッチを弾く）
    - いずれかの step が非零 exit なら return 1・全 PASS で return 0
    - 実行して exit=0（SC#3 smoke 合成層 GREEN・live-DB 不要）※ Plan 08-01 完了後であること
    - ruff check scripts/run_reproducibility_smoke.py が GREEN
  </acceptance_criteria>
  <done>SC#3 合成層再現性スモーク orchestrator が DB 不要 pytest を束ねて GREEN を返す・新規フルパイプライン runner なし・live-DB 必須 CLI は 08-03 に委譲。</done>
</task>

<task type="auto">
  <name>Task 2: src/audit/report.py + reports/08-audit.{md,json} 生成（D-01, D-05）</name>
  <files>src/audit/__init__.py, src/audit/report.py, reports/08-audit.md, reports/08-audit.json</files>
  <read_first>
    - src/ev/report.py（L26-33 imports: json/Path/Any + from src.model.artifact import _atomic_write_text・L41-53 REPORT_COLUMNS 定数外部化・L56 REPORT_TITLE・L59-90 ODDS_POLICY_FIXED_NOTE/NO_WINNER_OVERRIDE_NOTE/PHASE6_SELECTION_NOTE 定数 text 外部化・L93-135 _format_float/_format_comparison_table_md・L176-274 generate_report md+json 分離・sort_keys=True・ensure_ascii=False）
    - src/model/artifact.py（_atomic_write_text 署名・原子的書込プリミティブ）
    - reports/05-backtest.md（reports/ 慣例・md 構造: タイトル・表・注記セクション）
    - reports/05-backtest.json（reports/ 慣例・json 構造: sort_keys=True・comparison_table/metrics/constants/notes 階層）
    - reports/06-evaluation.json（Known Limitations 数値根拠: 回収率 LightGBM 0.7022/CatBoost 0.6808・calibration_max_dev LGB 0.2308 vs BL-1 0.0014）
    - tests/ev/test_metrics.py（compute_backtest_metrics の既存テスト: recovery_rate/refund/max_drawdown・F-05 evaluation_metrics サーフェスの existing_tests にマップ・L46+）
    - tests/model/test_evaluator.py（compute_metrics の既存テスト: calibration_max_dev 等・L93+・F-05 evaluation_metrics サーフェスの existing_tests にマップ）
    - tests/model/test_evaluator_gate.py（評価指標 gate 既存テスト・F-05 evaluation_metrics サーフェスの existing_tests にマップ）
    - reports/04-eval.json（BL 比較表・Calibration 劣位の数値根拠）
    - .planning/phases/08-adversarial-audit-suite/08-RESEARCH.md（SC#1 サーフェス別既存テストマッピング表・SC#2 の3ケース vs 既存テスト境界表・D-06 棚卸し表・reports/08-audit 構造）
    - .planning/phases/08-adversarial-audit-suite/08-PATTERNS.md（src/audit/report.py の analog 割当・REPORT_COLUMNS 定数外部化パターン・md+json 分離生成・Known Limitations 定数外部化）
    - .planning/phases/08-adversarial-audit-suite/08-01-SUMMARY.md（Plan 01 成果物: tests/audit/ の4テストファイル名・SC#1/#2/#3 対応表の素材）
  </read_first>
  <action>
    src/audit/__init__.py を空（docstring コメント1行）で作成。

    src/audit/report.py を新設。imports: from __future__ import annotations / import json / from pathlib import Path / from typing import Any / from src.model.artifact import _atomic_write_text（analog src/ev/report.py L26-33）。

    AUDIT_SURFACE_COLUMNS 定数を定義（analog REPORT_COLUMNS・tuple[str, ...]）: ("surface", "sc_id", "existing_tests", "adversarial_test", "status", "evidence")。surface は SC#1 #1-#8 の8サーフェス名（fukusho_label / payout_reconcile / refund_handling / odds_snapshot / virtual_purchase / feature_cutoff / race_id_split / class_normalization）+ 補足カテゴリ（categorical_missing・ui_csv_readonly）+ TEST-01 必須の「評価指標計算」サーフェス evaluation_metrics（REQUIREMENTS.md L65 TEST-01 が明示・F-05 対応）。

    SC#1 サーフェス別既存テストマッピングの定数データを構築（RESEARCH SC#1 サーフェス別既存テストマッピング表から直接転記）: SURFACE_ROWS: list[dict[str, Any]] = [...]。各 row は AUDIT_SURFACE_COLUMNS のキーを持つ。status は "COVERED"（既存機能テストのみ）/ "ADVERSARIAL"（Plan 01 で adversarial 新設）/ "COVERED+ADVERSARIAL"。existing_tests は RESEARCH マッピング表の既存テストファイル名（test_fukusho_label.py 等）。adversarial_test は Plan 01 の tests/audit/test_audit_*.py（該当サーフェスのみ・それ以外は空文字）。**evaluation_metrics 行を必ず含める**（surface="evaluation_metrics"・sc_id="TEST-01"・existing_tests="tests/ev/test_metrics.py (compute_backtest_metrics: recovery_rate/refund/max_drawdown), tests/model/test_evaluator.py (compute_metrics: calibration_max_dev 等), tests/model/test_evaluator_gate.py"・adversarial_test=""・status="COVERED"・evidence="REQUIREMENTS.md L65 TEST-01 評価指標計算"・F-05 対応）。

    Known Limitations 定数を定義（analog ODDS_POLICY_FIXED_NOTE 等・D-05・memory fukusho-recovery-070-structural-ceiling 整合）:
      - RECOVERY_CEILING_NOTE: "回収率天井 ~0.65-0.70: odds-free 1-A モデルの構造的限界（LightGBM 0.7022・CatBoost 0.6808）。閾値調整では改善しない・Phase 1-B（odds 特徴量）か評価リフレームで対処。memory fukusho-recovery-070-structural-ceiling 整合。"
      - CALIBRATION_BL_INFERIOR_NOTE: "Calibration BL 劣位: 主モデル(LGB calibration_max_dev=0.2308)が BL-1(0.0014)/BL-4 に劣位。Phase 4 SC#2 で確定・Phase 6 キャリブ指標再設計（quantile/ECE/MCE 併記）の文脈。"
      - ODDS_JODDS_REVERIFICATION_NOTE: "odds JODDS再検証 subject: Phase 5 実データ backtest 25件完走だが・odds 正確性は JODDS取得完了後に再検証。manual-only 分離。"
    KNOWN_LIMITATIONS: list[str] = [RECOVERY_CEILING_NOTE, CALIBRATION_BL_INFERIOR_NOTE, ODDS_JODDS_REVERIFICATION_NOTE]。

    def generate_audit_report(*, output_dir: str | Path = "reports") -> tuple[Path, Path]: を実装（analog generate_report L176-274）。docstring「監査結果を reports/08-audit.{md,json} に出力する（per D-01/D-05）。AUDIT_SURFACE_COLUMNS で md 列ヘッダと json キーを 1:1 に保ち・sort_keys=True で byte-reproducible。」。

    md 生成: md_lines に "# Phase 8 Adversarial Audit Report (TEST-01 / SC#1・SC#2・SC#3)\n\n" + "## サーフェス別カバレッジマップ (SC#1 #1-#8)\n\n" + _format_surface_table_md(SURFACE_ROWS) + "\n\n" + "## SC#1/#2/#3 対応表\n\n" + SC対応表 md + "\n\n" + "## Known Limitations (\"Looks Done But Isn't\" honest 開示)\n\n" + 各 KNOWN_LIMITATIONS の "- {lim}\n" + "\n\n" + "## フルスイート GREEN 証明 (D-04)\n\n" + "KEIBA_SKIP_DB_TESTS unset 全実行・全 requires_db 含む・0 skipped。詳細は checkpoint 08-03（Plan 08-03）で実施。\n"。_format_surface_table_md は analog _format_comparison_table_md（REPORT_COLUMNS 順・header/separator/body）。

    json 生成: json.dumps({"surface_map": SURFACE_ROWS, "constants": {"AUDIT_SURFACE_COLUMNS": list(AUDIT_SURFACE_COLUMNS)}, "known_limitations": KNOWN_LIMITATIONS, "sc_correspondence": SC_CORRESPONDENCE, "full_suite_result": {"d04_checkpoint": "Plan 08-03 で実施"}}, sort_keys=True, ensure_ascii=False)。_atomic_write_text で原子的書込。

    presence assert（LOW-05 analog）: generate_audit_report 末尾で・md に AUDIT_SURFACE_COLUMNS の全 column 名がヘッダ行に含まれること・json の surface_map 各 row が AUDIT_SURFACE_COLUMNS の全キーを持つことを assert（乖離で loud fail）。

    return (md_path, json_path)。

    if __name__ == "__main__": generate_audit_report() を呼んで reports/08-audit.{md,json} を生成。

    reports/08-audit.md / reports/08-audit.json を generate_audit_report() の実行で生成（スクリプト実行で産物を commits に含める）。SC_CORRESPONDENCE は SC#1（機能テスト8サーフェス）/ SC#2（3ケース adversarial・Plan 01 の test_audit_features/test_audit_label/test_audit_split）/ SC#3（run_reproducibility_smoke・Plan 02 Task 1）の対応表データ。action 内に fenced code block を含めないこと（定数名・署名・md セクション見出しは名前で言及）。
  </action>
  <verify>
    <automated>uv run python -c "from src.audit.report import generate_audit_report, AUDIT_SURFACE_COLUMNS; md, js = generate_audit_report(); print(md.name, js.name)" && KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/audit/ -q</automated>
  </verify>
  <acceptance_criteria>
    - src/audit/__init__.py が存在し空パッケージマーカー
    - src/audit/report.py に AUDIT_SURFACE_COLUMNS 定数（6要素 tuple: surface/sc_id/existing_tests/adversarial_test/status/evidence）が定義される
    - src/audit/report.py に RECOVERY_CEILING_NOTE / CALIBRATION_BL_INFERIOR_NOTE / ODDS_JODDS_REVERIFICATION_NOTE 定数が定義される
    - src/audit/report.py::generate_audit_report(*, output_dir="reports") -> tuple[Path, Path] が存在し _atomic_write_text で md+json を書込
    - generate_audit_report 内に presence assert（md ヘッダと json キーが AUDIT_SURFACE_COLUMNS と 1:1）が含まれる
    - reports/08-audit.md が生成され「# Phase 8 Adversarial Audit Report」「サーフェス別カバレッジマップ」「SC#1/#2/#3 対応表」「Known Limitations」「フルスイート GREEN 証明」の5セクション見出しを含む
    - reports/08-audit.md の Known Limitations セクションに回収率天井・Calibration劣位・odds JODDS再検証の3項目が含まれる（D-05）
    - reports/08-audit.json が生成され sort_keys=True・ensure_ascii=False・surface_map/known_limitations/constants/sc_correspondence 階層を持つ
    - reports/08-audit.json を2回生成して同一 bytes（byte-reproducible）
    - SURFACE_ROWS に evaluation_metrics 行が含まれる（`grep -c "evaluation_metrics" src/audit/report.py` >= 1・F-05 対応・REQUIREMENTS.md L65 TEST-01 評価指標計算）
    - reports/08-audit.md のサーフェステーブルに evaluation_metrics 行が表示される
    - ruff check src/audit/ が GREEN
  </acceptance_criteria>
  <done>SC#3 smoke orchestrator + reports/08-audit.{md,json} 生成ロジックが完成・reports 生成済・Known Limitations 3項目 honest 開示・byte-reproducible。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run_reproducibility_smoke → 既存 CLI（subprocess） | smoke スクリプトは既存 CLI と pytest を subprocess で呼ぶ。入力パラメータ固定（--check-reproduce / --synthetic）。 |
| src/audit/report.py → reports/08-audit.{md,json} | report 生成ロジックが md+json を原子書込。外部入力なし（SURFACE_ROWS / KNOWN_LIMITATIONS は定数）。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Tampering | 監査レポートが実態と乖離（カバレッジ虚偽・SURFACE_ROWS の既存テスト名が存在しない） | mitigate | presence assert で md ヘッダと json キーの 1:1 を機械検証（LOW-05 analog）。SURFACE_ROWS の既存テスト名は RESEARCH.md SC#1 マッピング表（直接精査済み・HIGH confidence）から転記。`pytest --collect-only -q` で対応表のテスト関数が実際に存在するか sanity check を生成時に実行可能（Claude's Discretion・A1）。 |
| T-08-06 | Tampering | Known Limitations の隠蔽（"Looks Done But Isn't" の核心侵害） | mitigate | D-05 ロック決定に従い RECOVERY_CEILING_NOTE / CALIBRATION_BL_INFERIOR_NOTE / ODDS_JODDS_REVERIFICATION_NOTE の3項目を定数で強制。generate_audit_report が KNOWN_LIMITATIONS を必ず md と json の両方に出力（隠蔽構造的に不可）。数値は reports/06-evaluation.json / reports/04-eval.json から直接確認済み。 |
| T-08-07 | Repudiation | SC#3 smoke が live-DB 全量で重く・反復実行困難で「実施した」と称する | mitigate | 08-02 は DB 不要 pytest のみで SC#3 合成層を証明（autonomous・user_setup なし）。live-DB 必須 CLI（run_train_predict --check-reproduce・run_backtest --check-reproduce）は 08-03 checkpoint が人間承認付きで実行（F-02/F-03 対応）。run_backtest --synthetic --check-reproduce が readonly_pool=None で必ず非零 exit する事実（L1031/L1308-1317/orchestrator L341）を PLAN に明記し誤使用を構造的に排除。 |
| T-08-08 | Tampering | smoke スクリプトが step 失敗を silent に無視 | mitigate | 各 step の subprocess.run の returncode != 0 で即座 logger.error + return 1。全 step PASS でのみ return 0（silent skip 構造的に不可）。 |
| T-08-SC | Tampering | npm/pip/cargo install | accept | Phase 8 は外部パッケージインストールなし（RESEARCH Package Legitimacy Audit 該当なし）。インストール task なし。 |
</threat_model>

<verification>
- uv run python scripts/run_reproducibility_smoke.py が exit=0（SC#3 smoke 合成層 GREEN・2 step 全 PASS・live-DB 不要・NC-03: trainer bit-identical 群は現状該当テスト0件のため step から除外）
- uv run python -c "from src.audit.report import generate_audit_report; generate_audit_report()" が reports/08-audit.{md,json} を生成
- reports/08-audit.md に5セクション見出し（サーフェス別カバレッジマップ / SC対応表 / Known Limitations / フルスイート GREEN 証明 / タイトル）が含まれる
- reports/08-audit.json を2回生成して同一 bytes（diff なし）
- ruff check scripts/run_reproducibility_smoke.py src/audit/ が GREEN
</verification>

<success_criteria>
- SC#3 run_reproducibility_smoke.py が DB 不要 pytest を束ねて GREEN・新規フルパイプライン runner なし（D-03）・live-DB 必須 CLI は 08-03 に委譲（F-02/F-03 対応）
- src/audit/report.py が src/ev/report.py DRY パターンで reports/08-audit.{md,json} を生成（D-01）
- reports/08-audit に Known Limitations 3項目（回収率天井・Calibration劣位・odds再検証）が honest 開示（D-05）
- reports/08-audit.json が byte-reproducible（sort_keys=True・ensure_ascii=False）
- AUDIT_SURFACE_COLUMNS で md 列ヘッダと json キーが 1:1（presence assert）
</success_criteria>

<output>
Create `.planning/phases/08-adversarial-audit-suite/08-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**新規シンボル:**
- `scripts/run_reproducibility_smoke.py::main(argv: list[str] | None = None) -> int` — SC#3 合成層 orchestrator（2 step subprocess・live-DB 不要 pytest のみ・NC-03: trainer bit-identical 群は現状該当テスト0件のため除外・将来追加時に戻すこと）
- `src/audit/__init__.py` — パッケージマーカー
- `src/audit/report.py::AUDIT_SURFACE_COLUMNS` — 列契約定数（tuple: surface/sc_id/existing_tests/adversarial_test/status/evidence）
- `src/audit/report.py::SURFACE_ROWS` — SC#1 #1-#8 サーフェス別マッピングデータ（list[dict]）
- `src/audit/report.py::RECOVERY_CEILING_NOTE` / `CALIBRATION_BL_INFERIOR_NOTE` / `ODDS_JODDS_REVERIFICATION_NOTE` — Known Limitations 定数（D-05）
- `src/audit/report.py::KNOWN_LIMITATIONS` — Known Limitations リスト（3要素）
- `src/audit/report.py::SC_CORRESPONDENCE` — SC#1/#2/#3 対応表データ
- `src/audit/report.py::generate_audit_report(*, output_dir="reports") -> tuple[Path, Path]` — md+json 生成

**新規 CLI フラグ:** `scripts/run_reproducibility_smoke.py` の `--step`（単一 step 実行・デバッグ用・省略時全 step）。※ 本スクリプトは run_train_predict.py / run_backtest.py の --check-reproduce 経路を呼ばない（live-DB 必須 / 非零 exit 確定のため・08-03 に委譲）

**新規ファイルパス:** `scripts/run_reproducibility_smoke.py` / `src/audit/__init__.py` / `src/audit/report.py` / `reports/08-audit.md` / `reports/08-audit.json`

**import する既存シンボル（READ only）:** `src.model.artifact._atomic_write_text`（src/audit/report.py から）。`tests/model/test_calibrator.py::test_reproduce_bit_identical` / `tests/model/test_trainer.py`（bit-identical 群・存在場合）/ `tests/audit/`（subprocess 経由・run_reproducibility_smoke.py から・全て live-DB 不要）。※ `scripts/run_train_predict.py` / `scripts/run_backtest.py` の --check-reproduce は本 plan から呼ばない（08-03 委譲）。
