---
phase: 08-adversarial-audit-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/audit/__init__.py
  - tests/audit/conftest.py
  - tests/audit/test_audit_features.py
  - tests/audit/test_audit_label.py
  - tests/audit/test_audit_split.py
  - tests/audit/test_audit_ui_csv.py
autonomous: true
requirements:
  - TEST-01
user_setup: []

must_haves:
  truths:
    - "SC#2 ケース1: feature 値が T+1 データを使用すると検出されて fail する adversarial テストが GREEN（注入を検出する）"
    - "SC#2 ケース2: payout 払戻対象正の馬が label に欠落すると検出されて fail する adversarial テストが GREEN"
    - "SC#2 ケース3: fold の train/test が race_id を共有すると ValueError で検出される adversarial テストが GREEN"
    - "D-06: src/ui/ の read-only 保証違反（書き込み/DDL SQL リテラル）を AST が検出する adversarial テストが GREEN"
    - "D-06: PREDICTION_CSV_COLUMNS から再現性スタンプを欠落させると presence assert が fail することを adversarial が実証"
    - "各 adversarial テストは docstring で SC#2 adversarial（注入型メタ検証）であることと・近接する既存機能テストへの cross-reference を明記（重複回避）"
    - "KEIBA_SKIP_DB_TESTS=1 でも tests/audit/ 全テストが実行される（合成データ中心・DB 不要）"
  artifacts:
    - path: "tests/audit/__init__.py"
      provides: "tests.audit パッケージマーカー"
      min_lines: 1
    - path: "tests/audit/conftest.py"
      provides: "合成 DataFrame 注入ヘルパー（_build_label_row / _build_payout_row 等の **overrides 機構・tests/features/conftest.py analog）"
      min_lines: 30
    - path: "tests/audit/test_audit_features.py"
      provides: "SC#2 ケース1 lookahead 注入 adversarial テスト（test_lookahead_injection_detected_and_fails）"
      contains: "SC#2 adversarial"
    - path: "tests/audit/test_audit_label.py"
      provides: "SC#2 ケース2 payout 正欠損注入 adversarial テスト（test_payout_positive_missing_from_labels_detected）"
      contains: "SC#2 adversarial"
    - path: "tests/audit/test_audit_split.py"
      provides: "SC#2 ケース3 fold race_id 共有注入 adversarial テスト（test_fold_race_id_shared_detected_and_raises）"
      contains: "SC#2 adversarial"
    - path: "tests/audit/test_audit_ui_csv.py"
      provides: "D-06 read-only 保証 + 再現性スタンプ inline adversarial テスト"
      contains: "D-06"
  key_links:
    - from: "tests/audit/test_audit_features.py"
      to: "src/features/rolling.py::build_rolling_features"
      via: "合成 history に T+1 行を注入し build_rolling_features が strict < cutoff で除外することを検証"
      pattern: "build_rolling_features"
    - from: "tests/audit/test_audit_label.py"
      to: "src/etl/label_reconcile.py::_check_payout_recall"
      via: "mock cursor で payout 不一致件数 > 0 を注入し _check_payout_recall.passed is False を検証"
      pattern: "_check_payout_recall"
    - from: "tests/audit/test_audit_split.py"
      to: "src/utils/group_split.py::get_bt_race_ids"
      via: "train_end == test_start で R2 を共有する BTWindow を注入し ValueError(match='race_id') を検証"
      pattern: "get_bt_race_ids"
    - from: "tests/audit/test_audit_ui_csv.py"
      to: "tests/ui/test_readonly_guarantee.py::_extract_sql_literals + src/ui/csv_columns.py::PREDICTION_CSV_COLUMNS"
      via: "AST で SQL 文字列リテラルの書き込み/DDL キーワード検出 + presence assert でスタンプ欠落検出"
      pattern: "_extract_sql_literals|PREDICTION_CSV_COLUMNS"
---

<objective>
SC#2 が明示する3つのリーク注入ケース（lookahead / payout 正欠損 / fold race_id 共有）と D-06（Phase 7 継承の UI/CSV 対抗的監査）を、`tests/audit/` に**リークを注入すると fail する独立 adversarial（注入型メタ検証）テスト**として新設する（per D-01, D-02, D-06）。

Purpose: TEST-01「リーク防止の対抗的監査テストを含む」を満たす。機能テスト（test_pit_cutoff / test_label_reconcile / test_group_split）が「正しく処理される」を検証するのに対し・本テスト群は「guard を無効化すると混入する（=リークがあれば検出される）」ことを実証する。SC#2 の「3ケースそれぞれ独立 adversarial」要件を docstring cross-reference で満たす。
Output: `tests/audit/` パッケージ（__init__・conftest・4テストファイル）。DB 不要・合成データ中心（KEIBA_SKIP_DB_TESTS=1 でも実行される）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-adversarial-audit-suite/08-CONTEXT.md
@.planning/phases/08-adversarial-audit-suite/08-RESEARCH.md
@.planning/phases/08-adversarial-audit-suite/08-PATTERNS.md
@.planning/phases/08-adversarial-audit-suite/08-VALIDATION.md

# 監査対象コード契約（READ・変更しない）
@tests/model/test_trainer.py
@tests/features/conftest.py
@tests/test_label_reconcile.py
@tests/utils/test_group_split.py
@tests/ui/test_readonly_guarantee.py
@tests/ui/test_csv_columns.py
@src/features/rolling.py
@src/etl/label_reconcile.py
@src/utils/group_split.py
@src/ui/csv_columns.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tests/audit/ パッケージ基盤 + SC#2 ケース1 lookahead 注入 adversarial（D-01, D-02）</name>
  <files>tests/audit/__init__.py, tests/audit/conftest.py, tests/audit/test_audit_features.py</files>
  <read_first>
    - tests/model/test_trainer.py（L277-486 test_no_target_encoding_leak: 5段階鋳型・合成データ→ベースライン→リーク注入→注入版で fail→検証力証明。adversarial docstring の正確な呼称「対抗的構造診断」を踏襲）
    - tests/features/conftest.py（_build_adversarial_rolling_rows / _build_race_obs_row / _build_se_history_row: 合成 DataFrame 注入ヘルパー・**overrides 機構・min_lines を把握）
    - tests/features/test_pit_cutoff.py（cross-reference 先・機能テスト「正しく除外される」の docstring と合成データ構造・本テストとの棲み分け理解）
    - src/features/rolling.py（build_rolling_features 署名・戻り値の rolling_*_mean_5 列・strict < cutoff guard の実装位置）
    - .planning/phases/08-adversarial-audit-suite/08-PATTERNS.md（test_audit_features.py の analog 割当・imports/鋳型/docstring パターン）
  </read_first>
  <behavior>
    - test_lookahead_injection_detected_and_fails: 合成 history（eligible 3行のみ・kakuteijyuni 識別値）で build_rolling_features のベースライン結果（mean=2.0 等）を取得 → T+1 行（cutoff 以後の as_of 偽装）を注入した history で再実行 → guard 有効なら混入を検出して同一結果（mean=2.0）・guard 無効なら別結果になることを示す注入→fail メタ検証。SC#2 adversarial であることを docstring で明記 + cross-reference: tests/features/test_pit_cutoff.py。
    - conftest.py の _build_label_row / _build_payout_row / _build_history_row 系ヘルパーが **overrides で任意カラム上書き可能（注入制御用）。
    - tests/audit/__init__.py は空パッケージマーカー。
  </behavior>
  <action>
    tests/audit/__init__.py を空（docstring コメント1行のみ）で作成。

    tests/audit/conftest.py を作成。analog tests/features/conftest.py のヘッダ/docstring 構造を再利用し・docstring に「Phase 8 audit 共通 fixtures / synthetic DataFrame builder（per D-01/D-02 SC#2 adversarial 注入ヘルパー）」と明記。imports: from __future__ import annotations / from unittest.mock import MagicMock / import pandas as pd / import pytest。合成行 builder 関数として _build_label_row(race_key, umaban, **overrides) と _build_payout_row(race_key, umaban, **overrides) と _build_history_row(race_key, kettonum, as_of, **overrides) を定義（各々デフォルト正常行 + **overrides で任意カラム上書き・PII なし・ID のみ）。これらの builder は後続 Task 2/3 でも再利用される共通基盤。

    tests/audit/test_audit_features.py を作成。def test_lookahead_injection_detected_and_fails() を実装。docstring の冒頭に「SC#2 adversarial（注入型メタ検証）: feature 値が T+1 データを使用すると検出されて fail する。」を明記し「本テストは SC#2 adversarial（注入型メタ検証）であり・tests/features/test_pit_cutoff.py（機能テスト: 正しく除外される）とは独立層。cross-reference: tests/features/test_pit_cutoff.py。」を含める（重複回避・PATTERNS.md docstring 鋳型準拠）。

    テスト本体は5段階鋳型（test_no_target_encoding_leak L277-486 構造）を PIT cutoff に適用: (1) 合成 history 構築（eligible 3行のみ・kakuteijyuni 識別値・seed 固定）→ build_rolling_features でベースライン取得 → assert で mean 値を検証。(2) T+1 リーク注入: history の previous_day 行の as_of を cutoff 直後に偽装（本来除外されるべきが混入する状態を作る）・または PIT guard を monkeypatch で無効化（strict < を <= に緩める）。(3) guard 有効な正規経路なら注入を検出して正しい結果・guard 無効なら混入する → assert で「guard 有効なら mean がベースラインと一致」を検証。注入の検証力証明として・guard monkeypatch 無効化版で別結果になることも補助 assert で実証（false-pass 回避）。合成データは tests/features/conftest.py の _build_adversarial_rolling_rows / _build_race_obs_row を直接 import 再利用可能（PATTERNS.md imports パターン）・再実装しない。DB 不要・marker なし。

    imports は from src.features.rolling import build_rolling_features（検査対象）と tests/features/conftest_helpers から _build_adversarial_rolling_rows / _build_race_obs_row の直接再利用を試み・利用不可の場合は本 conftest.py の builder で同等合成データを構築（PATTERNS.md imports パターン・analog に忠実）。action 内に fenced code block を含めないこと（識別子・署名・docstring 文字列は名前で言及）。
  </action>
  <verify>
    <automated>KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/audit/test_audit_features.py -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/audit/__init__.py が存在し空パッケージマーカー（docstring コメント可）
    - tests/audit/conftest.py に _build_label_row / _build_payout_row / _build_history_row の3 builder が定義され・各々 **overrides 引数を持つ
    - test_audit_features.py::test_lookahead_injection_detected_and_fails が存在し docstring に「SC#2 adversarial」と「cross-reference: tests/features/test_pit_cutoff.py」を含む
    - test_lookahead_injection_detected_and_fails が GREEN（注入を検出する）
    - test_audit_features.py は DB 不要（requires_db marker なし）・KEIBA_SKIP_DB_TESTS=1 で実行される
    - ruff check tests/audit/ が GREEN
  </acceptance_criteria>
  <done>SC#2 ケース1 lookahead 注入 adversarial テストが GREEN・docstring で既存機能テストと棲み分け明示・KEIBA_SKIP_DB_TESTS=1 で実行される。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SC#2 ケース2 payout 正欠損注入 + ケース3 fold race_id 共有注入 adversarial（D-02）</name>
  <files>tests/audit/test_audit_label.py, tests/audit/test_audit_split.py</files>
  <read_first>
    - tests/audit/conftest.py（Task 1 で作成・_build_label_row / _build_payout_row の **overrides 機構を再利用）
    - tests/test_label_reconcile.py（_mock_cursor: SQL 部分文字列マッチで fetchone 戻り値を切り替える mock cursor・_check_payout_recall の戻り値 CheckResult.passed / .detail["count"]・verdict='fail' 集計ロジック L555-568。cross-reference 先）
    - src/etl/label_reconcile.py（_check_payout_recall 署名・reconcile_against_payout 署名・CheckResult dataclass 構造・mock cursor 経由の検査ロジック）
    - src/etl/quality_gate.py（CheckResult dataclass の passed / detail フィールド定義）
    - tests/utils/test_group_split.py（test_get_bt_race_ids_raises_on_leak L208-234: train_end==test_start で R2 を共有→ValueError(match='race_id') の注入→raise パターン。test_fold の cross-reference 先）
    - src/utils/group_split.py（BTWindow / BT_WINDOWS / get_bt_race_ids 署名・race_id 共有検出の ValueError raise 位置）
    - .planning/phases/08-adversarial-audit-suite/08-PATTERNS.md（test_audit_label.py / test_audit_split.py の analog 割当・注入パターン）
  </read_first>
  <behavior>
    - test_payout_positive_missing_from_labels_detected: mock cursor で payout 不一致件数 N>0 を注入 → _check_payout_recall.passed is False を検証。合成 label/payout で「正の馬の欠落」を注入し end-to-end で verdict='fail' を実証。docstring で SC#2 adversarial + cross-reference: tests/test_label_reconcile.py。
    - test_fold_race_id_shared_detected_and_raises: train_end == test_start で R2 を意図的に共有する BTWindow を注入 → get_bt_race_ids が ValueError(match='race_id') を raise することを検証。docstring で SC#2 adversarial（再定式化）+ cross-reference: tests/utils/test_group_split.py::test_get_bt_race_ids_raises_on_leak（既存・同一注入パターン）。
  </behavior>
  <action>
    tests/audit/test_audit_label.py を作成。def test_payout_positive_missing_from_labels_detected() を実装。docstring 冒頭「SC#2 adversarial: 払戻テーブルで複勝対象(正)の馬が label.fukusho_hit_validated に欠落している場合・reconcile が verdict='fail' を返すことを実証。」+「本テストは SC#2 adversarial（注入型メタ検証）であり・tests/test_label_reconcile.py（機能テスト: mock cursor でロジック検証）とは独立層。cross-reference: tests/test_label_reconcile.py。」を含める。imports: from unittest.mock import MagicMock / import pandas as pd / import pytest / from src.etl.label_reconcile import _check_payout_recall, reconcile_against_payout / from src.etl.quality_gate import CheckResult。analog tests/test_label_reconcile.py の _mock_cursor(fetch_map) ヘルパーを本テスト内に複製（SQL 部分文字列マッチで fetchone 戻り値を返す）し・fetch_map に "fukusho_hit_validated = 0": (1,) を渡して不一致件数 1 件を注入 → _check_payout_recall(cur).passed is False を assert と detail["count"] == 1 を assert。さらに合成 label/payout DataFrame（conftest.py の _build_label_row / _build_payout_row で構築・payout 正の馬 umaban=7 を label から欠落させる注入）で end-to-end の fail を実証。DB 不要（mock cursor 使用）。

    tests/audit/test_audit_split.py を作成。def test_fold_race_id_shared_detected_and_raises() を実装。docstring 冒頭「SC#2 adversarial: fold の train/test が race_id を共有すると ValueError で検出される。」+「本テストは SC#2 adversarial（注入型メタ検証）。cross-reference: tests/utils/test_group_split.py::test_get_bt_race_ids_raises_on_leak（既存・同一注入パターン）。SC#2 要件『3ケースそれぞれ独立 adversarial』を docstring で明示するため再定式化。」を含める。imports: import pandas as pd / import pytest / from src.utils.group_split import BT_WINDOWS, BTWindow, get_bt_race_ids。analog test_get_bt_race_ids_raises_on_leak と同一注入構造: leak_races DataFrame（R0-R3・race_date/race_start_datetime 昇順）を構築し・bad_bt = BTWindow(name="BT-LEAK", train_start="2022-12-30", train_end="2023-01-01", test_start="2023-01-01", test_end="2023-01-02", window_type="expanding") で R2 を train/test 共有（リーク注入）→ with pytest.raises(ValueError, match="race_id"): get_bt_race_ids(leak_races, bad_bt) で検出を検証。DB 不要。

    両テストとも docstring の cross-reference で重複回避を文書化（PATTERNS.md Pitfall 1 対策）。action 内に fenced code block を含めないこと。
  </action>
  <verify>
    <automated>KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/audit/test_audit_label.py tests/audit/test_audit_split.py -v</automated>
  </verify>
  <acceptance_criteria>
    - test_audit_label.py::test_payout_positive_missing_from_labels_detected が存在し docstring に「SC#2 adversarial」と「cross-reference: tests/test_label_reconcile.py」を含む
    - test_payout_positive_missing_from_labels_detected が mock cursor で不一致件数 > 0 を注入し _check_payout_recall.passed is False を検証（GREEN）
    - test_audit_split.py::test_fold_race_id_shared_detected_and_raises が存在し docstring に「SC#2 adversarial」と「cross-reference: tests/utils/test_group_split.py」を含む
    - test_fold_race_id_shared_detected_and_raises が BTWindow で train_end==test_start の R2 共有を注入し ValueError(match='race_id') を検証（GREEN）
    - 両ファイルとも DB 不要（requires_db marker なし）
    - ruff check tests/audit/test_audit_label.py tests/audit/test_audit_split.py が GREEN
  </acceptance_criteria>
  <done>SC#2 ケース2・ケース3 の注入 adversarial テストが GREEN・docstring で SC#2 adversarial と cross-reference 明示。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-06 UI/CSV 対抗的監査（read-only 保証注入検出 + 再現性スタンプ欠落検出）</name>
  <files>tests/audit/test_audit_ui_csv.py</files>
  <read_first>
    - tests/ui/test_readonly_guarantee.py（_extract_sql_literals L44-64: AST で execute() Call 第一引数の str 定数を抽出・test_ui_has_no_write_ddl_sql L82: INSERT/UPDATE/DELETE/TRUNCATE/CREATE/DROP/ALTER 検出・target dir は src/ui/。cross-reference 先）
    - tests/ui/test_csv_columns.py（test_prediction_csv_has_all_stks L65: presence assert で odds_snapshot_policy/odds_snapshot_at/model_version/feature_snapshot_id/backtest_strategy_version の5スタンプ検証・cross-reference 先）
    - src/ui/csv_columns.py（PREDICTION_CSV_COLUMNS / BACKTEST_CSV_COLUMNS 定数定義・再現性スタンプ5項目の含まれ方）
    - tests/audit/conftest.py（Task 1 で作成・必要に応じて builder 再利用）
    - .planning/phases/08-adversarial-audit-suite/08-PATTERNS.md（test_audit_ui_csv.py の analog 割当・AST SQL リテラル抽出 + presence assert の組み合わせ・adversarial 拡張）
  </read_first>
  <behavior>
    - test_ui_write_sql_injection_detected: 一時的に INSERT を含むダミー .py を tmp_path に配置 → _extract_sql_literals が書き込み/DDL キーワードを検出して fail すること（検証力証明）を実証。正規 src/ui/ は GREEN（書き込み/DDL なし）。
    - test_reproducibility_stamp_missing_detected: PREDICTION_CSV_COLUMNS から1スタンプを除いた tuple で presence assert が fail すること（スタンプ欠落検出の検証力証明）を実証。正規 PREDICTION_CSV_COLUMNS は GREEN（5スタンプ全存在）。
    - 両テストとも docstring で D-06（Phase 7 継承）・SC#1 の明示サーフェス外だが TEST-01 包括表現でスコープ内・cross-reference: tests/ui/test_readonly_guarantee.py / tests/ui/test_csv_columns.py を明記。
  </behavior>
  <action>
    tests/audit/test_audit_ui_csv.py を作成。imports: from __future__ import annotations / import ast / from pathlib import Path / import pytest / from src.ui.csv_columns import PREDICTION_CSV_COLUMNS。

    _extract_sql_literals(tree) ヘルパーを analog tests/ui/test_readonly_guarantee.py L44-64 から複製（AST walk で ast.Call の func.attr == "execute" の第一引数 ast.Constant str を抽出・comment/docstring false positive 回避）。書き込み/DDL キーワード検出ヘルパー _contains_write_ddl(sql_literals) を定義（キーワード: INSERT / UPDATE / DELETE / TRUNCATE / CREATE / DROP / ALTER・大文字小文字区別なし・SQL 文字列リテラル内でマッチ）。

    def test_ui_write_sql_injection_detected(tmp_path) を実装。docstring 冒頭「D-06 adversarial: src/ui/ に書き込み/DDL SQL が混入すると AST 検査が fail することを実証。Phase 7 継承（07-CONTEXT Deferred）。cross-reference: tests/ui/test_readonly_guarantee.py。」。tmp_path に dammy_write.py を作成（cur.execute("INSERT INTO x ...") を含む）→ ast.parse して _extract_sql_literals で SQL リテラル抽出 → _contains_write_ddl が True を返すことを assert（注入で検出される）。続いて正規 src/ui/ ディレクトリ（Path("src/ui")）の全 .py を走査し・書き込み/DDL 検出が False であることを assert（正規経路は GREEN）。

    def test_reproducibility_stamp_missing_detected() を実装。docstring 冒頭「D-06 adversarial: PREDICTION_CSV_COLUMNS から再現性スタンプを欠落させると presence assert が fail することを実証（§19.1 聖域）。cross-reference: tests/ui/test_csv_columns.py::test_prediction_csv_has_all_stamps。」。5スタンプ（odds_snapshot_policy / odds_snapshot_at / model_version / feature_snapshot_id / backtest_strategy_version）を定数 _REPRO_STAMPS で定義。正規 PREDICTION_CSV_COLUMNS に対して全スタンプ存在を assert（GREEN）。検証力証明: _REPRO_STAMPS から1つ除いた縮退 tuple に対して presence assert が fail することを別ヘルパーで実証（例: pytest.fail を期待・try/except で捕捉して「欠落で fail する」を検証）。

    DB 不要・marker なし。action 内に fenced code block を含めないこと。
  </action>
  <verify>
    <automated>KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/audit/test_audit_ui_csv.py -v</automated>
  </verify>
  <acceptance_criteria>
    - test_audit_ui_csv.py に _extract_sql_literals / _contains_write_ddl ヘルパーと _REPRO_STAMPS 定数が定義される
    - test_ui_write_sql_injection_detected が存在し docstring に「D-06」と「cross-reference: tests/ui/test_readonly_guarantee.py」を含む
    - test_ui_write_sql_injection_detected が tmp_path のダミー INSERT .py で検出 True を assert し・正規 src/ui/ で False を assert（GREEN）
    - test_reproducibility_stamp_missing_detected が存在し docstring に「D-06」と「cross-reference: tests/ui/test_csv_columns.py」を含む
    - test_reproducibility_stamp_missing_detected が正規 PREDICTION_CSV_COLUMNS で5スタンプ全存在を assert し・縮退 tuple で fail することを実証（GREEN）
    - DB 不要（requires_db marker なし）
    - ruff check tests/audit/test_audit_ui_csv.py が GREEN
  </acceptance_criteria>
  <done>D-06 UI/CSV 対抗的監査の read-only 保証注入検出 + 再現性スタンプ欠落検出 adversarial テストが GREEN・Phase 7 継承スコープを docstring で明示。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests/audit → src/ (READ only) | adversarial テストは src/features / src/etl / src/utils / src/ui を読み込み検査する。書き込みなし。 |
| 合成注入データ → テスト本体 | 合成 DataFrame / mock cursor は制御された seed 固定入力。実データ入力なし・PII なし（ID のみ）。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | adversarial テストの false-pass（リーク注入でも fail しない） | mitigate | test_no_target_encoding_leak の5段階鋳型（ベースライン→注入→注入版で fail→検証力証明）を踏襲。注入版で結果が変わることを補助 assert で実証し false-pass を構造的に排除（per D-02）。 |
| T-08-02 | Tampering | UI/CSV read-only 保証違反の見逃し（書き込み SQL 混入） | mitigate | AST で execute() Call 第一引数 str 定数のみ検査（comment/docstring/変数名 false positive 回避・既存 test_readonly_guarantee L44-64 の REVIEW LOW-2 対策踏襲）。書き込み/DDL キーワード6種（INSERT/UPDATE/DELETE/TRUNCATE/CREATE/DROP/ALTER）で大文字小文字区別なしマッチ（per D-06）。 |
| T-08-03 | Tampering | 再現性スタンプ欠落（§19.1 聖域違反）の見逃し | mitigate | presence assert で5スタンプ（odds_snapshot_policy/odds_snapshot_at/model_version/feature_snapshot_id/backtest_strategy_version）の存在を検証。縮退 tuple で fail することを別 assert で実証し検証力を証明（per D-06・§19.1）。 |
| T-08-04 | Repudiation | adversarial テストと機能テストの重複で「カバー済み」と誤認 | mitigate | 各テスト docstring で SC#2 adversarial（注入型メタ検証）であることと cross-reference を明記。注入手法（guard monkeypatch 無効化・偽装 as_of・mock cursor 不一致注入・BTWindow 共有注入）を機能テストと明確に変える（PATTERNS.md Pitfall 1）。 |
| T-08-SC | Tampering | npm/pip/cargo install | accept | Phase 8 は外部パッケージインストールなし（既存 pytest/pandas/mlxtend/ruff のみ・RESEARCH Package Legitimacy Audit 該当なし）。インストール task なし。 |
</threat_model>

<verification>
- KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/audit/ -v が全 GREEN（4ファイル・SC#2 3ケース + D-06 2テスト）
- ruff check tests/audit/ が GREEN
- 各テスト docstring に SC#2 adversarial または D-06 と cross-reference が含まれる（grep で確認）
- tests/audit/ 配下に requires_db marker なし（KEIBA_SKIP_DB_TESTS=1 で全実行される）
</verification>

<success_criteria>
- SC#2 ケース1（lookahead 注入）adversarial テスト GREEN・docstring で test_pit_cutoff へ cross-reference
- SC#2 ケース2（payout 正欠損注入）adversarial テスト GREEN・docstring で test_label_reconcile へ cross-reference
- SC#2 ケース3（fold race_id 共有注入）adversarial テスト GREEN・docstring で test_group_split へ cross-reference
- D-06（UI read-only 保証 + 再現性スタンプ inline）adversarial テスト GREEN・Phase 7 継承スコープ明示
- KEIBA_SKIP_DB_TESTS=1 で tests/audit/ 全テスト実行・DB 不要
- ruff check GREEN
</success_criteria>

<output>
Create `.planning/phases/08-adversarial-audit-suite/08-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**新規シンボル（tests/audit/ パッケージ）:**
- `tests/audit/__init__.py` — パッケージマーカー
- `tests/audit/conftest.py` — 合成 DataFrame 注入ヘルパー: `_build_label_row(race_key, umaban, **overrides)` / `_build_payout_row(race_key, umaban, **overrides)` / `_build_history_row(race_key, kettonum, as_of, **overrides)`
- `tests/audit/test_audit_features.py::test_lookahead_injection_detected_and_fails` — SC#2 ケース1
- `tests/audit/test_audit_label.py::test_payout_positive_missing_from_labels_detected` — SC#2 ケース2（mock cursor + _mock_cursor ヘルパー複製）
- `tests/audit/test_audit_split.py::test_fold_race_id_shared_detected_and_raises` — SC#2 ケース3（BTWindow 注入）
- `tests/audit/test_audit_ui_csv.py::test_ui_write_sql_injection_detected` / `test_reproducibility_stamp_missing_detected` — D-06（_extract_sql_literals / _contains_write_ddl / _REPRO_STAMPS ヘルパー）

**新規 CLI フラグ:** なし（テストのみ・スクリプトなし）

**新規ファイルパス:** `tests/audit/__init__.py` / `tests/audit/conftest.py` / `tests/audit/test_audit_features.py` / `tests/audit/test_audit_label.py` / `tests/audit/test_audit_split.py` / `tests/audit/test_audit_ui_csv.py`

**import する既存シンボル（READ only）:** `src.features.rolling.build_rolling_features` / `src.etl.label_reconcile._check_payout_recall` / `src.etl.label_reconcile.reconcile_against_payout` / `src.etl.quality_gate.CheckResult` / `src.utils.group_split.BTWindow` / `src.utils.group_split.BT_WINDOWS` / `src.utils.group_split.get_bt_race_ids` / `src.ui.csv_columns.PREDICTION_CSV_COLUMNS` / `tests.features.conftest._build_adversarial_rolling_rows`（利用可能場合）
