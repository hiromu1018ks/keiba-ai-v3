---
phase: 08-adversarial-audit-suite
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified: []
autonomous: false
requirements:
  - TEST-01
user_setup:
  - service: postgresql
    why: "KEIBA_SKIP_DB_TESTS unset 全実行に live PostgreSQL（everydb2）接続が必要（D-04・SC#1 GREEN 証明）"
    env_vars:
      - name: KEIBA_DATABASE_URL
        source: ".env（PostgreSQL everydb2 接続文字列・Phase 1 で設定済み）"

must_haves:
  truths:
    - "D-04: KEIBA_SKIP_DB_TESTS を unset し live PostgreSQL（everydb2）の全 requires_db テストを含むフルスイートを実行して GREEN を証明する"
    - "SC#1 GREEN 証明: フルスイート実行で 0 skipped・全 requires_db テスト（14ファイル・38+ テスト）が実行される"
    - "memory phase7-ui-live-db-bugs 対処: live-DB でしか発覚しない bug（sys.path ガード・SQL 引用符・Streamlit deprecation）が unset 全実行で検出される"
    - "フルスイート GREEN の証憑（pytest 出力の passed/skipped 数）を人間が確認し approved する"
    - "SC#3 reproducibility smoke も GREEN であることを確認（Plan 08-02 成果物・合成層 + 08-03 で実行する live-DB 必須 CLI 層）"
    - "08-02 から委譲された SC#3 live-DB 必須 CLI（run_train_predict --check-reproduce・run_backtest --bt-filter BT-1 --check-reproduce）が live-DB で GREEN であることを確認（F-02/F-03 対応）"
  artifacts: []
  key_links:
    - from: "checkpoint:human-verify (live-DB full suite)"
      to: "reports/08-audit.md（Plan 08-02 生成）"
      via: "フルスイート GREEN の passed/skipped 数を人間が確認し・reports/08-audit.md の『フルスイート GREEN 証明』セクションと整合することを verify"
      pattern: "passed|skipped"
---

<objective>
D-04（KEIBA_SKIP_DB_TESTS unset 全実行）で SC#1 GREEN を live-DB で証明し・Phase 8 全体の出荷ゲートを人間が承認する。Phase 4 踏襲（38 requires_db 全実行・262 passed・0 skipped の先例）。memory `phase7-ui-live-db-bugs`（live-DB でしか発覚しない bug）への対処を兼ねる。加えて・08-02 から委譲された SC#3 live-DB 必須 CLI（run_train_predict --check-reproduce・run_backtest --bt-filter BT-1 --check-reproduce）を本 checkpoint で実行し GREEN を確認する（F-02/F-03 対応・08-02 frontmatter は autonomous: true / user_setup: [] を維持するため live-DB CLI を 08-03 に集約）。

Purpose: TEST-01 SC#1「フルスイート GREEN」を live-DB で証明し・Phase 8（v1 マイルストーン最終フェーズ）の出荷判定資料を完成させる。KEIBA_SKIP_DB_TESTS=1（開発時の一時回避）で skip される live-DB テストが、unset で全て実行され GREEN であることを人間が確認する（D-04・conftest.py fail-by-default policy）。
Output: checkpoint:human-verify の approved シグナル。reports/08-audit.{md,json}（Plan 08-02 生成）の「フルスイート GREEN 証明」セクションと live-DB 実行結果の整合確認。新規ファイル変更なし（検証のみ・files_modified 空）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-adversarial-audit-suite/08-CONTEXT.md
@.planning/phases/08-adversarial-audit-suite/08-RESEARCH.md
@.planning/phases/08-adversarial-audit-suite/08-01-SUMMARY.md
@.planning/phases/08-adversarial-audit-suite/08-02-SUMMARY.md

# 実行基盤（READ）
@tests/conftest.py
@pyproject.toml
@reports/08-audit.md
@reports/08-audit.json
@scripts/run_reproducibility_smoke.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: KEIBA_SKIP_DB_TESTS unset live-DB フルスイート GREEN 証明（D-04 / SC#1）</name>
  <files></files>
  <action>
    Phase 8 の Plan 08-01（tests/audit/ adversarial テスト4ファイル）と Plan 08-02（scripts/run_reproducibility_smoke.py + src/audit/report.py + reports/08-audit.{md,json}）が完了済みの前提で・本 task は KEIBA_SKIP_DB_TESTS を unset して live PostgreSQL（everydb2）の全 requires_db テストを含むフルスイートを実行し GREEN を人間が確認・承認する検証 task とする（per D-04）。ファイル変更なし（検証のみ・files_modified 空）。実行内容は how-to-verify に記載のコマンド（unset KEIBA_SKIP_DB_TESTS && uv run pytest tests/ -q・uv run python scripts/run_reproducibility_smoke.py）を人間が実行し・reports/08-audit.md（Plan 08-02 生成）と live-DB 実行結果の整合を確認する。Claude 側は事前自動化可能な検証（KEIBA_SKIP_DB_TESTS=1 での DB 不要層テスト・ruff check・reports/08-audit の presence assert）を実施済みの上で checkpoint に進む。
  </action>
  <what-built>
    Phase 8 の Plan 08-01（tests/audit/ adversarial テスト4ファイル）と Plan 08-02（scripts/run_reproducibility_smoke.py + src/audit/report.py + reports/08-audit.{md,json}）が完了済み。本 task は KEIBA_SKIP_DB_TESTS を unset して live PostgreSQL（everydb2）の全 requires_db テストを含むフルスイートを実行し・GREEN を人間が確認・承認する検証 task（per D-04）。ファイル変更なし（検証のみ）。
  </what-built>
  <how-to-verify>
    1. PostgreSQL（everydb2）が起動し .env の KEIBA_DATABASE_URL で接続可能であることを確認（pg_isready または psql 接続テスト）。
    2. KEIBA_SKIP_DB_TESTS を unset してフルスイートを実行:
       ```
       unset KEIBA_SKIP_DB_TESTS
       uv run pytest tests/ -q
       ```
       期待結果: 全テスト GREEN・**0 skipped**（38+ requires_db テスト全実行・memory phase7-ui-live-db-bugs の live-DB 必須 bug が検出されなければ GREEN）。
       期待実行時間: ~90-180 秒（491+ テスト・live-DB 含む）。
    3. 実行結果の passed / skipped / failed 数を確認:
       - skipped == 0（KEIBA_SKIP_DB_TESTS unset で全 requires_db 実行・conftest.py fail-by-default policy）
       - failed == 0
       - passed は 491+（既存 + Plan 08-01 の tests/audit/ 新規テスト）
    4. SC#3 reproducibility smoke（合成層）も GREEN であることを確認:
       ```
       uv run python scripts/run_reproducibility_smoke.py
       ```
       期待結果: exit=0・2 step 全 PASS（SC#4 calibrator bit-identical / SC#2 adversarial tests）。live-DB 不要。※ trainer bit-identical 群は現状（commit 731f679 時点）tests/model/test_trainer.py に該当テスト0件のため step から除外（NC-03・将来追加時に戻すこと）。
    5. 08-02 から委譲された SC#3 live-DB 必須 CLI 層を実行（F-02/F-03 対応）。**NC-01 対応: run_backtest --bt-filter BT-1 --check-reproduce --no-write-db は --check-reproduce ブロック（run_backtest.py:1070-1093）を終えた後に return せず通常 BT窓ループ（L1098+）に継続し・L1289-1295 の generate_report で既存の reports/05-backtest.{md,json} を BT-1 単窓の結果で上書きする副作用がある（--no-write-db は DB 書込 skip のみ・レポート書込は skip しない）。これは本 plan「検証のみ・files_modified 空」の契約と矛盾するため・実行前に reports/05-backtest を tmp dir に退避し・実行後に復元して契約を守ること。**手順:
       ```
       # 5a. reports/05-backtest の退避（NC-01・Phase 5 成果物を破壊しないため）
       STAMP=$(date +%Y%m%d%H%M%S)
       cp reports/05-backtest.md "/tmp/reports-05-backtest.md.${STAMP}.bak"
       cp reports/05-backtest.json "/tmp/reports-05-backtest.json.${STAMP}.bak"
       ORIG_MD_SHA=$(shasum reports/05-backtest.md | awk '{print $1}')
       ORIG_JSON_SHA=$(shasum reports/05-backtest.json | awk '{print $1}')

       # 5b. SC#3 live-DB CLI 層を実行
       uv run python scripts/run_train_predict.py --check-reproduce --no-write-db
       uv run python scripts/run_backtest.py --bt-filter BT-1 --check-reproduce --no-write-db

       # 5c. reports/05-backtest を復元（NC-01・契約「files_modified 空」を守る）
       cp "/tmp/reports-05-backtest.md.${STAMP}.bak" reports/05-backtest.md
       cp "/tmp/reports-05-backtest.json.${STAMP}.bak" reports/05-backtest.json

       # 5d. 復元検証（byte-identical・本 plan が reports/05-backtest を変更していないことの証憑）
       test "$(shasum reports/05-backtest.md | awk '{print $1}')" = "$ORIG_MD_SHA"
       test "$(shasum reports/05-backtest.json | awk '{print $1}')" = "$ORIG_JSON_SHA"
       ```
       注意: run_backtest には --synthetic を付けないこと（--synthetic 時は readonly_pool=None となりラベル未結合で ValueError で非零 exit する・F-02）。run_train_predict には --synthetic flag は存在しない（F-03）。両コマンドとも live-DB（everydb2）必須・KEIBA_DATABASE_URL が .env に設定済みであること。
       期待結果: 両コマンドとも exit=0（SC#4 bit-identical 検証 PASS）・かつ 5d の復元検証が exit=0（reports/05-backtest が byte-identical で復元され・本 plan の files_modified 契約が保持される）。
    6. reports/08-audit.md（Plan 08-02 生成）を確認:
       - 「サーフェス別カバレッジマップ」に SC#1 #1-#8 の8サーフェス + evaluation_metrics（TEST-01 評価指標計算・F-05）が COVERED / ADVERSARIAL / COVERED+ADVERSARIAL で記載
       - 「SC#1/#2/#3 対応表」に SC#2 の3ケース（lookahead/payout/fold）と D-06 の対応が明示
       - 「Known Limitations」に回収率天井・Calibration劣位・odds JODDS再検証の3項目が隠さず開示（D-05）
       - 「フルスイート GREEN 証明」セクションが上記 2-3 の live-DB 実行結果と整合（passed/skipped 数が actual と一致・または checkpoint 08-03 approved を参照）
    7. もし skipped > 0 または failed > 0 の場合: 原因調査（.env 未設定・DB 未起動・live-DB bug 検出）。memory phase7-ui-live-db-bugs の先例（sys.path ガード・SQL 引用符・Streamlit deprecation）を参考に修正が必要か判断。修正後 再実行。
  </how-to-verify>
  <resume-signal>Type "approved"（フルスイート GREEN・0 skipped・SC#3 smoke 合成層 GREEN・SC#3 live-DB CLI 層 GREEN・reports/08-audit 整合を確認）または失敗時は失敗内容を記述</resume-signal>
  <verify>
    <automated>KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/ -q && ruff check tests/audit/ scripts/run_reproducibility_smoke.py src/audit/</automated>
    <human-check>unset KEIBA_SKIP_DB_TESTS && uv run pytest tests/ -q が GREEN・0 skipped（38+ requires_db 全実行）・uv run python scripts/run_reproducibility_smoke.py が exit=0（合成層）・how-to-verify step 5 の退避→実行→復元手順に従い run_train_predict --check-reproduce --no-write-db / run_backtest --bt-filter BT-1 --check-reproduce --no-write-db が exit=0（live-DB CLI 層・08-02 から委譲）・かつ reports/05-backtest.{md,json} の SHA が実行前後で byte-identical に復元されること（NC-01・本 plan files_modified 空契約の保持）・reports/08-audit.md のフルスイート GREEN 証明セクションと live-DB 実行結果が整合することを人間が確認</human-check>
  </verify>
  <read_first>
    - tests/conftest.py（pytest_collection_modifyitems L66-78: KEIBA_SKIP_DB_TESTS=1 の時のみ requires_db を skip・unset で fail-by-default policy・D-04 ゲート基盤）
    - pyproject.toml（[tool.pytest.ini_options] L43-46: testpaths=["tests"]・addopts="-ra"・markers=["requires_db: ..."]）
    - reports/08-audit.md（Plan 08-02 生成・フルスイート GREEN 証明セクションの整合確認対象）
    - reports/08-audit.json（Plan 08-02 生成・byte-reproducible 確認）
    - scripts/run_reproducibility_smoke.py（Plan 08-02 生成・SC#3 smoke 合成層実行対象・live-DB 不要）
    - scripts/run_train_predict.py（SC#3 live-DB CLI 層・L125 --check-reproduce / L130 --no-write-db / L225 Settings() / L253 make_pool(role="readonly")・**--synthetic flag は非存在**・L257-261 が _prepare_feature_df(readonly_pool) → load_labels(cur) を呼ぶ・08-02 から委譲）
    - scripts/run_backtest.py（SC#3 live-DB CLI 層・L148 --check-reproduce / L159 --no-write-db / L143 --bt-filter・**--synthetic を付けると readonly_pool=None でラベル未結合→ValueError で非零 exit**（L1031/L1308-1317/orchestrator L341）・08-02 から委譲）
    - .planning/phases/08-adversarial-audit-suite/08-RESEARCH.md（Pitfall 2: KEIBA_SKIP_DB_TESTS unset で見えない live-DB bug・Validation Architecture Phase Requirements → Test Map）
  </read_first>
  <acceptance_criteria>
    - KEIBA_SKIP_DB_TESTS unset で `uv run pytest tests/ -q` が GREEN（failed == 0）
    - skipped == 0（全 requires_db テスト実行・conftest.py fail-by-default policy 確証）
    - passed >= 491（既存491 + Plan 08-01 の tests/audit/ 新規テスト）
    - `uv run python scripts/run_reproducibility_smoke.py` が exit=0（SC#3 smoke 合成層 GREEN・2 step 全 PASS・live-DB 不要・NC-03: trainer bit-identical 群は現状該当テスト0件のため除外）
    - `uv run python scripts/run_train_predict.py --check-reproduce --no-write-db` が exit=0（SC#3 live-DB CLI 層・08-02 から委譲・F-03 対応）
    - `uv run python scripts/run_backtest.py --bt-filter BT-1 --check-reproduce --no-write-db` が exit=0（SC#3 live-DB CLI 層・**--synthetic なし**・08-02 から委譲・F-02 対応）・かつ実行前後で reports/05-backtest.{md,json} の SHA が byte-identical（NC-01: how-to-verify step 5 の退避→復元手順で既存 Phase 5 成果物を破壊せず・files_modified 空契約を保持）
    - reports/08-audit.md の「フルスイート GREEN 証明」セクションが live-DB 実行結果と整合
    - 人間が "approved" を入力
  </acceptance_criteria>
  <done>KEIBA_SKIP_DB_TESTS unset live-DB フルスイート GREEN（0 skipped）+ SC#3 smoke GREEN + reports/08-audit 整合 を人間が確認し approved。Phase 8（v1 マイルストーン最終フェーズ）の出荷ゲート証憠が完成。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live PostgreSQL（everydb2）→ pytest requires_db テスト | live-DB 接続（.env KEIBA_DATABASE_URL）経由で requires_db テストが実 DB にアクセス。read-only（SELECT のみ）想定・書き込みなし。 |
| 人間承認 → 出荷ゲート | checkpoint:human-verify の approved シグナルが Phase 8 完了の最終承認。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | Repudiation | KEIBA_SKIP_DB_TESTS が unset でないと見えない live-DB bug を silent skip で「GREEN」と誤認 | mitigate | unset でフル実行し skipped == 0 を必須条件とする（D-04・conftest.py fail-by-default policy）。memory phase7-ui-live-db-bugs（sys.path ガード・SQL 引用符・Streamlit deprecation が unit test SKIP では検出不可だった実績）への直接対処。 |
| T-08-10 | Tampering | requires_db テストの silent skip（.env 未設定で Settings validation error が skip に化ける） | mitigate | acceptance_criteria で skipped == 0 を硬い条件とする。conftest.py は unset で Settings validation error で fail（skip に化けない）・policy 整合を人間が確認。 |
| T-08-11 | Information Disclosure | 出荷ゲート承認が実態と乖離（reports/08-audit の GREEN 証明が偽） | mitigate | reports/08-audit.md（Plan 08-02 生成）と live-DB 実行の passed/skipped 数を人間が突合（how-to-verify step 5）。整合しない場合は approved しない。 |
| T-08-SC | Tampering | npm/pip/cargo install | accept | Phase 8 は外部パッケージインストールなし。本 task は検証のみ・インストールなし。 |
</threat_model>

<verification>
- KEIBA_SKIP_DB_TESTS unset で `uv run pytest tests/ -q` が GREEN・0 skipped
- `uv run python scripts/run_reproducibility_smoke.py` が exit=0（合成層）
- `uv run python scripts/run_train_predict.py --check-reproduce --no-write-db` が exit=0（live-DB CLI 層・08-02 委譲）
- `uv run python scripts/run_backtest.py --bt-filter BT-1 --check-reproduce --no-write-db` が exit=0（live-DB CLI 層・--synthetic なし・08-02 委譲）・かつ reports/05-backtest.{md,json} が実行前後で byte-identical に復元される（NC-01・files_modified 空契約保持）
- reports/08-audit.md の「フルスイート GREEN 証明」セクションと live-DB 実行結果が整合
- 人間が "approved" を入力
</verification>

<success_criteria>
- D-04 KEIBA_SKIP_DB_TESTS unset live-DB フルスイート GREEN（0 skipped・全 requires_db 実行）
- SC#3 reproducibility smoke 合成層 GREEN（Plan 08-02 成果物・DB 不要 pytest のみ）+ SC#3 live-DB CLI 層 GREEN（run_train_predict --check-reproduce / run_backtest --bt-filter BT-1 --check-reproduce・08-02 から委譲・F-02/F-03 対応）
- reports/08-audit.{md,json}（Plan 08-02 生成）が live-DB 実行結果と整合・Known Limitations 3項目 honest 開示
- 人間承認（checkpoint approved）で Phase 8 / v1 マイルストーン出荷ゲート完成
</success_criteria>

<output>
Create `.planning/phases/08-adversarial-audit-suite/08-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**新規シンボル:** なし（本 plan は checkpoint:human-verify の検証のみ・ファイル変更なし・files_modified 空）

**新規 CLI フラグ:** なし

**新規ファイルパス:** なし（reports/08-audit.{md,json} は Plan 08-02 が生成・本 plan は検証のみ）

**検証対象（READ only）:** `tests/`（全491+ テスト・KEIBA_SKIP_DB_TESTS unset で実行） / `scripts/run_reproducibility_smoke.py`（Plan 08-02 生成） / `reports/08-audit.{md,json}`（Plan 08-02 生成） / `tests/conftest.py`（skip policy） / `pyproject.toml`（pytest 設定）
