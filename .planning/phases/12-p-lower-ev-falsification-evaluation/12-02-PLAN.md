---
phase: 12-p-lower-ev-falsification-evaluation
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/model/orchestrator.py
  - src/model/predict.py
  - src/model/artifact.py
  - src/ev/ev_rank.py
  - src/ev/purchase_simulator.py
  - src/ev/report.py
  - tests/model/test_orchestrator_p_lower.py
  - tests/ev/test_ev_p_lower.py
autonomous: true
requirements: [EV-01]
tags: [model, orchestrator, ev, p-lower, wave2, ev-01, artifact]
user_setup: []

must_haves:
  truths:
    - "orchestrator.train_and_predict が theta is not None 分岐（race-relative 補正 L714-754）内で compute_p_lower_conformal_shrinkage を calib slice のみで呼出し・pred_df に p_fukusho_hit_lower 列を付与する（theta=None/v1.0 binary の場合は NULL）"
    - "orchestrator が q_shrink 計算に score_split='calib' 経路の calib slice のみを使用し・test 窓 outcome を構造的に触れない（§11.2 聖域・codex HIGH#1・score_split guard）"
    - "[C-12-02-1 / HIGH] orchestrator.train_and_predict の signature に `p_lower_q_shrink: float | None = None`（keyword-only・test 窓に calib 済み q_shrink を外部注入）と `p_lower_q_level: float = 0.90`（keyword-only・事前登録値）が明示され・score_split='test' 経路で theta is not None かつ p_lower_q_shrink is None の場合に RuntimeError で fail-loud する（呼出側 run_phase12_evaluation.py からの受取経路を唯一に固定・test 窓 outcome を使った q_shrink 再計算を構造的に排除）"
    - "orchestrator の return dict に race_relative_theta と並んで p_lower_q_level/p_lower_q_shrink/p_lower_shrinkage_method が含まれる（§19.1 再現性・provenance）"
    - "save_native_artifact の metadata.json に p_lower_q_level/p_lower_q_shrink/p_lower_shrinkage_method が記録される（race_relative_theta idiom と同一）"
    - "[C-12-02-2 / MEDIUM] predict.predict_p_fukusho の signature に `pred_proba_lower: pd.Series | np.ndarray | None = None`（keyword-only・既定 None・v1.0 binary 呼出は後方互換）が追加され・orchestrator L764-781 の呼出で注入される（Wave 1→2 コンパイル整合・Plan 01 の PREDICTION_COLUMNS 拡張と協調・p_lower 列が常に None になる silent バグ回避）"
    - "compute_ev_and_rank が p_fukusho_hit_lower × fuku_odds_lower で EV_lower を計算可能（p_col 引数で差し替え・入力列差し替え・構造変更不要 D-03）"
    - "[C-12-02-3 / MEDIUM] _rank（ev_rank.py L37-70）にも p_col が渡り・EV 計算と rank 条件の確率基準が一致する（S/A/B rank の p_min 判定が p_fukusho_hit 固定でなく p_col に従う・投票層定義の分裂回避）"
    - "purchase_simulator.select_bets が p_min を p_lower ベースで解釈する経路を持ち（Claude's Discretion 事前登録: p_min_base='p_lower'・Pitfall 7）・投票層の定義が明示される"
    - "[C-12-02-4 / MEDIUM] report.REPORT_COLUMNS 拡張（p_lower 系表示）は Phase 5 既存 report dict に p_lower 系キーが無い場合の KeyError を回避するため・Phase 12 専用 reports（reports/12-evaluation/*）に閉じる・または generate_report が .get(c) で欠損キーを NaN 許容する（既存 backtest dict の regression 回避）"
    - "[C-12-02-5 / MEDIUM] src/model/artifact.py の metadata JSON 書き出しが json.dumps(..., allow_nan=False) で厳密化され・q_shrink が NaN のまま JSON 化される §19.1 違反（JSON 仕様違反の NaN リテラル）を fail-loud で検出する"
    - "EV 計算・purchase_simulator が byte-reproducible（mergesort sort・seed 非依存・入力列差し替えのみで決定論的）"
  artifacts:
    - "src/model/orchestrator.py::train_and_predict L754 後に p_lower 生成ブロック追加（theta is not None 分岐内）"
    - "src/model/orchestrator.py::return dict に p_lower_q_level/p_lower_q_shrink/p_lower_shrinkage_method 追加"
    - "[C-12-02-1 / HIGH] src/model/orchestrator.py::train_and_predict シグネチャに p_lower_q_shrink: float | None = None（keyword-only・test 窓外部注入）と p_lower_q_level: float = 0.90（keyword-only・事前登録値）を追加・score_split='test' 経路で theta is not None かつ p_lower_q_shrink is None の場合に RuntimeError で fail-loud"
    - "[C-12-02-2 / MEDIUM] src/model/predict.py::predict_p_fukusho シグネチャに pred_proba_lower: pd.Series | np.ndarray | None = None（keyword-only・既定 None）を追加・orchestrator L764-781 の呼出で注入"
    - "src/model/artifact.py::save_native_artifact シグネチャに p_lower_q_level/p_lower_q_shrink/p_lower_shrinkage_method keyword-only 引数"
    - "src/model/artifact.py::save_native_artifact metadata dict に p_lower 3キー追加"
    - "[C-12-02-5 / MEDIUM] src/model/artifact.py の metadata JSON 書き出しを json.dumps(..., allow_nan=False) で厳密化（NaN の JSON 化を fail-loud）"
    - "src/ev/ev_rank.py::compute_ev_and_rank に p_col keyword-only 引数（既定 'p_fukusho_hit'・'p_fukusho_hit_lower' で差し替え可能）"
    - "[C-12-02-3 / MEDIUM] src/ev/ev_rank.py::_rank に p_col 引数を伝播（compute_ev_and_rank の apply 経由）・EV と rank の確率基準一致"
    - "src/ev/purchase_simulator.py::select_bets に p_col/p_min_base keyword-only 引数（事前登録: p_min_base='p_lower'）"
    - "[C-12-02-4 / MEDIUM] src/ev/report.py の REPORT_COLUMNS 拡張は Phase 12 専用 reports（reports/12-evaluation/*）に閉じるか・generate_report が .get(c) で欠損キーを NaN 許容（Phase 5 既存 report の regression 回避）"
    - "tests/model/test_orchestrator_p_lower.py（新規・EV-01・calib slice 聖域・byte-reproducible・q_shrink 外部注入 API・fail-loud）"
    - "tests/ev/test_ev_p_lower.py（新規・EV-01・EV_lower=p_lower×odds_lower・p_min_base='p_lower'・_rank p_col 伝播）"
  key_links:
    - "orchestrator L754（race-relative 補正後 pred_proba = pd.Series(p_final, ...)）の直後で q_shrink 計算 + p_lower 生成を挿入・pred_proba と index 整合"
    - "orchestrator return dict → save_native_artifact → metadata.json（§19.1）"
    - "predict_p_fukusho の pred_df 構築で p_fukusho_hit_lower 列が付与済み（Plan 01 の PREDICTION_COLUMNS 拡張・orchestrator から pred_proba_lower を注入）"
    - "compute_ev_and_rank(p_col='p_fukusho_hit_lower') → purchase_simulator.select_bets(p_col='p_fukusho_hit_lower', p_min_base='p_lower') → refund_accounting（既存）"
  prohibitions:
    - "orchestrator は test 窓の outcome を q_shrink 計算に使用しない（score_split='calib' 経路・codex HIGH#1・§11.2 聖域）"
    - "EV 層（ev_rank / purchase_simulator）は FEATURE_COLUMNS を import しない・SAFE-01 聖域（odds は EV 計算層で使用するが feature 構築経路から切り離す）"
    - "p_min_base の事前登録値を test 窓で変更しない（§11.2 聖域・Shared Pattern 1・Pitfall 7）"
    - "ev_rank/purchase_simulator の sort は mergesort（seed 非依存）・byte-reproducible を壊さない"
    - "silent fallback 禁止（pred_proba_lower と pred_proba の index 不整合・長さ不一致は RuntimeError・Shared Pattern 4）"
---

<objective>
Plan 01 の compute_p_lower_conformal_shrinkage と PREDICTION_COLUMNS 拡張を消費し、orchestrator L754（race-relative 補正後）で calib slice のみ q_shrink 計算して p_lower を生成・pred_df に付与する経路を確立する。save_native_artifact の metadata に q_level/q_shrink/shrinkage_method を記録（§19.1 再現性）。EV 層（ev_rank / purchase_simulator）の入力列を p から p_fukusho_hit_lower に差し替え・p_min を p_lower ベースで解釈（D-03・Pitfall 7）。

Purpose: Plan 03-04 の falsification・拡張評価指標・switch_recommendation・run_phase12_evaluation.py が消費する「race-relative 行の p_fukusho_hit_lower 列」と「p_lower ベースの EV 計算」を整える。聖域（§11.2・SAFE-01・byte-reproducible）を保ちつつ・v1.0 binary 行は NULL・theta=None 呼出は後方互換（A5）。

Output: orchestrator p_lower 統合・artifact metadata 拡張・EV 層 p_lower 切替・unit tests。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-p-lower-ev-falsification-evaluation/12-CONTEXT.md
@.planning/phases/12-p-lower-ev-falsification-evaluation/12-RESEARCH.md
@.planning/phases/12-p-lower-ev-falsification-evaluation/12-PATTERNS.md
@.planning/phases/12-p-lower-ev-falsification-evaluation/12-01-SUMMARY.md
@src/model/orchestrator.py
@src/model/race_relative.py
@src/model/artifact.py
@src/model/predict.py
@src/ev/ev_rank.py
@src/ev/purchase_simulator.py
@src/ev/report.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: orchestrator L754 後に p_lower 生成挿入 + q_shrink test 窓外部注入 API（C-12-02-1 HIGH）+ return dict provenance + artifact metadata allow_nan=False（C-12-02-5）</name>
  <files>src/model/orchestrator.py, src/model/predict.py, src/model/artifact.py, tests/model/test_orchestrator_p_lower.py</files>
  <read_first>
    - src/model/orchestrator.py（L273-291 train_and_predict signature・現状 p_lower_q_level/q_shrink 引数なし〔C-12-02-1〕/ L449-455 score_split guard・codex HIGH#1 / L493-495 y_calib/y_test 同時存在・codex HIGH#1 / L706-754 race-relative 補正ブロック theta is not None 分岐 / L747-754 apply_race_relative_correction 呼出 + pred_proba = pd.Series(p_final, ...) / L756-826 predict_p_fukusho 呼出と meta 列付与 / L764-781 predict_p_fukusho 呼出〔pred_proba_lower 注入ポイント・C-12-02-2〕 / L828-846 return dict race_relative_theta）
    - src/model/race_relative.py（L176-259 apply_race_relative_correction の戻り値 p_final・calib slice index についての注記・L383 __all__ の compute_p_lower_conformal_shrinkage〔Plan 01 追加分〕）
    - src/model/predict.py（PREDICTION_COLUMNS 20列〔Plan 01〕・L180-195 predict_p_fukusho signature・現状 pred_proba のみで pred_proba_lower なし〔C-12-02-2〕・DataFrame 構築〔Plan 01 で p_fukusho_hit_lower 列追加済み〕）
    - src/model/artifact.py（L72-94 metadata JSON 書き出し json.dumps(..., sort_keys=True, ensure_ascii=False)・現状 allow_nan=False なし〔C-12-02-5〕 / L98-218 save_native_artifact・L108 race_relative_theta keyword-only 引数・L195-217 metadata dict に race_relative_theta 追加 idiom）
    - .planning/phases/12-p-lower-ev-falsification-evaluation/12-RESEARCH.md（Pattern 1/3・D-01/D-02/D-03・例1 完全呼出経路 L682-693）
    - .planning/phases/12-p-lower-ev-falsification-evaluation/12-PATTERNS.md（orchestrator Pattern L96-146・artifact Pattern L461-492）
    - .planning/phases/12-p-lower-ev-falsification-evaluation/12-REVIEWS.md（C-12-02-1 HIGH・q_shrink test 窓適用契約・C-12-02-2 MEDIUM・pred_proba_lower 注入・C-12-02-5 MEDIUM・allow_nan=False）
  </read_first>
  <behavior>
    - Test 1 (§11.2 聖域・calib slice のみ): train_and_predict(score_split='calib') 呼出で q_shrink 計算が calib slice の pred_proba と y_calib のみで行われる（test 窓 outcome を取らない・score_split guard が L449-455 で保証）
    - Test 2 (theta=None で NULL): theta=None 呼出（v1.0 binary）で pred_df['p_fukusho_hit_lower'] が全て None/NaN（後方互換・A5）
    - Test 3 (theta 指定で p_lower 付与): theta=1.0 呼出で pred_df['p_fukusho_hit_lower'] が非 NULL かつ p_lower <= p_fukusho_hit（D-01・max(0, p_final - q_shrink) <= p_final）
    - Test 4 (byte-reproducible): theta=1.0 で2回 train_and_predict を呼び・p_fukusho_hit_lower 列が np.array_equal で一致（FIXED_REPRODUCE_TS・固定 seed/thread・SC#3 idiom）
    - Test 5 (return dict provenance): return dict に p_lower_q_level=0.90・p_lower_q_shrink=float・p_lower_shrinkage_method='calibration_residual_conformal' が含まれる
    - Test 6 (artifact metadata): save_native_artifact で metadata.json に p_lower_q_level/p_lower_q_shrink/p_lower_shrinkage_method が記録される（race_relative_theta idiom と同一）
    - Test 7 (silent fallback 禁止): pred_proba_lower と pred_proba の index 不整合・長さ不一致で RuntimeError（Shared Pattern 4）
    - Test 8 ([C-12-02-1 / HIGH] q_shrink 外部注入 API・signature): inspect.signature(train_and_predict) の parameters に p_lower_q_shrink: float | None = None と p_lower_q_level: float = 0.90 が keyword-only で含まれる（keyword-only は * 以降・signature.parameters[...].kind == KEYWORD_ONLY）
    - Test 9 ([C-12-02-1 / HIGH] q_shrink test 窓 fail-loud): train_and_predict(score_split='test', theta=1.0, p_lower_q_shrink=None) の呼出が RuntimeError を送出（theta is not None かつ p_lower_q_shrink is None の組み合わせが・test 窓で q_shrink を再計算する経路に滑ることを構造的に阻止）
    - Test 10 ([C-12-02-1 / HIGH] q_shrink 外部注入で p_lower 生成): train_and_predict(score_split='test', theta=1.0, p_lower_q_shrink=<calibで計算した値>) の呼出で pred_df['p_fukusho_hit_lower'] が非 NULL かつ p_lower <= p_fukusho_hit（外部注入 q_shrink を test 窓 p_final に適用・§11.2 聖域・test 窓 outcome を使わない）
    - Test 11 ([C-12-02-2 / MEDIUM] predict_p_fukusho pred_proba_lower 引数): inspect.signature(predict_p_fukusho) の parameters に pred_proba_lower: pd.Series | np.ndarray | None = None が含まれる・None の場合は p_fukusho_hit_lower 列が NULL・非 None の場合は注入された値で列が埋まる
    - Test 12 ([C-12-02-5 / MEDIUM] artifact allow_nan=False): artifact.py の metadata JSON 書き出しが json.dumps(..., allow_nan=False) を呼び・NaN を含む dict を渡した場合に ValueError（または json.dumps が送出する TypeError）で fail-loud（§19.1 厳密性）
  </behavior>
  <action>
    orchestrator.py train_and_predict の signature に [C-12-02-1 / HIGH] `p_lower_q_shrink: float | None = None` と `p_lower_q_level: float = 0.90` の2つの keyword-only 引数を追加（`*` 以降・L273-291 signature 拡張・既定値は事前登録値・docstring に「§11.2 聖域・test 窓で変更不可・Shared Pattern 1」を明記）。p_lower_q_level は score_split='calib' の calib 計算経路でも使用。p_lower_q_shrink は score_split='test' 経路で外部から calib 済み値を注入する唯一の受取経路（呼出側 run_phase12_evaluation.py〔Plan 04〕から・レビュー C-12-02-1）。

    L714-754 の `if theta is not None:` ブロック内・L754 の `pred_proba = pd.Series(p_final, index=X_score.index, name="p_fukusho_hit")` の直後に p_lower 生成ブロックを挿入（RESEARCH.md Pattern 1・例1 L682-693）:

    (a) score_split='calib' 経路（θ選択経路・codex HIGH#1）では calib slice の pred_proba と y_calib（X_score と align した label）を compute_p_lower_conformal_shrinkage に渡し q_shrink と p_lower_calib を計算（p_lower_q_level を q_level として渡す）。score_split='test' 経路では [C-12-02-1 / HIGH] 「theta is not None かつ p_lower_q_shrink is None」の場合に RuntimeError で fail-loud（test 窓で q_shrink を再計算する経路に滑るのを構造的に阻止・`raise RuntimeError("score_split='test' with theta requires p_lower_q_shrink (§11.2 聖域・外部注入のみ)")`）。p_lower_q_shrink is not None の場合は外部注入値を使い max(0, p_final - p_lower_q_shrink) で p_lower を算出（§11.2 聖域・test 窓 outcome を使わない）。

    (b) q_shrink は score_split='calib' 経路で計算した値（return dict に格納）または score_split='test' 経路で p_lower_q_shrink 引数から受け取った値のいずれか。theta=None/v1.0 binary の場合は p_lower=None（NULL・後方互換・p_lower_q_shrink は無視）。

    (c) pred_proba_lower = pd.Series(p_lower, index=X_score.index) を構築。index と長さが pred_proba と一致することを RuntimeError で assert（silent fallback 禁止・Shared Pattern 4・L700-704 の pred_proba.index.equals(X_score.index) と同一 idiom）。

    (d) [C-12-02-2 / MEDIUM] predict_p_fukusho の signature に `pred_proba_lower: pd.Series | np.ndarray | None = None`（keyword-only・既定 None）を追加（L180-195 signature 拡張）。orchestrator L764-781 の呼出で pred_proba_lower を注入。predict_p_fukusho 側は PREDICTION_COLUMNS に p_fukusho_hit_lower が含まれるため〔Plan 01〕・注入された pred_proba_lower を DataFrame の同列に設定（index/長さを assert）・None の場合は NULL（v1.0 binary・後方互換）。本拡張を 12-02 の files_modified に固定することで Wave 1（Plan 01 PREDICTION_COLUMNS 拡張）→ Wave 2（本件 pred_proba_lower 注入）のコンパイル整合を保証（レビュー C-12-02-2・Wave 間 silent None バグ回避）。

    (e) return dict（L828-846）の race_relative_theta の直後に `p_lower_q_level: float | None`・`p_lower_q_shrink: float | None`・`p_lower_shrinkage_method: str | None` を追加（theta=None の場合は全て None・race-relative の場合は事前登録値と計算値・score_split='test' の場合は p_lower_q_shrink 引数の値）。

    artifact.py save_native_artifact に p_lower_q_level/p_lower_q_shrink/p_lower_shrinkage_method の3つの keyword-only 引数を追加（既定 None・race_relative_theta=L108 と同一 idiom）。metadata dict（L213 の race_relative_theta の直後）に3キーを追加・theta=None の場合は None を記録。[C-12-02-5 / MEDIUM] L72-94 の metadata JSON 書き出しを `json.dumps(metadata_dict, sort_keys=True, ensure_ascii=False, allow_nan=False)` に拡張（allow_nan=False 追加・NaN の JSON 化を RFC 8259 strict で fail-loud・§19.1 厳密性・q_shrink が計算不能で NaN のまま metadata に入る silent 再現性破壊を防止）。_sanitize_for_json で NaN → None 変換を先に行うか allow_nan=False で ValueError にするかは planner 裁量・どちらも §19.1 厳密性を満たす。
  </action>
  <verify>
    <automated>KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/model/test_orchestrator_p_lower.py -x -q</automated>
  </verify>
  <done>
    - orchestrator L754 後に p_lower 生成ブロックが存在（`grep -n "compute_p_lower_conformal_shrinkage" src/model/orchestrator.py` で1件以上）
    - [C-12-02-1 / HIGH] train_and_predict の signature に p_lower_q_shrink: float | None = None と p_lower_q_level: float = 0.90 が keyword-only で存在（`python -c "import inspect; from src.model.orchestrator import train_and_predict; p=inspect.signature(train_and_predict).parameters; assert p['p_lower_q_shrink'].kind == inspect.Parameter.KEYWORD_ONLY; assert p['p_lower_q_level'].kind == inspect.Parameter.KEYWORD_ONLY; print('ok')"` で ok）
    - [C-12-02-1 / HIGH] score_split='test', theta=1.0, p_lower_q_shrink=None の呼出で RuntimeError が送出される（fail-loud・§11.2 聖域）
    - [C-12-02-2 / MEDIUM] predict_p_fukusho の signature に pred_proba_lower が keyword-only で存在・None の場合 p_fukusho_hit_lower 列が NULL
    - theta=None 呼出で pred_df['p_fukusho_hit_lower'] が全て None/NaN・theta=1.0 で非 NULL かつ p_lower <= p_fukusho_hit
    - return dict に p_lower_q_level/p_lower_q_shrink/p_lower_shrinkage_method が含まれる
    - save_native_artifact metadata.json に3キーが記録される
    - [C-12-02-5 / MEDIUM] artifact.py の metadata JSON 書き出しが allow_nan=False（`grep -c 'allow_nan=False' src/model/artifact.py` >= 1）
    - byte-reproducible（2回 train_and_predict で p_fukusho_hit_lower が np.array_equal）
    - KEIBA_SKIP_DB_TESTS=1 で tests/model/test_orchestrator_p_lower.py 全 GREEN
    - KEIBA_SKIP_DB_TESTS=1 で既存 test（tests/model/test_orchestrator*.py・test_artifact*.py）が regression なし
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EV 層 p_lower 切替（ev_rank p_col 引数 + purchase_simulator p_min_base='p_lower' 事前登録 + report.py p_lower 表示）</name>
  <files>src/ev/ev_rank.py, src/ev/purchase_simulator.py, src/ev/report.py, tests/ev/test_ev_p_lower.py</files>
  <read_first>
    - src/ev/ev_rank.py（L15-18 RANK_THRESHOLDS コメント S/A/B/C/D / L29-34 RANK_THRESHOLDS dict / L37-77 _rank row 単位 / L80-113 compute_ev_and_rank: L107-113 `out["EV_lower"] = out["p_fukusho_hit"] * out["fuku_odds_lower"]`）
    - src/ev/purchase_simulator.py（L17-20 select_bets 条件 EV_lower>=1.05/p_fukusho_hit>=0.15/fuku_odds_lower>=1.5 / L33-36 FUKUSHO_EV_V1_THRESHOLDS p_min=0.15 / L86-114 select_bets 3条件 filter + mergesort sort）
    - src/ev/report.py（L41-53 REPORT_COLUMNS / L176-274 generate_report / L250-272 json.dumps sort_keys=True byte-reproducible idiom）
    - .planning/phases/12-p-lower-ev-falsification-evaluation/12-RESEARCH.md（Pattern 3・D-03・Pitfall 7・Claude's Discretion「SC#4 gate の具体的閾値」との整合）
    - .planning/phases/12-p-lower-ev-falsification-evaluation/12-PATTERNS.md（ev_rank Pattern L271-294 / purchase_simulator Pattern L297-318 / report Pattern L367-391）
  </read_first>
  <behavior>
    - Test 1 (EV_lower=p_lower×odds_lower): compute_ev_and_rank(df, p_col='p_fukusho_hit_lower') で EV_lower = p_fukusho_hit_lower × fuku_odds_lower（D-03・入力列差し替え）
    - Test 2 (p_col 既定値後方互換): compute_ev_and_rank(df)（p_col 省略）は従来通り p_fukusho_hit × fuku_odds_lower（v1.0 binary 呼出互換）
    - Test 3 (purchase_simulator p_min_base='p_lower'): select_bets(df, p_col='p_fukusho_hit_lower', p_min_base='p_lower') で p_min=0.15 を p_fukusho_hit_lower に対して適用（Pitfall 7・投票層の定義明示）
    - Test 4 (p_min_base='p' 従来): select_bets(df, p_min_base='p') は p_fukusho_hit >= 0.15（従来・後方互換）
    - Test 5 (mergesort byte-reproducible): select_bets を2回呼出し・selected 行が bit-identical（mergesort・seed 非依存）
    - Test 6 ([C-12-02-4 / MEDIUM] report p_lower 表示・既存 report regression なし): generate_report で REPORT_COLUMNS に p_lower 系（p_fukusho_hit_lower・q_level・q_shrink）が表示される・かつ p_lower 系キーが欠損した既存 backtest dict（v1.0 binary・Phase 11 race-relative）を渡しても KeyError にならず NaN/省略で表示される（.get(c) または Phase 12 専用 reports への分離）
    - Test 7 (SAFE-01・EV 層): ev_rank / purchase_simulator が FEATURE_COLUMNS を import しない（`python -c "import inspect,ast; from src.ev import ev_rank,purchase_simulator; [print(m.__name__, sum(1 for n in ast.walk(ast.parse(inspect.getsource(m))) if isinstance(n,ast.ImportFrom) and (n.module or '').endswith('FEATURE_COLUMNS'))) for m in [ev_rank,purchase_simulator]]"` == 0）
    - Test 8 ([C-12-02-3 / MEDIUM] _rank p_col 伝播): _rank 関数が p_col='p_fukusho_hit_lower' を受け取り・S/A/B rank の p_min 判定が p_fukusho_hit ではなく p_fukusho_hit_lower に対して行われる（EV 計算と rank 条件の確率基準が一致・投票層定義の分裂回避）。compute_ev_and_rank 側で _rank に p_col を apply 経由で伝播する実装（row.get(p_col) で参照）。
  </behavior>
  <action>
    ev_rank.compute_ev_and_rank に p_col keyword-only 引数を追加（既定 'p_fukusho_hit'・後方互換）。L107-113 の `out["EV_lower"] = out["p_fukusho_hit"] * out["fuku_odds_lower"]` を `out["EV_lower"] = out[p_col] * out["fuku_odds_lower"]` に・`out["EV_upper"] = out[p_col] * out["fuku_odds_upper"]` に変更（D-03・入力列差し替え）。[C-12-02-3 / MEDIUM] _rank 関数（L37-77）に p_col 引数を追加し・compute_ev_and_rank が apply で渡す形（`out.apply(lambda row: _rank(row, p_col=p_col), axis=1)` 相当）。_rank 内の `row.get("p_fukusho_hit")` を `row.get(p_col)` に変更し・S/A/B rank の p_min 判定が p_col に従う（EV 計算と rank 条件の確率基準一致・投票層定義の分裂回避・レビュー C-12-02-3）。最も単純な _rank p_col 伝播実装（apply 経由）を採用・後方互換（p_col 省略時は 'p_fukusho_hit'）。

    purchase_simulator.select_bets に p_col と p_min_base の2つの keyword-only 引数を追加（既定 p_col='p_fukusho_hit', p_min_base='p'・後方互換）。事前登録値（Claude's Discretion「SC#4 gate の具体的閾値」と整合・Shared Pattern 1）: p_min_base='p_lower' が Phase 12 のデフォルト経路。L95-97 の `eligible["p_fukusho_hit"] >= FUKUSHO_EV_V1_THRESHOLDS["p_min"]` を・p_min_base='p_lower' の場合は `eligible[p_col] >= FUKUSHO_EV_V1_THRESHOLDS["p_min"]` に解釈（p_col='p_fukusho_hit_lower'）。p_min_base='p' の場合は従来通り p_fukusho_hit。docstring に Pitfall 7（投票層の定義明示）と事前登録値を記載。

    [C-12-02-4 / MEDIUM] report.py REPORT_COLUMNS に p_fukusho_hit_lower / p_lower_q_level / p_lower_q_shrink の表示列を追加する場合・generate_report は REPORT_COLUMNS 全キーが各 backtest dict にある前提（L176-189）のため・p_lower 系キーが欠損した既存 backtest dict（v1.0 binary・Phase 11 race-relative）で KeyError になるのを回避する。実装選択（planner 裁量）: (A) Phase 12 専用 reports（reports/12-evaluation/*）に閉じるための generate_report_phase12 関数を新設し generate_report は不変・または (B) generate_report の row 構築を `row.get(c)`（欠損時 NaN/None）に変更し REPORT_COLUMNS 拡張が既存 report に regression しない。どちらも Phase 5 既存 report（REPORT_COLUMNS 全キー前提）への影響を回避する。json payload に q_level/q_shrink/shrinkage_method を追加（L250-272・sort_keys=True・byte-reproducible）。switch_recommendation・falsification 結果の表示列は Plan 04 で追加（本 plan では p_lower 系のみ）。

    ev_rank / purchase_simulator は FEATURE_COLUMNS を import しない（SAFE-01・EV 計算層で odds を使うが feature 構築経路から切り離す）。sort は mergesort を維持（byte-reproducible）。
  </action>
  <verify>
    <automated>KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/ev/test_ev_p_lower.py tests/ev/ -x -q</automated>
  </verify>
  <done>
    - compute_ev_and_rank に p_col keyword-only 引数（既定 'p_fukusho_hit'）・p_col='p_fukusho_hit_lower' で EV_lower=p_lower×odds_lower
    - [C-12-02-3 / MEDIUM] _rank にも p_col が伝播し S/A/B rank の p_min 判定が p_col に従う（EV と rank の確率基準一致）
    - select_bets に p_col / p_min_base keyword-only 引数・p_min_base='p_lower' が Phase 12 デフォルト経路
    - mergesort で2回 select_bets が bit-identical
    - [C-12-02-4 / MEDIUM] report.REPORT_COLUMNS に p_fukusho_hit_lower / q_level / q_shrink が含まれ・かつ既存 backtest dict（p_lower 系キー欠損）で KeyError にならない（.get(c) または Phase 12 専用 reports 分離）
    - ev_rank / purchase_simulator が FEATURE_COLUMNS を import しない（SAFE-01）
    - KEIBA_SKIP_DB_TESTS=1 で tests/ev/test_ev_p_lower.py と既存 tests/ev/ が全 GREEN（後方互換）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator score_split='test' 経路 ↔ q_shrink 計算 | test 窓の outcome を q_shrink 計算に混入させない・q_shrink は calib slice または外部注入のみ（§11.2 聖域） |
| EV 計算層 ↔ feature 構築経路 | ev_rank / purchase_simulator が FEATURE_COLUMNS を import すると odds/ninki proxy が feature に混入（SAFE-01） |
| pred_proba ↔ pred_proba_lower | index/長さ不整合は silent に誤った馬の p_lower を付与（Shared Pattern 4） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-06 | Tampering / Information disclosure | orchestrator L754 後の q_shrink 計算で score_split='test' 経路が test 窓 outcome を使用（§11.2 聖域違反） | high | mitigate | q_shrink は calib slice 経路（score_split='calib'）または呼出側（run_phase12_evaluation.py・Plan 04）から外部注入のみ・orchestrator 内部で test 窓 outcome を再計算に使わない。score_split guard（L449-455・codex HIGH#1）が構造的聖域ブロック。test_orchestrator_p_lower.py の Test 1 で検証 |
| T-12-07 | Tampering | ev_rank / purchase_simulator が FEATURE_COLUMNS を import し odds/ninki が feature に混入（SAFE-01 違反） | high | mitigate | ev_rank / purchase_simulator は FEATURE_COLUMNS / build_training_frame / load_feature_matrix を import しない。test_ev_p_lower.py の Test 7 と tests/audit/test_audit_p_lower_falsification.py（Plan 05）で AST 検証。EV 計算層は odds を消費するが feature 構築経路から切り離す（層分離・domain-analysis §5） |
| T-12-08 | Tampering | pred_proba と pred_proba_lower の index/長さ不整合で silent に誤馬の p_lower を付与 | medium | mitigate | orchestrator L754 後のブロックで pred_proba_lower.index.equals(pred_proba.index) と len() を RuntimeError で assert（Shared Pattern 4・L700-704 と同一 idiom）。test_orchestrator_p_lower.py の Test 7 で検証 |
| T-12-09 | Tampering | p_min_base の事前登録値を test 窓で変更（§11.2 聖域・Pitfall 7） | medium | mitigate | purchase_simulator.select_bets の p_min_base は keyword-only 引数・docstring に事前登録値（'p_lower' が Phase 12 デフォルト）と §11.2 聖域を明記。test 窓評価は run_phase12_evaluation.py（Plan 04）が事前登録値で固定呼出 |
| T-12-10 | Repudiation | save_native_artifact metadata に q_level/q_shrink が記録されず再現性破壊（§19.1） | medium | mitigate | save_native_artifact の metadata dict に3キーを必ず追加・theta=None の場合は None を記録。race_relative_theta idiom と同一・metadata.json sort_keys=True で byte-reproducible。test_orchestrator_p_lower.py の Test 6 で検証 |
</threat_model>

<verification>
- orchestrator L754 後に p_lower 生成ブロック・theta=None で NULL・theta=1.0 で p_lower <= p_fukusho_hit・byte-reproducible
- return dict + artifact metadata に p_lower_q_level/p_lower_q_shrink/p_lower_shrinkage_method
- compute_ev_and_rank(p_col='p_fukusho_hit_lower') で EV_lower=p_lower×odds_lower
- select_bets(p_col='p_fukusho_hit_lower', p_min_base='p_lower') で p_min を p_lower ベースで解釈
- report.REPORT_COLUMNS に p_lower 系表示列
- EV 層が FEATURE_COLUMNS を import しない（SAFE-01）
- KEIBA_SKIP_DB_TESTS=1 で tests/model/test_orchestrator_p_lower.py / tests/ev/test_ev_p_lower.py / 既存 regression なし
</verification>

<success_criteria>
- EV-01: EV 判定が p から p_lower × odds_lower に移行する経路が整う（compute_ev_and_rank / select_bets の入力列差し替え・byte-reproducible）
- §11.2: orchestrator の q_shrink 計算は calib slice または外部注入のみ・test 窓 outcome を触れない（codex HIGH#1 score_split guard）
- §19.1: artifact metadata に q_level/q_shrink/shrinkage_method が記録され再現性保証
- SAFE-01: EV 計算層が FEATURE_COLUMNS から切り離されている（odds は EV 計算で使うが feature でない）
- 後方互換: theta=None/v1.0 binary 呼出で p_lower=NULL・p_col 省略で従来 p_fukusho_hit ベース（A5）
</success_criteria>

<output>
Create `.planning/phases/12-p-lower-ev-falsification-evaluation/12-02-SUMMARY.md` when done
</output>
