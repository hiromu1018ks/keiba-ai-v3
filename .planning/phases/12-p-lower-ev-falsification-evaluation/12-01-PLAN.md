---
phase: 12-p-lower-ev-falsification-evaluation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - src/model/race_relative.py
  - src/db/schema.py
  - src/model/predict.py
  - src/db/prediction_load.py
  - scripts/run_apply_schema.py
  - tests/model/test_p_lower.py
  - tests/db/test_schema_p_lower.py
autonomous: true
requirements: [EV-01, SAFE-01]
tags: [model, p-lower, schema-migration, wave1, ev-01, safe-01, foundation]
user_setup:
  - service: PostgreSQL (everydb2)
    why: "p_lower 列 ALTER TABLE は owner/admin 権限必要（memory: migration-privilege-admin-required）・etl ロールで InsufficientPrivilege・run_apply_schema.py idiom で owner/admin が実行"
    dashboard_config:
      - task: "owner/admin 権限で run_apply_schema.py を実行し PREDICTION_ADD_P_LOWER_SQL を適用"
        location: "scripts/run_apply_schema.py (host PostgreSQL・everydb2)"

must_haves:
  truths:
    - "[C2-12-01-1] statsmodels==0.14.6（厳密固定・byte-reproducible §19.1）が pyproject.toml + uv.lock に追加され・sklearn/scipy/numpy/pandas と実行時互換で import できる（Pattern 5/6・clustered SE・multipletests の前提）"
    - "compute_p_lower_conformal_shrinkage(p_final, y_calib, p_final_calib, *, q_level=0.90) が calib slice のみで q_shrink を計算し・test 窓 outcome を引数に取らない（§11.2 聖域の構造的機械保証・D-01/D-02）"
    - "compute_p_lower_conformal_shrinkage が byte-reproducible（np.quantile は default linear interpolation・seed 非依存・2回実行で np.array_equal が True）"
    - "prediction.fukusho_prediction テーブルに p_fukusho_hit_lower double precision 列（NULL 許容・CHECK prediction_p_lower_range [0,1]）が idempotent ADD COLUMN IF NOT EXISTS で追加される（v1.0 binary 行は NULL で後方互換）"
    - "PREDICTION_COLUMNS が 19→20 列に拡張され・p_fukusho_hit の直後に p_fukusho_hit_lower が挿入される（3ファイル連鎖: schema.py DDL / predict.py PREDICTION_COLUMNS / prediction_load.py _FLOAT_COLS）"
    - "_assert_valid_prediction_df が p_fukusho_hit_lower ∈ [0,1]（NULL 許容）を検証する"
    - "compute_p_lower_conformal_shrinkage・race_relative.py が FEATURE_COLUMNS 構築経路を import せず・odds/ninki 系を引数に取らない（SAFE-01 聖域）"
    - "[C-12-01-1 / HIGH] scripts/run_apply_schema.py の手動 migration step list（L117-146・APPLY_ORDER 不参照のハードコード list）に (\"prediction_add_p_lower\", schema_module.PREDICTION_ADD_P_LOWER_SQL) が挿入され・owner/admin 権限での実行で live-DB の prediction.fukusho_prediction に p_fukusho_hit_lower 列と prediction_p_lower_range CHECK 制約が実際に追加される（schema.py の APPLY_ORDER 追加だけでは run_apply_schema.py は適用しない・レビュー C-12-01-1・memory: migration-privilege-admin-required）"
    - "[C-12-01-4 / MEDIUM] race-relative 行（is_primary=f・theta != None 呼出）で p_fukusho_hit_lower が常に非 NULL になり・v1.0 binary 行（is_primary=t・theta=None 呼出）のみ NULL になる検証が test に含まれる（NULL の混入が EV 計算や selected-only calib gate で silent にマスクされない）"
  artifacts:
    - "src/model/race_relative.py::compute_p_lower_conformal_shrinkage (新純粋関数)"
    - "src/db/schema.py::PREDICTION_ADD_P_LOWER_SQL (idempotent ALTER・CHECK 制約)"
    - "src/db/schema.py::APPLY_ORDER に prediction_add_p_lower 追加"
    - "src/db/schema.py::__all__ に PREDICTION_ADD_P_LOWER_SQL 追加"
    - "src/model/predict.py::PREDICTION_COLUMNS 20列化"
    - "src/model/predict.py::_assert_valid_prediction_df に p_lower range check"
    - "src/db/prediction_load.py::_FLOAT_COLS に p_fukusho_hit_lower 追加"
    - "[C-12-01-1 / HIGH] scripts/run_apply_schema.py の手動 step list（L117-146 のハードコード list・APPLY_ORDER 不参照）に (\"prediction_add_p_lower\", schema_module.PREDICTION_ADD_P_LOWER_SQL) を挿入（prediction_extend_model_type_domain の直後・backtest_table の前）"
    - "[C2-12-01-1] pyproject.toml dependencies に statsmodels==0.14.6（厳密固定・byte-reproducible §19.1）追加"
    - "[C2-12-01-1] uv.lock に statsmodels==0.14.6 と依存ツリー反映"
    - "tests/model/test_p_lower.py (新規・EV-01 unit)"
    - "tests/db/test_schema_p_lower.py (新規・EV-01 live-DB integration)"
  key_links:
    - "compute_p_lower_conformal_shrinkage が race_relative.py の P_CAL_CLIP_EPSILON (1e-6) と同一 logit clip 契約を参照（race_relative と层分離独立関数）"
    - "schema.py PREDICTION_ADD_P_LOWER_SQL → APPLY_ORDER → run_apply_schema.py（owner/admin 権限・memory: migration-privilege-admin-required）"
    - "predict.py PREDICTION_COLUMNS ↔ prediction_load.py _FLOAT_COLS ↔ schema.py DDL の3ファイル列順序一致（Pitfall 4・_assert_valid_prediction_df で機械保証）"
  prohibitions:
    - "compute_p_lower_conformal_shrinkage は test 窓 outcome（y_test/outcome_test/y_outcome_test）を引数に取らない（§11.2 聖域・ Shared Pattern 6）"
    - "q_level=0.90 は test 窓で変更不可（§11.2 聖域・Shared Pattern 1 事前登録パターン）"
    - "FEATURE_COLUMNS / build_training_frame / load_feature_matrix を race_relative.py の p_lower 関数経路から import しない（SAFE-01 聖域）"
    - "odds/ninki/fukuodds/ninkij/tansyouodds を race_relative.py の p_lower 関数の引数・Name・Attribute に混入させない（tests/audit AST 検査・SAFE-01）"
    - "silent fallback 禁止（p_final/y_calib の NaN/inf は RuntimeError・Shared Pattern 4 fail-loud）"
    - "silent 過度な保証主張禁止（docstring で「p 信頼区間保証」でなく「calib slice 上の過大予測を保守的に差し引く分布自由な shrinkage rule」（Pitfall 1・D-01 修正文））"
---

<objective>
Phase 12 の基盤（Wave 1）を構築する: (1) [C2-12-01-1] statsmodels==0.14.6（厳密固定・byte-reproducible §19.1）を pyproject.toml + uv.lock に追加（falsification 回帰・Holm 補正の前提・Plan 03/04 が消費）、(2) `compute_p_lower_conformal_shrinkage` 純粋関数を `race_relative.py` に追加（D-01/D-02・calib slice のみ・§11.2 聖域の機械保証・byte-reproducible）、(3) `prediction.fukusho_prediction` テーブルに `p_fukusho_hit_lower` 列を idempotent ALTER で追加（3ファイル連鎖: schema/predict/prediction_load・Pitfall 4・v1.0 binary 行は NULL で後方互換）。

Purpose: Plan 02-05 が消費する p_lower 計算関数・prediction 列永続化・statsmodels 依存を最初に確立し・後続 plan が並列・順次実行できる土台を作る。聖域（§11.2 test 窓・SAFE-01 odds proxy 排除・byte-reproducible）を機械保証する。

Output: 計算関数・DDL migration・3ファイル連鎖拡張・unit/integration tests・statsmodels 依存。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-p-lower-ev-falsification-evaluation/12-CONTEXT.md
@.planning/phases/12-p-lower-ev-falsification-evaluation/12-RESEARCH.md
@.planning/phases/12-p-lower-ev-falsification-evaluation/12-PATTERNS.md
@src/model/race_relative.py
@src/db/schema.py
@src/model/predict.py
@src/db/prediction_load.py
@pyproject.toml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: statsmodels 依存追加 + compute_p_lower_conformal_shrinkage 純粋関数（race_relative.py）</name>
  <files>pyproject.toml, src/model/race_relative.py, tests/model/test_p_lower.py</files>
  <read_first>
    - pyproject.toml（dependencies リスト・scipy>=1.17.1 の直後が statsmodels 追加位置）
    - src/model/race_relative.py（L33-46 imports / L70 P_CAL_CLIP_EPSILON=1e-6 / L176-259 apply_race_relative_correction の fail-loud idiom / L262-381 compute_overprediction_penalty の SAFE-01-ALLOW マーカー docstring idiom / L383-385 __all__）
    - .planning/phases/12-p-lower-ev-falsification-evaluation/12-RESEARCH.md（Pattern 1・Pitfall 1/2/6・D-01 修正文・split conformal exchangeability JMLR 2024）
    - .planning/phases/12-p-lower-ev-falsification-evaluation/12-PATTERNS.md（race_relative.py Pattern・compute_p_lower_conformal_shrinkage 契約 L70-92 / Shared Pattern 1/4/5/6）
  </read_first>
  <behavior>
    - Test 1 (EV-01 byte-reproducible): `compute_p_lower_conformal_shrinkage(np.array([0.6,0.3,0.8]), np.array([0,1,0]), np.array([0.6,0.3,0.8]), q_level=0.90)` を2回呼出し・戻り値の (p_lower, q_shrink) が np.array_equal で完全一致（seed 非依存・default linear interpolation）
    - Test 2 (§11.2 聖域・シグネチャ検証): `inspect.signature(compute_p_lower_conformal_shrinkage)` の parameters に y_test/outcome_test/y_outcome_test が含まれない（引数 = {p_final, y_calib, p_final_calib, q_level} のみ）
    - Test 3 (D-01 overprediction residual): y_calib=0, p_final_calib=0.8 → r_calib=max(0,0.8-0)=0.8 で q_shrink>0 かつ p_lower=max(0, p_final-q_shrink) <= p_final
    - Test 4 (D-02 q_level 事前登録): q_level=0.90 で呼出可能・q_level<=0 or >=1 で ValueError
    - Test 5 (Shared Pattern 4 fail-loud): y_calib/p_final_calib に NaN/inf 混入で RuntimeError（silent fallback 禁止）
    - Test 6 (SAFE-01 odds proxy 排除・[C2-12-01-2]): `inspect.getsource(compute_p_lower_conformal_shrinkage)`（race_relative モジュール全体でなく本関数のみに限定・src/model/race_relative.py:46,348-349 の既存 `_odds_band` 評価専用正当参照で false-red にならないようスコープ限定）に odds/ninki/fukuodds の Name/Attribute/Constant 出現 0件（AST scan）。加えて tests/audit/test_audit_race_relative.py:189-245 の allow-list marker（SAFE-01-ALLOW）+ feature 構築経路（FEATURE_COLUMNS/build_training_frame/load_feature_matrix）非参照の検査パターンを再利用し・compute_p_lower_conformal_shrinkage が feature 構築経路を import しないことを併用検証（false red と false green の両方を回避・Plan 05 test_audit_p_lower_falsification.py の完全 AST scan と二重保証）
  </behavior>
  <action>
    最初に statsmodels を依存に追加: pyproject.toml の dependencies で `scipy>=1.17.1` の直後の行に `statsmodels==0.14.6` を挿入（コメント `# Phase 12 falsification: clustered SE / Holm 補正（RESEARCH.md Standard Stack・byte-reproducible §19.1 で厳密固定）` 付き）。`uv add "statsmodels==0.14.6"` を実行し uv.lock に反映（byte-reproducible・§19.1・将来の 0.14.7 等の解決による false fail 回避）。`uv run python -c "import statsmodels; from statsmodels.stats.multitest import multipletests; import statsmodels.api as sm; print(statsmodels.__version__)"` で import と 0.14.6 であることを確認（A6 wheel 実行時互換 smoke）。

    次に race_relative.py に compute_p_lower_conformal_shrinkage を追加。シグネチャは `def compute_p_lower_conformal_shrinkage(p_final: np.ndarray, y_calib: np.ndarray, p_final_calib: np.ndarray, *, q_level: float = 0.90) -> tuple[np.ndarray, float]`。実装は RESEARCH.md Pattern 1 と 12-PATTERNS.md L72-88 の契約に従う: `r_calib = np.maximum(0.0, p_final_calib - y_calib)` → `q_shrink = float(np.quantile(r_calib, q_level))` → `p_lower = np.maximum(0.0, p_final - q_shrink)` → `return p_lower, q_shrink`。y_calib/p_final_calib の NaN/inf を RuntimeError で fail-loud（Shared Pattern 4・apply_race_relative_correction L227-232 と同一 idiom）。q_level ∈ (0,1) の ValueError guard。

    docstring は RESEARCH.md Pattern 1 の docstring の統計的厳密さ記述（D-01 修正文・JMLR 2024 exchangeability 必須・時系列で厳密保証は壊れる・過度な保証主張しない・Pitfall 1）・§11.2 聖域記述（y_calib/p_final_calib は calib slice のみ・test 窓 outcome は引数に取らない・q_level=0.90 は事前登録値・test 窓で変更不可）・byte-reproducible 記述（np.quantile default linear interpolation・seed 非依存）を全て含む。docstring は「p 信頼区間保証」でなく「calib slice 上の過大予測を保守的に差し引く分布自由な shrinkage rule」と明記（Pitfall 1 過度な保証主張回避）。D-01/D-02 を文末に引用。

    race_relative.py の __all__ に `compute_p_lower_conformal_shrinkage` を追加。odds/ninki 系を一切引数・実装に含めない（SAFE-01・本関数は p_final と calib slice のみ消費）。
  </action>
  <verify>
    <automated>KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/model/test_p_lower.py -x -q</automated>
  </verify>
  <done>
    - [C2-12-01-1] statsmodels==0.14.6（厳密固定）が pyproject.toml と uv.lock に追加され import 可能（`uv run python -c "import statsmodels"` が exit 0・version 0.14.6・byte-reproducible §19.1）
    - compute_p_lower_conformal_shrinkage が race_relative.py に存在（`grep -n "def compute_p_lower_conformal_shrinkage" src/model/race_relative.py` で1件）
    - tests/model/test_p_lower.py の Test 1-6 全て GREEN（KEIBA_SKIP_DB_TESTS=1）
    - [C2-12-01-2] compute_p_lower_conformal_shrinkage のソース（race_relative.py モジュール全体でなく本関数のみ・`inspect.getsource(compute_p_lower_conformal_shrinkage)`）に odds/ninki 系 Name/Attribute 出現 0件（`python -c "import ast,inspect; from src.model.race_relative import compute_p_lower_conformal_shrinkage; t=ast.parse(inspect.getsource(compute_p_lower_conformal_shrinkage)); print(sum(1 for n in ast.walk(t) if (isinstance(n,ast.Name) and n.id in {'odds','ninki','fukuodds'}) or (isinstance(n,ast.Attribute) and n.attr in {'odds','ninki','fukuodds'})))" == 0`・src/model/race_relative.py:46,348-349 の既存 `_odds_band` 評価専用正当参照で false-red にならないようスコープ限定）
    - inspect.signature が {p_final, y_calib, p_final_calib, q_level} のみ（test 窓 outcome 系引数なし）
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: prediction p_fukusho_hit_lower 列 migration（schema.py + predict.py + prediction_load.py + run_apply_schema.py・3ファイル連鎖 + migration 適用経路・Pitfall 4・C-12-01-1 HIGH）</name>
  <files>src/db/schema.py, src/model/predict.py, src/db/prediction_load.py, scripts/run_apply_schema.py, tests/db/test_schema_p_lower.py</files>
  <read_first>
    - src/db/schema.py（L117-131 PREDICTION_ADD_IS_PRIMARY_SQL idiom / L145-159 PREDICTION_ADD_PROVENANCE_SQL idiom / L176-181 PREDICTION_EXTEND_MODEL_TYPE_DOMAIN_SQL DROP IF EXISTS + ADD / L380-403 APPLY_ORDER / L406-424 __all__）
    - src/model/predict.py（L60-98 PREDICTION_COLUMNS 19列・コメント「= 19 列」と schema.py 定義順の一致注記 / L100-108 MODEL_TYPE_TO_SHORT / _assert_valid_prediction_df 360-398 p_fukusho_hit ∈ [0,1] check idiom）
    - src/db/prediction_load.py（L91-96 _FLOAT_COLS / _INT_COLS / _BOOL_COLS / L111-183 _df_to_prediction_tuples の PREDICTION_COLUMNS ループ）
    - scripts/run_apply_schema.py（L117-146 ハードコード手動 step list・schema.APPLY_ORDER を参照しない・Phase 11 prediction_add_provenance / prediction_extend_model_type_domain が既にハードコード挿入されている idiom・L42 `from db import schema as schema_module`）
    - .planning/phases/12-p-lower-ev-falsification-evaluation/12-RESEARCH.md（Pattern 2・Pitfall 3 3ファイル連鎖・owner/admin 権限）
    - .planning/phases/12-p-lower-ev-falsification-evaluation/12-PATTERNS.md（schema.py Pattern L188-237 / predict.py Pattern L149-186 / prediction_load.py Pattern L240-268 / Shared Pattern 7）
    - .planning/phases/12-p-lower-ev-falsification-evaluation/12-REVIEWS.md（C-12-01-1 HIGH・scripts/run_apply_schema.py:117-146 が APPLY_ORDER 不参照で手動 list・p_lower step 挿入が必須）
    - scripts/run_phase11_evaluation.py（L286-290 注記「ALTER TABLE は owner 権限が必要・run_apply_schema.py に一本化」の idiom・memory: migration-privilege-admin-required）
    - tests/model/test_prediction_load.py・tests/db/test_is_primary_flag.py（合成 row helper・PREDICTION_COLUMNS 拡張時の helper 更新 idiom）
  </read_first>
  <behavior>
    - Test 1 (3ファイル列数一致): PREDICTION_COLUMNS の len が 20・schema.py PREDICTION_TABLE_DDL + PREDICTION_ADD_P_LOWER_SQL で定義される prediction.fukusho_prediction の全列数と一致（Pitfall 4 機械保証）
    - Test 2 (p_fukusho_hit_lower の位置): PREDICTION_COLUMNS で `p_fukusho_hit_lower` が `p_fukusho_hit` の直後 index にある（3ファイル全てで同位置）
    - Test 3 (_assert_valid_prediction_df NULL 許容 [0,1]): p_fukusho_hit_lower が全て NULL の df → valid（v1.0 binary 行互換）・[0,1] 外の値で ValueError
    - Test 4 (_FLOAT_COLS 拡張): `prediction_load._FLOAT_COLS` に `p_fukusho_hit_lower` が含まれる・INSERT 時 float 変換 or None で NULL INSERT が可能
    - Test 5 (idempotent ALTER): PREDICTION_ADD_P_LOWER_SQL が `ADD COLUMN IF NOT EXISTS` + `DROP CONSTRAINT IF EXISTS` + `ADD CONSTRAINT` で2回適用可能（test_schema_p_lower で live-DB KEIBA_SKIP_DB_TESTS unset のみ検証可能）
    - Test 6 (CHECK 制約 prediction_p_lower_range): p_fukusho_hit_lower=-0.1 or 1.5 の INSERT が CHECK 違反で拒否（live-DB）
    - Test 7 (列数一致 helper): tests/model/test_prediction_load.py や test_is_primary_flag.py の合成 row helper が新20列に追従（Regression・必要に応じて helper を更新）
    - Test 8 ([C-12-01-1 / HIGH] run_apply_schema.py step list 反映): scripts/run_apply_schema.py を ast.parse または正規表現で走査し・ハードコード手動 step list（L117-146 相当・schema.APPLY_ORDER でなく for step_name, step_sql in [...] のリテラル）に ("prediction_add_p_lower", schema_module.PREDICTION_ADD_P_LOWER_SQL) のエントリが1件含まれることを検証。schema.py APPLY_ORDER への追加だけでは run_apply_schema.py は適用しない（レビュー C-12-01-1・本 plan で両方を修正）。
    - Test 9 ([C-12-01-4 / MEDIUM] race-relative 行で p_lower 非 NULL): predict_p_fukusho を theta=1.0 相当の p_lower 付与経路で呼出した合成 df で p_fukusho_hit_lower が全て非 NULL であることを検証。theta=None/v1.0 binary 経路の df では p_fukusho_hit_lower が全て NULL（後方互換）。NULL の混入が race-relative 行に残らないことの機械保証。
  </behavior>
  <action>
    schema.py に PREDICTION_ADD_P_LOWER_SQL を追加（PREDICTION_EXTEND_MODEL_TYPE_DOMAIN_SQL の直後・backtest_table DDL の前）。実装は RESEARCH.md Pattern 2 と 12-PATTERNS.md L207-220 の雛形通り: `ALTER TABLE prediction.fukusho_prediction ADD COLUMN IF NOT EXISTS p_fukusho_hit_lower double precision;` → `DROP CONSTRAINT IF EXISTS prediction_p_lower_range;` → `ADD CONSTRAINT prediction_p_lower_range CHECK (p_fukusho_hit_lower IS NULL OR (p_fukusho_hit_lower >= 0 AND p_fukusho_hit_lower <= 1));` → COMMENT ON COLUMN（Phase 12 SC#1・v1.0 binary 行は NULL・race-relative 行は p_lower を持つ・CHECK 制約で [0,1] 保証）。

    PREDICTION_TABLE_DDL（CREATE TABLE・L61-101 付近）にも p_fukusho_hit_lower を追加（新規 CREATE TABLE で20列化・再現性 §19.1・11-05 Deviation#2 と同じ教訓: ALTER だけだと CREATE TABLE と不整合）。p_fukusho_hit の直後に double precision・NULL 許容（CHECK 制約は CREATE TABLE 側でも定義）。

    APPLY_ORDER に `("prediction_add_p_lower", PREDICTION_ADD_P_LOWER_SQL)` を `prediction_extend_model_type_domain` の直後に挿入（L395 の直後）。__all__ に `PREDICTION_ADD_P_LOWER_SQL` を追加。

    **[C-12-01-1 / HIGH] scripts/run_apply_schema.py の手動 migration step list に p_lower step を挿入**（本 step が無いと live-DB に p_fukusho_hit_lower 列が追加されず Phase 12 全体が停止・レビュー C-12-01-1・codex HIGH）。run_apply_schema.py L117-146 のハードコード手動 step list（`for step_name, step_sql in [...]`・schema.APPLY_ORDER を参照しない・Phase 11 が prediction_add_provenance / prediction_extend_model_type_domain をハードコード挿入した idiom）に・`prediction_extend_model_type_domain` の直後（backtest_table の前）に `("prediction_add_p_lower", schema_module.PREDICTION_ADD_P_LOWER_SQL),` を挿入。コメントで「Phase 12 SC#1 p_lower 列追加 ALTER（schema.py APPLY_ORDER と同期・idempotent・owner/admin 権限・memory: migration-privilege-admin-required）」を明記。L42 の `from db import schema as schema_module` は既存のため・schema_module.PREDICTION_ADD_P_LOWER_SQL が解決可能（Phase 11 prediction_add_provenance と同一の参照経路）。dry-run（`uv run python scripts/run_apply_schema.py --dry-run`）で step list に prediction_add_p_lower が含まれることを確認。

    predict.py の PREDICTION_COLUMNS で `"p_fukusho_hit"` の直後に `"p_fukusho_hit_lower"` を挿入（L84 の直後・コメントの「= 19 列」を「= 20 列」に更新・schema.py の定義順と完全一致注記も更新）。_assert_valid_prediction_df に p_lower range check を追加: `p_lower = df["p_fukusho_hit_lower"]; if p_lower.notna().any(): pl_valid = p_lower.dropna(); if (pl_valid < 0).any() or (pl_valid > 1).any(): raise ValueError(...)`（RESEARCH.md Pattern 2・L317-322 の雛形）。predict_p_fukusho の DataFrame 構築で `df["p_fukusho_hit_lower"] = ...` を付与（theta=None/v1.0 binary 呼出の場合は None で NULL・race-relative の場合は compute_p_lower_conformal_shrinkage の戻り値・Plan 02 で orchestrator から伝播）。[C-12-01-4] race-relative 経路（theta != None・pred_proba_lower を受け取る）では p_fukusho_hit_lower が常に非 NULL になること・v1.0 binary 経路（theta=None・pred_proba_lower=None）では常に NULL になることを予測DataFrame 構築で保証（None 以外の silent NaN 混入を許さない）。

    prediction_load.py の _FLOAT_COLS を `{"p_fukusho_hit", "p_fukusho_hit_lower"}` に拡張（L92）。INSERT 列順序は PREDICTION_COLUMNS を import して従うため自動追従（3ファイル連鎖の auto-propagation・L137 の `for c in PREDICTION_COLUMNS` ループ）。

    Task 2 で migration の実行はしない（owner/admin 権限必要・memory: migration-privilege-admin-required・run_apply_schema.py に一本化・11-05 Deviation#1 と同じ）。test_schema_p_lower.py は (a) PREDICTION_ADD_P_LOWER_SQL 文字列の idempotent 性・CHECK 制約存在を unit で検証し・(b) [C-12-01-1] run_apply_schema.py の step list に prediction_add_p_lower が含まれることを検証（Test 8）・live-DB 適用は Plan 04/05 checkpoint で人間が `uv run python scripts/run_apply_schema.py`（psql でなく・レビュー C-12-05-2）経由で実行。
  </action>
  <verify>
    <automated>KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/db/test_schema_p_lower.py tests/model/test_prediction_load.py tests/db/test_is_primary_flag.py -x -q</automated>
  </verify>
  <done>
    - PREDICTION_COLUMNS の len が 20（`python -c "from src.model.predict import PREDICTION_COLUMNS; print(len(PREDICTION_COLUMNS))"` == 20）
    - p_fukusho_hit_lower が p_fukusho_hit の直後 index（`PREDICTION_COLUMNS.index('p_fukusho_hit_lower') == PREDICTION_COLUMNS.index('p_fukusho_hit') + 1`）
    - schema.py APPLY_ORDER に prediction_add_p_lower が1件（`grep -c 'prediction_add_p_lower' src/db/schema.py` == 1）
    - [C-12-01-1 / HIGH] scripts/run_apply_schema.py の手動 step list に prediction_add_p_lower が1件（`grep -c 'prediction_add_p_lower' scripts/run_apply_schema.py` == 1・schema.py APPLY_ORDER と同期・dry-run で step list に含まれる）
    - PREDICTION_ADD_P_LOWER_SQL が `ADD COLUMN IF NOT EXISTS p_fukusho_hit_lower` と `prediction_p_lower_range` CHECK 制約を含む（`grep -c 'p_fukusho_hit_lower' src/db/schema.py` >= 3）
    - _FLOAT_COLS に p_fukusho_hit_lower が含まれる（`python -c "from src.db.prediction_load import _FLOAT_COLS; print('p_fukusho_hit_lower' in _FLOAT_COLS)"` == True）
    - _assert_valid_prediction_df が p_lower=None 全行 df を通し・[0,1] 外で ValueError
    - [C-12-01-4 / MEDIUM] race-relative 経路（theta != None）で p_fukusho_hit_lower が常に非 NULL・v1.0 binary 経路（theta=None）で常に NULL のテストが GREEN
    - 既存 test_prediction_load.py / test_is_primary_flag.py が regression なしで GREEN（合成 row helper が20列に追従）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test 窓 outcome → q_shrink 計算関数 | q_shrink 計算・market_implied calibrator fit・falsification 回帰設計に test 窓 outcome が混入すると後知恵リーク（§11.2 聖域） |
| feature 構築経路 ↔ p_lower / falsification / EV 層 | odds/ninki proxy が FEATURE_COLUMNS に混入すると市場回帰で edge 消滅（SAFE-01 聖域） |
| etl ロール ↔ ALTER TABLE | owner/admin 権限が必要な DDL を etl ロールで実行すると InsufficientPrivilege（memory: migration-privilege-admin-required） |
| 3ファイル列定義 ↔ DB INSERT | schema.py DDL / predict.py PREDICTION_COLUMNS / prediction_load.py の列順序不整合は silent 誤作動（Pitfall 4） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Tampering / Information disclosure | compute_p_lower_conformal_shrinkage の test 窓 outcome 引数混入（§11.2 聖域違反・後知恵リーク） | high | mitigate | 関数シグネチャを {p_final, y_calib, p_final_calib, q_level} のみに固定・test 窓 outcome 系引数を取らない。tests/model/test_p_lower.py の Test 2 と tests/audit/test_audit_p_lower_falsification.py（Plan 05）の自己完結性 test で inspect.signature を検証。docstring に §11.2 聖域を明記 |
| T-12-02 | Information disclosure（誤報） | split conformal の過度な保証主張「p は 90% 信頼下限」（Pitfall 1・統計的厳密さ違反） | medium | mitigate | docstring に「calib slice 上の過大予測を保守的に差し引く分布自由な shrinkage rule（個体ごとの真の確率の下限保証でない・JMLR 2024 exchangeability 必須・時系列で厳密保証は壊れる）」と明記。coverage 表現は「実測 coverage + selected-only calibration」。q_level を α と同一視しない（D-02） |
| T-12-03 | Tampering | odds/ninki proxy の race_relative.py p_lower 経路への混入（SAFE-01 違反） | high | mitigate | compute_p_lower_conformal_shrinkage は p_final と calib slice のみ消費・odds 系引数を取らない。race_relative.py は FEATURE_COLUMNS / build_training_frame / load_feature_matrix を import しない。tests/model/test_p_lower.py の Test 6（AST scan sanity）と tests/audit/test_audit_p_lower_falsification.py（Plan 05・完全 AST scan）で機械保証 |
| T-12-04 | Tampering | prediction 列 migration の 3 ファイル列順序不整合（Pitfall 4 silent 誤作動） | high | mitigate | _assert_valid_prediction_df の列順序チェック・PREDICTION_COLUMNS を import して従う _df_to_prediction_tuples・p_fukusho_hit の直後に p_fukusho_hit_lower を3ファイル全てで同位置に挿入。test_schema_p_lower.py の Test 1/2 で検証 |
| T-12-05 | Elevation | ALTER TABLE を etl ロールで実行（InsufficientPrivilege・memory: migration-privilege-admin-required） | low | accept | 本 Phase のコードは migration を実行せず run_apply_schema.py（owner/admin 権限）に一本化。scripts/run_phase11_evaluation.py L286-290 と同一 idiom。run_apply_schema.py の実行は Plan 04/05 checkpoint で人間が行う |
| T-12-SC | Tampering | uv add / uv sync によるパッケージ install | low | mitigate | statsmodels は [SUS] false-positive（PyPI が download 数を公開しない・repo URL・成熟リリース歴で OK・RESEARCH.md Package Legitimacy Audit Approved）。本 plan では auto-approve せず checkpoint:human-verify を Plan 04/05 に配置し・import smoke と version==0.14.6（厳密固定・[C2-12-01-1]）を test で検証（Task 1 action の `uv run python -c "import statsmodels"` smoke） |
</threat_model>

<verification>
- [C2-12-01-1] statsmodels==0.14.6（厳密固定）が pyproject.toml + uv.lock に追加・import 可能（A6 wheel 実行時互換 smoke・Task 1 action・byte-reproducible §19.1）
- compute_p_lower_conformal_shrinkage が race_relative.py に存在・calib slice のみで q_shrink 計算・test 窓 outcome 引数なし・byte-reproducible
- prediction.fukusho_prediction に p_fukusho_hit_lower 列（NULL 許容・CHECK [0,1]）の idempotent ALTER SQL が定義され APPLY_ORDER に追加
- PREDICTION_COLUMNS が20列化（p_fukusho_hit の直後に p_fukusho_hit_lower）
- _assert_valid_prediction_df が p_lower ∈ [0,1]（NULL 許容）を検証
- prediction_load._FLOAT_COLS に p_fukusho_hit_lower が追加
- KEIBA_SKIP_DB_TESTS=1 で tests/model/test_p_lower.py / tests/db/test_schema_p_lower.py / 既存 test_prediction_load.py / test_is_primary_flag.py が全て GREEN
</verification>

<success_criteria>
- SC#1（部分）: p_lower 計算関数が train/calib データのみで設計可能（test 窓 outcome を引数に取らない構造的機械保証）・byte-reproducible
- SAFE-01: race_relative.py p_lower 経路に odds/ninki proxy 混入なし（AST scan・unit sanity）
- §19.1: [C2-12-01-1] statsmodels==0.14.6（厳密固定）が uv.lock で固定・再現性保証（将来の 0.14.7 等の解決による false fail 回避）
- Pitfall 3/4: 3ファイル列連鎖が機械保証（PREDICTION_COLUMNS import・列順序一致）
- memory: migration-privilege-admin-required: migration 実行は run_apply_schema.py に一本化（本 plan では SQL 定義のみ・実行は後続 checkpoint）
</success_criteria>

<output>
Create `.planning/phases/12-p-lower-ev-falsification-evaluation/12-01-SUMMARY.md` when done
</output>
