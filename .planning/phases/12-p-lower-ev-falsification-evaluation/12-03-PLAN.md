---
phase: 12-p-lower-ev-falsification-evaluation
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - src/eval/__init__.py
  - src/eval/falsification.py
  - src/model/evaluator.py
  - src/model/segment_eval.py
  - src/ev/refund_accounting.py
  - tests/evaluation/__init__.py
  - tests/evaluation/test_falsification.py
  - tests/evaluation/test_extended_metrics.py
  - tests/model/test_evaluator_phase12_gate.py
autonomous: true
requirements: [EVAL-01, EVAL-02, SAFE-01]
tags: [eval, falsification, calibration-gate, wave3, eval-01, eval-02, safe-01]
user_setup: []

must_haves:
  truths:
    - "src/eval/falsification.py が run_falsification_test（logit(outcome) ~ logit(market_implied) + logit(model_p)・race_id clustered SE・statsmodels cov_kwds={'groups': race_id}・Pitfall 4 対応）と fit_market_implied_calibrator（1/odds を outcome に calibration・train/calib のみ・test 窓聖域・MARKET_CALIB_SAMPLE_THRESHOLD=1000 で isotonic/sigmoid 切替）を提供する（D-04/D-05）"
    - "fit_market_implied_calibrator が calib slice のみで fit し・test 窓 outcome を引数に取らない（§11.2 聖域・Shared Pattern 6・シグネチャ検証）"
    - "run_falsification_test が α=0.05 単一係数検定（model_p）と bin/odds_band サブ解析の Holm 補正（multipletests method='holm'・HOLM_ALPHA=0.05）を実装し verdict='feature_gap' or 'structural_limit' を返す（D-05・EVAL-02）"
    - "evaluator.check_acceptance_gate の warn_reasons に Phase 12 専用 WARN gate（selected-only calibration / odds-band conditional calibration・BLOCK でなく WARN・D-06）が追加され・§15.2 既存 BLOCK/WARN gate は一切不変（D-06・事前登録指標不変）"
    - "segment_eval の binning 契約（ODDS_BAND_EDGES=[0,2.9,4.9,9.9,inf]・evaluate_segment_axis・_odds_band）が EV-decile / disagreement ROI 計算で再利用され・独自 binning は存在しない（bit-identical・codex review HIGH#2）"
    - "refund_accounting が snapshot→final payout slippage（payout/100 - fuku_odds_lower）を計算可能にする（D-07・HARAI PayFukusyoPay 再利用）"
    - "falsification / market_implied / segment_eval binning が FEATURE_COLUMNS を import せず・odds/ninki は evaluation 専用層のみで消費する（SAFE-01・層分離）"
    - "LOGIT_CLIP_EPS=1e-6 で logit clipping が適用され inf 回避（Pitfall 6・race_relative P_CAL_CLIP_EPSILON と同一契約）"
  artifacts:
    - "src/eval/__init__.py（新規・空パッケージ）"
    - "src/eval/falsification.py（新規・run_falsification_test + fit_market_implied_calibrator + logit_clip helper・statsmodels 依存）"
    - "src/model/evaluator.py::check_acceptance_gate warn_reasons に Phase 12 WARN gate（selected-only/odds-band conditional calib）追加・§15.2 gate 不変"
    - "src/model/evaluator.py に PHASE12_ODDS_BAND_CALIB_MAX_DEV_THRESHOLD / PHASE12_SELECTED_ONLY_CALIB_MAX_DEV_THRESHOLD 事前登録定数追加"
    - "src/model/segment_eval.py に evaluate_segment_axis の軸拡張（'ev_decile' / 'disagreement'・既存 binning 契約再利用・pd.qcut duplicates='drop'）"
    - "src/ev/refund_accounting.py に compute_snapshot_final_slippage 関数（payout/100 - fuku_odds_lower・HARAI _lookup_payfukusyo_pay 再利用）"
    - "tests/evaluation/__init__.py（新規・空パッケージ）"
    - "tests/evaluation/test_falsification.py（新規・EVAL-02 unit・clustered SE・Holm・logit clipping・market_implied 再校正・§11.2 聖域）"
    - "tests/evaluation/test_extended_metrics.py（新規・EVAL-01 unit・EV-decile/disagreement ROI・slippage・bit-identical binning）"
    - "tests/model/test_evaluator_phase12_gate.py（新規・SC#4 WARN gate・§15.2 不変検証）"
  key_links:
    - "src/eval/falsification.py::fit_market_implied_calibrator（calib slice のみ）→ run_falsification_test（test 窓適用のみ・§11.2 聖域）"
    - "segment_eval.ODDS_BAND_EDGES / evaluate_segment_axis ↔ evaluator.check_acceptance_gate の Phase 12 WARN gate（odds-band conditional calib・bit-identical binning）"
    - "statsmodels.Logit.fit(cov_type='cluster', cov_kwds={'groups': race_id})〔Pitfall 4 正しい API・GitHub #6287〕"
    - "statsmodels.stats.multitest.multipletests(pvals, alpha=0.05, method='holm')（bin/odds_band サブ解析のみ）"
    - "evaluator.check_acceptance_gate の warn_reasons〔L888-1029〕に Phase 12 専用 gate を分離追加・block_reasons〔§15.2〕は不変"
  prohibitions:
    - "fit_market_implied_calibrator は test 窓 outcome（y_test/outcome_test）を引数に取らない（§11.2 聖域・シグネチャ検証・Shared Pattern 6）"
    - "run_falsification_test は model_p/market_implied の「学習」を行わない（test 窓の予測のみで評価・§11.2 聖域）"
    - "§15.2 事前登録指標（calibration_max_dev/Brier/LogLoss/sum(p)）の定義・閾値を一切変更しない（D-06・regression test で不変性検証）"
    - "Phase 12 WARN gate を BLOCK にしない（D-06・Phase 12 は切替判断材料フェーズ・WARN が適切）"
    - "FEATURE_COLUMNS に odds/ninki/market_implied/fuku_odds_lower を混入させない（SAFE-01・層分離・tests/audit AST）"
    - "falsification / segment_eval で独自 binning を作成しない（segment_eval._odds_band / ODDS_BAND_EDGES / pd.qcut duplicates='drop' の再利用のみ・bit-identical・codex HIGH#2）"
    - "statsmodels で `groups=` を直接 fit に渡さない（error・GitHub #6287・cov_kwds={'groups': array} が正しい）"
    - "logit clipping 忘れ（Pitfall 6・inf 伝播・LOGIT_CLIP_EPS=1e-6 必須）"
    - "silent fallback 禁止（statsmodels ConvergenceWarning/multipletests 失敗は RuntimeError・Shared Pattern 4）"
---

<objective>
Plan 01-02 の p_lower 計算・EV 切替基盤を消費し・Phase 12 の評価・診断層（EVAL-01/EVAL-02/SAFE-01・SC#4）を構築する: (1) src/eval/falsification.py（新規）で logit(outcome) ~ logit(market_implied) + logit(model_p) の race_id clustered SE ロジット回帰 + market_implied 再校正（train/calib のみ・test 窓聖域）、(2) evaluator.check_acceptance_gate に Phase 12 専用 WARN gate（selected-only/odds-band conditional calibration・D-06・§15.2 gate 不変）を追加、(3) segment_eval binning 契約を再利用し EV-decile/disagreement ROI 計算、(4) refund_accounting で snapshot→final payout slippage 計算。

Purpose: Plan 04 の run_phase12_evaluation.py が消費する falsification 関数・market_implied calibrator・WARN gate・拡張評価指標を整える。core value 再定式化（オッズ帯別条件付き calibration・過大でないこと）の直接的測定を機械保証する。聖域（§11.2 test 窓・SAFE-01 odds proxy 排除・§15.2 事前登録指標不変・byte-reproducible）を保つ。

Output: falsification.py 新規・evaluator WARN gate 拡張・segment_eval binning 拡張・refund_accounting slippage・unit tests。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-p-lower-ev-falsification-evaluation/12-CONTEXT.md
@.planning/phases/12-p-lower-ev-falsification-evaluation/12-RESEARCH.md
@.planning/phases/12-p-lower-ev-falsification-evaluation/12-PATTERNS.md
@.planning/phases/12-p-lower-ev-falsification-evaluation/12-01-SUMMARY.md
@.planning/phases/12-p-lower-ev-falsification-evaluation/12-02-SUMMARY.md
@src/model/evaluator.py
@src/model/segment_eval.py
@src/ev/refund_accounting.py
@src/model/race_relative.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/eval/falsification.py 新規（run_falsification_test + fit_market_implied_calibrator・statsmodels clustered SE・§11.2 聖域）</name>
  <files>src/eval/__init__.py, src/eval/falsification.py, tests/evaluation/__init__.py, tests/evaluation/test_falsification.py</files>
  <read_first>
    - src/model/race_relative.py（L70 P_CAL_CLIP_EPSILON=1e-6・L262-381 compute_overprediction_penalty の SAFE-01-ALLOW マーカー docstring idiom〔falsification も market_implied 引数に同じマーカー〕・L33-46 imports idiom）
    - scripts/run_phase11_evaluation.py（L499-546 _select_theta_on_calib の calib-slice-only idiom・score_split guard・L128-140 事前登録値 idiom）
    - src/model/segment_eval.py（L74-75 ODDS_BAND_EDGES=[0,2.9,4.9,9.9,inf] / ODDS_BAND_LABELS / _odds_band / evaluate_segment_axis binning 契約）
    - .planning/phases/12-p-lower-ev-falsification-evaluation/12-RESEARCH.md（Pattern 5 falsification L386-447 / Pattern 6 market_implied L449-496 / Pitfall 4 cov_kwds / Pitfall 5 isotonic 1000 rule / Pitfall 6 logit clipping・D-04/D-05）
    - .planning/phases/12-p-lower-ev-falsification-evaluation/12-PATTERNS.md（falsification.py Pattern L599-654）
    - src/utils/calibrator.py（L100-102 FrozenEstimator + CalibratedClassifierCV idiom〔sklearn 1.9.0 prefit・market_implied 再校正で再利用〕）
  </read_first>
  <behavior>
    - Test 1 (§11.2 聖域・fit_market_implied_calibrator シグネチャ): `inspect.signature(fit_market_implied_calibrator)` が {odds_calib, y_calib, calib_sample_size, ...} のみで y_test/outcome_test/y_outcome_test を含まない（Shared Pattern 6）
    - Test 2 (§11.2 聖域・run_falsification_test は学習しない): run_falsification_test は test 窓の予測のみを消費・fit（学習）を行わない・market_implied calibrator は外部から受け取る（docstring とシグネチャで保証）
    - Test 3 (race_id clustered SE): run_falsification_test が `model.fit(cov_type='cluster', cov_kwds={'groups': race_id_test})` を呼び・戻り値に model_p_coef/model_p_pvalue/model_p_significant/verdict が含まれる
    - Test 4 (verdict feature_gap vs structural_limit): model_p_pvalue < 0.05 → verdict='feature_gap' / >= 0.05 → verdict='structural_limit'（D-05・EVAL-02）
    - Test 5 (Holm 補正): bin/odds_band サブ解析で multipletests(method='holm', alpha=0.05) が呼ばれ・corrected pvalues が返る（主検定 model_p 単一係数は Holm 不要）
    - Test 6 (logit clipping): LOGIT_CLIP_EPS=1e-6 で logit(0)/logit(1) が inf でなく ±13.8 範囲（Pitfall 6・P_CAL_CLIP_EPSILON と同一契約）
    - Test 7 (isotonic vs sigmoid 切替): calib_sample_size >= MARKET_CALIB_SAMPLE_THRESHOLD=1000 で method='isotonic' / < 1000 で method='sigmoid'（Pitfall 5・sklearn docs）
    - Test 8 (SAFE-01・falsification 層): falsification.py が FEATURE_COLUMNS / build_training_frame / load_feature_matrix を import しない・odds/market_implied 引数の docstring に SAFE-01-ALLOW マーカーがある
    - Test 9 (fail-loud・Pitfall 4): statsmodels ConvergenceWarning や cov_kwds groups 配列長不一致は RuntimeError（silent fallback 禁止・Shared Pattern 4）
  </behavior>
  <action>
    src/eval/__init__.py（空）と tests/evaluation/__init__.py（空）を作成。

    src/eval/falsification.py を新規作成。imports: `from __future__ import annotations` / `import numpy as np` / `import pandas as pd` / `import statsmodels.api as sm` / `from statsmodels.stats.multitest import multipletests` / `from sklearn.calibration import CalibratedClassifierCV` / `from sklearn.linear_model import LogisticRegression` / `from sklearn.frozen import FrozenEstimator` / `from src.model.segment_eval import ODDS_BAND_EDGES, _odds_band`（binning 再利用・codex HIGH#2）。FEATURE_COLUMNS / build_training_frame / load_feature_matrix の import は禁止（SAFE-01・層分離）。

    事前登録定数: Q_LEVEL_FALSIFICATION = 0.05（α・D-05・主検定）・HOLM_ALPHA = 0.05（bin/odds_band サブ解析・D-05）・LOGIT_CLIP_EPS = 1e-6（Pitfall 6・race_relative.P_CAL_CLIP_EPSILON と同一契約）・ODDS_CLIP_MIN = 1.0・ODDS_CLIP_MAX = 100.0（D-05 (c) odds clipping・planner 事前登録・JRA 複勝オッズ典型範囲 A2）・MARKET_CALIB_SAMPLE_THRESHOLD = 1000（isotonic vs sigmoid 切替・Pitfall 5・sklearn docs）。docstring に「§11.2 聖域・test 窓で変更不可・事前登録パターン Phase 10 D-12 / 11 D-03 踏襲」と明記。

    `def logit_clip(p, eps=LOGIT_CLIP_EPS)` helper: np.clip(p, eps, 1-eps) して log(p/(1-p))。Pitfall 6 inf 回避。

    `def fit_market_implied_calibrator(odds_calib: np.ndarray, y_calib: np.ndarray, *, calib_sample_size: int) -> CalibratedClassifierCV`。シグネチャは calib slice のみ（test 窓 outcome 引数なし・Shared Pattern 6・Test 1）。実装は RESEARCH.md Pattern 6 と 12-PATTERNS.md L632-653: odds_clipped = np.clip(odds_calib, ODDS_CLIP_MIN, ODDS_CLIP_MAX) → market_base_p = 1.0 / odds_clipped → method = 'isotonic' if calib_sample_size >= MARKET_CALIB_SAMPLE_THRESHOLD else 'sigmoid' → base = LogisticRegression(C=1e6) → base.fit(market_base_p.reshape(-1,1), y_calib.astype(int)) → frozen = FrozenEstimator(base) → calibrator = CalibratedClassifierCV(estimator=frozen, method=method) → calibrator.fit(market_base_p.reshape(-1,1), y_calib.astype(int)) → return calibrator。docstring に D-04 再校正の理由（overround・FLB・複勝プール歪み除去・model_p 係数が「市場にない純粋な residual」を測る）と §11.2 聖域（train/calib 窓のみで fit・test 窓は適用のみ）を明記。SAFE-01-ALLOW: odds/market_implied マーカーを docstring に記載（feature 構築経路からの切り離し機械保証・compute_overprediction_penalty と同一 idiom）。

    `def run_falsification_test(y_outcome_test, market_implied_test, model_p_test, race_id_test, *, field_size_test=None, odds_band_test=None, holm_alpha=HOLM_ALPHA) -> dict`。実装は RESEARCH.md Pattern 5 L386-447 と 12-PATTERNS.md L599-630。★ Pitfall 4 対応: `model = sm.Logit(y_outcome_test.astype(int), X); result = model.fit(cov_type='cluster', cov_kwds={'groups': race_id_test}, disp=0, maxiter=200)`（groups= 直接渡しでない・cov_kwds={'groups': array}・GitHub #6287）。戻り値 dict は {model_p_coef, model_p_pvalue, model_p_significant, verdict, sub_analyses_odds_band: {band: {pvalue, coef}}（Holm 補正後 pvalue 含む・multipletests(method='holm', alpha=holm_alpha))}。主検定 model_p 単一係数は Holm 不要・bin/odds_band サブ解析のみ Holm（D-05）。docstring に「§11.2 聖域・本関数は学習しない（test 窓予測のみで評価）・market_implied calibrator は train/calib 窓で fit 済みを外部から受け取る」を明記（Shared Pattern 6）。SAFE-01-ALLOW マーカー。statsmodels 収束失敗（ConvergenceWarning）や groups 長不一致は RuntimeError（Shared Pattern 4 fail-loud）。

    verdict 判定: model_p_pvalue < HOLM_ALPHA → 'feature_gap'（特徴量不足・市場 residual が残る）・>= HOLM_ALPHA → 'structural_limit'（core value 維持での黒字化棄却・market 係数が model を包摂）（D-05・EVAL-02）。
  </action>
  <verify>
    <automated>KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/evaluation/test_falsification.py -x -q</automated>
  </verify>
  <done>
    - src/eval/falsification.py が run_falsification_test と fit_market_implied_calibrator を提供
    - fit_market_implied_calibrator の引数が {odds_calib, y_calib, calib_sample_size} のみ（test 窓 outcome なし）
    - run_falsification_test が cov_kwds={'groups': race_id_test} で clustered SE を呼び・verdict='feature_gap' or 'structural_limit' を返す
    - Holm 補正が bin/odds_band サブ解析のみ・主検定 model_p 単一係数は Holm 不要
    - LOGIT_CLIP_EPS=1e-6・MARKET_CALIB_SAMPLE_THRESHOLD=1000 で isotonic/sigmoid 切替
    - falsification.py が FEATURE_COLUMNS を import しない（SAFE-01）
    - falsification.py の odds/market_implied 引数 docstring に SAFE-01-ALLOW マーカー
    - statsmodels 収束失敗で RuntimeError（fail-loud）
    - KEIBA_SKIP_DB_TESTS=1 で tests/evaluation/test_falsification.py 全 GREEN
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: evaluator WARN gate 拡張（Phase 12 selected-only/odds-band conditional calib・§15.2 不変） + segment_eval binning 拡張 + refund_accounting slippage</name>
  <files>src/model/evaluator.py, src/model/segment_eval.py, src/ev/refund_accounting.py, tests/evaluation/test_extended_metrics.py, tests/model/test_evaluator_phase12_gate.py</files>
  <read_first>
    - src/model/evaluator.py（L84-85 calibration_max_dev/calibration_max_dev_guarded / L89 CALIBRATION_CURVE_BINS=10 / L168-265 compute_metrics〔§15.2 事前登録指標〕/ L888-1029 check_acceptance_gate BLOCK/WARN hybrid・warn_reasons・block_reasons・return dict〔L1018-1029〕）
    - src/model/segment_eval.py（L54-61 imports / L74-75 ODDS_BAND_EDGES/LABELS / L120-139 _band_labels_from_edges np.digitize / L151-158 _odds_band / L166-285 evaluate_segment_axis binning 契約）
    - src/ev/refund_accounting.py（L38-75 _lookup_payfukusyo_pay slot 照合 / L78-177 determine_stake_payout）
    - .planning/phases/12-p-lower-ev-falsification-evaluation/12-RESEARCH.md（Pattern 4 binning・D-06/D-07・core value 再定式化・SC#4）
    - .planning/phases/12-p-lower-ev-falsification-evaluation/12-PATTERNS.md（evaluator Pattern L423-457 / segment_eval Pattern L395-419 / refund_accounting Pattern L322-345）
  </read_first>
  <behavior>
    - Test 1 (§15.2 gate 不変): check_acceptance_gate の block_reasons/block_triggered が Plan 03 適用前後で同一入力に対し同じ判定（calibration_max_dev/Brier/LogLoss/sum(p) gate は一切変更なし・regression test）
    - Test 2 (Phase 12 WARN gate 追加): check_acceptance_gate の return dict に phase12_warn_triggered / phase12_warn_reasons が含まれ・selected-only calib_max_dev が PHASE12_SELECTED_ONLY_CALIB_MAX_DEV_THRESHOLD を超えると warn に追加（BLOCK でなく WARN・D-06）
    - Test 3 (odds-band conditional calib): odds_band 別（ODDS_BAND_EDGES=[0,2.9,4.9,9.9,inf]）の conditional calib_max_dev が計算され・投票層（高オッズ域・EV 上位・SC#4 WARN gate の対象）で PHASE12_ODDS_BAND_CALIB_MAX_DEV_THRESHOLD を超えると warn
    - Test 4 (segment_eval binning bit-identical): evaluate_segment_axis(axis='ev_decile') と axis='disagreement' が pd.qcut duplicates='drop' で決定論的 binning・2回呼出で bit-identical
    - Test 5 (slippage 計算): refund_accounting.compute_snapshot_final_slippage が payout/100 - fuku_odds_lower を計算（HARAI PayFukusyoPay 再利用・D-07）
    - Test 6 (SAFE-01): evaluator / segment_eval / refund_accounting の Phase 12 拡張が FEATURE_COLUMNS を import しない
    - Test 7 (byte-reproducible): WARN gate 判定が2回呼出で同一結果（deterministic）
  </behavior>
  <action>
    evaluator.py に Phase 12 専用 WARN gate を check_acceptance_gate の warn_reasons に追加（block_reasons は一切触らない・D-06・§15.2 gate 不変）。事前登録定数（docstring に「§11.2 聖域・test 窓で変更不可・Phase 10 D-12 / 11 D-03 事前登録パターン踏襲」・Claude's Discretion「SC#4 gate の具体的閾値」の固定化）:

    PHASE12_SELECTED_ONLY_CALIB_MAX_DEV_THRESHOLD: float = 0.10（selected-only calibration の WARN 閾値・投票層〔p_lower EV で選ばれた馬〕の過大予測許容幅・v1.0 の p=0.16→実0.04 の4倍過大を catch する gate・SC#4）。PHASE12_ODDS_BAND_CALIB_MAX_DEV_THRESHOLD: float = 0.15（odds-band conditional calib の WARN 閾値・ODDS_BAND_EDGES 4 band 各々）。閾値の厳密値は planner 事前登録（Claude's Discretion）・report に明示。

    check_acceptance_gate の warn_reasons に (a) selected-only calib_max_dev > PHASE12_SELECTED_ONLY_CALIB_MAX_DEV_THRESHOLD の場合の理由追加（selected 行の mask は呼出側が構築・p_lower EV で選ばれた馬）（b) 各 odds_band で conditional calib_max_dev > PHASE12_ODDS_BAND_CALIB_MAX_DEV_THRESHOLD の場合の理由追加。block_triggered は既存 §15.2 gate そのまま（D-06）。return dict（L1018-1029）に phase12_warn_triggered: bool / phase12_warn_reasons: list[str] / phase12_selected_only_calib_max_dev: float / phase12_odds_band_calib_max_dev: dict[str, float] を追加。binning は segment_eval.ODDS_BAND_EDGES / _odds_band / evaluate_segment_axis を import 再利用（codex HIGH#2・独自 binning 禁止・bit-identical）。

    segment_eval.py の evaluate_segment_axis に axis='ev_decile' と axis='disagreement' の2軸を追加（既存 binning 契約の再利用・pd.qcut(EV_lower, 10, labels=False, duplicates='drop') / disagreement = |logit(model_p) - logit(market_implied)| を pd.qcut(disagreement, 5, labels=False, duplicates='drop')・logit_clip は falsification.py と同一契約 LOGIT_CLIP_EPS=1e-6）。各 bin で compute_backtest_metrics（src/ev/metrics.py・既存）を group-by 適用し recovery_rate/profit_loss/hit_rate を返す。ODDS_BAND_EDGES・_odds_band の定義は変更しない（既存契約・bit-identical）。

    refund_accounting.py に `def compute_snapshot_final_slippage(payout_amount: float, fuku_odds_lower: float) -> float` を追加（D-07）。payout_amount は HARAI PayFukusyoPay（100円あたり・既存 _lookup_payfukusyo_pay の戻り値）・fuku_odds_lower は snapshot odds。slippage = payout_amount/100.0 - fuku_odds_lower。docstring に測定式（planner 事前登録・CONTEXT.md Claude's Discretion「snapshot→final slippage の具体的測定」）と HARAI PayFukusyoPay 再利用を明記。NaN 伝播（payout=0 no_bet や odds=NaN）は呼出側で filter・関数は NaN をそのまま返す（silent fallback 禁止・Shared Pattern 4）。

    FEATURE_COLUMNS / build_training_frame / load_feature_matrix の import は追加しない（SAFE-01・層分離・domain-analysis §5）。
  </action>
  <verify>
    <automated>KEIBA_SKIP_DB_TESTS=1 uv run pytest tests/model/test_evaluator_phase12_gate.py tests/evaluation/test_extended_metrics.py tests/model/test_evaluator*.py -x -q</automated>
  </verify>
  <done>
    - check_acceptance_gate の block_reasons/block_triggered が §15.2 不変（regression test GREEN）
    - check_acceptance_gate の warn_reasons に Phase 12 専用 WARN gate（selected-only/odds-band conditional calib）が追加・BLOCK でなく WARN（D-06）
    - PHASE12_SELECTED_ONLY_CALIB_MAX_DEV_THRESHOLD / PHASE12_ODDS_BAND_CALIB_MAX_DEV_THRESHOLD が事前登録定数として存在
    - segment_eval.evaluate_segment_axis に 'ev_decile'/'disagreement' 軸が追加（既存 binning 再利用・bit-identical）
    - refund_accounting.compute_snapshot_final_slippage が payout/100 - fuku_odds_lower を計算
    - evaluator / segment_eval / refund_accounting が FEATURE_COLUMNS を import しない（SAFE-01）
    - byte-reproducible（WARN gate 判定が2回呼出で同一）
    - KEIBA_SKIP_DB_TESTS=1 で tests/model/test_evaluator_phase12_gate.py / tests/evaluation/test_extended_metrics.py / 既存 test_evaluator*.py が全 GREEN
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fit_market_implied_calibrator ↔ test 窓 outcome | test 窓 outcome を fit に混入させない（§11.2 聖域） |
| run_falsification_test ↔ 学習 | falsification 回帰は学習しない・test 窓予測のみで評価（§11.2 聖域） |
| evaluation 層（falsification/segment_eval/evaluator）↔ feature 構築経路 | odds/ninki/market_implied が FEATURE_COLUMNS に混入すると市場回帰（SAFE-01） |
| §15.2 gate ↔ Phase 12 WARN gate | §15.2 事前登録指標が後知恵で変更されると聖域違反（D-06） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-11 | Tampering / Information disclosure | fit_market_implied_calibrator に test 窓 outcome を引数で混入（§11.2 聖域違反・後知恵リーク） | high | mitigate | シグネチャを {odds_calib, y_calib, calib_sample_size} のみに固定・test 窓 outcome 系引数を取らない（Shared Pattern 6）。test_falsification.py の Test 1 と tests/audit/test_audit_p_lower_falsification.py（Plan 05）の自己完結性 test で inspect.signature を検証 |
| T-12-12 | Tampering | falsification 回帰が test 窓 outcome を学習に使用（§11.2 聖域違反） | high | mitigate | run_falsification_test は fit（学習）を行わない・test 窓の予測のみで評価。market_implied calibrator は外部から受け取る（train/calib 窓で fit 済み）。docstring と test_falsification.py の Test 2 で保証 |
| T-12-13 | Tampering | odds/ninki/market_implied の FEATURE_COLUMNS 混入（SAFE-01 違反・市場回帰で edge 消滅） | high | mitigate | falsification.py / evaluator.py / segment_eval.py / refund_accounting.py は FEATURE_COLUMNS / build_training_frame / load_feature_matrix を import しない。odds/market_implied 引数の docstring に SAFE-01-ALLOW マーカー。tests/audit/test_audit_p_lower_falsification.py（Plan 05）の AST 検査で機械保証 |
| T-12-14 | Tampering | §15.2 事前登録指標の変更（D-06 違反・後知恵すり替え） | high | mitigate | check_acceptance_gate の block_reasons/block_triggered は §15.2 gate のまま一切触らない。Phase 12 WARN gate は warn_reasons に分離。test_evaluator_phase12_gate.py の Test 1 regression で不変性検証。compute_metrics の calibration_max_dev/Brier/LogLoss/sum(p) も一切変更しない |
| T-12-15 | Information disclosure（誤報） | verdict='feature_gap'/'structural_limit' の過度な統計的主張（Pitfall 1 鏡像・D-01 修正文） | medium | mitigate | verdict は「market_p pvalue < α で市場 residual が残る（特徴量不足候補）」で「モデルが市場を打つ保証」でない。docstring に「α=0.05 は事前登録・bin/odds_band サブ解析は Holm 補正・race_id clustered SE で race 内相関を統制」を明記（D-05・統計的厳密さ） |
| T-12-16 | Tampering | segment_eval binning の独自実装で bit-identical 崩れ（codex HIGH#2） | medium | mitigate | evaluate_segment_axis の 'ev_decile'/'disagreement' 軸は既存 _band_labels_from_edges / _odds_band / ODDS_BAND_EDGES と pd.qcut duplicates='drop' の再利用のみ・独自 binning 作成禁止。test_extended_metrics.py の Test 4 で bit-identical 検証 |
| T-12-17 | Tampering | statsmodels で `groups=` を直接 fit に渡し error/無視（Pitfall 4） | medium | mitigate | cov_kwds={'groups': race_id_array} が正しい API（GitHub #6287）。test_falsification.py の Test 3 で clustered SE が呼ばれることを検証・groups が無視されないことを間接的に確認（clustered と robust で SE 値が異なる） |
</threat_model>

<verification>
- src/eval/falsification.py が run_falsification_test + fit_market_implied_calibrator を提供（§11.2 聖域・clustered SE・Holm・logit clipping）
- evaluator.check_acceptance_gate に Phase 12 WARN gate 追加・§15.2 gate 不変（D-06）
- segment_eval.evaluate_segment_axis に ev_decile/disagreement 軸（既存 binning 再利用）
- refund_accounting.compute_snapshot_final_slippage（payout/100 - fuku_odds_lower）
- evaluation 層が FEATURE_COLUMNS を import しない（SAFE-01）
- KEIBA_SKIP_DB_TESTS=1 で tests/evaluation/* と tests/model/test_evaluator_phase12_gate.py が全 GREEN・既存 test_evaluator*.py regression なし
</verification>

<success_criteria>
- EVAL-02: falsification logit(outcome) ~ logit(market_implied) + logit(model_p) が race_id clustered SE・Holm 補正・market_implied 再校正（train/calib のみ）で実行可能
- EVAL-01: selected-only calibration / EV-decile / disagreement ROI / snapshot→final slippage が測定可能
- SC#4: オッズ帯別条件付き calibration WARN gate（§15.2 gate 不変・D-06）が投票層の過大予測を catch する経路
- SAFE-01: evaluation 層（falsification/segment_eval/evaluator/refund_accounting）が feature 構築経路から切り離されている
- §11.2: fit_market_implied_calibrator は calib slice のみ・run_falsification_test は学習しない（シグネチャ検証）
- 統計的厳密さ: verdict は過度な保証主張でない（D-01 修正文・D-05）
</success_criteria>

<output>
Create `.planning/phases/12-p-lower-ev-falsification-evaluation/12-03-SUMMARY.md` when done
</output>
