---
phase: 06-evaluation-calibration-gates
plan: 05
type: execute
wave: 4
depends_on:
  - 06-02
  - 06-03
  - 06-04
files_modified:
  - scripts/run_evaluation.py
  - tests/model/test_run_evaluation.py
  - reports/06-evaluation.md
  - reports/06-evaluation.json
autonomous: false
requirements:
  - EVAL-01
  - EVAL-02
  - EVAL-03

must_haves:
  truths:
    - "開発者が scripts/run_evaluation.py を実行すると・reports/06-evaluation.{md,json} と reports/06-segments/{axis}.{json,html}×6軸 が生成される（EVAL-01/02/03 統合・SC#1/2/3 達成）"
    - "reports/06-evaluation.md に受入ゲート判定（BLOCK/WARN）・主モデル比較表（METRIC_COLUMNS_EXTENDED + backtest 指標）・主モデル確定記録（D-07 理由）・segment 安定性サマリ・注記が含まれる"
    - "reports/06-evaluation.json が sort_keys=True ensure_ascii=False で byte-reproducible（evaluator.write_eval_report パターン踏襲）"
    - "BLOCK が発火した場合・run_evaluation.py は reports/06-evaluation.{md,json} を atomic write した *後* に RuntimeError で fail-loud する（REVIEW HIGH#6・block_reasons/warn_reasons/condition_flags/segments を含む完全レポートが残る）"
    - "--primary-model 引数で人間が選定した主モデルを指定すると・set_primary_model 経由で prediction.fukusho_prediction の is_primary フラグが更新される（D-07/D-09）。--primary-model 省略時は recommended_primary_model（優先順位表）のみ提示で is_primary 更新スキップ（REVIEW C7）"
    - "reports/06-evaluation.json に sum_p_measurement（large/small violation_rate 実測値・threshold=0.30・threshold_appropriate）が記録される（REVIEW HIGH#5・0.30 の経験的根拠）"
    - "reports/06-evaluation.json に reproducibility_checks.race_id_split_disjoint が記録される（REVIEW Codex MEDIUM・§8.4 聖域の再検証）"
    - "reports/06-evaluation.{md,json} の主モデル確定セクションに selection_reason（D-07 理由）と tiebreak_applied（D-08 発火段階 or None）が記録される"
    - "reports/06-evaluation.json の gate_result に yearly_inversion_warn（{year: {spearman_corr, spearman_pvalue, bin_inversions}}）が含まれ・reports/06-evaluation.md の受入ゲート判定セクションに年次反転 WARN として併記される（EVAL-02 SC#2『年次 Calibration 極端反転なし』の機械的 WARN レポート化・D-03 年次要素充足）"
  artifacts:
    - path: "scripts/run_evaluation.py"
      provides: "統合評価 CLI（EVAL-01/02/03・成果物読込→指標計算→ゲート判定→segment 評価→主モデル確定→レポート出力）"
      contains: "def main"
      min_lines: 200
    - path: "reports/06-evaluation.md"
      provides: "統合評価レポート（人間確認・ゲート判定・主モデル比較表・確定記録・segment サマリ・注記）"
      contains: "受入ゲート判定"
    - path: "reports/06-evaluation.json"
      provides: "機械可読評価レポート（sort_keys=True byte-reproducible・Phase 7 Streamlit 消費）"
      contains: "gate_result"
    - path: "tests/model/test_run_evaluation.py"
      provides: "run_evaluation.py の E2E smoke テスト（合成データ・reports/06-* 生成検証・BLOCK 発火検証・年次反転 WARN 検証）"
      min_lines: 100
  key_links:
    - from: "scripts/run_evaluation.py"
      to: "src/model/evaluator.py"
      via: "evaluate_all_models / check_sum_p_distribution / check_acceptance_gate / compute_monotonicity_warn / build_comparison_table を import して統合評価を実行"
      pattern: "from src\\.model\\.evaluator import"
    - from: "scripts/run_evaluation.py"
      to: "src/model/segment_eval.py"
      via: "evaluate_all_segments / write_segment_reports を import して6軸 segment 評価を実行"
      pattern: "from src\\.model\\.segment_eval import"
    - from: "scripts/run_evaluation.py"
      to: "src/db/prediction_load.py::set_primary_model"
      via: "--primary-model 引数で人間が選定した主モデルを set_primary_model 経由で is_primary フラグに反映（etl ロール・D-09）"
      pattern: "set_primary_model"
---

<objective>
Phase 6 Wave 3 統合（SC#1/2/3 達成）: 新規 scripts/run_evaluation.py で Plan 06-02（evaluator.py 拡張）・Plan 06-03（segment_eval.py）・Plan 06-04（is_primary migration）の全成果物を統合し・Phase 4/5 の stamped 成果物（prediction/backtest/label テーブル・reports/04-eval.{md,json}・reports/05-backtest.{md,json}）を消費して EVAL-01/02/03 の統合評価経路を一本化する。reports/06-evaluation.{md,json} と reports/06-segments/ を生成し・主モデル確定（D-07 人間判断→ set_primary_model・D-09）を実行する。

Purpose: SC#1「開発者が評価スイートを実行し的中率/回収率/損益/maxDD/購入点数/Brier/LogLoss/Calibration Curve を受け取る」・SC#2「受入ゲートが通過する（年次 Calibration 極端反転なし・bin 単調-ish・LogLoss/Brier が baselines 超過・sum(p) 理論値適合・median/SD/p10/p90 報告）」・SC#3「segment 別安定性評価が per-year/month/競馬場/頭数/人気帯/オッズ帯 の Calibration Curve を生成し segment collapse を可視化」を達成する。Phase 6 は評価専用（モデル再学習・予測再生成なし・D-06・Phase 4 SC#4 bit-identical 維持）。

Output: scripts/run_evaluation.py（新規統合 CLI）・tests/model/test_run_evaluation.py（E2E smoke）・reports/06-evaluation.{md,json}・reports/06-segments/{axis}.{json,html}×6軸
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-evaluation-calibration-gates/06-CONTEXT.md
@.planning/phases/06-evaluation-calibration-gates/06-RESEARCH.md
@.planning/phases/06-evaluation-calibration-gates/06-PATTERNS.md
@src/model/evaluator.py
@src/ev/metrics.py
@src/ev/report.py
@src/db/prediction_load.py
@scripts/run_train_predict.py
@scripts/run_backtest.py
@reports/04-eval.json
@reports/05-backtest.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scripts/run_evaluation.py 新規 — 統合評価 CLI（EVAL-01/02/03 統合・SC#1/2/3）</name>
  <files>scripts/run_evaluation.py, tests/model/test_run_evaluation.py</files>
  <read_first>
    - scripts/run_train_predict.py（CLI 起動フロー lines 46-88・argparse + masked DSN + try/except PsycopgError + finally pool.close・sys.path.insert で src.* import・RESEARCH Shared Pattern 6 / PATTERNS.md run_evaluation.py セクション analog）
    - scripts/run_backtest.py（report 出力フロー・reports/05-backtest.{md,json} 生成パターン）
    - src/model/evaluator.py（evaluate_all_models / check_sum_p_distribution / check_acceptance_gate / compute_monotonicity_warn / build_comparison_table / write_eval_report・Plan 06-02 で check_acceptance_gate/compute_monotonicity_warn 追加済）
    - src/ev/metrics.py（compute_backtest_metrics・回収率/P/L/maxDD/購入点数・EVAL-01 統合対象）
    - src/ev/report.py（REPORT_COLUMNS lines 41-53・md/json 1:1 検証パターン LOW-05）
    - src/model/segment_eval.py（evaluate_all_segments / write_segment_reports・Plan 06-03 で追加済）
    - src/db/prediction_load.py（set_primary_model・Plan 06-04 で追加済・model_version scoped idempotent）
    - reports/04-eval.json（D-04 事前登録素材・metrics/comparison_table 構造）
    - reports/05-backtest.json（backtest 成果・comparison_table 構造・EVAL-01 統合対象）
    - 06-RESEARCH.md「統合評価経路の設計」節（scripts/run_evaluation.py フロー6ステップ・reports/06-evaluation.{md,json} 慣例・CLI 引数）+ reports/06-segments/ ディレクトリ構成
    - 06-PATTERNS.md scripts/run_evaluation.py セクション（imports・parse_args・error handling 実装例・run_train_predict.py analog）
    - 06-CONTEXT.md D-07/D-08/D-09（人間判断・タイブレーク規則・is_primary フラグ）
  </read_first>
  <behavior>
    - Test 1: test_run_evaluation_generates_reports — 合成データ（prediction_df + label_df + reports/04-eval.json + reports/05-backtest.json の fixture）で run_evaluation の評価関数を呼び・reports/06-evaluation.{md,json} と reports/06-segments/{axis}.{json,html}×6軸 が生成される（tmp_path・E2E smoke）
    - Test 2: test_run_evaluation_gate_block_writes_report_then_raises — **REVIEW HIGH#6 対応:** check_acceptance_gate が block_triggered=True を返す場合・run_evaluation は RuntimeError を raise するが・**raise 前に reports/06-evaluation.{md,json} を atomic write する**（block_reasons/warn_reasons/condition_flags を含む最小レポート・segments 含む全状態）。例外メッセージに block_reasons を含める。合成 BLOCK 条件データ（baselines_all_lose AND sum_p_violation 両立）で検証。
    - Test 3: test_run_evaluation_json_byte_reproducible — 同じ入力で2回生成した reports/06-evaluation.json が byte-identical（sort_keys=True ensure_ascii=False）
    - Test 4: test_run_evaluation_report_sections — reports/06-evaluation.md に「受入ゲート判定」「主モデル比較表」「主モデル確定」「segment 安定性サマリ」「注記」の5セクションが含まれる
    - Test 5: test_run_evaluation_primary_model_record — reports/06-evaluation.json の primary_model フィールドが {model_type, model_version, feature_snapshot_id, as_of_datetime, selection_reason, tiebreak_applied} スキーマに準拠。**REVIEW C7:** --primary-model 省略時は `primary_model=None`・`selection_reason="未確定（reports のみ生成）"`・`tiebreak_applied=None` で記録され・set_primary_model は呼ばれない（DB 更新スキップ）。
    - Test 6: test_run_evaluation_backtest_integration — reports/05-backtest.json の backtest 指標（回収率/P/L/maxDD）が reports/06-evaluation.md の主モデル比較表に統合される（EVAL-01）。**REVIEW C8:** 主モデル比較表の backtest 指標は「優位 policy の代表窓（30min_before/10min_before の優位方）」または「5窓×2policy の重み付き平均」のいずれか固定集計規則で1行に集約（reports/06-evaluation.md に集計方法を注記）。
    - Test 7: test_run_evaluation_yearly_inversion_warn_section — reports/06-evaluation.json の gate_result に yearly_inversion_warn キーが含まれ・reports/06-segments/year.json の各年 curve に compute_monotonicity_warn を適用した {year: {spearman_corr, spearman_pvalue, bin_inversions}} スキーマに準拠
    - Test 8: test_run_evaluation_sum_p_violation_rate_measured — **REVIEW HIGH#5 対応:** reports/06-evaluation.json に `sum_p_measurement: {large_violation_rate, small_violation_rate, total_races, threshold, threshold_appropriate: bool}` フィールドが含まれる。実データで violation_rate を計測し・0.30 が安全網として妥当か（偽陽性 BLOCK にならないか）の判定材料を記録。run_evaluation.py 完了後に人間が本フィールドを確認し・0.30 が不適切なら Plan 06-02 の SUM_P_BLOCK_THRESHOLD 定数を調整。
    - Test 9: test_run_evaluation_race_id_split_integrity — **REVIEW Codex MEDIUM 対応:** reports/06-evaluation.json の gate_result に `reproducibility_checks.race_id_split_disjoint: bool` フィールドが含まれる。prediction_df を split='train'/'val'/'test' で分割し・set(train_races) ∩ set(test_races) == ∅ を確認（§8.4 聖域の再検証・評価専用フェーズでも監視）。
  </behavior>
  <action>
    scripts/run_evaluation.py を新規作成（RESEARCH「統合評価経路の設計」節フロー6ステップ・PATTERNS.md scripts/run_evaluation.py セクション実装例準拠）:

    1. **imports + sys.path.insert（run_train_predict.py lines 58-82 パターン）:**
       - `from __future__ import annotations`, `import argparse, json, logging, sys`, `from pathlib import Path`
       - `_REPO_ROOT = Path(__file__).resolve().parent.parent` + `sys.path.insert(0, str(_REPO_ROOT))`
       - `from psycopg.errors import Error as PsycopgError`
       - `from src.config.settings import Settings`
       - `from src.db.connection import make_pool, readonly_cursor, write_cursor`
       - `from src.db.prediction_load import set_primary_model`
       - `from src.model.evaluator import (evaluate_all_models, check_sum_p_distribution, check_acceptance_gate, compute_monotonicity_warn, compute_yearly_inversion_warn, build_comparison_table, COMPARABLE_BASELINES, SUM_P_BLOCK_THRESHOLD, METRIC_COLUMNS_EXTENDED)`
       - `from src.model.segment_eval import evaluate_all_segments, write_segment_reports`
       - `from src.model.artifact import _atomic_write_text`

    2. **parse_args（run_train_predict.py lines 94-142 パターン・PATTERNS.md 実装例準拠・REVIEW C7 対応）:**
       - `--feature-snapshot-id`（required・READ scope・postreview-v2 等）
       - `--as-of-datetime`（required・prediction の as_of_datetime・UPDATE scope・ISO8601・set_primary_model 側で pd.Timestamp で canonical parse）
       - `--primary-model`（choices=["lightgbm","catboost"]・default=None・**REVIEW C7: 省略時は is_primary 更新スキップ・reports のみ生成**。D-08 タイブレーク規則は*自動適用せず* reports に優先順位表（backtest 回収率→計算コスト低=LightGBM→Brier→LogLoss→AUC）を `recommended_primary_model` として提示し・人間が --primary-model で明示指定した時のみ set_primary_model 経由で is_primary を更新。タイブレーク自動関数の実装 task は廃止）
       - `--selection-reason`（default=None・D-07 人間判断の理由テキスト・--primary-model 省略時は無視）
       - `--skip-segments`（action="store_true"・開発時・segment 評価スキップ）
       - `--out-md`（default="reports/06-evaluation.md"）
       - `--out-json`（default="reports/06-evaluation.json"）
       - `--segments-dir`（default="reports/06-segments"）

    3. **main フロー6ステップ（RESEARCH フロー・PATTERNS.md 実装例・REVIEW HIGH#5/HIGH#6/C8 対応）:**
       - Step 1: 成果物読込（readonly ロール・READ only）: prediction_df ← SELECT FROM prediction.fukusho_prediction WHERE feature_snapshot_id=? AND split='test'（両モデル・model_type/model_version/p_fukusho_hit/race_key/entry_count/race_date）・label_df ← SELECT FROM label.fukusho_label（race_key/race_date/entry_count/jyocd/ninki/fukuoddslower/fukusho_hit_validated・Plan 06-01 test_segment_axis_columns で経路確認済・label 欠損時は market データ JOIN を Segment 評価用 df 構築時に追加）・eval_metrics_04 ← json.loads(reports/04-eval.json)・backtest_metrics_05 ← json.loads(reports/05-backtest.json)
       - Step 2: 確率品質指標計算（evaluator.py・Plan 06-02）: prediction_df と label_df を race_key 7カラムで JOIN（HIGH-1/HIGH-2 契約・cartesian duplication 防御）・evaluate_all_models で両モデル + baselines の compute_metrics（quantile_max_dev/ECE/MCE 追加済・REVIEW C5: quantile_max_dev は _compute_quantile_max_dev（ガードなし）・MCE は _compute_mce（MIN_BIN_COUNT ガード付き）で別定義）・check_sum_p_distribution で sum(p) 検査・**REVIEW HIGH#5:** sum_p_measurement dict（large_violation_rate/small_violation_rate/total_races/threshold=0.30/threshold_appropriate）を構築（0.30 が実分布で偽陽性 BLOCK にならないかの判定材料）
       - Step 3: segment 別安定性評価（segment_eval.py・Plan 06-03）: --skip-segments でない場合・JOIN 済み df で evaluate_all_segments（6軸・REVIEW HIGH#4 banding 適用済み）・write_segment_reports で reports/06-segments/{axis}.{json,html}×6軸 生成
       - Step 4: 受入ゲート判定（evaluator.check_acceptance_gate・Plan 06-02・**REVIEW HIGH#6: segment 評価 *後に* gate 判定**）: metrics_dict と sum_p_check で gate_result を取得・compute_monotonicity_warn で WARN 指標（Spearman/反転数）・compute_yearly_inversion_warn で年次反転 WARN を集計（reports/06-segments/year.json の各年 curve に適用）・**REVIEW Codex MEDIUM: race_id split integrity check 追加**（prediction_df を split で分け train/test の race_id 集合が disjoint か確認・結果を gate_result["reproducibility_checks"]["race_id_split_disjoint"] に記録）
       - Step 5: レポート生成（reports/06-evaluation.{md,json}）→ **REVIEW HIGH#6: gate 判定後に必ず atomic write（BLOCK の場合も含む）**・その後 --primary-model 指定時は set_primary_model で is_primary UPDATE
       - Step 6: BLOCK 発火時（block_triggered==True）は reports/06-evaluation.{md,json} の atomic write 完了後・logger.error で block_reasons 出力・RuntimeError 送出（D-01・hybrid gate・出荷停止）。**REVIEW HIGH#6:** BLOCK 時も reports が残る（block_reasons/warn_reasons/condition_flags/segments 含む）ことで原因調査が可能。

       **重要（REVIEW HIGH#6 制御フロー）:** 従来は Step 3 (gate) → Step 4 (segments) → RuntimeError の順で・BLOCK 時に reports/06-segments/ が未生成になるリスクがあった。改訂後は Step 3 (segments) → Step 4 (gate) → Step 5 (reports atomic write・BLOCK の場合も含む) → Step 6 (BLOCK 時 RuntimeError) の順で・BLOCK 時も完全な reports が残る。

    4. **reports/06-evaluation.md 構造（RESEARCH lines 629 + PATTERNS.md reports セクション）:**
       - `# Phase 6 Evaluation Report (EVAL-01/02/03 / §15.1/§15.2/§15.3)`
       - `## 受入ゲート判定（BLOCK/WARN）` — gate_verdict / block_reasons / warn_reasons / condition_flags / WARN 指標（Spearman・反転数・bin 単調違反）・年次反転 WARN・**REVIEW HIGH#5: sum(p) violation_rate 計測結果（0.30 妥当性判定材料）**・**REVIEW Codex MEDIUM: race_id split integrity 結果**
       - `## 主モデル比較表（全指標）` — _df_to_markdown_table（METRIC_COLUMNS_EXTENDED + backtest 指標回収率/P/L/maxDD 統合・**REVIEW C8: 集計方法（優位 policy の代表窓 or 重み付き平均）を注記**）
       - `## 主モデル確定（理由記録・D-07）` — primary_model / selection_reason / tiebreak_applied（**REVIEW C7: --primary-model 省略時は recommended_primary_model + 優先順位表のみ**）
       - `## segment 安定性サマリ（6軸）` — reports/06-segments/ への参照・scalar 表へのリンク
       - `## 注記` — D-04 事前登録基準（Calibration 重視）・BL-1 caveat（順序付け能力ゼロの副産物・debug Resolution #4）・BL-3 §14.2 caveat（同一情報条件でない）・**REVIEW HIGH#5: SUM_P_BLOCK_THRESHOLD=0.30 の経験的根拠（現データ violation_rate 実測・安全網としての妥当性）**

    5. **reports/06-evaluation.json 構造（sort_keys=True・evaluator.write_eval_report パターン・PATTERNS.md reports セクション・REVIEW HIGH#5/Codex MEDIUM 対応）:**
       - `{gate_result, comparison_table, primary_model, segment_summary, backtest_summary, sum_p_measurement, reproducibility_checks, constants, notes}`
       - gate_result: check_acceptance_gate の戻り値（block_triggered/block_reasons/warn_reasons/condition_flags/gate_verdict/comparable_baselines/sum_p_block_threshold）+ compute_monotonicity_warn の結果 + yearly_inversion_warn
       - comparison_table: build_comparison_table の records（METRIC_COLUMNS_EXTENDED・**REVIEW C5: quantile_max_dev と mce は別列（定義分離）**）
       - primary_model: {model_type, model_version, feature_snapshot_id, as_of_datetime, selection_reason, tiebreak_applied}（**REVIEW C7: --primary-model 省略時は null・替わりに recommended_primary_model フィールド**）
       - segment_summary: 各軸の scalar 指標サマリ
       - backtest_summary: reports/05-backtest.json から統合（**REVIEW C8: 集計方法を backtest_aggregation_method フィールドで明記**）
       - **REVIEW HIGH#5:** sum_p_measurement: {large_violation_rate, small_violation_rate, total_races, threshold: 0.30, threshold_appropriate: bool, diagnostic_note} — 実データで violation_rate を計測し・0.30 が安全網として妥当か（偽陽性 BLOCK を出さないか）の判定材料。run_evaluation 完了後に人間が確認し・不適切なら Plan 06-02 の SUM_P_BLOCK_THRESHOLD 定数を調整（escape-hatch `--allow-sump-warn` も将来検討）。
       - **REVIEW Codex MEDIUM:** reproducibility_checks: {race_id_split_disjoint: bool, n_train_races, n_test_races} — §8.4 聖域の再検証
       - constants: {METRIC_COLUMNS_EXTENDED, SUM_P_BLOCK_THRESHOLD, COMPARABLE_BASELINES, CALIBRATION_CURVE_BINS, CALIBRATION_CURVE_MIN_BIN_COUNT}
       - notes: {d04_selection_criterion, bl1_caveat, bl3_caveat, sum_p_diagnostic, sum_p_threshold_rationale}

    6. **Error handling（run_train_predict.py パターン・CLAUDE.md hybrid gate・REVIEW HIGH#6）:** try/except PsycopgError / finally pool.close()・**BLOCK 発火時は reports atomic write 完了後に RuntimeError**（例外メッセージに block_reasons を含む）・masked DSN ログ出力（生 DSN 絶対禁止）

    tests/model/test_run_evaluation.py を新設（上記 behavior の9テスト・合成データ・DB 不要の unit smoke + reports/04-eval.json/05-backtest.json の fixture・REVIEW HIGH#5/HIGH#6/C7/C8/Codex MEDIUM 対応）:
    - 合成 fixture: `_make_synthetic_eval_inputs(tmp_path)` が prediction_df（両モデル・test split）+ label_df + reports/04-eval.json + reports/05-backtest.json の合成 fixture を生成
    - test_run_evaluation_generates_reports / test_run_evaluation_gate_block_writes_report_then_raises（REVIEW HIGH#6）/ test_run_evaluation_json_byte_reproducible / test_run_evaluation_report_sections / test_run_evaluation_primary_model_record（REVIEW C7 省略時）/ test_run_evaluation_backtest_integration（REVIEW C8 集計方法）/ test_run_evaluation_yearly_inversion_warn_section / test_run_evaluation_sum_p_violation_rate_measured（REVIEW HIGH#5）/ test_run_evaluation_race_id_split_integrity（REVIEW Codex MEDIUM）

    **厳守:** run_evaluation.py は評価専用・モデル再学習・予測再生成を行わない（D-06・Phase 4 SC#4 bit-identical 維持）。readonly ロールで READ・etl ロールで is_primary UPDATE と reports/ 出力のみ。masked DSN ログ出力。**BLOCK 発火時も reports を atomic write してから RuntimeError（REVIEW HIGH#6）。** 主モデル選定の判断は人間（D-07）・--primary-model 省略時は推奨表提示のみで is_primary 更新スキップ（REVIEW C7）。
  </action>
  <verify>
    <automated>uv run pytest tests/model/test_run_evaluation.py -v -x</automated>
  </verify>
  <done>
    - scripts/run_evaluation.py が新規作成される（imports・parse_args・main フロー6ステップ・error handling）
    - tests/model/test_run_evaluation.py の6テストが全て GREEN（合成データ E2E smoke）
    - test_run_evaluation_generates_reports で reports/06-evaluation.{md,json} と reports/06-segments/×6軸 が生成される
    - test_run_evaluation_gate_block_writes_report_then_raises で BLOCK 発火時に reports atomic write 後に RuntimeError（REVIEW HIGH#6）
    - test_run_evaluation_json_byte_reproducible で JSON が byte-identical（sort_keys=True）
    - test_run_evaluation_report_sections で md に5セクションが含まれる
    - test_run_evaluation_primary_model_record で primary_model フィールドスキーマ検証（REVIEW C7: --primary-model 省略時 null・替わりに recommended_primary_model）
    - test_run_evaluation_yearly_inversion_warn_section で gate_result.yearly_inversion_warn スキーマ検証
    - test_run_evaluation_sum_p_violation_rate_measured で sum_p_measurement フィールド検証（REVIEW HIGH#5）
    - test_run_evaluation_race_id_split_integrity で reproducibility_checks.race_id_split_disjoint 検証（REVIEW Codex MEDIUM）
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 実データ run_evaluation.py 実行 + 主モデル選定（D-07・SC#1/2/3 達成確認）</name>
  <files>reports/06-evaluation.md, reports/06-evaluation.json, reports/06-segments/</files>
  <action>
    checkpoint:human-verify — 実データ（prediction.fukusho_prediction・label.fukusho_label・reports/04-eval.json・reports/05-backtest.json）で run_evaluation.py を実行し・reports/06-evaluation.{md,json} と reports/06-segments/ を生成し・人間が D-04 事前登録基準（Calibration 重視）で主モデルを選定し・--primary-model で is_primary フラグを確定する。SC#1/2/3 達成の最終確認。
    実行手順: (1) uv run pytest tests/model/ tests/db/ -v が GREEN を確認（KEIBA_SKIP_DB_TESTS unset） (2) `uv run python scripts/run_evaluation.py --feature-snapshot-id 20260620-1a-postreview-v2 --as-of-datetime <Phase4 FIXED_REPRODUCE_TS>`（--primary-model 省略・reports のみ生成） (3) reports/06-evaluation.md を確認（ゲート判定 WARN 想定・主モデル比較表・segment サマリ・注記） (4) reports/06-evaluation.json の byte-reproducible 確認（2回実行で同一） (5) reports/06-segments/ の6軸×{json,html}=12ファイル確認 (6) D-04 基準で主モデル選定（reports/06-evaluation.md 比較表の quantile/ECE/MCE + calibration_max_dev + brier/logloss/auc + sum(p) + backtest 回収率を照合・現データでは LightGBM 優位の公算大） (7) `uv run python scripts/run_evaluation.py --feature-snapshot-id 20260620-1a-postreview-v2 --as-of-datetime <Phase4 FIXED_REPRODUCE_TS> --primary-model <lightgbm|catboost> --selection-reason "D-04 Calibration 重視基準で選定"` で is_primary 確定 (8) prediction.fukusho_prediction の is_primary フラグ確認（SELECT model_type, bool_or(is_primary) GROUP BY model_type） (9) SC#1/2/3 達成確認。
  </action>
  <verify>
    <automated>uv run pytest tests/model/ tests/db/ -v</automated>
    <human-check>実データ run_evaluation.py が reports/06-* を生成・人間が "approved" + 主モデル選定結果（lightgbm または catboost）で SC#1/2/3 達成を承認して resume</human-check>
  </verify>
  <done>実データで reports/06-evaluation.{md,json} と reports/06-segments/×6軸 が生成され・受入ゲート判定が WARN（現データで BLOCK 発火なし想定）・人間が D-04 基準で主モデルを選定し is_primary フラグを確定・SC#1/2/3 達成</done>
  <what-built>
    scripts/run_evaluation.py 統合 CLI と tests/model/test_run_evaluation.py E2E smoke テストが完成した。本 checkpoint は実データ（prediction.fukusho_prediction・label.fukusho_label・reports/04-eval.json・reports/05-backtest.json）で run_evaluation.py を実行し・reports/06-evaluation.{md,json} と reports/06-segments/ を生成し・人間が D-04 事前登録基準（Calibration 重視）で主モデルを選定し・--primary-model で is_primary フラグを確定する。SC#1/2/3 達成の最終確認。
  </what-built>
  <how-to-verify>
    1. uv run pytest tests/model/test_run_evaluation.py tests/model/test_evaluator.py tests/model/test_evaluator_gate.py tests/model/test_segment_eval.py tests/db/test_is_primary_flag.py -v が全て GREEN であることを確認（KEIBA_SKIP_DB_TESTS unset）
    2. 実データで run_evaluation.py を reports のみ生成（主モデル未確定）: `uv run python scripts/run_evaluation.py --feature-snapshot-id 20260620-1a-postreview-v2 --as-of-datetime <Phase 4 の FIXED_REPRODUCE_TS>`（--primary-model 省略・is_primary 更新スキップ）
    3. reports/06-evaluation.md を確認: 受入ゲート判定（現データでは WARN 想定・両 BLOCK 条件とも発火しない）・主モデル比較表（METRIC_COLUMNS_EXTENDED + backtest 指標）・segment 安定性サマリ・注記（D-04/BL-1/BL-3 caveat）
    4. reports/06-evaluation.json を確認: gate_result / comparison_table / segment_summary / constants / notes の構造・byte-reproducible（2回実行で同一）
    5. reports/06-segments/ を確認: 6軸 × {json,html} = 12ファイルが生成され・HTML に plotly.js（~3MB）が埋込まれている
    6. D-04 事前登録基準（Calibration 重視）で主モデルを選定: reports/06-evaluation.md の比較表を確認し・quantile_max_dev/ECE/MCE + calibration_max_dev（事前登録）+ brier/logloss/auc + sum(p) + backtest 回収率 を照合。現データでは LightGBM が全指標で優位の公算大（reports/04-eval.json 実データ: lightgbm brier=0.15222/logloss=0.47488 < catboost 0.15453/0.48243）。タイブレーク規則（D-08: backtest 回収率 → 計算コスト低=LightGBM）の発火有無を確認
    7. 選定後・--primary-model で is_primary フラグを確定: `uv run python scripts/run_evaluation.py --feature-snapshot-id 20260620-1a-postreview-v2 --as-of-datetime <Phase 4 の FIXED_REPRODUCE_TS> --primary-model lightgbm --selection-reason "D-04 Calibration 重視基準で LightGBM を選定（quantile_max_dev/ECE/MCE + brier/logloss/auc 全てで CatBoost を上回る）"`（選定モデルが LightGBM の場合・CatBoost の場合は --primary-model catboost）
    8. prediction.fukusho_prediction の is_primary フラグを確認: `SELECT model_type, count(*), bool_or(is_primary) FROM prediction.fukusho_prediction WHERE feature_snapshot_id='20260620-1a-postreview-v2' GROUP BY model_type`（選定モデル=true・未選定=false・両モデル行は保持）
    9. SC#1/2/3 達成を確認: SC#1（的中率/回収率/損益/maxDD/購入点数/Brier/LogLoss/Calibration Curve が reports/06-evaluation.md に含まれる）・SC#2（受入ゲート判定が BLOCK でなく WARN・現データで発火しない想定）・SC#3（6軸 segment 別 Calibration Curve が reports/06-segments/ に生成される）
  </how-to-verify>
  <resume-signal>Type "approved" + 主モデル選定結果（lightgbm または catboost）+ 選定理由で SC#1/2/3 達成を承認。課題がある場合は describe issues（BLOCK 発火時・segment 欠損時等）。</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → live DB (readonly) | run_evaluation.py が readonly ロールで prediction/label/backtest テーブルと reports/ を SELECT（READ only） |
| CLI → live DB (etl) | run_evaluation.py が etl ロールで prediction.fukusho_prediction の is_primary を set_primary_model 経由で UPDATE（model_version scoped） |
| CLI → filesystem | run_evaluation.py が reports/06-evaluation.{md,json} と reports/06-segments/ を _atomic_write_text で出力 |
| 人間判断 → 機械的反映 | checkpoint:human-verify で人間が主モデルを選定し・--primary-model 引数で set_primary_model 経由で is_primary フラグに反映（D-07/D-09） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | Tampering/Elevation | 主モデル選定の後知恵すり替え（結果を見てから --primary-model を変更） | mitigate | D-07 人間判断 + selection_reason 記録（reports/06-evaluation.{md,json}）・D-04 事前登録基準（Calibration 重視）を固定・D-08 タイブレーク規則の発火有無を tiebreak_applied に記録・Phase 8 対抗的監査（TEST-01・deferred） |
| T-06-14 | Denial of Service | BLOCK 発火時の RuntimeError でパイプライン停止 | accept | D-01 構造的 BLOCK は出荷停止が正しい挙動（モデル本質的破綻・hybrid gate）・現データでは両 BLOCK 条件とも発火しない想定（RESEARCH「現状データでの想定」節）・発火時は原因調査が必要 |
| T-06-15 | Information Disclosure | run_evaluation.py のログに生 DSN が含まれる | mitigate | Settings から dsn_masked / etl_dsn_masked を取得してログ出力（run_train_predict.py lines 13-32 パターン・生 DSN 絶対禁止） |
| T-06-16 | Tampering | reports/06-evaluation.json の改竄（主モデル確定記録の書換） | mitigate | json.dumps(sort_keys=True, ensure_ascii=False) + _atomic_write_text で byte-reproducible・Git 管理で改竄検知・selection_reason と tiebreak_applied で監査性担保 |
| T-06-17 | Tampering | クエリ文字列組み立てによる SQL injection（成果物読込・set_primary_model） | mitigate | parameterized query（psycopg.sql.SQL + Placeholder）・既存 prediction_load.py パターン踏襲（T-06-09 延長） |
</threat_model>

<verification>
- uv run pytest tests/model/ tests/db/ -v が GREEN（Phase 6 全テスト・KEIBA_SKIP_DB_TESTS unset）
- 実データで run_evaluation.py が reports/06-evaluation.{md,json} と reports/06-segments/×6軸 を生成
- reports/06-evaluation.json が byte-reproducible（2回実行で同一）
- 受入ゲート判定が現データで WARN（両 BLOCK 条件とも発火しない想定）
- 人間が D-04 基準で主モデルを選定し・--primary-model で is_primary フラグを確定
- SC#1/2/3 達成を確認
</verification>

<success_criteria>
- SC#1（EVAL-01）: reports/06-evaluation.md に的中率/回収率/損益/maxDD/購入点数/Brier/LogLoss/Calibration Curve が含まれる
- SC#2（EVAL-02）: 受入ゲート判定（D-01/D-02 BLOCK / D-03 WARN）が reports/06-evaluation.md に記録される
- SC#3（EVAL-03）: 6軸 segment 別 Calibration Curve が reports/06-segments/ に生成される
- D-07/D-09（主モデル確定）: 人間が選定し・is_primary フラグが確定し・reports/06-evaluation.{md,json} に理由が記録される
- Phase 4 SC#4 bit-identical 維持: モデル再学習・予測再生成なし（D-06）
</success_criteria>

<output>
Create `.planning/phases/06-evaluation-calibration-gates/06-05-SUMMARY.md` when done
</output>

<artifacts>
## Artifacts this phase produces

**CLI (scripts/run_evaluation.py 新規):**
- `main(argv=None)` — 統合評価 CLI エントリポイント（フロー6ステップ・argparse + masked DSN + try/except PsycopgError + finally pool.close）
- `parse_args(argv=None)` — CLI 引数（--feature-snapshot-id / --as-of-datetime / --primary-model / --selection-reason / --skip-segments / --out-md / --out-json / --segments-dir）
- フロー内の純粋関数群（テスト用に切り出し）: 成果物読込・指標計算統合・ゲート判定統合・segment 評価統合・レポート出力

**Report files (生成・実データ実行時):**
- reports/06-evaluation.md — 統合評価レポート（5セクション: 受入ゲート判定 / 主モデル比較表 / 主モデル確定 / segment 安定性サマリ / 注記）
- reports/06-evaluation.json — 機械可読レポート（sort_keys=True byte-reproducible・{gate_result, comparison_table, primary_model, segment_summary, backtest_summary, constants, notes}）
- reports/06-segments/{year,month,jyocd,entry_count,ninki,odds_band}.{json,html} — 6軸 segment 評価（Plan 06-03 write_segment_reports 経由）

**JSON schema (reports/06-evaluation.json):**
- `gate_result`: {block_triggered, block_reasons, warn_reasons, condition_flags: {baselines_all_lose, sum_p_violation}, gate_verdict, comparable_baselines, sum_p_block_threshold} + monotonicity_warn + yearly_inversion_warn
- `comparison_table`: METRIC_COLUMNS_EXTENDED records（両モデル + baselines・REVIEW C5: quantile_max_dev と mce は別列）
- `primary_model`: {model_type, model_version, feature_snapshot_id, as_of_datetime, selection_reason, tiebreak_applied}（**REVIEW C7: --primary-model 省略時は null・替わりに recommended_primary_model**）
- `segment_summary`: 各軸の scalar 指標サマリ
- `backtest_summary`: reports/05-backtest.json から統合（**REVIEW C8: backtest_aggregation_method フィールドで集計規則を明記**）
- **REVIEW HIGH#5:** `sum_p_measurement`: {large_violation_rate, small_violation_rate, total_races, threshold: 0.30, threshold_appropriate, diagnostic_note}
- **REVIEW Codex MEDIUM:** `reproducibility_checks`: {race_id_split_disjoint, n_train_races, n_test_races}
- `constants`: {METRIC_COLUMNS_EXTENDED, SUM_P_BLOCK_THRESHOLD, COMPARABLE_BASELINES, CALIBRATION_CURVE_BINS, CALIBRATION_CURVE_MIN_BIN_COUNT}
- `notes`: {d04_selection_criterion, bl1_caveat, bl3_caveat, sum_p_diagnostic, sum_p_threshold_rationale}

**Test files:**
- tests/model/test_run_evaluation.py（新規・9テスト・合成データ E2E smoke・REVIEW HIGH#5/HIGH#6/C7/C8/Codex MEDIUM 対応）
</artifacts>
