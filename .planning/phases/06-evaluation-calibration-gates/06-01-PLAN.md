---
phase: 06-evaluation-calibration-gates
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - tests/model/__init__.py
  - tests/model/test_evaluator.py
  - tests/model/test_segment_axis_columns.py
autonomous: true
requirements:
  - EVAL-01
  - EVAL-02
  - EVAL-03
user_setup: []

must_haves:
  truths:
    - "uv run pytest tests/model/test_evaluator.py が PASS する（evaluator.py 拡張前の現状契約を固定化）"
    - "uv run pytest tests/model/test_segment_axis_columns.py が PASS する（segment 6軸カラム経路確認・Open Question #1 解決）"
    - "uv add plotly + uv add scipy 実行後、uv.lock と pyproject.toml に plotly と scipy（明示依存・REVIEW Codex LOW）が追記される"
    - "reports/04-eval.json が現 evaluator.py の compute_metrics 出力（calibration_max_dev_guarded 含む9列）と整合するよう再生成される（REVIEW C6）・事前登録 unguarded 値は不変"
  artifacts:
    - path: "pyproject.toml"
      provides: "plotly 依存関係追加（segment HTML 出力用・D-10）"
      contains: "plotly"
    - path: "tests/model/test_evaluator.py"
      provides: "evaluator.py Phase 4 既存契約の固定化テスト（Phase 6 拡張前の回帰検知基盤）"
      min_lines: 80
    - path: "tests/model/test_segment_axis_columns.py"
      provides: "segment 6軸カラム経路確認テスト（Open Question #1・label.fukusho_label + prediction.fukusho_prediction の JOIN で ninki/odds_band 揃うか）。REVIEW HIGH#3: 欠損時は WARN-skip でなく fail-loud（pytest.fail）。market データフォールバック（test_market_data_segment_axes_if_label_missing）で SC#3 履行可能性を二段階確認。"
      min_lines: 70
  key_links:
    - from: "tests/model/test_evaluator.py"
      to: "src/model/evaluator.py"
      via: "compute_metrics / _compute_calibration_max_dev / _compute_calibration_max_dev_guarded / check_sum_p_distribution の現状契約を import して assert"
      pattern: "from src\\.model\\.evaluator import"
    - from: "tests/model/test_segment_axis_columns.py"
      to: "label.fukusho_label + prediction.fukusho_prediction"
      via: "requires_db テストで information_schema.columns を SELECT し segment 軸カラム（race_date/jyocd/entry_count/ninki/fukuoddslower）の存在を確認"
      pattern: "information_schema\\.columns"
---

<objective>
Phase 6 Wave 0 基盤: (1) plotly 依存関係追加（D-10 segment HTML 出力の前提）・(2) evaluator.py Phase 4 既存契約の固定化テスト新設（Phase 6 拡張前の回帰検知基盤・RESEARCH 指摘: test_evaluator.py は Phase 4 で未作成）・(3) segment 6軸カラム経路の実 DB 確認テスト（Open Question #1・researcher 確定の segment 軸が label.fukusho_label + prediction.fukusho_prediction の JOIN で揃うか検証）。

Purpose: Phase 6 本体（Wave 1 evaluator.py 拡張・segment_eval.py 新規）を実装する前に、(a) 外部依存 plotly を解決し、(b) Phase 4 evaluator.py の現状契約（事前登録指標 calibration_max_dev=0.23077/0.25789 等）を回帰テストで固定化し（T-04-24 後知恵すり替え防止の前提）、(c) segment 軸データ経路（researcher が CONTEXT.md で「揃うことを確認」としたが実コード未検証）を実 DB で検証する。これらは全後続 Wave の前提。

Output: pyproject.toml（plotly 追加）・uv.lock・tests/model/test_evaluator.py（Phase 4 契約固定化）・tests/model/test_segment_axis_columns.py（segment 軸カラム確認）・tests/model/__init__.py（package marker）
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-evaluation-calibration-gates/06-CONTEXT.md
@.planning/phases/06-evaluation-calibration-gates/06-RESEARCH.md
@.planning/phases/06-evaluation-calibration-gates/06-PATTERNS.md
@.planning/phases/06-evaluation-calibration-gates/06-VALIDATION.md
@src/model/evaluator.py
@tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: plotly 依存関係追加 + evaluator.py Phase 4 既存契約固定化テスト新設</name>
  <files>pyproject.toml, uv.lock, tests/model/__init__.py, tests/model/test_evaluator.py</files>
  <read_first>
    - pyproject.toml（現状の dependencies 配列・requires-python=">=3.12,<3.13"）
    - src/model/evaluator.py（Phase 4 実装の正・compute_metrics/METRIC_COLUMNS/CALIBRATION_CURVE_BINS=10/CALIBRATION_CURVE_MIN_BIN_COUNT=30/SUM_P_BOUNDS）
    - reports/04-eval.json（実データ値: lightgbm calibration_max_dev=0.23076923076923073, catboost=0.25789298770355484, bl1=0.0014259639516354672 等）
    - tests/conftest.py（KEIBA_SKIP_DB_TESTS policy・requires_db marker・pytest_collection_modifyitems）
    - 06-PATTERNS.md（tests/model/test_evaluator.py analog 行: tests/model/test_prediction_load.py 構造 + tests/model/test_orchestrator.py::test_reproduce_bit_identical bit-identical 回帰）
    - 06-RESEARCH.md Pitfall 3（事前登録指標の意図せぬ変更・T-04-24）
  </read_first>
  <behavior>
    - Test 1: test_compute_metrics_uniform_max_dev_unchanged — evaluator.compute_metrics の戻り値に calibration_max_dev キーが存在し・かつ事前登録定義（uniform・ガードなし）であること。合成データで2回呼出しで同値（Phase 4 SC#4 bit-identical 延長）
    - Test 2: test_calibration_max_dev_report_value_match — reports/04-eval.json の LightGBM calibration_max_dev=0.23076923076923073 / CatBoost=0.25789298770355484 / BL-1=0.0014259639516354672 を固定 seed 合成データではなく reports/04-eval.json を直接読込して assert（実データ値の回帰固定化・D-04 事前登録指標不変の前提）
    - Test 3: test_calibration_curve_bins_constant — CALIBRATION_CURVE_BINS == 10 / CALIBRATION_CURVE_STRATEGY == "uniform" / CALIBRATION_CURVE_MIN_BIN_COUNT == 30（binning 契約固定・Phase 6 拡張でも不変）
    - Test 4: test_compute_calibration_max_dev_guarded_bit_identical — _compute_calibration_max_dev_guarded を合成データで2回呼出し np.array_equal（純 NumPy bit-identical・Phase 4 SC#4 延長）
    - Test 5: test_check_sum_p_distribution_returns_violation_rate — check_sum_p_distribution が large_violation_rate / small_violation_rate キーを返す（§15.2 機械検査契約・Phase 6 gate 判定が消費）
    - Test 6: test_compute_metrics_single_class_nan — single-class y_true で calibration_max_dev が NaN（定義不可）
    - Test 7: test_y_pred_1_clipped_to_last_bin — y_pred==1.0 を含む合成データで _compute_calibration_max_dev_guarded が IndexError なく計算（Specialist 指摘 (b) clip の回帰・現行 evaluator.py で既に実装済みを固定化）
  </behavior>
  <action>
    1. **plotly 依存関係追加（D-10 前提）:** `uv add plotly` を実行。pyproject.toml の [project] dependencies 配列に plotly が追記される。planner は実行時に最新安定版を確認し pin する（RESEARCH A1: 6.x 系想定・A1参照）。**REVIEW Codex LOW（scipy 明示依存）:** scipy は sklearn 1.9.0 の推移依存で利用可能だが・Phase 6 で `from scipy.stats import spearmanr` を直接 import するため `uv add scipy` で pyproject.toml に明示依存として追加（lockfile の意図明確化・推移依存の暗黙前提を解消）。plotly + scipy の2つを追加。

    2. **REVIEW C6 対応（reports/04-eval.json stale 解消）:** 現 evaluator.py の `METRIC_COLUMNS`/`compute_metrics` は `calibration_max_dev_guarded` を吐く（9列）が・reports/04-eval.json は8列で guarded 列が無い（Phase 4 生成時点）。Wave 0 で現 evaluator.py により reports/04-eval.json を**再生成**（guarded 列含む9列化）。再生成は D-04 事前登録値（unguarded 0.23076923076923073 / 0.25789298770355484）を変えないことを test_calibration_max_dev_report_value_match で担保（unguarded 列は不変）。Wave 0 で `uv run python scripts/run_train_predict.py --check-reproduce`（または同等の評価再生成エントリ）を実行し reports/04-eval.json を更新する task を追加。**注意:** 再生成は Phase 4 SC#4 bit-identical の範囲内（prediction 再生成でなく evaluation レポート再生成のみ・モデル予測値は不変）。もし再生成が困難（run_train_predict.py が評価再生成をサポートしない等）の場合は・Plan 06-05 run_evaluation.py が reports/04-eval.json の guarded 列欠損を WARN で検知し代替値（compute_metrics 再計算）で補完する経路に切り替え（本 PLAN の read_first/action で executor が判断）。

    3. **tests/model/__init__.py 作成:** 空ファイル（package marker・tests.model を import 可能にする・tests/model/ が既存の場合は確認して作成）。

    3. **tests/model/test_evaluator.py 新設（Phase 4 既存契約の固定化・RED でなく GREEN を即座に成立させる）:**
       - imports: `import json, numpy as np, pandas as pd, pytest` + `from src.model.evaluator import (compute_metrics, _compute_calibration_max_dev, _compute_calibration_max_dev_guarded, check_sum_p_distribution, CALIBRATION_CURVE_BINS, CALIBRATION_CURVE_STRATEGY, CALIBRATION_CURVE_MIN_BIN_COUNT, METRIC_COLUMNS)`
       - 合成データヘルパ `_make_synthetic_y(n=1000, seed=42, p_range=(0.05, 0.95))`: rng=np.random.default_rng(seed); p=rng.uniform(*p_range,size=n); y=(rng.uniform(size=n)<p).astype(int); return y,p（PATTERNS.md test_evaluator.py セクション analog）
       - reports/04-eval.json 読込ヘルパ `_load_report_metrics()`: `json.loads(Path("reports/04-eval.json").read_text())["metrics"]`（実データ値の回帰固定化用・reports/04-eval.json が存在しない場合は pytest.skip）
       - test_compute_metrics_uniform_max_dev_unchanged: 合成 y,p で compute_metrics を2回呼出し metrics1["calibration_max_dev"] == metrics2["calibration_max_dev"]（float の bit-identical は == で検証）
       - test_calibration_max_dev_report_value_match: `_load_report_metrics()` で lightgbm/catboost/bl1 の calibration_max_dev が reports/04-eval.json と完全一致することを assert（実データ値の回帰固定化・Phase 6 でこの値が変わらないことの前提・Pitfall 3/T-04-24）
       - test_calibration_curve_constants: CALIBRATION_CURVE_BINS==10, CALIBRATION_CURVE_STRATEGY=="uniform", CALIBRATION_CURVE_MIN_BIN_COUNT==30, METRIC_COLUMNS に "calibration_max_dev" と "calibration_max_dev_guarded" が含まれる
       - test_compute_calibration_max_dev_guarded_bit_identical: 合成データで2回呼出し結果が == で一致
       - test_check_sum_p_distribution_returns_keys: 合成 DataFrame（race_key/p/entry_count 列・8頭以上と5-7頭を混在）で check_sum_p_distribution を呼び large_violation_rate/small_violation_rate/total_races/diagnostic_note キーが存在
       - test_compute_metrics_single_class_nan: y_true=np.zeros(100), y_pred=np.full(100,0.5) で compute_metrics の calibration_max_dev が NaN（pd.isna で判定）
       - test_compute_calibration_max_dev_guarded_y_pred_one: y_pred に 1.0 を含む合成データで _compute_calibration_max_dev_guarded が IndexError を起こさず float を返す（Specialist 指摘 (b) clip の回帰）

    全テストは GREEN を即座に成立させる（Phase 4 evaluator.py は実装済み・固定化テストのため RED でなく GREEN が正）。tdd="true" だが・これは「Phase 6 拡張前の現状契約を固定化」であり・Phase 6 本体（Plan 06-02）で evaluator.py を拡張する際にこれらのテストが回帰を検知する。
  </action>
  <verify>
    <automated>uv run pytest tests/model/test_evaluator.py -v -x</automated>
  </verify>
  <done>
    - uv add plotly が成功し pyproject.toml dependencies に plotly が含まれる（grep -q '^    "plotly' pyproject.toml）
    - uv run pytest tests/model/test_evaluator.py が全て GREEN（7テスト以上）
    - test_calibration_max_dev_report_value_match が reports/04-eval.json の実データ値（lightgbm 0.23076923076923073, catboost 0.25789298770355484, bl1 0.0014259639516354672）と一致を assert
    - test_compute_calibration_max_dev_guarded_y_pred_one が y_pred==1.0 で IndexError なく GREEN
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: segment 6軸カラム経路確認テスト（Open Question #1 解決）</name>
  <files>tests/model/test_segment_axis_columns.py</files>
  <read_first>
    - 06-CONTEXT.md specifics セクション（segment 軸のデータ経路: year/month=race_date, 競馬場=jyocd, 頭数=entry_count, 人気帯=ninki, オッズ帯=fukuoddslower 下限）
    - 06-RESEARCH.md Open Question #1（segment 軸の ninki/odds_band データ経路の実コード確認・prediction.fukusho_prediction 現 DDL に ninki/odds 列は存在しない・label.fukusho_label から JOIN 必要）
    - 06-RESEARCH.md Pitfall 6（segment 軸データの欠損・Wave 0 で information_schema.columns 確認）
    - src/db/schema.py（PREDICTION_TABLE_DDL: model_type/model_version/.../p_fukusho_hit/race_date/split のみ・ninki/odds 列なし）
    - tests/conftest.py（readonly_cur fixture・KEIBA_SKIP_DB_TESTS policy・requires_db marker）
  </read_first>
  <behavior>
    - Test 1: test_prediction_table_has_segment_join_keys — prediction.fukusho_prediction に race_key 構成カラム（year/jyocd/kaiji/nichiji/racenum/umaban/kettonum）+ race_date が存在する（information_schema.columns SELECT）。entry_count は prediction 側に無くても PASS（label JOIN で補完）。
    - Test 2: test_label_table_has_segment_axesOrFailLoud — label.fukusho_label に segment 軸カラム（race_date / jyocd / entry_count 又は sales_start_entry_count / ninki / fukuoddslower 又は同等の複勝オッズ下限カラム）が存在することを確認。**REVIEW HIGH#3 対応:** 欠損カラムは WARN skip でなく `pytest.fail(...)` で**fail-loud**（D-12「6軸全て生成」の前提検証として・代替カラム確定不能ならテスト失敗）。各軸について「取得可能カラム名」を特定できた場合のみ PASS（test が attrs dict に `{axis: column_name}` を記録）。データソース（label vs fetch_market_data）をテスト出力で明示。
    - Test 3: test_segment_join_coverage — prediction.fukusho_prediction（test split）と label.fukusho_label を race_key 7カラムで JOIN した結果の行数が prediction test split 行数と一致（cartesian duplication なし・HIGH-1/HIGH-2 JOIN 契約）。requires_db。
    - Test 4: test_market_data_segment_axes_if_label_missing — REVIEW HIGH#3/C3 対応。label.fukusho_label に ninki/fukuoddslower 系カラムが存在しない場合・`scripts/run_backtest.py::_compute_baselines` / `fetch_market_data` 経路（market.n_odds_tanpuku 等の市場データ由来）に ninki/odds カラムが存在するか information_schema を確認し・存在する場合は label 不足を market_data JOIN で補完可能と記録。label/market 双方に欠損の場合のみ pytest.fail（真に SC#3 履行不能）。
  </behavior>
  <action>
    tests/model/test_segment_axis_columns.py を新設（requires_db テスト・KEIBA_SKIP_DB_TESTS=1 で skip・conftest.py policy 準拠）。**REVIEW HIGH#3 対応: 欠損時は WARN-skip でなく fail-loud。** Open Question #1 の「RESOLVED」表記は本テスト GREEN 確定後に確定（RESEARCH/CONTEXT の過早表記は executor が確認後に書戻す）。

    1. imports: `import pytest` + `from src.config.settings import Settings`
    2. marker: 全テストに `@pytest.mark.requires_db`（conftest.py の readonly_cur / write_cur fixture を使用）
    3. ヘルパ `_columns(cur, schema, table)`: parameterized query `SELECT column_name FROM information_schema.columns WHERE table_schema=%s AND table_name=%s` を実行し set で返す（SQL injection 防御）
    4. test_prediction_table_has_segment_join_keys(readonly_cur): prediction.fukusho_prediction のカラム集合に {"year","jyocd","kaiji","nichiji","racenum","umaban","kettonum","race_date"} が含まれることを assert。entry_count は prediction テーブルに現状存在しない場合 PASS（label JOIN 経路で補完・test_label 側で検証）。test は attrs dict `{"prediction_join_keys": [...], "entry_count_source": "label"}` を stdout print し run_evaluation.py 側で参照可能にする。
    5. test_label_table_has_segment_axesOrFailLoud(readonly_cur): **REVIEW HIGH#3・fail-loud:** label.fukusho_label のカラム集合を取得し・各 segment 軸カラムの存在を以下順で確認:
       - race_date（year/month 軸の元）: 必須・不在なら即 `pytest.fail("label.fukusho_label.race_date 欠損: year/month segment 軸履行不能")`
       - jyocd（競馬場軸）: 必須・不在なら `pytest.fail`
       - entry_count 又は sales_start_entry_count（頭数軸）: いずれか存在で PASS・両方不在なら `pytest.fail`
       - ninki（人気帯軸）: 不在の場合は test_market_data_segment_axes_if_label_missing に委ね・本 test では記録のみ（`axes_status["ninki"] = "label_absent_check_market"`）。ただし market 側にも不在の場合は test_market_data 側で pytest.fail
       - fukuoddslower 又は同等の複勝オッズ下限カラム（オッズ帯軸）: 同上
       全6軸が「label 又は market のいずれかで取得可能」ことを確認するまでテストは fail-loud。「WARN 付き PASS」は廃止（REVIEW HIGH#3）。
    6. test_segment_join_coverage(readonly_cur): prediction.fukusho_prediction の test split 行数と・label.fukusho_label と race_key 7カラム（year/jyocd/kaiji/nichiji/racenum/umaban/kettonum）で INNER JOIN した結果の行数が一致することを assert（JOIN 後行数減少 = label 欠損・JOIN 後行数増加 = cartesian duplication・いずれも `pytest.fail`）。parameterized query で構築（SQL injection 防御）。
    7. test_market_data_segment_axes_if_label_missing(readonly_cur): **REVIEW HIGH#3/C3 対応:** label 側で ninki/odds が欠損していた場合のフォールバック確認。market.n_odds_tanpuku（又は run_backtest.py::fetch_market_data が参照する市場データテーブル）の information_schema.columns を取得し・ninki 系・複勝オッズ下限系カラムの存在を確認。label/market 双方に欠損の場合のみ `pytest.fail("SC#3 履行不能: ninki/odds band segment 軸が label/market 双方で欠損")`。取得可能な場合は attrs dict に `{"ninki_source": "market.n_odds_tanpiku.ninki", "odds_source": "..."}` を記録し Plan 06-05 run_evaluation.py が JOIN 経路を構築するための根拠を提供。

    **REVIEW 対応サマリ:** HIGH#3（segment 軸確認 fail-loud 化）・C3（label/market データソース二段階確認）を組込。Open Question #1 は本テストが GREEN になった時点で RESOLVED（実行前に仮に RESOLVED とは書かない・executor が確定後に SUMMARY へ記録）。
  </action>
  <verify>
    <automated>uv run pytest tests/model/test_segment_axis_columns.py -v -x</automated>
  </verify>
  <done>
    - uv run pytest tests/model/test_segment_axis_columns.py が GREEN（KEIBA_SKIP_DB_TESTS unset で実行・3テスト）
    - prediction.fukusho_prediction の race_key + race_date カラムが確認される
    - label.fukusho_label の segment 軸カラム（race_date/jyocd/entry_count/ninki/fukuoddslower）の存在/不在が記録される（Open Question #1 解決）
    - test_segment_join_coverage で JOIN 後行数が prediction test split と一致（cartesian duplication なし）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → filesystem | uv add plotly が pyproject.toml/uv.lock を更新（開発者意図的・信頼済み） |
| test → live DB (readonly) | test_segment_axis_columns.py が readonly_cur で information_schema.columns を SELECT（READ only・readonly ロール） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | uv add plotly（npm/pip 相当の package install） | mitigate | plotly は RESEARCH Package Legitimacy Audit で OK Approved（PyPI 12+年・数千万DL/月・github.com/plotly/plotly.py）。`uv add plotly` で最新版を確認し pin。T-06-SC 参照 |
| T-06-02 | Information Disclosure | test が reports/04-eval.json を読込 | accept | reports/04-eval.json は local-only 評価レポート・機密情報なし・test の回帰固定化に必要 |
| T-06-SC | Tampering | uv add による package install（plotly） | mitigate | RESEARCH Package Legitimacy Audit で plotly=OK Approved・[ASSUMED] は無し・blocking human checkpoint 不要（audit 通過済・auto_advance 可） |
</threat_model>

<verification>
- uv add plotly 成功・pyproject.toml + uv.lock 更新
- uv run pytest tests/model/ -v が GREEN（test_evaluator.py + test_segment_axis_columns.py）
- test_calibration_max_dev_report_value_match が reports/04-eval.json の実データ値と一致（T-04-24 後知恵すり替え防止の前提固定化）
- segment 6軸カラム経路が実 DB で確認される（Open Question #1 解決）
</verification>

<success_criteria>
- plotly が pyproject.toml dependencies に含まれる
- evaluator.py Phase 4 既存契約（calibration_max_dev 実データ値・binning 定数・single-class NaN・y_pred==1.0 clip）が回帰テストで固定化される
- segment 6軸カラム経路が実 DB で確認され・Phase 6 本体（segment_eval.py）の設計根拠が提供される
- 全テスト GREEN（KEIBA_SKIP_DB_TESTS unset で requires_db テスト含む実行）
</success_criteria>

<output>
Create `.planning/phases/06-evaluation-calibration-gates/06-01-SUMMARY.md` when done
</output>

<artifacts>
## Artifacts this phase produces

**Constants / Config:**
- pyproject.toml: dependencies 配列に `plotly` 追加（D-10 segment HTML 出力の前提）+ `scipy` 明示依存追加（REVIEW Codex LOW・spearmanr 直接 import のため）

**Test files (新規):**
- tests/model/__init__.py — package marker（空ファイル）
- tests/model/test_evaluator.py — evaluator.py Phase 4 既存契約固定化テスト（7テスト以上）:
  - 関数: `_make_synthetic_y(n, seed, p_range)`, `_load_report_metrics()`
  - テスト: `test_compute_metrics_uniform_max_dev_unchanged`, `test_calibration_max_dev_report_value_match`, `test_calibration_curve_constants`, `test_compute_calibration_max_dev_guarded_bit_identical`, `test_check_sum_p_distribution_returns_keys`, `test_compute_metrics_single_class_nan`, `test_compute_calibration_max_dev_guarded_y_pred_one`
- tests/model/test_segment_axis_columns.py — segment 6軸カラム経路確認テスト（requires_db・3テスト）:
  - 関数: `_columns(cur, schema, table)`
  - テスト: `test_prediction_table_has_segment_join_keys`, `test_label_table_has_segment_axes`, `test_segment_join_coverage`
</artifacts>
