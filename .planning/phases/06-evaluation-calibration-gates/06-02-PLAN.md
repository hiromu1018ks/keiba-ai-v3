---
phase: 06-evaluation-calibration-gates
plan: 02
type: execute
wave: 1
depends_on:
  - 06-01
files_modified:
  - src/model/evaluator.py
  - tests/model/test_evaluator.py
  - tests/model/test_evaluator_gate.py
autonomous: true
requirements:
  - EVAL-01
  - EVAL-02

must_haves:
  truths:
    - "開発者が compute_metrics を呼ぶと quantile_max_dev / ece / mce キーが追加で返される（D-05 新指標・事前登録指標 calibration_max_dev は不変）"
    - "開発者が check_acceptance_gate を呼ぶと gate_verdict が BLOCK または WARN で返り・block_triggered=True の場合は block_reasons リストが空でない（D-01/D-02 構造的 BLOCK）"
    - "構造的 BLOCK は D-02 の AND 条件（baselines 全敗 AND sum(p) 著乖離 の両方成立）でのみ発火する（REVIEW HIGH#2・片方だけでは warn_reasons 記録で BLOCK しない）"
    - "baselines_all_lose 単独・sum_p_violation 単独は warn_reasons に記録され gate_verdict==WARN（出荷停止しない）"
    - "compute_monotonicity_warn が Spearman 順位相関 + bin 逆転数を返す（D-03 曖昧 WARN・人間判定参考）"
    - "compute_yearly_inversion_warn が reports/06-segments/year.json の各年 curve に compute_monotonicity_warn を適用し・年次反転数（bin_inversions）と Spearman 順位相関を返す（D-03 年次要素充足・EVAL-02 SC#2『年次 Calibration 極端反転なし』の機械的 WARN レポート化）"
    - "reports/04-eval.json の事前登録指標値（lightgbm calibration_max_dev=0.23076923076923073 等）が Plan 06-01 の回帰テストで守られる（T-04-24 後知恵すり替え防止）"
  artifacts:
    - path: "src/model/evaluator.py"
      provides: "quantile_max_dev/ECE/MCE 計算 + check_acceptance_gate（構造的 BLOCK/曖昧 WARN）+ compute_monotonicity_warn + _compute_calibration_curve_bins ヘルパー（純 NumPy bit-identical）"
      contains: "def check_acceptance_gate"
    - path: "tests/model/test_evaluator_gate.py"
      provides: "ゲート判定ロジック単体テスト（BLOCK 正発火・WARN 停留・BL-2/BL-3 除外）"
      min_lines: 100
  key_links:
    - from: "src/model/evaluator.py::check_acceptance_gate"
      to: "src/model/evaluator.py::check_sum_p_distribution"
      via: "check_sum_p_distribution の戻り値（large_violation_rate/small_violation_rate）を消費して BLOCK 条件2 を判定"
      pattern: "large_violation_rate"
    - from: "src/model/evaluator.py::_compute_calibration_curve_bins"
      to: "src/model/evaluator.py::_compute_calibration_max_dev_guarded"
      via: "既存 _compute_calibration_max_dev_guarded の binning パターン（np.linspace + digitize + bincount + clip）を strategy='quantile' で拡張共有・bit-identical 保証"
      pattern: "np\\.bincount"
---

<objective>
Phase 6 Wave 1 評価本体（EVAL-01/02）: src/model/evaluator.py を拡張し、(1) 新キャリブ指標 quantile_max_dev/ECE/MCE を追加（D-05・純 NumPy bit-identical・事前登録指標 calibration_max_dev は不変）・(2) check_acceptance_gate 関数を新規追加（D-01/D-02 構造的 BLOCK = baselines 全敗 + sum(p) 著乖離 / D-03 曖昧 WARN = Spearman・反転数）・(3) compute_monotonicity_warn 関数を新規追加（D-03 bin 単調性 WARN 指標）。既存 _compute_calibration_max_dev / _compute_calibration_max_dev_guarded / METRIC_COLUMNS / CALIBRATION_CURVE_* 定数は一切変更しない（D-04 事前登録指標不変・T-04-24 回避）。

Purpose: EVAL-01（統合評価経路の確率品質指標）と EVAL-02（確率品質受入基準ゲート）の service 層を実装する。Phase 5 backtest metrics（回収率/P/L/maxDD）との統合と主モデル確定は Plan 06-04 run_evaluation.py で行う。本 plan は evaluator.py の純粋関数拡張に集中し・DB 不要・純 NumPy bit-identical を保証する。

Output: src/model/evaluator.py（拡張: _compute_calibration_curve_bins / _compute_ece / _compute_mce / _compute_quantile_max_dev / check_acceptance_gate / compute_monotonicity_warn + METRIC_COLUMNS_EXTENDED + SUM_P_BLOCK_THRESHOLD + COMPARABLE_BASELINES 定数）・tests/model/test_evaluator.py（拡張: 新指標の回帰テスト追加）・tests/model/test_evaluator_gate.py（新規: ゲート判定テスト）
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-evaluation-calibration-gates/06-CONTEXT.md
@.planning/phases/06-evaluation-calibration-gates/06-RESEARCH.md
@.planning/phases/06-evaluation-calibration-gates/06-PATTERNS.md
@src/model/evaluator.py
@tests/model/test_evaluator.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: evaluator.py 拡張 — quantile_max_dev/ECE/MCE + _compute_calibration_curve_bins ヘルパー（D-05・純 NumPy bit-identical）</name>
  <files>src/model/evaluator.py, tests/model/test_evaluator.py</files>
  <read_first>
    - src/model/evaluator.py（Phase 4 実装の正・特に _compute_calibration_max_dev_guarded lines 259-335: np.linspace/digitize/bincount/clip の純 NumPy binning パターン・整列 assert・MIN_BIN_COUNT ガード・これらをそのまま再利用）
    - src/model/evaluator.py（METRIC_COLUMNS lines 75-85, CALIBRATION_CURVE_BINS=10 lines 88, CALIBRATION_CURVE_MIN_BIN_COUNT=30 lines 90・これらは不変）
    - src/model/evaluator.py（compute_metrics lines 148-225: 戻り値 dict の構造・calibration_max_dev/calibration_max_dev_guarded キー・これらを拡張）
    - 06-RESEARCH.md Pattern 1（純 NumPy bit-identical binning・strategy='quantile' の bin_edges 構築・np.unique で重複 edge 対処）+ Code Examples（ECE/MCE 計算 lines 720-773）
    - 06-RESEARCH.md Pitfall 1（quantile binning の重複 edge・BL-1 離散値）・Pitfall 2（y_pred==1.0 out-of-range）・Pitfall 3（事前登録指標の意図せぬ変更）
    - 06-PATTERNS.md evaluator.py セクション（_compute_calibration_curve_bins / _compute_ece / _compute_mce の実装例・imports に scipy.stats.spearmanr 追加）
    - reports/04-eval.json（実データ値: lightgbm calibration_max_dev=0.23076923076923073, CatBoost 0.25789298770355484・Plan 06-01 テストで固定化済み・本 task で変わらないこと）
    - .planning/debug/calib-maxdev-vs-baselines.md（uniform max_dev 構造バイアス診断・Specialist SUGGEST_CHANGE (b)/(c)/(f)・D-04/D-05 の根拠）
  </read_first>
  <behavior>
    - Test 1: test_quantile_max_dev_bit_identical — _compute_quantile_max_dev を合成データで2回呼出し結果が == で一致（純 NumPy・Phase 4 SC#4 延長）
    - Test 2: test_ece_weighted_average — 既知の合成データ（手計算可能な小サンプル）で _compute_ece が Naeini 2015 定義 ECE = Σ(n_m/N)|frac_pos - mean_pred| と一致
    - Test 3: test_mce_worst_case_guarded — _compute_mce が MIN_BIN_COUNT=30 未満の bin を除外した上で max|dev| を返す
    - Test 4: test_quantile_duplicate_edges_bl1 — BL-1 のような離散値（少数の同値が多い）予測で _compute_calibration_curve_bins(strategy='quantile') が np.unique で重複 edge を削除し n_bins_actual < n_bins でも正しく計算
    - Test 5: test_compute_metrics_returns_extended_keys — compute_metrics の戻り値に quantile_max_dev / ece / mce キーが追加される（既存 calibration_max_dev / calibration_max_dev_guarded は不変）
    - Test 6: test_uniform_max_dev_unchanged_after_extension — 拡張後も reports/04-eval.json の calibration_max_dev 値が不変（Plan 06-01 test_calibration_max_dev_report_value_match が引き続き GREEN・Pitfall 3）
    - Test 7: test_compute_calibration_curve_bins_alignment — 戻り値の mean_pred/frac_pos/counts 配列が同長（整列 assert・Specialist 指摘 (c)）
    - Test 8: test_guarded_value_pinned_to_report — **REVIEW C14 対応:** `_compute_calibration_max_dev_guarded` の reports/04-eval.json LightGBM guarded 値（0.0987 または実際の実データ値・Plan 06-01 で reports/04-eval.json を読込）と一致することを assert。リファクタ（_compute_calibration_curve_bins 切出し）時の境界処理差分が silent に漂うのを検知。
    - Test 9: test_quantile_max_dev_unguarded_vs_mce_guarded — **REVIEW C5:** quantile_max_dev（ガードなし）と mce（MIN_BIN_COUNT ガード付き）が別値（count<30 bin がある入力で mce >= quantile_max_dev or NaN・同じでない）であることを検証。
  </behavior>
  <action>
    src/model/evaluator.py を拡張（既存関数は一切変更しない・新規関数と定数を追加）:

    1. **imports 拡張:** ファイル先頭の import ブロックに `from scipy.stats import spearmanr` を追加（scipy は sklearn 1.9.0 推移依存で利用可能・RESEARCH Environment Availability 確認済）。既存 imports（json/Path/Any/np/pd/calibration_curve/brier_score_loss/log_loss/roc_auc_score/_atomic_write_text）は不変。

    2. **新規定数追加（METRIC_COLUMNS / CALIBRATION_CURVE_* の直後）:**
       - `METRIC_COLUMNS_EXTENDED: list[str] = METRIC_COLUMNS + ["quantile_max_dev", "ece", "mce"]` — D-05 新指標。METRIC_COLUMNS（事前登録9列）は不変・拡張リストは別名で定義
       - `SUM_P_BLOCK_THRESHOLD: float = 0.30` — D-02 構造的 BLOCK 条件2（sum(p) 著乖離）の閾値。§15.2 [2.7,3.3]/[1.8,2.2] から 30% 超違反で BLOCK。**REVIEW HIGH#5 対応:** 0.30 は仮置き（経験的根拠は未検証・`check_sum_p_distribution` は Phase 4 で未呼出・reports/04-eval.json に violation_rate 実測値なし）。Plan 06-05 Wave 3 Step 3a（後述）で 06-02 完了直後に実データ violation_rate を計測し・0.30 が実分布に対して偽陽性 BLOCK（正常モデルで RuntimeError）を出さないか検証する。偽陽性が疑われる場合は本定数を調整（escape-hatch `--allow-sump-warn` も検討）。CLAUDE.md: 現データ LightGBM sum_p_mean=3.04 で violation_rate ほぼ 0% 想定だが・bucket 毎の実測は未取得。
       - `COMPARABLE_BASELINES: tuple[str, ...] = ("bl1", "bl4", "bl5")` — D-02 構造的 BLOCK 条件1（baselines 全敗）の比較対象。BL-2（NaN）・BL-3（§14.2 caveat・同一情報条件でない）は除外（RESEARCH 理由詳述）

    3. **新規ヘルパー _compute_calibration_curve_bins（純 NumPy bit-identical・PATTERNS.md 実装例準拠）:** 既存 _compute_calibration_max_dev_guarded lines 296-306 の binning ロジックを切り出し・strategy 引数（'uniform' | 'quantile'）で bin_edges 構築を切替。戻り値は dict（mean_pred/frac_pos/counts/bin_edges の np.ndarray）。strategy='quantile' の場合は np.quantile(y_pred, np.linspace(0,1,n+1)) で bin_edges を構築し・np.unique で重複 edge を削除（BL-1 離散値対策・Pitfall 1）。y_pred==1.0 は np.clip で最終 bin に（Pitfall 2・既存 Specialist 指摘 (b) と同一）。整列 assert で off-by-one 防止（Specialist 指摘 (c)）。single-class は呼出側で NaN 判定のため空配列で返さず・呼出側 _compute_ece/_compute_mce で len(np.unique(y_true))<2 なら NaN を返す。

    4. **新規 _compute_ece（D-05・Naeini 2015）:** _compute_calibration_curve_bins(strategy='quantile') を消費し・ECE = Σ(n_m/N)|frac_pos - mean_pred| を計算。single-class は NaN。空 bin の場合は NaN。float で返す。

    5. **新規 _compute_mce（D-05・worst-case・MIN_BIN_COUNT ガード付き）:** _compute_calibration_curve_bins を消費し・counts >= MIN_BIN_COUNT の bin のみ max|dev| を計算。全 bin が基準未満は NaN。float で返す。

    6. **新規 _compute_quantile_max_dev（D-05・REVIEW C5 対応: MCE と定義分離）:** quantile strategy での worst-case max|dev| を計算するが・**MIN_BIN_COUNT ガードなし**（全 bin 対象・事前登録 calibration_max_dev との対比）・_compute_mce は MIN_BIN_COUNT ガード付き（既存）。両者は別実装（D-05 の「3指標」quantile_max_dev + ECE + MCE を忠実に分離・METRIC_COLUMNS_EXTENDED でも別列）。

    7. **compute_metrics 拡張（既存戻り値 dict に3キー追加）:** 既存 compute_metrics の戻り値 dict に `"quantile_max_dev"`, `"ece"`, `"mce"` キーを追加。計算は上記新規関数を呼ぶ。既存 calibration_max_dev / calibration_max_dev_guarded / sum_p_* / brier / logloss / auc は一切変更しない（D-04 事前登録指標不変・Pitfall 3）。sum_p_note キーも不変。

    8. **__all__ 拡張:** 新規関数・定数を __all__ に追加（METRIC_COLUMNS_EXTENDED, SUM_P_BLOCK_THRESHOLD, COMPARABLE_BASELINES, _compute_calibration_curve_bins, _compute_ece, _compute_mce, _compute_quantile_max_dev, check_acceptance_gate, compute_monotonicity_warn, compute_yearly_inversion_warn）。

    **厳守:** 既存 _compute_calibration_max_dev（事前登録・ガードなし）・_compute_calibration_max_dev_guarded・check_sum_p_distribution・build_comparison_table・write_eval_report・evaluate_all_models は一切変更しない。新規ヘルパー _compute_calibration_curve_bins を追加するが・既存 _compute_calibration_max_dev_guarded はそれを呼ぶようにリファクタしてもよい（戻り値の bit-identical は test_compute_calibration_max_dev_guarded_bit_identical と test_calibration_max_dev_report_value_match で保証）。ただしリファクタは任意・既存実装を残して新規関数を独立させる方が安全（Pitfall 3 回避）。

    tests/model/test_evaluator.py に上記 behavior の7テストを追加（Plan 06-01 の既存7テストは温存・追記）。
  </action>
  <verify>
    <automated>uv run pytest tests/model/test_evaluator.py -v -x</automated>
  </verify>
  <done>
    - src/model/evaluator.py に _compute_calibration_curve_bins / _compute_ece / _compute_mce / _compute_quantile_max_dev が追加される
    - compute_metrics の戻り値に quantile_max_dev / ece / mce キーが追加される
    - METRIC_COLUMNS_EXTENDED / SUM_P_BLOCK_THRESHOLD=0.30 / COMPARABLE_BASELINES=("bl1","bl4","bl5") 定数が追加される
    - reports/04-eval.json の calibration_max_dev 値が不変（test_calibration_max_dev_report_value_match GREEN・Pitfall 3）
    - test_quantile_duplicate_edges_bl1 が BL-1 離散値で正しく計算（Pitfall 1）
    - 全テスト GREEN（Plan 06-01 の7テスト + 本 task の7テスト = 14テスト以上）
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: check_acceptance_gate + compute_monotonicity_warn 新規追加（D-01/D-02/D-03・hybrid gate）</name>
  <files>src/model/evaluator.py, tests/model/test_evaluator_gate.py</files>
  <read_first>
    - src/model/evaluator.py（check_sum_p_distribution lines 343-410: 戻り値 dict の large_violation_rate/small_violation_rate/diagnostic_note キー・これを check_acceptance_gate が消費）
    - src/model/evaluator.py（_compute_calibration_max_dev_guarded lines 259-335: binning パターン・compute_monotonicity_warn が返す bins dict の構造）
    - 06-CONTEXT.md D-01/D-02/D-03（構造的 BLOCK のみ hard fail・曖昧基準は WARN 参考レポート・hybrid gate 延長）
    - 06-RESEARCH.md「受入ゲート判定ロジックの設計」節（BLOCK 条件1: baselines 全敗の具体定義・BLOCK 条件2: sum(p) 著乖離 30% 閾値・WARN 指標: Spearman/反転数・現データでの想定: 両 BLOCK 条件とも発火しない）
    - 06-PATTERNS.md evaluator.py セクション check_acceptance_gate / compute_monotonicity_warn 実装例（COMPARABLE_BASELINES ループ・block_reasons 構築）
    - reports/04-eval.json（実データ: lightgbm logloss=0.4748831925590833 < bl1 0.5210148456609544 < bl4 0.5182457131489694 < bl5 0.5130483177244112・BLOCK 条件1 は発火しない想定）
  </read_first>
  <behavior>
    - Test 1: test_block_baselines_all_lose_AND_sum_p_ok — **REVIEW HIGH#2 (AND 条件):** 主モデルが LogLoss+Brier 両方で BL-1/BL-4/BL-5 全てに劣る（baselines_all_lose=True）が sum(p) violation_rate は正常（0.0）の場合 → **block_triggered==False**・gate_verdict=="WARN"・warn_reasons に baselines_all_lose を記録。D-02 は両条件の同時満たしのみ BLOCK。
    - Test 2: test_block_sum_p_divergence_AND_baselines_ok — sum(p) large_violation_rate=0.5（>0.30）だが baselines には勝っている場合 → **block_triggered==False**・gate_verdict=="WARN"・warn_reasons に sum_p_violation を記録（AND 条件・片方だけでは BLOCK しない）。
    - Test 3: test_block_triggered_when_both_conditions_met — baselines_all_lose AND sum_p_violation の両方が成立した場合 → **block_triggered==True**・block_reasons に両条件を含む・gate_verdict=="BLOCK"。**D-02 原文「baselines 全敗 ＋ sum(p) 著乖離」の忠実な AND 実装。**
    - Test 4: test_block_not_triggered_normal — reports/04-eval.json の実データ値（lightgbm 優位・sum_p violation_rate ほぼ 0%）→ block_triggered==False・gate_verdict=="WARN"・warn_reasons は空（両条件とも非該当）。
    - Test 5: test_bl2_bl3_excluded_from_comparable — metrics_dict に bl2/bl3 を入れても block/warn 判定に影響しない（COMPARABLE_BASELINES は bl1/bl4/bl5 のみ）。
    - Test 6: test_bl1_single_loss_not_block_nor_warn — 主モデルが LogLoss のみ劣る（Brier は優位）ケース → baselines_all_lose=False（両方劣る必要）・warn_reasons も空。
    - Test 7: test_warn_monotonicity_spearman — compute_monotonicity_warn が frac_pos と mean_pred の Spearman 順位相関 + bin 逆転数を返す。
    - Test 8: test_warn_monotonicity_short_array — frac_pos が長さ1以下の場合 spearman_corr=NaN・bin_inversions=0。
    - Test 9: test_gate_verdict_field — gate_verdict が "BLOCK"（block_reasons 非空）/"WARN"（block_reasons 空・warn_reasons の内容は不問）のいずれか。
    - Test 10: test_yearly_inversion_warn_applies_monotonicity_per_year — compute_yearly_inversion_warn が各年 curve に compute_monotonicity_warn を適用し {year: {spearman_corr, spearman_pvalue, bin_inversions}} を返す。
    - Test 11: test_yearly_inversion_warn_handles_empty — year_segment_results が空 dict の場合・空 dict を返す（欠損軍 WARN skip 時の安全網）。
    - Test 12: test_block_reasons_vs_warn_reasons_separation — REVIEW HIGH#2: block_reasons（BLOCK 発火要因・D-02 両条件成立時のみ）と warn_reasons（参考記録・baselines_all_lose 単独・sum_p_violation 単独・yearly_inversions 等）が分離されて戻り値に含まれる。
  </behavior>
  <action>
    src/model/evaluator.py に2関数を新規追加（Task 1 の新規定数 SUM_P_BLOCK_THRESHOLD / COMPARABLE_BASELINES を消費）:

    1. **check_acceptance_gate(metrics_dict, sum_p_check) -> dict（D-01/D-02・hybrid gate・PATTERNS.md 実装例準拠・REVIEW HIGH#2 AND 条件明示）:**
       - 引数: metrics_dict（各モデルの compute_metrics 戻り値を統合した dict・{"lightgbm":{...}, "catboost":{...}, "bl1":{...}, ...}）・sum_p_check（check_sum_p_distribution の戻り値 dict）
       - 戻り値 dict: `{block_triggered: bool, block_reasons: list[str], warn_reasons: list[str], gate_verdict: "BLOCK"|"WARN", comparable_baselines: list[str], sum_p_block_threshold: float, condition_flags: {baselines_all_lose: bool, sum_p_violation: bool}}`
       - **REVIEW HIGH#2 (D-02 AND 条件の忠実な実装):** BLOCK は「baselines 全敗 AND sum(p) 著乖離」の**両方**が成立した場合のみ発火。CONTEXT.md D-02 原文「LogLoss/Brier で baselines 全敗 ＋ `sum(p)` が理論値から著しく乖離」の「＋（両方）」を厳密に実装。
       - condition_flags.baselines_all_lose（D-02 条件1）: for main_model in ("lightgbm","catboost"): 主モデルの logloss と brier が COMPARABLE_BASELINES（bl1/bl4/bl5）の全ての max より大きい場合・True。片方のみ劣る場合は False（相補完の可能性）。metrics に logloss/brier キーがない・NaN の場合は skip（BL-2 の NaN 等）
       - condition_flags.sum_p_violation（D-02 条件2）: sum_p_check["large_violation_rate"] > SUM_P_BLOCK_THRESHOLD（0.30）OR sum_p_check["small_violation_rate"] > SUM_P_BLOCK_THRESHOLD の場合・True
       - **BLOCK 判定:** `block_triggered = baselines_all_lose AND sum_p_violation`（両立）。True の場合・block_reasons に両条件の詳細（"baselines_all_lose: lightgbm LogLoss+Brier both worse than bl1/bl4/bl5" / "sum_p_violation: large_violation_rate=50% >= 30%"）を追加。
       - **WARN 分離（REVIEW HIGH#2）:** baselines_all_lose 単独成立・sum_p_violation 単独成立の場合は BLOCK せず・warn_reasons に記録（参考レポート・出荷停止しない）。これらは D-03「曖昧基準は参考レポート」の延長。
       - gate_verdict は block_triggered==True なら "BLOCK"・それ以外は "WARN"（warn_reasons の内容は gate_verdict に影響しない）
       - comparable_baselines / sum_p_block_threshold / condition_flags を戻り値に含め（監査性・レポート記録用・Phase 8 対抗的監査で再現可能）

    2. **compute_monotonicity_warn(bins_dict) -> dict（D-03・曖昧 WARN・人間判定参考）:**
       - 引数: bins_dict（_compute_calibration_curve_bins の戻り値・{frac_pos: np.ndarray, mean_pred: np.ndarray, counts: np.ndarray, bin_edges: np.ndarray}）
       - 戻り値 dict: `{spearman_corr: float, spearman_pvalue: float, bin_inversions: int}`
       - frac_pos と mean_pred の長さが2未満の場合: spearman_corr=NaN, spearman_pvalue=NaN, bin_inversions=0
       - spearmanr(frac_pos, mean_pred, nan_policy="omit") で順位相関を計算（ties は rankdata(method='average') で決定論的・scipy.stats.spearmanr RESEARCH CITED）
       - bin_inversions は int(np.sum(np.diff(frac_pos) < 0))（frac_pos[i] > frac_pos[i+1] の回数）

    3. **compute_yearly_inversion_warn(year_segment_results) -> dict（D-03 年次要素・EVAL-02 SC#2 機械的 WARN レポート化・PATTERNS.md evaluator.py セクション実装例準拠）:**
       - 引数: year_segment_results（segment_eval.evaluate_segment_axis の year 軸戻り値と同一スキーマ・{str(year): {curve: {mean_pred: list, frac_pos: list, count: list}, scalar: {...}}}）
       - 戻り値 dict: `{str(year): {spearman_corr: float, spearman_pvalue: float, bin_inversions: int}}`（各年 curve に compute_monotonicity_warn を適用した結果）
       - 入力 dict が空の場合は空 dict を返す（Pitfall 6 延長・欠損軍 WARN skip 時の安全網）
       - 各年の curve の mean_pred/frac_pos は list の場合 np.asarray で ndarray に変換してから compute_monotonicity_warn に渡す（segment_eval の JSON シリアライズ済み入力と ndarray 入力の両方を受付）
       - D-03「反転数・単調違反 bin 数・Spearman 順位相関等の数値で併記」の年次要素を充足・run_evaluation.py（Plan 06-05 Step 3）が本関数を呼び・reports/06-evaluation.{md,json} の WARN セクションに yearly_inversions / yearly_spearman として併記

    tests/model/test_evaluator_gate.py を新設（上記 behavior の11テスト・合成データ・DB 不要・純粋関数テスト）:
    - imports: `import numpy as np, pytest` + `from src.model.evaluator import check_acceptance_gate, compute_monotonicity_warn, compute_yearly_inversion_warn, COMPARABLE_BASELINES, SUM_P_BLOCK_THRESHOLD`
    - test_block_baselines_all_lose: 合成 metrics_dict（lightgbm logloss=0.6 brier=0.2 / bl1 logloss=0.5 brier=0.18 / bl4 0.52/0.19 / bl5 0.51/0.185）+ sum_p_check（violation_rate=0.0）→ block_triggered==True
    - test_block_not_triggered_normal: reports/04-eval.json の実データ値を模した metrics_dict（lightgbm logloss=0.47488 brier=0.15222 / bl1 0.52101/0.16953 / bl4 0.51825/0.16870 / bl5 0.51305/0.16710）+ sum_p_check（large_violation_rate=0.0 small_violation_rate=0.0）→ block_triggered==False
    - test_bl2_bl3_excluded: metrics_dict に bl2（logloss=NaN brier=NaN）/bl3 を含めても test_block_not_triggered_normal と同結果
    - その他のテストは behavior 記載通り

    **厳守:** check_acceptance_gate は純粋関数（DB 不要・入力は metrics_dict と sum_p_check のみ）。BLOCK 発火時の RuntimeError 送出は呼出側（run_evaluation.py・Plan 06-04）の責務・本関数は block_triggered フラグを返すのみ（hybrid gate・Phase 1 D-01 / Phase 2 D-02 延長・Shared Pattern 3）。
  </action>
  <verify>
    <automated>uv run pytest tests/model/test_evaluator_gate.py -v -x</automated>
  </verify>
  <done>
    - src/model/evaluator.py に check_acceptance_gate と compute_monotonicity_warn と compute_yearly_inversion_warn が追加される
    - tests/model/test_evaluator_gate.py の11テストが全て GREEN
    - test_block_baselines_all_lose_AND_sum_p_ok で baselines 全敗単独では BLOCK しない（warn_reasons 記録・REVIEW HIGH#2 AND 条件）
    - test_block_sum_p_divergence_AND_baselines_ok で sum(p) 著乖離単独では BLOCK しない（warn_reasons 記録）
    - test_block_triggered_when_both_conditions_met で両条件成立時のみ block_triggered==True（D-02 AND 条件の忠実な実装）
    - test_block_reasons_vs_warn_reasons_separation で block_reasons / warn_reasons / condition_flags が分離されて戻り値に含まれる
    - test_bl2_bl3_excluded で BL-2/BL-3 が比較対象から除外される（COMPARABLE_BASELINES）
    - compute_monotonicity_warn が Spearman 順位相関 + bin 逆転数を返す
    - test_yearly_inversion_warn_applies_monotonicity_per_year で各年 curve に compute_monotonicity_warn が適用され {year: {spearman_corr, spearman_pvalue, bin_inversions}} が返る（D-03 年次要素・SC#2 機械的 WARN レポート化）
    - test_yearly_inversion_warn_handles_empty で空入力時に空 dict が返る（欠損軍 WARN skip 安全網）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pure function → pure function | evaluator.py の新規関数は全て純粋関数（DB 不要・入力は np.ndarray/dict のみ）・外部入力境界なし |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | _compute_calibration_max_dev（事前登録指標・ガードなし）の意図せぬ変更 | mitigate | D-04 事前登録指標不変・既存関数は一切変更しない・Plan 06-01 test_calibration_max_dev_report_value_match が reports/04-eval.json の値（0.23076923076923073/0.25789298770355484）と一致を assert で回帰検知（Pitfall 3 / T-04-24 回避） |
| T-06-04 | Tampering/Elevation | check_acceptance_gate の BLOCK 条件閾値の改変（後知恵で基準を緩める） | mitigate | SUM_P_BLOCK_THRESHOLD=0.30 / COMPARABLE_BASELINES=("bl1","bl4","bl5") を定数で外部化・テスト（test_block_baselines_all_lose / test_block_sum_p_divergence）で正発火を固定化・run_evaluation.py（Plan 06-04）は定数を import して使用（ハードコード禁止） |
| T-06-05 | Repudiation | check_acceptance_gate の block_reasons が空で block_triggered=True になる（判定根拠不明） | mitigate | block_triggered は len(block_reasons)>0 で決定・block_reasons リストを必ず戻り値に含め監査性担保・test_gate_verdict_field で "BLOCK"の場合は block_reasons 非空を assert |
</threat_model>

<verification>
- uv run pytest tests/model/test_evaluator.py tests/model/test_evaluator_gate.py -v が GREEN（test_evaluator_gate.py は11テスト）
- evaluator.py の既存契約（calibration_max_dev 実データ値・binning 定数）が不変（Plan 06-01 テスト GREEN）
- 新規 quantile_max_dev/ECE/MCE が純 NumPy bit-identical で計算される
- check_acceptance_gate が構造的 BLOCK（baselines 全敗 + sum(p) 著乖離）を正発火し・曖昧基準は WARN に留まる
- compute_monotonicity_warn が Spearman 順位相関 + bin 逆転数を返す
</verification>

<success_criteria>
- EVAL-01（確率品質指標）: compute_metrics が quantile_max_dev/ECE/MCE を追加で返す（事前登録指標不変）
- EVAL-02（受入基準ゲート）: check_acceptance_gate が D-01/D-02 構造的 BLOCK と D-03 曖昧 WARN を判定する
- reports/04-eval.json の事前登録指標値が回帰テストで守られる（T-04-24）
- 全テスト GREEN（DB 不要・純粋関数）
</success_criteria>

<output>
Create `.planning/phases/06-evaluation-calibration-gates/06-02-SUMMARY.md` when done
</output>

<artifacts>
## Artifacts this phase produces

**Functions (src/model/evaluator.py 新規):**
- `_compute_calibration_curve_bins(y_true, y_pred, *, strategy, n_bins=CALIBRATION_CURVE_BINS, min_bin_count=CALIBRATION_CURVE_MIN_BIN_COUNT) -> dict[str, np.ndarray]` — 純 NumPy bit-identical binning ヘルパー（strategy='uniform'|'quantile'）
- `_compute_ece(y_true, y_pred, *, strategy="quantile", n_bins=10) -> float` — ECE（Naeini 2015・重み付け平均）
- `_compute_mce(y_true, y_pred, *, strategy="quantile", n_bins=10, min_bin_count=30) -> float` — MCE（worst-case・MIN_BIN_COUNT ガード付き）
- `_compute_quantile_max_dev(y_true, y_pred, *, n_bins=10) -> float` — REVIEW C5: quantile strategy worst-case max|dev|（**MIN_BIN_COUNT ガードなし**・事前登録 calibration_max_dev との対比・_compute_mce と別実装）
- `check_acceptance_gate(metrics_dict, sum_p_check) -> dict` — §15.2 受入ゲート判定（D-01/D-02 BLOCK / D-03 WARN）。**REVIEW HIGH#2:** BLOCK は D-02 AND 条件（baselines_all_lose AND sum_p_violation の両立）でのみ発火。戻り値: `{block_triggered, block_reasons, warn_reasons, gate_verdict, comparable_baselines, sum_p_block_threshold, condition_flags: {baselines_all_lose, sum_p_violation}}`
- `compute_monotonicity_warn(bins_dict) -> dict` — bin 単調性 WARN 指標（Spearman + 逆転数・D-03）
- `compute_yearly_inversion_warn(year_segment_results) -> dict` — 年次反転 WARN 指標（各年 curve に compute_monotonicity_warn を適用・D-03 年次要素・SC#2 機械的 WARN レポート化）

**Constants (src/model/evaluator.py 新規):**
- `METRIC_COLUMNS_EXTENDED: list[str]` — METRIC_COLUMNS + ["quantile_max_dev", "ece", "mce"]
- `SUM_P_BLOCK_THRESHOLD: float = 0.30` — D-02 BLOCK 条件2 閾値
- `COMPARABLE_BASELINES: tuple[str, ...] = ("bl1", "bl4", "bl5")` — D-02 BLOCK 条件1 比較対象（BL-2/BL-3 除外）

**compute_metrics 戻り値拡張キー:**
- `quantile_max_dev`, `ece`, `mce`（既存キーは不変）

**Test files:**
- tests/model/test_evaluator.py（拡張: 7テスト追加・Plan 06-01 の7テスト + 本 plan の7テスト = 14テスト）
- tests/model/test_evaluator_gate.py（新規: 12テスト — REVIEW HIGH#2 AND 条件テスト 4件 + monotonicity/yearly 4件 + その他 4件）
</artifacts>
