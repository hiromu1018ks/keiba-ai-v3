---
phase: 06-evaluation-calibration-gates
plan: 04
type: execute
wave: 3
depends_on:
  - 06-02
  - 06-03
files_modified:
  - src/db/schema.py
  - src/model/predict.py
  - src/db/prediction_load.py
  - tests/db/test_is_primary_flag.py
autonomous: false
requirements:
  - EVAL-01
  - EVAL-02

must_haves:
  truths:
    - "prediction.fukusho_prediction テーブルに is_primary boolean NOT NULL DEFAULT false 列が追加される（D-09・REVIEW HIGH#8・idempotent ALTER・CHECK 制約付き）"
    - "PREDICTION_COLUMNS が15列→16列に拡張され is_primary が末尾に追加される（schema.py DDL と 1:1・Pitfall 4 3ファイル連鎖）"
    - "_df_to_prediction_tuples が is_primary の型処理（None→False 正規化・bool→bool・REVIEW HIGH#8）を正しく行う（Pitfall 4）"
    - "set_primary_model が model_type+model_version+feature_snapshot_id+as_of_datetime スコープで is_primary を UPDATE し・2回実行で同一状態（idempotent・D-09）・0 行 UPDATE で RuntimeError（REVIEW HIGH#7 post-condition）"
    - "is_primary 更新後も両モデル（lightgbm+catboost）の行が保持される（silent 履歴破壊防止・D-09）"
    - "人間が checkpoint:human-verify で主モデル選定を承認する（D-07 人間判断・理由記録）"
  artifacts:
    - path: "src/db/schema.py"
      provides: "PREDICTION_ADD_IS_PRIMARY_SQL（is_primary 列追加 ALTER・idempotent）+ APPLY_ORDER 拡張"
      contains: "is_primary"
    - path: "src/model/predict.py"
      provides: "PREDICTION_COLUMNS 16列（is_primary 末尾追加）"
      contains: "is_primary"
    - path: "src/db/prediction_load.py"
      provides: "set_primary_model 関数（model_version scoped・idempotent・D-09）+ _df_to_prediction_tuples の is_primary 型処理"
      contains: "def set_primary_model"
    - path: "tests/db/test_is_primary_flag.py"
      provides: "is_primary migration・backfill・idempotency・両モデル保持テスト（requires_db）"
      min_lines: 100
  key_links:
    - from: "src/db/schema.py::PREDICTION_ADD_IS_PRIMARY_SQL"
      to: "scripts/run_apply_schema.py::APPLY_ORDER"
      via: "APPLY_ORDER に prediction_add_is_primary を追加し・run_apply_schema.py が idempotent ALTER を実行"
      pattern: "prediction_add_is_primary"
    - from: "src/db/prediction_load.py::set_primary_model"
      to: "prediction.fukusho_prediction.is_primary"
      via: "model_type+model_version+feature_snapshot_id+as_of_datetime スコープで UPDATE（staging-swap idiom と同方針・両モデル保持）"
      pattern: "UPDATE prediction\\.fukusho_prediction SET is_primary"
    - from: "src/model/predict.py::PREDICTION_COLUMNS"
      to: "src/db/prediction_load.py::_df_to_prediction_tuples"
      via: "PREDICTION_COLUMNS 16列順に tuple を構築（3ファイル連鎖・Pitfall 4）"
      pattern: "PREDICTION_COLUMNS"
---

<objective>
Phase 6 Wave 2 主モデル確定（D-07/D-08/D-09）: prediction.fukusho_prediction テーブルに is_primary フラグを追加する DB migration を実装する。3ファイル連鎖更新（schema.py DDL + predict.py PREDICTION_COLUMNS + prediction_load.py 型処理・Pitfall 4）+ set_primary_model 新規関数（model_version scoped・idempotent・staging-swap idiom と同方針）。主モデル選定自体は D-07 人間判断（checkpoint:human-verify）・本 plan はフラグ付与の機構と検証を提供する。run_evaluation.py 統合 CLI（Plan 06-05）が本 plan の set_primary_model を呼ぶ。

Purpose: D-09「主モデル確定は prediction テーブルに is_primary フラグ付与」を実装する。Phase 7 Streamlit は is_primary=true の主モデルを表示する。§19.1 再現性（versioned artefact・両モデル行は保持・比較可能性維持）。人間が D-04 事前登録基準（Calibration 重視）で主モデルを選定し・その結果を is_primary フラグで機械的に反映する経路を提供する。

Output: src/db/schema.py（PREDICTION_ADD_IS_PRIMARY_SQL + APPLY_ORDER 拡張）・src/model/predict.py（PREDICTION_COLUMNS 16列）・src/db/prediction_load.py（set_primary_model + _df_to_prediction_tuples 拡張）・tests/db/test_is_primary_flag.py（requires_db・migration/idempotency/両モデル保持テスト）・checkpoint:human-verify（主モデル選定承認・D-07）
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-evaluation-calibration-gates/06-CONTEXT.md
@.planning/phases/06-evaluation-calibration-gates/06-RESEARCH.md
@.planning/phases/06-evaluation-calibration-gates/06-PATTERNS.md
@src/db/schema.py
@src/model/predict.py
@src/db/prediction_load.py
@tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 3ファイル連鎖 is_primary 列追加（schema.py DDL + predict.py PREDICTION_COLUMNS + prediction_load.py 型処理・Pitfall 4）</name>
  <files>src/db/schema.py, src/model/predict.py, src/db/prediction_load.py, tests/db/test_is_primary_flag.py</files>
  <read_first>
    - src/db/schema.py（PREDICTION_TABLE_DDL lines 59-94: 11カラム PK + 3 CHECK 制約・is_primary 追加対象・APPLY_ORDER lines 293-307 に prediction_add_is_primary を挿入）
    - src/db/schema.py（GRANT_ETL_SQL lines 248-253: prediction スキーマに etl ロールが SELECT/INSERT/UPDATE/DELETE/TRUNCATE を既に付与済・is_primary UPDATE に追加権限不要）
    - src/model/predict.py（PREDICTION_COLUMNS lines 62-82: 現状15列・末尾に is_primary を追加して16列・schema.py DDL と 1:1）
    - src/db/prediction_load.py（_df_to_prediction_tuples lines 108-166: 型処理・_FLOAT_COLS/_INT_COLS 集合・is_primary の bool 処理を追加・_PK_ORDER_COLUMNS lines 71-83 は変更不要・is_primary は PK でない）
    - 06-RESEARCH.md Pattern 4（is_primary フラグ DB migration・3ファイル連鎖・idempotent ALTER）+ Code Examples（is_primary DB migration lines 799-810）
    - 06-RESEARCH.md Pitfall 4（PREDICTION_COLUMNS 更新忘れ・3ファイル連鎖・INSERT で column count mismatch）
    - 06-PATTERNS.md schema.py / predict.py / prediction_load.py セクション（各ファイルの修正箇所・行番号付き実装例）
    - tests/conftest.py（write_cur fixture lines 58-63・requires_db marker・KEIBA_SKIP_DB_TESTS policy）
  </read_first>
  <behavior>
    - Test 1: test_prediction_columns_includes_is_primary — PREDICTION_COLUMNS に "is_primary" が含まれ・長さが16（旧15+1）・末尾に追加されている
    - Test 2: test_prediction_columns_matches_ddl_count — PREDICTION_COLUMNS（16列）と PREDICTION_TABLE_DDL + PREDICTION_ADD_IS_PRIMARY_SQL の合計列数が一致（Pitfall 4 機械検証）
    - Test 3: test_alter_adds_is_primary_column（requires_db）— ALTER TABLE 実行後（run_apply_schema.py 経由・または直接 ALTER）information_schema.columns に prediction.fukusho_prediction.is_primary が存在する（idempotent・2回実行でもエラーなし）。**REVIEW HIGH#8:** data_type='boolean'・is_nullable='NO'（NOT NULL）であることを追加 assert。
    - Test 4: test_is_primary_default_false_on_synthetic_rows（requires_db）— **REVIEW C10 対応:** 既存行（本番 22,213×2 を含む）の is_primary を assert すると Plan 06-05 完了後（本番で主モデル確定済）に true 行が存在して RED になる。代わりに **テスト挿入行スコープ** で検証: write_cur で合成 prediction 行（test 用 model_version="test_is_primary_scope"・両モデル2行）を INSERT し・その行の is_primary が DEFAULT false であることを assert・try/finally で当該 model_version の行を DELETE。global DB 状態に依存しない。
    - Test 5: test_is_primary_not_null_constraint（requires_db）— **REVIEW HIGH#8:** is_primary=NULL の INSERT が NOT NULL 制約違反で拒否されることを確認（psycopg IntegrityError）。CHECK 制約（is_primary IN (true,false)）は NOT NULL が先行するため・CHECK の実質的価値は「NOT NULL の二重防御」level。NULL 挿入拒否が主検証。
    - Test 6: test_is_primary_check_constraint（requires_db）— REVIEW HIGH#8: boolean 型に対し is_primary=2 等の整数は psycopg の型変換で INSERT 前に拒否される（CHECK 以前）。そのため CHECK 制約自体の検証は「制約存在確認（information_schema.check_constraints で prediction_is_primary_domain の存在）」にとどめ・boolean 型変換エラーの検証は省略（CHECK は NOT NULL 二重防御としての位置付けを注記）。
    - Test 7: test_df_to_prediction_tuples_is_primary_none — _df_to_prediction_tuples が is_primary=None の行を tuple の16番目要素として None に変換
    - Test 8: test_df_to_prediction_tuples_is_primary_bool — is_primary=True/False が bool として tuple に変換
  </behavior>
  <action>
    3ファイルを連鎖更新（Pitfall 4・順序依存・同時実施必須）:

    1. **src/db/schema.py 修正:**
       - 新規定数 `PREDICTION_ADD_IS_PRIMARY_SQL`（RESEARCH Code Examples + PATTERNS.md schema.py セクション準拠・**REVIEW HIGH#8: NOT NULL 明示**）:
         - `ALTER TABLE prediction.fukusho_prediction ADD COLUMN IF NOT EXISTS is_primary boolean NOT NULL DEFAULT false;`（idempotent・**REVIEW HIGH#8: NOT NULL を明示**）
         - `ALTER TABLE prediction.fukusho_prediction DROP CONSTRAINT IF EXISTS prediction_is_primary_domain;` + `ADD CONSTRAINT prediction_is_primary_domain CHECK (is_primary IN (true, false));`（CHECK 制約・NOT NULL の二重防御・既存 prediction_fukusho_hit_range / prediction_model_type_domain / prediction_calib_method_domain と同形式）
         - `COMMENT ON COLUMN prediction.fukusho_prediction.is_primary IS 'Phase 6 D-09: 主モデル確定フラグ. 選定モデル=true/未選定=false. NOT NULL DEFAULT false (REVIEW HIGH#8). etl ロールで model_type+model_version+feature_snapshot_id+as_of_datetime スコープ UPDATE. GRANT は GRANT_ETL_SQL で既に付与済.';`
       - APPLY_ORDER lines 293-307 の `("prediction_table", PREDICTION_TABLE_DDL)` の直後に `("prediction_add_is_primary", PREDICTION_ADD_IS_PRIMARY_SQL)` を挿入（GRANT の前・CREATE SCHEMA で prediction スキーマ既存のため）
       - __all__ に PREDICTION_ADD_IS_PRIMARY_SQL を追加

    2. **src/model/predict.py 修正:**
       - PREDICTION_COLUMNS lines 62-82 の末尾（"split" の後）に `"is_primary"` を追加。docstring コメント `# Phase 6 D-09 追加（bool・NOT NULL DEFAULT false・予測生成時は False で正規化・Phase 6 set_primary_model で True に UPDATE・REVIEW HIGH#8）` を付記
       - PREDICTION_COLUMNS の長さが 15→16 になる（provenance 5 + PK 7 + p_fukusho_hit 1 + 補助メタ race_date/split 2 + is_primary 1 = 16）
       - predict_p_fukusho 内で **REVIEW HIGH#8: df["is_primary"] = False で正規化**（None でなく False・NOT NULL 制約と整合・DB INSERT で NOT NULL 違反にならない）。_assert_valid_prediction_df は provenance 列 NOT NULL チェックが model_type/model_version/feature_snapshot_id/as_of_datetime/calib_method のみで is_primary は対象外（False 固定初期化）。

    3. **src/db/prediction_load.py 修正:**
       - `_BOOL_COLS = {"is_primary"}` 新規定数を追加（_FLOAT_COLS/_INT_COLS と並列・PATTERNS.md prediction_load.py セクション準拠）
       - _df_to_prediction_tuples lines 108-166 のループ内で is_primary の分岐を追加: `elif c in _BOOL_COLS:` → **REVIEW HIGH#8:** None は False に正規化（NOT NULL 整合）・True/False は bool(v)・その他は ValueError（厳格・silent fallback 禁止）
       - 新規関数 `set_primary_model`（Task 2 で実装）

    4. **tests/db/__init__.py 取扱（REVIEW Codex LOW cycle-2 明示判断）:** `find tests -name __init__.py` で tests/db/__init__.py は**既存**（tests/__init__.py・tests/model/__init__.py・tests/ev/__init__.py・tests/features/__init__.py も既存・本プロジェクトの package marker 規約）。**「既存規約に従い保持・新規作成不要・files_modified から除外」** と明示的に判断。Phase 6 で tests/db/test_is_primary_flag.py を追加する際は既存 __init__.py がそのまま機能する（存在確認のみ・作成ステップなし）。

    5. **tests/db/test_is_primary_flag.py 新設（requires_db・conftest.py write_cur fixture・try/finally teardown・REVIEW HIGH#8/C10 対応）:**
       - imports: `import pytest` + `from psycopg.errors import IntegrityError` + `from src.db.schema import PREDICTION_ADD_IS_PRIMARY_SQL` + `from src.db.prediction_load import set_primary_model, _df_to_prediction_tuples` + `from src.model.predict import PREDICTION_COLUMNS`
       - marker: 全テストに `@pytest.mark.requires_db`（KEIBA_SKIP_DB_TESTS=1 で skip・conftest.py policy）
       - test_prediction_columns_includes_is_primary / test_prediction_columns_matches_ddl_count: DB 不要・unit
       - test_alter_adds_is_primary_column(write_cur): ALTER 実行後・**REVIEW HIGH#8:** `SELECT data_type, is_nullable, column_default FROM information_schema.columns WHERE ... is_primary` で `data_type='boolean' AND is_nullable='NO' AND column_default='false'` を assert
       - test_is_primary_default_false_on_synthetic_rows(write_cur): **REVIEW C10:** テスト挿入行スコープ。`model_version='test_is_primary_scope_<uuid4>'` で合成 prediction 行（両モデル・2行）を INSERT → SELECT で is_primary==False を assert → try/finally で当該 model_version の行を DELETE（global DB 状態に依存しない・Plan 06-05 完了後も RED にならない）
       - test_is_primary_not_null_constraint(write_cur): **REVIEW HIGH#8:** is_primary=NULL の INSERT が IntegrityError で拒否されることを確認（NOT NULL 制約の検証・try/except で teardown）
       - test_is_primary_check_constraint(write_cur): CHECK 制約 `prediction_is_primary_domain` が information_schema.check_constraints に存在することを確認（boolean 型変換で整数挿入は CHECK 以前に拒否されるため・CHECK 自体は NOT NULL 二重防御としての存在確認のみ）
       - test_df_to_prediction_tuples_is_primary_none: **REVIEW HIGH#8:** is_primary=None を含む合成 DataFrame で _df_to_prediction_tuples を呼び・tuple の16番目要素が False（None→False 正規化）であることを assert
       - test_df_to_prediction_tuples_is_primary_bool: is_primary=True/False の合成 DataFrame で tuple の16番目要素が bool

    **厳守（Pitfall 4・3ファイル連鎖）:** schema.py / predict.py / prediction_load.py を同時更新。PREDICTION_COLUMNS と DDL 列数が一致することを test_prediction_columns_matches_ddl_count で機械検証。既存 test_prediction_load.py::test_idempotent_checksum_match（Phase 4・model_version scoped staging-swap）が引き続き GREEN であることを確認（is_primary 追加で INSERT 列数が変わるが・staging-swap は PREDICTION_COLUMNS を参照するため自動追従・回帰テストで検証）。

    **GRANT 権限:** 既存 GRANT_ETL_SQL（schema.py lines 248-253）が prediction スキーマに SELECT/INSERT/UPDATE/DELETE/TRUNCATE を etl ロールに付与済・is_primary UPDATE に追加権限不要。reader ロールも SELECT 済み（lines 222-224・is_primary 読取可・Phase 7 Streamlit 用）。
  </action>
  <verify>
    <automated>uv run pytest tests/db/test_is_primary_flag.py -v -x && uv run pytest tests/model/test_prediction_load.py -v -x</automated>
  </verify>
  <done>
    - src/db/schema.py に PREDICTION_ADD_IS_PRIMARY_SQL が追加され APPLY_ORDER に含まれる
    - src/model/predict.py の PREDICTION_COLUMNS が16列（is_primary 末尾）
    - src/db/prediction_load.py の _df_to_prediction_tuples が is_primary の bool 型処理を行う
    - tests/db/test_is_primary_flag.py の8テストが全て GREEN（requires_db・KEIBA_SKIP_DB_TESTS unset・REVIEW HIGH#8/C10 対応）
    - test_prediction_columns_matches_ddl_count で PREDICTION_COLUMNS と DDL 列数が一致（Pitfall 4）
    - test_is_primary_default_false_on_synthetic_rows でテスト挿入行スコープで DEFAULT false を検証（REVIEW C10）
    - test_is_primary_not_null_constraint で NULL INSERT が拒否される（REVIEW HIGH#8）
    - 既存 test_prediction_load.py::test_idempotent_checksum_match が引き続き GREEN（is_primary 追加で回帰なし）
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: set_primary_model 新規関数（model_version scoped・idempotent・D-09）</name>
  <files>src/db/prediction_load.py, tests/db/test_is_primary_flag.py</files>
  <read_first>
    - src/db/prediction_load.py（_idempotent_load_prediction lines 174-348: staging-swap idiom・model_version scoped・psycopg.sql.SQL/Identifier/Placeholder で SQL injection 防御・set_primary_model と同方針）
    - src/db/prediction_load.py（Task 1 で追加した _BOOL_COLS・PREDICTION_COLUMNS 16列）
    - 06-CONTEXT.md D-07/D-08/D-09（人間判断・タイブレーク規則・is_primary フラグ付与・両モデル行は保持）
    - 06-RESEARCH.md「主モデル確定の実装 D-09」節（set_primary_model 実装例・3ファイル連鎖・idempotent・両モデル保持）
    - 06-PATTERNS.md prediction_load.py セクション set_primary_model 実装例（SQL/Identifier/Placeholder・model_type+model_version+feature_snapshot_id+as_of_datetime スコープ）
    - 06-RESEARCH.md Pitfall 4（3ファイル連鎖・PREDICTION_COLUMNS と DDL 1:1）
  </read_first>
  <behavior>
    - Test 1: test_set_primary_model_basic（requires_db）— set_primary_model(write_cur, primary_model_type="lightgbm", primary_model_version="...", feature_snapshot_id="...", as_of_datetime=...) 実行後・当該スコープで lightgbm 行が is_primary=true・catboost 行が is_primary=false
    - Test 2: test_set_primary_model_idempotent（requires_db）— 2回連続実行で同一状態（staging-swap と同・idempotent・D-09）
    - Test 3: test_set_primary_model_both_models_retained（requires_db）— is_primary 更新後も両モデル（lightgbm+catboost）の行が保持される（行数が更新前後で変わらない・D-09 silent 履歴破壊防止）
    - Test 4: test_set_primary_model_scoped（requires_db）— 異なる feature_snapshot_id/as_of_datetime の行は is_primary が変更されない（model_version scoped・全行 UPDATE でない）
    - Test 5: test_set_primary_model_switch（requires_db）— lightgbm→catboost に主モデルを切り替えた場合・lightgbm=false catboost=true になる（再選定可能・D-09）
    - Test 6: test_set_primary_model_raises_on_zero_rows（requires_db）— **REVIEW HIGH#7:** 存在しない model_version や as_of_datetime（timezone/microsecond ズレ）を指定すると 0 行 UPDATE になる場合・set_primary_model が RuntimeError を raise（post-condition assert・silent no-op 防止）。スコープ条件合致で 0 行の合成ケースで検証。
    - Test 7: test_set_primary_model_post_condition_one_true_per_model_type（requires_db）— **REVIEW HIGH#7:** set_primary_model 完了後・post-condition として「当該スコープで is_primary=true が model_type=lightgbm/catboost のいずれか1 model_type のみ・かつ true 行数 >= 1」が成立（複数 model_type で true にならない・空にもならない）。synthetic INSERT で両モデル行を作ってから set_primary_model を呼び・SELECT COUNT(*) WHERE is_primary=true GROUP BY model_type を assert。
  </behavior>
  <action>
    src/db/prediction_load.py に set_primary_model 関数を新規追加（Task 1 の is_primary 列追加完了後・PATTERNS.md prediction_load.py セクション実装例準拠・**REVIEW HIGH#7 post-condition assert 組込**）:

    1. **set_primary_model(write_cur, *, primary_model_type, primary_model_version, feature_snapshot_id, as_of_datetime) -> None（D-09・model_version scoped・idempotent・REVIEW HIGH#7 post-condition 付き）:**
       - imports に `from psycopg.sql import SQL, Identifier, Placeholder` は既存（prediction_load.py lines 58）のため追加不要
       - **REVIEW C11/HIGH#7 (as_of_datetime canonical parse):** as_of_datetime 引数は ISO8601 文字列・datetime.datetime・pd.Timestamp のいずれかを受付・内部で `pd.Timestamp(as_of_datetime).to_pydatetime()` で canonical 化してから WHERE バインド（timezone/microsecond ズレで 0 行 UPDATE になる silent no-op を防止）。_feature_snapshot_id も str で正規化。
       - スコープ条件: `feature_snapshot_id = %s AND as_of_datetime = %s`（当該 snapshot+時点の全予測行が対象）
       - Step 1: 当該スコープの全行を is_primary=false にリセット: `UPDATE prediction.fukusho_prediction SET is_primary = false WHERE feature_snapshot_id = %s AND as_of_datetime = %s`（parameterized query・SQL injection 防御）・`write_cur.execute` の戻り rowcount を記録
       - Step 2: 選定 model_type+model_version の行を is_primary=true に: `UPDATE ... SET is_primary = true WHERE model_type = %s AND model_version = %s AND feature_snapshot_id = %s AND as_of_datetime = %s`・rowcount を記録
       - **REVIEW HIGH#7 post-condition assert（silent no-op 防止）:**
         - `if step2_rowcount == 0: raise RuntimeError(f"set_primary_model: 0 rows updated for model_type={primary_model_type} model_version={primary_model_version} feature_snapshot_id={feature_snapshot_id} as_of_datetime={as_of_datetime}. model_version/as_of_datetime の指定・timezone/microsecond ズレを確認")`
         - SELECT で post-condition 検証: `SELECT model_type, count(*) FILTER (WHERE is_primary) FROM prediction.fukusho_prediction WHERE feature_snapshot_id=%s AND as_of_datetime=%s GROUP BY model_type`。is_primary=true の model_type が1種類のみ・かつそれが primary_model_type に一致・かつ true 行数 >= 1。違反の場合 RuntimeError。
       - psycopg.sql.SQL と Placeholder を使用し・文字列組み立て禁止（既存 _idempotent_load_prediction パターン・T-06-09 mitigate）
       - docstring で「両モデル行は保持（silent 履歴破壊防止・prediction_load の model_version scoped swap と同方針）・2回実行で同一状態（idempotent）・0 行 UPDATE は RuntimeError（REVIEW HIGH#7）」を明記

    2. **__all__ 拡張:** set_primary_model を __all__ に追加

    3. **tests/db/test_is_primary_flag.py に7テストを追加（Task 1 の8テスト + 本 task の7テスト = 15テスト・REVIEW HIGH#7/C11 対応）:**
       - test_set_primary_model_basic(write_cur): テスト用データを INSERT（合成 prediction DataFrame・両モデル・当該スコープ・model_version="test_set_primary_<uuid4>"）→ set_primary_model(lightgbm) → SELECT で lightgbm=true catboost=false を確認・try/finally で teardown（当該 model_version の行 DELETE）
       - test_set_primary_model_idempotent(write_cur): set_primary_model を2回連続実行 → 同一 SELECT 結果
       - test_set_primary_model_both_models_retained(write_cur): set_primary_model 前後で当該スコープの行数が一致
       - test_set_primary_model_scoped(write_cur): 異なる feature_snapshot_id のテストデータも INSERT → スコープB の行は変更されない
       - test_set_primary_model_switch(write_cur): lightgbm→catboost 切替で lightgbm=false catboost=true
       - **test_set_primary_model_raises_on_zero_rows(write_cur): REVIEW HIGH#7:** 存在しない model_version（"nonexistent_<uuid4>"）で set_primary_model を呼ぶ → RuntimeError（0 行 UPDATE 検知）・try/finally で teardown
       - **test_set_primary_model_post_condition_one_true_per_model_type(write_cur): REVIEW HIGH#7:** set_primary_model 完了後・SELECT COUNT GROUP BY model_type で is_primary=true が1 model_type のみ・かつ >= 1 行

    **厳守:** set_primary_model は model_type+model_version+feature_snapshot_id+as_of_datetime スコープで UPDATE（全行 UPDATE でない・silent 履歴破壊防止・D-09）。psycopg.sql.SQL + Placeholder で SQL injection 防御（文字列組み立て禁止・T-06-09）。2回実行で同一状態（idempotent・staging-swap と同方針）。両モデル行は保持（DELETE しない・is_primary フラグのみ UPDATE）。**REVIEW HIGH#7: 0 行 UPDATE・post-condition 違反は RuntimeError（fail-loud・silent no-op 防止）。** 主モデル選定の「判断」は人間（D-07・checkpoint:human-verify）・set_primary_model は機械的なフラグ反映のみ。
  </action>
  <verify>
    <automated>uv run pytest tests/db/test_is_primary_flag.py -v -x</automated>
  </verify>
  <done>
    - src/db/prediction_load.py に set_primary_model が追加される
    - tests/db/test_is_primary_flag.py の15テストが全て GREEN（requires_db・KEIBA_SKIP_DB_TESTS unset・REVIEW HIGH#7/C11 対応）
    - test_set_primary_model_basic で選定モデル=true 未選定=false が確認される
    - test_set_primary_model_idempotent で2回実行で同一状態（idempotent）
    - test_set_primary_model_both_models_retained で両モデル行が保持される（D-09）
    - test_set_primary_model_scoped で異なるスコープの行が変更されない
    - test_set_primary_model_switch で主モデル切り替えが可能
    - test_set_primary_model_raises_on_zero_rows で 0 行 UPDATE が RuntimeError（REVIEW HIGH#7）
    - test_set_primary_model_post_condition_one_true_per_model_type で post-condition（true が1 model_type のみ・>=1 行）が検証される（REVIEW HIGH#7）
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: is_primary migration 機構承認（REVIEW C17 縮小・主モデル選定自体は 06-05 で実施）</name>
  <files>（checkpoint・ファイル変更なし・人間確認のみ）</files>
  <action>
    checkpoint:human-verify — **REVIEW C17 対応（06-05 Task 2 との重複解消）:** 本 checkpoint は「is_primary migration 機構（schema.py DDL + predict.py PREDICTION_COLUMNS + prediction_load.py set_primary_model・REVIEW HIGH#7/HIGH#8 対応済）」と「unit test GREEN・model_version scoped UPDATE・0 行検知 RuntimeError post-condition」の確認のみに縮小。主モデルの「選定」自体（D-04 基準でどちらにするか）は Plan 06-05 Task 2 checkpoint:human-verify で実施する（06-04 と 06-05 の重複を解消）。
    実行手順: (1) uv run pytest tests/db/test_is_primary_flag.py -v が GREEN を確認（KEIBA_SKIP_DB_TESTS unset・15テスト）(2) src/db/prediction_load.py::set_primary_model が model_version scoped UPDATE で・0 行 UPDATE で RuntimeError（HIGH#7 post-condition）・is_primary が boolean NOT NULL DEFAULT false（HIGH#8）であることを確認 (3) src/model/predict.py の PREDICTION_COLUMNS が16列（is_primary 末尾）であることを確認。
  </action>
  <verify>
    <automated>uv run pytest tests/db/test_is_primary_flag.py -v -x</automated>
    <human-check>人間が "approved" で resume（15テスト GREEN・KEIBA_SKIP_DB_TESTS unset を確認の上・is_primary migration 機構を承認。主モデルの選定自体は Plan 06-05 Task 2 で実施）</human-check>
  </verify>
  <done>人間が is_primary migration 機構を承認（15テスト GREEN・model_version scoped UPDATE・0 行検知 RuntimeError・NOT NULL DEFAULT false・主モデル選定自体は Plan 06-05 で実施）</done>
  <what-built>
    is_primary フラグ付与の機構（schema.py DDL・REVIEW HIGH#8 NOT NULL 明示 + predict.py PREDICTION_COLUMNS 16列 + prediction_load.py set_primary_model・REVIEW HIGH#7 post-condition assert 付き）と検証テスト（tests/db/test_is_primary_flag.py・15テスト）が完成した。主モデルの「選定」自体（D-07）は Plan 06-05 Task 2 checkpoint:human-verify で実施する（REVIEW C17 重複解消）。
  </what-built>
  <how-to-verify>
    1. uv run pytest tests/db/test_is_primary_flag.py -v が GREEN（15テスト・KEIBA_SKIP_DB_TESTS unset）を確認
    2. src/db/prediction_load.py に set_primary_model 関数が存在し・model_type+model_version+feature_snapshot_id+as_of_datetime スコープで UPDATE し・0 行 UPDATE で RuntimeError（REVIEW HIGH#7）・post-condition で is_primary=true が1 model_type のみであることを確認
    3. src/model/predict.py の PREDICTION_COLUMNS が16列（is_primary 末尾）であることを確認
    4. src/db/schema.py PREDICTION_ADD_IS_PRIMARY_SQL が `boolean NOT NULL DEFAULT false`（REVIEW HIGH#8）であることを確認
  </how-to-verify>
  <resume-signal>Type "approved" で is_primary migration 機構を承認（主モデル選定自体は Plan 06-05 Task 2 で実施）。課題がある場合は describe issues。</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → live DB (etl ロール) | set_primary_model が etl ロールで prediction.fukusho_prediction の is_primary を UPDATE（model_version scoped・parameterized query） |
| test → live DB (etl ロール) | tests/db/test_is_primary_flag.py が write_cur fixture で ALTER/INSERT/UPDATE を実行（try/finally teardown） |
| 人間判断 → 機械的反映 | checkpoint:human-verify で人間が主モデルを選定し・set_primary_model 経由で is_primary フラグを反映（D-07/D-09） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Tampering | set_primary_model の SQL injection（文字列組み立て） | mitigate | psycopg.sql.SQL + Placeholder で parameterized query（既存 _idempotent_load_prediction lines 174-348 パターン踏襲・文字列組み立て禁止） |
| T-06-10 | Tampering/Elevation | is_primary フラグの恣意的付与（後知恵で主モデルをすり替え） | mitigate | D-07 人間判断 + 理由記録（reports/06-evaluation.md・Plan 06-05）・model_version scoped UPDATE で監査性担保・Phase 8 対抗的監査（TEST-01・CONTEXT deferred） |
| T-06-11 | Tampering | silent 履歴破壊（is_primary UPDATE で他モデル行が削除される） | mitigate | set_primary_model は is_primary フラグのみ UPDATE（DELETE しない）・test_set_primary_model_both_models_retained で両モデル行保持を assert・staging-swap idiom と同方針（review HIGH#1 延長） |
| T-06-12 | Tampering | Pitfall 4・3ファイル連鎖の更新忘れ（schema.py/predict.py/prediction_load.py 不一致） | mitigate | test_prediction_columns_matches_ddl_count で PREDICTION_COLUMNS（16列）と DDL 列数の一致を機械検証・既存 test_prediction_load.py::test_idempotent_checksum_match が INSERT 列数不一致を検知（回帰） |
| T-06-12b | Tampering | **REVIEW HIGH#7:** set_primary_model の silent no-op（0 行 UPDATE・model_version/as_of_datetime ズレ）で主モデルなし状態 | mitigate | 0 行 UPDATE で RuntimeError（post-condition assert）・test_set_primary_model_raises_on_zero_rows / test_set_primary_model_post_condition_one_true_per_model_type で検証・as_of_datetime は pd.Timestamp で canonical parse（C11） |
| T-06-12c | Tampering | **REVIEW HIGH#8:** is_primary の NULL 許容で CHECK 制約が vacuous | mitigate | `boolean NOT NULL DEFAULT false` 明示・predict_p_fukusho で False 初期化・load 時 None→False 正規化・test_is_primary_not_null_constraint で NULL INSERT 拒否を検証 |
</threat_model>

<verification>
- uv run pytest tests/db/test_is_primary_flag.py -v が GREEN（12テスト・requires_db）
- uv run pytest tests/model/test_prediction_load.py -v が引き続き GREEN（is_primary 追加で回帰なし・Pitfall 4）
- PREDICTION_COLUMNS（16列）と PREDICTION_TABLE_DDL + ALTER の列数が一致
- set_primary_model が model_version scoped・idempotent・両モデル保持
- checkpoint:human-verify で主モデル選定機構を承認
</verification>

<success_criteria>
- D-09（is_primary フラグ付与）: prediction.fukusho_prediction に is_primary 列が追加され set_primary_model で UPDATE 可能
- Pitfall 4（3ファイル連鎖）: schema.py / predict.py / prediction_load.py が一致して更新され回帰テスト GREEN
- silent 履歴破壊防止: set_primary_model は両モデル行を保持（DELETE しない）
- idempotent: 2回実行で同一状態
- D-07（人間判断）: checkpoint:human-verify で主モデル選定機構を承認（実際の選定は Plan 06-05 run_evaluation.py 実行時）
</success_criteria>

<output>
Create `.planning/phases/06-evaluation-calibration-gates/06-04-SUMMARY.md` when done
</output>

<artifacts>
## Artifacts this phase produces

**DB Schema (src/db/schema.py):**
- `PREDICTION_ADD_IS_PRIMARY_SQL` — is_primary 列追加 ALTER（idempotent・**REVIEW HIGH#8: `boolean NOT NULL DEFAULT false` 明示**・CHECK 制約 prediction_is_primary_domain（NOT NULL 二重防御）・COMMENT ON COLUMN）
- `APPLY_ORDER` — `("prediction_add_is_primary", PREDICTION_ADD_IS_PRIMARY_SQL)` 追加（prediction_table の直後・GRANT の前）

**DB Column (prediction.fukusho_prediction):**
- `is_primary boolean NOT NULL DEFAULT false` — 主モデル確定フラグ（D-09・REVIEW HIGH#8 NOT NULL 明示・CHECK 制約 is_primary IN (true,false) は NOT NULL の二重防御）

**Constants (src/model/predict.py):**
- `PREDICTION_COLUMNS` — 15列→16列（末尾に "is_primary" 追加・predict_p_fukusho で False 初期化）

**Functions (src/db/prediction_load.py):**
- `_BOOL_COLS = {"is_primary"}` — _df_to_prediction_tuples の型処理用集合（None→False 正規化・REVIEW HIGH#8）
- `set_primary_model(write_cur, *, primary_model_type, primary_model_version, feature_snapshot_id, as_of_datetime) -> None` — model_version scoped is_primary UPDATE（idempotent・両モデル保持・D-09・**REVIEW HIGH#7: 0 行 UPDATE で RuntimeError（post-condition assert）**・**REVIEW C11: as_of_datetime の canonical parse（timezone/microsecond ズレ対策）**）

**Test files:**
- **REVIEW Codex LOW cycle-2 明示判断:** tests/db/__init__.py は**既存**（find で確認済）のため新規作成せず。本プロジェクトの package marker 規約（tests/__init__.py・tests/model/__init__.py・tests/ev/__init__.py・tests/features/__init__.py も既存）に従い保持。
- tests/db/test_is_primary_flag.py（新規・requires_db・15テスト）:
  - Task 1 (8テスト): test_prediction_columns_includes_is_primary, test_prediction_columns_matches_ddl_count, test_alter_adds_is_primary_column, **test_is_primary_default_false_on_synthetic_rows (REVIEW C10: テスト挿入行スコープ)**, **test_is_primary_not_null_constraint (REVIEW HIGH#8)**, test_is_primary_check_constraint, test_df_to_prediction_tuples_is_primary_none, test_df_to_prediction_tuples_is_primary_bool
  - Task 2 (7テスト): test_set_primary_model_basic, test_set_primary_model_idempotent, test_set_primary_model_both_models_retained, test_set_primary_model_scoped, test_set_primary_model_switch, **test_set_primary_model_raises_on_zero_rows (REVIEW HIGH#7)**, **test_set_primary_model_post_condition_one_true_per_model_type (REVIEW HIGH#7)**
</artifacts>
