---
phase: 05-ev-backtest
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/group_split.py
  - tests/utils/test_group_split.py
  - tests/ev/__init__.py
  - tests/ev/conftest.py
  - tests/ev/test_ev_rank.py
  - tests/ev/test_purchase_simulator.py
  - tests/ev/test_metrics.py
  - tests/ev/test_odds_snapshot.py
  - tests/ev/test_refund_accounting.py
  - tests/ev/test_bl3_betting.py
  - tests/model/test_orchestrator_bt.py
  - tests/db/__init__.py
  - tests/db/test_backtest_load.py
autonomous: true
requirements: [BACK-01]
user_setup: []

must_haves:
  truths:
    - "BACK-01: BT-1..5 窓が race_date filter + 既存 guard（race_id disjoint + strict chronological）で (train_race_ids, test_race_ids) を返す"
    - "§15.5 優先: BT-1..3 train_start='2019-06-01'（Phase 3 D-09 の 2016H2〜 でなく要件正を適用）"
    - "Wave 0 RED stubs: 9 テストファイルが全て ImportError/AttributeError で RED（後続 Wave 1/2 plan が GREEN 化）"
    - "group_split の既存 race_id_time_series_split は変更されず（後方互換）"
    - "MEDIUM-01a: 固定 BT窓と mlxtend.GroupTimeSeriesSplit の等価性が docstring/test で明示（race_id disjoint + strict chronological を固定窓で担保・本質は GroupTimeSeriesSplit と同一）"
  artifacts:
    - path: "src/utils/group_split.py"
      provides: "BTWindow dataclass / BT_WINDOWS 定数 / get_bt_race_ids ヘルパ"
      contains: "class BTWindow"
    - path: "tests/ev/conftest.py"
      provides: "合成データ fixtures（JODDS/HARAI/label/prediction mock）"
      contains: "def make_"
    - path: "tests/utils/test_group_split.py"
      provides: "BT窓ヘルパ新設分の unit test（追加）"
      contains: "def test_bt_window_disjoint"
  key_links:
    - from: "tests/utils/test_group_split.py::test_bt_window_disjoint"
      to: "src/utils/group_split.py::get_bt_race_ids"
      via: "BT窓 race_id filter → 既存 guard（disjoint + strict chronological）の検証"
      pattern: "get_bt_race_ids"
    - from: "src/utils/group_split.py::BT_WINDOWS"
      to: "scripts/run_backtest.py (Plan 05)"
      via: "BT窓定数を run_backtest が import して再学習ループを回す"
      pattern: "from src.utils.group_split import BT_WINDOWS"
---

<objective>
BT窓ヘルパ新設 + Phase 5 全 unit test の Wave 0 RED stub 集群を構築する。Phase 5 実装の基盤（他 plan が BT_WINDOWS と RED stubs に依存）。
Purpose: BACK-01（race_id-grouped + BT窓）の構造的ブロックを確立し・後続 plan が RED→GREEN で進める土台を作る。
Output: `BTWindow`/`BT_WINDOWS`/`get_bt_race_ids`（group_split.py）+ 9 RED stub テストファイル（tests/ev/, tests/db/, tests/model/test_orchestrator_bt.py）+ 合成データ fixtures（conftest.py・Umaban 列保持で per-horse odds テストを可能化）。
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ev-backtest/05-CONTEXT.md
@.planning/phases/05-ev-backtest/05-RESEARCH.md
@.planning/phases/05-ev-backtest/05-PATTERNS.md
@.planning/phases/05-ev-backtest/05-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BT窓ヘルパ新設（group_split.py 追記）+ 新設分 unit test GREEN</name>
  <files>src/utils/group_split.py, tests/utils/test_group_split.py</files>
  <read_first>
    - src/utils/group_split.py（既存 race_id_time_series_split の3ガード形式: 行104-127 raise ValueError / mlxtend re-export: 行26 / docstring の「BT-* helper は Phase 4 で追加予定」言及: 行42-46）
    - .planning/phases/05-ev-backtest/05-RESEARCH.md §5（BT窓ヘルパ・§15.5 BT-1..5 定義・Pitfall §15.5 2019-06 開始優先）
    - .planning/phases/05-ev-backtest/05-PATTERNS.md「src/utils/group_split.py」節（既存 guard 模倣）
    - tests/utils/test_group_split.py（既存 test_race_id_disjoint_raises / test_assert_is_replaced_by_raise 形式）
    - CLAUDE.md「要件定義書優先」（§15.5 vs Phase 3 D-09）
  </read_first>
  <behavior>
    - test_bt_window_disjoint: 各 BT窓で set(train_race_ids).isdisjoint(set(test_race_ids))
    - test_bt_window_strict_chronological: max(train.race_start_datetime) < min(test.race_start_datetime)
    - test_bt_window_2019_06_start: BT-1..3.train_start == '2019-06-01'（§15.5 優先・Phase 3 D-09 の 2016H2〜 でない）
    - test_bt_window_b4_b5_rolling: BT-4/5.window_type == 'rolling'（BT-1..3 は 'expanding'）
    - test_get_bt_race_ids_raises_on_leak: 合成 races で train/test が race_id 共有 → ValueError
    - test_get_bt_race_ids_uses_raise_not_assert: inspect.getsource に "raise ValueError" 含む（python -O 生存）
    - test_bt_window_equivalent_to_group_ts_split: 各 BT窓で get_bt_race_ids が返す train/test が、同一 races に対し race_id ソート後に mlxtend.evaluate.GroupTimeSeriesSplit を適用して得られるグループ分割と「train/test 全 race_id が同一集合」であることを assert（MEDIUM-01a：固定 BT窓と GroupTimeSeriesSplit の等価性を立証・race_id disjoint + strict chronological を本質的に等価担保）
  </behavior>
  <action>
    src/utils/group_split.py に追記（既存 race_id_time_series_split は触らない・後方互換）:
    - dataclasses.dataclass(frozen=True) BTWindow（フィールド: name, train_start, train_end, test_start, test_end, window_type）
    - BT_WINDOWS: list[BTWindow] 定数（§15.5 完全準拠・BT-1..3 train_start='2019-06-01'/expanding・BT-4/5 rolling・test 年は BT-1..3=2023/2024/2025・BT-4/5 も 2024 で揃える per D-03 + planner A2）
      - BT-1: 2019-06-01..2022-12-31 / 2023-01-01..2023-12-31 / expanding
      - BT-2: 2019-06-01..2023-12-31 / 2024-01-01..2024-12-31 / expanding
      - BT-3: 2019-06-01..2024-12-31 / 2025-01-01..2025-12-31 / expanding
      - BT-4: 2021-01-01..2023-12-31 / 2024-01-01..2024-12-31 / rolling（直近3年）
      - BT-5: 2019-01-01..2023-12-31 / 2024-01-01..2024-12-31 / rolling（直近5年）
    - get_bt_race_ids(races: pd.DataFrame, bt: BTWindow) -> tuple[list[str], list[str]]: race_date.between で filter → 既存3ガード（race_id disjoint / strict chronological max(train)<min(test) / non-empty）と同一の raise ValueError 形式で保証（assert 不可・python -O 生存 per 共有パターン2）
    - docstring（MEDIUM-01a）に明記: BT窓固定 race_date 区間分割は mlxtend.evaluate.GroupTimeSeriesSplit と等価である（どちらも race_id-level disjoint + strict chronological を保証する）。§15.5 がレース日で明示的な窓を指定しているため固定区間を採用したが、race_id 漏洩・時系列逆転が起きないことは GroupTimeSeriesSplit と同一保証である旨を docstring に記述。
    - mlxtend re-export（行26）はそのまま保持

    tests/utils/test_group_split.py に追記（既存 test は触らない）: 上記 behavior 7 テストを追加。合成 races DataFrame は race_id/race_date/race_start_datetime 列を持つ最小 fixture。test_bt_window_equivalent_to_group_ts_split は races を race_start_datetime 昇順でソート後、bt.train_end 以前 = train, bt.test_start 以降 = test と GroupTimeSeriesSplit の分割結果を race_id 集合で比較する。
  </action>
  <verify>
    <automated>uv run pytest tests/utils/test_group_split.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/utils/group_split.py に "class BTWindow" と "BT_WINDOWS" と "def get_bt_race_ids" が含まれる
    - BT_WINDOWS の BT-1.train_start == '2019-06-01'（§15.5 優先・grep で "2019-06-01" が3箇所 BT-1/2/3 に現れる）
    - get_bt_race_ids の source に "raise ValueError" が含まれる（inspect で検証する test 含む）
    - uv run pytest tests/utils/test_group_split.py が全件 GREEN（既存 + 新規6件）
    - 既存 race_id_time_series_split のシグネチャ・docstring は変更なし（git diff で既存関数本体が未編集）
  </acceptance_criteria>
  <done>BT窓ヘルパ新設 + 新設分 unit test 全 GREEN・§15.5 完全準拠（2019-06 開始）・既存 group_split は後方互換</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave 0 RED stub 集群（tests/ev/, tests/db/, tests/model/test_orchestrator_bt.py）+ 合成 fixtures</name>
  <files>tests/ev/__init__.py, tests/ev/conftest.py, tests/ev/test_ev_rank.py, tests/ev/test_purchase_simulator.py, tests/ev/test_metrics.py, tests/ev/test_odds_snapshot.py, tests/ev/test_refund_accounting.py, tests/ev/test_bl3_betting.py, tests/model/test_orchestrator_bt.py, tests/db/__init__.py, tests/db/test_backtest_load.py</files>
  <read_first>
    - tests/conftest.py（KEIBA_SKIP_DB_TESTS skip policy・Settings() validation fail デフォルト・@pytest.mark.requires_db marker）
    - tests/model/test_baseline.py（純粋関数テスト形式・合成 DataFrame・np.testing.assert_almost_equal）
    - tests/model/test_prediction_load.py（integration test 形式・write_cur fixture・idempotent verify）
    - tests/model/test_orchestrator.py（orchestrator test 形式）
    - .planning/phases/05-ev-backtest/05-VALIDATION.md（Wave 0 Requirements 9 ファイル + 合成データ設計指針）
    - .planning/phases/05-ev-backtest/05-RESEARCH.md §1.4/§2.5/§3.4/§4.6/§5.5/§6.5/§8.4/§9.4（各テストの検証内容）
  </read_first>
  <behavior>
    RED stub は全て ImportError/AttributeError/ModuleNotFoundError で失敗（実装未存在）・後続 Wave 1/2 plan が GREEN 化:
    - test_ev_rank.py::test_ev_calculation / test_rank_S / test_rank_D_low_ev / test_rank_B_no_odds_threshold
    - test_purchase_simulator.py::test_purchase_filter_conditions / test_purchase_top2 / test_purchase_tiebreak / test_purchase_no_eligible / test_purchase_no_sale
    - test_metrics.py::test_metrics_recovery_rate / test_metrics_refund_excluded / test_metrics_max_drawdown / test_metrics_counts
    - test_odds_snapshot.py::test_odds_snapshot_backward / test_odds_snapshot_no_bet_empty / test_odds_snapshot_special_values / test_odds_snapshot_future_leak / test_odds_snapshot_day_boundary / test_odds_snapshot_multi_horse（HIGH-1 MEDIUM: 同一 race_key・3頭・各異なる odds を入力・出力行数==3・各馬の odds が保持される・RESEARCH §1.1 PK は race_key 7カラム+Umaban の8カラム）
    - test_refund_accounting.py::test_refund_normal_hit / test_refund_normal_miss / test_refund_scratch_cancel / test_refund_race_excluded / test_refund_dead_loss / test_refund_fuseiritu / test_refund_race_cancelled / test_refund_no_sale / test_refund_deadheat
    - test_bl3_betting.py::test_bl3_select_top2_low_odds / test_bl3_no_ev / test_bl3_caveat
    - test_orchestrator_bt.py::test_split_3way_periods_injection / test_split_3way_backward_compat (periods=None で既存挙動)
    - test_backtest_load.py::test_backtest_load_idempotent (requires_db) / test_backtest_load_scoped_swap (requires_db)
  </behavior>
  <action>
    tests/ev/conftest.py に合成データ fixtures を定義（Plan 02/03 の GREEN 化で使用）:
    - make_jodds_mock(race_key, snapshots, *, umabans=None) → DataFrame（HappyoTime=mmddHHMM, FukuOddsLow/High, Umaban, race_key 7カラム・正常値/特殊値 ----/****/0000/0999 混在可）。umabans 指定で複数馬の per-horse snapshot を生成可能（HIGH-1 MEDIUM・test_odds_snapshot_multi_horse 用）。JODDS PK は race_key 7カラム + Umaban の8カラム（RESEARCH §1.1）なので必ず Umaban 列を持つ。
    - make_harai_mock(scenario) → DataFrame（FuseirituFlag2/HenkanFlag2/TokubaraiFlag2/PayFukusyoUmaban1..5/PayFukusyoPay1..5・6シナリオ）
    - make_label_mock(scenario) → DataFrame（is_scratch_cancel/is_race_excluded/is_dead_loss/is_race_cancelled/is_fukusho_sale_available/fukusho_payout_places/fukusho_hit_validated・6シナリオ）
    - make_prediction_mock(race_key, n_horses) → DataFrame（p_fukusho_hit + PK 7カラム + race_key + umaban）

    各 test_*.py は module-level import で ImportError を起こす（後続 plan が src.ev.* を実装して GREEN 化）:
    - テスト本体は assertion のみ記述（実装がないため RED）
    - conftest fixtures を parametrize で各シナリオに適用（例: test_refund_* を @pytest.mark.parametrize で6シナリオ）
    - test_backtest_load.py は @pytest.mark.requires_db + write_cur fixture（tests/conftest.py 既存）使用・KEIBA_SKIP_DB_TESTS skip policy 継承
  </action>
  <verify>
    <automated>uv run pytest tests/ev/ tests/db/test_backtest_load.py tests/model/test_orchestrator_bt.py --co -q | head -50 && uv run pytest tests/ev/ tests/model/test_orchestrator_bt.py -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/ev/__init__.py と tests/ev/conftest.py と tests/db/__init__.py が存在
    - tests/ev/conftest.py に "def make_jodds_mock" / "def make_harai_mock" / "def make_label_mock" / "def make_prediction_mock" が含まれる・make_jodds_mock の戻り値 DataFrame が Umaban 列を保持（HIGH-1 MEDIUM: JODDS PK は race_key 7カラム + Umaban の8カラム・per-horse odds 用）
    - 9 テストファイル（test_ev_rank / test_purchase_simulator / test_metrics / test_odds_snapshot / test_refund_accounting / test_bl3_betting / test_run_backtest_e2e ※Plan 05 で追加分 / test_orchestrator_bt / test_backtest_load）が --collect-only で列挙される。（LOW-01 整合: 本 plan で作成する RED stub は 7 ファイル＋ tests/db/__init__.py・test_backtest_load.py と test_orchestrator_bt.py は既存 tests 配下に新設・合計 9 テストファイル）
    - RED 実行: uv run pytest tests/ev/ tests/model/test_orchestrator_bt.py が ImportError/AttributeError で RED（src.ev 未実装・src.model.data.split_3way に periods パラメータ未存在）
    - test_backtest_load.py は --collect-only で表示・KEIBA_SKIP_DB_TESTS=1 で skip
    - tests/conftest.py の skip policy（KEIBA_SKIP_DB_TESTS / @pytest.mark.requires_db）は変更なし
  </acceptance_criteria>
  <done>Wave 0 RED stub 集群（9 テストファイル + 4 合成 fixtures）が全て collect 可能・実行は RED（実装未存在）・後続 plan が GREEN 化する土台完成</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests → src/ev,src/model | RED stub は実装前・実行時 ImportError を前提（silent green を許さない） |
| BT窓 race_date filter → guard | 合成 races が guard を bypass するデータで ValueError をraise すること |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | get_bt_race_ids の race_id disjoint / strict chronological | mitigate | 既存 group_split.py と同一の raise ValueError guard（python -O 生存・test_get_bt_race_ids_uses_raise_not_assert で検証） |
| T-05-02 | Tampering | BT-1..3 train_start を Phase 3 D-09（2016H2〜）にすり替え | mitigate | test_bt_window_2019_06_start が '2019-06-01' を assert（§15.5 優先・CLAUDE.md 要件定義書優先） |
| T-05-01b | Information Disclosure | 固定 BT窓が mlxtend.GroupTimeSeriesSplit と異なる race_id disjoint 挙動を示し test 期間 race_id が train に混入（MEDIUM-01a） | mitigate | docstring + test_bt_window_equivalent_to_group_ts_split で両者の分割集合が一致することを assert（race_id 漏洩構造的に不可） |
</threat_model>

<verification>
- uv run pytest tests/utils/test_group_split.py -x -q → GREEN（既存 + 新設7件・test_bt_window_equivalent_to_group_ts_split 含む）
- uv run pytest tests/ev/ tests/model/test_orchestrator_bt.py --co -q → 全テスト関数 collect 可能
- uv run pytest tests/ev/ tests/model/test_orchestrator_bt.py -q → RED（実装未存在・後続 plan が GREEN 化）
</verification>

<success_criteria>
- BACK-01 構造的ブロック確立: BT_WINDOWS（§15.5 完全準拠・2019-06 開始）+ get_bt_race_ids（race_id disjoint + strict chronological guard）が GREEN
- 固定 BT窓と mlxtend.GroupTimeSeriesSplit の等価性を docstring + test で立証（MEDIUM-01a・T-05-01b）
- Wave 0 RED stub 集群（9 テストファイル + 4 合成 fixtures・Umaban 列保持で per-horse odds テスト対応）が後続 Wave 1/2 plan で GREEN 化する準備完了
- 既存 race_id_time_series_split は後方互換（変更なし）
</success_criteria>

<output>
Create `.planning/phases/05-ev-backtest/05-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces（本 plan の分）

- `src/utils/group_split.py::BTWindow`（dataclass frozen=True）
- `src/utils/group_split.py::BT_WINDOWS`（§15.5 完全準拠リスト定数）
- `src/utils/group_split.py::get_bt_race_ids(races, bt) -> tuple[list[str], list[str]]`
- `tests/ev/__init__.py` / `tests/ev/conftest.py`（make_jodds_mock[Umaban 列保持] / make_harai_mock / make_label_mock / make_prediction_mock）
- `tests/ev/test_*.py` 6ファイル（RED stub・test_odds_snapshot.py は multi-horse case 含む）・`tests/model/test_orchestrator_bt.py`（RED stub）・`tests/db/__init__.py` + `tests/db/test_backtest_load.py`（RED stub）
