---
phase: 05-ev-backtest
plan: 05
type: execute
wave: 4
depends_on: ["05-01", "05-02", "05-03", "05-04"]
files_modified:
  - scripts/run_backtest.py
  - src/ev/report.py
autonomous: true
requirements: [BACK-01, BACK-02, BACK-03, BACK-04]
user_setup: []

must_haves:
  truths:
    - "D-03: scripts/run_backtest.py が BT_WINDOWS × {30min_before,10min_before} × {lightgbm,catboost} ≈ 20 backtest + 5 BL-3 backtest のフル行列を実行"
    - "BACK-04: reports/05-backtest.{md,json} が全25候補を一括報告（後知恵 winner 単独報告禁止・主モデル確定は Phase 6）"
    - "D-04 BL-3: 確定オッズ昇順 top-2 で固定ルール仮想購入 → 主モデル2つと回収率比較"
    - "§19.1: 各 backtest 行に backtest_strategy_version='fukusho_ev_v1' / odds_snapshot_policy / train_period / test_period / model_type / model_version stamp"
    - "合成データ E2E: JODDS mock + label mock + HARAI mock で run_backtest --synthetic がフル行列を完走・reports/05-backtest 生成（実JODDS未完でも検証可能）"
    - "HIGH-1+HIGH-2 馬単位 JOIN: 予測（馬単位）とオッズ snapshot（馬単位）と label（馬単位）の全 JOIN が race_key 単独でなく (race_key, umaban) で実行され・行数が入力予測行数と一致（cartesian duplication 構造的ブロック）"
    - "HIGH-5 BT窓 category map refit: 各 BT窓の再学習で category_map が BT train 期間のみで再 fit され・test 窓 race_id の ID 漏洩が構造的に防止される（CONTEXT Claude's Discretion・実行可能検証で担保）。**05-04 HIGH-A で category_map param が orchestrator に伝播・消費されることで本計画の train_and_predict(..., category_map=bt_fit_map) 呼出が有効化される**"
    - "MEDIUM-05 JODDS coverage gate: --synthetic 外す実行で BT test レース/各 policy の JODDS カバレッジが閾値未満の場合 loud fail（取得未完を黙って no_bet 化して不正 backtest を出さない）"
    - "**HIGH-B cycle-2: _carve_calib_from_train_tail(bt) は train_start を固定し train_end = calib_start - 1day に短縮（calib を train 尾から切り出す）・max(train)<min(calib)<max(calib)<min(test) 順序不変を BT-1..BT-5 全窓で deterministic に保証**"
    - "**HIGH-C cycle-2: HARAI 払戻は race-level slot レコード（PayFukusyoUmaban1..5 + PayFukusyoPay1..5・umaban 列なし）のため・run_backtest は HARAI を race-level のまま予測馬行に race-level merge(validate='many_to_one') でブロードキャストする（05-03 _lookup_payfukusyo_pay が行内 slot lookup で払戻を確定・ブロードキャストしない）・行数不変を assert**"
    - "**MEDIUM-A cycle-2: select_bets 後に選択候補だけでなく全候補（selected + non-selected with odds_missing_reason）の full candidate table を構築し load_backtest に渡す・後続監査で BT窓 test 中の no_bet 除外件数・内訳を SQL 集計可能**"
    - "**MEDIUM-B cycle-2: JODDS coverage gate は race-level でなく candidate-horse usable-odds coverage（no_bet/special-odds/FukusyoFlag≠'7' を除外した実利用可能オッズ馬の割合）を BT窓 × policy 毎に測定し閾値未満で loud fail**"
    - "**MEDIUM cycle-3 non-selected 会計ゼロ化: full_candidate_with_accounting のうち selected_flag=False 行（購入していない non-selected / no_bet sentinel）の stake/effective_stake/payout/refund/profit を永続化前に全て 0 に設定する（determine_stake_payout が selected_flag 分岐を持たない事に起因する架空会計を防止・ROI 計算は selected_flag==True filter 済みで非影響・監査行の全行集計での二重カウントを防ぐ）**"
  artifacts:
    - path: "scripts/run_backtest.py"
      provides: "BT窓再学習 + フル行列 backtest 実行 + reports/05-backtest 生成 CLI"
      contains: "def main"
    - path: "src/ev/report.py"
      provides: "generate_report(all_backtests) — 全候補一括報告"
      contains: "def generate_report"
    - path: "reports/05-backtest.md"
      provides: "Phase 5 フル行列 backtest 報告（25候補 + §11.2 odds policy 固定履行確認）"
      contains: "Phase 5 Backtest Report"
  key_links:
    - from: "scripts/run_backtest.py"
      to: "src/model/orchestrator.py::train_and_predict(split_periods=...)"
      via: "BT_WINDOWS ループで各窓 train_and_predict に split_periods を注入"
      pattern: "split_periods"
    - from: "scripts/run_backtest.py"
      to: "src/ev/{odds_snapshot,ev_rank,purchase_simulator,refund_accounting,metrics,bl3_betting,report}.py + src/db/backtest_load.py"
      via: "EV→rank→purchase→refund→metrics→load→report のフルパイプライン"
      pattern: "compute_ev_and_rank|select_bets|determine_stake_payout|compute_backtest_metrics|load_backtest|generate_report"
---

<objective>
scripts/run_backtest.py（BT窓再学習 + フル行列 backtest）と reports/05-backtest.{md,json} 生成を実装する。全 Plan 01-04 の成果物を統合し、Phase 5 の最終成果物（backtest 結果 + 報告）を生成する。
Purpose: BACK-01/02/03/04 + D-03/D-04 を統合し・全25候補一括報告で後知恵排除を履行。
Output: scripts/run_backtest.py + src/ev/report.py + reports/05-backtest.{md,json}（合成データ版）。
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ev-backtest/05-CONTEXT.md
@.planning/phases/05-ev-backtest/05-RESEARCH.md
@.planning/phases/05-ev-backtest/05-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/run_backtest.py（BT窓再学習 + フル行列 backtest + reports 生成）</name>
  <files>scripts/run_backtest.py, src/ev/report.py</files>
  <read_first>
    - scripts/run_train_predict.py（行218-411 起動フロー・Settings/dsn_masked ログ・readonly+etl pool・try/except PsycopgError/finally close・_REPO_ROOT 解決 行59-61・logging 設定 行84-88・_write_eval_report 行490-570）
    - src/model/orchestrator.py::train_and_predict（split_periods パラメータ・Plan 04 で追加）
    - src/utils/group_split.py::BT_WINDOWS（Plan 01 で新設）
    - src/ev/{odds_snapshot,ev_rank,purchase_simulator,refund_accounting,metrics,bl3_betting}.py（Plan 02/03 で実装）
    - src/db/backtest_load.py::load_backtest（Plan 04 で実装）
    - src/model/baseline.py::fetch_market_data（BL-3 用・確定オッズ）
    - src/model/data.py::load_feature_matrix/load_labels（予測+label 結合）
    - reports/04-eval.{md,json} + src/model/evaluator.py（レポートパターン）
    - .planning/phases/05-ev-backtest/05-RESEARCH.md §6.2（BT窓再学習ループ・calib carve）+ §9（BL-3）+ §10（全候補一括報告）
    - .planning/phases/05-ev-backtest/05-PATTERNS.md「scripts/run_backtest.py」節 + 共有パターン6（reports 慣例）
  </read_first>
  <action>
    scripts/run_backtest.py:
    - _REPO_ROOT 解決 + sys.path.insert（run_train_predict.py:59-61 パターン）
    - logging 設定（run_train_predict.py:84-88 パターン・logger=getLogger("run_backtest")）
    - main(args): Settings() から dsn_masked/etl_dsn_masked をログ出力（生 DSN 絶対禁止・T-04-27）
    - readonly pool + etl pool 構築（make_pool(role='readonly'|'etl')）
    - try / except PsycopgError / finally pool.close()
    - args: --snapshot-id（既定 '20260620-1a-postreview-v2'）/--synthetic（合成データ E2E・実JODDS未完でも検証）/--bt-filter（BT窓名絞り・既定全5窓）/--check-reproduce（bit-identical 検証）

    BT窓再学習ループ（D-03）:
    - for bt in BT_WINDOWS: for model_type in ['lightgbm','catboost']:
      - calib slice を BT窓 train 尾から carve（_carve_calib_from_train_tail(bt)）。**HIGH-B cycle-2 修正: train_start は固定し train_end = calib_start - 1day に短縮する（train の *開始* でなく *終了* を前に詰めて calib 分の空きを作る）**。例 BT-1: train=('2019-06-01', calib_start-1day) / calib=('calib_start','calib_end') / test=('2023-01-01','2023-12-31')。calib_start/calib_end のデフォルトは BT窓 train 期間の最後の6ヶ月程度（例 BT-1 では '2022-07-01'〜'2022-12-31'）。max(train)<min(calib)<max(calib)<min(test) 順序 guard は split_3way が保証（HIGH-4）。
      - periods = {'train': (bt.train_start, train_end), 'calib': (calib_start, calib_end), 'test': (bt.test_start, bt.test_end)} — **train_end = calib_start - 1day（HIGH-B cycle-2 修正済み）**
      - **HIGH-5 category_map BT窓再 fit: train_and_predict 呼出時に category_map を BT窓 train 期間の feature_df 行のみで再 fit して渡す（CONTEXT Claude's Discretion: test 窓 race_id の ID 漏洩防止・全期間固定 snapshot の category_map を再利用しない）。fit_category_map(train_feature_df) を BT窓 train 行（periods['train'] 期間・train_end = calib_start-1day）に対して呼び・その map を model_version メタに stamp する（test 窓の未観測 ID は __UNSEEN__ sentinel・§14.3）。**
      - result = train_and_predict(feature_df, model_type=model_type, feature_snapshot_id=args.snapshot_id, split_periods=periods, as_of_datetime=FIXED_REPRODUCE_TS, category_map=bt_fit_map)
      - result['pred_df'] に BT窓 test 予測

    フル行列 backtest（5窓 × 2policy × 2model + 5 BL-3 = 25）:
    - for bt in BT_WINDOWS: for policy in ['30min_before','10min_before']: for model_type in ['lightgbm','catboost']:
      - jodds_df = fetch_jodds(readonly_cur)（--synthetic の場合は合成 mock）
      - **HIGH-1 馬単位 race_times 構築: race_times は BT窓 test 予測馬単位（race_key + umaban + race_start_datetime）で構築し select_odds_snapshot に渡す。select_odds_snapshot は by=['race_key','umaban'] で per-horse snapshot を返す（Plan 03 契約）。**
      - snapshot = select_odds_snapshot(jodds_df, race_times, policy)（BT窓 test レースの予測馬単位）— 戻り値の odds 列は既に snake_case fuku_odds_lower/fuku_odds_upper（Plan 03 cross-plan contract・raw FukuOddsLow/FukuOddsHigh を Plan 03 側で rename 済み）
      - **HIGH-2 cartesian duplication 修正: pred_with_odds = pred_df.merge(snapshot, on=['race_key','umaban'], how='left')（race_key 単独でなく (race_key, umaban) で JOIN）。merge 後に行数 == 入力 pred_df 行数であることを assert し・不一致時は RuntimeError で loud fail（cartesian 膨張を構造的ブロック）。**
      - pred_with_ev = compute_ev_and_rank(pred_with_odds)
      - selected = select_bets(pred_with_ev) — **MEDIUM-A cycle-2 修正: select_bets 後に selected のみでなく pred_with_ev 全行を含む full candidate table を構築する。full_candidate = pred_with_ev.copy() に selected_flag 列を追加（select_bets で選ばれた馬は selected_flag=True・それ以外は False）し・odds_missing_reason 列を odds snapshot の no_bet sentinel から伝播（no_bet/special-odds/FukusyoFlag≠'7'）。full_candidate には pred_with_odds の全馬が残り・後続監査で除外理由別件数を SQL 集計可能。**
      - **HIGH-C cycle-2 修正: HARAI 払戻は race-level slot レコード（RESEARCH §2.1-§2.2 確認: PayFukusyoUmaban1..5 + PayFukusyoPay1..5 の slot 列のみで umaban 列なし・1レース1行）。run_backtest は HARAI を race-level のまま予測馬行（horse-level）にマージする: full_candidate_with_harai = full_candidate.merge(harai_race_df[['race_key', 'PayFukusyoUmaban1', 'PayFukusyoPay1', ..., 'PayFukusyoUmaban5', 'PayFukusyoPay5', 'FuseirituFlag2', 'TokubaraiFlag2', 'HenkanFlag2']], on=['race_key'], how='left', validate='many_to_one')。validate='many_to_one' で race-level HARAI 行が各馬行に一意にブロードキャストされることを pandas が保証（race_key 単位の重複 HARAI 行があれば ValueError で loud fail・ブロードキャスト事故防止）。次に label も馬単位で JOIN: full_candidate_with_label = full_candidate_with_harai.merge(label_df[['race_key','umaban', ...label_flags...]], on=['race_key','umaban'], how='left')（label は馬単位・race_key 単独 JOIN 禁止）。行数不変を assert。**
      - **払戻確定は 05-03 _lookup_payfukusyo_pay(row) で行う（行ベース slot lookup・race-level でマージされた HARAI slot 列から該当 umaban の PayFukusyoPay を取得）。HARAI のブロードキャストでは払戻を確定しない・slot lookup で確定する（HIGH-C cycle-2 契約）。** full_candidate_with_accounting = full_candidate_with_label.apply(determine_stake_payout, axis=1) で stake/payout/profit/effective_stake/refund 付与（determine_stake_payout 内部で _lookup_payfukusyo_pay が HARAI slot 列を参照）。**MEDIUM cycle-3 non-selected 会計ゼロ化: determine_stake_payout は is_fukusho_sale_available で分岐するが selected_flag 分岐を持たないため・購入されない non-selected（selected_flag=False）の normal 馬にも stake=100/effective_stake=100/profit が付与されてしまう。永続化前に non-selected 行の会計をゼロ化する: full_candidate_with_accounting.loc[full_candidate_with_accounting['selected_flag']==False, ['stake','effective_stake','payout','refund','profit']] = 0（non-selected は購入していないので投資も払戻も利益もゼロ・ROI 計算は既に line 114 で selected_flag==True に filter 済みなので非影響・監査行の誤解と全行集計での二重カウントを防止）。no_bet sentinel（odds_missing_reason 埋め）の馬も同様にゼロ化される。**
      - metrics = compute_backtest_metrics(full_candidate_with_accounting[full_candidate_with_accounting['selected_flag']==True]) — metrics は selected_flag=True の行のみで計算（仮想購入候補）
      - backtest_id = f"{bt.name}-{policy}-{model_type}"
      - **load_backtest には full_candidate_with_accounting を渡す（MEDIUM-A cycle-2: selected_flag=True/False の全候補行・odds_missing_reason 埋め済み）** — full_candidate_with_accounting に provenance 列（backtest_id/backtest_strategy_version='fukusho_ev_v1'/odds_snapshot_policy=policy/train_period/test_period/model_type/model_version/feature_snapshot_id）を付与
      - load_backtest(etl_cur, full_candidate_with_accounting)（backtest_id scoped swap・MEDIUM-04 + MEDIUM-A cycle-2: selected_flag=False の no_bet/special-odds/未選択候補行も odds_missing_reason 埋めで永続化・後続監査で除外理由別件数を SQL 集計可能）
    - for bt in BT_WINDOWS: BL-3 backtest（D-04・fetch_market_data で確定オッズ・select_bl3_bets・determine_stake_payout・compute_backtest_metrics・load_backtest・backtest_id=f"{bt.name}-confirmed-bl3"）

    MEDIUM-B cycle-2 horse-level usable-odds coverage gate（--synthetic 外す実行時）:
    - _assert_jodds_coverage_horse_level(readonly_cur, bt, policy): race-level でなく candidate-horse usable-odds coverage を測定する。BT窓 test 予測馬集合（race_key + umaban）に対し・select_odds_snapshot 後に no_bet sentinel（odds_missing_reason in {'no_bet','no_bet_empty','special_value','fukusyoflag_not_normal_sale'}）を **除いた** 実利用可能オッズ馬の割合を計算する。usable_coverage = (usable_odds_horses) / (total_candidate_horses)。usable_coverage < JODDS_HORSE_COVERAGE_THRESHOLD（既定 0.90）の場合 RuntimeError("Horse-level usable-odds coverage {cov:.2%} < threshold for {bt.name}/{policy}: 多数馬が no_bet/special-odds・取得未完か発売異常の可能性・--synthetic を除く実行は不可") で loud fail。race-level coverage（JODDS snapshot 存在レース割合）は secondary check として併用し・race-level coverage<0.95 の場合は race-level でも RuntimeError（元 MEDIUM-05 gate）。--synthetic の場合は本 gate を skip（合成データはカバレッジ前提）。
    - reports/05-backtest.md に coverage サマリ（BT窓 × policy 別・race-level と horse-level の両方）を記載し・閾値未満の窓があれば warning セクションで明示。

    --check-reproduce: BT窓でも seed=42 + thread count=1 + FIXED_REPRODUCE_TS で bit-identical 検証（orchestrator._assert_deterministic 拡張経由）

    reports 生成（src/ev/report.py::generate_report(all_backtests) + scripts/run_backtest.py から呼出）:
    - all_backtests = 全25件の metrics 辞書リスト
    - reports/05-backtest.md: # Phase 5 Backtest Report (BACK-01..04 / §15.5 / §19.1)・フル行列テーブル（backtest_id/bt_name/odds_policy/model_type/recovery_rate/P/L/max_DD/selected/effective_bet/refund/hit_rate）・§11.2 odds policy 固定履行確認セクション（事前登録/変更なし/no_bet sentinel/winner 単独報告なし）・注記（BL-3 §14.2 caveat・主モデル確定 Phase 6・実JODDS取得状況・HIGH-1/2 馬単位 JOIN 検証済・HIGH-5 BT窓 category map refit 検証済・MEDIUM-05 JODDS coverage gate 結果）
    - reports/05-backtest.json: comparison_table(list) + metrics(dict) + constants + notes（4セクション・04-eval.json パターン）
    - winner 単独報告禁止（BACK-04・RESEARCH §10.2）：全25候補を backtest_id 順で並べるのみ・highest-recovery backtest_id を採用候補として突出させる記述は一切書かない・主モデル確定は Phase 6 D-03/D-04 事前登録基準

    src/ev/report.py:
    - generate_report(all_backtests, *, output_dir='reports') -> None: reports/05-backtest.{md,json} を生成
    - _format_comparison_table_md(rows) -> str: markdown テーブル（列整列・PK 文字列化・run_train_predict.py:516-523 パターン）
    - _format_notes(jodds_status) -> list[str]: BL-3 caveat / Phase 6 確定 / 実JODDS状況（合成データ版/取得完了版の判定明記）
    - REPORT_COLUMNS = ['backtest_id','bt_name','odds_policy','model_type','recovery_rate','P/L','max_DD','selected','effective_bet','refund','hit_rate']
  </action>
  <verify>
    <automated>cd /Users/hart/develop/keiba-ai-v3 && uv run python -c "import ast; ast.parse(open('scripts/run_backtest.py').read()); ast.parse(open('src/ev/report.py').read()); print('syntax OK')" && uv run python scripts/run_backtest.py --help 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - scripts/run_backtest.py が存在し ast.parse で構文 OK・python scripts/run_backtest.py --help が argparse help を表示
    - src/ev/report.py に "def generate_report" と "REPORT_COLUMNS" と "05-backtest" が含まれる
    - scripts/run_backtest.py に "BT_WINDOWS" と "split_periods" と "load_backtest" と "generate_report" と "FIXED_REPRODUCE_TS" が含まれる
    - フル行列 25 backtest（5窓 × 2policy × 2model + 5 BL-3）のループ構造が含まれる（grep "30min_before" と "10min_before" と "bl3" が含まれる）
    - **HIGH-2 馬単位 JOIN（cycle-3 整合）: pred_df/snapshot と label の merge は on=['race_key','umaban']（race_key 単独禁止・行数不変 assert で cartesian 重複を捕捉）。HARAI は race-level slot レコードのため例外的に on=['race_key'] + validate='many_to_one'（HIGH-C）。「全 merge の on= に umaban 必須」という一律ルールは HARAI には適用されない・race-level HARAI は馬行に一意ブロードキャスト後に _lookup_payfukusyo_pay で行ベースに払戻を確定する（acceptance grep は snapshot/label=umaban 含む / HARAI=many_to_one を両方許容）**
    - **HIGH-2 行数不変 assert: pred_df と snapshot の merge 後に len(pred_with_odds) == len(pred_df) の assert が存在（grep "len(" と "pred_with_odds" の近接・または row_count_invariant 系の assert）**
    - **HIGH-5 BT窓 category map refit: train_and_predict 呼出し前に fit_category_map(train_feature_df) を BT窓 train 行に対して呼出し・その map を渡している（grep "fit_category_map" が run_backtest.py に含まれる・または orchestrator.train_and_predict の category_map 引数に BT窓 train で fit した map を渡す記述）**
    - **MEDIUM-B cycle-2 horse-level coverage gate: run_backtest.py に _assert_jodds_coverage_horse_level または horse_level usable-odds coverage を測定する関数が含まれ・usable_coverage < threshold で RuntimeError（grep "usable" または "horse_level" と "coverage" が含まれる）**
    - **HIGH-B cycle-2 carve typo 修正: run_backtest.py の _carve_calib_from_train_tail または periods 構築で train_start を固定し train_end = calib_start - 1day に設定する（grep "train_end" と "calib_start" の近接・"train_start" は固定側）**
    - **HIGH-C cycle-2 HARAI race-level merge: run_backtest.py の HARAI merge 呼出しで validate='many_to_one' が含まれ・on=['race_key']（race-level・umaban を含めない）でブロードキャスト（grep "many_to_one" と "PayFukusyoUmaban" または "PayFukusyoPay" が含まれる）**
    - **MEDIUM-A cycle-2 full candidate table: run_backtest.py で load_backtest に selected_with_accounting でなく full_candidate_with_accounting または selected_flag True/False の全候補行を渡す（grep "selected_flag" または "full_candidate" が含まれる）**
    - select_odds_snapshot に渡す race_times が馬単位（race_key + umaban を含む）である
    - reports 生成で winner 強調表記が無い（特定 backtest_id を採用候補として突出させる語句が生成コードに含まれない・BACK-04）
    - backtest_strategy_version='fukusho_ev_v1' がハードコード（grep 含む）
    - LOW-05: 列存在検証は grep 否定でなく column presence assert で行う（REPORT_COLUMNS の全要素が report.py の出力に含まれることを pytest で assert・run_backtest.py 内の "fuku_odds_lower" リテラル grep 否定 gate は使わない）
  </acceptance_criteria>
  <done>scripts/run_backtest.py + src/ev/report.py 実装・BT窓再学習 + フル行列 25 backtest + reports/05-backtest 生成・--synthetic/--check-reproduce CLI・HIGH-1/2 馬単位 JOIN + 行数不変 assert・HIGH-5 BT窓 category map refit・MEDIUM-05 JODDS coverage gate・BACK-04 winner 報告禁止</done>
</task>

<task type="auto">
  <name>Task 2: 合成データ E2E smoke（run_backtest --synthetic で reports/05-backtest 生成）</name>
  <files>tests/ev/test_run_backtest_e2e.py</files>
  <read_first>
    - scripts/run_backtest.py（Plan 05 Task 1 で実装）
    - src/ev/report.py（同上）
    - tests/ev/conftest.py（make_jodds_mock/make_harai_mock/make_label_mock/make_prediction_mock）
    - tests/conftest.py（KEIBA_SKIP_DB_TESTS / write_cur / readonly_cur fixture）
    - tests/model/test_orchestrator.py（orchestrator smoke 形式）
  </read_first>
  <action>
    tests/ev/test_run_backtest_e2e.py:
    - test_synthetic_full_matrix_smoke: 合成データ（mock JODDS/label/HARAI/prediction・feature_df mock で split_3way が動作する最小行）で run_backtest --synthetic --bt-filter BT-1 を実行（BT窓1件に絞って高速化）→ reports/05-backtest.{md,json} が生成される・25件でなく BT-1 の 5件（2policy×2model + 1 BL-3）
    - test_report_all_candidates_format: 生成された reports/05-backtest.md の comparison_table に全 backtest_id（--bt-filter 絞り込み後）が含まれる
    - test_report_no_winner_override: reports/05-backtest.md に highest-recovery backtest_id を採用候補として突出させる語句が無い（BACK-04）
    - test_report_strategy_version_stamp: 全 backtest 行が backtest_strategy_version='fukusho_ev_v1'
    - test_report_bl3_caveat_present: notes に BL3_COMPARISON_CAVEAT 由来の §14.2 注記が含まれる
    - test_check_reproduce_smoke: --check-reproduce で BT窓1件の bit-identical 検証が通る（seed=42 + FIXED_REPRODUCE_TS）
    - **test_pred_snapshot_join_row_count_invariant（HIGH-2）: 合成データで各 BT-1 test レースが複数馬（例 race R1 に umaban 1/2/3・race R2 に umaban 1/2）を持ち・各馬に異なる JODDS snapshot がある場合・load_backtest に渡る selected_with_accounting の行数が入力予測馬数を超えないことを assert（cartesian duplication 検出・race_key 単独 JOIN だと行数が馬数²に膨らむ）**
    - **test_horse_level_odds_preserved_in_pipeline（HIGH-1）: 合成データで race R1 の umaban 1 (odds 3.0)/umaban 2 (odds 5.0) がパイプライン通過後も各馬の EV_lower が odds 3.0/5.0 に基づく計算であることを assert（一方の馬の odds で両者上書きされない）**
    - **test_bt_category_map_refit_excludes_test_ids（HIGH-5）: BT-1 train 期間に現れない jockey_id/horse_id が BT-1 test 期間に存在する合成データで・train_and_predict に渡る category_map が test 窓の新規 ID を __UNSEEN__ sentinel に mapping していることを assert（test 窓 ID が train category_map に直接追加されていない）**
    - **test_report_columns_present（LOW-05）: REPORT_COLUMNS の全要素が reports/05-backtest.json の comparison_table のキー（または md の列ヘッダ）に存在することを assert（grep 否定でなく presence 検証）**
    - **test_carve_calib_strict_ordering_and_deterministic（HIGH-B cycle-2）: _carve_calib_from_train_tail(bt) を BT-1..BT-5 全窓で呼出し・各窓で max(train.race_date) < min(calib.race_date) < max(calib.race_date) < min(test.race_date) の順序不変を assert。さらに同じ BT 窓に対し2回 carve して train_start/train_end/calib_start/calib_end/test_start/test_end が全て bit-identical であることを assert（deterministic・seed 非依存）**
    - **test_harai_payout_lookup_no_broadcast（HIGH-C cycle-2）: 合成データで race R1 に umaban {1,2,3}・HARAI race-level slot が PayFukusyoUmaban1=1/PayFukusyoPay1=210・PayFukusyoUmaban2=3/PayFukusyoPay2=150・slot3-5='00'/0（不的中）の場合・full_candidate_with_harai が race-level HARAI merge(validate='many_to_one') で構築され・行数 == 入力候補馬数（3行・ブロードキャスト膨張なし）・_lookup_payfukusyo_pay(row) が umaban1→210 / umaban3→150 / umaban2→0（不的中）を返すことを assert（race-level マージ + 行ベース slot lookup の正契約）**
    - **test_load_backtest_persists_nonselected（MEDIUM-A cycle-2 + MEDIUM cycle-3）: 合成データで select_bets 後 selected_flag=True が2頭・selected_flag=False が5頭（odds_missing_reason='no_bet' 等で埋め）の full_candidate_with_accounting を load_backtest に渡し・backtest.fukusho_backtest に全7行（selected + non-selected）が永続化されることを assert（selected のみでなく non-selected も odds_missing_reason 埋めで残る・MEDIUM-04 監査性）。加えて cycle-3: 永続化された selected_flag=False 行の stake/effective_stake/payout/refund/profit が全て 0 であることを assert（non-selected は購入していないので架空の会計が付与されない・全行集計での二重カウント防止）**
    - **test_horse_level_odds_coverage_gate（MEDIUM-B cycle-2）: 合成データで BT-1 test の候補10頭中8頭が usable odds（fuku_odds_lower/upper が数値）・2頭が no_bet sentinel の場合・usable_coverage=0.80 で閾値 0.90 を下回り RuntimeError が raise されることを assert（race-level では全レース JODDS 存在で gate を pass するが horse-level で no_bet 多数を検出する・silent 不正 backtest 防止）**

    合成データは conftest.py の mock fixtures を拡張・feature_df mock は race_date を BT-1 test 期間に含む最小行（train/calib/test が空にならないよう十分な行数）。
    本テストは実JODDS/実DB 不要（--synthetic）・KEIBA_SKIP_DB_TESTS 影響外。
  </action>
  <verify>
    <automated>uv run pytest tests/ev/test_run_backtest_e2e.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/ev/test_run_backtest_e2e.py が存在し uv run pytest で全件 GREEN
    - reports/05-backtest.md と reports/05-backtest.json が生成される（テスト実行後）
    - reports/05-backtest.md に「推奨:」記述が無い（BACK-04・test_report_no_winner_override で検証）
    - reports/05-backtest.md に backtest_strategy_version='fukusho_ev_v1' と BL3_COMPARISON_CAVEAT 注記が含まれる
    - test_pred_snapshot_join_row_count_invariant が GREEN（HIGH-2: 行数不変・cartesian 検出）
    - test_horse_level_odds_preserved_in_pipeline が GREEN（HIGH-1: 各馬 odds 保持）
    - test_bt_category_map_refit_excludes_test_ids が GREEN（HIGH-5: test 窓 ID が __UNSEEN__ に mapping）
    - test_report_columns_present が GREEN（LOW-05: REPORT_COLUMNS presence 検証）
    - **test_carve_calib_strict_ordering_and_deterministic が GREEN（HIGH-B cycle-2: BT-1..5 全窓で順序不変 + 同一窓2回 carve で bit-identical）**
    - **test_harai_payout_lookup_no_broadcast が GREEN（HIGH-C cycle-2: race-level HARAI merge validate='many_to_one' + 行ベース slot lookup で行数不変・正しい払戻）**
    - **test_load_backtest_persists_nonselected が GREEN（MEDIUM-A cycle-2: selected + non-selected 全候補行が backtest テーブルに永続化）**
    - **MEDIUM cycle-3 non-selected 会計ゼロ化: test_load_backtest_persists_nonselected で selected_flag=False 行の stake/effective_stake/payout/refund/profit が全て 0 であることも assert（架空会計の防止・grep "selected_flag" 近傍のゼロ化 loc[...]==0 記述が run_backtest.py に含まれる）**
    - **test_horse_level_odds_coverage_gate が GREEN（MEDIUM-B cycle-2: horse-level usable coverage < threshold で RuntimeError）**
  </acceptance_criteria>
  <done>合成データ E2E smoke GREEN・reports/05-backtest.{md,json} 生成・BACK-04 winner 報告禁止・BL-3 caveat 付与・HIGH-1/2/5 + LOW-05 検証テスト GREEN・**HIGH-B/HIGH-C/MEDIUM-A/MEDIUM-B cycle-2 検証テスト GREEN**</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run_backtest CLI → DB pools | readonly 読込 + etl 書込の二系統（dsn_masked ログ・生 DSN 禁止） |
| 全25候補 → reports/05-backtest | winner 強調禁止・全件提示（BACK-04 構造的ブロック） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-15 | Information Disclosure | reports/05-backtest で回収率最高の backtest_id を「推奨」と強調（後知恵・§11.2/BACK-04） | mitigate | 「推奨:」記述禁止・test_report_no_winner_override で grep 検証・主モデル確定は Phase 6 D-03/D-04 事前登録基準 |
| T-05-16 | Tampering | BT窓再学習で calib slice が train と重複（look-ahead leak） | mitigate | split_3way の完全時系列条件 guard（max(train)<min(calib)<max(calib)<min(test)）が raise ValueError で保護（Plan 04） |
| T-05-17 | Tampering | 生 DSN がログに漏洩 | mitigate | Settings().dsn_masked / etl_dsn_masked を使用・run_train_predict.py:218 パターン踏襲 |
| T-05-17b | Tampering | 予測(snapshot/label) の JOIN が race_key 単独で cartesian duplication を起こし EV/ROI を破壊（HIGH-2） | mitigate | snapshot/label の merge は on=['race_key','umaban'] に統一（HARAI は race-level slot のため on=['race_key']+validate='many_to_one' で例外・HIGH-C cycle-3 整合）・merge 後 len(pred_with_odds)==len(pred_df) の assert で loud fail・test_pred_snapshot_join_row_count_invariant で検証 |
| T-05-17c | Information Disclosure | BT窓再学習で全期間固定 category_map を再利用し test 窓 race_id の ID 漏洩（HIGH-5） | mitigate | 各 BT窓 train 期間のみで fit_category_map を呼出し・test 窓未観測 ID を __UNSEEN__ sentinel に mapping・test_bt_category_map_refit_excludes_test_ids で検証 |
| T-05-17d | Tampering | JODDS 取得未完で実行し大部分を no_bet 化して不正 backtest を出す（MEDIUM-05 / MEDIUM-B cycle-2） | mitigate | --synthetic 外す実行で _assert_jodds_coverage_horse_level が candidate-horse usable-odds coverage < threshold で RuntimeError・race-level coverage も secondary check・reports に race-level + horse-level 両カバレッジ明記 |
| T-05-19 | Tampering | _carve_calib_from_train_tail で train_start を calib_start-1day に短縮する typo（正しくは train_end）で train 期間が冒頭から短縮され calib と train の時系列順序が崩れ look-ahead leak（HIGH-B cycle-2） | mitigate | train_start 固定・train_end = calib_start - 1day に修正・test_carve_calib_strict_ordering_and_deterministic で BT-1..5 全窓の max(train)<min(calib)<max(calib)<min(test) 順序不変と deterministic（同窓2回 carve で bit-identical）を assert |
| T-05-20 | Tampering | HARAI 払戻を馬単位 JOIN（on=['race_key','umaban']）しようとして umaban 列不存在で silent broadcast または KeyError となり払戻金が破壊される（HIGH-C cycle-2） | mitigate | HARAI は race-level slot レコード（RESEARCH §2.1-§2.2）のため race-level merge(on=['race_key'], validate='many_to_one') でブロードキャスト・払戻は 05-03 _lookup_payfukusyo_pay(row) の行ベース slot lookup で確定・test_harai_payout_lookup_no_broadcast で行数不変と正しい払戻を assert |
| T-05-21 | Tampering | select_bets 後に selected のみを load_backtest に渡し non-selected 候補行が破棄され MEDIUM-04 監査性が失われる（MEDIUM-A cycle-2） | mitigate | full candidate table（selected + non-selected with odds_missing_reason）を構築し load_backtest に渡す・test_load_backtest_persists_nonselected で全候補行が永続化されることを assert |
| T-05-24 | Tampering | determine_stake_payout が selected_flag 分岐を持たないため non-selected（購入していない）行に stake=100/effective_stake=100/profit が付与され・永続化された監査行で全行集計すると投資額・損益が二重カウントされる（MEDIUM cycle-3） | mitigate | 永続化前に selected_flag=False 行の stake/effective_stake/payout/refund/profit を 0 にゼロ化（loc[...]==0）・test_load_backtest_persists_nonselected で non-selected 行の会計ゼロを assert・ROI 計算は selected_flag==True filter 済みで非影響 |
| T-05-SC | Tampering | 新規パッケージなし（既存スタックのみ） | accept | LightGBM/CatBoost/sklearn/pandas/psycopg3 のみ・Package Legitimacy Audit 表 RESEARCH で空・T-05-SC は新規 install なしで accept |
</threat_model>

<verification>
- uv run python scripts/run_backtest.py --help → argparse help 表示
- uv run pytest tests/ev/test_run_backtest_e2e.py -x -q → 全 GREEN
- uv run pytest -q → フル suite green（Phase 5 全 plan 完了後）
- grep -c '推奨:' scripts/run_backtest.py src/ev/report.py == 0（BACK-04）
</verification>

<success_criteria>
- scripts/run_backtest.py が BT窓再学習 + フル行列 25 backtest + reports 生成を実装
- src/ev/report.py が全候補一括報告（winner 強調禁止）
- 合成データ E2E smoke GREEN（実JODDS未完でも検証可能）
- HIGH-1+2 馬単位 JOIN + 行数不変 assert で cartesian duplication を構造的ブロック（test_pred_snapshot_join_row_count_invariant / test_horse_level_odds_preserved_in_pipeline GREEN）
- HIGH-5 BT窓 category map refit で test 窓 ID 漏洩防止（test_bt_category_map_refit_excludes_test_ids GREEN）
- MEDIUM-05 JODDS coverage gate で取得未完の不正実行を loud fail
- LOW-05 REPORT_COLUMNS presence 検証（grep 否定でなく）
- BACK-01/02/03/04 + D-03/D-04 を統合履行
</success_criteria>

<output>
Create `.planning/phases/05-ev-backtest/05-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces（本 plan の分）

- `scripts/run_backtest.py`（BT窓再学習 + フル行列 backtest CLI・--synthetic/--bt-filter/--check-reproduce）。**HIGH-1+2 馬単位 JOIN（on=['race_key','umaban']）+ 行数不変 assert**・**HIGH-5 BT窓 train のみで fit_category_map + 05-04 HIGH-A category_map plumbing 経由で伝播**・**HIGH-B cycle-2 `_carve_calib_from_train_tail(bt)`（train_end = calib_start-1day に短縮・train_start 固定）**・**HIGH-C cycle-2 HARAI race-level merge(on=['race_key'], validate='many_to_one') + 行ベース slot lookup**・**MEDIUM-A cycle-2 full_candidate_with_accounting 構築（selected + non-selected with odds_missing_reason）→ load_backtest**・**MEDIUM-B cycle-2 `_assert_jodds_coverage_horse_level` gate**（--synthetic 外す実行時・race-level secondary check 併用）
- `src/ev/report.py::generate_report(all_backtests, *, output_dir='reports')` / `_format_comparison_table_md` / `_format_notes` / `REPORT_COLUMNS`
- `reports/05-backtest.md` + `reports/05-backtest.json`（合成データ版・実データ版は Plan 06 checkpoint で生成）
- `tests/ev/test_run_backtest_e2e.py`（合成データ E2E smoke・HIGH-1/2/5 + LOW-05 検証テスト含む）
