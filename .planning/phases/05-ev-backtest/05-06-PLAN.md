---
phase: 05-ev-backtest
plan: 06
type: execute
wave: 5
depends_on: ["05-05"]
files_modified:
  - reports/05-backtest.md
  - reports/05-backtest.json
  - .planning/phases/05-ev-backtest/05-VERIFICATION.md
autonomous: false
requirements: [BACK-01, BACK-02, BACK-03, BACK-04]
user_setup: []

must_haves:
  truths:
    - "backtest スキーマ + テーブル + GRANT が live-DB に適用される（run_apply_schema.py 実行）"
    - "合成データ E2E でフル行列 smoke が完走・reports/05-backtest.{md,json} が生成される"
    - "BACK-04 構造的ブロック GREEN: winner 単独報告なし・全候補一括提示・odds policy 固定"
    - "実データ backtest（BT期間 2019-2025）は JODDS 取得完了後の manual-only 検証として明示分離（VALIDATION.md Manual-Only と整合）"
  artifacts:
    - path: "reports/05-backtest.md"
      provides: "Phase 5 backtest 報告（合成データ版・実データ版は取得完了後に差替）"
      contains: "Phase 5 Backtest Report"
  key_links:
    - from: "scripts/run_apply_schema.py"
      to: "src/db/schema.py::BACKTEST_TABLE_DDL"
      via: "APPLY_ORDER の backtest_table エントリ経由で live-DB に CREATE TABLE"
      pattern: "backtest.fukusho_backtest"
---

<objective>
backtest スキーマを live-DB に適用し・合成データ E2E で Phase 5 全体の構造的ブロック（BACK-01..04）が GREEN であることを実証する。実データ backtest（BT期間 2019-2025）は JODDS 取得進行中のため manual-only 検証として明示的に分離する（2段階実行計画・VALIDATION.md Manual-Only と整合）。
Purpose: Phase 5 自動化可能部分の完了宣言と、実データ backtest 実行の manual-only 移行ポイントを確立。
Output: live-DB backtest スキーマ適用確認 + reports/05-backtest.{md,json}（合成データ版）+ VERIFICATION.md 更新。
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ev-backtest/05-CONTEXT.md
@.planning/phases/05-ev-backtest/05-RESEARCH.md
@.planning/phases/05-ev-backtest/05-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: live-DB backtest スキーマ適用 + 合成データフル行列 smoke + VERIFICATION.md 更新</name>
  <files>reports/05-backtest.md, reports/05-backtest.json, .planning/phases/05-ev-backtest/05-VERIFICATION.md</files>
  <read_first>
    - scripts/run_apply_schema.py（apply()・APPLY_ORDER・GRANT 適用・Phase 4 で prediction_table 追加済み）
    - src/db/schema.py（BACKTEST_TABLE_DDL + APPLY_ORDER の backtest_table 挿入位置・Plan 04 で実装）
    - scripts/run_backtest.py（--synthetic/--bt-filter・Plan 05 で実装）
    - tests/ev/test_run_backtest_e2e.py（合成データ E2E・Plan 05 で実装）
    - .planning/phases/05-ev-backtest/05-VALIDATION.md（Manual-Only Verifications・Validation Sign-Off）
    - .planning/phases/05-ev-backtest/05-CONTEXT.md §domain（JODDS 取得進行中・2段階実行計画）
    - MEMORY/feature-snapshot-regen-required.md（live-DB 検証が実データでしか発覚しない bug がある点）
  </read_first>
  <action>
    Step 1 — live-DB backtest スキーマ適用（許可済み live-DB 操作・ユーザー委任・run-authorized-ops-directly）:
    - uv run python scripts/run_apply_schema.py を実行（APPLY_ORDER の backtest_table エントリが CREATE TABLE IF NOT EXISTS backtest.fukusho_backtest + GRANT を適用）
    - psql で \d backtest.fukusho_backtest を確認（列定義 + PK + CHECK 制約 model_type IN ('lightgbm','catboost','bl3') / backtest_strategy_version='fukusho_ev_v1'）
    - GRANT 確認: \dp backtest.fukusho_backtest で reader=SELECT / etl=全権

    Step 2 — 合成データフル行列 smoke（実JODDS未完でも検証）:
    - uv run python scripts/run_backtest.py --synthetic を実行（BT_WINDOWS 全5窓 × 2policy × 2model + 5 BL-3 = 25 backtest）
    - reports/05-backtest.{md,json} が生成されることを確認
    - reports/05-backtest.md に 25 backtest が backtest_id 順で一覧表示される（winner 強調なし・BACK-04）
    - reports/05-backtest.json の comparison_table が 25 エントリ・全て backtest_strategy_version='fukusho_ev_v1'
    - 合成データ E2E テスト（test_pred_snapshot_join_row_count_invariant / test_horse_level_odds_preserved_in_pipeline / test_bt_category_map_refit_excludes_test_ids）が GREEN であることを確認（HIGH-1/2/5 構造的ブロック・Plan 05 Task 2）

    Step 3 — VERIFICATION.md 更新（Manual-Only 実データ backtest の分離明記）:
    - .planning/phases/05-ev-backtest/05-VERIFICATION.md の frontmatter nyquist_compliant: true に更新
    - Manual-Only Verifications セクションに「実データ backtest 実行（BT期間 2019-2025）= JODDS 取得完了後・scripts/run_backtest.py（--synthetic 外す）で手動実行・MEDIUM-05 `_assert_jodds_coverage` gate が coverage<threshold で loud fail するため取得未完での誤実行を防止」を明記
    - Validation Sign-Off の各項目にチェック
  </action>
  <verify>
    <automated>uv run python scripts/run_apply_schema.py 2>&1 | tail -20 && uv run python scripts/run_backtest.py --synthetic 2>&1 | tail -30 && test -f reports/05-backtest.md && test -f reports/05-backtest.json && uv run pytest -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - backtest.fukusho_backtest テーブルが live-DB に存在（\d で確認・PK + CHECK 制約含む）
    - reader ロールが SELECT のみ・etl ロールが全権（\dp で確認）
    - reports/05-backtest.md と reports/05-backtest.json が生成される
    - reports/05-backtest.md の comparison_table に 25 backtest が一覧（winner 強調なし）
    - reports/05-backtest.json の全エントリが backtest_strategy_version='fukusho_ev_v1'
    - uv run pytest -q がフル suite green（Phase 5 全 plan 完了・KEIBA_SKIP_DB_TESTS 未設定で requires_db 含む全 test 実行）
    - 05-VERIFICATION.md の nyquist_compliant が true・Manual-Only に実データ backtest が明記
  </acceptance_criteria>
  <done>live-DB backtest スキーマ適用 + 合成データフル行列 smoke GREEN + reports/05-backtest 生成 + VERIFICATION.md 更新・Phase 5 自動化部分 完了</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 5 自動化部分 完了確認 + 実データ backtest の manual-only 分離合意</name>
  <action>
    Phase 5 自動化部分の完了を人間が確認する checkpoint。実データ backtest（BT期間 2019-2025）は JODDS 取得進行中のため本 checkpoint では実行せず manual-only 検証として分離する（2段階実行計画・VALIDATION.md Manual-Only と整合）。
  </action>
  <what-built>
    Phase 5 の自動化可能部分（コード実装・単体テスト・合成データ検証・staging-swap idempotent・orchestrator BT窓 periods 注入・live-DB backtest スキーマ適用・reports/05-backtest 合成データ版生成）が全て完了。フル suite green・BACK-01..04 構造的ブロック GREEN。実データ backtest（BT期間 2019-2025）は JODDS 取得進行中のため本 checkpoint では実行せず manual-only として分離。
  </what-built>
  <how-to-verify>
    1. スキーマ確認: psql で `\d backtest.fukusho_backtest` を実行 → 列定義 + PK (backtest_id, year, jyocd, kaiji, nichiji, racenum, umaban, kettonum) + CHECK 制約 (model_type IN ('lightgbm','catboost','bl3') / backtest_strategy_version='fukusho_ev_v1') が表示される
    2. GRANT 確認: `\dp backtest.fukusho_backtest` で keiba_readonly=SELECT / keiba_etl=arw が表示される
    3. フル suite: `uv run pytest -q` が green（KEIBA_SKIP_DB_TESTS 未設定・requires_db 含む全 test 実行）
    4. 合成データ reports: `cat reports/05-backtest.md` を確認 → 25 backtest が一覧・「推奨:」記述が無い（BACK-04）・BL3_COMPARISON_CAVEAT 注記が含まれる
    5. odds policy 固定履行: reports/05-backtest.md の「§11.2 odds policy 固定の履行確認」セクションが存在・全 backtest で odds_snapshot_policy が事前登録（30min_before/10min_before/confirmed）
    6. 実データ backtest の manual-only 分離確認: VERIFICATION.md の Manual-Only Verifications に「実データ backtest 実行 = JODDS 取得完了後・scripts/run_backtest.py（--synthetic 外す）・MEDIUM-05 JODDS coverage gate で取得未完を loud fail」が明記されている
    7. （将来・JODDS 取得完了後）実データ backtest を手動実行: `uv run python scripts/run_backtest.py`（--synthetic 外す・--snapshot-id=20260620-1a-postreview-v2）→ MEDIUM-05 `_assert_jodds_coverage` が BT test レース/policy 別 coverage ≥ 0.95 を確認後 reports/05-backtest.{md,json} を実データ版に差替
  </how-to-verify>
  <resume-signal>Type "approved" または課題を記述（実データ backtest は JODDS 取得完了後に別途手動実行・Phase 5 自動化部分は本 checkpoint で完了宣言）</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live-DB schema 適用 → 本番スキーマ | CREATE TABLE IF NOT EXISTS + GRANT（既存 prediction スキーマと対称・破壊的操作なし） |
| 合成データ reports → 実データ reports | 合成データ版と実データ版を明示区別（reports に判定明記・誤採用防止） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-18 | Tampering | 合成データ版 reports を実データ版と誤認（誤採用） | mitigate | reports/05-backtest.md に「合成データ版 / 取得完了版」判定を明記・VERIFICATION.md Manual-Only で実データ手動実行を明示 |
| T-05-19 | Repudiation | 実データ backtest を JODDS 未完で実行して不正結果を出す | mitigate | --synthetic 外す場合は JODDS 取得完了を前提・checkpoint で人間が確認 |
</threat_model>

<verification>
- uv run python scripts/run_apply_schema.py → backtest.fukusho_backtest 作成
- uv run python scripts/run_backtest.py --synthetic → reports/05-backtest.{md,json} 生成
- uv run pytest -q → フル suite green
- psql \d backtest.fukusho_backtest / \dp でスキーマ + GRANT 確認
- VERIFICATION.md nyquist_compliant=true + Manual-Only 実データ backtest 明記
</verification>

<success_criteria>
- live-DB backtest スキーマ適用確認（テーブル + GRANT）
- 合成データフル行列 smoke GREEN・reports/05-backtest 生成
- BACK-01..04 構造的ブロック GREEN（フル suite green + winner 報告禁止 + odds policy 固定）
- 実データ backtest を manual-only として明示分離（VALIDATION.md/VERIFICATION.md と整合）
- Phase 5 自動化部分 完了宣言・主モデル確定は Phase 6 に引き継ぎ
</success_criteria>

<output>
Create `.planning/phases/05-ev-backtest/05-06-SUMMARY.md` when done
</output>

## Artifacts this phase produces（本 plan の分）

- live-DB `backtest.fukusho_backtest` テーブル + GRANT（CREATE TABLE/GRANT 適用済み）
- `reports/05-backtest.md` + `reports/05-backtest.json`（合成データ版・実データ版は JODDS 取得完了後に手動差替）
- `.planning/phases/05-ev-backtest/05-VERIFICATION.md`（nyquist_compliant=true + Manual-Only 実データ backtest 明記）
