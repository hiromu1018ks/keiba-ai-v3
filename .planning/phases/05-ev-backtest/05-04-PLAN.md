---
phase: 05-ev-backtest
plan: 04
type: execute
wave: 2
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - src/config/settings.py
  - src/db/connection.py
  - src/db/schema.py
  - src/db/backtest_load.py
  - src/model/data.py
  - src/model/orchestrator.py
  - tests/db/test_backtest_load.py
  - tests/model/test_orchestrator_bt.py
autonomous: true
requirements: [BACK-03]
user_setup: []

must_haves:
  truths:
    - "BACK-03: backtest.fukusho_backtest テーブルが存在・backtest_id スコープ staging-swap idempotent load で永続化"
    - "A5 後方互換: split_3way(periods=None) は Phase 4 と bit-identical（既存 test_group_split/test_orchestrator green・SC#4 回帰防止）"
    - "D-03: train_and_predict(split_periods=...) で BT窓 train/calib/test 期間を注入可能"
    - "backtest_id scoped swap: 同一 backtest_id のみ DELETE→INSERT・他 backtest_id 行は保持"
  artifacts:
    - path: "src/db/schema.py"
      provides: "BACKTEST_TABLE_DDL + GRANT 追記 + APPLY_ORDER 挿入"
      contains: "BACKTEST_TABLE_DDL"
    - path: "src/db/backtest_load.py"
      provides: "load_backtest(write_cur, df) — backtest_id scoped staging-swap idempotent"
      contains: "def load_backtest"
    - path: "src/config/settings.py"
      provides: "db_schema_backtest フィールド"
      contains: "db_schema_backtest"
    - path: "src/model/data.py"
      provides: "split_3way に periods パラメータ追加（後方互換）"
      contains: "periods"
    - path: "src/model/orchestrator.py"
      provides: "train_and_predict に split_periods パラメータ追加（後方互換）"
      contains: "split_periods"
  key_links:
    - from: "src/db/backtest_load.py::load_backtest"
      to: "src/db/prediction_load.py::_idempotent_load_prediction"
      via: "11ステップ staging-swap パターン（advisory lock/TRUNCATE/INSERT/scope DELETE/checksum）を backtest_id スコープで踏襲"
      pattern: "pg_advisory_xact_lock"
    - from: "src/model/orchestrator.py::train_and_predict"
      to: "src/model/data.py::split_3way"
      via: "split_periods パラメータを split_3way(periods=split_periods) に伝播"
      pattern: "split_3way"
---

<objective>
backtest 結果永続化（DB DDL + staging-swap idempotent load）と BT窓再学習のための orchestrator/split_3way 拡張（後方互換）を実装する。
Purpose: BACK-03 永続化 + D-03 BT窓再学習ループの土台。Phase 4 回帰を防ぐ（A5 bit-identical）。
Output: schema.py BACKTEST_TABLE_DDL + GRANT、settings/connection 拡張、backtest_load.py、data.py/orchestrator.py の periods 拡張。
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ev-backtest/05-CONTEXT.md
@.planning/phases/05-ev-backtest/05-RESEARCH.md
@.planning/phases/05-ev-backtest/05-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: split_3way periods 拡張 + orchestrator split_periods 拡張（後方互換・A5）— RED→GREEN</name>
  <files>src/model/data.py, src/model/orchestrator.py, tests/model/test_orchestrator_bt.py</files>
  <read_first>
    - src/model/data.py:502-575（split_3way 既存: 固定暦年 mask train 2016-07〜2023 / calib 2024-01〜06 / test 2024-07〜12 + 完全時系列条件 guard raise ValueError 行540-552 + 正準 race_key pairwise disjoint guard 行554-568）
    - src/model/orchestrator.py:154-452（train_and_predict 既存シグネチャ + 行246 の split_3way(feature_df) 呼出 + 行615-666 の _assert_deterministic）
    - .planning/phases/05-ev-backtest/05-RESEARCH.md §6.1-§6.5（split_3way periods 拡張・後方互換・category_map BT窓再 fit・bit-identical SC#4）+ Pitfall 3（ハードコードで BT窓表現不可）
    - .planning/phases/05-ev-backtest/05-PATTERNS.md「src/model/data.py::split_3way」「src/model/orchestrator.py」節
    - tests/model/test_orchestrator_bt.py（Plan 01 RED stub・test_split_3way_periods_injection / test_split_3way_backward_compat）
    - tests/model/test_orchestrator.py（既存 bit-identical 検証・SC#4）
  </read_first>
  <behavior>
    - test_split_3way_periods_injection: periods={'train':('2019-06-01','2022-12-31'),'calib':('2022-07-01','2022-12-31'),'test':('2023-01-01','2023-12-31')} 指定 → 各 split が BT窓で分割・完全時系列条件 guard + race_key disjoint guard 継承
    - test_split_3way_backward_compat: periods=None（既定）→ 既存ハードコード（train 2016-07〜2023 / calib 2024-01〜06 / test 2024-07〜12）と同一の行選択
    - test_split_3way_periods_guard_raises: periods で train_max >= calib_min 等の違反 → ValueError（既存 guard 継承）
    - test_train_and_predict_split_periods: train_and_predict(split_periods=periods) が split_3way(periods=split_periods) に伝播
    - test_train_and_predict_backward_compat: split_periods=None（既定）→ Phase 4 と同一挙動（既存 test_orchestrator.py が green を維持）
  </behavior>
  <action>
    src/model/data.py::split_3way:
    - シグネチャに periods: dict[str, tuple[str, str]] | None = None を追加（キーワード専用・末尾）
    - periods is None の場合: 既存ハードコード dict を periods に設定（後方互換・A5・Phase 4 回帰防止）
    - periods 指定時: periods['train'/'calib'/'test'] で frame.between(*periods[key]) を使用
    - 既存の完全時系列条件 guard（行540-552 raise ValueError）+ race_key pairwise disjoint guard（行554-568）はそのまま継承（BT窓でも同一保証）
    - holdout 行（frame >= test_max + 1 day）は BT窓では空可・既存ロジック維持

    src/model/orchestrator.py::train_and_predict:
    - シグネチャに split_periods: dict[str, tuple[str, str]] | None = None を追加（既存パラメータ全て保持・末尾）
    - 行246 の split_3way(feature_df) を split_3way(feature_df, periods=split_periods) に変更（1行）
    - 既定値 None で Phase 4 挙動維持（A5）
    - _assert_deterministic も split_periods を受け取るよう拡張（BT窓でも bit-identical・seed=42 + 固定 thread + FIXED_REPRODUCE_TS）

    tests/model/test_orchestrator_bt.py: Plan 01 RED stub を GREEN 実装で満たす。
  </action>
  <verify>
    <automated>uv run pytest tests/model/test_orchestrator_bt.py tests/model/test_orchestrator.py tests/utils/test_group_split.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/model/data.py の split_3way シグネチャに "periods: dict[str, tuple[str, str]] | None = None" が含まれる
    - periods is None で既存ハードコード dict を使用（grep "2016-07-01" が既存位置に残る）
    - src/model/orchestrator.py の train_and_predict シグネチャに "split_periods" が含まれ・split_3way(feature_df, periods=split_periods) に伝播
    - 既存 test_orchestrator.py / test_group_split.py（既存分）が全て green（Phase 4 回帰防止・A5）
    - uv run pytest tests/model/test_orchestrator_bt.py が全件 GREEN
  </acceptance_criteria>
  <done>D-03 BT窓再学習ループの土台（split_3way/orchestrator periods 拡張）実装・後方互換（A5）・Phase 4 回帰なし</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: backtest 永続化（schema/settings/connection/backtest_load）— RED→GREEN</name>
  <files>src/config/settings.py, src/db/connection.py, src/db/schema.py, src/db/backtest_load.py, tests/db/test_backtest_load.py</files>
  <read_first>
    - src/config/settings.py:45（db_schema_prediction 既存・db_schema_backtest 追加位置）
    - src/db/connection.py:42-50（etl search_path 既存・db_schema_prediction の後に db_schema_backtest 追加）
    - src/db/schema.py:26（SCHEMAS 既存・"backtest" 含む CREATE SCHEMA のみ）/ 行61-96（PREDICTION_TABLE_DDL 構造）/ 行128-148（GRANT_READER_SQL prediction 3行セット）/ 行166-171（GRANT_ETL_SQL prediction 3行セット）/ 行205-216（APPLY_ORDER prediction_table 挿入位置）
    - src/db/prediction_load.py:174-348（_idempotent_load_prediction 11ステップ・model_version スコープ）/ 行108-166（_df_to_prediction_tuples）/ 行356-394（load_predictions wrapper）/ 行96-105（_is_na ヘルパ）
    - .planning/phases/05-ev-backtest/05-RESEARCH.md §7.1-§7.5（§16.2 schema 参考 + settings/connection/schema 拡張 + backtest_load + backtest_id スコープ + Pitfall A5/APPLY_ORDER 挿入位置）
    - .planning/phases/05-ev-backtest/05-PATTERNS.md「src/db/backtest_load.py」節 + 共有パターン1（11ステップ staging-swap）
    - tests/db/test_backtest_load.py（Plan 01 RED stub・requires_db + write_cur fixture）
    - tests/model/test_prediction_load.py（integration test analog・idempotent checksum / scoped swap）
  </read_first>
  <behavior>
    - test_backtest_schema_apply: scripts/run_apply_schema.py で backtest.fukusho_backtest テーブル + GRANT が作成される（requires_db）
    - test_backtest_load_idempotent: load_backtest を2回連続実行 → checksum bit-identical（requires_db）
    - test_backtest_load_scoped_swap: backtest_id='BT-1-30min_before-lightgbm' 書込後 'BT-1-10min_before-lightgbm' 書込 → 両方残る（他 backtest_id 行は保持・requires_db）
    - test_backtest_load_columns_contract: BACKTEST_COLUMNS が schema DDL 列順と 1:1
    - test_backtest_load_empty_raises: 空入力 → RuntimeError（CR-04(a) silent data loss 防止）
  </behavior>
  <action>
    src/config/settings.py: db_schema_backtest: str = "backtest" を db_schema_prediction の直後に追加。
    src/db/connection.py: etl search_path に f"{settings.db_schema_backtest}," を db_schema_prediction と db_schema_normalized の間に追加（1行）。
    src/db/schema.py:
    - BACKTEST_TABLE_DDL: CREATE TABLE IF NOT EXISTS backtest.fukusho_backtest。列は RESEARCH §7.3（provenance NOT NULL: backtest_id/backtest_strategy_version/odds_snapshot_policy/train_period_start/end/test_period_start/end/model_type/model_version/feature_snapshot_id + PK RACE_KEY 7 + 選択会計 selected_flag/stake/refund_flag/refund_amount/payout_amount/profit/effective_stake + 的中rankEV fukusho_hit_validated/recommend_rank/EV_lower/EV_upper + odds provenance odds_snapshot_at/odds_source_type/odds_missing_reason + race_date）。PRIMARY KEY (backtest_id, year, jyocd, kaiji, nichiji, racenum, umaban, kettonum)。CHECK 制約 model_type IN ('lightgbm','catboost','bl3') + backtest_strategy_version = 'fukusho_ev_v1'。
    - GRANT_READER_SQL: prediction 3行セットの直後に backtest 用3行セット（GRANT USAGE ON SCHEMA / GRANT SELECT ON ALL TABLES / ALTER DEFAULT PRIVILEGES GRANT SELECT）を {reader} 向けに追記。
    - GRANT_ETL_SQL: prediction 3行セットの直後に backtest 用3行セット（GRANT USAGE, CREATE / GRANT SELECT,INSERT,UPDATE,DELETE,TRUNCATE / ALTER DEFAULT PRIVILEGES）を {etl} 向けに追記。
    - APPLY_ORDER: ("backtest_table", BACKTEST_TABLE_DDL) を ("prediction_table", ...) の直後・("grant_reader", ...) の直前に挿入（CREATE SCHEMA で backtest スキーマ既存・GRANT が本テーブルを拾えるように・Pitfall APPLY_ORDER 挿入位置）。

    src/db/backtest_load.py:
    - BACKTEST_COLUMNS（schema DDL 列順と 1:1・predict.py::PREDICTION_COLUMNS と同一規約）
    - _is_na(v) ヘルパ（prediction_load.py:96-105 を複製）
    - _df_to_backtest_tuples(df) -> list[tuple]（列順整列 + date/datetime/bool/int/float/str 型変換・prediction_load.py:108-166 パターン）
    - _idempotent_load_backtest(write_cur, rows, *, reader_role) -> str: prediction_load.py の11ステップ構造を backtest_id スコープで踏襲
      - Step 0: pg_advisory_xact_lock(hashtext('backtest.fukusho_backtest'))
      - Step 2: backtest_id 単一性 assert（rows 全て同一 backtest_id）
      - Step 3: CREATE TABLE IF NOT EXISTS backtest.fukusho_backtest_staging (LIKE backtest.fukusho_backtest INCLUDING ALL)
      - Step 7: DELETE FROM backtest.fukusho_backtest WHERE backtest_id=%s（scope・他 backtest_id 行は保持）
      - Step 8: INSERT INTO 本テーブル SELECT 明示的列リスト FROM staging（wild-card 禁止）
      - Step 10: md5(string_agg(...)) checksum（ORDER BY backtest PK・WHERE backtest_id scope）
    - load_backtest(write_cur, backtest_df, *, reader_role=None) -> str: 薄い wrapper（prediction_load.py:356-394 パターン）

    tests/db/test_backtest_load.py: Plan 01 RED stub を GREEN 実装で満たす。@pytest.mark.requires_db + write_cur fixture（tests/conftest.py 既存）。KEIBA_SKIP_DB_TESTS skip policy 継承。
  </action>
  <verify>
    <automated>uv run pytest tests/db/test_backtest_load.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/config/settings.py に "db_schema_backtest: str = \"backtest\"" が含まれる
    - src/db/connection.py の etl search_path に "db_schema_backtest" が含まれる
    - src/db/schema.py に "BACKTEST_TABLE_DDL" と "backtest.fukusho_backtest" と APPLY_ORDER の "backtest_table" エントリが含まれる
    - BACKTEST_TABLE_DDL の model_type CHECK 制約に 'bl3' が含まれる（BL-3 用・D-04）
    - src/db/backtest_load.py に "def load_backtest" と "pg_advisory_xact_lock" と "DELETE FROM" と backtest_id scope が含まれる
    - uv run pytest tests/db/test_backtest_load.py が全件 GREEN（requires_db・KEIBA_SKIP_DB_TESTS 未設定で実行）
    - 既存 test_prediction_load.py / schema 関連 test が green（回帰防止）
  </acceptance_criteria>
  <done>BACK-03 永続化実装・backtest_id scoped staging-swap idempotent・schema/settings/connection 拡張・test_backtest_load.py 全 GREEN</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| etl ロール → backtest スキーマ書込 | GRANT 拡張で USAGE+CREATE + SELECT/INSERT/UPDATE/DELETE/TRUNCATE（readonly は SELECT のみ） |
| split_3way periods → guard | BT窓 periods でも完全時系列条件 + race_key disjoint guard が raise ValueError で保護 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Tampering | split_3way 拡張で Phase 4 ハードコードが壊れる（A5 違反・SC#4 回帰） | mitigate | periods=None 既定で既存 dict 使用・test_split_3way_backward_compat で既存挙動 assert・既存 test_orchestrator.py/test_group_split.py green 維持 |
| T-05-12 | Tampering | backtest_id scoped swap で他 backtest_id 行が消える（silent 履歴破壊） | mitigate | DELETE WHERE backtest_id=%s scope・test_backtest_load_scoped_swap で他 backtest_id 残存を assert（共有パターン1） |
| T-05-13 | Tampering | BACKTEST_TABLE_DDL の model_type CHECK に 'bl3' が無いと BL-3 永続化失敗 | mitigate | CHECK 制約に 'lightgbm','catboost','bl3' を明記・test で検証 |
| T-05-14 | Elevation of Privilege | readonly ロールが backtest 書込可能になる | mitigate | GRANT_READER_SQL は SELECT のみ・GRANT_ETL_SQL のみ書込権・既存 REVOKE_RAW_WRITES 原則維持 |
</threat_model>

<verification>
- uv run pytest tests/model/test_orchestrator_bt.py tests/db/test_backtest_load.py -x -q → 全 GREEN
- uv run pytest tests/model/test_orchestrator.py tests/model/test_prediction_load.py tests/utils/test_group_split.py -q → 既存 green 維持（回帰防止）
- uv run python scripts/run_apply_schema.py（要 live-DB・checkpoint で実行）→ backtest.fukusho_backtest 作成確認
</verification>

<success_criteria>
- D-03 BT窓再学習ループ土台（split_3way/orchestrator periods 拡張・後方互換 A5）
- BACK-03 永続化（backtest.fukusho_backtest DDL + backtest_id scoped staging-swap idempotent load）
- Phase 4 回帰なし（既存 test green 維持）
</success_criteria>

<output>
Create `.planning/phases/05-ev-backtest/05-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces（本 plan の分）

- `src/config/settings.py::db_schema_backtest: str = "backtest"`（新規フィールド）
- `src/db/connection.py` etl search_path に `db_schema_backtest` 追加
- `src/db/schema.py::BACKTEST_TABLE_DDL` + GRANT_READER/GRANT_ETL backtest スキーマ分 + APPLY_ORDER に `("backtest_table", BACKTEST_TABLE_DDL)`
- `src/db/backtest_load.py::load_backtest(write_cur, df, *, reader_role=None)` / `_idempotent_load_backtest` / `_df_to_backtest_tuples` / `BACKTEST_COLUMNS`
- `src/model/data.py::split_3way(frame, *, periods=None)`（periods パラメータ追加・後方互換）
- `src/model/orchestrator.py::train_and_predict(..., split_periods=None)`（split_periods パラメータ追加・後方互換）
