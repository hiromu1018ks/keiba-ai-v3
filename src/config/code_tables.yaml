# Keiba AI v3 — 補助コード表（実データ SELECT DISTINCT で裏取り・Pitfall 3: CODE.md 不完全）
#
# 由来: PostgreSQL everydb2 に接続し SELECT DISTINCT で確認した値のみ記載。
# docs/everydb2/CODE.md はスクレイピング不完全（D-10）のため参照元にしない。

# ---------------------------------------------------------------------------
# jyocd（場コード）— JRA 東西10場のみ。地方競馬（jyocd>=30）は対象外（Pitfall 2）
# 実測: everydb2.public.n_race で jyocd IN ('01'..'10') が JRA。30以上は NAR/海外
# ---------------------------------------------------------------------------
jyocd:
  "01": "札幌"
  "02": "函館"
  "03": "福島"
  "04": "新潟"
  "05": "東京"
  "06": "中山"
  "07": "中京"
  "08": "阪神"
  "09": "京都"
  "10": "小倉"

# ---------------------------------------------------------------------------
# syubetucd（競走種別コード）— §7.3 で障害（18/19）はモデル対象外
# 実測: 平地と障害でコード分離。class_normalize 側で syubetucd IN ('18','19') を
# 交差確認に使用（gradecd='D' が障害G3かの判定・A1）
# ---------------------------------------------------------------------------
syubetucd:
  "00": " flat（通常・平地）"
  "11": "2歳新馬（平地）"
  "12": "3歳新馬（平地）"
  "13": "2歳未勝利（平地）"
  "14": "3歳未勝利（平地）"
  "15": "条件戦（平地）"
  "16": "OP・重賞（平地）"
  "17": "特別指定競走"
  "18": "障害（飛越）— §7.3 モデル除外対象"
  "19": "障害（障害OP・J・G1/2/3）— §7.3 モデル除外対象"
  note: |
    実測は plan 01-03 の ETL で SELECT DISTINCT syubetucd, count(*) を実行して
    完全網羅する。上記は docs/everydb2/03-RACE.md SyubetuCD の主要値。
    grade_numeric='D' と syubetucd IN ('18','19') の交差確認を推奨（A1）。
